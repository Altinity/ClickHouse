<clickhouse>
    <merge_tree>
        <storage_policy>azure_cache</storage_policy>
    </merge_tree>
    <!-- Temp spills stay on the local disk. Sharing cached_azure with
         MergeTree cache-on-write fills the cache with non-releasable
         holds and later tests hit NOT_ENOUGH_SPACE. -->
</clickhouse>
