<clickhouse>
    <token_processors>
        <!-- Validate JWT via an explicit JWKS URI -->
        <keycloak_jwks>
            <type>jwt_dynamic_jwks</type>
            <jwks_uri>http://keycloak:8080/realms/clickhouse-test/protocol/openid-connect/certs</jwks_uri>
            <expected_issuer>http://keycloak:8080/realms/clickhouse-test</expected_issuer>
            <username_claim>preferred_username</username_claim>
            <verifier_leeway>60</verifier_leeway>
        </keycloak_jwks>

        <!-- Manual trust-chain mode: OIDC discovery now rejects non-HTTPS URLs
             from the discovery document, and the test Keycloak speaks HTTP. -->
        <keycloak_discovery>
            <type>openid</type>
            <userinfo_endpoint>http://keycloak:8080/realms/clickhouse-test/protocol/openid-connect/userinfo</userinfo_endpoint>
            <jwks_uri>http://keycloak:8080/realms/clickhouse-test/protocol/openid-connect/certs</jwks_uri>
            <expected_issuer>http://keycloak:8080/realms/clickhouse-test</expected_issuer>
            <username_claim>preferred_username</username_claim>
            <verifier_leeway>60</verifier_leeway>
        </keycloak_discovery>
    </token_processors>
</clickhouse>
