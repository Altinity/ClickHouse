# CAS wire-keys phase-1 prose sweep report

1. Internal references: `gtest_cas_ref_catalog.cpp` removed `[codex r2/r3 finding 9]`; `CasRefCatalogFormat.h` removed `(spec §3)`; six test helpers changed `Retired-in-snapshot (T4)` to the snapshot-seal reason; `gtest_cas_encoding_pins.cpp` changed the task reference to the line-scratch rendering property; `CasInspect.cpp` removed `(spec INV-4)`. Verification: the retained helper text describes `RunMarker::Condemned` rows read by `anyCondemnedInSeal` at each helper's return line; no factual behavior was changed.
2. `CasRefWireVocab.h`, `CasWireVocab.h`: `CORRUPTED_DATA` → `LOGICAL_ERROR` for out-of-range writer enum values. Verification: all three delegates call `EnumWireTable::toWord` (`CasRefWireVocab.cpp:31`, `CasWireVocab.cpp:25,40`), whose invalid-value branch throws `LOGICAL_ERROR` (`CasEnumWireTable.h:46-48`).
3. `CasBlobMetaFormat.h`: added the `metaStateToWireWord` contract. Verification: its delegate calls `kMetaStateWords.toWord` (`CasBlobMetaFormat.cpp:39`), using the same `LOGICAL_ERROR` branch (`CasEnumWireTable.h:46-48`).
4. `CasWireVocab.h`: changed the match-helper rationale from a guaranteed call-boundary claim to header-defined, per-key inlining intent. Verification: the helpers remain defined inline immediately below the comment (`CasWireVocab.h:163`).
5. `CasPoolMetaFormat.cpp`: narrowed the validation-fence rationale to raw-byte-vector validation and clean rejection. Verification: `validatePoolAlgosUsed` accepts `std::vector<uint8_t>` and directly checks membership (`CasPoolMetaFormat.cpp:58-76`).
6. `Pool/CasPoolMeta.cpp`: corrected `blobHashAlgoName` to programming-error semantics, not input validation. Verification: it delegates to `toWord` (`CasBlobDigest.cpp:9-12`), whose invalid-value branch is `LOGICAL_ERROR` (`CasEnumWireTable.h:46-48`).
7. `CasBlobDigest.h`: removed the false shared-contract statement and stated the checked divergence: `blobHashLenFor` uses `BAD_ARGUMENTS`, `blobHashAlgoName` uses `LOGICAL_ERROR`. Verification: `blobHashLenFor` throws `BAD_ARGUMENTS` (`CasBlobDigest.cpp:16-24`); `blobHashAlgoName` calls `toWord` (`CasBlobDigest.cpp:9-12`), which throws `LOGICAL_ERROR` (`CasEnumWireTable.h:46-48`).
8. `Gc/CasGc.cpp`: corrected the decoder comment to `blobHashAlgoFromWord` → `CORRUPTED_DATA`. Verification: the decoder calls it at `CasWireVocab.cpp:101`; it delegates to `fromWord` (`CasWireVocab.cpp:33-36`), whose unknown-word branch throws `CORRUPTED_DATA` (`CasEnumWireTable.h:57-63`).
9. `gtest_cas_enum_wire_table.cpp`: replaced the stale "two cases above" referent with `dup_value` and `invalid_value`. Verification: those tables and their negative asserts are at lines 102-108; both fail `denseAndOrdered` because the duplicate/nonconsecutive values violate its arithmetic check (`CasEnumWireTable.h:35-41`).
10. Deleted the Inbox line for the removed `task-2-report.md`; no source file exists to edit.
11. `CasTextFormat.h`: scoped `WireKey` to migrated writer sites and added the borrowed-`string_view` lifetime rule. Verification: `WireKey` stores `std::string_view text` (`CasTextFormat.h:150`) and its constructor copies that view (`CasTextFormat.h:152`).
12. `CasEnumWireTableAsserts.h`: added the `magic_enum` reflectable-range premise. Verification: the proof obtains declared values solely from `magic_enum::enum_values<Enum>` (`CasEnumWireTableAsserts.h:23`).

BACKLOG bookkeeping: removed the eleven requested Inbox entries, including both duplicate toWord entries; left all requested KEEP items and the 2026-08-04 batches untouched.

Verification gates: `ninja -C build unit_tests_dbms` linked successfully; `unit_tests_dbms --gtest_filter='CAS*'` passed 2212 tests across 284 suites, including all 11 `CASRefWriterChunkedFlush` tests.
