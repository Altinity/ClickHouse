name: Scheduled Altinity Stable Builds

on:
  schedule:
    - cron: "0 0 * * 4" # Change to "0 0 * * 6" (Saturday midnight) once tested
  # Make sure that any changes to this file is actually tested with PRs
  pull_request:
    types:
      - synchronize
      - reopened
      - opened
    paths:
      - '**/scheduled_runs.yml'

jobs:
  v24_3_5_47_altinitystable:
    uses: Altinity/ClickHouse/.github/workflows/release_branches.yml@24.3-scheduled-run
    secrets: inherit
  v24_3_5_48_altinityfips:
    uses: Altinity/ClickHouse/.github/workflows/release_branches.yml@24.3-fips-scheduled-run
    secrets: inherit
