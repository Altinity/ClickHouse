# Unattended Night Log — 2026-06-05 → 06-06

**Branch:** `cas-mergetree-poc`. **Mandate:** work unattended ~12h, no questions/stops, subagents+codex allowed. Foreground builds (→ log, subagent-summarize), bounded foreground tests, never commit to `master`, no rebase/amend, trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## Goals (verbatim intent)
1. **Phase A — finish the projection-overlay refactor.** Replace the CA-only `registerCarriedForwardProjectionForCA` workaround with a directory-level read-your-writes overlay (mirror the `existsFile` B59 prelude on `existsDirectory`), so `loadProjections` finds the staged projection during finalize the normal way; then delete the workaround (+ its B63 rows_count/index back-fill). brainstorm→spec→plan→implement (subagent-driven).
2. **Phase B — 3 rounds of simplification/clarification.** `/simplify` + YAGNI/Occam reviews + **architecture/file-organization review**. User pain point: *"things are organized in files [so it] fragments important things — some connected/related entities sit far from each other, and some unrelated are close."* Prefer expressive code over comment-walls. Reorganize files so related entities are co-located; each reorg must build + keep tests green.
3. **Phase C — full test suite** on **CA on local disk** AND **CAS on S3 (minio)**. Investigate/establish the minio path.
4. Track work + results here; push deferred items to `deferred_backlog/cas-mergetree-integration.md`.

## Design decisions made unattended (no user to consult)
- Acting as the design decision-maker per the unattended mandate (can't run interactive brainstorming). Decisions recorded in the specs + here.

## Status timeline
- (start) Scaffolding created. Beginning Phase A spec.
- **Phase A DONE + reviewed (APPROVED).** Commits `ba5038840f3` (spec) → `51192075c3f` (hasInFlightDirectory) → `f528a7f2b7e` (existsDirectory prelude) → `998c67f0b46` (delete workaround + overlay extensions). `registerCarriedForwardProjectionForCA` + its B63 back-fill DELETED.
  - **Deeper than spec (root cause):** the real bug was `DiskObjectStorageTransaction::createHardLink` QUEUED the op into `operations_to_execute` and only staged at commit-replay (AFTER `loadProjections`), so `recorded` was empty at probe time. Fix: CA `createHardLink` now stages EAGERLY (mirroring CA `writeFile`); non-CA keeps the queued lambda. Read overlays added at 4 layers (`hasInFlightDirectory`+`listInFlightDirectory` on `IDiskTransaction`/`IMetadataStorage`/`DiskObjectStorageTransaction`/`ContentAddressedTransaction`) + `DataPartStorageOnDiskFull::existsDirectory`/`exists`/`iterate` consult the open txn (all `transaction &&`-guarded → non-CA byte-identical).
  - **Review verdict:** eager-hardlink SAFE (no double-exec — CA op is eager-only, never queued; commit publishes from staging identically; FREEZE atomic; rollback clean). 158 `ContentAddressed*` gtests green. `05000_content_addressed_projection_carry_forward` proves the carried projection is queryable + non-empty `projection_parts` in the SAME session (no reload). Non-CA regression (`01710_projection_mutation`, `01710_projection_materialize_with_missing_columns`) unchanged.
  - **`04295_content_addressed_mutation_no_leftovers` FAILS** — confirmed PRE-EXISTING + out of scope (no-projection GC-leftovers oracle, test+ref byte-unchanged in range, tombstone-only leftovers, run-to-run variance 140/126 = flaky GC-reaper race vs 60s poll). → backlog.
  - 3 non-blocking follow-ups → backlog (B79 merged-iterator isFile staged inaccuracy; B80 04295 tombstone-leftover flakiness; B81 CA tryCommit-retry-loses-staging).
- **Phase B round 1 DONE + reviewed (APPROVED).** `258ab719c3cf` — simplified the GC cluster (GcLogWriter/GcDelta/GcCompaction/ContentAddressedGC), net −79 lines. Extractions: `flushShardEpochs` (dedup flushAll/flushDueWindows), `foldDeltas` (dedup compact/rebuild, single cross-epoch dedup scope preserved), `candidateObjectKey`, `parseEpochShard`, `tryReadSession`, `computeReachability` (called FRESH at both sweep sites — §6.2 re-check preserved, verified). `enqueue`→`void` (all call sites discarded). Pruned redundant "what" comments, kept invariant "why". 158 gtests green.
- **Phase B round 2 (architecture reorg)** — read-only architecture review produced a ranked reorg plan. Key enabler: the CA dir is CMake-globbed (`CONFIGURE_DEPENDS`) → new files auto-compile, no CMake edits. Executing Moves 1–4 (each behavior-preserving, test-gated):
  - **Move 1:** extract `RefPayload.{h,cpp}` from `ContentAddressedGC` (decouples GC from the 11 commit/read callers).
  - **Move 2:** shared `ObjectIO`/`readSmallObject` (kills 5 duplicate copies; watch 404-semantics divergence).
  - **Move 3:** `GcLayout.{h,cpp}` (shard/epoch/snapshot/sealed-index out of PoolPaths → GC subsystem).
  - **Move 4:** `ContentAddressedWriteBuffers.{h,cpp}` (the two write buffers out of the 2505-line transaction giant, −258 lines).
  - Deferred (review's advice): Move 5 (disk-path grammar, wider-tree includes) only if time; Move 6 + splitting the transaction CLASS across .cpp = NOT done (high-risk, low-payoff per review).
  - Architecture-review proposal archived in this run's transcript; per-file concern map + fragmentation findings captured there.
  - **Round 2 DONE + reviewed (APPROVED).** Moves 1-4 landed (`4234d9de9dd` RefPayload, `f1820540994` ObjectIO, `e02606dd71c` GcLayout, `1175fb1d66d` WriteBuffers). All relocation-only (verified byte-identical bodies), Move-2 throw-vs-optional semantics preserved (PoolCoordination::readIfExists left untouched), Move-4 callbacks-only (no friend). Net: `ContentAddressedTransaction.cpp` 2505→2243, `PoolPaths.cpp` 734→565, `ContentAddressedGC.cpp` 1030→999; 8 new cohesive files. 158 gtests + clickhouse build green.
- **Phase B round 3 DONE.** `be36cced6d0` — code was already well-polished by rounds 1-2 + reorg; the simplifier made only the high-confidence win (renamed cryptic single-letter locals in the codec deserializers — `PartManifest`/`RefSidecar`/`WriteSession` deserialize; byte format unchanged) and correctly LEFT Tier B/C (storage + commit core) untouched (no safe behavior-preserving change; guardrail = "when in doubt, leave it"). 158 gtests + clickhouse green. **Phase B complete.**
- **Phase C (full suite: CA-local + CAS-on-S3/minio)** — both runs fully wired (no new config): CA-local job `"Stateless tests (arm_binary, content_addressed storage, parallel)"` (object_storage_type=local), CA-S3 job `"Stateless tests (arm_binary, content_addressed s3 storage, parallel)"` (object_storage_type=s3, endpoint minio :11111, auto-started by the job; first run needs dl.min.io). Expected baseline = `Failed:0` with ~10 directly-`no-content-addressed-storage`-tagged tests SKIPPED (path-shape ×6, FETCH-partition ×1 B21, BACKUP/s3_plain ×3) + the by-definition-incompatible categories; B65 is the one real CA bug still gated. Binary current (HEAD be36cced6d0). docker_ok.
  - **Run 1 (CA-local full suite) DONE + triaged.** `Failed: 58, Passed: 10256, Skipped: 80`. Known-gated tests correctly SKIPPED; B65 OK. Triage of the 58:
    - **Group A (33) — REAL bug, now FIXED:** `resolveAndResurrectGeneration exceeded 8 iterations (CORRUPTED_DATA)`, cascading into ordinary mutation/insert tests under the CA-default policy. Root cause: the GC writes a PERMANENT `.tombstone`/round (5s test interval) → a hot identity accrues sealed generations faster than the fixed 8-cap; the loop is provably correct+terminating at any cap. **Fix `cee3eaa0925`: cap 8→256** (safe headroom + production robustness — a transient GC-seal burst no longer fails a user query). NOT a Phase-B-reorg regression (reorg verified behavior-preserving). Principled cure (in-flight manifest pin to close the seal-vs-resurrect race) → backlog **B83**; bounded-gravestones → **B84**.
    - **Stale CA tests — FIXED `cf561106a4d`:** `04283` (B33 Replicated-on-CA gate lifted → rewrote rejection into a positive smoke); `04284` (RESTORE-onto-CA now SUCCEEDS — B30 closed the fail-close path — rewrote to positive smoke); `04215` (relative `system.parts.path` on CA → added `no-content-addressed-storage` tag).
    - **Not actionable (documented):** `04295` B81 GC-leftover flake (empty stderr, not resurrection); `03649`/`03650` alias_marker = pre-existing NON-CA branch-WIP (chmod / unregistered `enable_alias_marker` setting); Groups E/F/G = infra/flaky/qemu/network/MySQL-not-running.
    - **Post-fix targeted verify** (cap + 3 test fixes + GC tests): `Failed:1/Passed:7/Skipped:1` — all Group-A representatives GREEN, 04283/04284 OK, 04215 SKIPPED, 04279 OK; the 1 fail = 04295 B81 flake (empty stderr). 158 gtests still green. So CA-local is green modulo {B81 flake, infra, non-CA alias-WIP}.
  - **Run 2 (CA-S3/minio full suite) DONE + triaged.** minio came up; **Group A GONE on S3** (the cap fix holds). `Failed: 147 (142 unique), Passed: 3454, Skipped: 33`. Of the 147: **~139 are S3-latency INFRA** (≈84 `system-log-flush-to-S3 180s timeout`, ≈46 process wall-clock timeouts, a few timing-flaky diffs, network/MySQL) — NOT CA logic bugs; all pass on CA-local/plain disks → backlog **B86** (route logs off the CA-S3 disk or tune S3 timeouts). **3 REAL CA-on-S3 bugs, all now FIXED:**
    - **B-new-1 (read-path gen resolution), FIXED `25c26b4d2e3`:** blob 404 on S3 (`03566`, `03711`) — bytes were present at `g1` but the read resolved bare H→g0 (swept after resurrection) with no fallback. NOT data loss (recoverable). Root: `resolveBlobGenKeyForRead`/`resolvePartGenKeyForRead` defaulted to g0 without reading the `active` hint (dropped plan Task 2 Step 1). Fix: read the `active` hint on cache miss (default 0 → common g0 path preserved). The completing safety net (404→`repairBlobGenOn404` retry, for stale `active`) → backlog **B85**.
    - **B-new-2 (double-slash shadow path), FIXED `2cd297c3c9c`:** `shadowRefsPrefix` produced `<uuid>//refs/` (MinIO `XMinioInvalidObjectName`; local FS normalized silently) — `03611`. Fix: strip trailing slash before appending `/refs/` in `shadowRefsPrefix` (covers shadowRefKey/MetaKey/MutableFileKey; non-shadow builders untouched).
    - **CA-S3 targeted verify** (post-fix): `Failed:0, Passed:6, Skipped:2` — 03566/03711/03611 + 04279/04292 all OK; 158 gtests still green.
  - **Final full CA-local re-run — server ABORTED mid-suite (NOT a regression; investigated).** praktika reported `Hung check failed: server is not responding`. Root cause (stacktrace-confirmed): a **server SIGABRT** on `00753_alter_attach` — `ATTACH PARTITION` found the attaching part broken (missing blob generation `blobs/e80a…/1`, the B85 read-resolution family on the attach-LOAD path) → `PartsTemporaryRename::rollBackAll` → `disk->moveFile(dir→dir)` → CA `moveFile` threw `LOGICAL_ERROR "source not recorded"` (it only handles part-FILE renames, not the detached-part-DIRECTORY rollback) → `abort_on_logical_error` → server died → hung-check fired. **The read-path FIX 1 is EXONERATED** — zero frames in `resolveBlobGenKeyForRead`/`gen_cache_mutex` in the stacktrace; lock scope verified clean (I/O outside the lock). This is a pre-existing latent CA write-path gap (→ backlog **B87**), exposed (not caused) by the run; B85's 404-repair would prevent the trigger. NOTE: the two `clickhouse/clickhouse-server:24.3` containers on the host (`mv_data-source-1`/`-destination-1`, up 8h) are the USER's, not test leftovers, and coexisted fine with Runs 1 & 2 — not the cause.
  - **Read-path/cap fix validation rests on:** 158 `ContentAddressed*` gtests green + targeted CA-local (9 tests, post-cap-fix) green + targeted CA-S3 (6 tests, post read-path/path fixes) green. A clean full-suite CA-local number is best captured after **B87** is fixed (the abort truncated the run).

---

## NIGHT SUMMARY (2026-06-05 → 06-06)

**All goals delivered.** Branch `cas-mergetree-poc`, all work committed + (final push below). 158 `ContentAddressed*` gtests green throughout.

- **Phase A — projection-overlay refactor: DONE + reviewed (APPROVED).** Replaced the `registerCarriedForwardProjectionForCA` workaround with a directory-level read-your-writes overlay; root-caused deeper (CA `createHardLink` now stages eagerly so `loadProjections` sees the staged projection during finalize). Workaround + B63 back-fill deleted. Eager-hardlink verified safe (no double-exec, FREEZE atomic, rollback clean, non-CA byte-identical). Commits `ba5038840f3`→`998c67f0b46`.
- **Phase B — 3 rounds: DONE.** R1 simplified the GC cluster (`258ab719c3cf`, net −79, extractions verified behavior-preserving). R2 architecture reorg (4 relocation-only moves, all reviewed): `RefPayload`/`ObjectIO`/`GcLayout`/`ContentAddressedWriteBuffers` extracted; `ContentAddressedTransaction.cpp` 2505→2243, `PoolPaths.cpp` 734→565, `ContentAddressedGC.cpp` 1030→999; 8 cohesive new files (`4234d9de9dd`,`f1820540994`,`e02606dd71c`,`1175fb1d66d`). R3 final clarify (`be36cced6d0`, codec-deserializer locals; commit-core left untouched).
- **Phase C — full suite CA-local + CA-S3(minio): DONE (run + triage + fix).** Both full suites run. **Fixed:** the dominant **Group-A resurrection-cap exhaustion** (8→256, `cee3eaa0925` — safe robustness, root-caused to the aggressive test GC cadence vs unbounded gravestones); **CA-S3 read-path generation resolution** (read the `active` hint, `25c26b4d2e3`); **CA-S3 double-slash shadow path** (`2cd297c3c9c`); **stale CA tests** 04283/04284/04215 (`cf561106a4d`). CA-S3 targeted post-fix: Failed:0/Passed:6.
- **Backlog added:** B80 (merged-iterator `isFile` staged), B81 (04295 GC-leftover flake), B82 (CA tryCommit-retry staging), B83 (seal-vs-resurrect in-flight manifest pin — the principled resurrection-cap cure), B84 (bounded gravestones), B85 (404→repair read fallback — the completing read-path safety net), B86 (S3 full-suite timeout tuning — infra), B87 (CA `moveFile` LOGICAL_ERROR on attach-rollback dir rename — server abort).
- **Known-open after the night:** B85 (read-path 404-repair) + B87 (moveFile rollback) are the two real CA write/read robustness items the CA-S3 + final runs surfaced; both are recoverable-class (not data loss) and have precise analyses in the backlog. A clean full CA-local number awaits B87.

---

## FOLLOW-UP (2026-06-06) — fix B85/B87/B86 → all-green (CA-local + CA-S3)

User: "brainstorm / plan / fix those. unattended. then rerun tests. ultimate goal — all tests green in both minio and local cas." Spec: `docs/superpowers/specs/2026-06-06-ca-all-green-design.md`.

- **Fixes (designed via grounded read-only investigation, then implemented + reviewed):**
  - **B85 DONE `a6588c9d7ac`** — `getStorageObjects` HEAD-checks the resolved blob generation (`resolveBlobGenKeyChecked`: `tryGetObjectMetadata` → on miss `repairBlobGenOn404`); stale-`active` reads recover to the live generation. Also fixes the attach-LOAD missing-generation trigger.
  - **B86 DONE `60c4f5e36ed`** — CA-S3 test config routes all system logs to the local `default` policy (the ~84 system-log-flush-to-S3 180s timeouts). `opentelemetry_span_log` needed the `<engine>`-with-`settings storage_policy` form (it ships an explicit engine); it's the ONLY engine-bearing system log (reviewer-verified, no latent startup risk). User tables still default to `content_addressed_s3`.
  - **B87 DONE `bc911ec2126` + completion `6e89957d7d4`** — first cut handled active part-dir renames; the **concurrent-attach race `01164_detach_attach_partition_race`** exposed that the rollback path is `store/<uuid>/detached/attaching_X → .../detached/X` (parses as part_name=detached, file=attaching_X, non-empty) → still LOGICAL_ERROR → server abort. Completion: `is_part_dir` predicate delegates BOTH active (empty file) AND detached (part_name==detached, single-component file) dir renames to `moveDirectory`. The race also exposed **two more fatals**, both fixed in `6e89957d7d4`: (2) `LocalObjectStorage::listObjects` recursive-walk couldn't tolerate a subdir vanishing mid-walk → rewrote as an explicit `directory_iterator` stack with `error_code` overloads (race-tolerant; reviewer-confirmed faithful — follow-up B88 for dir-symlink parity); (3) `republishDetachedStagingIntoActive` `LOGICAL_ERROR`→`FILE_DOESNT_EXIST` on the benign concurrent-consume (matches plain-disk recoverable semantics). All reviewed APPROVED; 158 gtests green throughout; `01164`+`00753`+`03566`+`03711` pass on CA-local + CA-S3 targeted.
- **First full CA-local rerun (binary with B85/B86/B87-v1) truncated at 33%** by the `01164` abort (the incomplete-B87 case) — Group A confirmed GONE, no other CA failure before the abort. Re-running full on the completed binary (`6e89957d7d4`).
- B88 added (listObjects dir-symlink parity + republish diagnosability — non-blocking).
- **Full reruns on `6e89957d7d4`:** in progress (see below).
