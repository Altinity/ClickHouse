# Unattended Night Log — 2026-06-05 → 06-06

**Branch:** `cas-mergetree-poc`. **Mandate:** work unattended ~12h, no questions/stops, subagents+codex allowed. Foreground builds (→ log, subagent-summarize), bounded foreground tests, never commit to `master`, no rebase/amend, trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## Goals (verbatim intent)
1. **Phase A — finish the projection-overlay refactor.** Replace the CA-only `registerCarriedForwardProjectionForCA` workaround with a directory-level read-your-writes overlay (mirror the `existsFile` B59 prelude on `existsDirectory`), so `loadProjections` finds the staged projection during finalize the normal way; then delete the workaround (+ its B63 rows_count/index back-fill). brainstorm→spec→plan→implement (subagent-driven).
2. **Phase B — 3 rounds of simplification/clarification.** `/simplify` + YAGNI/Occam reviews + **architecture/file-organization review**. User pain point: *"things are organized in files [so it] fragments important things — some connected/related entities sit far from each other, and some unrelated are close."* Prefer expressive code over comment-walls. Reorganize files so related entities are co-located; each reorg must build + keep tests green.
3. **Phase C — full test suite** on **CA on local disk** AND **CAS on S3 (minio)**. Investigate/establish the minio path.
4. Track work + results here; push deferred items to `deferred_backlog/cas-mergetree-integration.md`.

## Design decisions made unattended (no user to consult)
- Acting as the design decision-maker per the unattended mandate (can't run interactive brainstorming). Decisions recorded in the specs + here.

## Status timeline
- (start) Scaffolding created. Beginning Phase A spec.
- **Phase A DONE + reviewed (APPROVED).** Commits `ba5038840f3` (spec) → `51192075c3f` (hasInFlightDirectory) → `f528a7f2b7e` (existsDirectory prelude) → `998c67f0b46` (delete workaround + overlay extensions). `registerCarriedForwardProjectionForCA` + its B63 back-fill DELETED.
  - **Deeper than spec (root cause):** the real bug was `DiskObjectStorageTransaction::createHardLink` QUEUED the op into `operations_to_execute` and only staged at commit-replay (AFTER `loadProjections`), so `recorded` was empty at probe time. Fix: CA `createHardLink` now stages EAGERLY (mirroring CA `writeFile`); non-CA keeps the queued lambda. Read overlays added at 4 layers (`hasInFlightDirectory`+`listInFlightDirectory` on `IDiskTransaction`/`IMetadataStorage`/`DiskObjectStorageTransaction`/`ContentAddressedTransaction`) + `DataPartStorageOnDiskFull::existsDirectory`/`exists`/`iterate` consult the open txn (all `transaction &&`-guarded → non-CA byte-identical).
  - **Review verdict:** eager-hardlink SAFE (no double-exec — CA op is eager-only, never queued; commit publishes from staging identically; FREEZE atomic; rollback clean). 158 `ContentAddressed*` gtests green. `05000_content_addressed_projection_carry_forward` proves the carried projection is queryable + non-empty `projection_parts` in the SAME session (no reload). Non-CA regression (`01710_projection_mutation`, `01710_projection_materialize_with_missing_columns`) unchanged.
  - **`04295_content_addressed_mutation_no_leftovers` FAILS** — confirmed PRE-EXISTING + out of scope (no-projection GC-leftovers oracle, test+ref byte-unchanged in range, tombstone-only leftovers, run-to-run variance 140/126 = flaky GC-reaper race vs 60s poll). → backlog.
  - 3 non-blocking follow-ups → backlog (B79 merged-iterator isFile staged inaccuracy; B80 04295 tombstone-leftover flakiness; B81 CA tryCommit-retry-loses-staging).
- Beginning Phase B round 1 (/simplify + YAGNI/Occam).
