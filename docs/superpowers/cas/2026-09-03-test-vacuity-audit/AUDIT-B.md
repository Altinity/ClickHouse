---
description: 'Static test-vacuity audit of the CAS unit tests after the backend request-contract migration, auditor B (files-01): per-test property, killing mutation and verdict, plus the ranked production mutants for the deferred dynamic battery.'
sidebar_label: 'Test-vacuity audit B (2026-09-03)'
sidebar_position: 4
slug: /superpowers/cas/test-vacuity-audit-2026-09-03/audit-b
title: 'CAS test-vacuity audit — auditor B table (2026-09-03)'
doc_type: 'reference'
---

# Test-vacuity audit — auditor B (files-01) {#test-vacuity-audit-auditor-b-files-01}

Base: f4190da88a1, branch cas-gc-rebuild. Engine read: `CasRequests.h/.cpp`, `cas_test_helpers.h`.

Summary counts (69/69 files, 1393 physical TEST/TEST_F/TEST_P bodies read against the actual code):
- DISCRIMINATING: 1388
- WEAK: 3
- TAUTOLOGICAL: 2
- UNSURE: 0

(A handful of table rows below use a shorthand "(+Aborts)" or "N pairs" annotation for a release-mode
test paired with its debug/sanitizer death-test twin, without also updating that row's "(×N)" verdict
tag to include the twin. Those twins share the exact same property/reasoning as their paired row -- this
is a bookkeeping gap in the per-row multiplier, not a coverage gap: every physical TEST body in every
assigned file was read against the production code. The summary counts above are computed from the real
`grep -c "^TEST"` count per file, not from summing the "(×N)" tags.)

Top 5 WEAK/TAUTOLOGICAL findings:
1. gtest_cas_protocol_scenarios.cpp:CASProtocol.DISABLED_RevalidateAbsentTreeDepRecreates -- TAUTOLOGICAL: unconditional `GTEST_SKIP()`, no assertion ever executes.
2. gtest_cas_protocol_scenarios.cpp:CASProtocol.DISABLED_AdoptTreeOfReclaimedTreeFailsClosedAtAdoptTime -- TAUTOLOGICAL: same, unconditional skip.
3. gtest_cas_record_stream_format.cpp:CASRecordStream.WriterIsByteDeterministic -- WEAK: bare `EXPECT_EQ(f(x), f(x))` reflexive self-comparison, no order-shuffle or alternate-input check.
4. gtest_cas_ref_recovery_cas_walk.cpp:CASRefRecoveryCasWalk.PutHookBackendComposesHidingListBackendCasPutFaultInjection -- WEAK: its named property is fixture-class composition, not production code; the direct killing mutation lives in this test file's own class hierarchy.
5. gtest_cas_s3_staging.cpp:CASS3Staging.GcBlobDiscoveryPrefixExcludesStagingObjects -- WEAK: compares the real `Layout::blobsPrefix()` against a hand-written staging-prefix literal instead of the real `stagingKeyPrefix()` accessor, so it can't catch a regression on the staging side.

## src/Disks/tests/gtest_cas_ns_creation_lifecycle.cpp {#src-disks-tests-gtest-cas-ns-creation-lifecycle-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| HappyPathReachesLiveWithADurableCkptAndAStableIncarnation | full 3-write creation sequence reaches Live with a durable `_ckpt` at the creator's `writer_epoch` and a stable incarnation | in `completeCreation`, skip step 3's `Creating->Live` CAS (return early after `_ckpt`) | DISCRIMINATING |
| CreateNamespaceRejectsAnAlreadyExistingEntry | `createNamespace` refuses a namespace that already has a catalog entry | delete the pre-check `read`+refuse in `createNamespace`, letting a second `casAdmitEntry` run | DISCRIMINATING |
| CreateNamespaceRejectsAnAlreadyExistingEntryAborts (death, debug/san only) | same precondition, enforced as `chassert`/abort in debug builds | remove the abort/assert guard | DISCRIMINATING |
| CreatingForbidsPublication | `checkPublicationAdmittedOrThrow` throws for a `Creating` entry | remove the `Creating` branch's throw | DISCRIMINATING |
| LiveAndRemovingAndAbsentAllAdmitPublication | `Live`/`Removing`/no-entry all admit publication (no throw) | make the function throw unconditionally, or add a spurious check on `Removing` | DISCRIMINATING |
| FencedOutBetweenTheCkptPublishAndGoLiveRefusesAndLeavesEntryCreating | fence lost between `_ckpt` publish and step 3's CAS is caught by step 3's own admission check, leaving the entry `Creating` and the `_ckpt` in place | remove `completeCreation`'s admission check before its catalog read/write | DISCRIMINATING |
| CreateNamespaceRacingASiblingsStillCreatingEntryReportsSupersededNotAbort | a loser whose pre-check read observes a still-`Creating` sibling entry is told `Superseded`, not aborted | change the pre-check's `Creating`-observed branch to throw/abort instead of returning `Superseded` | DISCRIMINATING |
| CreateNamespaceRacingASiblingsFullCreateBetweenPreCheckAndStep1ReportsSupersededNotAbort | `createNamespaceStep1`'s own per-write recheck (not just the outer pre-check) catches a sibling that fully raced in between, reporting `Superseded` | delete step1's own duplicate recheck so only the outer pre-check guards, letting two full creates race to an encode-time LOGICAL_ERROR/abort | DISCRIMINATING (regression test for CI PR#2073) |
| EntryStolenByAConcurrentReconcilerRefusesGoLiveAndLeavesTheStolenEntryAlone | step 3's entry-token check refuses when a concurrent reconciler stole the entry between the read and the CAS, leaving the thief's write untouched | remove `completeCreation`'s entry-equality check before its CAS write | DISCRIMINATING |
| BothFenceAndEntryStaleRefusesGoLiveViaTheFenceCheckWhichRunsFirst | when both the fence and the entry are stale, the fence check (checked first) determines the outcome (`FencedOut`, not `Superseded`) | delete the `admitted()` check inside the mutate closure (test's own comment names this exact mutation) | DISCRIMINATING |
| ReconcileRefusedWhileTheOriginalCreatorFenceIsStillLive | `reconcileStaleCreator` refuses (leaves entry untouched) while `is_creator_fence_terminal` says the original creator is still live | make the terminality check always return true / delete the check | DISCRIMINATING |
| ReconcileSucceedsTokenExactlyAfterTheOriginalCreatorFenceIsTerminalThenResumesToLive | reconciliation moves only the `creator` field (same incarnation), and the resuming actor can then reach `Live` with `_ckpt.life_epoch` = the winning writer_epoch | mint a fresh incarnation in `reconcileStaleCreator`, or stamp `_ckpt.life_epoch` from the original creator instead of the resuming one | DISCRIMINATING |
| ReconcileFailsClosedWhenTheEntryAlreadyChanged | a second reconciler holding a stale `entry` token is refused (`EntryChanged`) even though terminality alone would allow it | drop the entry-token CAS precondition from `reconcileStaleCreator` (compare-and-swap degenerates to blind write) | DISCRIMINATING |
| CompleteCreationRejectsANonCreatingEntry / …RejectsANonCreatingEntryAborts | `completeCreation` refuses (throws `LOGICAL_ERROR` / aborts) a non-`Creating` entry | remove the state-guard at function entry | DISCRIMINATING |
| ReconcileStaleCreatorRejectsANonCreatingEntry / …RejectsANonCreatingEntryAborts | `reconcileStaleCreator` refuses a non-`Creating` entry the same way | remove its state-guard | DISCRIMINATING |

Verdict counts: DISCRIMINATING 15 (counting each #ifdef pair as one property, 17 physical TEST bodies), WEAK 0, TAUTOLOGICAL 0, UNSURE 0.
## src/Disks/tests/gtest_cas_ns_file_incarnation.cpp {#src-disks-tests-gtest-cas-ns-file-incarnation-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ColdReaderUsesCatalogCutWhileOldFileSurvivesRemoval | a real removal reaches a catalog-absent cut despite LIST permanently omitting the old-life file; a cold reader follows the catalog cut, not physical residue; old bytes never alias successor bytes; no `/_cleanup/` key is ever touched | make `namespaceFilesLifeIfReadable` (cold reader) fall back to LIST-discovered old-life files when the catalog row is absent | DISCRIMINATING |
| FreshReaderAssignsOnlyLiveCatalogLifeWithoutMutation | only a `Live` catalog row yields a readable/ref-table life; `Creating`/`Removing`/absent yield none; the read path performs zero PUTs | remove the `NsState::Live` guard in the life-assignment read path so `Creating`/`Removing` also resolve | DISCRIMINATING |
| RebirthDoesNotWaitForFilesToBeEmpty | a GC fold records terminal cleanup evidence for a removed life even while its `_files` debris remains physically present | gate the fold's `cleanup_evidence` write on an empty-files LIST check | DISCRIMINATING |

## src/Disks/tests/gtest_cas_ns_file_read_contract.cpp {#src-disks-tests-gtest-cas-ns-file-read-contract-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| HeldLifeAfterSameNameRebirthNeverSeesSuccessorBytes | a warm runtime holding life 1 never observes life-2 bytes after same-name rebirth, even mid-transaction | resolve the write/read path by namespace NAME instead of the held opaque life id | DISCRIMINATING |
| DelayedInlineFinalizeCannotChangeSuccessorTokenOrBytes | a buffer opened against life 1 and finalized after rebirth either fails `NETWORK_ERROR` or lands on life 1's own key, never touching life 2's object/etag | capture only the namespace name in the inline buffer and resolve the life lazily at finalize time (test's own comment names this) | DISCRIMINATING |
| ListThroughHeldLifeIssuesZeroCatalogRequests | `listNamespaceFiles` costs exactly one LIST against the life-scoped files prefix, no catalog round-trip | make `listNamespaceFiles` re-resolve the life via the catalog before listing | DISCRIMINATING |
## src/Disks/tests/gtest_cas_observability.cpp {#src-disks-tests-gtest-cas-observability-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RenewalCountersHaveExactPhysicalAndLogicalDeltas | `CASMountRenewal{Attempts,Retries,Resolved,Recovered}` counters take exact deltas per injected fault (none / throw-before-commit / land-then-throw) | drop the `Recovered` increment on the ThrowBefore-recovery path, or double-count `Attempts` on retry | DISCRIMINATING |
| ExternalLeaseDeadlineCountsOnceWithoutReconstructingAttempts | a pre-exhausted lease refuses before any attempt and increments `DeadlineExceeded` exactly once, no attempt/retry counters move | increment `CASMountRenewalAttempts` even when admission refuses before the first attempt | DISCRIMINATING |
| StageManifestEmitsManifestPut | `stageManifest` emits exactly one `ManifestPut` event with the right kind/hash/non-empty token | remove the event emission in `stageManifest` | DISCRIMINATING |
| AbandonEmitsPrecommitRemoved | `abandon` of a precommitted build emits exactly one `PrecommitRemoved` with correct ns/ref/kind/hash | remove the emission on abandon | DISCRIMINATING |
| AbandonWithoutPrecommitEmitsNoPrecommitRemoved | `abandon` of a never-precommitted build emits zero `PrecommitRemoved` | emit the event unconditionally regardless of precommit state | DISCRIMINATING |
| ResurrectSupersedeEmitsOnlyRetireReplacedWithOldToken | the supersede-peek path emits exactly one `blob_retire_replaced` (correct old/new tokens, logical size) and zero `blob_retire`, incrementing `CASGCRetireReplaced` but not double-counting `CASGCRetiredCondemned` | make the supersede path call the fresh-condemn `head_blob` hook (double-emits `blob_retire` + double-counts `CASGCRetiredCondemned`) | DISCRIMINATING |
| CaInspectDecodesRefSnapshotToJson | `caInspectToJson` renders a real encoded ref-snapshot with correct namespace/snapshot_id/ref_name/precommits and no stale `lifecycle` key | swap in the wrong field encoding, or fail to strip the retired `lifecycle` key | DISCRIMINATING |
| CaInspectDecodesRefLogToJson | same for a ref-log txn (op kind, binding fields) | mis-render `owner_transition`/binding fields | DISCRIMINATING |
| CaInspectDecodesPartManifestToJson | same for a part manifest, including `EntryPlacement` full-word rendering | render `placement` as the enumerator spelling instead of the wire word | DISCRIMINATING |
| CaInspectDecodesMountLeaseToJson | same for a mount lease | wrong field mapping in the mount-lease decode branch | DISCRIMINATING |
| CaInspectDecodesGcStateToJson | same for gc/state | wrong field mapping | DISCRIMINATING |
| CaInspectDecodesEnvelopeHeaderWithEveryProvenanceOpWord | `ProvenanceOp`/`ObjectKind` render as full wire words for every enumerator | mis-map one `ProvenanceOp` word (e.g. `Repack` -> "merge") | DISCRIMINATING |
| CaInspectUnknownKeyThrows | an unrecognized key throws instead of silently decoding garbage | remove the fail-closed default branch | DISCRIMINATING |
## src/Disks/tests/gtest_cas_operation_gate.cpp {#src-disks-tests-gtest-cas-operation-gate-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ProbesOnVanishedAnswerAbsentEmpty | probe class ops (exists/list/isEmpty/getStorageObjectsIfExist) read truth-absent on a Vanished pool without reaching the backend | make one probe (e.g. `existsFile`) still consult the pool/backend instead of the Vanished short-circuit | DISCRIMINATING |
| RemovesOnVanishedAreNoOpSuccessBackendUntouched | remove-class ops no-op-succeed on Vanished and never touch the backend (part still there after restoring Live) | route a Vanished-disk remove through the real backend delete | DISCRIMINATING |
| ContentReadOnVanishedThrowsTypedPerReasonMessage | content-read throws with the exact sub-state substring ("foreign pool" vs "erasure was NOT verified") | swap the two message branches or collapse them to one generic string | DISCRIMINATING |
| EveryClassThrowsRetryableTransientOnTransientNotLive | every op class except Factory throws `NETWORK_ERROR` with "mount lease not held"/"TRANSIENT"/"recovers to Live" on `TransientNotLive` | classify `TransientNotLive` as `INVALID_STATE` (terminal) instead of `NETWORK_ERROR` | DISCRIMINATING |
| EveryClassThrows668OnIdentityLost | every op class throws `INVALID_STATE` with "identity lost" on `IdentityLost` | classify `IdentityLost` as the transient `NETWORK_ERROR` code | DISCRIMINATING |
| TransientRefusalIsUpstreamRetryableTerminalIsNot | `isRetryableException` (upstream's own classifier) says yes for the transient refusal, no for the terminal one | let `isRetryableException` accept `INVALID_STATE`, or fail to recognize the transient `NETWORK_ERROR` shape | DISCRIMINATING |
| FactoryClassWorksOnVanished | `createTransaction`/getters construct without throwing on a Vanished pool | make `createTransaction` consult pool state and throw on Vanished | DISCRIMINATING |
| TryGetInManifestBytesPropagatesTypedError | `tryGetInManifestBytes` propagates the typed refusal instead of swallowing to `nullopt` | catch the typed exception and return `nullopt` (the regression this test guards against, per its own comment) | DISCRIMINATING |
| NullPoolFailsLoudForEveryClass | after `shutdown()` (null pool) every class including Probe throws `INVALID_STATE` ("not started"), no benign-absent | restore the old transitional not-Mounted branch that answered Probe with benign-absent | DISCRIMINATING |
| GcEntryPointsRefuseOnNotLive | GC entry points refuse with the typed per-state message on Vanished/TransientNotLive | let GC run unconditionally regardless of pool lifecycle | DISCRIMINATING |
| GcSchedulerIsQuiescentReflectsRoundInFlight | `isQuiescent` tracks the round-in-flight flag exactly | hardcode `isQuiescent` to always return true | DISCRIMINATING |
| RemoveThrowsDuringTransientAndDrainsAfterRecovery | a Remove throws in a transient lease gap, then actually drains (removes the part) after real self-remount recovery | let the post-recovery Remove silently no-op instead of really removing | DISCRIMINATING |
## src/Disks/tests/gtest_cas_orphan_manifest_sweep.cpp {#src-disks-tests-gtest-cas-orphan-manifest-sweep-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| EligibleAndUnownedIsDeleted | a staged-but-unowned manifest body in a watermark-eligible prefix is deleted | remove the delete call once eligibility+unowned is established | DISCRIMINATING |
| CheckpointSnapshotAtOlderEpochSealSkipsDeletion | a forged same-id snapshot at an older `EpochSeal` base must NOT be treated as an empty owner view; candidate retained + warning emitted | drop the base-epoch-seal comparison so metadata equality alone authorizes deletion | DISCRIMINATING |
| OwnedBodyIsSkipped | a committed (owned) manifest body is never swept | delete owned bodies regardless of owner-view membership | DISCRIMINATING |
| PendingCommittedRemovalBodyIsSkipped | a committed-then-dropped-but-unsealed manifest survives the sweep (GC-WEDGE-2026-07-10 regression) | remove the pending-committed-removal protection so drop+unsealed bodies are swept | DISCRIMINATING |
| EmitsNoBlobDeltas | the sweep never advances the in-degree generation or touches blob edges | make the sweep emit a blob delta / bump generation | DISCRIMINATING |
| CursorPageAdvancesAndWrapsWithListBudget | the cursor page honours `list_budget`, advances `next_cursor`, and wraps (empty cursor) once exhausted | ignore `list_budget` or fail to wrap the cursor at the end | DISCRIMINATING |
| NoWatermarkIsNotAuthority | absent watermark fact = not eligible = nothing deleted | treat prefix as eligible by default (fail-open) | DISCRIMINATING |
| CursorPageDeletesEligibleUnownedBody | one page deletes exactly the eligible unowned body it lists | skip the delete call on an unowned candidate | DISCRIMINATING |
| CursorPageRespectsDeleteBudget | exactly `delete_budget` deletions happen even when more candidates are eligible | ignore `delete_budget` and delete everything listed | DISCRIMINATING |
| CursorPageDeletesObservedBodyWhenCatalogOmitsNamespace | a manifest whose namespace has no catalog row at all (dead-life debris) is still eventually swept | refuse to sweep bodies for namespaces absent from the catalog (leaks debris forever) | DISCRIMINATING |
| CursorPageCannotDeleteManifestReplacedAfterObservation | a manifest replaced (new token) after the sweep's observation but before delete must survive (exact-token delete loses) | switch the delete to unconditional/current-token instead of the frozen observed token | DISCRIMINATING |
| CursorPageRefusesAmbiguousCatalogLifeIndex | a duplicate catalog life id makes the whole cut ambiguous and refuses (CORRUPTED_DATA), retaining even unrelated bodies | let the sweep pick one of the duplicate rows arbitrarily instead of refusing | DISCRIMINATING |
| CursorPageSkipsOwnedBody | (cursor-page variant of OwnedBodyIsSkipped) | delete committed bodies via the cursor-page path | DISCRIMINATING |
| MissingRequiredCheckpointSuppressesDestructiveDecision | a catalog-`Live` namespace missing its mandatory `_ckpt` retains its body rather than being treated as empty | derive an empty owner set when `_ckpt` is missing instead of refusing to decide | DISCRIMINATING |
| EpochSealFoldCursorCrossesTailByExactDecodedSuccessor | the fold-cursor tail scan crosses every intermediate epoch seal by its exact decoded successor, protecting the removal's target while still sweeping an unrelated eligible body | skip an intermediate epoch-seal hop (e.g. jump straight to the removal epoch) | DISCRIMINATING |
| MissingImmediateEpochAfterCleanedCursorCannotBeSkippedByLaterBacklink | a later epoch's `prev_epoch_seal` backlink cannot substitute for a missing immediately-next epoch head; the whole namespace fails closed | let the tail scan follow a later backlink when the immediate successor is absent | DISCRIMINATING |
| CleanedCursorCrossesOnlyThroughExactImmediateEpochHead | control: when the immediate successor head DOES exist, the tail scan crosses it and correctly protects/sweeps | make the tail scan require every seal even when only the immediate one is needed (over-retention) or vice versa | DISCRIMINATING |
| LaterCatalogCutCannotSpliceOwnershipAuthority | the catalog cut used for coverage and the life used for ownership must be one frozen read; a second catalog read racing in must not be consulted | re-resolve the catalog a second time mid-sweep instead of reusing the frozen entry | DISCRIMINATING |
## src/Disks/tests/gtest_cas_orphan_nomination.cpp {#src-disks-tests-gtest-cas-orphan-nomination-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RetiresExactManifestSourcesBeforeDelete | sweeping an aborted precommit retires its exact source edges before deleting the body; unrelated sources untouched; reducer stays accounting-neutral (`unmatched_removes`/`transactions_unapplied`=0) | delete the manifest body without first retiring its source edges | DISCRIMINATING |
| CorruptManifestIsRetainedAndSurfaced | an undecodable manifest body is retained, surfaced via `undecodable` metric, round does not throw | let nomination delete an undecodable body instead of retaining it | DISCRIMINATING |
| TokenAbaIsRetainedAndSurfaced | a manifest whose token changed underneath the sweep (ABA) throws `CORRUPTED_DATA` and the body survives | make the delete unconditional (current-token) instead of exact-token, silently "succeeding" on the ABA | DISCRIMINATING |
| SuppressedRoundNominatesNothing | a suppressed-universe round never even LISTs candidates (`listed`=0, `deleted`=0); body survives | move the `suppress_destructive` check to gate only the delete, not the LIST/plan | DISCRIMINATING |
| SourceRetirementIsAccountingNeutral | retirement input removes a matching edge and is an idempotent no-op on an already-absent one, leaving the `applied` byte-vector untouched | let retirement mutate `applied` or fail to remove the matching edge | DISCRIMINATING |

## src/Disks/tests/gtest_cas_parallel_commit.cpp {#src-disks-tests-gtest-cas-parallel-commit-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PromoteReportsCreatedAndManifest | `promoteBuild` returns an exact `CommitOutcome` (created=true, correct ns/ref/manifest_ref) | return a stale or default-constructed `CommitOutcome` | DISCRIMINATING |
| DropRefIfMatchesRemovesOnlyExact | `dropRefIfMatches` is a true conditional guard: refuses on a stale manifest_ref, succeeds on the current one | make `dropRefIfMatches` unconditional (drop whatever occupies the ref) | DISCRIMINATING |
| DropRefIfMatchesOnAbsentRefIsANoOp | dropping against a bogus ref with nothing committed is a no-op (`false`) | return `true`/throw on an absent ref | DISCRIMINATING |
| RepointRefByteEqualNoOpReportsCurrentManifestNotCreated | a byte-equal repoint is a zero-mutation no-op reporting the CURRENT manifest, `created=false` | mint a fresh manifest even on byte-equal content | DISCRIMINATING |
| AbsentBeforeDroppedPreExistingUntouched | commit rollback drops only the just-created part, never touches an earlier already-committed ref | roll back by namespace-wide sweep instead of the exact per-part outcome | DISCRIMINATING |
| RepointByOtherWriterSurvivesRollback | rollback uses `dropRefIfMatches(M1)`; a concurrent repoint to M2 survives because M1≠current | make rollback use unconditional `dropRef` instead of `dropRefIfMatches` | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_folder_access.cpp {#src-disks-tests-gtest-cas-part-folder-access-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RetainedHitCostsNoRequest | a retained warm hit costs zero backend requests of any kind | make the retained path issue a validating HEAD/GET | DISCRIMINATING |
| HitPathJournalEmptyAndCheapWhenExplainDisabled | with explain disabled, the hit path writes no journal entry and `explain()` defaults to Miss | write the journal entry unconditionally | DISCRIMINATING |
| GetViewServesCommittedFolder | `getView` serves real committed content; absent ref is nullptr, never an exception | throw instead of returning nullptr for an absent ref | DISCRIMINATING |
| GetViewFailsClosedOnMissingBody | every freshness mode throws `FILE_DOESNT_EXIST` on a physically-deleted body, no HEAD issued | swallow the missing body into a benign nullopt | DISCRIMINATING |
| WritePrimitivesRoundTrip | promote/dropRefIfPresent/dropRefBestEffort/dropNamespace round-trip correctly, replay-safe | make a second `dropRefIfPresent` throw instead of no-op | DISCRIMINATING |
| RepublishRefMovesCommittedRef | `republishRef` moves committed content, source becomes absent, dest carries it over | leave source present after republish, or fail to carry `txn_version.txt` | DISCRIMINATING |
| RepublishRefIdempotentRedriveAndConflict | byte-identical redrive is idempotent-skip (no fresh manifest); differing content conflicts and leaves source untouched (`ABORTED`) | mint a fresh manifest on identical redrive, or let a content conflict silently overwrite dst | DISCRIMINATING |
| PublishEntriesAbandonsBuildOnPromoteFailure | a proven-conflict promote failure fences the mount, schedules exactly one remount, and the foreign object stays the sole/greatest occupant of the key | let a cleanup append carve a fresh id around the fenced damage | DISCRIMINATING |
| PublishEntriesAbandonsBuildOnARetryablePromoteFailure | an ordinary (non-fencing) promote rejection does NOT fence the mount, drains 4 append attempts including the abandon+destructor backstop, and leaves no live precommit | drop the catch-abandon so only 3 attempts run and the precommit binding leaks (test's own comment states this exact discriminator) | DISCRIMINATING |
| PrepareThenPromoteMatchesPublishEntries | prepare leaves exactly one live precommit and no committed ref; promote matches `publishEntries`'s outcome shape and clears the precommit | commit the ref during `prepareEntries` instead of at `promote` | DISCRIMINATING |
| PrepareThenAbortAppendsThePrecommitRemoval | abort appends the exact precommit removal (ledger view empty afterward), body survives (delete-after-sealed) | let abort merely drop the in-memory handle without appending the removal | DISCRIMINATING |
| DestroyingAnUnfinishedPreparedPartWriteAborts | destructor is a last-resort abort when neither promote nor abort ran | let the destructor leak the live precommit | DISCRIMINATING |
| PreparedPartWriteRejectsASecondTerminal(+DeathTest) | a second terminal call throws/aborts (`LOGICAL_ERROR`), never re-drives | make a second `promote`/`abort` a silent no-op | DISCRIMINATING |
| PreparedPartWriteMoveTransfersTheTerminalDuty | move transfers the terminal duty; moved-from destructor is a no-op, destination still owes one terminal | double-abort on move (both source and moved destructor act) | DISCRIMINATING |
| ExplainRecordsDecisions | `explain()` records the correct `LastDecision` per call shape (Miss/ForceFreshRead/StrictBypass/Invalidated) and non-zero estimated_bytes | mislabel one decision kind | DISCRIMINATING |
| BaselineRequestCountsWithoutRetention | without retention, exactly one body GET and zero manifest HEAD across repeated reads (decode cache absorbs the rest) | issue a HEAD per read instead of relying on the decode cache | DISCRIMINATING |
| MismatchRebuildAfterRepublish | a retained view whose manifest id no longer resolves rebuilds from the new manifest rather than serving stale content | serve the stale retained view instead of detecting the manifest-id mismatch | DISCRIMINATING |
| ForceFreshServesImmutableDecodeWithoutManifestRequestsAfterBodyDeletion | `ForceFresh`/`StrictValidate` serve the immutable id-keyed decode with zero manifest requests even after body deletion | make `ForceFresh` re-fetch the manifest body instead of trusting the id-keyed decode cache | DISCRIMINATING |
| DeletedBodyFailsClosedInEveryModeWhenDecodeCacheDisabled | with decode cache off, every mode re-GETs and throws `FILE_DOESNT_EXIST`, no caching of the failure | cache a negative result across calls (masking a later real publish) | DISCRIMINATING |
| AbsenceIsNeverRetained | dropped ref reads as nullptr and is never retained; republish is immediately visible | retain/cache a negative (absent) lookup | DISCRIMINATING |
| GetViewEmitsRefResolveOnlyOnRealResolveWork | `RefResolve` fires only on real resolve work (cold build, ForceFresh), not on a warm retained hit | emit `RefResolve` unconditionally on every `getView` call | DISCRIMINATING |
| OversizedViewServedNotRetained | an oversized view is served correctly every call but never retained (rebuilt each time, no new manifest request thanks to decode cache) | retain an oversized entry, breaking the cache-size bound | DISCRIMINATING |
| DisabledModeKeepsBaseline | `CacheParams{}` (bytes=0) matches the explicit no-retention baseline | make the default-constructed params retain anyway | DISCRIMINATING |
| SingleFlightColdBuild | concurrent cold reads of the same key single-flight into exactly one body GET | remove single-flight coalescing, causing one GET per thread | DISCRIMINATING |
| DropNamespaceErasesAllViews | dropping a namespace erases every cached view for it; a subsequent read is a genuine cold miss, never a stale hit | leave retained entries in the cache after `dropNamespace` | DISCRIMINATING |
| BestEffortRollbackDropCountsAndSurvivesABackendOutage | best-effort rollback drop is noexcept under a real backend outage and increments the failure counter, paying the retry window in virtual time | let the best-effort drop throw, or fail to increment the counter | DISCRIMINATING |
| AnUnresolvedPromoteIsNotReportedAsDefinitelyNotCommitted | a promote whose append landed but whose ack was lost is classified `commitIsUnresolved`, not a proven-failed mechanism fallback; the ref is truly committed | classify an unresolved landed-then-lost promote as `MechanismFallbackAllowed`, causing a duplicate byte-fetch publish | DISCRIMINATING |
| APostCommitFailureLeavesTheHandleTerminal | a post-commit-work exception leaves the handle terminal with zero `BuildAbort` events and the ref genuinely committed | abandon an already-promoted build in the failed-promote catch (emits a spurious `BuildAbort` on a durable commit) | DISCRIMINATING |
| PreparedPartWriteIsNotMoveAssignable | move-assignment is statically deleted; move-construction is allowed | (compile-time property) implement move-assignment without a fallible-abandon path | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_folder_view.cpp {#src-disks-tests-gtest-cas-part-folder-view-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| CacheKeyIsUnambiguous | `\0`-joined cache key disambiguates ns/ref boundary collisions (`(a,"b/c")` vs `(a/b,"c")`) | join ns and ref with `/` instead of `\0` | DISCRIMINATING |
| FindFileAndHasFile | findFile/hasFile distinguish files, absent paths, and directories | make `hasFile` return true for a directory prefix | DISCRIMINATING |
| ListChildrenCollapsesFirstComponent | listChildren collapses nested paths to their first path component under a prefix | fail to collapse `p.proj/checksums.txt` under `p.proj` | DISCRIMINATING |
| HasDirectory | hasDirectory is true only for a real prefix with children | return true for a non-existent prefix | DISCRIMINATING |
| SizesAndBytes | fileSize/inlineBytes report inline-byte-length vs blob_size correctly per entry kind | return blob_size for an inline entry (unit confusion) | DISCRIMINATING |
| ProjectionDirPrefixRecognizer | `.proj`/`.tmp_proj` suffix recognizer returns the right prefix or nullopt | match a non-projection filename as a projection dir | DISCRIMINATING |

## src/Disks/tests/gtest_cas_plain_objects.cpp {#src-disks-tests-gtest-cas-plain-objects-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| CasPutObjectIssuesHeadsOnly | put (create and replace) issues HEAD-only precondition checks, never a body GET | make replace fetch the body via GET instead of HEAD | DISCRIMINATING |
| CasRemoveObjectReheadsOnMismatch | `removeCurrent` transparently re-heads and retries on a `Mismatch`, never surfaces it as a failure | let a `Mismatch` propagate as an exception instead of retrying | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_manifest_format.cpp {#src-disks-tests-gtest-cas-part-manifest-format-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PartManifest (battery, golden bytes) | encode produces the EXACT pinned byte layout (header/records/trailer/payload-zone), decode inverts it | reorder/rename any wire field | DISCRIMINATING |
| RoundTripDescriptorAndEntries | ref/namespace/digest/entries round-trip, entries land in canonical sorted order regardless of input order | drop the canonical sort on encode | DISCRIMINATING |
| EmptyEntriesRoundTrips | zero entries round-trip; no payload zone (`==>`) is emitted when there are no Inline entries | emit an empty payload zone banner unconditionally | DISCRIMINATING |
| PlacementWordsRenderAndRejectUnknown | placement renders as `blob`/`inline`; unknown word fails closed | accept an unrecognized placement word | DISCRIMINATING |
| ClosedSetPinsEntryPlacementWords | encode/decode word tables for `EntryPlacement` are the SAME table (round-trips every enumerator via `magic_enum`) | let the two directions diverge onto separate tables for one enumerator | DISCRIMINATING |
| SizeBeforePlaceIsAcceptedForBothPlacements | field order within a JSON record line is irrelevant to the parser | make the parser positionally order-dependent | DISCRIMINATING |
| MissingSizeIsRejectedForBothPlacements | missing `size` fails closed with the exact per-placement message (not just a shared code) | drop the per-placement message text / merge to a generic error | DISCRIMINATING |
| DuplicateSizeIsRejected | a duplicate `size` key in one record is rejected | accept the second (or first) of duplicate keys silently | DISCRIMINATING |
| InlineBytesWithEmbeddedSpecialCharsRoundTripByteFaithfully | inline payload with `\n`/NUL/`"` round-trips byte-exact via the length-delimited payload zone | encode inline bytes as an escaped JSON string instead of raw length-delimited bytes | DISCRIMINATING |
| InlineEntryPathSurvivesEveryEscapableByte | a path containing LF/CR/tab/quote/NUL round-trips exactly | fail to escape LF in the payload-zone banner (the reported reproducer) | DISCRIMINATING |
| InlineBannerCarriesTheEscapedPath | the payload-zone banner escapes the path with the SAME writer as the record line | hand-roll a separate (divergent) escaper for the banner | DISCRIMINATING |
| ByteDeterminism | encoding is deterministic and independent of input entry order (canonical sort) | drop the canonical sort so entry order leaks into the bytes | DISCRIMINATING (note: the `encode(m)==encode(m)` half is a trivial reflexivity check; the order-independence half is the real discriminator) |
| MixedAlgoEntriesRoundTrip | entries with different `BlobHashAlgo` (CityHash128 vs Sha256) round-trip with correct algo/digest/size | conflate hash-algo widths on decode | DISCRIMINATING |
| DecodeRejectsMalformedEntryPaths | decode enforces path hygiene (`..`, absolute, empty, `//`, `/./`) that encode itself does not check | drop the decode-time path-shape check | DISCRIMINATING |
| DecodeAcceptsLegalProjectionSubdirPath | a legal `.proj/` subdir path is NOT caught by the hygiene check | over-tighten the check to reject `.proj/` paths | DISCRIMINATING |
| DuplicatePathRejectedOnEncode | encode itself refuses a duplicate path | remove the encode-time duplicate check | DISCRIMINATING |
| DecodeRejectsOutOfOrderEntries | decode enforces strict ascending path order (hand-forged swapped lines) | drop the ordering check on decode | DISCRIMINATING |
| DecodeRejectsNonAdjacentDuplicatePath | ordering check catches a non-adjacent duplicate (not just adjacent-pair comparison) | weaken the check to only compare adjacent pairs | DISCRIMINATING |
| UnknownEntryAlgoFailsClosed | an unrecognized `algo` word fails closed | accept an unknown algo silently | DISCRIMINATING |
| DigestHexWidthMismatchFailsClosedNotBadArguments | a hex-width mismatch fails as `CORRUPTED_DATA`, not the codec's raw `BAD_ARGUMENTS` | remove decode's own width pre-check, letting the codec's exception leak through uncoded | DISCRIMINATING |
| PayloadDigestStableAndContentSensitive | `computePayloadDigest` is independent of the stored digest field and sensitive to real content changes | make the digest depend on the `payload_digest` field itself (self-reference) | DISCRIMINATING |
| TrailingByteAfterPayloadZoneFailsClosed | one extra trailing byte after the payload zone is rejected | drop the final `!in.eof()` check | DISCRIMINATING |
| InlineRecordSizeMismatchWithPayloadZoneBannerFailsClosed | a record `size` disagreeing with the payload-zone banner fails closed | trust the record's `size` blindly without cross-checking the banner | DISCRIMINATING |
| RefMatchesBodyAcceptsExactRef | `refMatchesBody` accepts an exact match | make it accept a mismatched ref | DISCRIMINATING |
| RefMatchesBodyRejectsEachFieldMismatch | rejects a mismatch in each of the 3 `ManifestRef` fields independently | compare only 2 of the 3 fields | DISCRIMINATING |
| ManifestNamespaceMatchesAcceptsOwningNs | accepts the exact owning namespace | accept a namespace prefix match | DISCRIMINATING |
| ManifestNamespaceMatchesRejectsForeignNs | rejects a foreign namespace and a strict-prefix namespace (no loose comparison) | use a prefix/substring comparison instead of exact equality | DISCRIMINATING |
| FindEntryBinarySearch | binary search finds exact paths, rejects prefixes/past-end/empty | break the binary search bounds (off-by-one) | DISCRIMINATING |
| EntryRangeContiguousPrefix | `entryRange` returns the exact contiguous prefix span, whole span on empty prefix, empty range on no match | off-by-one the range boundaries | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_write.cpp (part 1 of N) {#src-disks-tests-gtest-cas-part-write-cpp-part-1-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RacingWritersBothHeadMissAndPublishEquivalentBodies | two racing writers of equivalent content both HEAD-miss then both publish successfully, both reach `Materialized` proof | serialize/fail the second racing writer instead of letting both publish | DISCRIMINATING |
| WrongSizeSourcePublishesNothing | a source whose declared size disagrees with actual bytes fails `CORRUPTED_DATA` and publishes nothing | skip the size cross-check, publishing truncated content | DISCRIMINATING |
| PutBlobWritesEnvelopeWithFixedHeader | envelope header_len/build_id/incarnation_tag are correct, payload starts at fixed offset | write payload at a variable offset instead of the fixed header length | DISCRIMINATING |
| StageManifestUsesPerBuildOrdinals | manifest ordinals increment per-build, reset per new build, and the manifest key encodes the exact hex build segment | fail to increment/reset the ordinal, or encode the wrong key path | DISCRIMINATING |
| PutBlobDedupSecondWriterAdopts | a second writer of identical content adopts the first writer's incarnation untouched (no overwrite) | let the second writer overwrite instead of adopting | DISCRIMINATING |
| PutBlobFreshUploadWritesCleanMeta | a fresh upload writes a Clean meta descriptor with the right size | skip the Clean meta write on fresh upload | DISCRIMINATING |
| CreateCleanAndChokePointCountOnFreshBody | fresh upload increments `CASMetaPut` and `CASMetaCreateClean` exactly once each | double-count or skip the reason counter | DISCRIMINATING |
| AdoptBackfillCountsChokePointAndReason | adopting a meta-less pre-existing body backfills Clean meta via `CASMetaPut`/`CASMetaAdoptBackfill` | skip the backfill branch, leaving future point-readers with no meta | DISCRIMINATING |
| PutBlobAdoptsWhenMetaCleanNoRetireView | adopt decision comes purely from the meta point-read (no RetireView needed); adopted incarnation survives untouched, meta stays Clean | make the adopt decision depend on a RetireView instead of the meta | DISCRIMINATING |
| AdoptBeforePrecommitFailsClosed | `putBlob` without a durable precommit aborts ("durable precommit required") | remove the precommit-required guard | DISCRIMINATING |
| PutBlobRepublishesWhenMetaCondemned | a Condemned meta causes putBlob to displace the body with a fresh token (old token never returns) and flip meta to Clean | let putBlob adopt a condemned body instead of displacing it | DISCRIMINATING |
| CondemnedRepublicationCountsCasAndReason | condemned-displacement meta flip counts `CASMetaCompareSwap`/`CASMetaResurrectClean` | skip incrementing the reason counter | DISCRIMINATING |
| PutBlobWrongSizeFailsClosed | a lying source (declares 11, writes 5) fails `CORRUPTED_DATA`, creates nothing | let the truncated write land as a valid object | DISCRIMINATING |
| PutBlobStreamsSourceOnceNoFullMaterialization | happy-path upload invokes `source.open` exactly once (no full in-memory pre-materialization) | buffer the whole blob into a String before uploading (the OOM regression this guards) | DISCRIMINATING |
| DependencyProofDistinguishesMaterializedAndTrustedManifest | `dependencyProof` correctly distinguishes Materialized (uploaded/observed) vs TrustedManifest (adopted evidence) vs nullopt (unknown) | conflate the two proof kinds or return a proof for an unknown hash | DISCRIMINATING |
| PutBlobRepublishesVanishedBodyFromHeldSource | a body deleted mid-observation (GC race in the HEAD→GET window) is republished under a fresh token from the held source, old token never returns, meta reconciled to Clean | fail instead of republishing when the observed body vanishes underneath the writer | DISCRIMINATING |
| AGiveUpReportsHowManyAttemptsItSent | a `GaveUp` from an all-ambiguous meta write reports the EXACT attempt count the store saw, reissues paced on the virtual clock | report `sent_any` without the exact count, or miscount attempts | DISCRIMINATING |
| PutBlobFreshMetaExhaustionThrowsRetryLater | a persistently-ambiguous `.meta` write surfaces as `NETWORK_ERROR` to putBlob's caller while the body PUT itself still lands | swallow the meta-write exhaustion and report success with a stale meta | DISCRIMINATING |
| PutBlobCondemnedDedupNeverGetsTheDyingObject | INV-1: republishing over a condemned-then-absent dedup hit performs zero GETs on the blob key | GET the condemned body to "revive" it instead of re-uploading from source | DISCRIMINATING |
| PutBlobCondemnedDedupPresentNeverGetsTheDyingObject | INV-1 variant: condemned-but-still-present dedup hit also performs zero GETs, displaces with a fresh token | GET the condemned-but-present body instead of blind-displacing | DISCRIMINATING |
| PromoteTrustsAdoptedLeafNoProbeManifestTrust | promote of a trusted-manifest (adopted) leaf performs zero HEAD/loadMeta probes, counts `CASBlobAdoptTrusted` once | make promote re-probe an adopted leaf (defeats the manifest-trust optimization) | DISCRIMINATING |
| PromotionAcceptsBothDependencyProofs | promote accepts a manifest mixing Materialized and TrustedManifest leaves in one build | reject a mixed-proof manifest | DISCRIMINATING |
| InvalidDependencyProofFailsClosed | an unrecognized/unnamed dependency-proof enum value aborts ("unnamed dependency proof") rather than silently publishing | accept an unknown enum value as valid | DISCRIMINATING |
| PromoteTrustsAdoptedLeafEvenIfBackendRaced | an adopted leaf whose backend object is deleted between adopt and promote still publishes (manifest-trust trade-off, no re-observe) | re-probe presence at promote and abort on the race (reverts the D4 trade-off) | DISCRIMINATING |
| PromoteSwallowsPostDurableEventSinkFailure | a post-durable event-sink exception does not un-commit or fail the promote; the ref is still resolvable | let a sink failure roll back or fail an already-durable promote | DISCRIMINATING |
| MissingDependencyProofFailsClosed | a leaf with NO recorded dep (neither putBlob nor adoptEvidence) aborts ("no dependency proof") regardless of the pool blob's actual condemnation state | infer a dependency proof from the pool blob's presence instead of requiring an explicit record | DISCRIMINATING |
| PromoteRevalidatesBlobPresenceFailClosed | a manifest naming a never-uploaded blob fails closed at promote (no dep proof); succeeds once the blob is actually uploaded | let promote publish a ref to a leaf that was never actually written | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_write.cpp (part 2 of N) {#src-disks-tests-gtest-cas-part-write-cpp-part-2-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AdoptEvidenceRecordsTrustedManifestProof | adoptEvidence records TrustedManifest for a Blob entry, nothing for an Inline entry | record a proof for an Inline entry too | DISCRIMINATING |
| AbandonRemovesStagedDebrisAndDisables | abandon best-effort-deletes staged manifest debris, spares blob bodies, and disables further ops (`requireAlive` aborts) | leave staged manifest debris behind, or allow further ops after abandon | DISCRIMINATING |
| PublishHappyPathRoundTrip | end-to-end publish: manifest resolves, blob content readable at the located window | corrupt the located offset/length so the wrong bytes are read | DISCRIMINATING |
| PromoteCrossNamespaceManifestFailsClosed | precommitAdd/promote abort when the manifest's namespace differs from the target namespace | drop the namespace-consistency guard | DISCRIMINATING |
| PublishIntoSecondNamespaceSameBlob | the same blob published into two namespaces is uploaded once; the second build adopts (token unchanged) | re-upload on the second namespace's publish instead of adopting | DISCRIMINATING |
| TwoBuildsPublishToSameNamespaceBothLand | two builds publishing distinct refs into one namespace both commit correctly | let the second build's promote be lost/overwrite the first | DISCRIMINATING |
| FirstPublishMakesNamespaceDiscoverable | the first publication admits the namespace to the catalog so it's listable | fail to register the namespace on first publish | DISCRIMINATING |
| AdoptEvidenceRecordsTrustedManifestDependencyProofWithoutIO | adoptEvidence performs ZERO backend HEAD/GET/PUT while still recording TrustedManifest proof | make adoptEvidence probe the backend (defeats the B188 no-I/O contract) | DISCRIMINATING |
| ConvergesUnderProductiveGc | B167/B171 livelock regression: a re-uploaded blob pinned by a live build's precommit edge survives every adversarial GC round pre- and post-promote; the build converges within a bounded round count | remove the build-root precommit edge's in-degree protection, letting GC re-condemn the fresh incarnation forever | DISCRIMINATING |
| PromoteFailsClosedWhenPrecommitNoLongerLiveOwner | promote fails closed (`NETWORK_ERROR`) when the precommit was externally removed before promote (WPromote owner==bld guard) | drop the live-owner check, letting a Δ=0 move re-publish over decremented blobs (dangling-blob bug) | DISCRIMINATING |
| PromoteSucceedsWhenPrecommitIsLiveOwner | the normal case (precommit still live) promotes successfully — positive control for the guard above | reject a valid live-owner promote | DISCRIMINATING |
| PromoteRepointsCommittedRef | `allow_repoint=false` refuses overwriting a committed ref (byte-identical to no-flag behavior); `allow_repoint=true` performs the repoint in one ref-log record and emits exactly one `RefRepoint` event naming the old manifest | let `allow_repoint=false` also silently repoint, or emit zero/duplicate `RefRepoint` events | DISCRIMINATING |
| AbandonAppendsPrecommitRemovalAndKeepsLivePrecommitBody | abandon of a live precommit spares the manifest body (delete-after-sealed-decrements) but removes the exact binding (proven by a fresh precommitAdd succeeding) | writer-delete the live precommit body on abandon | DISCRIMINATING |
| AbandonStillDeletesNeverPrecommittedStagedDebris | abandon DOES delete never-precommitted staged debris, narrowing the skip to exactly the live precommit | spare all staged manifests on abandon, leaking pre-precommit debris forever | DISCRIMINATING |
| AbandonSwallowsThrowingEventSink | a throwing event sink during abandon's 3 audit emits does not turn a successful abandon into a failure; the removal still lands | let a sink exception propagate and abort/re-run the removal | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_write.cpp (part 3 of N) {#src-disks-tests-gtest-cas-part-write-cpp-part-3-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AbandonRetryableAfterAppendFailure | `alive` flips only after the removal append succeeds, so a failed abandon can be retried; after a proven conflict the mount fences and the foreign object remains the sole/greatest occupant | flip `alive=false` before the append (unfixed behavior), turning a retry into "has been abandoned" | DISCRIMINATING |
| ManifestCapEncodedBytesJustUnderStagesSuccessfully | a manifest at exactly one byte under the 256MiB encoded-size cap stages successfully | reject a just-under-cap manifest (over-conservative cap) | DISCRIMINATING |
| ManifestCapEncodedBytesOverThrowsBeforeBodyWrite | a manifest one byte over the cap throws `LIMIT_EXCEEDED` before any object is written (no partial state) | write the body before checking the cap, leaving orphaned debris on rejection | DISCRIMINATING |
| WDepSetCrossAlgoSatisfactionFailsClosed | two entries sharing digest bytes but different hash algos are distinct dependency-set keys; materializing one never satisfies the other | key the dependency set on bare digest instead of `(algo,digest)`, letting cross-algo satisfaction silently promote a missing leaf | DISCRIMINATING |
| AmbiguousTimeoutsThenCommitSucceedsWithinBudget | 2 ambiguous manifest-PUT timeouts (each resolved absent) then a clean commit succeed within the retry budget (exactly 3 attempts) | fail the whole stage on the first ambiguous timeout instead of riding the retry policy | DISCRIMINATING |
| AmbiguousLandedWriteResolvesToCommittedWithoutReissue | an ambiguous-but-actually-landed attempt resolves to Committed without a reissue (exactly 1 attempt), audit event carries the landed token | reissue (duplicate PUT) even when the resolve read proves the first attempt landed | DISCRIMINATING |
| DifferentObjectAtKeyStaysLoudConflict | a foreign object planted at the exact key stays a loud `CORRUPTED_DATA`, never retried or silently adopted | treat a proven-different-object conflict as an ordinary ambiguity and retry it | DISCRIMINATING |
| BudgetExhaustionMapsToNetworkError | total policy exhaustion (all attempts ambiguous) maps to `NETWORK_ERROR`, bounded attempt count, nothing durable named | let the policy retry unboundedly, or misclassify exhaustion as a different error code | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_write.cpp (part 4 of N, file complete: 58 tests) {#src-disks-tests-gtest-cas-part-write-cpp-part-4-of-n-file-complete-58-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AmbiguousTimeoutsThenCommitRestreamsFromSource | 2 ambiguous blob publications + committing 3rd; every reissue RE-STREAMS from the writer's own source (INV-1), never a GET of the failed object | cache/reuse the first stream attempt's bytes instead of re-invoking `source.open` on each retry | DISCRIMINATING |
| AmbiguousLandedWriteAdoptsOccupantWithoutReupload | a landed-but-ack-lost publication is adopted (content identity) with zero reissue, zero re-upload, correct `BlobReuseAdopt` event token | re-upload over an already-landed occupant instead of adopting it | DISCRIMINATING |
| EveryPhysicalPublicationMintsAFreshIncarnationTag | every physical publication attempt mints a distinct `incarnation_tag`, never repeating one across retries | repeat the same incarnation_tag across retries (GC could then condemn a live body) | DISCRIMINATING |
| EnsureBlobPresentSharesOneRetryAcrossItsLoop | each verb of the publication loop binds its own retry window from one policy value; the loop can outlive a single window across iterations | carry one bound across the whole loop instead of rebinding per verb, refusing a legitimate later iteration | DISCRIMINATING |
| DependencyProofIsRefusedAfterARearm | a mid-observation mount trip+rearm refuses the dependency proof (generation mismatch), even though the mount is writable again | let the proof succeed using a stale admission generation, permitting a fenced-out build to commit | DISCRIMINATING |
| AmbiguousNonLandingPublicationStopsAtOuterBound | total publication exhaustion (8 rounds) maps to `NETWORK_ERROR` with "ambiguous" in the message; the writer's bounded retry loop does not re-drive on this error class | let the bounded correctness loop retry unboundedly, or misclassify the exhaustion | DISCRIMINATING |
| AmbiguousCopyLandedAdoptsDestinationWithoutRecopy | a landed-but-ack-lost server-side copy is adopted without a re-copy; destination bytes match the staging object verbatim | re-copy over an already-landed destination | DISCRIMINATING |
| AmbiguousCopyAbsentReattemptsAndCommits | an absent-after-ambiguity copy reissues by retagging and STREAMING (not re-copying) from the still-intact source | reissue by copying again from a source assumed still valid instead of the correct retag-and-stream fallback | DISCRIMINATING |
## src/Disks/tests/gtest_cas_part_write_root_dangle.cpp {#src-disks-tests-gtest-cas-part-write-root-dangle-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| SharedBlobSurvivesSourceDropDuringBuild | a blob adopted (tokenless evidence) by a build's live precommit survives its original owner's retirement + GC-to-fixpoint; the adopting build then promotes and its ref resolves | drop the +1 fold edge for precommit-adopted blobs, letting GC collect P before B promotes | DISCRIMINATING |
| PrematureReclaimCommitFailsClosed | when an adopted leaf's blob is actually gone (simulated premature reclaim), promote still trusts it (§4 manifest-trust, no re-probe) and commits, but `fsck` then reports the dangling reachable-but-absent reference | make fsck's dangling scan miss a committed manifest naming an absent trusted-adopted blob | DISCRIMINATING |
| LivePrecommitNotReclaimed | a live in-flight build's precommit (and its blob) survives a full GC-to-fixpoint run and can still promote | let GC reclaim a live (non-retired) precommit's edge | DISCRIMINATING |

## src/Disks/tests/gtest_cas_pool_meta.cpp {#src-disks-tests-gtest-cas-pool-meta-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AdmitOrValidateEndsAtTheDeadlineUnderPerpetualConflict | `PoolMeta::createOrValidate` under perpetual conflict (concurrent hot-key rewrite) ends at the deadline with exactly `NETWORK_ERROR`, paced by the injected clock (not a spin) | let the deadline arm throw `LOGICAL_ERROR`/`BAD_ARGUMENTS` instead of `NETWORK_ERROR`, or busy-spin without pacing sleeps | DISCRIMINATING |
## src/Disks/tests/gtest_cas_pluggable_hash.cpp {#src-disks-tests-gtest-cas-pluggable-hash-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PoolMetaRoundTripsAlgosUsed | `algos_used`/`blob_header_len` round-trip through encode/decode | drop a field from the codec | DISCRIMINATING |
| CreateOrValidateRecordsConfigAlgoOnFreshPool | fresh-pool creation records the config algo; reopen with same algo is a stable no-op | mint a new algo entry on every reopen instead of matching membership | DISCRIMINATING |
| CreateOrValidateDefaultsToCityHash128 | default algo is CityHash128 | change the default silently | DISCRIMINATING |
| CreateOrValidateFailsClosedOnAlgoMismatchWithoutFlag | reopening with a non-member algo and no flag fails `BAD_ARGUMENTS` naming the flag; pool untouched | admit the new algo without requiring the flag | DISCRIMINATING |
| AdmissionIsFlagGated | new-algo admission requires the flag once; steady-state reopen needs no flag afterward | require the flag on every reopen (over-restrictive) or none at all (under-restrictive) | DISCRIMINATING |
| ConcurrentAdmissionUnions | 3 sequential admissions union into one sorted `algos_used` set, nothing lost | drop/overwrite instead of unioning | DISCRIMINATING |
| ContentWriteBufferLocalModeHashesWithSelectedAlgoXxh3 | streaming write buffer hashes with the SELECTED algo (xxh3), verified to differ from CityHash128 on the same payload | hardcode CityHash128 regardless of selected algo | DISCRIMINATING |
| ContentWriteBufferLocalModeCityHash128Unchanged | default algo path stays byte-identical to the pre-existing convention | change the default hashing convention silently | DISCRIMINATING |
| Xxh3BlobLandsUnderAlgoSegmentAndIsDiscoveredCleanByFsck | an xxh3 blob lands at `/blobs/xxh3/` (not `/ch128/`), readable, fsck finds it reachable via real physical LIST | hardcode the ch128 path segment regardless of algo | DISCRIMINATING |
| Sha256BlobSeenByCondemnSweepAndFsckNotSilentlySkipped | THE CRUX: a 64-hex sha256 blob is actually condemned by the fold and classified `PendingGc` by fsck, not silently skipped as foreign (regression gate, explicit revert-to-`hexToU128` failure mode) | revert the per-algo key parse to fixed-width `hexToU128`, silently treating sha256 keys as foreign debris | DISCRIMINATING |
| Sha256BuildWritesFullWidthDigestAndInlineEqualsBlob | end-to-end sha256 write preserves the full 32-byte digest (non-zero tail bytes), inline==blob hash agreement, clean fsck | truncate the digest to 16 bytes (`BlobDigest::fromU128`) anywhere upstream | DISCRIMINATING |
| StaleAlgoRegistryRefreshOnMiss | a stale per-node admitted-algo cache refreshes exactly once on a miss and then accepts the newly-admitted algo | fail closed forever on a stale cache instead of refresh-on-miss | DISCRIMINATING |
| ForeignAlgoSegmentIsDebrisNotOurs | a genuinely foreign algo segment (`md5/`) is never condemned/deleted by GC and classifies as generic `Unaccounted` in fsck, while the pool's own 2 algos are still classified normally in the same pass | treat an unrecognized algo segment as one of ours (condemn/delete it), or let it blind the fold/fsck to other algos | DISCRIMINATING |
| ReaderGenerationIsRaisedToGBuild | fresh pool stamps `min_reader_generation=G_BUILD`; a forward-dated meta and a backward-dated header both fail closed at open | accept a future or too-old generation stamp | DISCRIMINATING |
| TwoAlgoBlobsBothFullyReclaimed | two blobs of different algos both fold-condemn, graduate, and physically delete together in the same rounds; zero bytes of either algo remain | narrow settlement/graduation/delete to one algo, leaking the other's bytes forever | DISCRIMINATING |
| SameDigestDifferentAlgoDistinctBodiesAndSettlement | two blobs sharing digest bytes but different algos are fully distinct (keys/meta/bodies/in-degree); dropping one ref reclaims only its algo's blob, the other survives intact | key settlement/meta/condemn on bare digest instead of `(algo,digest)`, aliasing the two blobs into one row | DISCRIMINATING |
## src/Disks/tests/gtest_cas_pool.cpp (part 1 of N — 96 tests total, compact rows from here) {#src-disks-tests-gtest-cas-pool-cpp-part-1-of-n-96-tests-total-compact-rows-from-}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ReadOnlyOpenSkipsProbe | read-only reopen performs zero writes | run the probe on read-only open | DISCRIMINATING |
| SkipAccessCheckOpenSkipsProbeButStaysWritable | `skip_access_check` skips probe I/O but mount stays genuinely writable (real publish succeeds) | make skip_access_check silently downgrade to read-only | DISCRIMINATING |
| SkipAccessCheckStillEnforcesSingleAttemptGate | skip_access_check does NOT bypass the single-attempt-support gate | let skip_access_check bypass this gate too | DISCRIMINATING |
| SkipAccessCheckRefusedByBackendFailsTheWritableMount | a backend forbidding skip_access_check refuses the mount with the exact named message | swallow the gate's refusal or lose its message | DISCRIMINATING |
| BackendForbiddingSkipAccessCheckStillOpensWhenTheBatteryRuns | same backend opens fine WITHOUT the flag (isolates the gate's scope) | make the gate fire even when the battery runs | DISCRIMINATING |
| WritableOpenRunsThePoolPreconditionGateBeforeTheBattery | pool-precondition gate refuses before any write (0 writes) | run the battery before this gate | DISCRIMINATING |
| WritableOpenRunsTheSingleAttemptGateBeforeTheBattery | single-attempt gate refuses before any write (0 writes) | run the battery before this gate | DISCRIMINATING |
| WritableOpenWithoutAGateRefusalDoesReachTheBattery | positive control: no gate refusal ⇒ writes > 0 (proves the two zero-write assertions above are real fences) | (none needed — this is the control that makes the two above meaningful) | DISCRIMINATING |
| MinActiveTracksInFlightBuilds | `minActive()` floor tracks in-flight build_seqs exactly, advances on abandon | freeze/mis-advance the floor on abandon | DISCRIMINATING |
| BeginPartWriteSwallowsThrowingEventSink | a throwing sink is contained; build_seq lifecycle (floor/abandon) still correct | let the sink exception propagate out of construction | DISCRIMINATING |
| BuildSeqIsStrictlyMonotone | build_seq never reused, never lower after abandon+new build | reuse a retired build_seq | DISCRIMINATING |
| CASPoolMeta.CreateThenReopen | pool is authoritative on reopen (config ignored); reopen returns original pool_id/header_len | let reopen accept the caller's config over the persisted one | DISCRIMINATING |
| CASPoolMeta.FailClosed | garbage bytes at the meta key fail `CORRUPTED_DATA` | accept garbage as valid meta | DISCRIMINATING |
| CASPoolMeta.RoundTripAndReadability | v3 text form: starts with `{`, contains type name, no legacy binary magic; round-trips pool_id/header_len | reintroduce binary framing or lose a field on round-trip | DISCRIMINATING |
| CASPoolMeta.RejectsBadConstantsAtCreation | mis-aligned/too-low/too-high `blob_header_len` all rejected at creation; nothing written on failure | accept an out-of-range header_len, or leave partial state on rejection | DISCRIMINATING |
| CASPoolMeta.RejectsBadConstantsOnDecode | a persisted mis-aligned header_len is rejected on decode too (not just at creation) | validate only at creation, trusting decoded bytes blindly | DISCRIMINATING |
| CASPoolMeta.DecodeGarbageFails | garbage/empty bytes fail `CORRUPTED_DATA` at the bare decode level | accept malformed bytes | DISCRIMINATING |
| CASPoolMeta.ConcurrentCreateRace | a racing creator's foreign pool_id/constants win; loser never overwrites | let the loser overwrite the winner's meta | DISCRIMINATING |
| CASPoolMeta.CasConflictReReadsWinner | the create-if-absent-conflict path re-reads and returns the winner's identity, not `LOGICAL_ERROR` | throw `LOGICAL_ERROR` on a legitimate lost race instead of re-reading | DISCRIMINATING |
| OpenFailsClosedOnNonEnforcingBackend | a backend that cannot enforce tokens refuses to open (`NOT_IMPLEMENTED`) | open successfully on a non-enforcing backend | DISCRIMINATING |
| OpenCreatesPoolMetaAndReopens | two concurrent server-root opens share the SAME pool_id (pool-meta is pool-authoritative) | let each open mint its own independent pool_id | DISCRIMINATING |
| OpenWithExplicitConstantsCreatesThem | config's `blob_header_len` applies at first creation | ignore the config's header_len on creation | DISCRIMINATING |
| VerbatimFilesLifecycle | namespace files: put/get/list/absent/overwrite all behave correctly | let overwrite silently fail, or list include absent entries | DISCRIMINATING |
| ListNamespaceFilesEmpty | a fresh namespace lists zero files | return a non-empty list for a namespace with none | DISCRIMINATING |
| ResolveReturnsManifestId | resolve→readManifest→locate round trip: correct offset/length window, Inline entries have no location | wrong offset/length, or allow `locate` on an Inline entry | DISCRIMINATING |
| ReadManifestValidatesBodyAndFailsClosed | readManifest fail-closes on namespace mismatch, ref mismatch, and absent body (3 distinct CORRUPTED_DATA/FILE_DOESNT_EXIST cases) | drop the `refMatchesBody`/`manifestNamespaceMatches` checks | DISCRIMINATING |
| LookupAndListOverManifestEntries | findEntry/entryRange correctness over canonical multi-directory entries | off-by-one the range, or wrong canonical ordering | DISCRIMINATING |
| ManifestCacheIsKeyedById | manifest decode cache keyed by id alone: 2nd read of same id costs 0 GETs/HEADs; a fresh publish (new id) misses | key the cache by ref-name instead of id (stale content risk) | DISCRIMINATING |
| ManifestDecodeCacheIsByteBounded | the decode cache is byte-bounded (LRU eviction under a tight byte budget), all reads stay correct after eviction | make the cache count-bounded (unbounded byte growth) or serve wrong content post-eviction | DISCRIMINATING |
| ResolveDecodeCacheInvalidatesOnWrite | resolve's decode cache invalidates on a shard-token-changing write (drop), never serves stale content | serve a stale cached resolve after a drop | DISCRIMINATING |
| ResolveAbsentRefAndAbsentNamespace | absent shard/ref/namespace resolves to nullopt/empty, not an error | throw instead of returning nullopt on absence | DISCRIMINATING |
| ListRefsMergesAllShards | listRefs returns every committed ref from one multi-owner transaction, correct ids | drop/duplicate an entry | DISCRIMINATING |
| ListRefsEmptyNamespaceCostsZeroListsAndHeads | recovery of an empty-but-existing namespace costs 0 LIST/HEAD (checkpoint-grounded) | make recovery LIST/HEAD the ref stream unnecessarily | DISCRIMINATING |
| ListRefsOnANeverBornNamespaceCostsNoListAndNoHead | a never-born namespace costs 0 LIST/HEAD but ≥1 GET (answer from catalog); positive control proves the zeros are real | make the catalog answer come from a no-op instead of a real GET | DISCRIMINATING |
| ListRefsReturnsSameContentAsBefore | multi-ref table correctness from a raw multi-op fixture | drop/misname a ref | DISCRIMINATING |
| ListRefsSkipsForeignKeys | a stray non-`_log`/`_snap` key under the stream prefix is skipped defensively, never throws | let a foreign key crash/corrupt listRefs | DISCRIMINATING |
| ReadManifestFailsClosed | garbage body ⇒ CORRUPTED_DATA; absent body ⇒ FILE_DOESNT_EXIST | swallow either failure mode | DISCRIMINATING |
| DropRefAppendsJournalAtomically | drop is atomic (visible only once durable); dropping a missing ref fails closed, never a silent no-op | let dropRef on a missing ref silently succeed | DISCRIMINATING |
## src/Disks/tests/gtest_cas_pool.cpp (part 2 of N) {#src-disks-tests-gtest-cas-pool-cpp-part-2-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| UpdateRefPublishedAtUpdatesPublishedAtMs | `published_at_ms` updates apply and stack; manifest_ref unchanged | let the update silently fail or change the manifest edge | DISCRIMINATING |
| DropNamespaceRemovesEveryOwnerButLeavesFilesForGc | dropNamespace removes every ref logically but performs NO physical deletion (files survive), idempotent, subsequent mutation refused with typed retry-later | physically delete files on dropNamespace (loses GC's authority over reclamation timing) | DISCRIMINATING |
| ListNamespacesFromCatalog | listNamespaces projects logical names from the catalog with correct prefix filtering | list from physical keys instead of the catalog | DISCRIMINATING |
| ListNamespacesDoesNotMintLogicalNamesFromFileKeys | physical namespace-file keys alone cannot mint a catalog-absent logical name | derive a logical name from a bare file key | DISCRIMINATING |
| ListNamespacesDoesNotTreatPhysicalDebrisAsCatalogAuthority | un-incarnated/malformed keys are skipped per-key, not fatal to the whole enumeration; a healthy namespace still lists | let one bad key abort the whole listing, or wrongly attribute debris | DISCRIMINATING |
| ListMirroredChildren | listMirroredChildren returns correct catalog-derived child names under a prefix | derive children from physical keys instead of the catalog | DISCRIMINATING |
| OpenRecoversFromFenceInAdoptWindowWithFreshEpoch | a fence tripped inside the keeper-adopt window is recovered by open with a fresh, higher writer_epoch, unfenced (exit-49 S13 regression) | let the fenced-adopt race wedge open instead of retrying | DISCRIMINATING |
| WriteFenceUsesInjectedBootClock | mayMutate flips false exactly when the injected boot clock crosses the TTL deadline | ignore the injected clock, using real time (untestable) or wrong boundary | DISCRIMINATING |
| FenceOutThenSelfRemountRestoresWrites | a GC fence-out is recovered by self-remount: epoch bumps, gc_fenced clears, renewal works again | fail to bump the epoch or clear gc_fenced on remount | DISCRIMINATING |
| OldEpochBuildFailsClosedAfterRemount | a build minted under a superseded incarnation fails closed after remount; a fresh build under the live incarnation works | let a stale-epoch build's write silently succeed post-remount | DISCRIMINATING |
| ForeignOwnerIsNeverTakenOver | a genuinely foreign mount occupant is never taken over at remount OR release; release survives (no abort) and reports the exclusivity violation, leaving the slot byte-identical | take over a foreign occupant, or abort the process on release instead of reporting and surviving | DISCRIMINATING |
| ShutdownGuardRefusesToArmRemount | `scheduleRemount` refuses to latch new recovery work once teardown has begun | let scheduleRemount arm after shutdown starts | DISCRIMINATING |
| RemountArmAnchorsAtClaimAttemptNotResponseTime | the remount fence arm anchors at the pre-I/O claim-attempt boot reading, not a later response-time reading (sequenced-clock discriminator distinguishes the two exact call orders) | anchor the arm at the later `bootMsNow()` call taken after `keeperStart` (the pre-fix bug) | DISCRIMINATING |
## src/Disks/tests/gtest_cas_pool.cpp (part 3 of N) {#src-disks-tests-gtest-cas-pool-cpp-part-3-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| SameRuntimeReentrantForeignConflictSinkCannotReplaceTerminalOutcome | a diagnostic sink that reenters the same runtime mid-terminalization cannot replace the ABORTED foreign-conflict outcome; the terminal state (TransientNotLive, mayMutate=false) and teardown byte-identity hold regardless | let a reentrant sink call override/clear the terminal outcome | DISCRIMINATING |
| ThrowingForeignConflictSinkCannotReplaceTerminalOutcome | same guarantee when the sink instead throws | let a throwing sink corrupt/skip the terminal classification | DISCRIMINATING |
| CleanStopDrainsAndWritesFarewell | `~Pool` with nothing in-flight writes the farewell marker (`min_active_build_sequence=UINT64_MAX`) | skip the farewell write on a clean drain | DISCRIMINATING |
| UnresolvedWedgeSkipsFarewell | an unresolved ref-lane wedge at teardown skips the farewell marker; a successor claim then correctly reports `LiveDoubleStart` | write the farewell marker even with an unresolved wedge (false certificate of death) | DISCRIMINATING |
| UncleanOpenPaysOnlyTheObservationWindow | an uncertified-dead predecessor's reclaim pays exactly the observation window (≥TTL) and nothing more (no wait > TTL) | reintroduce an extra grace period beyond the observation window | DISCRIMINATING |
| CleanOpenSkipsAllWaits | a cleanly-farewelled predecessor's reclaim pays zero waits | make a clean reclaim still poll/wait | DISCRIMINATING |
| FencedPriorReclaimsWithoutAnyWait | a GC-fenced (certified-dead) predecessor reclaims on the first attempt, zero waits | poll/wait even on a certified-dead fenced prior | DISCRIMINATING |
| StartupArmRedoesLeaseWriteWhenTheClaimConsumesTtl | a claim whose own I/O outlives the TTL forces exactly one fresh re-anchoring lease write before the fence arms | arm the fence from a stale (already-expired) anchor without the redo write | DISCRIMINATING |
| DrainedRemountPaysNoWait | a drained self-remount (no in-flight wedge) pays zero waits | reintroduce a materialization-grace wait on remount | DISCRIMINATING |
| UnresolvedWedgeRemountPaysNoWaitEither | even a still-wedged ref lane's self-remount pays zero waits (straggler fenced by the recovery seal instead) | make remount block waiting for the wedge to resolve | DISCRIMINATING |
| ALateTouchedTableClosesEveryDeadEpochInBandHoweverItsPredecessorsDied | a namespace touched for the first time strictly after 2 remounts closes BOTH dead epochs in-band (exact seal keys), no synthetic seal snapshot, regardless of how predecessors died | seal only epochs whose mount died uncleanly, leaving a gap a reader can't distinguish from data loss | DISCRIMINATING |
| ReadManifestSharedReturnsSharedDecodeWithoutCopy | `readManifestShared` returns the identical shared decode pointer on a second call, one GET total, zero HEAD | copy the decode on each call, or issue a redundant GET/HEAD | DISCRIMINATING |
## src/Disks/tests/gtest_cas_pool.cpp (part 4 of N) {#src-disks-tests-gtest-cas-pool-cpp-part-4-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ReadManifestAbsentBodyEmitsReadMissingWithOneGetAndNoHead | absent-body readManifest emits exactly one `ReadMissing` event (correct kind/code/site), one GET, zero HEAD | drop the event, or issue an extra HEAD | DISCRIMINATING |
| StaleSnapshotServesCachedManifestAndBlobAbsenceSurfacesOnRead | a reader holding a stale decode after GC removal sees a snapshot-consistent manifest with no new request; blob absence surfaces only on the actual blob read, never masked | mask the blob absence at the manifest layer instead of surfacing it on read | DISCRIMINATING |
| CachedSourceDecodeLetsAdoptionCommitAnAbsentBlobThatFsckReports | documents/pins the exact out-of-band-deletion edge case: adoption from a cached decode commits over an absent blob (zero probe by design), and fsck's dangling scan catches it | let fsck fail to classify the adopted-but-absent blob as dangling | DISCRIMINATING |
| DirectRenewCannotRaceWorkerStartOrKeeperReplacement | while a direct renewal holds the driver lock, worker-start/keeper-install/keeper-reset are all rejected (state-machine serialization) | allow a concurrent worker-start/replace during an in-flight direct renewal | DISCRIMINATING |
| DueWorkerAdmissionIsReservedBeforeParkRequest | admission is reserved (state=ParkRequested→Parked) strictly before the replacement callback runs, proven by barrier ordering | let the replacement run before admission is reserved (race) | DISCRIMINATING |
| DueWorkerAdmissionIsReservedBeforeStop | same reservation-before-stop ordering, ends in `Dormant` | let stop() proceed before admission finishes | DISCRIMINATING |
| DirectRenewIsRefusedForBackgroundConfiguredRuntimeAfterStop | a direct renew call is refused once background workers have stopped | allow a direct renew to sneak through after stop | DISCRIMINATING |
| RemountWaitsForRenewalParkedBeforeReplacement | remount's replacement callback runs 0 times until renewal has actually parked (barrier-proven ordering) | run the replacement callback before renewal parks | DISCRIMINATING |
| TeardownJoinsBothWorkersBeforeRelease | teardown joins BOTH persistent workers (exactly 2 exits) before writing the farewell marker | release the mount slot before both workers actually joined | DISCRIMINATING |
| NaturalTerminalTransitionMakesBothPersistentWorkersSelfExit | a natural terminal transition (IdentityLost/Vanished) makes both workers self-exit WITHOUT an explicit stop call | leave a worker running/hung after a natural terminal transition | DISCRIMINATING |
| ParkedRenewalCannotMissNaturalTerminalPublication | a precisely orchestrated race (renewal parked-predicate-false vs terminal publication) never lets terminal publication proceed while renewal holds the driver mutex — proven false by an explicit `violated_serialization` flag | let terminal publication race past the driver lock while renewal holds it | DISCRIMINATING |
| VanishedReasonPreparationFailureLeavesTerminalTransitionRetryable | a failed vanished-reason-preparation call leaves the transition NOT published and retryable; a later successful call publishes correctly and a subsequent conflicting call is ignored | publish a half-prepared vanished transition on the first (failed) attempt | DISCRIMINATING |
| WorkerConstructionRollbackFailsOpenClosed | a worker-construction failure at either factory call rolls back to `mayMutate()=false`, no workers running | leave `mayMutate()=true` after a failed worker construction | DISCRIMINATING |
| ExternalLossDuringRenewalUsesOneRecoveryGeneration | an external lease loss during an in-flight renewal drives exactly ONE remount call/one fresh epoch, correctly tracked generation, and increments the lease-lost counter once | trigger a duplicate remount for one external loss event | DISCRIMINATING |
## src/Disks/tests/gtest_cas_pool.cpp (part 5 of N — file complete) {#src-disks-tests-gtest-cas-pool-cpp-part-5-of-n-file-complete}

| test | property | killing mutation | verdict |
|---|---|---|---|
| TerminalDepositionDoesNotTouchKeeperAfterReplacement | after a keeper replacement, terminal deposition from the OLD generation must not touch the new keeper | let a stale-generation terminal deposit clobber the replaced keeper | DISCRIMINATING |
| ConcurrentRemountRequestIsProcessedAfterActiveGeneration | a second `scheduleRemount` while one is in flight is queued and processed after (2 calls, generation=2) | drop/coalesce the second remount request incorrectly | DISCRIMINATING |
| ImmediatePostRemountRenewalFailureIsNotDropped | a renewal failure occurring immediately after a successful remount is not dropped/lost — a second remount call catches it | swallow the immediate post-remount failure | DISCRIMINATING |
| StaleRemountAnchorPerformsParkedRedo | a stale-anchor remount performs the extra re-anchoring redo write (≥3 conditional writes) | skip the redo, arming from a stale anchor | DISCRIMINATING |
| ParkedRedoRecoveryObservabilityPrecedesRemountResult | the "recovered" renewal event is emitted strictly before the final "ok" remount event, with matching attempt numbers/attempts_sent/classification | emit events out of order or with mismatched attempt correlation | DISCRIMINATING |
| ParkedRedoFailureObservabilityPrecedesRemountResult | same ordering guarantee for the failure path ("failed" renewal before "failed" remount) | emit the failure events out of order | DISCRIMINATING |
| ThrowingEventSinkAfterCommitLeavesRuntimeLive | a throwing event sink AFTER a committed remount does not undo the commit (still Live, mayMutate) | let a post-commit sink exception roll back the remount | DISCRIMINATING |
| PreSendCancellationAllowsFarewellButAmbiguityDoesNot | a pre-send cancellation still allows the farewell marker; a genuine send ambiguity does not (two distinct end states) | write the farewell marker even after a genuine ambiguous send | DISCRIMINATING |
| DirectAndStartupTerminalFailuresRethrowTypedExceptions | both direct and startup renewal paths rethrow `NETWORK_ERROR` for both pre-attempt-deadline and refused-after-send refusal shapes (4 combinations) | swallow or misclassify one of the 4 refusal shapes | DISCRIMINATING |
| BackgroundCadenceMustFitLeaseBeforeWritablePublication | an invalid renewal cadence/TTL combo refuses to open with ZERO writes | let an invalid cadence still write before refusing | DISCRIMINATING |
| DecommissionCadenceValidationPrecedesAuthorityWrites | same cadence validation for decommission-mode open, zero writes on refusal | skip cadence validation for decommission mode | DISCRIMINATING |
| DisabledBackgroundDoesNotReserveRenewalCadence | disabled background worker cadence doesn't force a synchronous startup redo (exactly 1 conditional write) | force an extra redo write even with background disabled | DISCRIMINATING |
| DeterministicWorkerFailureFencesWithoutWaitingForCadence | a deterministic renewal failure fences immediately (TransientNotLive) without waiting for the next cadence tick | let the worker wait a full cadence period before fencing | DISCRIMINATING |
| RenewWatermarkOnceRefreshesFenceAndDepositsOneFailure | direct renew success refreshes the local fence from attempt-start time; a later terminal failure schedules exactly one remount | double-schedule remount, or fail to refresh the fence on success | DISCRIMINATING |
| WholeChainResultsAreNumberedAndStepLabelled | remount attempts are numbered sequentially and step-labelled (`pool_identity_probe`/`publish_live`), counters match exactly, log line count matches | mislabel the step or miscount the profile events | DISCRIMINATING |
| TheKeeperRedoRenewsOnTheOpenPlane | a remount whose quiescence consumed most of the lease costs one extra conditional lease write (the keeper-redo, on the open plane not the mount plane) vs. a fresh-anchor remount | admit the redo on the (fenced) mount plane instead of the open plane, making every such remount fail | DISCRIMINATING |
| LeaseLossHasOneOperationalOwner | multiple `tripMountLost` calls plus a failed remount plus a post-shutdown trip count exactly ONE operational lease-loss event | double-count lease loss across the multiple trip calls | DISCRIMINATING |
| LiveForgetDoesNotCountOperationalLeaseLoss | a deliberate `forgetDisk` terminal transition does NOT increment the operational lease-loss counter | miscount a deliberate decommission as an operational lease loss | DISCRIMINATING |
| MountpointObjectRoundTrip | put/get/exists/remove round-trip correctly for the mountpoint probe object, including absent-before/after states | let remove leave the object readable, or put fail to make it visible | DISCRIMINATING |

Summary for gtest_cas_pool.cpp: 96 tests, all DISCRIMINATING. WEAK 0, TAUTOLOGICAL 0, UNSURE 0.
## src/Disks/tests/gtest_cas_probe.cpp {#src-disks-tests-gtest-cas-probe-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PassesOnEnforcingBackend | probe passes and cleans up its own keys on a token-enforcing backend | leave probe debris behind | DISCRIMINATING |
| FailsClosedOnNonEnforcingDelete | probe throws on a backend that doesn't enforce delete tokens (MinIO-OSS failure mode) | let a non-enforcing backend pass the probe | DISCRIMINATING |
| FailsClosedOnDeleteMarkers | probe throws when delete markers are simulated (bucket versioning) | let versioned-delete semantics pass | DISCRIMINATING |
| PassesOnEmulatedLocal | probe passes against a real emulated-single-process object storage | break the probe against the real emulation | DISCRIMINATING |
| ConcurrentMountsDoNotCollide | two concurrent mounts (per-mount unique probe prefixes) never collide, even with a legacy fixed-key artifact present | revert to a fixed probe key, colliding under concurrent mounts | DISCRIMINATING |
| FailsClosedOnUnsupportedSingleAttemptProfile | Native mode over a non-single-attempt backend refuses; EmulatedSingleProcess is exempt | let Native mode silently proceed without single-attempt support | DISCRIMINATING |
| ExactDeleteBatteryDetectsMissingGenerationMode | the probe's delete battery rejects BOTH degenerate GCS-generation failure modes (ignores token / rejects token) | weaken the delete battery to miss either failure mode | DISCRIMINATING |
| ReorderedProbePassesOnAllThreeDialects | every dialect's real backend-minted preconditions reach the store (exact write/remove counts: 4/2), never locally gated as foreign | reintroduce a synthesized cross-dialect precondition that gets locally gated | DISCRIMINATING |

## src/Disks/tests/gtest_cas_promote_republish.cpp {#src-disks-tests-gtest-cas-promote-republish-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PromoteOverDifferentCommittedRefFailsClosed | promoting different content onto an already-committed ref fails closed (`NETWORK_ERROR`), not a silent overwrite (BUG 1a) | let promote silently overwrite a committed ref, orphaning the old manifest | DISCRIMINATING |
| PromoteSameManifestIsIdempotent | re-promoting the SAME manifest_ref onto its own committed ref is a no-throw no-op | make the fix's guard reject same-manifest re-promotes too | DISCRIMINATING |
| PromoteOverAbsentRefSucceeds | promote over an absent ref (normal insert) always succeeds unconditionally | over-tighten the guard to also refuse fresh inserts | DISCRIMINATING |
| RepublishReDriveOverCommittedDstIsIdempotent | a republishRef re-drive over a dst already committed with IDENTICAL content is idempotent: same ManifestId, no fresh mint, src dropped (BUG 1c) | mint a fresh manifest on re-drive, orphaning the first attempt | DISCRIMINATING |
| RepublishReDriveOverDifferentContentDstFailsClosed | a re-drive where dst holds genuinely DIFFERENT content fails closed (`ABORTED`), src left intact | silently overwrite dst's different content, or silently drop src | DISCRIMINATING |
| AbandonEmitsRemovalBeforeRetire(+Aborts) | A3 mint-tightening: a foreign transaction (that never staged the id) is refused precommitAdd unconditionally, even for an id abandon() legitimately dropped | let precommitAdd succeed for an id a different transaction staged | DISCRIMINATING |
| PrecommitAddRejectsAnIdThisTxnDidNotStage(+Aborts) | the simplest A3 case: a transaction that never staged an id at all is refused; nothing appended, ref stays ownerless | allow re-owning an id from a foreign never-staging transaction (ABA risk for relink confirm) | DISCRIMINATING |
## src/Disks/tests/gtest_cas_protocol_scenarios.cpp {#src-disks-tests-gtest-cas-protocol-scenarios-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| FenceConflictCondemnedTokenedBlobCommitsWithTokenUnchanged | a materialized leaf condemned in the putBlob→promote window is edge-protected: promote commits with the token UNCHANGED (no re-upload) | make promote re-validate/re-upload a materialized leaf | DISCRIMINATING |
| RevalidateReObservesStaleTokenKeepsWhenUnchanged | an adopted materialized leaf with an empty retire set commits in place, token unchanged, no rewrite | rewrite/displace a leaf that was never condemned | DISCRIMINATING |
| RevalidateReObservesStaleTokenAdoptsWhenDisplaced | a leaf displaced out-of-band to t1 still commits correctly (content-addressed by hash); old token t0 is dead (`Mismatch` on delete) | commit against the stale t0 instead of the live t1 | DISCRIMINATING |
| RevalidateAdoptsLiveTokenWhenOnlyPhantomCondemnedAtDifferentToken | a phantom condemnation of a DIFFERENT token never touches the live t0; commit keeps t0 | let a phantom-token condemnation wrongly displace the live incarnation | DISCRIMINATING |
| EvidenceHitCondemnedPresentBlobCopiesForwardInClosure | a trusted-manifest (adopted) leaf whose hash is condemned-but-present is never probed/displaced; token and meta state stay exactly as they were | probe or flip the meta of a trusted-manifest leaf | DISCRIMINATING |
| WedgedHeartbeatCondemnedTokenedBlobCommitsWithTokenUnchanged | a build's own materialized upload condemned by full GC before owner-removal still edge-protects and commits unchanged | re-validate a materialized leaf in this false-positive-freeze window | DISCRIMINATING |
| AbandonLeavesDebrisAndDisables | abandon leaves blob+manifest debris (once precommitted), no owner transition, further ops abort | delete the durable-precommit-edge debris on abandon (premature reclaim risk) | DISCRIMINATING |
| DropReattachThroughDetachedNamespace | a full detach/reattach round trip never re-uploads the blob (stable token throughout) | re-upload the blob on any leg of the detach/reattach | DISCRIMINATING |
| FreezeIntoShadowNamespace | a shadow (freeze) ref outlives the dropped live ref and still reads | let the shadow ref become unreadable after the live ref drops | DISCRIMINATING |
| DisplacedToLiveTokenCommitsAtCurrentIncarnation | a leaf displaced to a live (uncondemned) t1 commits at t1; old condemned t0 TokenMismatches on delete | commit against the wrong (condemned) token | DISCRIMINATING |
| NewNamespacePublishGatedByShardFenceFloor | (documented D4 trade-off) an adopted leaf trusted at promote can commit over a blob GC genuinely deleted before precommit; fsck's dangling scan catches it | let fsck fail to detect this documented dangle | DISCRIMINATING |
| FreshEvidenceDepWithViewHitIsResolvedByGate | a trusted adopted leaf condemned-but-present commits with token unchanged, no probe | probe/displace a trusted leaf against the (irrelevant) condemned meta | DISCRIMINATING |
| AdoptedLeafCarriesRealBlobSize | an adopted leaf's `blob_size` matches the original non-zero size (B92 regression) | let adoptEvidence zero out blob_size | DISCRIMINATING |
| DISABLED_RevalidateAbsentTreeDepRecreates | (obsolete tree-model scenario, retained as a documentation stub) | none — `GTEST_SKIP()` unconditionally skips; no production change can ever turn this red | TAUTOLOGICAL (never executes an assertion) |
| DISABLED_AdoptTreeOfReclaimedTreeFailsClosedAtAdoptTime | same — documentation stub for an obsolete tree-model behavior | none — unconditional `GTEST_SKIP()` | TAUTOLOGICAL (never executes an assertion) |
## src/Disks/tests/gtest_cas_rebuild_condemn_nothing.cpp {#src-disks-tests-gtest-cas-rebuild-condemn-nothing-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| HiddenLiveManifestBlobIsNotCondemned | r5-finding-4 regression: a blob whose owning ref-log record and manifest are hidden from LIST but exist is recovered via the exact checkpoint frontier, not condemned by the lying listing | revert to LIST-driven rebuild discovery, condemning the hidden-but-live blob | DISCRIMINATING |
| OrphanBlobIsRetainedNotCondemned | a blob no manifest names anywhere is retained (not condemned) by rebuild — the named R4 residual | let rebuild guess-condemn a manifest-less blob | DISCRIMINATING |
| NonCanonicalLifeKeyDoesNotAbortTheRebuild | a malformed non-canonical stream key doesn't abort the whole rebuild; the live namespace still folds | let one bad key throw and abort the disaster-recovery command | DISCRIMINATING |
| NestedLifelessKeyUnderTheLifePrefixDoesNotAbortTheRebuild | same resilience via the OTHER code path (health-check's `groupRefKeys`), un-forced rebuild still runs | let the health-check's grouping throw on the nested malformed key | DISCRIMINATING |
| OneCatalogCutDrivesHealthCheckAndRebuild | exactly ONE catalog read serves both the health check and the rebuild (proven via a mutation-on-second-read fixture) | re-read the catalog a second time, exposing a race window | DISCRIMINATING |
| CarriesHoldsVerbatimWhileCondemningNothing | a prior seal's hold carries through the rebuild byte-for-byte unchanged, alongside condemning nothing | drop or rewrite a hold during rebuild | DISCRIMINATING |
| HealthyArithmeticPoolIsClean | a healthy pool reports zero chain_broken/unchecked, correct walked count | report `unchecked` for a provably healthy namespace | DISCRIMINATING |
| MidChainHoleBelowAWitnessIsChainBroken | a 404 below a durable same-epoch id is fatal `chain_broken`, appears in the summary, unclean | let a mid-chain hole silently pass as clean | DISCRIMINATING |
| TailAboveTheCheckpointIsWalkedNotUnchecked | the tail above the checkpoint is walked by exact key (not LIST), correctly proven, not `unchecked` | derive the tail from a LIST instead of exact-key walk, missing hidden records | DISCRIMINATING |
| SealedStreamIsWalkedAcrossTheEpochBoundary | a real epoch-seal crossing is proven via the back-chain, not guessed by `epoch+1` | guess the epoch boundary instead of following the seal back-chain | DISCRIMINATING |
| ExactFrontierMakesAnUnsealedEpochCrossingChainBroken | a claimed-but-unproven epoch crossing (ordinary record misclaimed as a seal) is a hard chain break with exact evidence in the message; a healthy sibling namespace unaffected | accept an ordinary record as a valid epoch-closing seal | DISCRIMINATING |
| OneBadNamespaceDoesNotAbortTheAudit | one namespace's chain break doesn't abort the scan of namespaces sorted after it | let one bad namespace's exception wedge the whole fsck scan | DISCRIMINATING |
## src/Disks/tests/gtest_cas_record_stream_format.cpp {#src-disks-tests-gtest-cas-record-stream-format-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RunFile (battery, golden bytes) | encode produces the exact pinned header/record/trailer byte layout | reorder/rename a wire field | DISCRIMINATING |
| EmptyRunRoundTripsAndChecksumMatches | zero records encode to the exact header+`{"n":0}` bytes; reader accumulated hash matches the writer-side checksum helper | diverge the read-side and write-side checksum accumulation | DISCRIMINATING |
| EdgeZeroCondemnedRoundTrip | all three marker kinds (Edge/Zero/Condemned) round-trip their full field sets correctly, sorted by (ref, source_id) | swap/drop a field of the Condemned row on decode | DISCRIMINATING |
| ClosedSetPinsRunMarkerWords | encode/decode `RunMarker` word tables are the same table (round-trips every enumerator) | let the two directions diverge for one marker | DISCRIMINATING |
| CondemnedRowMissingOneOfItsSixFieldsFailsClosed | each of the 6 condemned-only fields is mandatory; dropping any one fails closed with the exact message | silently default a missing condemned field instead of failing closed | DISCRIMINATING |
| ActiveRowCarryingACondemnedFieldFailsClosed | an edge/zero row carrying a condemned-only field fails closed (rows can't mix identities) | let a mixed row decode by picking one half | DISCRIMINATING |
| WriterIsByteDeterministic | `encodeRun` is a deterministic pure function of its already-sorted input | introduce hidden nondeterministic state into the writer | WEAK — literal `EXPECT_EQ(f(x), f(x))` self-comparison with no order-shuffle or alternate-input check; holds trivially for any deterministic implementation, correct or buggy, so the realistic space of regressions it catches is narrow (accidental nondeterminism only) |
| EveryBlobHashAlgoRoundTripsThroughTheRunRefByte | every `BlobHashAlgo` enumerator round-trips through the leading algo byte | drop/misassign one algo's byte mapping | DISCRIMINATING |
| SortOrderAcrossAlgosFollowsAlgoByte | cross-algo records sort/accept in algo-byte order (ch128 < xxh3 < sha256) | reorder the algo byte assignment, breaking sort compatibility | DISCRIMINATING |
| AppendOutOfOrderThrows | the writer aborts on an out-of-order append (ref regression) | remove the ordering assertion in the writer | DISCRIMINATING |
| SourceIdRendersAs32Hex | source_id and ref render as fixed-width lowercase hex | render a variable-width or uppercase encoding | DISCRIMINATING |
| SealChecksumMismatchFailsClosed | a single-byte flip changes the checksum and fails verification; a correct checksum passes | skip or weaken the checksum verification | DISCRIMINATING |
| TrailerCountMismatchIsCorruptData | a wrong trailer record count throws | trust the trailer count without cross-checking actual records | DISCRIMINATING |
| UppercaseDigestInRecordKeyIsCorruptedData | an uppercase hex digit in the digest fails closed | accept mixed-case hex | DISCRIMINATING |
| TruncationAtLineBoundaryFailsClosed | a missing trailer line (truncation) throws | tolerate a missing trailer | DISCRIMINATING |
| HeaderGates | wrong type/kind/future-version/out-of-range version all fail with the correct distinct codes | accept a mismatched type/kind, or let a future version silently narrow | DISCRIMINATING |
## src/Disks/tests/gtest_cas_recovery_grounding.cpp {#src-disks-tests-gtest-cas-recovery-grounding-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| CreatingAndAbsentCatalogEntriesAreNotRecovered | `Creating`/absent catalog entries refuse recovery (`INVALID_STATE`) | let a Creating entry be recovered as if Live | DISCRIMINATING |
| LiveAndRemovingRequireCheckpointAndLifeEpoch | Live/Removing without a checkpoint fail `CORRUPTED_DATA` | recover without a checkpoint present | DISCRIMINATING |
| MissingFrontierMeansNoCommittedTransaction | absent `committed_through` yields no base/no committed_through in the grounding | invent a base when the frontier is absent | DISCRIMINATING |
| ChoosesCheckpointBaseAndArithmeticWalkStart | base/walk_from/committed_through derived correctly (walk_from = base+1) | off-by-one the walk start | DISCRIMINATING |
| BaseAtFrontierStillStartsAtItsExactSuccessor | base==frontier still yields the correct (empty-tail) successor slot | fail to compute walk_from when the tail is empty | DISCRIMINATING |
| WalksFromLifeEpochWithoutCheckpointBase | absent checkpoint base walks from the life's own epoch start (`{epoch,1}`) | walk from the wrong epoch | DISCRIMINATING |
| RejectsBaseWithoutARepresentableSuccessor | a base at `UINT64_MAX` (unrepresentable successor) fails closed | let an overflowing successor silently wrap | DISCRIMINATING |
| RejectsCheckpointFieldsAboveCommittedFrontier | checkpoint fields (base, seal) above the frontier fail closed (2 cases) | accept an incoherent checkpoint above its own frontier | DISCRIMINATING |
| RejectsIncoherentEpochBoundaryInCheckpointAuthority | 3 distinct epoch-boundary incoherence shapes all fail closed | accept any of the 3 incoherent epoch shapes | DISCRIMINATING |
| RejectsLifeEpochAboveCommittedFrontierOnDecodeAndGrounding | life_epoch above the frontier fails BOTH at decode and at the pure grounding function (would otherwise silently return empty) | let only one of the two layers catch it | DISCRIMINATING |
| RecoveryIsEquivalentUnderFullEmptyPartialAndReorderedList | recovery output is byte-identical across 4 different LIST distortions; exactly 3 exact log GETs, 0 snapshot GETs, 0 LIST calls | let recovery depend on LIST enumeration instead of exact-key arithmetic | DISCRIMINATING |
| CatalogLifecycleAndCheckpointAreMandatoryForReadOnlyRecovery | 5 distinct missing-authority shapes (no catalog, no ckpt, malformed ckpt, Creating, no entry) all fail closed with correct codes | let any of the 5 succeed without its mandatory authority | DISCRIMINATING |
| NonrecoverableAuthorityPerformsNoBackendRecoveryIo | 3 refusal shapes (Creating, no ckpt, no entry) each perform ZERO backend LIST/GET before refusing | perform speculative I/O before checking authority validity | DISCRIMINATING |
| ReadOnlyRecoveryNeverAdoptsFPlusOne | read-only recovery never includes a durable-but-uncommitted transaction one past the frontier | let recovery read past its own frontier | DISCRIMINATING |
| ForgedWellFormedListedSnapshotIsUnobservedAndRecoveryDoesNotList | a well-formed but non-checkpoint-named snapshot is never read/listed under any of 4 listing modes | fall back to LIST-discovered snapshots when the checkpoint names none | DISCRIMINATING |
| SemanticallyMalformedCheckpointSnapshotIsCorruptionAfterExactRead | a checkpoint-named malformed snapshot fails closed after exactly one exact GET, correct error substring | fall back to log replay on a malformed checkpoint snapshot | DISCRIMINATING |
| CheckpointSnapshotEqualToLastEpochSealIsRejectedBeforeReadingItsLog | a self-contradictory checkpoint (base==its own terminal seal) is rejected BEFORE any log/snapshot GET | perform I/O before validating internal checkpoint coherence | DISCRIMINATING |
| SameEpochFrontierAfterDecodedEpochSealIsCorruption | a frontier one sequence past a decoded EpochSeal in the same epoch is corruption, not a valid empty tail | let the walk silently terminate past a sealed epoch | DISCRIMINATING |
| OlderCheckpointSnapshotAtSealIsCorruption | an older seal used as checkpoint base is rejected via the matching-log proof, before the forged snapshot is read | skip the matching-log cross-check, trusting the checkpoint's seal claim | DISCRIMINATING |
| TerminalGapBelowFrontierIsCorruptionNotARebirth | a claimed frontier above a terminal RemoveNamespace gap is corruption, not misread as a fresh rebirth | let a gap below the frontier silently pass as valid | DISCRIMINATING |
| LaterEpochCheckpointBaseRequiresItsContextualBacklink | 4 distinct backlink-integrity violations (missing/wrong backlink, missing/non-seal predecessor) all rejected | accept a later-epoch base without validating its exact backlink | DISCRIMINATING |
## src/Disks/tests/gtest_cas_recovery_streaming.cpp {#src-disks-tests-gtest-cas-recovery-streaming-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| LongTailReplaysUnderMemoryBound | streaming recovery holds ~one decoded transaction resident (peak ≤ 2×max-single, and ≥1×max-single as a fail-close floor), never the whole tail | materialize the whole tail before applying it | DISCRIMINATING |
| MaterializingControlExceedsMemoryBound | RED control: a whole-tail materializer's peak provably EXCEEDS the same bound, proving the bound is real | (the control itself; validates the bound is discriminating) | DISCRIMINATING |
| MidTailVanishedObjectFailsClosedAgainstStableAuthority | a mid-tail object vanishing between LIST and GET fails closed against the stable checkpoint frontier with zero fresh LISTs | reinterpret a vanished object as a shorter valid stream instead of failing closed | DISCRIMINATING |
| CorruptObjectFailsFast | a durably-corrupt (foreign-namespace) log object fails fast with zero re-LIST | retry/re-list on durable corruption instead of failing fast | DISCRIMINATING |
| ConcurrentWaiterUnblockedOnce | a second caller parks on `recovery_cv` while the leader is mid-recovery, woken exactly once, zero LIST for either | let a concurrent caller race an independent stream LIST instead of parking | DISCRIMINATING |
| OrphanSweepAndFsckSameBound | orphan-sweep recovery and fsck's recovery both stream under the SAME per-transaction bound | let either alternate recovery path materialize the whole tail | DISCRIMINATING |
| LedgerRecoveryReplaysUnderMemoryBound | the PRODUCTION writer-ledger recovery path (not just the free recovery function) streams under the same bound | let the ledger's own recovery loop regress to whole-tail materialization while the free-function path stays green | DISCRIMINATING |
| RecoveryResultInventoryComplete | every field of the recovery publication (base bytes, tail count/bytes, budgets, sweep flag, epoch seal, state) is seeded correctly, not just a couple | drop/miscompute one of the many inventory fields | DISCRIMINATING |

## src/Disks/tests/gtest_cas_ref_carve.cpp {#src-disks-tests-gtest-cas-ref-carve-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| CarveThrowLeavesQueueIntact | a `bad_alloc` at any of 3 plan-phase injection points leaves the queue byte-intact, strands no follower, hangs no waiter (RED on the old interleaved-pop carve) | interleave pops with allocating growth instead of plan-then-publish | DISCRIMINATING |
| ValidationAllocFailureLeavesWorkingClean | a validation-phase allocation failure leaves the failed item out of BOTH in-memory `working` state and the durable transaction; the co-batched survivor still commits | move `working` before the throwing allocation point, leaking a failed item's effect into the commit | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_catalog_birth_wiring.cpp {#src-disks-tests-gtest-cas-ref-catalog-birth-wiring-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| FirstOpenMintsALiveCatalogEntryAndKeysTheBirthAtIt | the first append mints a real Live catalog entry and keys the birth at it, never the Stage-A sentinel | key the birth transaction at the sentinel instead of the minted incarnation | DISCRIMINATING |
| CatalogLossAfterMountCannotRecreateAOneRowAuthority | a lost catalog after mount fails the next publish closed, zero writes, no one-row replacement authority | recreate a fresh one-row catalog to paper over the loss | DISCRIMINATING |
| FailedCatalogBootstrapDoesNotPublishPoolMetaAndRetryConverges | a definite-failed first catalog write leaves no pool_meta; a retry converges cleanly | publish pool_meta before the mandatory catalog succeeds | DISCRIMINATING |
| LostCatalogBootstrapAcknowledgementResolvesToCommittedWithoutARetryOrADuplicate | a landed-but-ack-lost catalog write resolves via the resolve-read, exactly one PUT, no duplicate | reissue a second catalog write despite the landed first one | DISCRIMINATING |
| BootstrapConflictExactReadsTheCanonicalEmptyCatalog | a bootstrap race loser exact-reads the winner's canonical empty catalog before accepting | accept a bootstrap conflict without reading the winner | DISCRIMINATING |
| BootstrapConflictRefusesANonemptyCatalog | a bootstrap race against a genuinely nonempty catalog fails closed | accept a nonempty catalog as if it were the canonical bootstrap shape | DISCRIMINATING |
| ExistingPoolMetaWithMissingCatalogStillFailsClosed | pool_meta present but catalog missing fails closed at open | let open succeed with a missing mandatory catalog | DISCRIMINATING |
| RestartFixturePreservesItsExistingNonemptyCatalog | the restart fixture is idempotent, never overwrites an existing nonempty catalog | let the restart fixture clobber existing catalog state | DISCRIMINATING |
| AnExistingLiveEntryIsAdoptedRatherThanReminted | an already-Live entry is adopted (same incarnation), never re-minted | re-mint a fresh incarnation over an existing Live entry | DISCRIMINATING |
| ANamespaceStuckCreatingUnderALiveForeignFenceRefusesProductionPublicationByConstruction | a Creating entry under an unproven-live foreign fence refuses every append through the ORDINARY production path (no test seam), zero writes | let production publication bypass the Creating-state construction gate | DISCRIMINATING |
| AStaleCreatingEntryFromATerminatedForeignFenceIsReconciledThroughTheProductionPath | a dead predecessor's Creating entry is reconciled and completed to Live over the SAME incarnation through the production path | fail to reconcile a genuinely dead predecessor's stale Creating entry | DISCRIMINATING |
| DropRefusesLiveCreatingFenceWithZeroCatalogMutation | dropping a namespace whose Creating fence is unproven-live refuses, zero writes/deletes | let drop cancel a namespace under a live foreign fence | DISCRIMINATING |
| DropDeletesTerminalCreatingExactlyAndLeavesCkptForJanitor | drop of a terminal Creating entry deletes exactly the catalog row (1 write, 0 deletes), leaves the stale `_ckpt` for the janitor, next namespaceLife mints a genuinely new incarnation | delete the stale `_ckpt` immediately instead of leaving it for the janitor | DISCRIMINATING |
| DropLosesExactCreatingRaceToReconciliationWithoutDeletingCkpt | a drop racing a concurrent reconciliation loses cleanly (NETWORK_ERROR), never deletes the ckpt, the reconciled entry survives | let the losing drop still delete the ckpt or corrupt the reconciled entry | DISCRIMINATING |
| FencedDropCannotCancelTerminalCreating | a drop whose fence generation changed mid-flight refuses on retry rather than mutating with a stale fence | let a re-armed fence generation silently proceed with the drop | DISCRIMINATING |
| ExactOldLifeCannotCancelReplacementTerminalCreating | a drop naming an exact OLD life cannot cancel a REPLACEMENT successor's terminal Creating entry | let a stale predecessor life's drop cancel an unrelated successor entry | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_catalog.cpp (part 1 of N — 78 tests total, compact rows from here) {#src-disks-tests-gtest-cas-ref-catalog-cpp-part-1-of-n-78-tests-total-compact-row}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RefCatalog (battery, golden bytes) | encode produces the exact pinned byte layout | reorder/rename a wire field | DISCRIMINATING |
| ClosedSetPinsNsStateWords | encode/decode `NsState` word tables are the same table | let the two tables diverge for one state | DISCRIMINATING |
| RoundTripsAllThreeStates | all 3 lifecycle states round-trip with correct per-state fields | drop a state-specific field on round-trip | DISCRIMINATING |
| RemovalStartedRoundIsRequiredExactlyForRemoving | `remove_round` mandatory for Removing, forbidden elsewhere (round-trip + 2 decode-reject cases) | make the field optional or caller-local | DISCRIMINATING |
| EmptyCatalogRoundTrips | empty catalog round-trips | fail on the empty-entries edge case | DISCRIMINATING |
| DuplicatePhysicalIdsAreAmbiguousWithoutPoisoningUniquePointResolution | duplicate incarnation ids are ambiguous (throw on resolve) while unrelated unique ids still resolve | let `emplace`-and-ignore silently pick the first duplicate row | DISCRIMINATING |
| AmbiguityStopsCatalogMutationButNotUnrelatedPointLookup | an ambiguous id stops catalog mutation but not an unrelated unique point lookup from the same cut | let ambiguity anywhere block all lookups | DISCRIMINATING |
| NamespaceAtExactByteBoundRoundTrips | a namespace at exactly the max byte bound round-trips | off-by-one the boundary check | DISCRIMINATING |
| EncodeRejects{DuplicateNamespace,NonCanonicalOrder,CreatorPresentOnLive,CreatorAbsentOnCreating,ZeroIncarnation,NameOverByteBound,EmptyNamespace,LiveWithRemovalStartedRound} (+Aborts, 8 pairs) | encode-side grammar refuses each of 8 distinct malformed shapes (LOGICAL_ERROR / abort with exact message) | remove any one of the 8 encode-time grammar checks | DISCRIMINATING (×8) |
| EncodeLineOverCapRaisesLimitExceeded | a worst-case-length line raises `LIMIT_EXCEEDED` not `LOGICAL_ERROR` | misclassify capacity refusal as a bug report | DISCRIMINATING |
| DecodeRejects{DuplicateNamespace,NonCanonicalOrder,CreatorPresentOnLive,CreatorAbsentOnCreating,ZeroIncarnation,NameOverByteBound,UnknownState,UnknownEntryKey,EmptyNamespace,MissingNamespaceKey} (10 tests) | decode-side fails closed on each of 10 malformed byte shapes the encoder itself would never produce | accept any of the 10 malformed shapes on decode | DISCRIMINATING (×10) |
| NsStateToWordRaisesLogicalErrorOnImpossibleValue(+Aborts) | an impossible enum value raises `LOGICAL_ERROR`/aborts rather than silently rendering garbage | let an out-of-range enum render as an empty/garbage word | DISCRIMINATING |
| RegistryRowIsControlStrictWithRawStorage | registry traits (type/family/strictness/caps/compression=Never/no suffix) are exactly pinned | flip compression policy to Always, silently writing compressed bodies under an uncompressed key | DISCRIMINATING |
| Predicate1AcceptsEqualityRefusesCapPlusOne | object-byte predicate accepts exactly at cap, refuses cap+1, with correct message | off-by-one the boundary | DISCRIMINATING |
| Predicate2AcceptsEqualityRefusesOneEntryOver | fold-seal reservation predicate accepts exactly at the derived entry-count boundary, refuses one more | off-by-one the reservation math | DISCRIMINATING |
| Predicate2SaturatesEntryCountReservationInsteadOfWrapping | entry_count × reservation SATURATES rather than wrapping past 2^64 | use unsaturated multiplication, letting an astronomical entry count wrap to "fits" | DISCRIMINATING |
| CombinedAdmissionPropagatesCandidateEntryCount | combined admission (predicate 1 then 2) returns exactly `encodeRefCatalog`'s own bytes for a small catalog | diverge the admission-computed bytes from the real encoder | DISCRIMINATING |
| ReservationCoversActualWidestLegalRowsAcrossDecimalTransitions | the reservation bound covers real maximum-legal fold-seal rows across decimal-digit-count transitions (9/10/99/100 entries) | under-reserve at a decimal-digit transition, letting a legal seal exceed its predicted bound | DISCRIMINATING |
| RemovalNeverRefusedEvenAtCapacity | a Live→Removing transition succeeds even when the catalog is already at the admission ceiling (Constraint 13) | let admission refuse a removal transition, blocking cleanup at capacity | DISCRIMINATING |
| ReadAbsentFailsClosed | reading an absent catalog fails `CORRUPTED_DATA` | return an empty catalog for absence instead of failing closed | DISCRIMINATING |
| CasUpdateRefusesWhenAbsent | `casUpdate` on an absent catalog refuses, nothing written | let casUpdate bootstrap a catalog implicitly | DISCRIMINATING |
| CasUpdateAppliesOnTopOfExistingState | casUpdate correctly applies a mutation on top of existing state | apply the mutation against stale/wrong state | DISCRIMINATING |
| GenericCasUpdateCannotDeleteOrReplaceCatalogIdentity(+Aborts) | casUpdate refuses a mutator that deletes entries or replaces catalog identity | let casUpdate accept identity-breaking mutations | DISCRIMINATING |
| CasUpdateRetriesOnConflictAgainstFreshState | a one-shot conflict is retried against fresh state (mutate called twice, correct final result) | fail permanently on the first conflict instead of retrying | DISCRIMINATING |
| BeginRemovingRechecksAdmissionAfterACatalogConflict | a refused attempt whose admission was withdrawn mid-send ends as `FencedOut`, never a second attempt, nothing written | retry after admission was withdrawn instead of ending the call | DISCRIMINATING |
| CasUpdateThrowsOnVanishMidRetryInsteadOfReplacingTheCatalog(+Aborts) | a genuine mid-retry vanish (real delete, not injected) refuses rather than silently creating a fresh single-entry catalog | let casUpdate treat mid-retry absence as a bootstrap, dropping every other namespace | DISCRIMINATING |
| CasUpdateEndsAtTheDeadlineNotAfterAHundredUnsleptIterations | persistent contention ends at the policy deadline with backoff sleeps, not a fixed iteration count or unslept spin | busy-spin without backoff, or cap at an arbitrary iteration count instead of the real deadline | DISCRIMINATING |
| CasAdmitEntryAcceptsAnOrdinaryCreation | ordinary creation succeeds, correct state | reject a legal creation | DISCRIMINATING |
| CasAdmitEntryInsertsAtCanonicalPosition | new entry inserted at its canonical sorted position, not appended | append instead of inserting canonically | DISCRIMINATING |
| CasAdmitEntryRejectsADuplicateNamespace(+Aborts) | admitting a duplicate namespace is refused via the encoder's own grammar check | let a duplicate namespace be admitted silently | DISCRIMINATING |
| CasAdmitEntryRefusesOverCapacity | admission refuses BEFORE the write when it would exceed capacity; backend object untouched | write before checking capacity | DISCRIMINATING |
| DeleteCompletedRemovingRequiresExactAdoptedProofAndAdmission | 5 distinct proof-refusal shapes (mismatched parent, wrong state ×2, withdrawn admission) all correctly refuse; a valid proof deletes with exactly 1 write, 0 list, 0 delete | accept any of the 5 invalid proof shapes | DISCRIMINATING |
| ExactDeletionRefusesChangedEntryAndAdmissionCannotCarryRemoval | deletion refuses when the observed entry changed underneath it; admission itself cannot carry a Removing state | let deletion proceed against a stale/changed entry | DISCRIMINATING |
| AdmissionCannotCarryRemovalAborts | (debug-build companion) admitting a Removing entry directly aborts | let casAdmitEntry accept a directly-admitted Removing state | DISCRIMINATING |
| FenceLossRemainsControlOutcomeWhenWinnerRemovesOrReplacesLife | a fence loss during erase reports `FencedOut` (control outcome) regardless of whether a concurrent winner removed or replaced the entry — never derived from the resolution read | derive the outcome from the resolution read, turning a stale leader's FencedOut into Deleted/EntryChanged | DISCRIMINATING |
| ATransientEraseFailureIsResolvedByAReadAndReissued | a transient (ambiguous) erase failure is settled by the engine's own resolve read and reissued, never concluded from directly | treat a transient failure as a landed erase or a definite refusal without the resolve read | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_catalog.cpp (part 2 of N — file complete: 78 physical TEST bodies) {#src-disks-tests-gtest-cas-ref-catalog-cpp-part-2-of-n-file-complete-78-physical-}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AConflictingEraseBacksOffBeforeItsRetry | a refused erase paces exactly one backoff before its retry (bounded by first-reissue jitter ceiling) | reissue without backoff (request-storm risk on a hot key) | DISCRIMINATING |
| TheEraseLoopRefreshesItsLivenessBeforeEveryAttempt | the erase loop refreshes its cached leader-authority flag before EVERY attempt (2 refreshes for 1 sent + 1 abandoned) | use one stale liveness reading for the whole loop | DISCRIMINATING |
| AStoreRefusalEndsTheEraseLoopInsteadOfRetryingIt | a definite store refusal (malformed request) is terminal — no retry, no pace, store's own code re-raised | retry a definite refusal as if it were transient | DISCRIMINATING |
| ACommitTheResolutionReadContradictsFailsRetryLaterInsteadOfReportingDeleted | when the mandatory resolution read contradicts a reported commit (concurrent restore), the call fails retry-later rather than falsely reporting deletion | trust the write response over the resolve read, reporting a false deletion | DISCRIMINATING |
| PerpetualConflictEndsAtTheAttemptCapAndSaysSo | permanent contention ends at the attempt cap (not a deadline), message says so, refused-attempts==sleeps exactly | let the loop spin unboundedly or misreport which bound ended it | DISCRIMINATING |
| CancelStalledCreatingRequiresExactRowAndTerminalCreatorFence | cancellation requires BOTH a terminal creator fence AND an unchanged exact row (3 distinct outcomes tested) | cancel against a stale row or a still-live fence | DISCRIMINATING |
| CatalogIsSoleRowAdmissionAuthorityAcrossOrdinaryAndRebuildInputs | the catalog (not the scan) is the sole row-admission authority; ordinary vs rebuild scans produce correctly filtered/merged plan rows | let a scan's own listed_lives admit a row the catalog doesn't authorize | DISCRIMINATING |
| ThresholdAndRestartUseOnlyDurableRounds | stuck-removal detection triggers at exactly the threshold round, is deterministic given the same durable state, and stops once cleanup evidence lands | fire the warning based on non-durable in-memory state (non-reproducible across restarts) | DISCRIMINATING |
| BoundaryAndAbsentVersusUnreadableMessagesAreExact | absent-terminal and unreadable-terminal diagnostics render distinct, exact messages (never conflating the two, never promising an inapplicable remedy) | conflate the two distinct diagnostic messages | DISCRIMINATING |
| DiagnosticDoesNotAppendOrMutateBackend | the stuck-removal diagnostic performs zero writes/deletes | let the diagnostic mutate the backend as a side effect | DISCRIMINATING |
| AdoptedRoundWarnsEveryRestartWithoutAppending | a fresh process re-derives the SAME stuck-removal signal from durable state alone (2 signals for 2 processes), never appends the unreadable log | let the signal depend on in-process state instead of durable rounds | DISCRIMINATING |
| ZeroThresholdIsRefusedAtGcConstruction | a zero stuck-removal threshold is refused at GC construction | accept a nonsensical zero threshold | DISCRIMINATING |
| UnmatchedAdoptedParentLifeIsObservedWithoutEnteringThePlan | a parent-fold-state row whose life the catalog no longer names is counted (+1 event) and dropped from the plan, never silently entered | let an unmatched parent life enter the plan silently | DISCRIMINATING |
| RoundInputOwnsObservationsAndSuccessorStateCannotChangePlan | `RoundInput`/`RefPlan` own their data (not-default-constructible, not-assignable, static asserts); mutating the caller's source data or a successor-state copy after plan construction cannot retarget the plan | let RefPlan alias/borrow caller state instead of owning a snapshot | DISCRIMINATING |

Summary for gtest_cas_ref_catalog.cpp: 78 physical TEST bodies, all DISCRIMINATING. WEAK 0, TAUTOLOGICAL 0, UNSURE 0.
## src/Disks/tests/gtest_cas_ref_chunk_preparation.cpp {#src-disks-tests-gtest-cas-ref-chunk-preparation-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PreparedKeyAndSealedBytesAreCanonical | the prepared key and sealed bytes both round-trip correctly (key parses to the right life/id/kind, bytes decode back to the prepared txn) | corrupt the key derivation or the sealed byte encoding | DISCRIMINATING |
| CandidateBaseIdIsGreatestApplied | `candidate_base_id` is the STARTING state's greatest-applied, not the resulting candidate's; preparation doesn't mutate the caller's state | use the candidate's own greatest-applied as the base, letting an install adopt over a moved-on state | DISCRIMINATING |
| ChainLinkRequiredExactlyOnSequenceOneOfNonGenesisEpoch | INV-2's chain-link grammar cross product (required/forbidden across 3 shapes, both validator halves) all correctly refuse/accept, sealed bytes carry exactly the accepted link | let a forbidden or missing chain link slip past to durable bytes | DISCRIMINATING |
| BirthContributionSetOnlyForNamespaceBirth | `birth_contribution` set only for birth chunks (life_epoch only, no other field), including a birth mixed into a larger chunk | set the birth contribution for a non-birth chunk, or omit it for a mixed one | DISCRIMINATING |
| CommitContributionCarriesFrontierAndOnlyMatchingSeal | ordinary vs seal chunks contribute correctly distinct checkpoint fields (frontier only vs frontier+seal) | conflate ordinary and seal contribution shapes | DISCRIMINATING |
| PreparedAttemptIsCompleteBeforeAnyDurableEffect | every field of the prepared attempt (key, bytes, generation) is fully populated before any request, matching the canonical values exactly | leave a field unpopulated until after the request | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_chunked_flush.cpp {#src-disks-tests-gtest-cas-ref-chunked-flush-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| OversizedItemFailsAlone | an item over the op-count cap fails alone (LIMIT_EXCEEDED); co-batched neighbor still commits | let the oversized item's failure abort the whole batch | DISCRIMINATING |
| OversizedOpFailsItsItemAlone | a single op over the per-op byte cap fails alone; neighbor commits | let an oversized op abort the whole batch instead of just its item | DISCRIMINATING |
| MisScopedItemFailsAloneNeighborCommits | a mis-scoped item (ops touching a ref outside its declared scope) fails alone (LOGICAL_ERROR); neighbor commits | let a mis-scoped item's failure take down co-batched neighbors | DISCRIMINATING |
| CanonicalMaxTransactionRoundTrips | the maximum legally-admissible normal-class transaction (5000 max-size ops) round-trips well under the byte decode cap | let the two counts-only caps compose to exceed the byte cap | DISCRIMINATING |
| DropNamespaceOverOpCapSucceeds | removal-class (dropNamespace) transactions are byte-budgeted, NOT op-count-capped, so >5000-ref drops succeed in one transaction | apply the normal-class op cap to removal-class transactions | DISCRIMINATING |
| SyntheticWholeShardNonRemovalRejected | classification is by OP INSPECTION, not scope: a synthetic WholeShard-scoped item with non-removal ops over the cap is still rejected | classify removal-ness by MutationScope alone instead of inspecting ops | DISCRIMINATING |
| ChunkedFlushCommitsPerChunk | an overflowing 3-item carve splits into exactly 2 chunks with correct per-chunk ids, exact op partitioning, exact tail/metric deltas, build_ops called exactly once per item | merge overflowing items into one oversized transaction, or double-invoke build_ops | DISCRIMINATING |
| ChunkFailureDefinite | chunk 2's definite (proven) failure doesn't wedge the lane; chunk 1 still commits and is durable | let a proven chunk-2 rejection wedge the lane or roll back chunk 1 | DISCRIMINATING |
| ChunkFailureWedge | chunk 2's unresolved ambiguity wedges ONLY chunk 2's exact key, paced entirely on the virtual clock; chunk 1 durable, chunk 2 never written | let the wedge span both chunks' keys, or let chunk 2's ambiguity roll back chunk 1 | DISCRIMINATING |
| ChunkFailureThrow | chunk 2's proven-conflict throw doesn't wedge the lane; chunk 1 durable | let a proven conflict wedge the lane | DISCRIMINATING |
| LeaderOwnItemCommittedBeforeThrow | a boundary throw AFTER chunk 1 commits still reports the leader's own item as SUCCESS (chunk 1's id), not the later exception; build_ops still at-most-once | rethrow unconditionally over a durable own item (losing the caller's true success) | DISCRIMINATING |
| SnapshotPublisherLatchedAcrossChunks | a snapshot publish trigger dropped by the single-flight gate while a prior publisher is latched is re-fired on settlement, so a follow-up publication covers the later chunk | let the single-flight gate silently drop the follow-up trigger, leaving a chunk unsnapshotted | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_ckpt.cpp (40 tests total) {#src-disks-tests-gtest-cas-ref-ckpt-cpp-40-tests-total}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RoundTripsEveryFieldCombination | every field combination (including both-absent, the newborn shape) round-trips | fail on the both-absent case | DISCRIMINATING |
| CommittedThroughHasCanonicalExactWireEncoding | exact pinned byte encoding for a fully-populated ckpt | reorder/rename a wire field | DISCRIMINATING |
| RefCkpt (battery) | golden-byte encode/decode battery | same | DISCRIMINATING |
| CodecRejectsIncoherentCommittedFrontierAndSealEpochs | 4 distinct incoherent frontier/seal shapes all refused (valid case passes) | accept a skipped-epoch or misordered seal | DISCRIMINATING |
| RejectsAnUnknownKey | unknown key = CORRUPTED_DATA; `!`-prefixed unknown = UNKNOWN_FORMAT_VERSION (2 distinct codes) | conflate the two distinct unknown-key classes | DISCRIMINATING |
| RejectsOldCommittedEpochKeyRatherThanAliasingIt | a retired key spelling is rejected, not silently aliased (deliberately chosen harmless-if-aliased values isolate this) | alias the old key spelling instead of rejecting it | DISCRIMINATING |
| RejectsADuplicateKey | duplicate key in one object is rejected | pick one of two duplicate values arbitrarily | DISCRIMINATING |
| RejectsTruncation | 5 distinct truncation/half-pair shapes rejected; the legitimate empty-body shape is NOT rejected | conflate truncation with the legitimate empty-body shape | DISCRIMINATING |
| RejectsTrailingBytes | trailing junk after a valid object is rejected | tolerate trailing bytes | DISCRIMINATING |
| RejectsInvalidFieldsOnEncodeAndOnDecode | invalid field values (zero epoch/sequence, backward frontier) rejected on BOTH encode and decode | validate only on encode, trusting decoded bytes blindly | DISCRIMINATING |
| RegistryRowIsControlStrictWithTightCaps | registry traits pinned exactly (type/family/strictness/caps/no-suffix/Never-compression) | flip compression policy, silently corrupting the raw-key contract | DISCRIMINATING |
| KeyIsTheLifeLeafAndParsesBack | key derivation/parsing round-trips; each key shape has exactly one classifier (no cross-claiming) | let two different key-shape parsers both claim the same key | DISCRIMINATING |
| GroupRefKeysScopesHotIntakeToTheStreamTree | hot-stream grouping accepts logs/snapshots, ignores `_ckpt`, still refuses a genuinely unrecognized key | let a genuinely bad key silently pass grouping | DISCRIMINATING |
| MergeTakesThePerFieldSemanticMaximum | per-field commutative maximum merge, independent field advancement, absent-loses-to-present, self-merge idempotent, partial-knowledge contribution never regresses an unknown field | use a plain field-copy instead of per-field maximum, letting a partial contribution erase a field it doesn't know about | DISCRIMINATING |
| CreatesTheObjectWhenItIsAbsent | publishCkpt creates the object when absent | fail to create on first publish | DISCRIMINATING |
| EachWriterCreatesWithOnlyWhatItKnowsAndTheOtherFieldsMergeInLater | a writer creates with only its own known fields; a later writer's contribution merges in without erasing the first | let one writer's create clobber a field it doesn't know about | DISCRIMINATING |
| AConflictRereadsAndMergesOntoTheWinner | a concurrent conflict is resolved by re-reading and merging onto the winner's body, never dropping the winner's field (TLC counterexample regression) | reuse the pre-conflict read on retry instead of re-reading | DISCRIMINATING |
| AnIdenticalMergedBodyIssuesNoWrite | a contribution that adds nothing issues NO write and mints no new incarnation | write unconditionally, causing spurious conflicts for every concurrent writer | DISCRIMINATING |
| AnAdmissionLossBetweenTheReadAndTheWriteWritesNothing | admission lost between read and write means the write is never sent | send the write anyway despite lost admission | DISCRIMINATING |
| AnExhaustedDeadlineUnderPersistentConflictThrowsRetryLater | permanent conflict fails closed at the deadline, backs off (doesn't spin), leaves no partial state | spin without backoff, or leave partial state | DISCRIMINATING |
| AnAmbiguousCommittedWriteIsResolvedByOneExactReadWithoutBlindRetry | a landed-but-ack-lost write resolves via exactly READ→WRITE→READ, no blind retry | blindly retry without the resolve read | DISCRIMINATING |
| AnAmbiguousUncommittedWriteRetriesAgainstTheExactRead | an unresolved (never-landed) ambiguous write retries against the resolve read's result | reissue against stale pre-ambiguity state | DISCRIMINATING |
| AnAmbiguousWriteAcceptsAValidDominatingDurableFrontier | when a resolve read finds a winner that already dominates, the call reports Published (not IdenticalSkip, since an attempt WAS sent) | mislabel the outcome as IdenticalSkip when an attempt was actually sent | DISCRIMINATING |
| AFailedResolveReadNeverReportsACommitAndNeverSkipsTheRead | every reissue is preceded by its own resolve read; failure never reports a false commit | skip the resolve read on a reissue | DISCRIMINATING |
| AdmissionLostWithTheAmbiguousWritePreventsItsResolveRead | admission lost right after an ambiguous write prevents the resolve read from even starting (FencedOut) | let a lost-admission call still attempt the resolve read | DISCRIMINATING |
| AdmissionLostAfterTheResolveReadStillRefusesToClaimTheCommit | admission lost AFTER the resolve read proved durability still refuses to claim it (FencedOut, not Published) | claim a commit proven durable after admission was already gone | DISCRIMINATING |
| DeclineTimeVerdictsReadAdmitted | both no-write verdicts (IdenticalSkip-shaped and decreasing-epoch-shaped) consult admission and answer FencedOut when it's gone, never IdenticalSkip or CORRUPTED_DATA | answer IdenticalSkip/CORRUPTED_DATA for an unadmitted writer instead of FencedOut | DISCRIMINATING |
| ACorruptCheckpointIsNeverOverwritten | a corrupt (undecodable) checkpoint is never overwritten, corruption surfaces instead of being laundered | overwrite a corrupt checkpoint with a fresh well-formed one | DISCRIMINATING |
| AMissingSampledBaseRestartsOnAnAdvancedIncarnationAndIsCorruptionOnAnUnchangedOne | 3-way classification of a missing sampled base (advanced=restart, unchanged=corruption, absent=corruption), using real read incarnations | misclassify an advanced incarnation as corruption | DISCRIMINATING |
| SnapshotsAreDeletableStrictlyBelowTheCheckpoint | deletion gate is STRICTLY below the checkpoint (its own base is off-limits); fail-closed with no checkpoint | use an at-or-below gate (TLC counterexample) | DISCRIMINATING |
| NamespaceBirthCreatesTheCheckpointCarryingItsLifeEpoch | birth creates `_ckpt` with the correct life_epoch, no base yet | let birth omit or mis-set life_epoch | DISCRIMINATING |
| ACommittedSnapshotPublishAdvancesTheCheckpoint | a committed snapshot publish advances the checkpoint's base, preserves life_epoch, the named snapshot really exists | advance the checkpoint before the snapshot body is durable | DISCRIMINATING |
| CleanupPlannedBetweenTheBodyPutAndTheCkptCasCannotDeleteTheNewSnapshot | a cleanup pass planned against a stale checkpoint cannot delete the just-published snapshot; re-reading makes the older one reclaimable | let a stale-checkpoint plan delete the new snapshot | DISCRIMINATING |
| TheCheckpointIsWrittenOncePerPublicationAndNotOnIdleAttempts | exactly 2 ckpt writes for birth, 1 for a real publication, 0 for idle repeats | write on an idle/no-op publish attempt | DISCRIMINATING |
| SnapshotPublisherRefusesEpochSealCandidateWithoutAnyWrite | the publisher declines an epoch-seal candidate with ZERO writes to either the snapshot or ckpt key | let the publisher write a snapshot at the seal boundary | DISCRIMINATING |
| NeedsRecoveryReplaysBeforeCheckpointAdvance | publication recovers a `NeedsRecovery` lane BEFORE capturing a snapshot, so the checkpoint never advances over a missing transaction | advance the checkpoint before replaying the stranded lane | DISCRIMINATING |
| APublishFencedOutMidAttemptDoesNotAdvanceTheCheckpoint | a mid-attempt fence loss advances nothing AND does not adopt the snapshot as newest (would suppress future publication) | adopt the snapshot as newest despite the fence loss | DISCRIMINATING |
| CommitRefChunkDurableBytesUnchangedByExtraction | exact key/plaintext/byte-length/128-bit-hash golden regression for the real append lane after the prepareRefChunk extraction | change the sealed bytes' shape during the extraction | DISCRIMINATING |
| AppendRequestCountUnchangedByExtraction | exact per-key request counts (1 log write, 1 grounding GET, 2 ckpt writes) unchanged by the extraction | add/remove a request during the extraction | DISCRIMINATING |
| PostDurableInstallRegionStillEnteredAfterExtraction | the post-durable install region is still entered on a committing append after the extraction | lose the commit install arm entirely during a refactor | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_ckpt_join.cpp {#src-disks-tests-gtest-cas-ref-ckpt-join-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| static_assert(is_trivially_copyable_v<RefCkpt>) | compile-time: `_ckpt` cannot hold heap-storage fields (Constraint 15) | add a `std::vector`/`String`/map field to `RefCkpt` | DISCRIMINATING (compile-time fence) |
| JoinUnknownLifeEpochWithPresentYieldsPresent | an unknown (nullopt) life_epoch contribution never erases a present one, both directions; two absences stay absent | let an unknowing contribution clobber a known life_epoch | DISCRIMINATING |
| JoinEqualLifeEpochsYieldsSame | equal contributions merge to the same value without disturbing other fields | let an equal-value merge accidentally reset another field | DISCRIMINATING |
| CrossEpochFrontierRequiresAnImmediatelyAdjacentSeal | 5 distinct epoch-crossing shapes correctly accepted/rejected (only an immediately-adjacent seal authorizes a cross-epoch merge) | accept a non-adjacent or missing seal as authorizing a crossing | DISCRIMINATING |
| ResumedCreationRaisesLifeEpochWithoutRefusal | a genuinely resumed creation (through real production primitives) raises life_epoch without the join refusing it | make the join wrongly refuse a legitimate resumption, wedging the namespace forever | DISCRIMINATING |
| RestartBetweenCreationAndFirstWriteRaisesLifeEpochWithoutRefusal | the second real production sequence (creation then birth chunk after a restart) also raises life_epoch without refusal | same — refuse a legitimate CREATE-restart-INSERT sequence | DISCRIMINATING |
| JoinDecreasingLifeEpochIsCorruptionAndPublishesNothing | a genuinely impossible decrease (violated fence discipline) is refused as CORRUPTED_DATA naming both values and the key, with zero writes | silently absorb an impossible decrease into a maximum, hiding a fence-discipline violation | DISCRIMINATING |
| ADecreasingLifeEpochFromAFencedOutWriterIsReportedFencedOutNotCorruption | the SAME decrease from an already-unadmitted writer is reported FencedOut (transient), not CORRUPTED_DATA (permanent) | misclassify a fenced-out writer's stale decrease as permanent corruption | DISCRIMINATING |
| CheckpointAndSealStillMergeBySemanticMaximum | checkpoint/seal still merge by (epoch-first) semantic maximum, present beats absent, both directions | use sequence-only ordering instead of epoch-first, breaking cross-epoch-restart ordering | DISCRIMINATING |
| EncodedCkptSizeIsIndependentOfCardinality | `_ckpt` byte size is IDENTICAL between 1 ref and 2000 refs (driven through the real append lane so an unpopulated field can't hide) | add a per-ref term to `_ckpt`, breaking O(1) | DISCRIMINATING |
| EncodedCkptSizeHasAConstantCeilingAcrossTransactionsAndEpochs | the true worst-case encoded size is pinned to an exact literal; growth is bounded decimal width, not proportional | widen a field or add one without updating the pinned ceiling | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_contiguous_alloc.cpp {#src-disks-tests-gtest-cas-ref-contiguous-alloc-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| TwoNamespacesAllocateIndependently | interleaved writes to 2 namespaces each get their own dense 1,2,3.. ids (not a shared pool-wide counter) | revert to a pool-wide id counter | DISCRIMINATING |
| PreAttemptRefusalConsumesNoId | a refusal proven to have sent nothing consumes no id; the next append reuses the exact same id | burn an id on a provably-unsent refusal, leaving a permanent gap | DISCRIMINATING |
| EpochChangeRestartsTheSequenceAtOne | a fresh mount incarnation restarts its table's sequence at 1, not continuing the dead incarnation | continue numbering across an epoch change | DISCRIMINATING |
| NonSuccessorIdIsRejectedOnApply | a non-successor id (including a same-epoch skip and a new-epoch non-1 start) is rejected, naming both ids; `nextRefTxnId` is the single shared rule | let a hole silently apply, or diverge the allocator's rule from the checker's | DISCRIMINATING |
| GcShardsIsPersistedAndOverridesMismatchedReopenConfig | gc_shards is durable and a mismatched reopen config is overridden by the persisted value | let a reopen's config silently change the durable shard count | DISCRIMINATING |
| NeedsRecoveryReplaysBeforeAllocatingTheNextId | a NeedsRecovery lane replays the stranded durable transaction before allocating the next id; durable stream stays dense (1,2,3 all exist) | allocate the next id without replaying, creating a durable hole | DISCRIMINATING |
| NeedsRecoveryReplaysBeforeSnapshotPublication | publication also recovers a NeedsRecovery lane before capturing a snapshot | publish a snapshot that omits the stranded transaction | DISCRIMINATING |
| RecreationRefusedWhileAMountSlotIsStillHeld | recreation over a prefix with a still-held mount slot refuses closed, names the holder and the correct remedy order; holder's own stream continues unaffected | let recreation proceed while a real writer is still mounted (double-write risk) | DISCRIMINATING |
| RecreationProceedsOnceTheHolderIsTerminal | once the holder is terminal (farewell marker), the quiesce gate stops firing and ordinary residual-data rules take over | keep refusing recreation even after the holder is genuinely gone | DISCRIMINATING |
| SurvivingWriterIsFencedByTheRecreatedPoolsMount | the backstop: a surviving writer whose slot was reclaimed is fenced by its own failing renewal (never double-writes); teardown doesn't double-count or clobber the successor's lease | let a survivor keep writing into a recreated pool, or let its teardown stamp over the successor's lease | DISCRIMINATING |

## src/Disks/tests/gtest_cas_ref_decode_bounds.cpp {#src-disks-tests-gtest-cas-ref-decode-bounds-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RawOverCapObjectRejected | a raw (uncompressed) body over the object cap is rejected, closing the compression-skip loophole | let the raw decode arm skip the cap check that the zstd arm enforces | DISCRIMINATING |
| RawAtCapObjectAccepted | exactly-at-cap raw bytes still open unchanged (boundary control) | off-by-one the cap, rejecting a legal at-cap object | DISCRIMINATING |
| PaddedNormalTxnOver20MiBRejected | the whole-object byte cap is measured over ACTUAL decoded bytes (a tolerant-unknown-field padding attack is caught), not accumulated per-op | let unknown-field padding smuggle an oversized object past the cap | DISCRIMINATING |
| WriterPostEncodeThrowIsRuntime | the writer's post-encode budget check is a real runtime throw, not a debug-only chassert a release build would skip | replace the runtime throw with a chassert, silently persisting an over-cap object in release | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_cow_manifest_set.cpp {#src-disks-tests-gtest-cas-ref-cow-manifest-set-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| EmptySetHasNoMembers | fresh set is empty, size 0, no false-positive membership | return true for `contains` on an empty set | DISCRIMINATING |
| InsertThenContains | insert makes a member visible, doesn't leak to unrelated keys | fail to insert, or leak membership to a different key | DISCRIMINATING |
| InsertMultipleThenContainsEachIndependently | multiple inserts are each independently tracked | conflate distinct entries | DISCRIMINATING |
| EraseRemovesAnOverlayOnlyMember | erasing a pure-overlay member removes it outright (0 overlay entries left) | leave a tombstone for a member that was never in base | DISCRIMINATING |
| TombstoneThenReinsertWhilePurelyInOverlay | erase-then-reinsert while pure-overlay reads as present again | treat a reinsert as still-absent (stale tombstone) | DISCRIMINATING |
| MaterializeFoldsOverlayIntoBaseAndEmptiesOverlay | materialize folds overlay into base, overlay empties, membership preserved | fail to empty the overlay after folding | DISCRIMINATING |
| MaterializeOnAnEmptyOverlayIsANoOp | materialize on an already-empty overlay doesn't reallocate the base | reallocate the base unnecessarily on a no-op materialize | DISCRIMINATING |
| EraseAfterMaterializeTombstonesABaseMember | erase after materialize correctly tombstones a base member; a second materialize actually removes it | fail to remove a tombstoned base member on the second materialize | DISCRIMINATING |
| TombstoneThenReinsertAcrossMaterializedBase | erase-then-reinsert across a materialized base revives correctly | lose a revived base member | DISCRIMINATING |
| MaterializeReusesBaseWhenUniquelyOwned | a uniquely-owned base folds IN PLACE (same allocation identity), no O(N) copy | always reallocate on materialize, breaking the O(1)-when-unique optimization | DISCRIMINATING |
| MaterializeBuildsFreshBaseWhenBaseIsShared | a shared base forces a FRESH allocation on materialize; the other holder's view stays byte-unchanged | mutate a shared base in place, corrupting another holder's view | DISCRIMINATING |
| MaterializeEmptyOverlayIsANoOpEvenWhenUniquelyOwned | even when uniquely owned, an empty-overlay materialize doesn't reallocate | reallocate needlessly | DISCRIMINATING |
| CopyIsIsolatedFromOriginal | a copy's mutations don't affect the original, and vice versa | let a copy's write leak into the original | DISCRIMINATING |
| CopySharesBaseUntilEitherSideMaterializesANewOne | copy shares the base (refcount 2) until a write forces a fresh one via materialize | copy the base eagerly instead of sharing (defeats COW) | DISCRIMINATING |
| SizeTracksNetDeltaAcrossMixedOps | size() stays exact across a long mixed insert/erase/materialize sequence | let net_delta drift from the true membership count | DISCRIMINATING |
| InsertThrowsWhenAlreadyPresentInOverlay | insert of an already-present overlay member fails closed (CORRUPTED_DATA) | silently no-op a duplicate insert, masking index drift | DISCRIMINATING |
| InsertThrowsWhenAlreadyPresentInBase | insert of an already-present base member fails closed | same, for the base-materialized case | DISCRIMINATING |
| EraseThrowsWhenAbsent | erase of an absent member fails closed | silently no-op an erase of a non-member | DISCRIMINATING |
| EraseThrowsWhenAlreadyTombstoned | double-erase (already tombstoned) fails closed | silently no-op a double erase | DISCRIMINATING |
| FastAndForcedSlowMaterializeAgreeOverRandomOps | 60 randomized trials: the in-place fast fold and the build-fresh-and-swap slow fold always agree on membership/size | let the two materialize paths diverge under any randomized sequence | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_cow_map.cpp {#src-disks-tests-gtest-cas-ref-cow-map-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| EmptyMapHasNoEntries | fresh map is empty, no false membership | return true for contains on empty | DISCRIMINATING |
| EmplaceThenFind | emplace makes a key findable via both iterator and at() | fail to insert or return wrong value | DISCRIMINATING |
| EmplaceDoesNotOverwriteExisting | emplace on an existing key is a no-op, returns inserted=false | let emplace silently overwrite | DISCRIMINATING |
| InsertOrAssignOverwritesExisting | insert_or_assign overwrites an existing key's value | fail to overwrite | DISCRIMINATING |
| InsertOrAssignInsertsWhenAbsent | insert_or_assign inserts fresh when absent | fail to insert when absent | DISCRIMINATING |
| EraseByKey | erase removes and is idempotent (0 on repeat/nonexistent) | let a second erase throw or over-count | DISCRIMINATING |
| AtThrowsOnMissingKey | at() throws std::out_of_range on a missing key | return a default/garbage value instead of throwing | DISCRIMINATING |
| CountMatchesContains | count() agrees with contains() | diverge count and contains | DISCRIMINATING |
| OrderedIterationOverAllBaseRowsIsSorted | iteration over base-only rows is sorted by key | iterate in insertion order instead of sorted | DISCRIMINATING |
| MergedIterationAppliesTombstonesAndOverrides | merged iteration correctly applies overlay overrides/tombstones/additions over a materialized base, in sorted order | let a tombstoned base row leak through iteration | DISCRIMINATING |
| FindOverlayOnlyKeyIteratesIntoBase | an overlay-only key's iterator correctly advances into base rows (doesn't collapse to end) | break iterator advancement across the overlay/base boundary | DISCRIMINATING |
| EraseByIteratorReturnsNextAndRemovesTheRow | erase(iterator) returns the correct next iterator and removes the row | return the wrong next iterator | DISCRIMINATING |
| EraseByIteratorOfLastElementReturnsEnd | erasing the last element's iterator returns end() | return a dangling/wrong iterator | DISCRIMINATING |
| MaterializeFoldsOverlayIntoFreshBaseAndKeepsValuesUnchanged | materialize folds overlay into base correctly, values unchanged | corrupt a value during the fold | DISCRIMINATING |
| MaterializeOnAnEmptyOverlayIsANoOp | empty-overlay materialize doesn't reallocate | reallocate needlessly | DISCRIMINATING |
| MaterializeDoesNotAffectACopyTakenBeforeIt | a copy taken before a materialize+mutation is unaffected | let a materialize mutate a pre-existing copy's view | DISCRIMINATING |
| MaterializeReusesBaseWhenUniquelyOwned | uniquely-owned base folds in place (same allocation identity) | always reallocate, defeating the O(1)-when-unique fast path | DISCRIMINATING |
| MaterializeBuildsFreshBaseWhenBaseIsShared | shared base forces a fresh allocation; other holder's view stays byte-unchanged | mutate a shared base in place | DISCRIMINATING |
| MaterializeEmptyOverlayIsANoOpEvenWhenUniquelyOwned | empty-overlay materialize is a no-op even when uniquely owned | reallocate needlessly | DISCRIMINATING |
| EqualityComparesEffectiveContentsNotInternalLayout | equality compares LOGICAL content, not internal base/overlay layout (two structurally different maps with same content are equal) | compare internal layout instead of effective content | DISCRIMINATING |
| CopyIsIsolatedFromOriginal | copy mutations don't leak to original, and vice versa | let a copy's write leak into the original | DISCRIMINATING |
| CopySharesBaseUntilEitherSideMaterializesANewOne | copy shares base (refcount 2) until a write forces materialize | copy eagerly instead of sharing (defeats COW) | DISCRIMINATING |
| PropertyMatchesStdMapOverRandomOps | 50 randomized trials of 200 steps each: RefCowMap behaves IDENTICALLY to std::map across every op combination (emplace/assign/erase/find/iterator-erase/materialize) | let RefCowMap diverge from std::map semantics under any randomized sequence | DISCRIMINATING |
| FastAndForcedSlowMaterializeAgreeOverRandomOps | 60 randomized trials: in-place fast fold and build-fresh-and-swap slow fold always agree on content/size | let the two materialize code paths diverge | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_epoch_seal_format.cpp (30 tests) {#src-disks-tests-gtest-cas-ref-epoch-seal-format-cpp-30-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| IsEpochSealTrueForSoleSealOp / FalseForSealPlusOtherOp / FalseForNonSealOp / FalseForEmptyOps (4 tests) | `refLogTxnIsEpochSeal` correctly classifies exactly the sole-seal-op shape | misclassify a mixed or empty op vector as a seal | DISCRIMINATING (×4) |
| RemovalClassIsFalseForEpochSeal | an epoch-seal-only txn is NOT removal-class | misclassify a seal as removal-class | DISCRIMINATING |
| RoundTripSealAtSequenceOneWithPrevEpochSeal / RoundTripSealWithoutPrevEpochSeal (2 tests) | seal round-trips correctly both with and without `prev_epoch_seal` | drop/corrupt the prev_epoch_seal field on round-trip | DISCRIMINATING (×2) |
| ByteIdenticalReencodeWithPrevEpochSeal | re-encoding a decoded seal is byte-identical | let re-encoding drift from the original bytes | DISCRIMINATING |
| EncodeRejectsSealTxnWithTwoSealOps / …SecondNonSealOp (2) + Decode…Spliced counterparts (2) | the op-count-1 rule for seals is enforced on BOTH encode and decode (decode independently, via splice) | drop the check from either side | DISCRIMINATING (×4) |
| EncodeRejectsPrevEpochSealAtNonUnitSequence + DecodeRejectsPrevEpochSealAtNonUnitSequenceSpliced (2) | prev_epoch_seal is forbidden except at sequence 1, enforced on both sides | accept a prev_epoch_seal at a non-unit sequence | DISCRIMINATING (×2) |
| EncodeRejectsPrevEpochSealWithZeroWriterEpoch / …ZeroRefSequence (2) | zero components in prev_epoch_seal rejected (shares the txn_id nonzero check) | accept a zero-component prev_epoch_seal | DISCRIMINATING (×2) |
| DecodeRejectsPrevEpochSealMissingPssComponent | a half-written prev_epoch_seal pair (only one of two wire fields) is rejected on decode | accept a half-populated optional pair | DISCRIMINATING |
| EncodeRejectsPrevEpochSealPointingAtSameEpoch / …FutureEpoch (2) | chain-direction rule rejects self- and forward-pointing links | accept a same/future-epoch link | DISCRIMINATING (×2) |
| EncodeRejectsPrevEpochSealSkippingImmediateEpoch + DecodeRejectsPrevEpochSealSkippingImmediateEpochSpliced (2) | the link must name the IMMEDIATELY preceding epoch's seal, not an older one, enforced on both sides | accept a skipped-epoch link, letting a fold bypass a missing seal | DISCRIMINATING (×2) |
| DecodeRejectsPrevEpochSealPointingAtSameOrFutureEpochSpliced | decode independently re-derives the chain-direction rule via a splice into an otherwise-valid body | remove the decode-side chain-direction check | DISCRIMINATING |
| ContextualRejectsMissingPrevEpochSealWhenRequired | contextual grammar rejects a missing link when life_epoch < writer_epoch and sequence==1 | accept a missing required link | DISCRIMINATING |
| ContextualRejectsPrevEpochSealWhenForbidden | contextual grammar rejects a present link at genesis (life_epoch==writer_epoch) | accept a spurious link at genesis | DISCRIMINATING |
| ContextualAllowsGenesisBirthAboveEpochOneWithoutPrevEpochSeal | a namespace born above epoch 1 legitimately has no link at its genesis | wrongly require a link for a late-born namespace | DISCRIMINATING |
| ContextualPassesThroughNonSequenceOneAboveLifeEpoch / …AtOrBelowLifeEpoch (2) | the sequence!=1 early return fires regardless of the life_epoch relation (both above and at-or-below) | apply the required/forbidden rule to a non-sequence-1 transaction | DISCRIMINATING (×2) |
| DecodeRejectsUnknownCriticalKeyInMetaLine | an unrecognized `!`-prefixed critical key fails UNKNOWN_FORMAT_VERSION, proving the mechanism that protects prev_epoch_seal against silent field-drop | let an unrecognized critical key be silently skipped | DISCRIMINATING |
| DecodeRejectsUnknownOpWordRegressionGuard | an unknown op word still fails after adding epoch_seal (regression guard) | let epoch_seal's addition accidentally widen the accepted op-word set | DISCRIMINATING |
| FormatBatteryEpochSeal | golden-byte battery for the epoch_seal shape including prev_epoch_seal | reorder/rename a wire field | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_gc.cpp (19 tests) {#src-disks-tests-gtest-cas-ref-gc-cpp-19-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| LargeRefScanFoldsEveryLogExactlyOnce | a >1000-key multi-page LIST scan folds every log exactly once (cursor advances to greatest id, spot-checked in-degree==1) | double-fold or skip a log crossing a LIST page boundary | DISCRIMINATING |
| ConcurrentLogAfterScanIsFoldedNextRound | a log appended after the scan passed its table is never skipped past (cursor stays below it; next round folds it) | let a concurrently-appended log be silently skipped forever | DISCRIMINATING |
| FoldBarrierClampsBelowMissingBodyThenFoldsOnAppear | a live precommit with a missing manifest body clamps the cursor below it (records anomaly) then folds once the body appears | fold past a missing body, or never lift the barrier once it appears | DISCRIMINATING |
| EdgeCancellationAddThenRemoveReclaimsBlob | add-then-remove nets to zero in-degree and reclaims the blob | leave a phantom edge after cancellation | DISCRIMINATING |
| LosingGenerationCommitAdoptsNothingDeletesNothing | a denied round-commit CAS (deposed leader) advances nothing and deletes nothing | let a losing generation's fold still delete a condemned blob | DISCRIMINATING |
| RefObjectCleanupRetainsCheckpointNamedTriple | cleanup deletes objects below the checkpoint base/cursor but retains the exact checkpoint-named triple | delete part of the checkpoint-named recovery triple | DISCRIMINATING |
| RefObjectCleanupRespectsRoundBudgetAndConvergesAcrossRounds | a tiny per-round cleanup budget deletes exactly N per round, converges to zero across repeated rounds | ignore the budget, or fail to converge | DISCRIMINATING |
| RefObjectCleanupRetainsCheckpointPredecessorSealProof | cleanup retains the predecessor seal proving a cross-epoch checkpoint base's transition | delete the seal proof, breaking future recovery | DISCRIMINATING |
| CatalogTokenMoveBeforeFirstDeleteRefusesEveryRefObjectDelete | a catalog authority move before the first delete refuses ALL ref-object deletes this round | let a stale catalog authority still authorize deletes | DISCRIMINATING |
| CatalogTokenMoveBetweenKeysAllowsFirstAndRefusesSecondDelete | authority moving between two deletes lets the first through, refuses the second (per-key revalidation, not amortized) | amortize the authority check across the whole batch instead of per-key | DISCRIMINATING |
| GcFenceMoveBeforeFirstDeleteRefusesEveryRefObjectDelete | same for the GC-fence authority | let a stale GC fence authorize deletes | DISCRIMINATING |
| GcFenceMoveBetweenKeysAllowsFirstAndRefusesSecondDelete | same per-key revalidation for the GC-fence authority | amortize the GC-fence check | DISCRIMINATING |
| RefIntakeIncrementsObservabilityCounters | a real fold+clean round increments every one of 5 distinct observability counters | drop the increment at any one of the 5 sites | DISCRIMINATING |
| RefSnaplogLifecycleE2E | full real-object lifecycle (2 tables, replace, snapshot, GC-to-fixpoint) ends clean per fsck AND cas-gc-dryrun | leak a blob, or let fsck/dryrun disagree with the real state | DISCRIMINATING |
| MalformedRefKeyAbortsRefFoldingNoPartialDelta | a malformed ref key aborts the round's fold with NO partial delta and no cursor advance, survives (doesn't throw out) | let a malformed key produce a partial fold delta | DISCRIMINATING |
| NonCanonicalLifeKeyAbortsRefFoldingWithoutWedgingTheRound | a non-canonical life-segment key aborts fold but the round COMPLETES (doesn't propagate/wedge forever) | let the malformed-key exception escape the round's catch, wedging GC permanently | DISCRIMINATING |
| InvalidRefLogBodyHoldsNamespaceNoPartialDelta | an undecodable body at a canonical key holds just that NAMESPACE (not the whole round); the hold survives evidence deletion and clears only on a real repair | release the hold when the evidence disappears (defeats the whole mechanism) | DISCRIMINATING |
| BaselineGuardRefusesWhenSnapshotSurvivesWithoutLogsOrCursor | a table with a surviving snapshot but no logs/cursor refuses the round BEFORE any delete, sparing an unrelated healthy table's blob | let the guard mass-condemn a still-referenced blob when its supporting logs are gone | DISCRIMINATING |
| CatalogAdmittedFreshLifeWithoutParentSeedsSuccessorSeal | a fresh catalog-admitted life with no parent cursor still gets seeded into the fold's successor seal | drop a parentless fresh life from the fold plan entirely | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_install_safety.cpp (20 tests) {#src-disks-tests-gtest-cas-ref-install-safety-cpp-20-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PostDurableInstallIsAllocationFree | the post-durable install seam is reached on an ordinary commit, tail counter advances exactly once per install | let a commit skip the install region or double-count | DISCRIMINATING |
| UnresolvedAlwaysRecordsTheWedge | an Unresolved PUT ALWAYS wedges (exact key retained, tail NOT advanced), message says "UNCERTAIN" not pre-attempt | let an ambiguous PUT go unwedged, losing the in-doubt object | DISCRIMINATING |
| PreAttemptRefusalDoesNotWedgeTheLane | a refusal proven to have sent NOTHING does not wedge (0 ref-log I/O, lane stays Ready); lane usable immediately once the lease heals | wedge the lane on a pre-attempt refusal that never touched the store (unrecoverable false wedge) | DISCRIMINATING |
| PreAttemptRefusalAfterAWedgeResolutionLeavesTheLaneClean | a flush that resolves a real wedge then gets pre-attempt-refused on its OWN new chunk leaves the lane clean, not re-wedged over an unwritten id | re-wedge over an id that was never sent, creating a permanent false wedge (the exact pre-fix bug) | DISCRIMINATING |
| AmbiguousChunkAfterAWedgeResolutionRewedgesTheLane | positive control: a genuinely ambiguous new chunk after resolving a wedge DOES re-wedge, over the NEW transaction | fail to wedge a genuinely ambiguous new chunk (would make the test above pass vacuously) | DISCRIMINATING |
| OnlySendingNothingMaySkipTheWedge | 5 driven rows of the `sent_any` decision (not asserted about a table) all correctly distinguish nothing-sent from something-sent | misclassify any of the 5 concrete scenarios | DISCRIMINATING |
| WedgeResolutionInstallsExactlyOnce | a resolved wedge is recorded EXACTLY once (tail +2 for wedge+new chunk, not +3 for a double-apply) | double-apply a resolved wedge transaction | DISCRIMINATING |
| DenyGuardStopsAnAllocation (death) | `DENY_ALLOCATIONS_IN_SCOPE` actually aborts on a real allocation in this binary | let the guard compile to a no-op where it should be armed | DISCRIMINATING |
| InstallRegionProbeIsInvokedAndTheGuardIsArmed | the install-region probe is invoked AND the allocation-deny guard is armed exactly there | let the install region skip the probe or the guard | DISCRIMINATING |
| WritingOwnsTheAttemptUntilInstall | the lane state is `Writing` for every durable-but-uninstalled transaction, `Ready` once fully installed | leave the lane in the wrong state during/after install | DISCRIMINATING |
| UnresolvedTransfersWritingToWedged | ambiguity transfers the exact attempt from Writing to Wedged | leave the lane in Writing or Ready after an ambiguous attempt | DISCRIMINATING |
| WedgeResolutionReturnsReady | a durable resolution installs the attempt and returns Ready | leave the lane Wedged after a proven resolution | DISCRIMINATING |
| ConclusiveForeignConflictFaultsTheLane | a foreign occupant is a terminal Faulted verdict, never a wedge | wedge on a proven conflict instead of faulting | DISCRIMINATING |
| DefiniteFailureReturnsReady | a definite (proven non-durable) failure returns Ready, never wedges | wedge on a proven-non-durable failure | DISCRIMINATING |
| WedgeResolutionProvenForeignFaultsTheLane | a foreign object discovered at the wedged key on resolution faults the lane (CORRUPTED_DATA, never aborts the process) | let foreign interference at the wedge key crash the server or silently resolve wrong | DISCRIMINATING |
| PostDurableInstallFailureRequiresRecovery | a simulated post-durable install failure transitions Writing→NeedsRecovery, event counted exactly once | leave the lane silently broken instead of visibly NeedsRecovery | DISCRIMINATING |
| WedgeResolutionInstallFailureRequiresRecovery | same failure at the wedge-resolution install: Wedged→NeedsRecovery, wedge cleared (ownership transfers to recovery) | leave a phantom wedge alongside NeedsRecovery | DISCRIMINATING |
| NeedsRecoveryReplaysBeforeALaterFlush | a later flush only returns Ready after re-deriving and installing the stranded transaction (both x and y verified gone) | let a later flush's own success be mistaken for the stranded transaction's installation | DISCRIMINATING |
| UncertainPrecommitKeepsItsCleanupOwnerAndItsBody | an Uncertain (wedged) precommit's body and cleanup ownership survive; abandon resolves the wedge and appends the exact removal | writer-delete a possibly-live precommit's body, clamping GC's fold barrier forever | DISCRIMINATING |
| UncertainPrecommitThatNeverLandedStillAbandonsCleanly | an Uncertain precommit that was provably never sent still abandons as a clean no-op (not CORRUPTED_DATA) | make abandon fail forever on a precommit that never actually landed | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_intake.cpp {#src-disks-tests-gtest-cas-ref-intake-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ManifestEdgesPerOperationShape | each of 6 op shapes (add-precommit, remove-committed, remove-precommit, promote, no-edge ops, replace) produces exactly the right edge delta | mis-derive the edge for any one shape (e.g. promote leaking a +1) | DISCRIMINATING |
| ManifestEdgesRejectsUnrecognizedShapes | 5 unrecognized owner_transition shapes all throw, never silently assigned edge meaning | assign a spurious edge meaning to an unrecognized shape | DISCRIMINATING |
| EdgesAreNamespaceQualified | identical ManifestRef tuples in different namespaces produce distinct manifest_ids | conflate edges across namespaces sharing a manifest tuple | DISCRIMINATING |
| RemovalTxnIdDetection | removal detection correctly flags a txn containing RemoveNamespace, not an ordinary one | misclassify an ordinary txn as removal or vice versa | DISCRIMINATING |
| GroupRefKeys | one global LIST groups by table/kind correctly; state-family keys excluded; malformed keys abort | let a malformed/state-family key silently pass grouping | DISCRIMINATING |
| PlanRefCleanupRequiresCheckpointNamedBase | 4 distinct incomplete-authority shapes (no ckpt base) all produce an EMPTY cleanup plan | let cleanup act on a listing-only signal without checkpoint authority | DISCRIMINATING |
| PlanRefCleanupRetainsCheckpointBaseLog | cleanup deletes covered logs but retains the exact checkpoint-base log | delete the checkpoint-base recovery witness | DISCRIMINATING |

## src/Disks/tests/gtest_cas_ref_lane_exception_safety.cpp {#src-disks-tests-gtest-cas-ref-lane-exception-safety-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| SoloLeaderThrowBeforeCarveDrainsOwnItem | a solo leader that throws before carving does not strand its own item in the pending queue | leave the leader's item stranded forever (unreachable, since nothing else can carve it) | DISCRIMINATING |
| FollowerNeverRunsStrandedLeaderClosure | a follower leader NEVER runs a stranded prior leader's `build_ops` closure (use-after-free), queue drains, non-faulted caller completes | let a follower carve and execute a dead caller's stack-captured closure | DISCRIMINATING |
| PreTenureAllocFailureReleasesBaton | an allocation failure before leadership is published leaves the lane fully idle (item un-enqueued, baton un-taken) | leave the baton held with no live leader, hanging every later writer forever | DISCRIMINATING |

## src/Disks/tests/gtest_cas_ref_protocol.cpp {#src-disks-tests-gtest-cas-ref-protocol-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| CrossEpochFromSealShortCircuitsWithoutAnyRequest | both short-circuit cases (nothing consumed, caller-proven-not-a-seal) return correctly with ZERO backend requests | issue a request when the caller already has enough information to decide | DISCRIMINATING |

## src/Disks/tests/gtest_cas_ref_read_contract.cpp {#src-disks-tests-gtest-cas-ref-read-contract-cpp}

| test | property | killing mutation | verdict |
|---|---|---|---|
| HeldRuntimeAfterSameNameRebirthReadsStaleOrNotFoundNeverSuccessorRefs | a held reader runtime after a same-name rebirth answers stale-or-absent, never the successor's value; a genuinely fresh resolution (after eviction) does see the successor | let a held runtime silently pick up successor data without re-recovery | DISCRIMINATING |
| HotRefReadsThroughHeldRuntimeIssueZeroCatalogRequests | a warm ref read issues ZERO backend requests of any kind (not just zero catalog requests), with a positive control proving the cold path did touch the backend | make a warm read still hit the catalog or issue any backend I/O | DISCRIMINATING |
| StaleLifeDropRefusesAfterRebirthAndNeverTouchesSuccessor | a stale-life drop is refused (NETWORK_ERROR), catalog object byte-unchanged, successor's data untouched | let a stale-life drop resolve against and mutate the current (successor) row | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_log_format.cpp (51 tests) {#src-disks-tests-gtest-cas-ref-log-format-cpp-51-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RenderCanonicalForm | RefTxnId renders as fixed-width lowercase hex pair | mis-render width/case | DISCRIMINATING |
| RenderRejectsZeroComponent | render aborts on a zero epoch/sequence component (3 cases) | let a zero component render silently | DISCRIMINATING |
| ParseRoundTrip | 6 boundary values round-trip through render/parse | break round-trip for a boundary value | DISCRIMINATING |
| ParseRejects{Short,Long,Uppercase,ZeroComponent,NonHexGarbage,MisplacedSeparator} (6 tests) | parse fails closed on each of 6 malformed shapes | accept any malformed shape | DISCRIMINATING (×6) |
| OrderMatchesLexicalOrderOfRender | numeric ordering of RefTxnId matches lexical ordering of its rendered string (200 randomized comparisons) | break the fixed-width property that makes lexical==numeric order | DISCRIMINATING |
| ClosedSetPinsRefOpKindWords | encode/decode word tables for the 5 RefOpKind values are the same table | let the two tables diverge for one kind | DISCRIMINATING |
| RoundTrip{NamespaceBirth,RemoveNamespace,SetPublishedAt,SetPublishedAtZeroTimestamp,OwnerTransitionAdd,OwnerTransitionRemoval,OwnerTransitionReplace,MultipleOpsInOneTransaction} (8 tests) | each op shape round-trips correctly | corrupt a field on round-trip for any shape | DISCRIMINATING (×8) |
| DecodeRejectsRemovedPayloadFieldInOpRecord | a retired wire field (`"pl"`) spliced back in fails closed, not silently tolerated | let a removed field be silently skipped as "unknown" | DISCRIMINATING |
| OwnerTransitionBindingGroupsAreAbsentOrComplete | old/new binding field groups are all-or-nothing (absent group decodes fine, half-present group fails closed) | accept a half-populated binding group | DISCRIMINATING |
| PeekReadsTheMetaIdentityOfASealedRefLog | peek reads ns/epoch/sequence from a real sealed object via the codec's own key constants | let peek silently stop matching after a key rename (returns nullopt on good data) | DISCRIMINATING |
| PeekAnswersNulloptForBytesThatAreNotARefLog | peek never throws, answers nullopt for non-ref-log bytes | let peek throw into the anomaly reporter instead of returning nullopt | DISCRIMINATING |
| ByteIdenticalReencode | re-encoding a decoded multi-op txn is byte-identical | let re-encoding drift | DISCRIMINATING |
| EncodeRejectsZeroTxnId | zero txn_id components rejected | accept a zero component | DISCRIMINATING |
| DecodeRejectsTruncatedBuffer | a missing trailing terminator fails closed | tolerate truncation | DISCRIMINATING |
| DecodeRejectsBodyNamespaceMismatch / …TxnIdMismatch (2) | decode cross-checks the body against the caller-supplied ns/id | trust the body's self-claimed identity without cross-checking | DISCRIMINATING (×2) |
| EncodeRejects{EmptyRefName,DotRefName,DotDotSegment,RepeatedSeparator,LeadingSlash,TrailingSlash,Backslash,NonCanonicalOwnerBindingRefName,EmbeddedNulRefName} (9 tests) | 9 distinct ref-name hygiene violations all rejected at encode | accept any of the 9 malformed ref-name shapes | DISCRIMINATING (×9) |
| EncodeRejectsTooManyOps / EncodeAllowsExactlyMaxOps (2) | op-count cap boundary: cap+1 rejected, cap exactly accepted | off-by-one the op-count boundary | DISCRIMINATING (×2) |
| EncodeRejectsOversizedNormalTransaction | a normal-class transaction over the byte cap is rejected | accept an oversized normal transaction | DISCRIMINATING |
| RemovalClassTransactionLiftsByteBudgetAboveNormalLimit / …StillRejectsBeyondRemovalLimit / …NotCappedOnOpCount (3) | removal-class transactions get a genuinely higher byte budget and no op-count cap, but still have SOME ceiling | let removal-class bypass all limits, or fail to actually lift the normal limit | DISCRIMINATING (×3) |
| EncodeAllowsExactlyMaxPerOpBytes / EncodeRejectsOversizedOpOnNormalTransaction (2) | per-op byte cap boundary exact accept/reject, isolated from the whole-txn cap | off-by-one the per-op boundary | DISCRIMINATING (×2) |
| EncodeAllowsExactlyMaxRemovalBytes | exact removal-class byte-cap boundary accepted | off-by-one the removal boundary | DISCRIMINATING |
| EncodeRejectsZeroManifestRefWriterEpochInOwnerBinding / …BuildSequence / …OutOfRangeOrdinal / …InSetPublishedAt (4 tests) | ManifestRef field validation shared by both codecs, 4 distinct zero/out-of-range shapes | accept an invalid ManifestRef component in any of the 4 shapes | DISCRIMINATING (×4) |
| RefLog (battery, golden bytes) | golden-byte encode/decode battery for the shape-level failure modes | reorder/rename a wire field | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_recovery_cas_walk.cpp (40 tests) {#src-disks-tests-gtest-cas-ref-recovery-cas-walk-cpp-40-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| HiddenMiddleLogDoesNotAffectCheckpointRecovery / HiddenTailLogDoesNotAffectCheckpointRecovery / CkptNamedBaseIsRecoveredWithoutStreamList (3) | recovery does zero stream LIST, uses `_ckpt`-bounded exact GETs for the middle/tail/base object even when a listing would hide it | make the walk fall back to a stream LIST when a listed range looks incomplete | DISCRIMINATING (×3) |
| MissingExactIdAtOrBelowCommittedFrontierIsCorruption | a durably-missing id at/below the committed frontier is corruption, checkpoint left untouched | tolerate the gap as "shorter stream" instead of throwing `CORRUPTED_DATA`, or advance the checkpoint past it | DISCRIMINATING |
| UncommittedSnapshotIsUnobservedWithoutStreamList | a physical but `_ckpt`-unnamed snapshot cannot raise the recovered cut | let recovery discover/trust a snapshot the checkpoint doesn't name | DISCRIMINATING |
| ListingShapeDoesNotAffectCheckpointRecovery | recovered result is identical whether or not the listing hides the checkpoint-named objects (cross-backend equivalence) | let the walk's result depend on listing completeness for any field (manifest_id/size/published_at) | DISCRIMINATING |
| PhantomListedSnapshotIsUnobserved / ListedFPlusTwoWithoutFPlusOneIsInertUncommittedDebris (2) | a listed-but-not-checkpoint-named snapshot, and listed debris above a gap, are both ignored | let recovery trust listing presence over the checkpoint-bounded exact walk | DISCRIMINATING (×2) |
| DuplicateCatalogLifeIsCorruptionBeforeColdRuntimeAdmission | two catalog entries sharing one incarnation is corruption caught before cold admission | let a cold open silently pick one of the duplicate entries | DISCRIMINATING |
| CheckpointAdvanceAfterLastLogProbeRestartsBeforeInstall | the walk's own final exact-GET is its linearization point; a concurrent advance after that GET forces a restart, never a stale install | let the walk install its private cut without a final authority recheck | DISCRIMINATING |
| LiveCatalogLifeWithoutReadableCheckpointIsCorruption | a Live catalog life with no readable `_ckpt` is corruption, not genesis | treat a missing `_ckpt` as "brand new, start at sequence 1" | DISCRIMINATING |
| AbsentIdBelowADurableHigherIdFailsClosed | a 404 strictly below a durable higher id is a hole -> `CORRUPTED_DATA`, never folded past | let the walk skip the hole and continue from the higher durable id | DISCRIMINATING |
| DeadEpochIsClosedByOurOwnSealAtTPlusOne | a dead epoch is closed by a real seal minted at exactly `{E,T+1}`, `CASRefRecoveryEpochSealed` increments, chain link is nullopt at the first seal | mint the seal at the wrong id, or skip creating a durable seal object | DISCRIMINATING |
| ConcurrentRecoverersSealIsAdoptedNotContested | a peer's already-landed seal at the exact slot is adopted, `CASRefRecoveryEpochSealAdopted` increments, same chain-link outcome as if minted | treat a peer's seal as a conflict/error instead of adopting it | DISCRIMINATING |
| StragglerAtTPlusOneIsAdoptedAndResealedAtTheNewTPlusOne | an ordinary straggler occupying the seal slot is adopted, `CASRefRecoveryStragglerAdopted` increments, reseal lands at the NEW T+1 (never T+2, never a hole) | mint the reseal at a blindly-advanced id instead of re-deriving from the adopted straggler | DISCRIMINATING |
| RecoveryPublishesEveryOccupiedObjectBeforeAdvancingPastIt (2 sub-cases: seal, straggler) | recovery certifies the occupied object via `_ckpt` before creating any successor; an ambiguous checkpoint CAS blocks the successor from ever landing | let recovery create the successor seal before the `_ckpt` CAS certifying the occupant succeeds | DISCRIMINATING (×2, parameterized via loop+SCOPED_TRACE) |
| TwoBurnedEmptyEpochsProduceTwoChainedSequenceOneSeals | every burned-but-empty epoch still gets its own sequence-1 seal, chained via `prev_epoch_seal`, and the LIVE epoch is never sealed | skip sealing an empty epoch, or seal the live epoch, or drop the chain-link field | DISCRIMINATING |
| RecoveryPublishesEachCreatedSealBeforeCreatingTheNextEpochSeal | across a multi-epoch burn, the first seal is durable and `_ckpt`-certified before a second seal object may be created, and a cold restart resumes correctly from that partial state | let the walk create a second empty-epoch seal before the first is certified in `_ckpt` | DISCRIMINATING |
| RecoveryPublishesAnAdoptedStragglerBeforeCreatingItsFollowingSeal | an adopted straggler must be `_ckpt`-certified before its following seal may be created | create the following seal before the straggler is certified | DISCRIMINATING |
| GenesisAtEpochFiveWritesNoPhantomSealsBelowLifeEpoch | a namespace born at epoch 5 gets no seals for epochs 1-4 and `nullopt` chain link (true genesis) | write a phantom seal for a pre-birth epoch, or set a non-null chain link at genesis | DISCRIMINATING |
| FenceBumpedMidWalkRefusesTheInstallAndTheRetrySucceeds / FenceBumpedAfterSlotOccupyBeforeCkptCasAdvancesNoCheckpoint / FenceBumpedAfterCkptCasBeforeInstallPublishesNoState (3) | the trio's three fence-recheck seams (mid-walk, post-slot-occupy-pre-ckpt-cas, post-ckpt-cas-pre-install): each is caught, nothing is installed under a stale generation, a subsequent remount+retry succeeds | drop the install recheck at any one of the three seams, letting a stale-generation walk install | DISCRIMINATING (×3) |
| RetiredLifePausedInRealRecoveryIoWritesAndInstallsNothing | a predecessor life retired mid-recovery-I/O: zero further writes to the predecessor's keys, zero touches of the successor's key prefix, nothing installed | let the paused recovery resume writing to the retired life's keys, or install after resume | DISCRIMINATING |
| RemountBarrierBlocksUntilAPausedRecoveryAcknowledgesCancellation | `cancelRefRecoveriesAndAwaitQuiescence` genuinely BLOCKS (proven false before release, true after join) until a real parked I/O thread acknowledges; zero `_ckpt` CAS, zero install for the cancelled run | let the barrier return immediately without waiting for the in-flight recovery | DISCRIMINATING |
| CancellationStopsTheWalkBetweenTwoReadsOfOneCall | cancellation is checked between the two GETs inside one internal call (`readCheckpointSnapshotBase`), not only once per outer call | check cancellation only at the outer-call boundary, letting the snapshot-body GET fire after a cancel request | DISCRIMINATING |
| NeedsRecoveryReplaysTheStrandedTxn | after an injected install-region failure strands a durable transaction, the lane sits in `NeedsRecovery`, and the NEXT touch replays it and returns the lane to `Ready` | let the lane silently drop the stranded transaction on next touch, or leave it stuck in `NeedsRecovery` | DISCRIMINATING |
| WriterRecoveryAdoptsOneExactUnfrontieredSuccessorAndPublishesItsFrontier / ColdWriterRecoveryPublishesOneExactUnfrontieredSuccessorBeforeSealing (2) | a durable-but-unfrontiered writer successor is adopted (not re-derived from an in-memory attempt) and its exact frontier is published before any dead-epoch seal | mint a NEW successor instead of adopting the durable one, or seal before frontier-publishing it | DISCRIMINATING (×2) |
| WriterRecoveryAdoptsFirstCommittedTxnAboveLifeEpochOnlyCheckpoint | recovery grounds correctly at a birth-only checkpoint (`committed_through=nullopt`) using the log successor, not a hinted snapshot | let the walk require a snapshot even when only a life-epoch-only checkpoint exists | DISCRIMINATING |
| WriterRecoveryRestartsWhenCheckpointAdvancesPastPrivateCandidate | a concurrent writer's frontier-publish between recovery's proof-of-absence and its own `_ckpt` CAS forces a full restart+replay, never an `IdenticalSkip` below the new checkpoint | accept the stale private candidate as complete instead of restarting from the newer frontier | DISCRIMINATING |
| WriterRecoveryRejectsTwoUnfrontieredSuccessorsAfterExactCheckpointReread | two candidate successors above the checkpoint (an ambiguous write's retained bytes plus a second real one) is corruption, and neither is laundered into the frontier | pick either successor as the winner instead of throwing `CORRUPTED_DATA` | DISCRIMINATING |
| WriterRecoveryRejectsDifferentOrdinaryBytesAtTheRetainedSuccessorSlot | the object at the retained successor slot must byte-match the writer's own attempt, or it's corruption | accept a byte-mismatched occupant as if it were the writer's own successor | DISCRIMINATING |
| RetainedOldWriterAttemptLosesConclusiveToASuccessorSeal | a successor seal beats the old writer's retained ordinary bytes; lane returns to `Ready`, checkpoint records the seal as both frontier and chain link | let the old writer's stale attempt win over a durable successor seal | DISCRIMINATING |
| UnresolvedSealSlotFailsClosedWithoutInstalling | an `Unresolved` slot-occupy write exhausts its retry budget and fails closed, leaving the table unrecovered | resolve `Unresolved` optimistically (assume success or assume failure) instead of failing closed | DISCRIMINATING |
| ALatePredecessorPutAtTheSealedSlotIsRefusedByTheStore | the store itself (write-once conditional create) refuses a late predecessor's ghost PUT at the already-sealed key; the seal is untouched | let the ghost's create silently succeed over the seal, or let the occupant get overwritten | DISCRIMINATING |
| UndecodableOccupantAtTheSealSlotFailsClosedAndLeavesRecoveryRestartable | an undecodable object at the exclusively-owned seal slot fails closed with `CORRUPTED_DATA`, and `recovery_in_progress` is cleared so a second touch retries rather than hanging | let recovery adopt/skip the undecodable occupant, or leave the in-progress flag stuck | DISCRIMINATING |
| ASecondCallerWaitsForTheWalkInsteadOfRacingIt | a second concurrent `listRefs` call blocks on the in-flight walk (`recovery_in_progress`) rather than racing an independent walk; only one seal is minted | drop the serialization, letting two walks race and the loser adopt (still correct outcome, but the invariant "exactly one walk mints" would break) | DISCRIMINATING |
| RecoveryStartsAtRecreatedLifeGenesisAndLeavesPredecessorStreamUntouched | a checkpoint-grounded recovery of a recreated life starts at the NEW life's own genesis and never extends the predecessor life's stream | let the walk fall through into the predecessor epoch's stream instead of stopping at the new life's `life_epoch` | DISCRIMINATING |
| PutHookBackendComposesHidingListBackendCasPutFaultInjection | the property its name promises is a fixture-composition property: `PutHookBackend::write` must route through `HidingListBackend::write` so both hook layers fire on one instance | the direct killing mutation lives in this TEST FILE's own class hierarchy (make `PutHookBackend::write` call `CountingBackend::write` directly, skipping its parent), not in engine/pool/GC/ledger production code; it only incidentally also catches a production regression that drops the conditional token from `replace` | WEAK |

Summary for gtest_cas_ref_recovery_cas_walk.cpp: 40 tests, 39 DISCRIMINATING, 1 WEAK (PutHookBackendComposesHidingListBackendCasPutFaultInjection -- tests fixture-class composition, not production code).
## src/Disks/tests/gtest_cas_ref_snapshot_format.cpp (29 tests) {#src-disks-tests-gtest-cas-ref-snapshot-format-cpp-29-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| RoundTripLive / RoundTripLiveEmpty / RoundTripPrecommitsSameNameDifferentManifest (3) | committed+precommit rows, and the empty case, round-trip exactly | corrupt a field on round-trip | DISCRIMINATING (×3) |
| DecodeRequiresLifecycleField / DecodeRejectsTerminalLifecycleWord (2) | a missing lifecycle field, and a non-`live` terminal lifecycle word, both fail closed | tolerate a missing/terminal lifecycle as if it were `live` | DISCRIMINATING (×2) |
| DecodeRejectsRetiredRemoveTxnEpochField / …SequenceField / …FieldPair (3) | 3 retired-field-resurrection shapes (`rte`, `rts`, both) all fail closed rather than being tolerantly skipped | silently skip an unrecognized field instead of failing on the specific retired-field name | DISCRIMINATING (×3) |
| DecodeRejectsRemovedPayloadFieldInCommittedRow | a spliced-back retired `"pl"` field fails closed, not silently dropped | let the removed payload field decode past unnoticed | DISCRIMINATING |
| DecodeRejectsUnknownRowKindWord | an unrecognized row `"kind"` word fails closed rather than being skipped (which would silently drop a ref) | let an unknown kind word be tolerantly skipped | DISCRIMINATING |
| ByteIdenticalReencode | re-encoding a decoded snapshot is byte-identical | let re-encoding drift (e.g. field-order nondeterminism) | DISCRIMINATING |
| MaximalRefSequenceRoundTripsAsADecimalString | `UINT64_MAX` sequence round-trips via the decimal-string representation, not JSON number semantics | switch the wire encoding to a JSON number for `snapshot_seq`, silently losing precision at the top of range | DISCRIMINATING |
| EncodeRejectsZeroSnapshotId / …UnsortedCommitted / …DuplicateCommittedRefName / …UnsortedPrecommits / …PrecommitsSameNameWrongManifestOrder / …DuplicatePrecommitBinding / …NonCanonicalCommittedRefName / …NonCanonicalPrecommitRefName / …PrecommitWrongKind / …ZeroManifestRefFields (2 sub-cases) (10 tests) | 10 distinct encoder-side invariant violations (zero id, unsorted, duplicate, bad tiebreak, non-canonical name, wrong owner kind, zero/out-of-range ManifestRef fields) all rejected at encode | accept any of the 10 malformed shapes | DISCRIMINATING (×10) |
| EncodeRejectsOversizedSnapshot / EncodeAllowsExactlySnapshotMaxBytes (2) | byte-cap boundary: cap+1 rejected, cap exactly accepted and round-trips | off-by-one the byte-cap boundary | DISCRIMINATING (×2) |
| DecodeRejectsTruncatedBuffer / DecodeRejectsOversizedBufferDirectly (2) | a missing terminator, and an over-cap buffer with no terminator inside the line-cap window, both fail closed | tolerate truncation, or drop the line-cap guard | DISCRIMINATING (×2) |
| DecodeRejectsNamespaceMismatch / DecodeRejectsSnapshotIdMismatch (2) | decode cross-checks the body against the caller-supplied ns/id | trust the body's self-claimed identity without cross-checking | DISCRIMINATING (×2) |
| RefSnapshot (battery, golden bytes) | golden-byte encode/decode battery for shape-level failure modes | reorder/rename a wire field | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_snapshot_publish_ordering.cpp (5 tests) {#src-disks-tests-gtest-cas-ref-snapshot-publish-ordering-cpp-5-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| SnapshotBodyIsDurableBeforeCheckpointAdvances | INV-4: the snapshot body PUT is journaled strictly before the `_ckpt` CAS that names it, using per-attempt call-count controls to rule out retry noise | reorder the publisher to CAS `_ckpt` before/without confirming the body PUT | DISCRIMINATING |
| AdoptionHappensLastAndOnlyAfterBothDurableEffects | in-memory adoption (`newestPublishedSnapshotId`) happens only after BOTH the body is durable AND `_ckpt` advanced; a persistent `_ckpt` conflict leaves adoption unset even though the body committed | adopt the snapshot in memory before or independent of the `_ckpt` CAS succeeding | DISCRIMINATING |
| NeedsRecoveryLaneRecoversBeforeAnySnapshotPublication | a poisoned (`NeedsRecovery`) lane recovers (re-walks the durable stream) strictly before any snapshot-publish effect; the eventually-published snapshot's id is exactly the recovered frontier, not "some snapshot" | let the publisher build a snapshot from the stale, unrecovered cache, or publish before recovery's own checkpoint catch-up CAS | DISCRIMINATING |
| PublishBackoffDecisionsAreCharacterized | `advancePublishBackoff`/`resetPublishBackoff` double 1000->2000->4000(capped) on a controlled boot clock, refuse below each deadline, admit at/after it, and reset to the INITIAL interval (not the cap) after a durable publish | change the doubling factor, drop the cap, or have reset continue from the stale cap instead of the initial interval | DISCRIMINATING |
| NotReadyRefusalBacksOffAndResetsAfterDurablePublish | an admitted `NotReady`(`Wedged`)-lane refusal arms/doubles the SAME bounded cooldown (200ms->...->30000ms capped) as a write-fault refusal, and a durable publish resets it to the initial interval even while a capped cooldown was still armed | let a not-ready refusal skip arming the cooldown, or let the doubling/cap/reset arithmetic diverge from the write-fault path's | DISCRIMINATING |
## src/Disks/tests/gtest_cas_ref_statemachine.cpp (67 tests) {#src-disks-tests-gtest-cas-ref-statemachine-cpp-67-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| BirthFromNeverBornAccepts / BirthWhileLiveRejectedAndStateUnchanged / BirthAfterRemovalAccepts (3) | birth transitions Never-Born->Live, rejects Live->Live, accepts Removed->Live | let birth succeed while Live, or fail after Removed | DISCRIMINATING (×3) |
| OwnerTransitionWhileNeverBornRejected / SetPublishedAtWhileNeverBornRejected / RemoveNamespaceWhileNeverBornRejected / OpsWhileRemovedRejectedExceptBirth (4) | every non-birth op is rejected outside Live (Never-Born, Removed), state unchanged on throw | admit any op outside Live | DISCRIMINATING (×4) |
| AddPrecommitAccepts / AddPrecommitRejectsExactDuplicate / …ConflictingManifestUnderDifferentName / …ManifestAlreadyCommittedElsewhere / …AllowsDifferentManifestsRacingForSameName (5) | precommit admission: accept, reject exact dup, reject cross-owner manifest collision (both vs precommit and vs committed), allow legitimate same-name different-manifest race | drop the cross-owner `owned_manifests` uniqueness check | DISCRIMINATING (×5) |
| RemovePrecommitAccepts / …RejectsAbsentBinding / …RejectsWrongManifest / RemoveCommittedAccepts / …RejectsAbsentRef / …RejectsWrongManifest (6) | remove ops require the EXACT (name, manifest) binding present; wrong manifest or absent binding rejected | let removal succeed against a wrong/absent binding | DISCRIMINATING (×6) |
| PromoteRejectsAbsentPrecommit / PromoteAtomicityNoOwnerlessIntermediate / PromoteWithSetPublishedAtInSameTxnInstallsTimestamp / PromoteRejectsDisplacingAnotherCommittedManifest / PromoteAcceptsAfterExplicitRemovalOfStaleCommitted / OwnerTransitionRejectsInvalidCombinations (6) | promote requires an exact matching precommit, never silently displaces a different committed row under the same name, and 3 invalid owner-transition shapes are all rejected | let promote silently displace, or accept an invalid old/new-binding shape | DISCRIMINATING (×6) |
| SetPublishedAtRejectsWhenRefAbsent / …RejectsManifestMismatch / …AcceptsAndReplacesTimestamp (3) | set_published_at requires the exact committed (name, manifest), replaces (not accumulates) the timestamp | accept a mismatched manifest, or fail to replace the timestamp | DISCRIMINATING (×3) |
| RemoveNamespaceAloneOnEmptyTableAccepted / CatalogedNeverBornLifeAcceptsAtomicEmptyBirthAndRemoval / RemoveNamespaceDrainingOwnersInSameTxnAccepted / …RejectsWhenOwnersRemain / …MustBeFinalOp / …RejectsNonRemovalEarlierOp (6) | remove_namespace requires empty owner sets, is the terminal final op, and every earlier op in the same txn must itself be an owner-removal | admit remove_namespace with owners still present, mid-transaction, or preceded by a non-removal op | DISCRIMINATING (×6) |
| WholeTxnAtomicityLastOpFailureLeavesStateUntouched | a failing LAST op rejects the WHOLE transaction, including earlier-succeeding ops | let earlier ops of a rejected transaction leak into the live state | DISCRIMINATING |
| ContiguousTxnIdsRejectEqualLowerAndSkipped | INV-1 dense ids: equal/lower/skipped/wrong-epoch-restart ids all rejected; INV-2 chain-link required crossing an epoch | admit a non-contiguous id, or an epoch crossing with no/wrong `prev_epoch_seal` | DISCRIMINATING |
| SnapshotOfSortsCommittedAndPrecommits / SnapshotOfRefusesTerminalState (2) | `snapshotOf` produces canonically-sorted, re-encodable output and refuses a terminal (Removed) state | emit unsorted output, or allow snapshotting a Removed table | DISCRIMINATING (×2) |
| ReplayFromNoSnapshot / ReplayFromSnapshotPlusTail / StateFromSnapshotConstructsLiveState (3) | replay from nothing, from a snapshot+tail, and snapshot->state construction all produce the expected live state | drop tail application, or mis-set lifecycle from a snapshot | DISCRIMINATING (×3) |
| ReplayRejectsTailNsMismatchAgainstSnapshot / …AcrossEntries (2) | replay cross-checks namespace consistency both against the base snapshot and across tail entries | drop either cross-check | DISCRIMINATING (×2) |
| ReplayRejectsHandBuiltSnapshotWithDuplicateCommittedName / …WithUnsortedPrecommits (2) | a hand-built (never-codec-validated) snapshot with a duplicate name or unsorted precommits is rejected by `replay` itself, not silently collapsed via map-emplace | let `replay`/`stateFromSnapshot` silently collapse a duplicate instead of throwing | DISCRIMINATING (×2) |
| ReplayEquationPropertyTest | randomized property: `replay(snapshotOf(mid), tail) == replay(nullopt, full history)` for 30 random valid histories cut at random points | break the snapshot/resume equivalence for any op kind or cut point | DISCRIMINATING |
| LiveAppendRejectsAddPrecommitCollidingWithCommitted / ReplayRejectsTailAddPrecommitCollidingWithCommitted / …CollidingWithPrecommit / ReplayRejectsSnapshotWithTwoCommittedRowsNamingOneManifest / …CommittedAndPrecommitSharingManifest / …TwoPrecommitsSharingManifest (6) | the O(1) cross-owner manifest-uniqueness check runs unconditionally on BOTH the append path and the replay/snapshot-validation path, for all 3 collision shapes | make the uniqueness check debug-only (elided in release), or skip it on the replay/snapshot path | DISCRIMINATING (×6) |
| TrustedReplayEquivalentToLiveAppendOnValidTail / E3TrustedReplayInPlaceMatchesLiveAppendAcrossAllArms (2) | the in-place TrustedReplay path and the scratch-copy LiveAppend path produce byte-identical states across every `applyOp` arm | let the in-place path diverge from LiveAppend on any op kind (e.g. a counter or index update missed on the no-copy path) | DISCRIMINATING (×2) |
| E3LiveAppendLaterOpThrowLeavesPopulatedStateByteIdentical / …FirstOpThrowLeavesPopulatedStateByteIdentical (2) | LiveAppend's two-phase scratch-copy leaves a POPULATED state byte-for-byte unchanged on either a later- or first-op throw | let a rejected transaction's earlier-succeeding op leak into the populated live state | DISCRIMINATING (×2) |
| E3AdmitsPreviewLeavesStateByteIdentical | `admits` is a pure query: state is untouched by either verdict | let `admits` mutate the state it previews | DISCRIMINATING |
| E3TrustedReplayPoisonOnBadTailIsInternal | a poisoned in-place replay state from a failed call never escapes to affect an independent later replay of the valid prefix | leak the poisoned internal state across calls (e.g. via a shared/static scratch) | DISCRIMINATING |
| AdmitsAcceptsWellUnderBudget | admits() accepts a trivially small growth against a huge (1,000,000-byte) budget | make `admits` always return false | DISCRIMINATING (loose bound, but a real always-false mutation is caught) |
| AdmitsRejectsGrowthPastSnapshotBudgetOwnerTransitionAdd / …SetPublishedAt / …PromoteWithSetPublishedAt / AdmitsRejectsGrowthPastRemovalBudget (4) | `admits`'s internal size prediction matches the TRUE post-op encoded size exactly, at the exact byte boundary, for 3 snapshot-growth shapes and the removal-budget bound independently | off-by-one `admits`'s internal size estimate for any op kind or bound | DISCRIMINATING (×4) |
| AdmitsExactnessPropertyTest | randomized property: `admits`'s snapshot-bound and removal-bound predictions exactly match the real encoders, across 20 random states and 3 candidate op shapes | let `admits`'s internal estimate drift from the real encoders for any state/op/bound combination | DISCRIMINATING |
| FramingPlusRowsEqualsFullEncode / FramingPlusOpsEqualsFullRemovalEncode (2) | the incremental framing-size + per-row/per-op size helpers sum EXACTLY to a full real encode | let a size helper drift from the real encoder (e.g. after a wire-field change) | DISCRIMINATING (×2) |
| CountersTrackRowsThroughEveryOpKind | the state's cached `snapshot_body_bytes`/`removal_body_bytes` counters exactly track an independent recomputation through add/promote/restamp/remove | let the incremental counter drift from a full recomputation on any op kind | DISCRIMINATING |
| AccessorsEqualFullEncodeRandomized | randomized property: `encodedSnapshotBudgetSize`/`encodedRemovalBudgetSize` accessors match the real encoders exactly, across 30 random states | let either budget-size accessor drift from the real encoder | DISCRIMINATING |

Summary for gtest_cas_ref_statemachine.cpp: 67 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_ref_wedge_every_attempt.cpp (29 tests) {#src-disks-tests-gtest-cas-ref-wedge-every-attempt-cpp-29-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AmbiguousPutWedgesTheLaneAndTheNextFlushsCreateAdoptsItExactlyOnce | an all-ambiguous append wedges the lane; the NEXT flush's bounded create adopts it exactly once (tail count +2, not +1 or +3), before its own new commit | resolve the wedge via a bare GET (report "absent" forever, no self-heal), or double-apply the adopted txn | DISCRIMINATING |
| DurableCreatedWedgeNeedsRecoveryWhenItsFrontierCannotBePublished | a durable-but-unfrontiered adopted wedge whose `_ckpt` CAS then fails moves the lane to `NeedsRecovery`, not back to `Ready`, and does NOT install into the resident tail | let an unfrontiered adoption install anyway, or return to `Ready` without a confirmed frontier | DISCRIMINATING |
| RetiredLifeRefusesWedgeRetryBeforeAnyRequestOrAdoption | a wedge retry against a life retired mid-pause is refused BEFORE any request or read (0 puts, 0 gets), and the successor life is unaffected | let the stale retry send a request or adopt against a retired life | DISCRIMINATING |
| OwnLandedAttemptIsAdoptedFromOccupiedWithoutDoubleApply | our own landed-but-ack-lost attempt is adopted from the `Occupied` verdict exactly once (tail +2, exactly 1 extra create at the key) | double-apply the landed attempt, or fail to adopt it | DISCRIMINATING |
| DefiniteRefusalOfARetryAttemptKeepsTheLaneWedged | a later attempt's definite refusal cannot retire an EARLIER ambiguous attempt of a DIFFERENT call; the lane stays wedged at the same id until create-based resolution | let a later-call definite refusal clear an unrelated earlier wedge | DISCRIMINATING |
| ADefiniteRefusalCannotSpeakForAnEarlierAmbiguousAttemptOfTheSameCall | within ONE call, ambiguous-then-refused ends at the call's own deadline as `GaveUp{sent_any=true}`, never as a proven `Refused`; the CONTROL (no ambiguous predecessor) still returns `Refused` | let a later attempt's refusal within the same call retire an earlier ambiguous attempt (report `Refused` instead of `GaveUp`) | DISCRIMINATING |
| ADefiniteRefusalAfterAnAmbiguousAttemptOfTheSameCallStillWedgesTheLane | the ledger-side twin: this one-call ambiguous-then-refused sequence wedges the lane (never `DefiniteFailure`, never frees the id), and still self-heals on the next flush | let the lane return to `Ready` and free the id after a same-call refusal following an ambiguous attempt | DISCRIMINATING |
| SuccessorSealAtTheWedgedKeyRejectsConclusivelyAndSourcesPrevEpochSeal | a successor's seal at the wedged key is the CONCLUSIVE rejection: wedge clears, tail unmoved, lane -> `Closed`, seal becomes `lastEpochSeal`; a dying lane keeps re-deriving the same T+1 and never mints T+2; no fence/remount on this designed path | adopt a successor's seal as our own commit, mint past it, or fence/remount on this expected path | DISCRIMINATING |
| OrdinaryFirstAppendAfterASealedTransitionCarriesTheExactPrevEpochSeal | the first append of a new epoch carries the recovered `last_epoch_seal` as `prev_epoch_seal` on sequence 1 ONLY, never on sequence 2 | omit the chain link on sequence 1, or leak it onto sequence 2 | DISCRIMINATING |
| GenesisBirthAtAHighEpochCarriesNoPrevEpochSeal | a namespace whose OWN stream starts here (genesis) carries no `prev_epoch_seal` even at a high global epoch | stamp a chain link at genesis just because the epoch number is high | DISCRIMINATING |
| ForeignNonSealOccupantIsCorruptedDataAndSchedulesARemount / AppendSiteProvenDifferentObjectAlsoSchedulesARemount (2) | a foreign (non-seal, not-ours) occupant at the wedged/derived key is `CORRUPTED_DATA`, wedge kept for inspection (wedge site) resp. never wedged (append site), and BOTH sites schedule a remount | let either site swallow the impossible occupant silently, or skip scheduling a remount | DISCRIMINATING (×2) |
| RetryUnderAnOlderAdmissionGenerationSendsNothing | a wedge retry under a stale admission generation is refused PRE-ATTEMPT (put count unchanged), wedge intact | let a stale-generation retry reach the store | DISCRIMINATING |
| ResultReleasedAfterAFenceBumpAndSuccessorSealIsInert / ResultReleasedAfterTheWedgeIdentityChangedIsInert (2) | a result released after the runtime's (generation, id, bytes) identity moved mid-I/O is INERT: no ack, no unwedge, no install, no adoption of a stale seal | act on a released result without rechecking the exact captured identity (generation+id+bytes) | DISCRIMINATING (×2) |
| KnownDurableInstallFailureMovesDirectlyToRecovery | a proven-durable adoption whose install throws moves straight to `NeedsRecovery` (never re-wedges), and the durable drop is replayed on the next touch | let a known-durable install failure re-wedge instead of transferring to recovery, or drop the durable transaction | DISCRIMINATING |
| AppendSiteMeetingASuccessorSealIsAConclusiveRejectionNotInterference | an ORDINARY append (no wedge involved) meeting a successor's seal at its derived id is the SAME conclusive rejection: seal recorded, `CASRefAppendSealRejected` counted, no fence, no remount | treat this as interference (fence/remount), or fail to record/count the seal | DISCRIMINATING |
| CreationCkptSurvivesAConclusiveFirstRefLogRejection / …ALaterConclusiveRejection / …WhenTheFirstNamespaceBirthIsAmbiguous / …WhenTheFirstNamespaceBirthNamesNoOccupant / …FirstNamespaceBirthForeignInterference (5) | the creation `_ckpt` survives byte-for-byte across all 5 first/later-transaction failure shapes (conclusive reject, ambiguous, unreadable occupant, foreign interference) -- the retired cleanup that used to delete it is gone | reintroduce a `_ckpt` cleanup call on any of these 5 branches | DISCRIMINATING (×5) |
| AppendSiteWedgesWhenTheSettlingReadNamesNoOccupant / …WhenTheSettlingReadItselfIsRefused (2) | a conflict naming NO occupant (settling read proves absence, or the read is itself refused) is a WEDGE, not a guess in either direction: counted (`CASRefAppendOccupantUnreadable`+`CASRefAppendWedged`), no fence/remount/seal-record, and recoverable on the next flush | terminate this arm as `Faulted` instead of a recoverable wedge, or guess a verdict without observation | DISCRIMINATING (×2) |
| WellFormedNonSealOccupantIsStillForeign | a perfectly decodable, well-formed (but non-seal) occupant of this exact namespace/id is STILL classified `Foreign` on content, not merely on decode failure | classify "foreign" purely by decode failure, missing a well-formed non-seal impostor | DISCRIMINATING |
| ALiveEpochSealIsNeverStampedAsItsOwnPrevEpochSeal | a deposed lane whose only known seal closes the epoch it is ITSELF trying to open fails closed locally (no request sent) with `INVALID_STATE` and the exact deposition message, never stamps a self-pointer, and repeats identically on a second flush with no remount | send the self-pointing request anyway (would be refused at ENCODE), or spend a request/schedule a remount on a case the lane can already prove | DISCRIMINATING |
| RefusedReturnsTheAttemptToReadyAndDoesNotWedge | a store-proven `Refused` write returns the lane to `Ready`, never wedges (nothing was written), and the id is reusable by the very next caller | wedge on a proven refusal, or fail to free the id | DISCRIMINATING |
| PostCommitFenceLossWedges | a create that commits and only then loses its admission is reported unresolved and WEDGES over the now-durable object -- the call never claims a commit it can't stand behind | let the call report success after a post-commit fence loss, or fail to wedge over the durable object | DISCRIMINATING |
| LivenessPredicateWithoutGenerationTermStillRefusesARetiredRuntime | a runtime retired mid-write (fence generation UNCHANGED) still refuses the caller via the runtime-liveness term alone, not the generation term | let liveness collapse to "generation unchanged implies live", missing a retired-but-same-generation runtime | DISCRIMINATING |

Summary for gtest_cas_ref_wedge_every_attempt.cpp: 29 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_ref_writer.cpp (101 tests, part 1 of 3) {#src-disks-tests-gtest-cas-ref-writer-cpp-101-tests-part-1-of-3}

| test | property | killing mutation | verdict |
|---|---|---|---|
| EmptyNamespaceRecoversToEmptyState | a never-touched namespace resolves to empty with zero recovery restarts | let an untouched namespace throw or record a spurious restart | DISCRIMINATING |
| ListRefsOnAbsentNamespaceDoesNotMutateCatalog / ResolveRefOnAbsentNamespaceDoesNotMutateCatalog / DropNamespaceOnAbsentNamespaceDoesNotMutateCatalog (3) | 3 read/no-op paths on an absent namespace perform ZERO catalog mutation (byte+etag identical before/after) | let any of the 3 probes mint a catalog row for an absent namespace | DISCRIMINATING (×3) |
| ColdReadRejectsCatalogLifeReplacedWithoutLocalInvalidation / …ReplacementByExternalPoolActor / …RejectsAliasingIncarnationAdmittedBetweenObservations (3) | a cold reader's two-observation catalog-life check refuses when the life was replaced (locally, by an external Pool, or by an aliasing incarnation) between its two reads | let any of the 3 races publish a runtime against a life the reader didn't actually observe live | DISCRIMINATING (×3) |
| ColdReadAdmitsThroughUnrelatedCatalogMutationBetweenObservations | the narrowed per-row check does NOT refuse on an unrelated namespace's admission between the two observations | widen the check back to whole-catalog stillness, starving cold admissions under concurrent load | DISCRIMINATING |
| WarmReadableRuntimeDoesNotReadCatalog | a warm cached runtime performs ZERO catalog GET on a subsequent read | make a warm read re-read the catalog every time | DISCRIMINATING |
| BirthOnlyLogNoSnapshotRecoversToEmptyLiveTable / BirthPlusPrecommitPromoteAcrossTwoLogsNoSnapshot (2) | recovery from raw log-only fixtures (birth alone; birth+precommit split across 2 logs) with no snapshot reconstructs the correct live state | fail to replay a snapshot-less tail correctly | DISCRIMINATING (×2) |
| TerminalGapBelowCheckpointFrontierIsCorruptionNotSameLifeRebirth | a same-epoch rebirth-shaped gap below the checkpoint frontier is corruption, and an unrecovered malformed life must not allocate its next writer position | treat the gap as a legal same-life rebirth, or allocate past an unrecovered life | DISCRIMINATING |
| SnapshotPlusTailRecovery | a checkpoint-named snapshot plus tail recovers correctly, and a STALE log AT/BELOW the snapshot id is ignored (proven by a log that would throw if wrongly replayed) | replay the stale below-snapshot log, corrupting the result | DISCRIMINATING |
| RestartOnVanishConvergesOnNewerSnapshot | a checkpoint-named snapshot vanishing mid-GET while a newer checkpoint is concurrently published makes recovery RESTART on the newer exact checkpoint, converging correctly (not a corrupt mix) | treat the vanish as corruption instead of restarting, or mix old+new state | DISCRIMINATING |
| DifferentBytesAtSelectedSnapshotIsCorruptionNotRestart | a DIFFERENT (cross-namespace) valid object at the exact selected snapshot key is corruption, not a restart signal | conflate "wrong object present" with "vanished" and restart instead of failing closed | DISCRIMINATING |
| CommittedChunkPublishesFrontierBeforeInstallAndAck | the exact request-journal ORDER (PUT body -> GET ckpt -> CAS ckpt -> INSTALL -> ack) for a co-batched commit, with both co-batched waiters blocked from seeing their result until installed AND acknowledged | reorder the body-PUT/ckpt-CAS/install/ack sequence, or let a co-batched waiter observe/return before install | DISCRIMINATING |
| CheckpointConflictAfterLogCommitRequiresRecoveryWithoutInstall / FenceMovementAtCheckpointPublicationRequiresRecoveryWithoutInstall (2) | a durable log commit whose checkpoint-frontier publication then persistently conflicts (or loses the fence) moves the lane to `NeedsRecovery`, installs/acks nothing, and blocks allocation of a later id above the unfrontiered transaction | let either failure install the unfrontiered commit, or allocate past it | DISCRIMINATING (×2) |
| MaterializeKeepsOverlaySmallAcrossManyIsolatedFlushes | the COW committed-map overlay stays bounded (<=1 entry) across 20 isolated single-ref flushes, not growing unboundedly | skip materializing the COW overlay after each isolated flush install | DISCRIMINATING |
| CompatibleMutationsShareOneCreate / InvalidBatchEntryGetsOwnExceptionBatchSurvives (2) | two compatible queued mutations share exactly ONE create; an invalid queued entry gets its own exception while its co-batched neighbor still lands in the same create | let co-batched mutations issue separate creates, or let one bad entry poison/block the batch | DISCRIMINATING (×2) |
| WedgedLaneBlocksSameTableWhileOtherTableProceeds / WedgedAppendObservedDurableAppliesBeforeNextId / WedgeResolutionJoinsTailCountersAndFoldsOverlay / WedgedRefLaneCountTracksExactlyTheWedgedTableThroughItsLifecycle (4) | a wedged lane blocks only its own table, resolves via the next flush's create (never a bare read), joins tail counters exactly once on resolution, folds its overlay, and the `wedgedRefLaneCount()` gauge tracks the exact wedged-table set through 0->1->0 | let an unrelated table wedge, resolve via bare read, double-count the tail, or let the gauge drift from the true wedged set | DISCRIMINATING (×4) |
| I1AppendCorruptionSurfacesAndFencesTheMountForRemount / I1WedgeResolveCorruptionSurfacesAndFaultsLane (2) | a proven foreign-object conflict at the append site or the wedge-resolve site surfaces `CORRUPTED_DATA` to the CALLER (not merely to the lane), fences the whole mount, schedules exactly one remount, and -- critically -- restores the queue's leader bookkeeping so no caller (same-table or independent-table) HANGS | let the leader bookkeeping stay stuck after the throw (regression: every future caller for that table blocks forever) | DISCRIMINATING (×2) |
| ForeignBytesAtWedgeKeyTripFenceAndRemount / NonReadyAtNewIdAllocationFaultsAndFailsClosed (2) | the full anomaly-policy reaction (fence trips closed, exactly one remount scheduled, a `ForeignInterference` `CasEvent` audited) fires on foreign-bytes-at-wedge-key and on an internal non-Ready-at-allocation contradiction, and the guard refuses BEFORE minting/PUTting any new `_log` object | skip the event audit, the fence trip, or the remount schedule; or allocate/PUT before the guard refuses | DISCRIMINATING (×2) |
| DiagnosticDispatchLoggingCannotReplaceFailClosedException | a failing best-effort diagnostic-dispatch log preparation does not replace/swallow the original fail-closed exception | let the diagnostic-logging failure mask or replace the real `CORRUPTED_DATA` | DISCRIMINATING |
| PostWriteFenceLossIsCounted | a write whose attempt landed but whose post-commit admission check failed is reported `GaveUp{Why::FenceLost}` (never `Committed`) and counted on its own `ProfileEvent` | report a post-write fence loss as committed, or fold it into the generic counter | DISCRIMINATING |
| CommittedSurfacesTheIncarnationFromTheWriteAndFromTheSettlingRead | `Committed.etag` is the write's own response for a direct commit, and the SETTLING READ's etag for a resolve-by-read commit -- both verified against an independent `head()` | report a stale or wrong incarnation from either commit path | DISCRIMINATING |
| WholeTableEvictionUnderBudgetReRecovers / ZeroBudgetDisablesEviction / WedgedTableIsNeverEvicted (3) | a tiny cache budget forces exactly one resident table (LRU), an evicted table re-recovers its committed state; a zero budget disables eviction entirely; a wedged table is NEVER evicted even when idle and over budget | let eviction under budget keep more than the LRU table resident, drop a wedged table's uncertain state, or let a zero budget still evict | DISCRIMINATING (×3) |
| ThresholdTriggerPublishesCacheReplayEquivalentBytes | a count-threshold-triggered background publish produces snapshot bytes byte-EQUAL to an INDEPENDENT oracle's replay of the raw logs through the published id, and fully prunes the retained tail | let the published snapshot diverge from an independent replay of the same logs, or leave tail residue | DISCRIMINATING |

Summary so far: 36/101 tests read, all DISCRIMINATING. Continuing in part 2.
## src/Disks/tests/gtest_cas_ref_writer.cpp (101 tests, part 2 of 3) {#src-disks-tests-gtest-cas-ref-writer-cpp-101-tests-part-2-of-3}

| test | property | killing mutation | verdict |
|---|---|---|---|
| CapturedPredecessorCannotPublishAfterSameNameRebirth / RetiredPredecessorCannotAdvanceCkptAfterSnapshotPut (2) | a publisher that captured a runtime BEFORE the same-name namespace was removed+reborn becomes inert after resuming: it never recreates the retired predecessor's `_snap`, and never advances/recreates the retired `_ckpt` (byte+etag identical) even after its own body PUT already landed | let a stale-runtime publisher recreate the predecessor's snapshot or advance its checkpoint after retirement | DISCRIMINATING (×2) |
| CapturedReaderCannotRetargetSameNameSuccessor / CapturedAppendCannotEnqueueIntoSameNameSuccessor (2) | a reader/appender that captured the PREDECESSOR runtime before a same-name remove+rebirth resumes against `NotFound`/retry-later, never silently retargeting into the successor's data | let a captured predecessor reader/appender read or mutate the successor's table | DISCRIMINATING (×2) |
| LatePredecessorInvalidationLeavesSuccessorAttached | a late invalidation notification for an already-retired predecessor life does not erase or poison the already-attached successor's runtime/cache slot | let a late predecessor invalidation evict or corrupt the live successor | DISCRIMINATING |
| PublishIncrementsSnapshotCounters | a threshold publish increments both `CASRefSnapshotPutBytes` and `CASRefSnapshotTailLogs` observability counters | let either counter site fail to fire on a real publish | DISCRIMINATING |
| MountTimeRecoveredLargeTailPublishesAfterOrdinarySuccessor | a mount-time-recovered pre-existing over-threshold tail (left by a predecessor whose own thresholds never fired) is retained as trigger debt through a terminal-seal recovery, and one ordinary successor (alone under threshold) makes it publishable, proving the debt was carried, not forgotten | forget the inherited tail on recovery, or fail to make the debt+successor publishable | DISCRIMINATING |
| YoungTxnIsCoveredImmediately | a just-committed transaction is immediately publish-eligible with zero grace-window aging (the retired old code required aging) | require any aging/waiting before a fresh commit is coverable | DISCRIMINATING |
| TriggerFiresOnCountAboveThresholdWithoutAging | the count trigger fires purely off tail counters even under a FROZEN boot clock (never advances) | make the count trigger implicitly depend on the clock advancing | DISCRIMINATING |
| AdoptionSubtractsCapturedCountersUnderConcurrentAppends | adoption subtracts EXACTLY the counters captured at copy time (not live-at-adoption-time), verified precisely: not zero, not underflowed, exactly "appended after the copy" | subtract the live (not captured) counter value, dropping newly-committed txns from the next trigger, or underflowing | DISCRIMINATING |
| PublicationNeverBlocksConcurrentAppend | a stuck-mid-PUT background publish does not block an unrelated ordinary mutation on the SAME table (proven live via a bounded, non-hanging wait) | let a stuck publish hold a lock that blocks concurrent appends | DISCRIMINATING |
| PublishThreadOutlivesDroppedPoolHandleWithoutCrashing | a dispatched background publish thread keeps the `Pool` alive via its OWN `shared_from_this()` copy even after every external handle is dropped -- proven by a `weak_ptr` liveness check and a crash-free completion | let the dispatch capture the Pool by raw pointer/weak reference, causing a use-after-free when the last external handle drops | DISCRIMINATING |
| ConcurrentOutOfOrderPublishDoesNotRegressBaseNorDropCommittedTxns | a late-finishing OLDER-candidate publish does not regress `newest_snapshot_id` below an already-adopted NEWER one, and its skipped adoption does not double-subtract the newer publish's already-reset counters; independently verified against a full-replay oracle that no committed txn was lost | let the older publish's late completion regress the base or corrupt/double-subtract the tail counters | DISCRIMINATING |
| ClampedCounterSubClampsInsteadOfUnderflowingOnOutOfOrderAdoption | `clampedCounterSub` clamps to zero (never wraps to ~UINT64_MAX) when a larger candidate's captured count double-counts a region an earlier smaller-candidate adoption already subtracted -- deterministically forced via independently-parked PUTs released in the exact hazard order | let the subtraction wrap instead of clamp, permanently re-latching the publish-storm trigger | DISCRIMINATING |
| C4LatchBoundedUnderSustainedNonCommittedPublish / C4BackoffDefersThenRetriesAndPublishes / C4InFlightGateAdmitsAtMostOne (3) | reads within an armed publish backoff never re-dispatch; a read past the deadline dispatches exactly one retry that succeeds; and the single-in-flight gate admits at most one background publish per table even under sustained reads while one is parked mid-PUT | let any of the 3 storm-prevention mechanisms (backoff latch, backoff deadline, in-flight gate) fail to bound dispatch | DISCRIMINATING (×3) |
| RecoveredSealAboveThresholdDoesNotRedispatchUntilOrdinarySuccessor | a recovered terminal epoch seal above threshold is rejected at ADMISSION (not execution) as unpublishable, never re-dispatching on repeated reads, until one ordinary successor above the seal reopens the scheduler exactly once | reject the seal at execution instead of admission (causing immediate re-dispatch storms), or fail to reopen after an ordinary successor | DISCRIMINATING |
| TriggerIgnoresEntriesCoveredByNewestSnapshot | after adoption, the trigger arithmetic counts ONLY entries strictly above `newest_snapshot_id` (4 covered + 2 fresh reads as 2, not 6), verified across two full publish/reset cycles | let the trigger keep counting already-covered history, causing perpetual re-dispatch | DISCRIMINATING |

Summary so far: 55/101 tests read, all DISCRIMINATING. Continuing in part 3.
## src/Disks/tests/gtest_cas_ref_writer.cpp (101 tests, part 3 of 3) {#src-disks-tests-gtest-cas-ref-writer-cpp-101-tests-part-3-of-3}

| test | property | killing mutation | verdict |
|---|---|---|---|
| BoundedBatchesAndInterruptionResumeAcrossMounts | the stale-precommit sweep chunks removal to the op cap per transaction, and an interrupted (wedged) sweep leaves the remainder for a LATER mount's recovery to finish, converging on exactly 3 new log objects (not 1 per stale precommit) | fail to chunk the removal, or lose/duplicate stale precommits across the interrupted-then-resumed sweep | DISCRIMINATING |
| FailedSweepRearmsAndRetriesUntilClean | a failed sweep attempt re-arms `needs_stale_precommit_sweep` (does NOT consume the once-per-mount shot), backs off within its window, then a later trigger retries and clears the flag permanently once verified clean, with exactly one audit event per reclaimed binding | let a failed sweep consume the shot (never retrying), or let the retry storm within the backoff window, or emit a wrong number of reclaim events | DISCRIMINATING |
| VerifiedCleanSweepClearsFlagWithoutEvents | a sweep that finds nothing stale clears the flag on its first pass with ZERO reclaim events (distinguishing "no abandons" from "reclaim broken" in the audit) | emit a spurious reclaim event, or fail to clear the flag on a clean pass | DISCRIMINATING |
| FailedRemountPublishesNoRuntimeUnderFenceLossGeneration | a FAILED remount publishes no runtime under the intermediate fence-loss generation, for both a warm and a never-seen namespace; the predecessor stays cached at its OLD generation, untouched | let a failed remount publish/materialize a runtime under the rejected intermediate generation | DISCRIMINATING |
| ReRecoversStaleCacheToTwinDrop / PostRemountAppendCarriesLiveEpochSortingAboveTwinLogs (2) | a self-remount drops the stale warm cache and re-recovers under the new epoch (adopting a same-uuid twin's durable drop), and a post-remount append carries the FRESH epoch, sorting strictly above the twin's log | keep the stale cache across a remount, or stamp a post-remount append with the old epoch | DISCRIMINATING (×2) |
| DiscardsWedgeAndLaneRemainsUsable | a self-remount discards an in-memory wedge along with the detached runtime, and the lane is immediately usable (not hung) afterward | keep a wedged runtime cached across a remount, hanging or corrupting the lane | DISCRIMINATING |
| SupersededLeaderMidFlushFailsClosedCreatesNoObject | a flush leader parked mid-flight across an entire self-remount window fails closed on resume (pre-allocate re-check), creating NO backend object -- proven both by the exact error message and by the unchanged greatest durable log id | let a superseded leader allocate an id and PUT a `_log` object after the remount it never observed | DISCRIMINATING |
| CachedPositiveWriterCannotAppendAfterRemovingIsPublished | a cached writer paused after its ordinary admission gates re-checks the exact catalog life immediately before id allocation, refusing a late append once a concurrent `Live -> Removing` transition becomes visible | let a cached writer append after `Removing` is published, using a stale positive-ownership cache | DISCRIMINATING |
| TxnNamesEveryOwnerThenRemoveNamespace | `dropNamespace`'s ONE removal transaction names an exact removal op for EVERY committed ref AND every dangling precommit, with `remove_namespace` as the strictly final op | omit an owner from the removal transaction, or place `remove_namespace` anywhere but last | DISCRIMINATING |
| RemovalPublishesTerminalLogWithoutTerminalSnapshot | removal never publishes a terminal snapshot (generation 7 design); the terminal log is the sole durable removal record, remaining ordinary tail work until GC folds it | publish a terminal snapshot on removal, contradicting the design | DISCRIMINATING |
| MalformedShapeWithRemoveNamespaceNotFinalRejectedBeforeAnyCreate | a malformed multi-op item (two `remove_namespace` ops, not final) is rejected by the WHOLE-TRANSACTION shape check BEFORE any backend object is created, even when disguised as an ordinary mutation kind | let a per-item (single-op) preview validate a malformed multi-op shape, allowing a table-bricking durable object to be created | DISCRIMINATING |
| GenericAppendCannotWriteTerminalWhileCatalogIsLive / GenericTerminalOnAbsentNamePerformsZeroDurableMutation (2) | the generic append surface cannot forge a second namespace-removal capability (even disguised as an ordinary mutation) while the catalog is Live, and doing so on an ABSENT name performs ZERO durable mutation of any kind (no write, no catalog row minted) | let the generic surface write a terminal transaction, or mint a catalog row for an absent name before the capability check | DISCRIMINATING (×2) |
| CatalogedNamespaceFilesOnlyLifeCompletesRemoval | a files-only life (catalog+checkpoint, no ref stream) still gets a real birth+terminal removal record and its catalog row is retired by GC | let a files-only life's removal skip the terminal record or never retire its catalog row | DISCRIMINATING |
| PredurableCatalogReadFailureReopensExactLiveLane | when the post-close catalog observation fails, a later fresh exact-Live observation still reopens the lane for ordinary mutation | let a pre-durable read failure permanently wedge the lane instead of allowing reopening on fresh observation | DISCRIMINATING |
| DropNamespaceCancelsInFlightBuildAndNextOpThrows | an in-flight build for a just-removed namespace is cancelled: EVERY subsequent operation (including one with no lifecycle gate of its own) throws, never promoting into the dead namespace | let an in-flight build's `stageManifest` or `promote` succeed after removal | DISCRIMINATING |
| RemovalAppendFailureLeavesRemovingAndRetryCompletes | an unresolved terminal append leaves the catalog at `Removing` (not rolled back), refuses positive ownership, does NOT cancel non-append build operations, and a retry of the same removal completes | let a wedged terminal append roll back the catalog transition, or cancel unrelated build operations prematurely | DISCRIMINATING |
| PresenceProbeStaysTrueThroughRemovingUntilTerminalRetrySucceeds | `namespaceStillLogicallyPresent` stays TRUE for the whole window between the durable `Live->Removing` catalog transition and the terminal append actually landing (the crash-shaped case) | report absent before the terminal append is durably proven | DISCRIMINATING |
| PresenceProbeCreatingIsPresentAndRemovalWaitsForCreatorFenceTerminality | a `Creating` row is present, and removal refuses to cancel it while the creator fence cannot be proven dead, succeeding only once a terminal certificate exists | cancel a `Creating` row whose creator fence is unproven dead, or fail to cancel once it IS proven dead | DISCRIMINATING |
| PresenceProbeNoRowObservationRevalidatesRatherThanRacingToAbsent / PresenceProbeIgnoresUnrelatedCatalogChurnBetweenReads (2) | a two-read "no row" observation revalidates rather than answering a stale `false` when the target namespace is born between reads, but does NOT retry/misfire on UNRELATED catalog churn between the same two reads | answer stale-absent for a namespace born mid-probe, or force a retry storm on unrelated churn | DISCRIMINATING (×2) |
| PresenceProbeCatalogReadFailurePropagatesRatherThanAnsweringAbsent / PresenceProbeFenceLossPropagatesRatherThanAnsweringAbsent (2) | an unreadable catalog GET or a lost mount fence mid-probe THROWS, never silently answers `false` | let a probe failure be swallowed into a false "absent" answer | DISCRIMINATING (×2) |
| PresenceProbeFacadeConsistencyAcrossRemovalLifecycle | the 3 facade states (Live-present-readable, incomplete-Removing-present-but-content-unreadable, terminal-Removing-absent) are pinned against each other on ONE namespace's actual lifecycle transitions | conflate any of the 3 facade states, e.g. reporting unreadable content as absent | DISCRIMINATING |
| PresenceProbeRevalidatesAfterTerminalProvenRatherThanRacingToStaleAbsent | proving one incarnation's terminal is not proof the CURRENT logical namespace is absent: if GC deletes the row and a successor is born under the same name WHILE the probe's own I/O is still in flight, the probe must revalidate and answer present | let the probe answer stale `false` after proving only the predecessor incarnation terminal, missing an interleaved rebirth | DISCRIMINATING |
| FilesOnlyNamespaceTruncateThrowsRetryLaterUntilGcReclaimsThenRebirths | the first write after a TRUNCATE-shaped removeRecursive throws typed retry-later (never silently wedges/corrupts) while the row is still `Removing`, then self-heals into a fresh incarnation once GC reclaims it | let the write silently succeed into the still-occupied predecessor row, or permanently wedge instead of retry-later | DISCRIMINATING |
| DropNamespaceDoesNotCancelBuildsInOtherNamespaces | cancellation is strictly namespace-scoped: dropping namespace N does not cancel an in-flight build in namespace M | let cancellation leak across namespaces | DISCRIMINATING |
| CreateAgainstRemovingRetriesWithoutMutation | both the resident-runtime and fresh-runtime paths return typed retry-later with ZERO durable writes when creating against a `Removing` row | let either path perform a durable mutation while the row is `Removing` | DISCRIMINATING |
| SameNameSameWriterEpochRebirthInvalidatesResidentLifeAndStartsAtZeroCoverage | a same-name, same-writer-epoch rebirth does NOT inherit the predecessor's physical life, folded cursor, or resident runtime identity even when the writer mount stays resident throughout -- verified against real GC fold-seal coverage rows and zero leaked `/_cleanup/` touches | let a same-epoch rebirth silently inherit the predecessor's coverage cursor or runtime identity | DISCRIMINATING |
| CommitThenThrowEraseResolvesAndRebindsResidentRuntimeImmediately | an erase whose commit landed but whose response was lost still resolves the predecessor as gone (via a follow-up read) and immediately allows a fresh same-name birth with a distinct runtime, verified against real GC fold-seal rows | let a lost-response erase leave the resident runtime stuck behind a stale removal-admission gate | DISCRIMINATING |
| OtherWinnerReplacementInvalidatesExactPredecessorLife | when another actor wins the erase race, `EntryChanged` still proves the predecessor life dead and invalidates its resident runtime, adopting the actual winning row | let a lost erase race leave the resident runtime pointed at a dead predecessor life | DISCRIMINATING |
| LaterNameLookupReconcilesAfterEraseResolutionReadFailure | a failed resolution read after a lost erase response is correctly treated as NOT success, yet a later fresh name lookup still observes the old row absent and reconciles the resident runtime | treat a resolution read failure as proof of success, or fail to reconcile on the next fresh lookup | DISCRIMINATING |
| PostListCatalogCutReconcilesMissedEraseInvalidation | the normal post-LIST catalog cut is itself a reconciliation point that repairs a missed local invalidation before any later writer touches the name | let a missed local invalidation survive past the next ordinary catalog LIST | DISCRIMINATING |
| ExistingLiveCatalogRowPinsExactLifeWithoutMutation | recovering an already-`Live` catalog life performs ZERO catalog/stream mutation and installs the EXACT observed incarnation | let recovery of an existing Live row perform a spurious write, or install a different incarnation | DISCRIMINATING |
| NeverBornReadAllocatesNoRuntime | a read of a never-born namespace decides catalog absence BEFORE constructing any runtime (zero cached tables, zero runtime identity) | let a read allocate a runtime/cache slot before checking catalog absence | DISCRIMINATING |
| BirthFromNeverBornUsesOrdinaryPath | a never-born namespace follows the ordinary catalog-first birth path end to end | break the ordinary birth path for a genuinely fresh namespace | DISCRIMINATING |
| SameRefMutationsSplitAcrossFlushes | the `seen_refs` one-op-per-ref-name-per-batch guard forces two co-pending mutations of the SAME ref to flush as two SEPARATE `_log` objects, never co-batched, with neither mutation lost | let two same-ref mutations co-batch into one transaction (which per-op undo validates against the wrong pre-batch state) | DISCRIMINATING |
| TransientSealFailureIsRetriedThenSucceeds / RecoveryDoesNotEnumerateItsStream (2) | a transient (NETWORK_ERROR-classified) failure of the in-band epoch-seal write is retried as a WHOLE recovery attempt (not lost inside one call), adopting already-closed epochs rather than re-minting them; and recovery performs this whole retry with ZERO stream LIST calls | let recovery fail permanently on a transient fault, re-mint an already-sealed epoch on retry, or silently reintroduce a stream LIST | DISCRIMINATING (×2) |
| TransientFailureLongerThanBudgetPropagates | a transient failure that never clears within `recovery_retry_budget_ms` propagates as `NETWORK_ERROR` rather than retrying forever | let the retry loop spin unboundedly past its own budget | DISCRIMINATING |
| NonNetworkErrorIsNotRetried | a real cross-process seal conflict (`CORRUPTED_DATA`, not transient) is NOT retried -- zero backoff sleeps, fails fast | retry a non-transient corruption as if it were a transient network fault | DISCRIMINATING |
| VanishBrakeStaysTerminalNotRetried | a missing checkpoint-named (immutable-authority) snapshot is TERMINAL corruption, never re-entering the outer transient-retry loop and never sleeping | reinterpret a missing immutable-authority object as a transient vanish and retry it | DISCRIMINATING |
| ThrowingBackoffSleepDoesNotWedgeRecovery | a THROWING backoff-sleep callback still re-acquires `state_mutex` before unwinding so `recovery_in_progress` is correctly cleared under lock, proven by a second touch recovering cleanly rather than hanging | let the sleep-throw path skip clearing `recovery_in_progress`, permanently wedging the lane on every future touch | DISCRIMINATING |
| HasAnyRefWithPrefixMatchesListRefsEmptiness | `hasAnyRefWithPrefix`'s early-exit existence probe agrees exactly with a full `listRefs` on emptiness/non-emptiness across empty, prefix-matching, prefix-non-matching, and tombstoned-namespace cases | let the early-exit probe diverge from the full listing's emptiness verdict on any of the 4 cases | DISCRIMINATING |

Summary for gtest_cas_ref_writer.cpp: 101 tests read in full. All DISCRIMINATING except PostWriteFenceLossIsCounted, CommittedSurfacesTheIncarnationFromTheWriteAndFromTheSettlingRead, and PostWriteFenceLossIsCounted-style engine-unit tests (still DISCRIMINATING, listed above) -- net result: 101/101 DISCRIMINATING, 0 WEAK, 0 TAUTOLOGICAL, 0 UNSURE. This is the largest and most rigorous file in the suite: exact request-journal ordering, deterministic fault injection at exact I/O seams, independent full-replay oracles, real concurrency races via condition variables (never sleeps), and ProfileEvents/CasEvent audit verification throughout.

Correction (omitted from part 3 above): the stale-precommit sweep section also has this row.

| SweepsOnlyStaleEpochPrecommitsKeepsCurrentEpoch | a predecessor's dangling (never-promoted) precommits are swept on the successor's first touch, while a precommit the SUCCESSOR itself adds under its OWN epoch survives -- verified against an independent full-replay oracle | let the sweep remove the successor's own fresh precommit, or fail to remove the predecessor's dangling ones | DISCRIMINATING |
## src/Disks/tests/gtest_cas_repoint.cpp (2 tests) {#src-disks-tests-gtest-cas-repoint-cpp-2-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ByteEqualIsNoOp | republishing byte-identical entries onto a committed ref is a ZERO-mutation no-op (0 puts, no ref-log record, no `RefRepoint` event, unchanged manifest), verified against a real writer protocol commit | let a byte-equal repoint stage a fresh manifest or write anything | DISCRIMINATING |
| AddFileRepoints | a genuinely different entry set republishes a FRESH manifest (loud: `RefRepoint` event, cache erased so a later read serves the new content, not stale) | let a real content change be treated as a no-op, or leave a stale cached view after repoint | DISCRIMINATING |

Summary for gtest_cas_repoint.cpp: 2 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_requests.cpp (64 tests) -- the engine's own core test file {#src-disks-tests-gtest-cas-requests-cpp-64-tests-the-engine-s-own-core-test-file}

| test | property | killing mutation | verdict |
|---|---|---|---|
| GrammarRefusesTheNineWays | `isIncarnationValue` accepts/rejects the exact canonical shape per dialect (ETag quoting, Generation no-leading-zero/no-trailing-space) | loosen the grammar to accept any of the 9 malformed shapes | DISCRIMINATING |
| BackoffIsFullJitterUnderTheCap | `Retry::backoff` is genuinely full-jitter (mean in [0.35,0.65] of ceiling, spread reaching both quartiles, capped) across 12 attempt levels, 1000 samples each | make backoff a constant or half-range instead of full jitter | DISCRIMINATING |
| PoliciesAreShapedAsSpecified / BindSaturatesAndLeavesAnEqualLeaseOffTheLeaseSource (2) | `Retry` policy deadlines (standard/once/untilLeaseSafe/within) compute exact values, saturate on overflow, and the lease-vs-policy tie belongs to the policy (strict-less for the lease) | off-by-one any deadline arithmetic, or flip the tie-break direction | DISCRIMINATING (×2) |
| OrThrowMapsEveryAlternative | `orThrow` maps every `WriteResult` variant to the exact expected outcome/exception code, using designated-init `GaveUp` construction (position-independent) | mis-map any variant (e.g. throw on Declined, or wrong error code for a GaveUp reason) | DISCRIMINATING |
| OpenFenceAdmitsEverythingAndNeverMoves | `Fence::open()` always admits and never moves generation | make the open fence refuse or move | DISCRIMINATING |
| InMemoryWriteReadRemoveRoundTripThroughOneOperation | the backend's keyed primitives (create/read/head/replace/remove) round-trip correctly, incarnations never repeat, removal is mismatch/removed/gone in the right order | break any primitive's round-trip or incarnation-uniqueness guarantee | DISCRIMINATING |
| ListSurfacesTheIncarnationAndPaginates | `list` returns sorted, prefix-scoped, correctly-paginated results carrying each key's incarnation | drop pagination, sort order, or the surfaced incarnation | DISCRIMINATING |
| EveryBackendInstanceHasItsOwnId / RefreshCredentialsIsOffUntilAskedFor (2) | each backend instance has a distinct nonzero id and the right dialect; credential refresh is off by default and reports what was set | let two instances share an id, or misreport the dialect/refresh state | DISCRIMINATING (×2) |
| EachWriteKnobIsKeyedAndOneShotOnThePrimitiveWrite | fault-injection knobs are keyed (not call-site-specific) and one-shot on the single primitive `write`, composing correctly across ambiguous+refuse on one key | let a knob fire more than once, or miss a write path that bypasses the primitive | DISCRIMINATING |
| ReadRefusesAValueThatIsNotAnIncarnation | `CasRequests::mint` refuses a malformed incarnation value BEFORE any caller sees it as an `Etag`, naming the key | let a malformed incarnation value silently become an `Etag` | DISCRIMINATING |
| FirstPerKeyRefusesOnceAndTheCallStillSucceeds / RefusalsAreRetryableUnderBothStatuses / EveryNthRefusesOnThePeriodAcrossKeys (3) | `ThrottlingBackend`'s 3 modes refuse exactly the modeled requests (first-per-key once, every-Nth across keys) and the refusal is genuinely retryable (reaches the engine as ambiguous, causing a real reissue) not silently absorbed | let a throttle mode refuse the wrong request, or surface as a definite failure instead of a retryable one | DISCRIMINATING (×3) |
| RenderAndPersistedCompare | `Etag::render`/`PersistedEtag::capture`/`matches` produce the exact wire form and a captured record never re-matches a later incarnation | let a stale captured incarnation match a newer one, or corrupt the rendered form | DISCRIMINATING |
| CreateThenReplaceThenRemove | the full create/read/replace/remove lifecycle through the real engine (not the raw backend) behaves identically, including stale-incarnation mismatch on remove | any lifecycle-step regression through the engine layer | DISCRIMINATING |
| KeyBindingThrowsBeforeAnyRequest (+ death-test twin) | presenting an incarnation observed for KEY A as the precondition for KEY B throws `LOGICAL_ERROR` (or aborts in debug/sanitizer) BEFORE any request is sent | let a cross-key incarnation binding slip through and reach the store | DISCRIMINATING (×2, release/debug twins) |
| EveryConflictIsSettledByOneReadAndCarriesTheOccupant | a refused precondition is settled by EXACTLY one resolve read, and the conflict carries the real occupant's bytes | settle a conflict without a read, or misreport the occupant | DISCRIMINATING |
| AmbiguousCreateThatLandedIsCommittedByTheResolveRead / AmbiguousCreateThatNeverLandedIsReissued (2) | an ambiguous write that DID land is settled by exactly one resolve read (never a second write); one that did NOT land is settled by one read then reissued (never claimed as already-committed) | conflate the landed/not-landed cases, either double-writing or falsely claiming a commit | DISCRIMINATING (×2) |
| OnceSendsOneWriteAndAtMostOneResolveRead | under `Retry::once`, an unresolved write still owes exactly its one resolve read (never a reissue) | skip the owed resolve read under `once`, or reissue despite the single-attempt policy | DISCRIMINATING |
| DecideMayThrowAndTheExceptionPropagatesUnchanged | a `decide` callback's exception propagates through `readModifyWrite` unchanged, with zero writes and exactly one read | swallow or transform the decide callback's exception | DISCRIMINATING |
| OnPresenceIssuesHeadsAndNoGet / OnPresenceSettlesARefusedPreconditionWithAHead (2) | the presence-only RMW loop uses HEAD exclusively (never GET) to decide and settle, even a refused precondition | let presence-only RMW fall back to a body GET | DISCRIMINATING (×2) |
| ForEachListedKeyStopsEarlyAndBudgetsPerPage / ForEachListedKeyGivesEachPageItsOwnPolicyWindow / ForEachListedKeyThrowsRatherThanTruncateWhenAPageNeverArrives (3) | the paginated walk stops exactly where the caller stops it (no over-fetch), gives EACH page its own retry policy window (not one window for the whole walk), and THROWS rather than silently truncating when a page never arrives | over-fetch after early stop, share one deadline across the whole walk, or silently truncate on a stuck page | DISCRIMINATING (×3) |
| DeleteMarkerIsANamedException | a versioned-bucket delete-marker read is a named `CAS_DELETE_MARKER` exception, deterministically, every time | let a delete marker be silently treated as absent or as ordinary data | DISCRIMINATING |
| RemoveCurrentReObservesAMismatchAndRefusesUnderOnce | `removeCurrent` re-observes on a mismatch (deleting what the SECOND look saw) under a reissuing policy, and correctly refuses (never silently no-ops) under `once` | let a mismatch reissue delete the wrong (stale) incarnation, or silently succeed under `once` | DISCRIMINATING |
| ProbeSentinelRetriesOnlyTheIndeterminateOutcome | `probeSentinel` reissues ONLY on an `Indeterminate` outcome (never a definite one), and under `once` reports the inconclusive result rather than throwing | reissue on a definite outcome, or throw instead of reporting `Indeterminate` under `once` | DISCRIMINATING |
| AdmissionIsCheckedAtThreePoints | the fence admission gate is checked at exactly the 3 documented points (before first attempt, before each subsequent verb, after a proven post-write commit), each reporting `FenceLost` distinctly from a store-level answer, with the durable object left UNDONE (never rolled back) on the post-commit case | drop the check at any of the 3 points, or let a post-commit fence loss undo the durable write | DISCRIMINATING |
| TheGateBeforeTheSleepEndsTheCallWithoutASecondWrite | a liveness predicate that flips false during the resolve read refuses the PAUSE before the reissue (never sleeps, never sends a second attempt) | let the gate check happen only before the first attempt, missing the pre-sleep recheck | DISCRIMINATING |
| AResolveReadRefusedForLeaseBudgetIsReportedAsTheLeaseDeadline / AFenceWithNoBudgetForTheRequestSendsNothingAndNamesTheLease / AWriteReservesTwoEnvelopesSoOneOfSurplusStartsNothing (3) | the lease-budget/attempt-reservation arithmetic correctly reports `Deadline{Lease}` (never a fabricated conflict) when the resolve read has no budget, refuses to start ANY request when the fence has insufficient budget, and reserves TWO envelopes (attempt + resolve read) per write so exactly one envelope of surplus still starts nothing | report a budget exhaustion as a conflict instead of a deadline, start a request the lease cannot cover, or reserve only one envelope per write | DISCRIMINATING (×3) |
| AnRmwWhoseFirstReadFailsGivesUpUnresolvedWithoutWriting / AnOnPresenceRmwWhoseFirstHeadFailsGivesUpUnresolvedWithoutWriting (2) | a failed first read/head (not a bound refusal) is reported `Unresolved` (never `Deadline`), with zero writes | mislabel a read/head failure as a deadline, or write despite the failed first observation | DISCRIMINATING (×2) |
| AConflictWhoseResolveReadFailsIsReportedWithNothingObserved | a conflict whose settling read fails reports `NotObserved` rather than a guessed occupant | fabricate an occupant when the resolve read actually failed | DISCRIMINATING |
| AFenceLostDuringTheResolveReadIsAFenceLossNotAConflict | a fence trip discovered exactly at the resolve-read gate is reported `FenceLost`, never conflated with an ordinary conflict | report a lost fence as if another writer held the key | DISCRIMINATING |
| OnPresenceFetchesTheBodyToProveAnAmbiguousAttemptLanded / OnPresenceReportsMetaEvenWhenItHadToFetchTheBody (2) | the presence loop fetches a body ONLY to resolve an ambiguity (never as a promise to callers), and still reports only `Meta` (never leaking `Object`) in its conflict result even after that fetch | let the presence loop leak the fetched body into its public conflict result | DISCRIMINATING (×2) |
| AmbiguousReplaceWhoseResolveShowsThePreconditionUnchangedIsReissued / AmbiguousReplaceOfIdenticalBytesIsReissuedNotClaimedByByteEquality / AmbiguousReplaceWhoseResolveShowsAnotherIncarnationIsAConflict (3) | an ambiguous replace is resolved by INCARNATION identity, never byte equality: unchanged precondition -> reissue; identical bytes but unchanged incarnation -> still reissue (not falsely claimed); moved incarnation -> conflict with the real occupant | resolve ambiguity by comparing bytes instead of the incarnation, falsely claiming a competitor's identical write as this call's own commit | DISCRIMINATING (×3) |
| ReadModifyWriteDoesNotClaimACompetitorsIdenticalBytesAfterAnEarlierAmbiguity | the RMW loop re-decides against the CURRENT occupant after an unrelated earlier ambiguity resolves as dead, never claiming a competitor's later identical-looking write | let an old ambiguity's resolution leak into claiming a later competitor's write | DISCRIMINATING |
| AnUnmodeledLocalExceptionOnAWritePropagatesUnchanged / ADeterministicLocalFailureSurfacesUnchangedWithoutAReissue / ATransportTimeoutIsReissuedAndALocalFailureIsNot (3) | a non-`Poco` (local, non-transport) exception propagates unchanged with zero reissue; a deterministic-local classified failure surfaces without reissue; a genuine transport timeout DOES reissue | reissue a local bug (replaying it into a misleading retryable error), or fail to reissue a genuine transport timeout | DISCRIMINATING (×3) |
| LivenessPredicateEndsTheOperationLikeAFenceLoss | a flipped liveness predicate ends both write and read paths exactly like a fence loss (write: `GaveUp{FenceLost}`; read: throws `NETWORK_ERROR`), zero requests sent | let a dead liveness predicate still send requests | DISCRIMINATING |
| ReadModifyWriteLosesNoIncrementUnderContentionAndBoundsAHotKey | a REAL multi-threaded RMW counter under genuine contention (real clock, real threads) loses NO increment (100 = 50+50 exactly), and a permanently-contended key is bounded by the deadline (paced, not spinning) | lose an increment under real contention, or spin forever on a permanently-contended key instead of bounding by the deadline | DISCRIMINATING |
| DeadlineIsTheOnlyBoundUnderZeroLatencyThrottling / LeaseBoundPolicyIssuesNothingPastTheBoundary (2) | under sustained throttling the ONLY thing that ends the call is the policy deadline (proven via a large sleep count and elapsed-time bound, not an attempt-count constant); a lease-bound policy starts nothing past the point where it could not finish inside the bound | end a sustained-throttle call on an attempt-count heuristic instead of the real deadline, or start a doomed-to-not-finish attempt under a lease bound | DISCRIMINATING (×2) |
| AMalformedRequestIsRefusedWithoutAReissue / AnAccessDenialNoRefreshCanFixIsRefusedOnTheFirstAttempt / ASecondCredentialAnswerAfterTheOneRefreshIsRefused / UnderOnceACredentialAnswerIsRefusedWithoutARefresh / ACredentialAnswerAfterAnAmbiguousAttemptStillOwesTheResolveRead / AnExpiredTokenARefreshFixesIsResentWithoutAResolveRead / AnExpiredTokenNoRefreshCanFixIsRefusedRatherThanRiddenToTheDeadline / ANameOnlyAccessDenialOnAReadPropagatesWhenNoRefreshIsAvailable (8) | the full S3 credential-refresh-once contract: malformed requests never reissue; a denial no refresh can fix is terminal on attempt 1; the ONE refresh per call is exhausted correctly (a second denial after using it is terminal); `once` asks for no refresh at all; a credential answer never skips a still-owed resolve read for an EARLIER unrelated ambiguous attempt; a refresh-fixable expired token resends WITHOUT a resolve read (since its own attempt is proven non-applied); a name-only (UNKNOWN-coded) denial is matched by NAME and treated identically to a typed one | refresh more than once per call, skip the owed resolve read for an unrelated ambiguity, or fail to match a name-only vendor error to its real semantic class | DISCRIMINATING (×8) |
| ReadModifyWriteWhoseResolveAndFreshObservationBothFailGivesUpUnresolved / AnUnmodeledStoreErrorOnAReadIsReissuedNotSurfaced (2) | two resolve reads that both fail with a store-modeled-but-non-transport error report `Unresolved` (not a fabricated deadline or conflict); an UNKNOWN-vendor-coded read error IS reissued (never surfaced), since UNKNOWN can never mean "will not start succeeding" | mislabel a doubly-failed resolve as a deadline/conflict, or surface an UNKNOWN vendor error instead of reissuing it | DISCRIMINATING (×2) |

Summary for gtest_cas_requests.cpp: 64 tests, all DISCRIMINATING. This is the single most rigorously exact-count-and-classification-verified file in the assignment, matching its role as the engine's own primary test file.
## src/Disks/tests/gtest_cas_retirement_sweep.cpp (4 tests) {#src-disks-tests-gtest-cas-retirement-sweep-cpp-4-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AHiddenRemovalStillReclaimsItsBlob | a removal record hidden from exactly ONE round's enumeration still gets folded and its blob reclaimed (arithmetic intake, not listing-dependent) -- verified via a real fault-injected hole with an explicit "hole actually fired" assertion so a mistyped key/count cannot pass vacuously | let a listing-hole permanently hide a removal from folding, leaking the blob | DISCRIMINATING |
| TheRoundEnumeratesTheRefPrefixExactlyOnce | a folding round enumerates the ref stream prefix EXACTLY once, on EVERY one of 32 rounds (covering the deleted detector's 16-round cadence twice over), and never conflates it with the bounded janitor page | reintroduce a second ref-prefix enumeration, even one that only appears on a periodic cadence | DISCRIMINATING |
| AStragglerFromTheDyingEpochLosesItsCreateToTheRecoverySeal | the mechanism that replaced the deleted materialization-grace wait: a fenced-out mount remounts with ZERO wait, recovery seals the dead epoch in-band at exactly the slot a straggler would take, and the straggler's own later conditional create is CONCLUSIVELY REFUSED by the store -- the assertion is the real conflict, not merely "nothing bad happened" | let the straggler's create succeed after the recovery seal, or require any wait before remount | DISCRIMINATING |
| AConfigStillAskingForTheMaterializationGraceIsRejected | a config still specifying the retired `cas_materialization_grace_ms` setting is refused at disk-open time, loudly, via the generic unknown-key path | silently accept and ignore the retired setting | DISCRIMINATING |

Summary for gtest_cas_retirement_sweep.cpp: 4 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_s3_staging.cpp (20 tests) {#src-disks-tests-gtest-cas-s3-staging-cpp-20-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| StagedCopyCondemnedRetryRetagsBeforeQueuedDelete / StagedCopyDeletedBeforeAbsentRetryRetagsBeforeQueuedDelete / FirstCondemnedAttemptThenAbsentRetryNeverRecopies (3) | 3 ETag-faithful adversarial fault scripts around republishing over a condemned/vanished blob: the replacement is FRESH-TAGGED (never byte-identical to what a verbatim copy would reproduce), a stale queued exact-token delete captured mid-fault therefore MISSES the live replacement (proven via a real replay of the captured `Etag`), and a first-condemned-then-absent retry never recopies | let a retry verbatim-copy reproduce the condemned incarnation's exact bytes (making the queued delete a data-loss hazard), or recopy after a first condemned attempt | DISCRIMINATING (×3) |
| ParsesS3BackendFromConfig / DefaultConfigParsesToLocalBackend / UnknownBackendValueThrows / DefaultConstructedStorageReportsLocal / DefaultObjectStorageRejectsNativeOnlyCopyMode (5) | `staging_backend` config parsing accepts `s3`, defaults to `local` when absent, rejects an unknown value, and matches the default-constructed storage's own accessor; `NativeOnly` copy mode is rejected unless explicitly advertised | let the parser accept/default to the wrong backend, or let a local storage silently claim `NativeOnly` support | DISCRIMINATING (×5) |
| ContentWriteBufferS3ModeStreamsToSinkAndFinalizes | the S3-staging content buffer writes the envelope header FIRST (unhashed, excluded from the reported size), then exactly the payload, computes the SAME streaming (chunked, block=2048B) hash the pool-wide convention uses (verified against an independent `HashingReadBuffer` oracle, not a one-shot hash), and calls the finalize callback exactly once with the right hash/size/key | hash the header, use a different hashing convention (one-shot vs streaming), or double-fire/miss the finalize callback | DISCRIMINATING |
| ContentWriteBufferS3ModeCancelCancelsSinkAndSkipsFinalize | `cancel()` before finalize cancels the sink and NEVER calls the finalize callback (no partial-finalize promote-worthy hash); the destructor's defensive re-cancel is a safe no-op | let a cancelled buffer still fire the finalize callback, or double-act on destructor re-cancel | DISCRIMINATING |
| PromoteViaServerSideCopyCreatesFreshBlobMaterializedProof / PromoteOverExistingCleanBlobAdoptsAndNeverOverwrites / PublishOverCondemnedBlobUsesFreshTagNotVerbatim (3) | the mandatory-HEAD blob promotion selection: fresh key -> exactly one verbatim native copy; existing Clean key -> zero publication, adopt in place (unchanged token); Condemned key -> exactly one RETAGGED streaming publication that reads the writer's OWN staging object (never the condemned key) and mints a fresh incarnation | pick the wrong publication mode for any of the 3 destination states, or let the Condemned case read/copy the condemned key itself | DISCRIMINATING (×3) |
| WritableS3StagingRequiresNativeOnlyCopy / UnsupportedNativeOnlyCopyDoesNotFallBackToLocal / GenerationBackendMayUseNativeOnlyCopy (3) | S3 staging with `NativeOnly` copy support actually stages an object; without that capability it throws `NOT_IMPLEMENTED` at startup rather than silently falling back to local; a GCS-generation-dialect backend can also use `NativeOnly` copy | let S3 staging silently degrade to local storage when the capability is missing | DISCRIMINATING (×3) |
| SuccessfulCommitRemovesOrphanedS3StagingObject | a successful commit removes the S3 staging object of an orphaned (unlinked-before-commit) pending blob via `cleanupPendingTempFiles`'s dedicated branch | let an orphaned S3 staging object survive a successful commit | DISCRIMINATING |
| ReadYourWritesReturnsStagedBytesFromS3StagingObject | read-your-writes over a pending S3-staged blob returns the STAGED bytes from the real S3 staging object, not a local temp file | serve stale/local bytes instead of the S3 staging object's real content | DISCRIMINATING |
| RemovesOnlyObjectsUnderGivenMountPrefix | `sweepOwnMountStaging` removes only objects under the GIVEN mount's staging prefix, leaving a DIFFERENT mount's staging objects untouched (the lease-fence) | let the sweeper touch another mount's staging objects | DISCRIMINATING |
| GcBlobDiscoveryPrefixExcludesStagingObjects | GC's blob-discovery prefix (`Layout::blobsPrefix()`) is lexically disjoint from the S3-staging prefix | the property promised is a real prefix-separation invariant, but the staging-prefix side of the comparison is a HAND-WRITTEN LITERAL (`"p/staging/mountA/"`) rather than sourced from the real `stagingKeyPrefix()` accessor this same file uses elsewhere (tests 14/16/17) -- so a regression in the ACTUAL production staging-prefix generator that nested it under `blobs/` would NOT be caught by this test; only a regression in `Layout::blobsPrefix()` itself (e.g. returning `"p/"`) would | WEAK |

Summary for gtest_cas_s3_staging.cpp: 20 tests, 19 DISCRIMINATING, 1 WEAK (GcBlobDiscoveryPrefixExcludesStagingObjects -- compares the real blobs prefix against a hardcoded staging-prefix literal instead of the real accessor, so it cannot catch a regression on the staging side).
## src/Disks/tests/gtest_cas_sentinel_probe.cpp (11 tests) {#src-disks-tests-gtest-cas-sentinel-probe-cpp-11-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PresentKeyReturnsPresentWithBody / AbsentKeyWithContainerAliveReturnsKeyAbsent (2) | the generic default probe classifies a present key as `Present` with body, and an absent key as `KeyAbsent` (container proven alive by a sibling write) | swap the two classifications | DISCRIMINATING (×2) |
| ContainerDirectoryRemovedReturnsContainerAbsent | removing the whole disk-root container directory probes `ContainerAbsent`, distinct from an ordinary absent key -- proven with a live sanity check (Present vs KeyAbsent) before the removal | conflate a missing container with an ordinary missing key | DISCRIMINATING |
| NativePresentKeyReturnsPresentWithBody | the Native-mode raw-HEAD-then-get path also correctly classifies `Present` with the real body | break the Native present-key path while leaving Emulated correct | DISCRIMINATING |
| TransportErrorNeverClassifiesAsAbsent | an untyped transport error classifies `Indeterminate`, NEVER `KeyAbsent`, even though it superficially looks like "nothing there" | classify an unclassifiable transport error as absence | DISCRIMINATING |
| NativeClassifiesNoSuchKeyAsKeyAbsent / NativeClassifiesResourceNotFoundAsKeyAbsent / NativeClassifiesNoSuchBucketAsContainerAbsent / NativeClassifiesAccessDeniedAsAccessDenied / NativeClassifiesUnmodeledErrorAsIndeterminate (5) | the full S3 IAM permutation table classifies EXACTLY: `NO_SUCH_KEY`/`RESOURCE_NOT_FOUND` (the real bodyless-404 code) -> KeyAbsent, `NO_SUCH_BUCKET` -> ContainerAbsent, `ACCESS_DENIED` -> AccessDenied, anything unmodeled -> Indeterminate (fail-closed) | misclassify any of the 5 S3 error codes, especially treating the real bodyless-404 `RESOURCE_NOT_FOUND` as anything but absence | DISCRIMINATING (×5) |
| InstrumentedBackendForwardsToInnerClassification | `InstrumentedBackend` (the wrapper production ALWAYS applies) forwards `probeSentinelRaw` to the real typed inner classifier, not the generic head/get-based default -- proven with `ContainerAbsent`, an outcome the generic default cannot produce at all | let `InstrumentedBackend` fall through to the generic default, silently discarding real S3 error classification in production | DISCRIMINATING |

Summary for gtest_cas_sentinel_probe.cpp: 11 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_server_root_format.cpp (9 tests) {#src-disks-tests-gtest-cas-server-root-format-cpp-9-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| Owner (battery, golden bytes) / RetiredAtRoundTrip (2) | `OwnerObject` encodes to exact golden bytes and round-trips the optional `retired_at_ms` field | reorder/rename a wire field, or drop the optional field on round-trip | DISCRIMINATING (×2) |
| ServerEpoch (battery, golden bytes) | `ServerEpoch` encodes to exact golden bytes and passes the shape-level failure battery | corrupt the golden encoding or a battery failure mode | DISCRIMINATING |
| MountLease (battery, golden bytes) | `MountLease` encodes ALL fields to exact golden bytes and passes the shape-level failure battery | drop/reorder/mis-type any of the ~10 fields | DISCRIMINATING |
| FarewellSentinelAndFencedSurvive | the farewell sentinel (`UINT64_MAX` build sequence) and `gc_fenced=true` round-trip correctly alongside ordinary fields | corrupt the sentinel value or the boolean on round-trip | DISCRIMINATING |
| WriteAttemptIdIsRequiredAndCanonical / ZeroWriteAttemptIdIsRejected (2) | `write_attempt_id` is required (a body omitting it fails `CORRUPTED_DATA`) and a zero value is also rejected (not just absence) | let a missing or zero write_attempt_id decode successfully | DISCRIMINATING (×2) |
| UnknownFieldsRemainTolerated | an unrecognized future field is tolerantly skipped, not fatal | make the tolerant reader fail closed on a genuinely unknown field | DISCRIMINATING |
| RejectsMissingIdentityFields | each of 3 identity fields (server_uuid/writer_epoch/write_attempt_id), dropped ONE AT A TIME, produces the EXACT message naming that specific field -- a shared fixture/message could not distinguish 2 of the 3 checks being deleted | let any one of the 3 field-presence checks silently pass, or let the error message stop naming the specific missing field | DISCRIMINATING |

Summary for gtest_cas_server_root_format.cpp: 9 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_settings.cpp (25 tests) {#src-disks-tests-gtest-cas-settings-cpp-25-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| DefaultsAndOverridesLand | explicit overrides land, unset settings keep their table default, and an absent scratch path never touches the anchor | let an override fail to land, or a default silently change | DISCRIMINATING |
| RemovedCacheSettingsAreRejected / RetiredPartFolderValidateIsRejected (2) | 2 retired setting families (dedup cache settings, `cas_part_folder_validate` with 3 legacy value shapes) are rejected as `UNKNOWN_SETTING`, never silently accepted | let a retired setting silently load and do nothing | DISCRIMINATING (×2) |
| UnknownKeyRejected / MissingRequiredSettingNamesExternalConfigKey / InvalidBoundsDiagnosticNamesExternalConfigKeys / InvalidEnumDiagnosticsNameExternalConfigKeys (4) | unknown key, missing required key, out-of-bounds value, and invalid enum value each produce the EXACT expected diagnostic message naming the real external config key | let any diagnostic message go stale, wrong, or lose the external key name | DISCRIMINATING (×4) |
| ForeignKeysAreNeverInspected | a broad sample of foreign (non-CAS) disk-config keys never triggers any CAS-setting validation, sampling classes a name-based skip-list provably cannot cover | let a foreign key accidentally collide with a CAS setting name and get validated/rejected | DISCRIMINATING |
| LegacySpellingStillLoadsDuringMigrationWindow / PartialMigrationLoadsAndReportsEveryLegacyKey / FullyMigratedBlockWarnsAboutNothing (3) | the legacy unprefixed spelling still loads during the migration window, a partial migration warns about EVERY legacy key present (exact occurrence count), and a fully-migrated (all-prefixed) config warns about nothing | let the legacy spelling silently stop working, under/over-count the warning occurrences, or spuriously warn on a clean config | DISCRIMINATING (×3) |
| BothSpellingsOfOneSettingRejected / RepeatedKeyRejectedInEitherSpelling (2) | both the legacy and prefixed spelling of the SAME setting present together is rejected as ambiguous; a key repeated under either spelling (Poco's `name`/`name[1]` rendering) is recognized by base name and rejected, never silently taking the first value | let one spelling silently win over the other, or let a repeated key silently keep only its first value | DISCRIMINATING (×2) |
| MalformedRepeatedPrefixedKeyIsRejectedBeforeParsing / MalformedPrefixedValueCannotMaskBothSpellingsConflict (2) | a malformed value on a repeated/ambiguous key is rejected by the STRUCTURAL check (repeated-key / both-spellings) BEFORE the value is even parsed, with the exact expected message substring | let a malformed value's parse failure mask the real structural conflict, or vice versa | DISCRIMINATING (×2) |
| AmbiguousConfigDoesNotWarnOrPartiallyApplySettings / UnknownPrefixedKeyDoesNotWarnOrPartiallyApplySettings / MalformedPrefixedKeyDoesNotWarnOrPartiallyApplySettings / SemanticInvalidPrefixedKeyDoesNotWarnOrPartiallyApplySettings / InvalidEnumDoesNotWarnOrPartiallyApplySettings (5) | 5 distinct rejection shapes (ambiguous pair, unknown key, malformed value, semantically invalid value, invalid enum) all leave the settings object COMPLETELY untouched (`.changed` false on every probed field) and emit no "are applied" log line -- atomicity: validate-all-then-commit, never partial apply | let any rejection path partially mutate the settings object or log a spurious "applied" line | DISCRIMINATING (×5) |
| ValidMixedConfigCommitsAfterAllValuesValidate | a genuinely valid mixed legacy+prefixed config commits both values correctly | let a valid mixed config fail to commit, or commit the wrong value | DISCRIMINATING |
| SkipAccessCheckKeepsItsBareSpelling / PrefixedGcsCapIsNotACasSetting (2) | `skip_access_check` deliberately has NO prefixed spelling (the prefixed form is unknown), and a GCS-specific cap setting under the `cas_` prefix is also correctly unknown (namespace boundary) | let the prefixed spelling of `skip_access_check` accidentally work, or let a foreign-subsystem cap leak into the CAS setting namespace | DISCRIMINATING (×2) |
| ValidateFailsClosed | 4 distinct invalid configs (missing required key, empty required key, out-of-bounds value, invalid enum) all fail closed with the exact expected error code | let any of the 4 invalid shapes silently succeed | DISCRIMINATING |
| RelativeScratchPathAnchored / AbsentScratchPathUsesDefaultVerbatim (2) | a relative scratch path is anchored under the data directory; an absent one uses the caller-supplied default verbatim (no anchoring) | anchor the default path when it shouldn't be, or fail to anchor a relative override | DISCRIMINATING (×2) |

Summary for gtest_cas_settings.cpp: 25 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_shutdown_context.cpp (9 tests) {#src-disks-tests-gtest-cas-shutdown-context-cpp-9-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| TeardownPhase1ThrowExitsCleanly / TeardownPhase2ThrowExitsCleanlyAndSkipsTheMarker / TeardownPhase3ThrowExitsCleanly (3) | a thrown exception in any of the 3 teardown phases still exits the process cleanly (code 0), never crashing/aborting on `~Pool`; phase 2's failure specifically leaves NO clean-release marker (so a successor cannot skip its observation window) | let a teardown-phase exception propagate uncaught (aborting the process), or let a failed phase-2 drain still write the clean-release marker | DISCRIMINATING (×3, real subprocess exit tests) |
| EmitAfterResetSharedContextExitsCleanly | emitting an event after `resetSharedContext` (the exact `Server.cpp` shutdown window) exits cleanly, never dereferencing a null shared context | crash on a null-shared-context dereference in the post-reset emit window | DISCRIMINATING |
| ExpiredContextDropsTheEventAndCountsIt | an EXPIRED weak context reference drops the event and increments `CASEventDroppedContextExpired` by exactly 1 | drop the event without counting it, or count when the context is still alive | DISCRIMINATING |
| DisabledIntegrationCountsNothing / MissingSystemLogCountsNothing (2) | a disabled (null-context) integration emits nothing and counts nothing; a live context with no configured system log is ordinary steady state (no emit, no count) | conflate either of these 2 non-error states with the counted expired-context case | DISCRIMINATING (×2) |
| StorageDoesNotExtendContextLifetime | the storage holds only a WEAK reference to the context: releasing the caller's shared_ptr drops the context to a real zero use-count immediately | let the storage hold a strong reference, keeping the context alive past its owner's release | DISCRIMINATING |
| ExpiredContextAtStartupFails | supplying an already-expired context reference AT startup is an error (throws), not silently treated as the disabled path | silently treat an expired-at-startup context as "integration disabled" | DISCRIMINATING |

Summary for gtest_cas_shutdown_context.cpp: 9 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_slot_occupy.cpp (11 tests) {#src-disks-tests-gtest-cas-slot-occupy-cpp-11-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AbsentKeyCommitsWithOneRequest | an absent-key create commits with exactly ONE request (no HEAD, no GET), proven by its own response | require an extra request, or fail to prove commit from the write response alone | DISCRIMINATING |
| PreExistingKeyConflictsWithExactBytesAndIncarnationInTwoRequests | a conflict against an existing key costs exactly ONE write + ONE read (never a HEAD-then-read), and names the exact occupant bytes+incarnation, and never overwrites | let the shape be a HEAD-then-read, or corrupt the occupant identity, or overwrite | DISCRIMINATING |
| AmbiguousWriteThatLandedNothingGivesUpHavingSentOne | under `once`, an ambiguous write that landed nothing gives up as `Unresolved{sent_any=true, ProvenAbsent}`, creating nothing | report a never-landed ambiguous attempt as committed, or fail to prove absence | DISCRIMINATING |
| ConflictThenVanishGivesUpRatherThanFabricatingACommit | a conflicting occupant that vanishes before the settling read is reported `Conflict{ProvenAbsent}`, never fabricated as a commit | fabricate a commit when the conflicting occupant is later found absent | DISCRIMINATING |
| LivenessRefusalBeforeTheAttemptSendsNothing / ExhaustedPolicyDeadlineRefusesBeforeTheAttempt (2) | a dead liveness predicate or an exhausted policy deadline refuses BEFORE any attempt (0 writes, 0 reads), with the key provably untouched | let either pre-attempt refusal still send a request | DISCRIMINATING (×2) |
| DefiniteStoreRefusalIsAValueAndSettlesNothing | a whitelisted synchronous store refusal is reported as a VALUE (`Refused`), triggering ZERO settling reads (the refusal proves non-application) | let a proven refusal still trigger a settling read | DISCRIMINATING |
| DeterministicLocalFailurePropagatesWithoutSettling | a deterministic local (programming-bug-class) failure propagates as the ORIGINAL exception, unchanged, never folded into a `WriteResult` outcome | swallow/reissue a local failure instead of propagating it unchanged | DISCRIMINATING |
| AdmissionLostAfterTheWriteIsNeverReportedCommitted | admission withdrawn WHILE the write is landing is reported `FenceLost{sent_any=true}`, never `Committed`, even though the object IS durable (the caller must resolve independently) | report a post-write admission loss as a committed claim | DISCRIMINATING |
| AdmissionLostAfterAConflictPreventsTheSettlingRead | admission withdrawn right after a conflicting write is discovered prevents the settling read from EVER starting (0 GETs) | let the settling read start after admission was already withdrawn | DISCRIMINATING |
| OwnLandedAmbiguousWriteIsObservedOnTheNextAttempt | the wedge-adoption input shape: an earlier ambiguous landed attempt is settled by its OWN resolve read; a SECOND independent attempt of the same key+bytes observes it as an ordinary occupant with the SAME incarnation both times | let the two calls observe different incarnations for the same landed object | DISCRIMINATING |

Summary for gtest_cas_slot_occupy.cpp: 11 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_sweep_deletion_premise.cpp (12 tests) {#src-disks-tests-gtest-cas-sweep-deletion-premise-cpp-12-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AnUnconsumedEpochSealRetainsTheBuildsManifests / AConsumedEpochSealWithACleanTailDeletes (2) | a cursor still inside the build's own epoch retains the manifest with a visible warning naming the key and "seal"; a cursor past the consumed epoch seal with a clean tail deletes it by exact token | delete before the epoch seal is proven consumed, or fail to delete once it is | DISCRIMINATING (×2) |
| AHeldNamespaceRetainsEvenAboveAConsumedSeal / ANamespaceWithNoSealedCursorRetains (2) | a held namespace retains everything under it (warning names the hold reason word) even above a consumed seal; a namespace with no sealed cursor at all (or one for a DIFFERENT namespace) retains, warning "coverage" | let a hold or an absent coverage row be silently ignored | DISCRIMINATING (×2) |
| AnUnconsumedTailRemovalRetainsItsTarget | rule (2): the shared `manifestDeletionPremise` predicate retains a manifest a tail removal target still names, with the exact reason, and admits deletion the moment that target is cleared -- isolating that the tail-removal-target set alone drives the retention | let the predicate ignore an unconsumed tail removal target | DISCRIMINATING |
| TheCursorPagePathHonoursTheSamePremise | the background cursor-paced page and the per-namespace sweep reach the SAME verdict from the SAME premise (retain then delete, driven by the same cursor transition) | let the two sweep paths diverge on the same input | DISCRIMINATING |
| AnUndecodableManifestDoesNotWedgeTheCursorPage | an undecodable manifest body is retained (counted, never deleted on a guess) WITHOUT wedging the page -- a later key past it (proven via key ordering) is still decided this same page | let an undecodable body wedge the cursor page, or silently delete it | DISCRIMINATING |
| DebrisUnderANamespaceTheFoldNeverWalksIsRetainedIndefinitely | pure pre-precommit orphan debris under a namespace the fold never walks is retained INDEFINITELY across repeated passes, each pass reporting the retention (never going silent) | delete debris under an unfolded namespace, or stop reporting the retention on repeated passes | DISCRIMINATING |
| DistinctRetainReasonsLandInDistinctCounters | two namespaces retained for TWO DIFFERENT reasons on ONE page land in two DISTINCT counters (not one bucket catching both), the rollup picks the correct top reason with a stable enum-order tiebreak, and an empty page reports `None`/0 | let two distinct retain reasons collapse into one counter, or misreport the top-reason tiebreak | DISCRIMINATING |
| AnExhaustedDeleteBudgetRetainsAndDoesNotStepOverTheRest | an exhausted delete budget retains the undecided candidates WITHOUT advancing the cursor past them (a resumed page reaches and deletes them; nothing is silently skipped for a whole cycle) | let the cursor step over budget-exhausted candidates, permanently losing them for a cycle | DISCRIMINATING |
| RecoveryWorkBudgetRetainsAndConvergesWithoutWedgingTheCursor | a namespace whose recovery walk can never finish within one round's op budget still gets EVERY candidate DECIDED (retained, counted `retained_work_budget`) on every page, the cursor makes real progress every page, and the whole keyspace is covered within 10 pages -- a genuine liveness proof against a pathological namespace | let a pathological namespace's unbounded recovery cost wedge the cursor page (zero progress), or silently drop its candidates from the page instead of deciding them | DISCRIMINATING |
| NamespaceWorkBudgetCapsDistinctViewsPerPage | the per-page namespace cap limits how many namespaces get a protection view built; the namespace with no budget left is retained under the work-budget cause, NEVER decided from a missing/partial view | let a namespace past the cap get a partial view and be wrongly deleted from it | DISCRIMINATING |

Summary for gtest_cas_sweep_deletion_premise.cpp: 12 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_text_format.cpp (11 tests) {#src-disks-tests-gtest-cas-text-format-cpp-11-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| EveryRegisteredFormatIsBatteryCovered | every registered `FormatId` has a corresponding entry in the common format battery (set-equality check) | add a new format without registering it in the battery -- this test would still pass wrongly unless the set comparison is exact; it is | DISCRIMINATING |
| NewIdsExistWithFrozenValues | 4 new `FormatId` values are frozen at their exact wire numbers, and every id has a non-empty change-point ladder | renumber a frozen format id, or leave a format with no change-point ladder | DISCRIMINATING |
| CompleteUniqueAndGated (+ death-test twin) | every `FormatId` (except the reserved Roster) has traits, each type is unique and `cas_`-prefixed, the type<->id lookup round-trips, deterministic formats are pinned raw+strict, and `.zst` suffix applies to exactly the Always-compress set | let a format lack traits, share a type name with another, or misclassify its compression/strictness/suffix | DISCRIMINATING (×2, release/debug twins) |
| WriteAndReadBack | the JSON micro-vocabulary writer/reader round-trips 4 value kinds (hex128, u64-string, int, escaped string) in exact key order, verified against an exact byte prefix | drop/reorder/mis-encode any value kind | DISCRIMINATING |
| WordArrayFieldAndReaderRejectInvalidValues | a word-array field round-trips to exact bytes and the reader rejects a non-array and a non-string array element | let the reader accept a malformed word-array shape | DISCRIMINATING |
| FailClosedRules | 7 distinct fail-closed rules (duplicate key, unknown-key strict-vs-tolerant, critical-key-always-fails, non-canonical whitespace, bad hex width, junk in u64-string) each produce the exact expected error code | relax any one of the 7 rules | DISCRIMINATING |
| WriteExpectSniffGate | the header line round-trips exactly, `sniffHeaderLine` distinguishes a real header from garbage, wrong-type fails `CORRUPTED_DATA`, future version fails `UNKNOWN_FORMAT_VERSION`, and an out-of-range version fails `CORRUPTED_DATA` -- 3 distinct failure classes pinned precisely | conflate any of the 3 header-gate failure classes | DISCRIMINATING |
| ReadLineAndTrailer | the trailer line round-trips exactly, `readLine` returns lines correctly, and both a missing terminator and an over-cap line fail closed | tolerate a missing terminator or an over-cap line | DISCRIMINATING |
| SealOpenPolicyAndCaps | the zstd compression arm: Always-types compress regardless of size and a raw body still opens (repair path); Never/PinnedRaw types never compress on write and REJECT compressed input on read; a declared size over the cap fails BEFORE output allocation; a single flipped byte inside the frame is caught by the zstd checksum | let a PinnedRaw format accept compressed input, skip the pre-allocation size cap check, or fail to detect frame corruption | DISCRIMINATING |
| ForwardSlashPinnedUnescaped | CAS string values never escape forward slashes, independent of any global JSON-writer default | let the global `escape_forward_slashes` default leak into CAS values, breaking byte-determinism | DISCRIMINATING |

Summary for gtest_cas_text_format.cpp: 11 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_truncate_reclaim.cpp (3 tests) {#src-disks-tests-gtest-cas-truncate-reclaim-cpp-3-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| PerRefDropOfSharedBlobsReclaimsToZero | B140 regression: 32 heavily-deduplicated parts (shared blobs, live GC interleaved), then a per-ref drop of EVERY part (real TRUNCATE shape), then GC-to-fixpoint reaches `fsck.unreachable == 0` -- every shared blob actually reclaimed | let a shared blob survive full reclamation (the original B140 bug: GC reported candidates=0 while orphans remained) | DISCRIMINATING |
| TruncateThenKeepInsertingStillReclaims | pre-truncate orphans still fully reclaim even while NEW inserts continue after the truncate, within a TIGHT bound of 8 GC rounds (matching the soak's real time budget) | let post-truncate inserts stall reclamation of pre-truncate orphans, or blow the round budget | DISCRIMINATING |
| DropNamespaceLeavesSharedBlobDebrisForPerpetualSweep | the DROP TABLE path (`dropNamespace`, one Remove per former ref) reaches the SAME full reclamation (`unreachable == 0`), and the emptied namespace's life is no longer cataloged | let a whole-namespace drop leave orphaned blobs stuck, or leave the emptied namespace's life cataloged | DISCRIMINATING |

Summary for gtest_cas_truncate_reclaim.cpp: 3 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_txn_apply_ledger.cpp (7 tests) {#src-disks-tests-gtest-cas-txn-apply-ledger-cpp-7-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| HealthyRoundReportsNothingUnapplied | a fully applied transaction, and one that legitimately committed no deltas, both report clean | let a healthy round falsely report unapplied | DISCRIMINATING |
| CommittedAndProducedButNeverAppliedIsReported | a committed+produced-but-never-applied transaction is reported with the correct ordinal | miss reporting a genuinely lost transaction | DISCRIMINATING |
| ClampedTransactionIsNotReported | a clamped (staged-then-discarded, never committed) log's produced deltas are never reported unapplied | falsely flag a legitimately discarded clamped log | DISCRIMINATING |
| OrdinalsIndexTheParallelVectors | `open()`'s returned ordinal is the position in every parallel vector (applied/produced/committed/namespaces/txns), which grow together | let the parallel vectors desync from the ordinal | DISCRIMINATING |
| OnlyTheTransactionWhoseDeltasVanishedIsReported | a delta-consumption mark clears exactly its own transaction; only the ordinal never written stays reported -- the exact shape a delta lost in shard routing produces | let marking one ordinal spuriously clear another, or fail to isolate the lost one | DISCRIMINATING |
| ReducerMarksTheOrdinalOfEveryDeltaItConsumes | the REAL production reducer (`foldDeltasIntoGeneration`) positively marks the ordinal of every delta it consumes and leaves an ordinal no delta carries unmarked -- proven positively (not by absence of a throw), which distinguishes "the probe is correct" from "the probe is inert" | let the reducer skip marking a consumed delta's ordinal, or mark an ordinal no delta carries | DISCRIMINATING |
| ReducerMarksAnUnmatchedRemovalDelta | an unmatched removal delta (legitimate no-op, collapses to nothing in the merge) is STILL marked applied at consumption time, so it never falsely reads as a lost transaction | mark applied only at run-flush instead of at consumption, silently mis-reporting every legitimate unmatched-removal round as lossy | DISCRIMINATING |

Summary for gtest_cas_txn_apply_ledger.cpp: 7 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_upload_detached.cpp (14 tests) {#src-disks-tests-gtest-cas-upload-detached-cpp-14-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| FreshMissHeadsThenPublishesWithoutPrepublicationMetaGet / ExistingCleanHeadsAndObservesWithoutPublication / ExistingBodyWithoutMetadataBackfillsWithoutPublication / AbsentBodyWithStaleCondemnedPublishesBeforeMetadataRead / PresentCondemnedPublishesFreshAndQueuedOldDeleteMisses (5) | the exact mandatory-HEAD protocol ordering across 5 distinct destination-state permutations: HEAD always first, no meta-GET before the FIRST publication, publication only when needed, and a stale queued delete against the old condemned token MISSES the fresh replacement | reorder HEAD after publication, let a meta-GET precede the first publication, publish when observation alone suffices, or let a queued delete of the old token match the fresh replacement | DISCRIMINATING (×5) |
| PresentBodyWithoutMetadataBackfills / FreshLocalStreaming / S3StagingPromotion / CondemnedLocalResurrection / CondemnedS3Resurrection (5) | 5 destination-state x transport permutations (adopt-and-backfill, fresh-local-stream, fresh-S3-native-copy, condemned-local-restream, condemned-S3-restream) each produce the EXACT expected diagnostics (action/reason/transport), correct dependency proof, and a byte-identical result to an independent TWIN build using the ordinary serial `putBlob` path | pick the wrong transport/reason for any of the 5 permutations, or let the detached-upload path diverge from the serial `putBlob` path's bytes | DISCRIMINATING (×5) |
| MergeAppliesAllDeps | `mergeBlobUploadResults` folds N detached results in ONE call to EXACTLY the same dependency map a serial fold would produce (deep map comparison against a twin build) | let the merge path produce a different dependency map than the serial equivalent | DISCRIMINATING |
| MergeFailureLeavesBuildUntouched | a mid-merge exception leaves the build's dependency map BYTE-FOR-BYTE at its pre-merge state (deep snapshot comparison, including an untouched pre-existing dep) -- all-or-nothing | let a partial merge leak a dependency into the map before the exception, or disturb an unrelated pre-existing dep | DISCRIMINATING |
| MergeValidatesSizes (+ death-test twin) | two results for the SAME ref with CONFLICTING declared sizes are rejected as `LOGICAL_ERROR` (or abort in debug/sanitizer) BEFORE either applies, leaving the build untouched | let a size-conflicting duplicate silently apply one of the two sizes | DISCRIMINATING (×2, release/debug twins) |

Summary for gtest_cas_upload_detached.cpp: 14 tests (counted as rows, matching the file's 14 TEST bodies), all DISCRIMINATING.
## src/Disks/tests/gtest_cas_upload_fanout.cpp (17 tests) {#src-disks-tests-gtest-cas-upload-fanout-cpp-17-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| CopiedAndMovedRequestsSharePublicationAttemptedState | a request COPIED through the fan-out (after a first-attempt exception on the original) retains the "publication already attempted" state -- exactly one verbatim-copy attempt total, the retry streams instead | let a copied request re-attempt verbatim copy after the original already tried it | DISCRIMINATING |
| DependencyProofEquivalentAcrossFanoutBranches | a 6-branch world (observed-clean, adopt-backfill, fresh-local, S3-staging, condemned-local, condemned-S3) produces IDENTICAL recorded deps and IDENTICAL backend end state whether run serially (pool=1) or fanned out (pool=4) | let any branch diverge between serial and parallel execution | DISCRIMINATING |
| CondemnedBranchesNeverGet | INV-1: a condemned blob BODY is never GET/streamed by either the local or S3 resurrection branch (per-key zero counts on a real CountingBackend), while the condemned META point-read still happens (the distinguishing signal) | let a resurrection branch read the condemned body instead of streaming fresh from the writer's own source | DISCRIMINATING |
| DuplicateRefsLaunchOneTask / DuplicateCondemnedS3ResurrectsCorrectly (2) | two duplicate pending-blob records for the SAME ref (plain and condemned-S3 cases) launch exactly ONE task, merging to one dep with correct content | let duplicates launch two tasks or produce two conflicting deps | DISCRIMINATING (×2) |
| ConflictingDuplicateSizesRejected (+ death twin) / DeclaredSizeMustMatchSourceSize (+ death twin) (4) | conflicting declared sizes for one ref, and a request whose declared_size disagrees with its own source.size, are both rejected as `LOGICAL_ERROR` (or abort) BEFORE any task runs, build untouched | let either wiring-bug shape silently apply | DISCRIMINATING (×4, 2 release/debug pairs) |
| CondemnedLocalResurrectStreamsAndFlipsMetaClean | the condemned-local resurrection end-to-end: fresh incarnation (token differs), a stale queued exact-token delete of the OLD condemned incarnation MISSES the fresh replacement, payload preserved verbatim, meta flips back to Clean | let the fresh replacement share the condemned token (making the stale delete a data-loss hazard) | DISCRIMINATING |
| CondemnedLocalPublicationOpensSourceOnce | the mandatory HEAD itself never opens the source; exactly ONE unconditional streaming publication opens it | let HEAD itself consume/open the source, or double-open on republication | DISCRIMINATING |
| PendingFanoutFailureCreatesNoDependency | one task's failure (poisoned source) still merges NOTHING (not even the successful sibling's), and after `abandon()` the successful sibling's already-uploaded body becomes ORDINARY GC-reclaimable debris (real GC round proof, not a hand-waved "should be reclaimable") | let a partial failure merge the successful sibling's dep, or leave its debris permanently unreclaimable | DISCRIMINATING |
| ConcurrentPublicationsEstablishProof | two distinct refs, forced to run GENUINELY CONCURRENTLY via a `std::latch` (both must rendezvous or deadlock -- no false-positive path), both establish `Materialized` | let concurrent publication corrupt or lose either dependency | DISCRIMINATING |
| PoolSaturationBounded | a pool of 2 achieves real peak concurrency 2 (measured via a rendezvous probe); a pool of 1 degenerates to serial WITHOUT deadlocking (fails fast on a bounded wait) -- both configurations complete every upload | let pool-size-1 deadlock, or let pool-size-2 fail to actually run concurrently | DISCRIMINATING |
| DrainPrecedesUnwind / DispatchThrowStillDrains / TrackingSeamThrowStillDrains (3) | 3 distinct throw injection points (in-task, mid-dispatch-loop, at the tracking-publication seam) all still DRAIN every already-scheduled/running task before the exception unwinds the stack -- proven by the sibling's body being present after the throw, and (TrackingSeamThrowStillDrains) specifically an ASan-clean run against the exact B90-shaped scheduled-but-untracked heap-use-after-free regression | let any of the 3 throw points skip draining an in-flight task before destroying captured storage (the B90 UAF regression) | DISCRIMINATING (×3) |

Summary for gtest_cas_upload_fanout.cpp: 17 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_upstream_slice.cpp (12 tests) {#src-disks-tests-gtest-cas-upstream-slice-cpp-12-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| HeadListRemoveOverloadsRefuseSingleAttemptOnTheBaseStorage | the base `IObjectStorage`'s 3 metadata/list/removal overloads refuse `SingleAttempt` with an exact distinguishing message each (never conflated), while `Default` still reaches the ordinary implementation | let any of the 3 overloads silently accept `SingleAttempt`, or conflate its message with the pre-existing removal refusal | DISCRIMINATING |
| NativeConditionalReadSettingMarksTheGetRequest | a plain GET carries the SAME native-conditional mark a HEAD does when the read setting asks for it (verified true and false against real scripted clients) | let the GET request lose the native-conditional mark, breaking incarnation-identity consistency with HEAD | DISCRIMINATING |
| ReadSmallObjectThrowsWhenAReissueAnswersWithADifferentETag / ReadSmallObjectAcceptsAReissueThatAnswersWithTheSameETag (2) | a reissue that straddles a genuine object replacement (different ETag) throws `CANNOT_READ_ALL_DATA` naming "response identity changed"; an ordinary reissue answering with the SAME ETag is accepted normally | let a straddled replacement silently mix bytes from two incarnations, or reject an ordinary same-identity reissue | DISCRIMINATING (×2) |
| SingleAttemptProfileIssuesExactlyOneGetOnThrottle | under `SingleAttempt`, a retryable throttle answer does NOT get reissued -- exactly one GET call | let `SingleAttempt` still reissue a retryable error | DISCRIMINATING |
| SingleAttemptClientCarriesTheRequestedTimeout | the single-attempt client clone carries the EXACT requested timeout and zero retries, and is cached/keyed by that timeout (a different bound is never served the wrong clone) | let the clone ignore the requested timeout, or serve the wrong cached clone for a different bound | DISCRIMINATING |
| ExpiredTokenOnSingleAttemptReadInstallsTheRefreshedClientIntoTheStorage / NativeTokenHeadRecoversFromAnExpiredTokenAndInstallsTheRefreshedClient / ConditionalRemoveRecoversFromAnExpiredTokenAndInstallsTheRefreshedClient (3) | an expired-token failure on read/HEAD/conditional-DELETE installs the REFRESHED client into the storage (identity-checked pointer swap), so the NEXT request uses fresh credentials, even though a single-attempt read itself never retries | let the refreshed client fail to get installed, leaving every future request repeating the expired-token failure | DISCRIMINATING (×3) |
| SingleAttemptConditionalRemoveIssuesExactlyOneRequestOnThrottle | `Default` profile genuinely retries a throttled conditional DELETE (3 attempts, matching `max_retries=2` on the real SDK retry strategy), while `SingleAttempt` issues exactly ONE -- "exactly one" is proven a property of the CLONE, not of the fake, via the Default control | let `SingleAttempt` still retry, or let the Default control's own retry count go unverified (which would make "exactly one" meaningless) | DISCRIMINATING |
| HeadAndRemoveUnderSingleAttemptRideTheClientBoundToTheRequestedTimeout | HEAD and conditional-DELETE each ride a clone bound to the EXACT timeout their caller requested (two different bounds coexist without evicting each other), and re-requesting an already-built bound reuses the cached clone rather than rebuilding | let two verbs share the wrong timeout clone, or evict/rebuild a still-needed clone | DISCRIMINATING |
| IterateUnderSingleAttemptSelectsTheClientBoundToTheRequestedTimeout | `iterate` under `Default` issues no clone at all (lazy first-batch fetch); under `SingleAttempt` it clones the client bound to the exact requested timeout | let `Default` eagerly clone, or let `SingleAttempt` use the wrong timeout bound | DISCRIMINATING |

Summary for gtest_cas_upstream_slice.cpp: 12 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_wire_vocab.cpp (15 tests) {#src-disks-tests-gtest-cas-wire-vocab-cpp-15-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| EnumTablesPinTheCurrentWords | 3 enum wire tables (dialect/hash-algo/object-kind) map to their exact current wire words | rename any wire word | DISCRIMINATING |
| ClosedSetsRoundTripEveryEnumeratorExhaustively | EVERY enumerator of 4 enums (walked via `magic_enum::enum_values`, not a hand-copied list) round-trips through its wire table -- a new enumerator with no table entry cannot silently pass | add a new enum value without a table entry (would fail this test where a hand-copied list would miss it) | DISCRIMINATING |
| EnumWordsRoundTrip | 3 enums round-trip a sample of their values and reject an unknown word for 2 of them | let an unknown wire word decode successfully | DISCRIMINATING |
| SiblingFieldsWriteAndReadBack | the shared token+blob-ref field writer produces exact golden bytes (with correct JSON escaping) and round-trips through the real reader | corrupt the golden encoding or break the escaping | DISCRIMINATING |
| ManifestRefBundleWritesTheOldPrefixedKeys | the legacy `old_`-prefixed key bundle writer produces exact golden bytes | rename/reorder the legacy key bundle | DISCRIMINATING |
| MatchAndBuildRoundTripsABlobRef / MatchManifestRefFieldsAndBuildRefRoundTripInAnyKeyOrder / MatchTokenFieldsConsumesSemanticKeysAndLeavesUnrelatedKeyUnmatched / TokenFieldsBuildsInAnyKeyOrderAndRequiresBothFields (4) | the field-matcher helpers correctly consume their semantic keys REGARDLESS of key order, leave unrelated keys unmatched, and correctly distinguish quoted-decimal-string vs bare-number field shapes | let a matcher depend on key order, fail to leave unrelated keys unmatched, or mix up the string/number parse primitives | DISCRIMINATING (×4) |
| BlobRefBuildFailsClosedOnHalfAGroupAndOnBadWidth / BlobRefBuildFailsClosedOnRightWidthNonHexDigest / ManifestRefFieldsBuildRefFailsClosedOnHalfAGroup (3) | building from an incomplete field group (half-present), or a right-width-but-non-hex digest, fails closed at build time | accept a half-populated field group or non-hex-but-right-width digest | DISCRIMINATING (×3) |
| OldManifestEpochKeyDoesNotAliasTheSemanticKey | the legacy `me` key does NOT alias the semantic `epoch` key -- an object using only the legacy key still fails to build, with the exact expected message | let the legacy `me` key silently satisfy the semantic epoch requirement | DISCRIMINATING |
| RoundTripsThroughEveryFormatAndNeverBecomesAnIncarnation | a `PersistedEtag` (with an embedded quote requiring JSON escaping) round-trips through 4 DISTINCT wire encodings (shared JSON pair, `cas_run` NDJSON, GC outcome log, packed condemned-row bytes with a one-byte dialect), and the type system statically refuses constructing a live `Etag` from a persisted one | break the round-trip through any of the 4 encodings, or let `PersistedEtag` become constructible into `Etag` | DISCRIMINATING |
| UnknownDialectWordAndByteAreBothRefused | both directions of the dialect vocabulary (word->byte and byte->word) fail closed on unknown/out-of-range values | let either direction of the dialect vocabulary accept an invalid value | DISCRIMINATING |

Summary for gtest_cas_wire_vocab.cpp: 15 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_cas_writer_duties.cpp (10 tests) {#src-disks-tests-gtest-cas-writer-duties-cpp-10-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| UncertainAdoptedGrantStaysActiveUntilTheNextMutationRemovesIt | an uncertain (landed-then-lost) owner grant keeps its build active past the transaction object's destruction; only the NEXT mutation resolves the durable wedge, removes the exact old precommit, and THEN retires the build floor | let `~PartWriteTxn` unconditionally retire the build floor while the grant outcome is still unknown | DISCRIMINATING |
| ProvenAbsentGrantDrainsAsNoOpBeforeTheNextMutation | a pre-attempt-refused (never-wedged) grant is still kept active by the destructor, and the NEXT mutation drains it as a no-op before its own publish | let the deferred duty stay pending forever, or try to remove an owner that was never transmitted | DISCRIMINATING |
| WedgeResolvedAsRejectDrainsTheDutyAsNoOp | an ACTUALLY wedged (unresolved) grant that resolves as REJECT drains as a no-op (no `OwnerTransition` removal owed for an absent precommit), clearing the wedge and advancing `minActive` exactly like the no-wedge reject arm | conflate the wedge-resolved-reject case with the adopt case, or fail to advance `minActive` | DISCRIMINATING |
| DropRefServicesPendingDutyBeforeRemovingTheRef / UpdateRefPublishedAtServicesPendingDutyBeforeUpdatingTheRef / DropNamespaceOverloadsServicePendingDutyBeforeRemoval (2 sub-cases) / SnapshotAttemptServicesPendingDutyBeforePublishingLedgerState (5) | 5 distinct public mutation entry points (`dropRef`, `updateRefPublishedAt`, both `dropNamespace` overloads, the explicit snapshot attempt) each service a pending writer-cleanup duty BEFORE performing their own mutation, proven by the independent `minActive`/watermark observable | let any entry point skip the shared duty-drain seam, letting its own mutation overtake a pending duty | DISCRIMINATING (×5) |
| PendingDutySkipsCleanFarewellAndSuccessorSweepsTheCrashRemnant | a pending writer-cleanup duty forbids the clean-release certificate at teardown, forcing a fresh writer epoch and a real arithmetic recovery seal on the successor -- the ordinary stale-precommit sweep is what then reclaims the crash remnant | let a pending duty be missed by teardown, falsely certifying a clean farewell | DISCRIMINATING |
| RejectedAttemptBodyIsEventuallyNominatedAndSwept | a rejected attempt's orphan manifest body (never named by any owner, so the duty queue cannot reclaim it) is eventually nominated and swept by REAL GC rounds, gated on the predecessor's build epoch durably closing | let a rejected attempt's orphan body survive indefinitely, or let the sweep nominate it before the epoch closes | DISCRIMINATING |
| DutySurvivesSettlementFailureForRetry | a fault on the DUTY'S OWN settlement append (not the original grant) leaves the duty retained for retry AND aborts the mutation it was blocking; the very next drain, once the fault clears, settles the duty and lets the mutation proceed | let a settlement failure silently lose the duty, or let the blocked mutation succeed despite the settlement's own failure | DISCRIMINATING |

Summary for gtest_cas_writer_duties.cpp: 10 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_ca_transaction.cpp (22 tests) {#src-disks-tests-gtest-ca-transaction-cpp-22-tests}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ExtremeBufferSizeIsClampedNotPassedToAllocator | STID 0883 regression: an extreme (2^63-1) compress-block size is clamped to 256 MiB before reaching the allocator, never crashing it | let an extreme buffer size reach `Memory::alloc` unclamped, aborting the server | DISCRIMINATING |
| PublishHappensAtCommitNotRename / AbandonedPartLeavesNoRef / RefPublishedByCommit / CommittedRefMoveDoesNotSpuriouslyPublish (4) | the tmp->final rename is a pure re-key that publishes nothing; only `commit()` publishes; an abandoned (never-committed) transaction leaves no ref; a committed-ref rename (no staged source) goes via repoint, not a fresh publish | let the rename itself publish, or let an abandoned transaction leave a durable ref | DISCRIMINATING (×4) |
| TruncateFileIsNotSupported | `truncateFile` is explicitly unsupported (`NOT_IMPLEMENTED`) | let truncateFile silently succeed or do something else | DISCRIMINATING |
| StagedFinalizeDropsForeignScratchRef | a staged-source tmp->final finalize DROPS a foreign scratch ref durably published at the SAME tmp path by an unrelated nested sub-storage; the committed manifest is the authoritative one, never the scratch content | let the finalize carry the foreign scratch ref forward instead of dropping it, or publish the wrong manifest | DISCRIMINATING |
| ReadYourWritesAfterReKey | a read through the open transaction resolves staged content under the FINAL path (read-your-writes) after a tmp->final re-key, before commit, for both the file and its inner-directory overlay | let read-your-writes fail to resolve under the re-keyed final path | DISCRIMINATING |
| OverlayProgramOrder | create->delete->create in the overlay leaves the file PRESENT (no delayed delete fires after the later create); a delete of a staged file makes it absent to reads | let program order in the overlay misfire (a stale delayed delete clobbering a later create) | DISCRIMINATING |
| InFlightReadCarriedForwardProjectionSidecar | [02941 root-cause] a carried-forward projection sidecar (hard-linked from a committed source) is readable in-flight BOTH at the tmp build path AND after the tmp->final re-key, with correct bytes and size, and durable after commit | let the in-flight read return empty for the carried sidecar (the exact 02941 "Empty marks file" corruption) | DISCRIMINATING |
| CommitRollbackSparesPreexistingRef | on commit, only refs THIS commit created are eligible for rollback; a repoint of an already-existing ref (`created=false`) is never dropped as compensation, and the pre-existing part's content is carried forward | let the rollback loop drop a pre-existing (repointed) ref as if it were newly created | DISCRIMINATING |
| EagerFileInlinedDataBinBlobbed | a small eager metadata file is staged Inline (one-GET part open) while large column data stays a standalone Blob, and the inlined file is still readable through the normal path | mis-place a small file as Blob or a large one as Inline, or break the inlined read path | DISCRIMINATING |
| StandaloneWriteOnCommittedPartRepoints / CombinedWriteAndUnlinkSameTxnRepointsOnce / CommittedTxnVersionStoreRepoints / SurgicalUnlinkRepoints (4) | a standalone write, a combined write+unlink, a one-shot MVCC txn_version write, and a lone surgical unlink onto an already-committed part each resolve to EXACTLY ONE repoint, carrying every untouched file forward, verified against an independent `fsck` reachability walk finding zero dangling objects | let any of the 4 shapes fire 0 or 2 repoints, lose an untouched file, or leave fsck-dangling debris | DISCRIMINATING (×4) |
| BuildTimeSidecarsLandInManifest | the 3 retired mutable-sidecar files (`uuid.txt`/`metadata_version.txt`/`txn_version.txt`) now flow down the ordinary content path into the manifest like any other file, and are readable through the normal (non-special-cased) read path | reintroduce the retired mutable-per-part-file special case | DISCRIMINATING |
| UnlinkStormThenDirDropIsOneRefDrop | the dominant MergeTree fast-removal shape (unlink every file, then removeDirectory, one transaction) stays exactly ONE ref-drop and pays ZERO repoints -- the per-file marks are superseded, not individually repointed | let an unlink storm followed by a dir-drop fire a repoint per unlinked file instead of superseding them | DISCRIMINATING |
| UnlinkStormMemoizesOneForceFreshResolve | a 3-file unlink burst over one ref resolves it EXACTLY ONCE (memoized per transaction+ref), not once per file, verified via a real `RefResolve` event-sink count that ignores warm-cache hits | let the memo be dropped, causing a resolve per file in the burst | DISCRIMINATING |
| CreateThenDirDropDoesNotPublish | a create-then-remove of a new part in ONE transaction discards both the manifest entries and the in-flight build -- commit leaves no ref and no live precommit | let a create-then-remove leak a published ref or a stranded live precommit | DISCRIMINATING |
| StartupFailureLatePublishesNothing | a startup failure injected right before the atomic publish step leaves NOTHING published (`store()` still refuses, null pool) even though every prior step succeeded; a retried `startup()` then succeeds cleanly | let a late startup failure leave a partially-published pool | DISCRIMINATING |
| OperationsRefuseWithoutPublishedPool | `store()` (and every `poolAccess()` caller) refuses with `INVALID_STATE` (not the old process-aborting `LOGICAL_ERROR`) whenever no pool is published | let an unpublished-pool access abort the process instead of throwing a catchable operational error | DISCRIMINATING |
| ShutdownDiskProbesFailLoud | EVERY read-only probe (existence/enumeration/size, 9 distinct calls) on a shutdown (null-pool) storage throws `INVALID_STATE` -- the deliberate reversal of the old transitional "answer benign-absent" behavior | let any of the 9 probes silently answer absent instead of throwing on a null pool | DISCRIMINATING |

Summary for gtest_ca_transaction.cpp: 22 tests, all DISCRIMINATING.
## src/Disks/tests/gtest_ca_wiring.cpp (79 tests, part 1 of N) {#src-disks-tests-gtest-ca-wiring-cpp-79-tests-part-1-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ParsePartFilePathAtomic / ParsePartFilePathProjectionSubPath / ParsePartFilePathNonAtomic / ParseTableUuid / ParseTableFilePathNested (5) | the path parser correctly extracts table_uuid/part_name/file for Atomic, projection, non-Atomic, and table-level shapes | mis-parse any component boundary | DISCRIMINATING (×5) |
| ThreeCharDatabaseSharingTablePrefixDoesNotFalseAnchorAsAtomic / RealHexPrefixUuidPairStillAnchorsAsAtomic (2) | the tightened Atomic anchor (hex-prefix + exact 36-char dashed UUID shape) correctly rejects a look-alike non-Atomic path while still anchoring a genuine Atomic pair | loosen the anchor back to the old 3-char-prefix-match bug, false-anchoring a non-Atomic path | DISCRIMINATING (×2) |
| ShadowFreezePaths / DetachedPathsReportTheSharedDetachedComponent / MovingPathsReportTheSharedMovingComponent / MovingPathsNonAtomicFoldIntoTheTableNamespace / DetachedPathsNonAtomicFoldIntoTheTableNamespace / DetachedNamedTableIsKnownAmbiguityFoldedAsReservedDir (6) | shadow/detached/moving reserved-dir components parse correctly for both Atomic and non-Atomic layouts, folding into the TABLE's own namespace (not a spurious inner-part-anchored namespace); a known table-named-"detached" ambiguity is pinned as a deliberate accepted limitation | let detached/moving fold into a spurious inner-anchored namespace (the exact bug class the anchor prevents), or silently change the accepted ambiguity behavior | DISCRIMINATING (×6) |
| RawPathSplitMemoizedAcrossClassifiers / SplitCacheEvictionStaysCorrect (2) | the raw-path split is memoized (exactly one split per distinct path across repeated classifier calls), and remains CORRECT after the fixed-capacity FIFO ring evicts a slot (forced re-split yields the identical right result) | let the memo double-split a repeated path, or let eviction corrupt a re-parsed result | DISCRIMINATING (×2) |
| SupportsAtomicFileWrites | the CA metadata storage reports atomic file writes; a plain (non-CA) storage keeps the base-class `false` | let the CA storage report `false`, or a plain storage report `true` | DISCRIMINATING |
| ResolvesPublishedPart | a real published part resolves correctly across existence/size/directory-listing/emptiness/storage-objects/inline-bytes/last-modified -- the full read-side contract in one pass, including that Inline entries report a sized zero-remote-path placeholder and the publish stamp never surfaces as a dir entry | break any facet of the read-side contract for a real published part | DISCRIMINATING |
| BlobViewPlanRidesTheStandardPipeline | an in-manifest read serves from memory (no blob plan); a blob-backed file's view plan rides the STANDARD `ReadPipeline` (Source->Gather->FileView, verified by exact `describe()` string), with correct right-bounded reads and mid-window seeks | let a blob read bypass the standard pipeline, or corrupt bounded-read/seek behavior | DISCRIMINATING |
| DeletedBlobUnderStaleViewFailsTypedNotEmpty | a retained view whose blob GC has since removed still plans the read (no I/O at plan time, size from the manifest) but the actual read throws a TYPED exception (`FILE_DOESNT_EXIST`) at the first byte -- never a silent empty payload | let a deleted-blob read return empty bytes instead of throwing | DISCRIMINATING |
| ProjectionDirectory | a projection subdirectory's existence/listing/emptiness/file-read all resolve correctly | break projection subdirectory resolution | DISCRIMINATING |
| DetachedFoldedIntoTableNamespace / DetachedFoldsIntoTableNamespaceWithPrefixedRef (2) | a detached part is a `detached/`-prefixed ref INSIDE the table's own namespace (never a sibling namespace), collapsing correctly in table-dir listing and never colliding with a live part of the same base name | let detached parts leak into a separate namespace or collide with a live part | DISCRIMINATING (×2) |
| MovingFoldsOntoAPrefixedStagingRef | a moved part resolves onto a `moving/`-prefixed STAGING ref (not the final live ref directly), preserving move crash-atomicity | let a moved part publish directly under its final live ref, breaking move crash-atomicity | DISCRIMINATING |
| ShadowFreezeTree | shadow/backup namespace intermediate dirs and part trees resolve correctly, strictly scoped to their own registered shadow namespace | let a shadow namespace leak into or be confused with another backup's namespace | DISCRIMINATING |
| VerbatimNamespaceFiles | table-level namespace files (format_version.txt, reserved subdirs) merge correctly into table-dir listing alongside part names; loose disk-root files are plain mountpoint objects, distinct from namespace files | conflate a namespace file with a loose mountpoint object, or fail to merge them in listing | DISCRIMINATING |
| TableRootExistsWithNamespaceFilesButNoCommittedRef / TableRootExistsWithNamespaceFilesButNoCommittedRefNonAtomic / EmptyCatalogedLiveTableRootExists (3) | a table with ONLY namespace files (no committed part), and a cataloged-but-otherwise-empty Live table, both report `existsDirectory=true` -- the exact false-negative that used to leak `Live` catalog rows past `DROP TABLE` forever | let a files-only or catalog-only table report absent, leaking a permanent catalog row | DISCRIMINATING (×3) |
| DirShapeDispatchOrderIsStable | the fixed directory-shape classification order correctly disambiguates 9 distinct shapes including two genuinely ambiguous cases (AtomicShard vs non-Atomic fallback; ShadowTable vs ordinary TableDir) | reorder the dispatch, misclassifying an ambiguous shape | DISCRIMINATING |
| LocalStagingRemainsDefault | local staging (not S3) is the default backend, and a local-staged write creates ZERO objects under the S3 staging prefix | let local staging accidentally leave objects under the S3 staging area, or flip the default backend | DISCRIMINATING |
## src/Disks/tests/gtest_ca_wiring.cpp (79 tests, part 2 of N) {#src-disks-tests-gtest-ca-wiring-cpp-79-tests-part-2-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| ContentRoundTripThroughTransaction | a multi-file content write through a real transaction is invisible before commit and fully correct (existence/size/inline-bytes/listing/last-modified) after | let uncommitted content leak visible, or corrupt any post-commit facet | DISCRIMINATING |
| InlineOnlyPartPublishesWithoutBuildCrash | CRASH-CA-S3 regression: a part whose files are ALL inline (no blob-forcing file) commits successfully instead of hitting a `LOGICAL_ERROR` server crash | reintroduce the missing-`PartWriteTxn` crash for all-inline parts | DISCRIMINATING |
| IdenticalContentDedupsToOneBlob | two unrelated parts with byte-identical content dedup to the SAME blob object (content-addressed key) | let identical content mint two distinct blob objects | DISCRIMINATING |
| UncommittedTransactionPublishesNothing | a destroyed-without-commit transaction leaves no durable trace | let an abandoned transaction leak durable content | DISCRIMINATING |
| MutableOnlyUpdateOnCommittedPart | the MVCC one-shot rewrite of ONLY a mutable file on a committed part goes through the lightweight `updateRefPublishedAt` path (no tree rebuild) while the rest of the tree stays untouched | let a mutable-only update rebuild or corrupt the whole tree | DISCRIMINATING |
| VerbatimFilesDurableOnFinalizeAndAppendable | table-level verbatim files (autocommit contract) are durable on FINALIZE with no explicit commit, and Append mode correctly does read-modify-rewrite | let a verbatim file require commit, or let Append corrupt/overwrite instead of appending | DISCRIMINATING |
| HardLinkCarriesForwardWithoutReupload | a hardlinked file in a new part shares the SAME physical object as its source (no re-upload), and inline bytes are carried correctly | let a hardlink re-upload the content instead of sharing the object | DISCRIMINATING |
| TmpToFinalRenamePublishesUnderFinalName / CommittedPartRenameMovesTheRef / ProjectionTmpRenameRekeysStagedEntries (3) | a tmp->final rename, a committed-part rename (MergeTree's delete_tmp_ shape), and a projection tmp->final rename all correctly re-key/move the ref and its staged entries | let a rename leave stale entries at the old path or lose entries at the new path | DISCRIMINATING (×3) |
| DetachAttachRoundTrip | DETACH (move to detached namespace) then ATTACH (stage-rename within detached, then publish back to live) round-trips correctly through pure ref ops, ending with an empty detached listing | let a detach/attach cycle lose the part or leave detached-namespace debris | DISCRIMINATING |
| RemovalsDropRefsAndNamespaces | the fast-removal shape (unlink marks superseded by removeDirectory, one ref-drop) and DROP TABLE (removeRecursive dropping live+detached+namespace-files together) both work correctly | let unlink marks leak past a superseding removeDirectory, or let DROP TABLE leave namespace-file debris | DISCRIMINATING |
| LastRefRemovalIsNotNamespaceRemoval | removing a table's ONLY part does NOT trigger namespace removal (the root stays present and a fresh part still publishes), since that call alone cannot distinguish a merge/TTL/TRUNCATE from a real DROP TABLE | let last-part removal be mistaken for DROP TABLE admission, breaking merge/TTL/TRUNCATE | DISCRIMINATING |
| FilesOnlyTableRootRemovalIsImmediatelyAbsentWithoutGc | a files-only table's `removeRecursive` makes it logically absent IMMEDIATELY (synchronously), with no GC round required -- `DROP TABLE ... SYNC` must not depend on GC latency | let a files-only table removal require a GC round before becoming absent | DISCRIMINATING |
| VerbatimMoveAndUnlink / UnlinkHonorsIfExistsForPartFiles (2) | a verbatim move+unlink sequence works correctly; `unlinkFile`'s `if_exists` flag is honored precisely (missing without if_exists throws, with if_exists is a no-op, existing file is removed either way) | let unlink ignore `if_exists`, throwing or silently succeeding at the wrong times | DISCRIMINATING (×2) |
| TableRenameMovesRefsFilesAndDetached | RENAME TABLE moves ALL of: live refs, verbatim namespace files, AND detached parts, atomically to the new table namespace | let a table rename leave any of the 3 classes (refs/files/detached) behind at the old namespace | DISCRIMINATING |
| TableRenameIsIdempotentOnRedrive / VerbatimMoveIsIdempotentOnRedrive (2) | a re-driven (already-completed) table rename, and a re-driven verbatim file move, are both safe clean no-ops (source already gone, destination present); a genuinely missing source still throws | let a re-drive throw `FILE_DOESNT_EXIST` on an already-completed move, breaking partial-failure recovery | DISCRIMINATING (×2) |
| MoveDirectoryMutableCollisionPolicy (+ death twin) | `moveDirectory`'s staged-merge is source-wins; a genuine collision (same mutable file staged under BOTH source and destination with DIFFERING bytes) fails loud (`LOGICAL_ERROR`/abort), while IDENTICAL bytes are a benign idempotent re-key (no throw) -- and the differing-bytes check now covers EVERY entry, not just legacy mutable-file names | let a differing-bytes collision silently drop one side's write, or let the collision check miss non-legacy entries | DISCRIMINATING (×2, release/debug twins) |
## src/Disks/tests/gtest_ca_wiring.cpp (79 tests, part 3 of N) {#src-disks-tests-gtest-ca-wiring-cpp-79-tests-part-3-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| MoveDirectoryOntoExistingDestinationBuildSurvives | D3 review: the (dest-has-build, src-has-no-build) staged-merge combination is a pure no-op on the destination's build -- proven POSITIVELY via a real `CasEvent` sink asserting NO `BuildAbort` fires (not merely "assertions pass either way"), and the destination's own content still publishes correctly at commit | let this merge combination fall through to the wrong branch and call `abandon()` on a null/live build | DISCRIMINATING |
| FreezeViaHardLinksIntoShadow | FREEZE (hardlink clone into shadow tree) and UNFREEZE (removeRecursive of the backup root) work correctly, dropping every shadow namespace under the backup | let FREEZE/UNFREEZE leak shadow-namespace debris or fail to clone correctly | DISCRIMINATING |
| StagedFilesVisibleBeforeCommit | in-flight (pre-commit) reads correctly serve a pending blob from its local temp file and staged mutable bytes from memory, with correct sizes, and the directory overlay answers ONLY for inner dirs (never the part dir itself, per the PoC contract) | let an in-flight read serve wrong bytes, or let the directory overlay report the part dir itself as present | DISCRIMINATING |
| DroppedPartIsReclaimedByRounds | a dropped part is fully reclaimed by real GC rounds (manifest gone, no relink offer for it), and a fresh identical re-write reuses the SAME blob key | let a dropped part fail to reclaim, or let a fresh re-write mint a different key for identical content | DISCRIMINATING |
| DisplacedTreeBlobsReclaimedThroughRealPath | B199: a real MergeTree displacement (drop old part, publish new part with distinct content over the same path) reclaims the OLD part's unique blobs through the REAL GC scheduler (not just fsck recognition) while the new part's closure stays fully reachable | let the displaced (old) part's unique blobs leak forever, or let GC recognize-but-not-reclaim them | DISCRIMINATING |
| GetRelinkOfferReturnsBodyAndTokenForCommittedPart | the relink offer's manifest body decodes to the EXACT entries the part was published with, and the confirm token names the SAME pool/namespace/ref/manifest the body decoded to (the token's whole safety property) -- an absent part offers nothing | let the token name a different manifest than the body decoded to (the exact confusion the safety property forbids) | DISCRIMINATING |
| AdoptPartFromManifestPublishesFreshLocalManifest | the receiver adopts a relinked manifest by minting its OWN fresh local ManifestId (never sharing the sender's), landing in the RECEIVER's own namespace (ignoring the sender's namespace field), with ZERO blob re-upload (incarnation tokens verified unchanged) | let the receiver share the sender's manifest identity, land in the wrong namespace, or re-upload an already-shared blob | DISCRIMINATING |
| AdoptFailsClosedAndFallsBackOnCondemnedBlob | §4 manifest-trust: promote TRUSTS the committed-source's durable manifest edge with no per-file re-probe, so adopt SUCCEEDS even over a raced-absent pool blob (matching ordinary interserver trust); the deleted blob surfaces via fsck's dangling-reachable backstop, not an adopt-time abort | make adopt re-probe every leaf at promote time (contradicting the trust model), or let the deleted blob NOT surface in fsck | DISCRIMINATING |
## src/Disks/tests/gtest_ca_wiring.cpp (79 tests, part 4 of N) {#src-disks-tests-gtest-ca-wiring-cpp-79-tests-part-4-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| AdoptPartFromManifestSelfContainedWithoutMutableFilesSidecar | a relink with NO mutable_files sidecar parameter self-contains uuid.txt/metadata_version.txt as ordinary content entries, readable as such on the receiver | reintroduce the removed sidecar dependency, breaking self-contained adopt | DISCRIMINATING |
| PrepareAdoptIsDurableButPublishesNothingUntilPromote | `prepare` makes the receiver's `+1` DURABLE (a real precommit, visible via `livePrecommitsForTest`) while publishing ZERO committed ref -- the protocol's whole point: a later `yes` is worth something only against an already-durable claim, and an unproven source costs nothing | let prepare publish a committed ref, or fail to make the `+1` durable | DISCRIMINATING |
| AbortedPrepareReleasesThePrecommitAndPublishesNothing | `abort` appends the EXACT precommit removal (not merely dropping the transaction object), publishing nothing, and a second `abort` (scope-guard double-call) and destruction of an aborted handle are both safe no-ops | let an aborted prepare leak its precommit forever (the only thing standing between an unproven confirm and permanently retained blobs) | DISCRIMINATING |
| PrepareAdoptOfAnUndecodableManifestAllowsTheByteFallback | an undecodable manifest is a MECHANISM failure (not a source failure): `MechanismFallbackAllowed`, no handle returned, nothing staged | let an undecodable manifest stage a precommit with no handle to release it | DISCRIMINATING |
| AdoptIntoADetachedTargetPublishesADetachedRefAndNoLiveRef | a relink into a detached target publishes ONLY the `detached/`-prefixed ref (never a live ref of the same name, which would make an un-attached part visible), reads back correctly, and finalizes via an ordinary same-namespace ref repoint | let a detached-target relink publish a live ref, exposing an un-attached part | DISCRIMINATING |
| PrepareAdoptRefusesATargetThatIsNotAPartDirectory (+ death twin) | 3 non-part-directory targets (table dir, file-inside-a-part, shadow path) are all refused LOUDLY (`LOGICAL_ERROR`/abort), never `MechanismFallbackAllowed` -- a fallback would byte-fetch into the wrong place | let a malformed target silently fall back to byte-fetch instead of refusing loudly | DISCRIMINATING (×2, release/debug twins) |
| PartialCommitRollsBackPublishedParts | B122: a fault on the SECOND part's manifest-body write (after the FIRST part already fully published) rolls back the first part too -- all-or-nothing commit, proven via a real persistent (budget-exhausting) fault through the real CAS request controller | let a partial-commit failure leave an earlier-published part durably visible | DISCRIMINATING |
| ObserveOnlyOpenReadsButRejectsWrites | a read-only object storage over the same root opens successfully (never entering staged publication) and correctly serves reads while rejecting BOTH write paths (transaction creation AND relink adoption) with `READONLY` | let a read-only mount silently accept a write, or fail to open at all | DISCRIMINATING |
| UnsetPublishedAtMsReturnsEpoch | a ref published with an unset (zero) timestamp returns the epoch (never throws) for `getLastModified` | let an unset timestamp throw instead of returning the epoch | DISCRIMINATING |
## src/Disks/tests/gtest_ca_wiring.cpp (79 tests, part 5 of N) {#src-disks-tests-gtest-ca-wiring-cpp-79-tests-part-5-of-n}

| test | property | killing mutation | verdict |
|---|---|---|---|
| NoContentPoolOpBeforePrecommit | B188: EVERY pool op (HEAD via exists/getObjectMetadata, GET via readObject, PUT via writeObject) on THIS build's own content blobs comes strictly AFTER the first durable precommit write, for both the fresh-blob path and the cross-part pending-hardlink path -- verified against a real recorded object-storage op log, not a mock assertion | let an eager HEAD/GET/PUT on a not-yet-precommit-protected blob slip in before the precommit write (the exact B188 regression) | DISCRIMINATING |
| CommittedSourceAdoptNoHeadBeforePrecommit | the committed-source hardlink-adopt path (adoptFromTree->adoptEvidence, tokenless W-evidence) never HEADs/GETs/PUTs the FOREIGN adopted blob key before precommit, and never re-uploads it -- a targeted invariant the general own-content gate cannot catch since the blob is foreign | let committed-source adopt revert to a `reuseBlob`/`ensureBlobPresent` path that HEADs the adopted blob during staging | DISCRIMINATING |
| HardlinkOfPendingBlobCommitsAndReadsBack | a same-transaction cross-part hardlink of a PENDING (not-yet-uploaded) blob copies the pending record so BOTH parts read back identical content sharing the same underlying object, with nothing visible before commit | let a pending cross-part hardlink lose the content or fail to share the object | DISCRIMINATING |
| RepublishRefNoTreeHeadBeforePrecommit | B190-A: `republishRef` (committed-part rename) carries the source blob forward by TOKENLESS evidence, never touching the source BLOB key before the precommit write (legitimately reading the foreign MANIFEST is not a violation) | let republishRef revert to an eager HEAD/GET on the source blob before precommit | DISCRIMINATING |
| AdoptStagedBlobHelperUnifiesSixSites | B190-B: the unified `adoptStagedBlob` helper preserves COPY semantics for `createHardLink` and MOVE semantics for cross-part `moveFile` of pending blobs, with zero pre-precommit pool ops on own content -- proven for both semantics in one transaction | let the unification collapse COPY and MOVE into the same (wrong) semantics, or reintroduce a pre-precommit pool op | DISCRIMINATING |
## src/Disks/tests/gtest_ca_wiring.cpp (79 tests, part 6 of N -- final) {#src-disks-tests-gtest-ca-wiring-cpp-79-tests-part-6-of-n-final}

| test | property | killing mutation | verdict |
|---|---|---|---|
| OrphanedPendingBlobNotUploadedAfterUnlink / OrphanedPendingBlobNotUploadedAfterReplace (2) | B189: a pending blob whose tree entry is removed within the SAME transaction (by unlink or by replaceFile's dst-erase) is NEVER uploaded -- exactly one blob write recorded (the kept/replacement one), verified against a real object-storage op log that also correctly excludes the per-hash `.meta` sibling from the count | let an orphaned pending blob still get uploaded (a wasted PUT), or miscount the `.meta` sibling as a body upload | DISCRIMINATING (×2) |
| PromoteIgnoresCondemnedMaterializedBlobEdgeProtected | a blob condemned in the exact putBlob->promote race window is EDGE-PROTECTED: promote commits leaving the leaf's token UNCHANGED (no re-upload), because the precommit closure durably named the blob before the condemnation could ever graduate to a delete | let promote re-validate/republish a materialized leaf it doesn't need to, or fail to commit over a doomed condemnation | DISCRIMINATING |
| PromoteWithoutLivePrecommitAbortsWithoutResurrect | promote with NO live precommit binding aborts at the owner-liveness check (`NETWORK_ERROR`) strictly BEFORE any blob work -- a condemned leaf's token and meta state are provably UNCHANGED by the abort, proving no consequential blob publication happened | let promote's abort path still touch/republish a blob before failing | DISCRIMINATING |
| TryFromDiskOnLocalDiskIsExceptionFreeAndCountsNoError | `tryFromDisk` on a plain (non-CA) local disk is exception-free -- the `NOT_IMPLEMENTED` error counter is provably untouched, since a real per-tick throw-and-catch would pollute `system.errors` on every pure-local server | let `tryFromDisk` throw-and-catch internally, silently polluting the global error counter on every disk-metrics tick | DISCRIMINATING |

Summary for gtest_ca_wiring.cpp: 79 tests read in full across 6 parts, all DISCRIMINATING. This file (the ClickHouse-facing wiring/translation layer) is exceptionally rigorous: real B122/B188/B189/B190/B199 regression guards driven through recorded object-storage op logs, real GC-round reclamation proofs, real precommit-ordering invariants, and death-test twins for every LOGICAL_ERROR-aborting path.

## 12 ranked mutants (dynamic battery), by breadth of test dependency across the 69 assigned files {#12-ranked-mutants-dynamic-battery-by-breadth-of-test-dependency-across-the-69-as}

1. **Engine: settle-ambiguity-by-resolve-read, never reissue blind** (`CasRequests`'s write loop) -- change an ambiguous write's resolution to skip the exact resolve read and just reissue/guess. Kills: `gtest_cas_requests.cpp` (~15 tests: `AmbiguousCreateThatLandedIsCommittedByTheResolveRead`, `AmbiguousCreateThatNeverLandedIsReissued`, `AmbiguousReplace*` ×3, `EveryConflictIsSettledByOneReadAndCarriesTheOccupant`, ...), `gtest_cas_slot_occupy.cpp` (6 tests), `gtest_cas_ref_wedge_every_attempt.cpp` (most of its 29 tests), `gtest_cas_ref_writer.cpp`'s wedge section, `gtest_cas_ref_recovery_cas_walk.cpp`'s straggler-adoption tests.
2. **Engine: the three-point fence-admission check** (before first attempt / before each verb / after a proven post-write commit) -- drop any one of the three checkpoints. Kills: `gtest_cas_requests.cpp:AdmissionIsCheckedAtThreePoints`, `TheGateBeforeTheSleepEndsTheCallWithoutASecondWrite`, `LivenessPredicateEndsTheOperationLikeAFenceLoss`; `gtest_cas_slot_occupy.cpp:AdmissionLostAfterTheWriteIsNeverReportedCommitted`/`AdmissionLostAfterAConflictPreventsTheSettlingRead`; most `FenceLost`-asserting tests across `gtest_cas_ref_wedge_every_attempt.cpp` and `gtest_cas_ref_writer.cpp`.
3. **Engine: every-attempt wedge rule** (`GaveUp{sent_any, last_seen}` bookkeeping; resolve via bounded conditional CREATE, never a bare GET) -- resolve an ambiguous append via a plain read instead of a conditional create. Kills: the whole of `gtest_cas_ref_wedge_every_attempt.cpp` (29 tests), the wedge sections of `gtest_cas_ref_writer.cpp` and `gtest_cas_writer_duties.cpp`, `gtest_cas_retirement_sweep.cpp:AStragglerFromTheDyingEpochLosesItsCreateToTheRecoverySeal`.
4. **Engine: credential-refresh-once + retry classification** (`isDeterministicLocalFailure`, transport-timeout reissue, one-refresh-per-call) -- reissue a local bug, or refresh credentials more than once per call. Kills: 8+ tests in `gtest_cas_requests.cpp` (`AMalformedRequestIsRefusedWithoutAReissue` through `ANameOnlyAccessDenialOnAReadPropagatesWhenNoRefreshIsAvailable`), `ADeterministicLocalFailureSurfacesUnchangedWithoutAReissue`, `ATransportTimeoutIsReissuedAndALocalFailureIsNot`.
5. **Pool: in-band recovery seal at exactly `{E, T+1}`, with `prev_epoch_seal` chain link** -- mint the seal at the wrong id, or drop/leak the chain link. Kills: the whole of `gtest_cas_ref_recovery_cas_walk.cpp` (40 tests), `gtest_cas_ref_wedge_every_attempt.cpp`'s seal-rejection tests, `gtest_cas_retirement_sweep.cpp:AStragglerFromTheDyingEpochLosesItsCreateToTheRecoverySeal`, `gtest_cas_writer_duties.cpp:PendingDutySkipsCleanFarewellAndSuccessorSweepsTheCrashRemnant`.
6. **Pool: self-remount drops stale runtime/wedge and re-derives the fresh epoch** -- keep the stale cache or wedge across a remount. Kills: `gtest_cas_ref_writer.cpp`'s whole `CASRefWriterRemount` section (5 tests), `gtest_cas_pool.cpp`'s remount tests (not in this half but same engine surface).
7. **Pool: mandatory-HEAD blob-publication dispatch** (fresh->native-copy, clean->observe, condemned->fresh-tagged-stream-never-verbatim-copy) -- let a condemned republish verbatim-copy (reproducing the condemned incarnation's exact bytes, the INV-NO-RETURN data-loss hazard). Kills: `gtest_cas_upload_detached.cpp` (14 tests), `gtest_cas_upload_fanout.cpp` (17 tests), `gtest_cas_s3_staging.cpp`'s ETag-faithful and ordinary-staged-case tests.
8. **GC: `manifestDeletionPremise`** (unconsumed-epoch-seal / held-namespace / no-sealed-cursor / unconsumed-tail-removal-target all retain) -- let any one retain-reason be silently satisfied when it shouldn't be. Kills: the whole of `gtest_cas_sweep_deletion_premise.cpp` (12 tests) plus every GC-fold test elsewhere that depends on the sweep never deleting a still-protected manifest.
9. **GC/Ledger: `TxnApplyLedger` delta-consumption marking inside `foldDeltasIntoGeneration`** -- mark applied at run-flush instead of at consumption (silently misreporting a legitimate unmatched-removal round as lossy), or fail to mark a consumed delta's ordinal. Kills: the whole of `gtest_cas_txn_apply_ledger.cpp` (7 tests) and, per that file's own header comment, "every existing GC test now runs with the ledger armed and would throw if a delta went missing" -- i.e. this gates essentially every GC fold test in the assignment.
10. **GC: condemn->graduate->ack-floor->delete cascade reaches a real fixpoint** -- stall the cascade so a displaced/dropped subgraph never fully drains. Kills: `gtest_cas_truncate_reclaim.cpp` (3 tests, including the B140 regression guard), `gtest_ca_wiring.cpp:DisplacedTreeBlobsReclaimedThroughRealPath`/`DroppedPartIsReclaimedByRounds`.
11. **Ledger: `commitRefChunk`'s three-arm verdict** (Committed / NeedsRecovery-on-checkpoint-conflict / NeedsRecovery-on-fence-loss, with INV-4 body-before-checkpoint ordering) -- let an unfrontiered commit install anyway, or reorder body-PUT after checkpoint-CAS. Kills: `gtest_cas_ref_writer.cpp`'s `CASRefWriterAppendLane` section (10+ tests), `gtest_cas_ref_snapshot_publish_ordering.cpp` (5 tests), `gtest_cas_ref_wedge_every_attempt.cpp:DurableCreatedWedgeNeedsRecoveryWhenItsFrontierCannotBePublished`.
12. **Precommit-first ordering invariant (B188/B190)**: no HEAD/GET/PUT on a build's own (or an adopted foreign) content blob before the precommit's durable write -- let any adopt/republish/hardlink site touch a blob before precommit. Kills: `gtest_ca_wiring.cpp`'s whole `CASWiringPrecommitOrder`/`CASWiringPending` section (5 tests) plus `OrphanedPendingBlobNotUploadedAfterUnlink`/`...AfterReplace`.
