[{"cluster_id": "C-3549", "canonical_claim": "Review finding \u211622 is real: `gc_interval_sec=0` causes back-to-back GC rounds, while `gc_shards=0` makes `gc/state` unreadable and can trigger modulo-by-zero, permanently wedging GC. Additionally, The CAS review triage confirmed that zero-valued `gc_interval_sec` or `gc_shards` could cause a spin or permanent wedge; task T4 added bounds validation in commit `4fdbb3eaf11`. Additionally, `gc_interval_sec=0` can spin the scheduler and `gc_shards=0` can persist unreadable `gcs=0` state because bounds are not validated before opening the pool. Additionally, The GC configuration factory must reject `gc_interval_sec=0` and `gc_shards=0` before pool open, and `encodeGcState` must raise a runtime exception rather than rely on a release-inert `chassert`.; Unknown content-addressed disk settings are rejected; `gc_enabled` defaults to `true`, `gc_interval_sec` defaults to 60 seconds, and `gc_shards` defaults to 1. Additionally, `gc_interval_sec` and `gc_shards` must both be at least 1; zero values must fail with `BAD_ARGUMENTS` before opening a content-addressed pool. Additionally, `gc_enabled` defaults to `true`, and `gc_interval_sec` defaults to 60 seconds with a minimum value of 1 second.", "target": "configuration", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/ContentAddressedSettings.cpp:178-179~ContentAddressedSettings::validate and Formats/CasGcStateFormat.cpp:19~encodeGcState \u2014 current settings validation rejects zero `gc_interval_sec`/`gc_shards` with BAD_ARGUMENTS before pool open, and `encodeGcState` throws for zero `gc_shards`, matching the claim; the literal combined setting string and commit hash `4fdbb3eaf11` were not found but are not needed to confirm the behavior."}, {"cluster_id": "C-3550", "canonical_claim": "Review finding \u211631 is real: an explicit `staging_backend=s3` can silently fall back to local staging after a failed conditional-copy probe, unexpectedly consuming local disk and hiding a configuration mismatch. Additionally, Explicit `staging_backend=s3` can silently fall back to local staging after conditional-copy capability probing fails, hiding configuration errors and unexpectedly consuming local disk.", "target": "configuration", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/ContentAddressedMetadataStorage.cpp:820,827~ContentAddressedMetadataStorage::startup \u2014 the startup path probes conditional-copy capability for staging_backend=s3 and logs a fallback to local staging when the probe does not succeed, matching review finding #31."}, {"cluster_id": "C-3552", "canonical_claim": "The capacity contract must use independent predicates encoded_catalog <= catalog_object_cap and fold_fixed_bytes + sum(reservation) <= fold_object_cap, accepting equality and refusing cap plus one Additionally, The capacity tests must separately accept equality and refuse cap plus one for encoded catalog size and fold_fixed_bytes plus reservation sum, while keeping the other predicate slack Additionally, Each capacity predicate needs its own equality-acceptance and cap-plus-one refusal tests that assert which named predicate failed Additionally, The capacity fix independently tests equality acceptance and cap-plus-one refusal for each predicate while keeping the other predicate slack", "target": "configuration", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasRefCatalogFormat.cpp:286~checkCatalogObjectBytes, CasRefCatalogFormat.cpp:377~checkFoldSealReservation, gtest_cas_ref_catalog.cpp:597~Predicate1AcceptsEqualityRefusesCapPlusOne, gtest_cas_ref_catalog.cpp:615~Predicate2AcceptsEqualityRefusesOneEntryOver \u2014 two independent capacity predicates exist with equality-accepted/cap-plus-one-refused tests named per predicate; identifier names differ cosmetically (encoded_catalog_bytes/foldSealFixedBytes/foldSealCaps().object_cap vs the claim's encoded_catalog/fold_fixed_bytes/fold_object_cap, per not_found) but the substance matches."}, {"cluster_id": "C-3702", "canonical_claim": "The scenario scratch high-water sampler was not reliable at scale because its 2-second interval missed the `OPTIMIZE FINAL` spike; direct measurement found 93 GiB while the sampler reported 0.100 GiB.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scenarios/cards/s01_s02_huge_blob.py:84 configures `sampler_mod.MetricsSampler(..., interval_s=2.0, pool_every=1000, ...)` and lines 152-157 read `scratch_peaks = smp.peak_scratch_bytes()` as the 'scratch high-water' verdict. utils/ca-soak/scenarios/BACKLOG.md:748 records the direct measurement: '100 GiB part -> 93 GiB scratch (measured)' for the OPTIMIZE FINAL / write-path spill, corroborating that direct measurement caught a spike the periodic sampler could plausibly miss."}, {"cluster_id": "C-3704", "canonical_claim": "After the per-server-tree relocation, manifests live under `cas/manifests/<srid>/...` and refs under `cas/refs/<srid>/...`; the old classifiers placed the tree in `other`, causing S08 to report 858081 objects and 138 GB as `other` and allowing a real manifest leak to pass the leftover verdict. Additionally, The harness `pool_shape` classifier was stale after per-server-tree relocation: S08 classified 858081 objects and 138 GB as `other` while `_manifests=0` for a 100k-part pool.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scenarios/framework/observe.py:304~pool_shape and :93/:640~classify_pool_path, plus tests/test_classifiers.py:14~test_classifier, implement per-server-tree-aware classification of cas/manifests/<srid>/... and cas/refs/<srid>/... at HEAD, matching the claimed post-relocation classifier fix (H2)"}, {"cluster_id": "C-3705", "canonical_claim": "The harness `gc_log_rows` query used removed `forgotten_on_delete` and `forgotten_absent` columns, causing `UNKNOWN_IDENTIFIER` on every poll and making every GC verdict in the 2026-07-05 campaign vacuous. Additionally, The campaign harness `gc_log` query still referenced removed columns `forgotten_on_delete` and `forgotten_absent`, causing 213 `UNKNOWN_IDENTIFIER` errors per night and empty `gc_log` capture.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scenarios/framework/observe.py:365/388/389~gc_log_rows and :460~gc_log_all implement the gc_log query at HEAD without erroring, consistent with the H1 column-removal harness bug having been fixed"}, {"cluster_id": "C-3706", "canonical_claim": "Before scoped and partial fsck, the scenario campaign lost five verdicts because fsck timed out at large object counts and returned no accumulated result.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/soak/fsck.py implements exactly the described fix: `PARTIAL_MARGIN_S` constant, `FsckTimeout` exception documented as '(an O(pool) scan crawling under load \u2014 B146/B154)', and `run_fsck`/`run_dryrun` build a `--timeout {scan_budget} --partial` command line (fsck.py:207) so `cas-fsck` prints partial accumulated counts before the deadline instead of returning nothing."}, {"cluster_id": "C-3709", "canonical_claim": "Before `ca-inspect`, diagnosing a CA bucket object required an ephemeral `mc` container, an `od` hexdump, and hand parsing of protobuf or custom binary data even though decoders already existed.", "target": "operations/debugging", "current_evidence": "programs/disks/CommandCaInspect.cpp:28~CommandCaInspect::CommandCaInspect implements the ca-inspect disks command at HEAD, matching the claim that a dedicated introspection tool now exists (INTROSPECTION-2)"}, {"cluster_id": "C-3712", "canonical_claim": "The S31 dry-run tool inspects only shard 0, so its result is a known tool-only gap rather than a CAS product verdict.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scenarios/BACKLOG.md documents this three times as a tracked entry, e.g. line 642: '## S31-20260702T060328-1: cas-gc-dryrun previews only target shard 0; subset-oracle blind to shard>=1 under ...', repeated at lines 1991 and 2165. utils/ca-soak/scenarios/cards/s28_s33_corner.py:598 has the corroborating code comment: '# preview < cumulative is EXPECTED (2026-07-18 S31 RCA; the old \"previews only shard 0\" ...)'."}, {"cluster_id": "C-3716", "canonical_claim": "The ASan battery scripts `build/asan_battery.sh` and `build/asan_battery2.sh` misidentified aborting tests because plain `grep` treated round logs containing embedded NUL bytes from CAS payloads as binary files. Additionally, The ASan battery's culprit-detection scripts used plain `grep` (not `grep -a`) on archived round logs containing thousands of embedded NUL bytes from CAS blob payloads in stdout, causing silent misattribution: rounds 27-66 (39 consecutive rounds) were all the same unfixed `CasRefSnapshotFormat.RejectsSnapshotIdBelowSealedFrom` abort, never actually excluded, so the peeling loop looped on one bug while accumulating ~30 wrong bystander test names in the known-debt list; the fix switched the scripts to `grep -a` and the exclusion list was corrected from 58 down to 28 verified real culprits. Additionally, Because of the binary-log parsing bug, `CasRefSnapshotFormat.RejectsSnapshotIdBelowSealedFrom` aborted rounds 27 through 66, a 39-round loop in which the battery accumulated false bystander exclusions instead of converging. Additionally, The ASan culprit detector must use `grep -a` for round logs, and the corrected `build/asan_battery_aborted.txt` contains 28 verified real culprits rather than the previous 58-name list; the bogus list is preserved as `build/asan_battery_aborted.txt.bak_bogus`.; Completed unfiltered `unit_tests_dbms` logs can contain NUL bytes and omit the beginning of redirected stdout; use `grep -a`, keep completion markers in a separate small file, and rely on the surviving gtest summary for totals.", "target": "operations/debugging", "current_evidence": "build/asan_battery.sh:23 and build/asan_battery2.sh:18 both use `grep -a '^\\[ RUN      \\]' \"$LOG\" | tail -1 | awk '{print $4}'` (the -a fix is present). build/asan_battery_aborted.txt.bak_bogus contains 58 test names (matching the claimed old inflated list, `wc -l` = 58) and includes `CasRefSnapshotFormat.RejectsSnapshotIdBelowSealedFrom` repeated as the misattributed culprit across many entries in tmp/asan_true_culprits.tsv (rows 27-37+). That test exists at src/Disks/tests/gtest_cas_ref_snapshot_format.cpp and was converted to EXPECT_DEATH per tmp/logfix_batch2_prompt.txt, consistent with it being a genuine LOGICAL_ERROR-abort culprit that plain grep (NUL-byte-blind) misattributed across rounds."}, {"cluster_id": "C-3719", "canonical_claim": "`quiesce_cluster` falsely marked S08 inconclusive because it failed immediately on any transient non-empty `replication_queue.last_exception` instead of applying the sibling backlog-stall grace period; commit `35faaae182c` added the grace period and two regression tests.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scenarios/framework/lifecycle.py:89~quiesce_cluster; utils/ca-soak/scenarios/tests/test_framework.py:149~test_quiesce_cluster_tolerates_transient_errored_entry \u2014 grace-period logic and its regression test are present, backed by S06/S08/S39 card usage."}, {"cluster_id": "C-3720", "canonical_claim": "Seven ASan gtest findings were test-lifetime bugs rather than product bugs: six stack-use-after-scope cases came from an event-sink vector declared after `Pool`, and `CasPoolShutdown.UnresolvedWedgeSkipsFarewell` had a heap-use-after-free from reading `store->layout()` after `store.reset()`. Additionally, An ASan battery run found 7 genuine memory-safety bugs among 49 peeled aborts: 6 stack-use-after-scope instances in `CasSweepLateLog`-family tests, root-caused to an event-sink lambda capturing a local `events` vector declared AFTER the `Pool` object, so `~Pool` emits terminate events into the already-destroyed vector (fixed by declaring the vector before the Pool); and 1 heap-use-after-free in `CasPoolShutdown.UnresolvedWedgeSkipsFarewell`, root-caused to a `const Layout &` held across `store.reset()` (fixed by taking it by value).", "target": "operations/debugging", "current_evidence": "src/Disks/tests/gtest_cas_pool.cpp:1612,1613,1629~TEST(CASPoolShutdown, UnresolvedWedgeSkipsFarewell) \u2014 layout captured/copied before store.reset(), matching the claimed heap-use-after-free fix; the CasSweepLateLog stack-use-after-scope naming itself was not_found, so only the second sub-claim is directly evidenced."}, {"cluster_id": "C-3721", "canonical_claim": "The CAS gtest battery's established `Cas*:CA*:...` filter had never matched about 89\u201390 tests in the `CaWiring*`, `CaTransaction*`, `CaDedupCache*`, `CaInlinePlacement*`, and `CaPartPathParser*` suites; correcting the filter exposed three bugs in `gtest_ca_wiring.cpp`. Additionally, The obvious CAS gtest filter `Cas*:CA*` under-selects entire families such as `RefWriter*` and `CaWiring*`; the release gate must enumerate test families explicitly. Additionally, The CAS gate filter `Cas*:CA*` omitted `CaWiring`, `CaLifecycle`, several `RefWriter*` suites, and `RefTableCacheEviction`; twenty suites, including 17 CAS-relevant suites, were unclaimed by the gate.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scenarios/BACKLOG.md:1433~GTEST-CAWIRING-3-PREEXISTING plus src/Disks/tests/gtest_ca_wiring.cpp, gtest_cas_parallel_commit.cpp:36~CaWiringFixture, gtest_cas_ref_carve.cpp:44~CASRefWriterCarve, gtest_cas_ref_writer.cpp:1989~CASRefTableCacheEviction \u2014 CaWiring/RefWriter*/RefTableCacheEviction families exist and are tracked as previously under-selected; the specific not_found filter strings (Cas*:CA*:..., CaTransaction*, CaDedupCache*, CaInlinePlacement*, CaPartPathParser*) are not corroborated as exact suite names."}, {"cluster_id": "C-3722", "canonical_claim": "Constructing `ReadBufferFromMemoryFileBase` with `owns_memory=true` and empty data can pass `nullptr` from `std::string_view::data()` to `memcpy` with length zero, violating the nonnull contract under UBSan. Additionally, The empty-file `ReadBufferFromMemoryFileBase` fix is to skip `memcpy` when `data.empty()`; no bytes need copying and the owned buffer must still construct at EOF.", "target": "operations/debugging", "current_evidence": "src/IO/ReadBufferFromMemory.cpp:65,70,78~ReadBufferFromMemoryFileBase.ReadBufferFromMemoryFileBase and src/IO/tests/gtest_read_buffer_from_memory.cpp:16~EmptyOwnedBufferConstructsWithoutUB \u2014 guarded memcpy plus a dedicated regression test confirm the empty-data UB fix at HEAD."}, {"cluster_id": "C-3724", "canonical_claim": "The `DispatchThrowStillDrains` test was racy because it could cancel the first scheduled task before task entry; a latch must prove that already-running work is drained before dispatch unwinds.", "target": "operations/debugging", "current_evidence": "src/Disks/tests/gtest_cas_upload_fanout.cpp:758,780~CASUploadFanout.DispatchThrowStillDrains \u2014 the named test and its first-task-running synchronization (the latch fix) are present at HEAD."}, {"cluster_id": "C-3729", "canonical_claim": "The T15 trace dump SELECT aliased `'${tt}' AS trace_type`, and ClickHouse alias resolution in `WHERE` caused the literal to shadow the table column, so the dump was an unfiltered mix of trace types until fix `52f110e94b3`.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scripts/predown_dump.sh:101~predown_dump trace query \u2014 current query uses dumped_trace_type for the selected label and filters the real trace_type column, consistent with the described alias-shadowing bug having been fixed; the old literal-alias SQL and hash 52f110e94b3 are not_found (expected, since they predate the fix)."}, {"cluster_id": "C-3737", "canonical_claim": "The CAS suite gate initially missed `ContentAddressedSettings`, a six-test CAS suite whose file `gtest_content_addressed_settings.cpp` matched neither `gtest_ca*.cpp` nor `gtest_cas*.cpp`; those six tests had never been gated.", "target": "operations/debugging", "current_evidence": "src/Disks/tests/gtest_cas_settings.cpp:38~CASContentAddressedSettings.DefaultsAndOverridesLand \u2014 the six-test settings suite lives in gtest_cas_settings.cpp, matching gtest_cas*.cpp (not the claimed non-matching gtest_content_addressed_settings.cpp filename), consistent with the gate having been corrected to include it."}, {"cluster_id": "C-3743", "canonical_claim": "The first CAS suite enumeration missed a multiline INSTANTIATE_TEST_SUITE_P prefix, but the generator caught CasWinnerShape/CASGcCompletedRemovalFenceRace; the invariant must therefore be checked against the built binary rather than only line-based source enumeration.", "target": "operations/debugging", "current_evidence": "src/Disks/tests/gtest_cas_gc_frontier_gate.cpp:3002-3004~CASWinnerShape.CASGCCompletedRemovalFenceRace \u2014 the parameterized suite/test is present and enumerable at HEAD, consistent with the multiline INSTANTIATE_TEST_SUITE_P case now being caught."}, {"cluster_id": "C-3744", "canonical_claim": "`utils/ca-soak/scenarios/tests/test_render_tuned_config.py` was a red test because it still expected `<metadata_type>content_addressed</metadata_type>` after the setting value changed to `cas`; the assertion was corrected to `cas`.", "target": "operations/debugging", "current_evidence": "utils/ca-soak/scenarios/tests/test_render_tuned_config.py:3,20 \u2014 notes confirm the test currently asserts <metadata_type>cas</metadata_type>; the old <metadata_type>content_addressed</metadata_type> is not_found, consistent with the correction having landed."}, {"cluster_id": "C-3747", "canonical_claim": "Passing `*before.token` to `casPut` unnecessarily dereferenced an optional before wrapping it again; passing `before.token` directly removes the UB risk without changing the call contract.", "target": "operations/debugging", "current_evidence": "src/Disks/tests/gtest_cas_ref_recovery_cas_walk.cpp:206~PutHookBackend.casPut \u2014 before.token is used directly (not dereferenced); not_found: *before.token \u2014 the old dereferenced form is absent, consistent with the fix having landed."}, {"cluster_id": "C-3748", "canonical_claim": "The tidy finisher found two broken `NOLINTNEXTLINE` placements and one missed `readability-make-member-function-const` diagnostic; all three were corrected before the final verification gate.", "target": "operations/debugging", "current_evidence": "src/Disks/tests/gtest_cas_part_folder_access.cpp:663 and gtest_cas_ref_statemachine.cpp:1074 \u2014 NOLINTNEXTLINE placements are present in CAS test files as claimed; the readability-make-member-function-const evidence (src/IO/ChainedBuffers.h:58) is on unrelated code per notes, so that specific sub-claim is weakly supported."}, {"cluster_id": "C-3749", "canonical_claim": "An fsck catalog-resolution ambiguity must be recorded as a finding rather than aborting the entire audit, and stateless golden `05020` must reflect the new report columns.", "target": "operations/debugging", "current_evidence": "tests/queries/0_stateless/05020_cas_fsck.reference line 3 shows the SYSTEM CAS FSCK header row includes `unchecked` and `lifeless_keys` columns among the report columns, and programs/disks/CommandFsck.cpp documents `unchecked` as a non-fatal finding ('the audit could not PROVE those namespaces either way ... a statement about coverage', lines 89-97), i.e. an ambiguity is recorded as a finding rather than aborting the audit."}, {"cluster_id": "C-3751", "canonical_claim": "CI needed 6-hour-job-timeout sharding after a TSan lane finished 10,990/10,990 tests but was killed 16 seconds before it could report the result.", "target": "operations/debugging", "current_evidence": "Commit 53e8164f519a8b5324d4ecbf1672e52153a51156 ('ci: shard the content-addressed s3 sanitizer stateless lanes') states verbatim: 'In run 30203355812 (sha e2d04bfe37e): tsan finished all 10990/10990 tests at 5h59m and was killed 16s later during teardown'. It modifies ci/defs/job_configs.py (which exists at HEAD) to shard the CA-s3 sanitizer lanes to fit the 6h GitHub job timeout."}, {"cluster_id": "C-3752", "canonical_claim": "A CI fast-test build broke because `dbms` linked the dead `ch_contrib::crc32c` target left over from a phase-1a change, and the fast-test job does not initialize that submodule; the fix dropped the link so crc32c stayed google-cloud-cpp-only.", "target": "operations/debugging", "current_evidence": "Commit 4212f275e92bf2705505376881869354af386c5b ('build: drop the dead ch_contrib::crc32c dependency of dbms') fixes exactly this: 'Fast test fails at cmake generation: Target \"dbms\" links to ch_contrib::crc32c but the target was not found ... fast-test job initializes a limited submodule list that does not include contrib/crc32c ... The dependency is dead: it was wired in for per-block CRC32C in the early CAS run-file format (5f1272cc8a7)... Restore the pre-CAS state: crc32c is built only for google-cloud-cpp, and dbms does not link it.' The change touched src/CMakeLists.txt and contrib/CMakeLists.txt, both present at HEAD."}, {"cluster_id": "C-3754", "canonical_claim": "B207 was a fsck phantom-dangle finding across a ref-walk/HEAD-confirm window.", "target": "operations/debugging", "current_evidence": "Commit 94970514116066822d868b1c4d8cfdca90b7bab4 ('fix(cas): fsck re-resolves refs at HEAD-absent to kill phantom-dangling (B207)') states: '`runFsck`'s ref-walk (collecting `reachable_blobs`/`blob_labels`) and its HEAD-confirm run minutes apart with no snapshot between them ... made the stale walk look like a genuine dangle (a \"phantom dangling\")'. `runFsck` is implemented at src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Tools/CasFsck.cpp (line 1127)."}, {"cluster_id": "C-3755", "canonical_claim": "The `ca-fsck` summary previously omitted `corrupted_runs` from its rendered summary line and exit code even though it was counted and part of `clean()`, so a failing seal checksum was invisible twice over; the summary line was moved into `Cas::formatFsckSummary` (reachable from a unit test) with `corrupted_runs` added.", "target": "operations/debugging", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Tools/CasFsck.cpp:1162~formatFsckSummary \u2014 emits corrupted_runs and CommandFsck.cpp:72~CommandFsck::executeImpl uses the summary before its exit checks; gtest_cas_fsck.cpp:1556~CasFsckSummary exercises it."}, {"cluster_id": "C-3948", "canonical_claim": "The required Stage B tests include old-file hiding across drop and rebirth, stale readers and delayed writers after rebirth, resumed stale cleanup, zero catalog requests on hot paths, unchanged rewrite/append/remove/dedup-rotation request counts, LIST-independent recovery, missing and conflicting `_ckpt.life_epoch`, rejection of legacy keys, and compile-time absence of namespace-only key overloads.", "target": "operations/debugging", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.h:717 '_ckpt.life_epoch' (Stage B Task 4-C comment) and src/Disks/tests/gtest_cas_ref_ckpt.cpp / gtest_cas_gc_frontier_gate.cpp / gtest_cas_fsck.cpp cover checkpoint publication, frontier gating, and FSCK recovery respectively, matching the enumerated Stage B test surface (old-file hiding, stale readers, missing/conflicting _ckpt.life_epoch, LIST-independent recovery)."}, {"cluster_id": "C-3957", "canonical_claim": "The verification model must include one shared blob and pool-wide in-degree and retirement state, with controls for late +1, catalog transitions, forced REBUILD after a hold, classification-4 codec limits, stale reads, duplicate nomination, token ABA, and _ckpt-absent decommission finalization.", "target": "operations/debugging", "current_evidence": "Each element is implemented: classification-4 hold grammar in src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasFoldSealFormat.cpp (encoder refuses classification-4 row without a hold) and src/Disks/tests/gtest_cas_gc_hold_grammar.cpp; forced 'SYSTEM CAS GC REBUILD' after a hold in src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp (lines ~1789/2843/3301/4017/4250 'HOLDS RIDE THROUGH A REBUILD VERBATIM'); token ABA + duplicate nomination coverage in src/Disks/tests/gtest_cas_orphan_nomination.cpp (TEST(CASOrphanNomination, TokenAbaIsRetainedAndSurfaced)); _ckpt-absent decommission finalization in src/Disks/tests/gtest_cas_decommission_catalog_duties.cpp (RootNamespace(\"victim/db/missing_ckpt\"))."}, {"cluster_id": "C-3958", "canonical_claim": "Verification must force missing prior seals, disappearing held witnesses, an incarnation-dropping key builder, and an old-generation wedge result after successor seal adoption; recovery and wedge installs need a generation recheck under the install lock.", "target": "operations/debugging", "current_evidence": "src/Disks/tests/gtest_cas_ref_wedge_every_attempt.cpp implements exactly this: 'the ADMISSION FENCE: a wedge carries the mount-fence generation it was admitted under, every retry is gated on THAT generation ... every result is re-checked' (line 45-46), TEST(CASRefWedgeEveryAttempt, SuccessorSealAtTheWedgedKeyRejectsConclusivelyAndSourcesPrevEpochSeal) (line 742) covers successor-seal adoption, and 'the old-generation-retry-inert rule' (line 930) covers the old-generation wedge result."}, {"cluster_id": "C-4039", "canonical_claim": "The previously observed S01 memory bug was caused by `Build::putBlob` materializing a staged `BlobSource` into a `String`; streaming through `uploadFromSource` reduced a 2 GiB INSERT peak from 13 GiB to 4.33 GiB.", "target": "operations/monitoring", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasPartWriteTxn.cpp:159~PartWriteTxn::putBlob and :439~PartWriteTxn::uploadFromSource \u2014 the described String-materialization bug and streaming fix are present under the renamed owner PartWriteTxn::putBlob (not_found only the old 'Build::putBlob' name, consistent with the tracked Pool/PartWriteTxn rename)."}, {"cluster_id": "C-4045", "canonical_claim": "CAS retry-later failures were opaque because the message said `retry budget exhausted` even for a pre-attempt fence rejection, and the failures were logged at Information level without populating `system.replication_queue.last_exception`. The retry-later helper's message carries the precise cause, and changing the escaping error to non-exempt `NETWORK_ERROR` automatically populates `system.replication_queue.last_exception` and `last_exception_time` through existing merge handling.", "target": "operations/monitoring", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Backend/CasRequestControl.cpp:193~throwCasWriteRetryLater and src/Storages/System/StorageSystemReplicationQueue.cpp:57 \u2014 retry-later message carries precise cause and NETWORK_ERROR escaping populates last_exception/last_exception_time via existing merge handling."}, {"cluster_id": "C-4052", "canonical_claim": "Deferred GC rounds previously logged as `round 0` because the defer path returned before assigning `report.round`, making skipped rounds indistinguishable from empty folds.", "target": "operations/monitoring", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:671~Gc::runRegularRound assigns report.round in the defer path, asserted by gtest_cas_gc_round_defer.cpp:263/266~CASGCRoundDefer; 'round 0' appears only in the test's historical bug comment, confirming the fix is in place."}, {"cluster_id": "C-4058", "canonical_claim": "The mount-lease keeper can be silent at the default log level during a fence-to-remount episode, leaving renewal failure, fence arming, remount phases, and completion absent from the server log.", "target": "operations/monitoring", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasMountRuntime.cpp: `noteLeaseLost` (Live->TransientNotLive, the renewal-failure transition, lines 310-323) and `noteRemounted` (TransientNotLive->Live, the successful-remount completion, lines 325-336) contain no LOG_* call at all. Only the terminal `enterIdentityLost` (line 364) and `enterVanished` (line 416) transitions call LOG_WARNING. CasPool.cpp's fence-arming path (armMountFence call sites) likewise has no LOG_DEBUG/LOG_TRACE around the ordinary renew-fail -> fence-arm -> remount -> complete sequence -- only fallback/error branches (LOG_WARNING/LOG_INFO at lines 759, 783, 1072, 1110, 1118, 1187, and LOG_ERROR at 1454+) are logged."}, {"cluster_id": "C-4068", "canonical_claim": "Formatting an error with `fmt::format(...)` before passing it to `S3Exception` loses the static `message_format_string`, degrading grouping in `system.text_log` and `system.errors`. `copyS3File.cpp` should construct `S3Exception` with `PreformattedMessage::create(\"Message: {}, Key: ...\", ...)` so the static message format remains available for error grouping.", "target": "operations/monitoring", "current_evidence": "src/IO/S3/copyS3File.cpp:253-254~MultipartUpload::completeMultipartUpload already constructs `S3Exception` via `PreformattedMessage::create`; not_found: 'fmt::format(...)' confirms the criticized pattern is absent, i.e. the recommended fix is already in place"}, {"cluster_id": "C-4069", "canonical_claim": "The two new CAS system logs (content_addressed_log, content_addressed_garbage_collection_log) ship enabled-by-default, yet SystemLog.h documents content_addressed_log as \"off by default\" -- the comment and config.xml default disagree and need reconciling. The CA event log is on by default since `cbe0ffb7608`, and `system.content_addressed_garbage_collection_log` records per-round GC audit data. The `ContentAddressedLog` system-log description must reflect that its configuration is enabled by default for the experimental CA feature; the stale `SystemLog.h` off-by-default wording is corrected.", "target": "operations/monitoring", "current_evidence": "src/Interpreters/ContentAddressedLog.h:36~ContentAddressedLog description states the log is enabled by default per notes, reconciling the previously-claimed stale 'off by default' SystemLog.h wording; current names are `cas_log`/`cas_gc_log`"}, {"cluster_id": "C-4071", "canonical_claim": "During the 4h chaos soak, the detail=False summary fsck exceeded its 180 s bound at ~183 GB / 2.14M objects at the gc_checkpoint, so the harness's `dangling==0` safety gate was skipped (`fsck_status: skipped`) rather than failing \u2014 the quadratic discovery/LIST cost blinds the correctness oracle at this pool size (tracked as B146/B154).; A 4-hour chaos soak can become blind when detail-less fsck exceeds its 180-second bound at about 2.14 million objects; long-run validation also needs a TTL-band oracle that does not hard-fail during an active fault window and a pool-size or compaction limit.", "target": "operations/monitoring", "current_evidence": "utils/ca-soak/soak/run.py:595~run_checkpoint, utils/ca-soak/scripts/smoke_fsck.sh:6~smoke_fsck, utils/ca-soak/failure_t14.json:52~failure_record \u2014 per notes the fsck detail spelling, `dangling==0` gate text, and `fsck_status: skipped` value all occur in current soak code/artifacts, confirming the claim"}, {"cluster_id": "C-4075", "canonical_claim": "Risk register #18 (Low, confirmed): a single `SingleWriterSlot`/`MountLeaseKeeper` renew failure emits two separate `mount_conflict` audit rows (one classified, one a timeline marker), so a naive `count()` over the audit log over-reports conflict occurrences. A single mount-lease renewal failure emits two audit rows (a classified `mount_conflict` plus a separate `renew_failed` timeline marker), causing a naive count of `mount_conflict` events to over-report failures.", "target": "operations/monitoring", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasServerRoot.cpp:1060~MountLeaseKeeper::onRenewFailed plus CasEvent.cpp:62~Cas::toString confirm both the `mount_conflict` classified row and a separate renew-failed timeline marker are emitted on a single renewal failure"}, {"cluster_id": "C-4400", "canonical_claim": "CA product operability still required a `SYSTEM` control surface for starting and stopping GC, putting a pool read-only, and running checks; product-side GC stop existed only as a soak-harness workaround.", "target": "operations/monitoring", "current_evidence": "src/Parsers/ParserSystemQuery.cpp:246/462/494/504~ParserSystemQuery::parseImpl and src/Interpreters/InterpreterSystemQuery.cpp:1071~execute \u2014 SYSTEM surface for CAS GC run/rebuild/stop-start/drop-member exists; no not_found gaps."}, {"cluster_id": "C-4406", "canonical_claim": "The T8 healthy-pool soak should check zero `no usable checkpoint` anomalies in addition to zero `unproven` and zero frontier-probe budget, because a namespace with no usable checkpoint now suppresses through an anomaly rather than silently.", "target": "operations/monitoring", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:2359/2725/2745~Gc::fold logs 'no usable checkpoint' and documents unproven-frontier/probe-budget accounting, with gtest_cas_gc_frontier_gate.cpp:412~openPoolWithProbeBudget and gtest_cas_gc_bounded_walk.cpp:360 exercising the behavior; no not_found gaps."}, {"cluster_id": "C-4416", "canonical_claim": "When a blocked renewal resumes after GC fenced the expired slot, its token-guarded `putOverwrite` receives `PreconditionFailed` and the old code reports the misleading `touched by a foreign writer \u2014 failing closed, never re-minting` message.; The generic `SingleWriterSlot` message `touched by a foreign writer` is reserved for an own-UUID, same-epoch, unfenced token violation; GC-fence recovery must report `fenced by GC after lease expiry` instead.", "target": "operations/troubleshooting", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasServerRoot.cpp:1068~MountLeaseKeeper::onRenewMismatch and :1357~SingleWriterSlot::onRenewMismatch, with gtest_cas_heartbeat.cpp:369~KeeperAdoptRefusesFencedSelfWithTypedError \u2014 both the generic 'touched by a foreign writer' and GC-fence messages, plus PreconditionFailed/putOverwrite, are present; no not_found gaps."}, {"cluster_id": "C-4417", "canonical_claim": "A read-only `ca_ro` disk over the same pool and `server_root_id` as writable `ca` causes MergeTree part discovery to find parts on both disks and fail restart with `UNKNOWN_DISK` and `ASYNC_LOAD_WAIT_FAILED` when `ca_ro` is absent from policy `ca`.", "target": "operations/troubleshooting", "current_evidence": "src/Storages/MergeTree/MergeTreeData.cpp:2447~MergeTreeData::loadDataParts, src/Common/ErrorCodes.cpp:392~ERROR_CODES (UNKNOWN_DISK), and src/Common/AsyncLoader.cpp:473~AsyncLoader::wait (ASYNC_LOAD_WAIT_FAILED) confirm the discovery/restart-failure mechanism for a read-only ca_ro disk sharing server_root_id with writable ca; no not_found gaps."}, {"cluster_id": "C-4420", "canonical_claim": "`LocalObjectStorage.ListObjectsHandlesPathWithEmbeddedNul` can spin at 100% CPU because an embedded-NUL path can make the object walk loop without advancing; the issue is outside CAS and requires a bounded standalone reproduction followed by a fix or justified test retirement. Additionally, `LocalObjectStorage::listObjects` must reject an embedded-NUL path because an injected `\\0` in `icebergLocal(...)` could make `recursive_directory_iterator` loop indefinitely.", "target": "operations/troubleshooting", "current_evidence": "src/Disks/DiskObjectStorage/ObjectStorages/Local/LocalObjectStorage.cpp:433/464~LocalObjectStorage::listObjects \u2014 notes confirm listObjects now rejects embedded-NUL input before traversal, backed by regression tests gtest_local_object_storage.cpp:156/212"}, {"cluster_id": "C-4424", "canonical_claim": "`quiesce_cluster` previously failed immediately on a transient replication-queue `last_exception` during S08's 20,000-fetch workload; adding the existing `no_progress_grace_s` grace period made transient errors non-fatal while persistent errors still fail.; S08's end-checkpoint false-INCONCLUSIVE was caused by `quiesce_cluster`'s `drain()` raising instantly on the first sighting of any `replication_queue` entry with a non-empty `last_exception`, with no grace period (unlike its sibling backlog-stall check); fixed in `35faaae182c` by adding `error_since`/grace-period tolerance (`no_progress_grace_s`) plus two regression tests. Additionally, The S08 harness should either raise the CI-scale timeout above 900 seconds or implement real parallelism; its `clients` parameter only round-robins node selection and does not parallelize the create loop.", "target": "operations/troubleshooting", "current_evidence": "utils/ca-soak/scenarios/framework/lifecycle.py:89/129/131~quiesce_cluster.drain uses error_since/no_progress_grace_s tolerance for replication_queue last_exception, verified by tests utils/ca-soak/scenarios/tests/test_framework.py:149 (tolerates transient) and :173 (raises on persistent)"}, {"cluster_id": "C-4425", "canonical_claim": "The S36 and S37 placement failures at `ci` and `full` scale were caused by card parameters producing parts larger than the `max_data_part_size_bytes = 4194304` routing threshold on the `hot` volume of the `ca_local` and `ca_local3` policies; ClickHouse correctly routed those parts to `ca`. Additionally, S37 leg 4 failed because all six 15 MiB mixed parts landed on `ca`; the report measured `mixed_bytes_per_part = 15728640` and `route_threshold_bytes = 4194304`. Additionally, The `param_table` entries for `ci` and `full` in `cards/s36_s37_disk_move.py` were not threshold-aware: both S36 `rows_per_part*payload_bytes` and S37 `mixed_rows*mixed_payload_bytes` exceeded the 4 MiB routing line. Additionally, At `ci` scale, the S36 prefill part is 5.86 MiB and the S37 mixed part is 15.0 MiB, so both exceed the 4 MiB routing threshold and route to `ca`. Additionally, At `dev` scale, S36 generates a 0.29 MiB prefill part and S37 generates a 3.75 MiB mixed part, keeping both below the 4 MiB threshold and on local disks.", "target": "operations/troubleshooting", "current_evidence": "utils/ca-soak/configs/storage_conf_multidisk_ch1.xml:52-84 (ca_local/ca_local3 hot volumes) plus utils/ca-soak/scenarios/cards/s36_s37_disk_move.py:142-514 (S36/S37 param_table and run) implement the threshold-based routing described; specific SQL/constant assignment phrasing not_found verbatim but the mechanism is present"}, {"cluster_id": "C-4428", "canonical_claim": "A lazy `StorageTableProxy` caused `SYSTEM SYNC REPLICA` to misidentify a table as not replicated after chaos recovery; the proxy must be materialized for the check, and the lazy proxy itself took about 18 milliseconds to materialize in the reproduction.", "target": "operations/troubleshooting", "current_evidence": "src/Storages/StorageTableProxy.h:16~StorageTableProxy plus regression test tests/queries/0_stateless/05017_lazy_load_tables_sync_replica.sh:9/24 confirm the lazy-proxy SYSTEM SYNC REPLICA misidentification and its fix/test coverage"}, {"cluster_id": "C-4430", "canonical_claim": "`fsck` can read `gc/state` and then stream source-edge runs after a snap-prune retires that generation, producing `CORRUPTED_DATA` on an otherwise healthy pool.", "target": "operations/troubleshooting", "current_evidence": "programs/disks/CommandFsck.cpp:19 implements the `fsck` command; src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.h:769 and :778 document that fsck 'THROWS CORRUPTED_DATA on that refusal' and 'fails refMatchesBody / manifestNamespaceMatches throws CORRUPTED_DATA' precisely in the gc/state-then-source-edge-stream path; `gc/state` is referenced pervasively in the same file (e.g. line 37, 93) confirming fsck reads gc/state before streaming ref-log/source-edge runs."}, {"cluster_id": "C-4432", "canonical_claim": "The 4-hour soak harness initially classified the mount-fenced error `INVALID_STATE` code 668 as a hard failure even though its message says to retry after the disk recovers to Live; the classifier had recognized only `ABORTED` plus substrings.", "target": "operations/troubleshooting", "current_evidence": "utils/ca-soak/soak/cluster.py:212~mount-fenced classifier \u2014 notes confirm INVALID_STATE and ABORTED classifier references including retry-later handling for mount fencing are now present, matching the claimed fix"}, {"cluster_id": "C-4433", "canonical_claim": "The observed msan incident had a renewal PUT hang inside its retry loop, a silently expired `deadline_boot_ms`, and a later silent `setMountDeadline` re-arm without epoch bump or self-remount; this produced a roughly two-minute durable-write fence window with no transition logs.", "target": "operations/troubleshooting", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasMountRuntime.h:80 declares `std::atomic<uint64_t> deadline_boot_ms` and line 125 declares `void setMountDeadline(uint64_t deadline_boot_ms)`; CasMountRuntime.cpp:122 reads the deadline via `bootMsNow() < mount_fence.deadline_boot_ms.load(...)`, and line 246's comment ('a superseded or foreign renewal latches the fence and starts recovery') documents exactly the re-arm/expiry mechanics the claim describes as producing a silent durable-write fence window."}, {"cluster_id": "C-4437", "canonical_claim": "S18 may encounter the pre-existing `B3` freeze/shadow bug; the card records a freeze failure as inconclusive with a backlog anomaly instead of a hard failure.", "target": "operations/troubleshooting", "current_evidence": "utils/ca-soak/scenarios/ASSUMPTIONS.md:57~known live-build caveats and utils/ca-soak/scenarios/cards/s15_s18_shards_lifecycle.py:588~S18.run both use the exact B3 token, matching the claimed inconclusive-classification handling"}, {"cluster_id": "C-4444", "canonical_claim": "Without the `ReadBufferFromFileView` position fix, packed-file readers over remote-like buffers can return wrong rows without a checksum exception, including with `optimize_read_in_order`.", "target": "operations/troubleshooting", "current_evidence": "src/IO/ReadBufferFromFileView.cpp:13~ReadBufferFromFileView::ReadBufferFromFileView plus regression test src/IO/tests/gtest_read_buffer_from_file_view.cpp:203~ReadBufferFromFileViewTest.GranulePatternRegression implement and cover the position fix for packed-file readers."}, {"cluster_id": "C-4445", "canonical_claim": "Without `Expect: 100-continue`, some S3-compatible stores stream a doomed conditional PUT body, close the connection mid-upload, or return a retryable 500, causing retry storms and stalled merges or inserts.", "target": "operations/troubleshooting", "current_evidence": "src/IO/S3/PocoHTTPClient.cpp:630,669~PocoHTTPClient::sendRequest implement Expect: 100-continue handling; utils/ca-soak/configs/storage_conf.xml:21 configures it and tests/queries/0_stateless/03353_http_100_continue.sh:65~http_100_continue regression-tests it."}, {"cluster_id": "C-4448", "canonical_claim": "S37's single failing verdict (22/23) was diagnosed as the scenario card's own oracle bug rather than a product defect: the card expected 100 rows but the true count across both legs is 100+100=200, and the checksum stayed stable across the kill with rows=200 even when the mover no-op'ed; this masks a never-verified atomicity property and was tracked as a BACKLOG verify item rather than a shipped fix.", "target": "operations/troubleshooting", "current_evidence": "utils/ca-soak/scenarios/cards/s36_s37_disk_move.py's S37 `dev`-scale param_table (lines 473-477) sets `small_rows: 100` and `ttl_rows: 100` as the scenario's two legs -- exactly the 100+100=200 total the claim describes -- and utils/ca-soak/scenarios/BACKLOG.md:3059 explicitly references 'S37's vacuous TTL leg' as a known oracle issue in the same suite-wide GC-blindness discussion, confirming the diagnosed oracle-bug (not product-defect) framing and its BACKLOG tracking."}, {"cluster_id": "C-4453", "canonical_claim": "D2 found S10 (patch parts + lightweight deletes) left a deleted bucket with 60 rows and patch parts observed = 0 (which may merge away quickly); classified as SCALE/TIMING, recommending verification at bigger scale with sync/settle before the count rather than treating it as a product bug.", "target": "operations/troubleshooting", "current_evidence": "utils/ca-soak/scenarios/cards/s09_s11_mutations.py implements the S10 'patch parts and lightweight deletes' card exactly as described (title at line 266, max_patch_parts tracking at 316-366). Multiple S10 run reports confirm the finding pattern: e.g. utils/ca-soak/scenarios/runs/20260629T234547_S10_seed7/report.json has \"max_patch_parts_observed\": 0 alongside a deleted_buckets/deleted_rows structure (960 rows in that run; other S10 runs show 720/200000/600000-row deletions), consistent with 'patch parts observed = 0, may merge away quickly' as a recurring, not one-off, S10 outcome."}, {"cluster_id": "C-4522", "canonical_claim": "The #37 reproduction's reported no-orphan outcome was counter-supported but not fsck-verified; the follow-up requires running `fsck` to fixpoint after fence-loss recovery and relating the precommit-window edge to S30 `DANGLING-PRECOMMIT`.", "target": "operations/troubleshooting", "current_evidence": "tests/integration/test_cas_replicated_relink/test.py:874~test recovery fsck check \u2014 fsck-to-fixpoint verification present; DANGLING-PRECOMMIT/S30 follow-up documented in utils/ca-soak/scenarios/BACKLOG.md:1249,1294."}, {"cluster_id": "C-4523", "canonical_claim": "The stuck-removal observability path must surface a `Pending` item wedged by an unreadable terminal, including the actor that can unstick it and the operator-visible recovery state.", "target": "operations/troubleshooting", "current_evidence": "src/Disks/tests/gtest_cas_ref_catalog.cpp:1574~AdoptedRoundWarnsEveryRestartWithoutAppending \u2014 Pending-item stuck-removal surfacing confirmed alongside gtest_cas_gc_log.cpp:461~ReflectsLeadershipAndPendingReclaim."}, {"cluster_id": "C-4539", "canonical_claim": "Generic Ring-2 fixes such as `ThreadStatus` `parent_thread_group` retention, `ReadBufferFromFileView` position handling, `ReadBufferFromS3` cancel-stop, `LocalObjectStorage` TOCTOU robustness, `MergeTreeDeduplicationLog` null-writer fail-close, S3 conditional-write plumbing, GCS dialect/signer, and the 412/Expect fixes are tracked for separate upstream pull requests.; The upstream carve-out inventory includes `ReadBufferFromFileView` position corruption (B115), `ReadBufferFromS3` retry-cancel (B117), and `ThreadStatus` lifetime (B90) as standalone fixes with tests.; Generic fixes for `ThreadStatus` B90, `ReadBufferFromFileView` B115, `ReadBufferFromS3` B117, `LocalObjectStorage` TOCTOU, the `MergeTreeDeduplicationLog` null-writer guard, `copyS3File` message formatting, `Expect: 100-continue`, `S3Exception::isPreconditionFailed()`, and the GCS dialect/signer should be carved into separate upstream pull requests to reduce the fork's Ring-2 diff.", "target": "roadmap", "current_evidence": "src/IO/ReadBufferFromFileView.cpp:73~getPosition \u2014 every listed Ring-2 item (ThreadStatus, ReadBufferFromFileView, ReadBufferFromS3, LocalObjectStorage, MergeTreeDeduplicationLog, copyS3File, S3Exception::isPreconditionFailed, PocoHTTPClient) has confirmed production and test occurrences."}, {"cluster_id": "C-4602", "canonical_claim": "Phase 3 converts `cas_ref_log` and `cas_ref_snap` to Always-compressed text, re-derives JSON-inflated byte budgets, preserves key-to-body `ns` and `txn_id` binding, and updates builders and parsers for the `.zst` suffix.", "target": "roadmap", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasRefLogFormat.cpp:160~writeLogMeta, src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasRefSnapshotFormat.cpp:95~writeSnapshotMeta, src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasLayout.cpp:128~CasLayout::parseRefObjectKey \u2014 cas_ref_log/cas_ref_snap text-format writers/parsers with ns/txn_id binding and .zst suffix handling are implemented at HEAD."}, {"cluster_id": "C-4603", "canonical_claim": "Phase 4 converts the fixed 22-byte `CasBlobMeta` sidecar to one-line JSON while keeping deduplication, resurrection, and token semantics unchanged.", "target": "roadmap", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasBlobMeta.h:7~include, src/Disks/tests/gtest_cas_blob_meta_format.cpp:2~include \u2014 the sidecar was converted to a one-line JSON format (CasBlobMetaFormat) per the notes, implementing Phase 4 with the decoded type renamed to BlobMeta."}, {"cluster_id": "C-4606", "canonical_claim": "Phase 7 converts `CasEnvelope` to a 256-byte JSON header line padded with spaces, keeps payload offset 256, removes `hash_algo`, `domain_id`, `header_hash`, and `writer_version`, and treats former TLV fields as `!`-critical keys.", "target": "roadmap", "current_evidence": "utils/ca-soak/scenarios/BACKLOG.md:1341, src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasBlobEnvelopeFormat.h:55/56/58/71~EnvelopeHeader, ContentAddressedTransaction.cpp:739~buildS3StagingBlobHeader, CasPartWriteTxn.cpp:465~uploadFromSource, cas_test_helpers.h:211~writeBlobRaw \u2014 the redesigned envelope (offset 256, removed fields, `!`-critical-key markers) is implemented and exercised across production and test code at HEAD"}]