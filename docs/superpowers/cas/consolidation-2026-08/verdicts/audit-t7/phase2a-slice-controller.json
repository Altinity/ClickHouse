[{"cluster_id": "C-0077", "verdict": "open", "canonical_claim": "The `utils/ca-soak` text-log probe using `logger_name = 'CasStore'` is unresolved because neither the `CasStore` logger nor its `crossed soft limit` message exists in `src/`; it is documented as best-effort and fails open when unavailable.", "current_evidence": "utils/ca-soak/scenarios/cards/s06_s08_manifest_parts.py:87-91 (_soft_limit_warnings) documents CasStore as a historical logger/message name; notes confirm neither the CasStore logger nor 'crossed soft limit' text was found elsewhere in src/.", "results_md_hits": {"CasStore": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:419:   any `AllocEpoch`, whereas `CasStore.cpp:312-316`'s STRICT ORDER allocates the writer epoch BEFORE", "docs/superpowers/models/CaCasMountCore_RESULTS.md:714:(`CasStore.cpp:312-316`) allocates the durable writer epoch first and never reaches that state, and"]}}, {"cluster_id": "C-0088", "verdict": "open", "canonical_claim": "The whole-database `DROP REPLICA` path may silently skip a table proxy, and `STOP <action>` may attach its `ActionLock` to a proxy; these pre-existing tails are tracked in `BACKLOG`.", "current_evidence": "src/Interpreters/InterpreterSystemQuery.cpp:1691 (DROP REPLICA handling) and src/Common/ActionLock.h:14 confirmed present; not_found 'STOP <action>' exact fragment, so the ActionLock-on-proxy tail is not confirmed resolved.", "results_md_hits": {"BACKLOG": ["docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:26:   BACKLOG release-blocker `{#list-as-journal-dataloss-2026-07-25}` reproduced formally, and the", "docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:213:This is the BACKLOG blast-radius scenario mechanised: a durable `+1` on a **deduplicated** blob is", "docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:218:Two things this trace establishes beyond the BACKLOG prose:"]}}, {"cluster_id": "C-3373", "verdict": "unverifiable", "canonical_claim": "The generic `DownloadPart` path measured approximately 15 `HEAD`s and 15 `GET`s per replicated part.", "current_evidence": "Searched src/Storages/MergeTree/DataPartsExchange.{h,cpp} (the DownloadPart fetch path, downloadPartToDisk/downloadPartToDiskRemoteMeta) and all of utils/ca-soak/ for '15 HEAD' / '15 GET' / a per-part HTTP-op-count measurement of ~15/~15 \u2014 no such figure exists anywhere in src/ or utils/ca-soak/. PartLog.cpp:69,112 defines the DownloadPart part-log event but carries no op-count claim. Only found DownloadPart as a soak part-event label and HEAD/GET as generic HTTP method names in utils/ca-soak/scripts/gcs_goog4_probe.py.", "results_md_hits": {"HEAD": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:25:Confirmed GREEN/RED against the pre-task committed model (`git show HEAD:...`), before any", "docs/superpowers/models/CaCasMountCore_RESULTS.md:253:against the ORIGINAL, pre-task committed `.tla` file (via `git show HEAD:...`) produces an", "docs/superpowers/models/CaCasMountCore_RESULTS.md:372:(`git show HEAD:...`, in a scratch tree) and once against the modified one. **All twelve are"]}}, {"cluster_id": "C-3395", "verdict": "stale", "canonical_claim": "Loose verbatim files use roots/<server>/<path>, while files inside a CAS archive use \u2026@cas@/_files/<name>; GC does not scan loose mountpoint objects. Additionally, Loose verbatim mountpoint objects use keys shaped as `roots/<server_root_id>/<path>` and are probed with a directory-safe `HEAD`. Additionally, Namespace files under `roots/<srid>/<ns>/_files/<name>` and loose mountpoint objects under `roots/<srid>/<path>` bypass CAS and are removed eagerly because they are not shared. Additionally, Namespace verbatim files at <prefix>/roots/<ns>/_files/<relative-path> and mountpoint objects at <prefix>/roots/<relative-path> are raw passthrough bytes whose integrity belongs to their external file formats; Namespace-level verbatim files in the reserved `@cas@/_files/` segment are served from root-shard `_files`-keyed inline bytes and bypass the blob/manifest pipeline.", "current_evidence": "CasLayout.h:231 `Layout::namespaceFileKey` and CasLayout.h:292 `Layout::mountpointObjectKey` are the actual key builders; none of the seven claimed literal `roots/<server>/<path>`, `roots/<srid>/...`, `<prefix>/roots/...` shapes matched their real key grammar.", "results_md_hits": {"HEAD": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:25:Confirmed GREEN/RED against the pre-task committed model (`git show HEAD:...`), before any", "docs/superpowers/models/CaCasMountCore_RESULTS.md:253:against the ORIGINAL, pre-task committed `.tla` file (via `git show HEAD:...`) produces an", "docs/superpowers/models/CaCasMountCore_RESULTS.md:372:(`git show HEAD:...`, in a scratch tree) and once against the modified one. **All twelve are"]}}, {"cluster_id": "C-1934", "verdict": "stale", "canonical_claim": "`CaGcAckFloorCore.tla`'s `SabotageClampNoSuppress` config, which removes the `~clampedL` graduation guard, reproduces the 2026-07-03 night incident of 31 dangling blobs as an `INV_NO_DANGLE` counterexample; the shipped clamp-suppression guard (`suppress_destructive` gating `Gc::graduationDue`, `CasGc.cpp:1360`) blocks graduation and pending deletes while any shard is clamped, so held refs persist in `landed` until a later clamp-free pass consumes them. If any fold pass records a clamp anomaly, destruction is suppressed: no graduations or redeletes occur, pending entries carry `UNCHANGED`, condemn/spare continue, and deletes resume on the first clamp-free pass; the pass emits a warning and increments `CasGcClampSuppressedPasses`.", "current_evidence": "Notes state `CasGc.cpp:1360` is a `RootAdd`/`RootRemove` event line, not the claimed graduation guard; `CaGcAckFloorCore.tla`, `SabotageClampNoSuppress`, `~clampedL`, and `CasGcClampSuppressedPasses` were all searched for and not found, contradicting the claim's specific citations.", "results_md_hits": {"suppress_destructive": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:177:suppress_destructive = anomalies"], "INV_NO_DANGLE": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:53:rejected it \u2014 not only for the model's `INV_NO_DANGLE` reason", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:114:sab_acceptnamespacemismatch        violation         violation:INV_NO_DANGLE                45       PASS", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:116:sab_advancepastmissingbody         violation         violation:INV_NO_DANGLE                19       PASS"], "UNCHANGED": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:341:Every pre-existing action gained a single `UNCHANGED rgVars` conjunct rather than having its", "docs/superpowers/models/CaCasMountCore_RESULTS.md:342:already-long `UNCHANGED` tuple retyped \u2014 an edit touching 19 multi-line tuples is exactly where a", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:254:`GReduceShard`'s composite `UNCHANGED vars` tuple as referencing an"]}}, {"cluster_id": "C-1680", "verdict": "stale", "canonical_claim": "Settlement semantics remain byte-for-byte unchanged in the retired-in-snapshot plan: condemn, carry, graduate with `delete_pending`, redelete, clamp suppression, pre-CAS single delete site, resurrect-supersede, `.meta` writes, B170 events, and GC-log counters are preserved. Additionally, The retired-in-snapshot design keeps B170 events, GC-log counters, `.meta` writes, and the `system.content_addressed_garbage_collection_log` schema unchanged.", "current_evidence": "utils/ca-soak/scripts/t8_s45_drain_observation.py:8~module_docstring \u2014 notes state source comments at HEAD use `system.cas_gc_log`, not the claimed `system.content_addressed_garbage_collection_log` table name", "results_md_hits": {"delete_pending": ["docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:101:  run condemn \u2192 `delete_pending` \u2192 physical delete to completion.", "docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:116:  \u2192 **`delete_pending`** (round 2) \u2192 **physical delete**, `present[b1] = FALSE` (round 3).", "docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:210:| S10\u2013S11 | `GFold` (complete) / `GSettle` (round 2) | id 1 is now **below** `cursor[r1] = 2`, so it is never in `Avail` again \u2014 the complete pages cannot recover it. \u2192 **`delete_pending`** |"]}}, {"cluster_id": "C-3497", "verdict": "stale", "canonical_claim": "The identity micro-header merge places `RootNamespace`, `Token`, `BlobDigest`, `BlobRef`, `ManifestId`, and `RefTxnId` in `CasTypes.h`, while codec and hex helpers remain in the separate `CasCodecUtil.h` file.", "current_evidence": "found_at shows BlobDigest and BlobRef defined in CasBlobDigest.h (lines 69, 207), not in CasTypes.h as the claim states; only RootNamespace, Token, ManifestId, RefTxnId are confirmed in CasTypes.h", "results_md_hits": {"ManifestId": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:13:`ManifestId`s with owner transitions and blob-only in-degree, precommit"]}}, {"cluster_id": "C-3498", "verdict": "stale", "canonical_claim": "`CasPlainObjects` is stateless over `Backend &` and `const Layout &`, and `CasStore` retains the external API through thin delegates.", "current_evidence": "notes: CasStore occurs only in soak backlog/card references (utils/ca-soak/scenarios/cards/s06_s08_manifest_parts.py:87), while the current production owner of the external API is Pool (CasPool.h:1069~Pool), contradicting the claim that CasStore retains the external API via delegates", "results_md_hits": {"CasStore": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:419:   any `AllocEpoch`, whereas `CasStore.cpp:312-316`'s STRICT ORDER allocates the writer epoch BEFORE", "docs/superpowers/models/CaCasMountCore_RESULTS.md:714:(`CasStore.cpp:312-316`) allocates the durable writer epoch first and never reaches that state, and"]}}, {"cluster_id": "C-3504", "verdict": "stale", "canonical_claim": "The D1 shard-incarnation and registry-removal work deleted `gc/registry`, stamps `ShardIncarnation` in `CasRootShardCodec.h`, and discovers the universe with `LIST`-based `discoverUniverse`.", "current_evidence": "notes state gc/registry still occurs in current code/tests (contradicting the claimed deletion); CasRootShardCodec.h has no occurrence except in a soak-backlog reference (not an actual file); not_found: ShardIncarnation as a type (only embedded in test names) [also: src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:3681~Gc::discoverUniverse]", "results_md_hits": {"ShardIncarnation": ["docs/superpowers/models/CaGcShardIncarnationCore_RESULTS.md:1:# CaGcShardIncarnationCore \u2014 TLC results (D1 phase-0 gate)", "docs/superpowers/models/CaGcShardIncarnationCore_RESULTS.md:3:**Model:** `CaGcShardIncarnationCore.tla` (focused sibling, in the style of `CaGcIndegRefoldCore`).", "docs/superpowers/models/CaGcShardIncarnationCore_RESULTS.md:7:Run: `java -XX:+UseParallelGC -cp <tla2tools.jar> tlc2.TLC -workers auto -config <cfg> CaGcShardIncarnationCore.tla`"], "LIST": ["docs/superpowers/models/CaRefTableSnapshotLogCore_RESULTS.md:189:**`_sab_scanistruth` \u2192 `INV_RECOVERY`** (\u00a74/\u00a75, \"LIST is a zero-trust hint\").", "docs/superpowers/models/CaRefTableSnapshotLogCore_RESULTS.md:285:- **The LIST hint is not modelled on the honest path**, because v9 consumes it nowhere: recovery is", "docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:155:`Gc::drainCompletedRemoving` before heartbeat work, DEFER, the hot stream LIST,"]}}, {"cluster_id": "C-3632", "verdict": "stale", "canonical_claim": "Stage 1 adds one server-level CAS upload-pool size setting and rejects an uninitialized or zero-sized pool; non-CAS disks and engines have no behavior change. Additionally, The stage-1 pool setting `content_addressed_blob_upload_pool_size` defaults to 16, rejects 0, and the pool getter throws `LOGICAL_ERROR` before server or test initialization.", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasBlobUploadPool.cpp:39,43~initializeBlobUploadPool \u2014 the implemented setting is `cas_blob_upload_pool_size`, not `content_addressed_blob_upload_pool_size` (not_found), though default 16, zero-rejection, and LOGICAL_ERROR-before-init behavior match.", "results_md_hits": {"LOGICAL_ERROR": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:17:model-**ALIGNMENT**, not a model change: the implementation's `LOGICAL_ERROR` abort on a"]}}, {"cluster_id": "C-0289", "verdict": "stale", "canonical_claim": "Refsnaplog text uses `we`/`rs` for the primary `RefTxnId`, `rte`/`rts` for `remove_txn_id`, `sfe`/`sfs` for `sealed_from`, and `me`/`mb`/`mo` for `ManifestRef` fields.", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasRefSnapshotFormat.cpp:180~decodeRefTableSnapshot \u2014 `rte`/`rts` are recognized only to reject retired fields (not active `remove_txn_id` encoding), `sealed_from` occurs only in retired comments, and `sfs` was not_found at all, contradicting the claimed active field mapping.", "results_md_hits": {"ManifestRef": ["docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:18:2. **Every sabotage violates.** Gate 1's exact-`ManifestRef` equality (rule 5), lane quiescence", "docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:107:### `_sab_nogate1` \u2014 gate 1's exact-`ManifestRef` equality is load-bearing {#sab-nogate1}"]}}, {"cluster_id": "C-3605", "verdict": "stale", "canonical_claim": "The adaptive large-blob HEAD-before-PUT threshold `dedup_head_first_min_bytes` defaults to 1 MiB. Additionally, `dedup_head_first_min_bytes` defaults to 1 MiB; below that threshold a speculative conditional PUT is preferred to a HEAD plus PUT unless the dedup cache already says the blob is present. Additionally, The CA upload path uses `deduplication_head_first_min_bytes` with a default of 1 MiB, causing a HEAD-before-PUT for large blobs; on fresh data the measured 498/498 HEADs missed and avoided zero PUTs. Additionally, `deduplication_head_first_min_bytes` sends a `HEAD` request before the upload body when `source.size >= cfg.deduplication_head_first_min_bytes`, allowing an already-present upload to be skipped; `0` disables this behavior.", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasPartWriteTxn.cpp:200~PartWriteTxn::uploadFromSource \u2014 the implemented setting is deduplication_head_first_min_bytes (1 MiB default, 0 disables); the claimed spelling 'dedup_head_first_min_bytes' is in not_found.", "results_md_hits": {"HEAD": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:25:Confirmed GREEN/RED against the pre-task committed model (`git show HEAD:...`), before any", "docs/superpowers/models/CaCasMountCore_RESULTS.md:253:against the ORIGINAL, pre-task committed `.tla` file (via `git show HEAD:...`) produces an", "docs/superpowers/models/CaCasMountCore_RESULTS.md:372:(`git show HEAD:...`, in a scratch tree) and once against the modified one. **All twelve are"]}}, {"cluster_id": "C-3135", "verdict": "stale", "canonical_claim": "A cold `resolveRef` shard read costs one `HEAD` and one `GET`, while a warm single-flight resolution costs zero additional operations.", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Pool/CasRefLedger.cpp:275-286 (`CasRefLedger::resolveRef`) documents that the read path is now a single cached `RefTableState` per namespace for the sole mounted writer: 'The `allow_stale` staleness-tolerance knob no longer selects anything: this mounted writer is the ONLY writer of `ns`'s ref state... unlike the old per-shard decode cache'. This explicitly supersedes the per-shard HEAD+GET cost model the claim describes; a warm resolveRef reads only the in-memory cached table (no per-call HEAD/GET at all), not '0 additional operations on top of 1 HEAD + 1 GET cold'.", "results_md_hits": {"HEAD": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:25:Confirmed GREEN/RED against the pre-task committed model (`git show HEAD:...`), before any", "docs/superpowers/models/CaCasMountCore_RESULTS.md:253:against the ORIGINAL, pre-task committed `.tla` file (via `git show HEAD:...`) produces an", "docs/superpowers/models/CaCasMountCore_RESULTS.md:372:(`git show HEAD:...`, in a scratch tree) and once against the modified one. **All twelve are"]}}, {"cluster_id": "C-3137", "verdict": "stale", "canonical_claim": "Strict-freshness callers such as the publish gate pass `allow_stale = false` and always issue a shard `HEAD` regardless of `shard_decode_cache_ttl_ms`.", "current_evidence": "not_found 'shard_decode_cache_ttl_ms' \u2014 the setting the claim conditions HEAD-skipping on does not exist at HEAD; only allow_stale=false plumbing (CasRefLedger.h:125, CasPool.h:489) is present.", "results_md_hits": {"HEAD": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:25:Confirmed GREEN/RED against the pre-task committed model (`git show HEAD:...`), before any", "docs/superpowers/models/CaCasMountCore_RESULTS.md:253:against the ORIGINAL, pre-task committed `.tla` file (via `git show HEAD:...`) produces an", "docs/superpowers/models/CaCasMountCore_RESULTS.md:372:(`git show HEAD:...`, in a scratch tree) and once against the modified one. **All twelve are"]}}, {"cluster_id": "C-3149", "verdict": "stale", "canonical_claim": "`PartFolderView` is an immutable no-I/O snapshot containing a resolved ref, copied mutable files, and the same `shared_ptr<const PartManifest>` held by the store manifest cache; mutable refreshes create a new view sharing that manifest pointer. Additionally, `PartFolderView` contains `PartRefKey`, `ManifestId`, encoded manifest size, publication time, copied mutable files, and the shared immutable manifest pointer; its query surface is pure and index-free. Additionally, `PartFolderView` is immutable and joins `RootNamespace`, `ref_name`, resolved `RootRef` payload, decoded `PartManifest`, a file index, and a directory/projection index.; `PartFolderView` never calls back into metadata storage, never mutates `Cas::Store`, and performs no object-store I/O; `Cas::Store` does not depend on the facade.", "current_evidence": "not_found 'Cas::Store' \u2014 notes state the implementation uses Cas::Pool, not a class named Cas::Store; PartFolderView (PartFolderAccess.h:20,70,81,98) does hold PartRefKey/ManifestId/shared_ptr<const PartManifest> as claimed, but the 'Cas::Store does not depend on the facade' framing names a nonexistent type.", "results_md_hits": {"PartManifest": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:6:title: CaGcRootLocalPartManifestCore \u2014 TLA+ gate results", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:10:# `CaGcRootLocalPartManifestCore` \u2014 TLA+ gate results {#ca-gc-root-local-part-manifest-core-results}", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:75:- `CaGcRootLocalPartManifestCore.tla` gained two comments (at the `CONSTANTS`"], "ManifestId": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:13:`ManifestId`s with owner transitions and blob-only in-degree, precommit"]}}, {"cluster_id": "C-1814", "verdict": "stale", "canonical_claim": "A janitor nomination is valid only when it came from an actual listed physical id; `NominationCapturedPhysicalId` checks this provenance.", "current_evidence": "not_found `NominationCapturedPhysicalId` \u2014 no such provenance-check symbol exists; src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasNamespaceJanitor.cpp:56~NamespaceJanitor::runOnePage uses a local `life_id` variable populated from parsed LIST keys instead of a named provenance check", "results_md_hits": {"NominationCapturedPhysicalId": ["docs/superpowers/models/CaRefNsCleanupStaleLeaderCore_RESULTS.md:38:resume after rebirth and still cannot name the successor's object. `NominationCapturedPhysicalId`"]}}, {"cluster_id": "C-0007", "verdict": "open", "canonical_claim": "`CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault` now asserts `frontier_complete == true` and `suppress_destructive == false`, so its test name and section header state the opposite of the behavior being verified. `CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault` asserts `frontier_complete == true` and `suppress_destructive == false`, so its name and section header misdescribe the behavior it verifies.", "current_evidence": "src/Disks/tests/gtest_cas_gc_frontier_gate.cpp:471~GateVerdict \u2014 `frontier_complete`/`suppress_destructive` fields exist; not_found the exact test name (suite fixture is `CASGCFrontierGate`), consistent with the naming-mismatch finding TEST-2 still being live.", "results_md_hits": {"CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:204:`CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault`, not modelled"]}}, {"cluster_id": "C-0010", "verdict": "open", "canonical_claim": "`docs/superpowers/cas/06-tla-models.md` still describes `Gc::computeDiscoverDecisions`, `ShardCoverage`, `CasGcPrecommitRevisitForced`, and `min_live_precommit_*` as a landed fix even though those seams and fields were deleted; it also names deleted `stage5_tokendiff` and skip-precommit configs.", "current_evidence": "not_found: `docs/superpowers/cas/06-tla-models.md`, `Gc::computeDiscoverDecisions`, `ShardCoverage`, `CasGcPrecommitRevisitForced`, `stage5_tokendiff` \u2014 these seams are absent from the current tree (only a stale `min_live_precommit_*` test comment survives per src/Disks/tests/gtest_cas_fold_seal_codec.cpp:16), consistent with the T7 doc-staleness finding not yet fixed.", "results_md_hits": {"ShardCoverage": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:24:`ShardCoverage.folded_token`, letting `GDiscoverSkip` advance the fold cursor", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:31:found no `ShardCoverage`/`folded_token` type, and no discovery path that"], "stage5_tokendiff": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:70:  `stage5_tokendiff`, `sab_skipchangedshard`, `sab_skipparksdeadprecommit`,"]}}, {"cluster_id": "C-0018", "verdict": "stale", "canonical_claim": "`suppress_destructive` (`!report.anomalies.empty()`) is independently recomputed in both `Gc::fold` and `Gc::runRegularRound`, coupled only through a mutable `report.anomalies`; if future code appends an anomaly between the two reads, the two decisions could silently diverge (fold suppresses deletes but the round doesn't, or vice versa).", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:3039~Gc::fold \u2014 notes state `suppress_destructive` is 'assigned in the fold and consumed by the round', i.e. passed/consumed rather than independently recomputed in both places, contradicting the claim's mutable-report coupling description.", "results_md_hits": {"Gc::runRegularRound": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:154:Immediately after lease acquisition, `Gc::runRegularRound` invokes"], "suppress_destructive": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:177:suppress_destructive = anomalies"], "Gc::fold": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:170:Later, `Gc::fold` computes the physical gate in `Gc/CasGc.cpp`:"]}}, {"cluster_id": "C-2138", "verdict": "stale", "canonical_claim": "`ContentAddressedMetadataStorage` owns one facade instance, constructs it after `Store::open` during startup, resets it before `cas_store` during shutdown, and its accessor throws `LOGICAL_ERROR` before startup.", "current_evidence": "ContentAddressedMetadataStorage.cpp:879~startup currently calls Pool::open, not Store::open as claimed (Store::open only occurs in utils/ca-soak/scenarios/BACKLOG.md:911); the cited LOGICAL_ERROR at runGarbageCollectionRoundNow:397 is a runtime guard rather than the specific pre-startup accessor-throw pattern described.", "results_md_hits": {"LOGICAL_ERROR": ["docs/superpowers/models/CaCasMountCore_RESULTS.md:17:model-**ALIGNMENT**, not a model change: the implementation's `LOGICAL_ERROR` abort on a"]}}, {"cluster_id": "C-0110", "verdict": "open", "canonical_claim": "The archaeology report's recommended architectural-correction backlog (\u00a713.3, medium risk/effort, not yet applied): move `IContentAddressedExchange` to a neutral home and fold `parseTableUuid` into `adoptPartFromManifest`; encapsulate the projection whole-part-transaction rule so callers never branch on `isContentAddressed()` directly (kills the B58-class trap, removes 6 duplicated conditionals); route plain-object writes through `CasRequestController` or give them an explicitly-documented exemption; split `CasGc.cpp` (a 2.3k-line unit carrying four redesigns) into workflow units (fold / deletion / cleanup / cursor / budget) keeping `Gc` as pure orchestration; split `Store::open` into explicit create/open-rw/open-ro modes, since a read-only open can still write `_pool_meta` on an empty pool; probe TOKEN\u21d2CONTENT at startup; and add a dedicated access right for `GC REBUILD` that requires an explicit disk.", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:489~Gc::runRegularRound shows CasGc.cpp is still a single large file (not yet split into fold/deletion/cleanup/cursor/budget units); utils/ca-soak/scenarios/BACKLOG.md:911 references `Store::open` only in soak documentation, not as split create/open-rw/open-ro modes \u2014 the \u00a713.3 architectural corrections remain not-yet-applied as the claim states.", "results_md_hits": {"CasGc.cpp": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:170:Later, `Gc::fold` computes the physical gate in `Gc/CasGc.cpp`:", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:39:- The landed ref-log intake fold (`CasGc.cpp`, the frozen-tail walk around the", "docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:195:(`CasGc.cpp:829,1033`, \"the durable cursor advances per FULLY folded log\" at `:1035-1037`; no"]}}, {"cluster_id": "C-0120", "verdict": "open", "canonical_claim": "The source-layout refactoring design defers a backlog of deliberately-not-done improvements: a constructor config struct for the pool facade's ~25 positional parameters, flattening `SingleWriterSlot` into `MountLeaseKeeper` if no second subclass appears, and retiring vestiges like `ShardIncarnation` and the unreachable `CasNs::Server`.", "current_evidence": "src/Disks/tests/gtest_cas_gc_shard_incarnation.cpp:49~CASGCShardIncarnation shows ShardIncarnation still present (not yet retired); not_found: 'CasNs::Server' \u2014 the config-struct and MountLeaseKeeper-flattening backlog items remain undone.", "results_md_hits": {"ShardIncarnation": ["docs/superpowers/models/CaGcShardIncarnationCore_RESULTS.md:1:# CaGcShardIncarnationCore \u2014 TLC results (D1 phase-0 gate)", "docs/superpowers/models/CaGcShardIncarnationCore_RESULTS.md:3:**Model:** `CaGcShardIncarnationCore.tla` (focused sibling, in the style of `CaGcIndegRefoldCore`).", "docs/superpowers/models/CaGcShardIncarnationCore_RESULTS.md:7:Run: `java -XX:+UseParallelGC -cp <tla2tools.jar> tlc2.TLC -workers auto -config <cfg> CaGcShardIncarnationCore.tla`"]}}, {"cluster_id": "C-0121", "verdict": "open", "canonical_claim": "Stage-B has two non-gating follow-ups scheduled after the T9 closeout: F1, a post-baseline mechanical split of `CasGc.cpp` and `CasRefLedger.cpp` along existing seams (request-count/order goldens captured before and rerun unchanged after); and F2, pre-upstream sanitation that strips branch-local documentation references and coordination markers from production code while keeping each rationale, starting from a measured 76-reference comment inventory and remeasuring before declaring it clean.; Stage-B follow-up Task F1 (outside the Stage-B verdict) is a purely mechanical split of `CasGc.cpp` and `CasRefLedger.cpp` (each ~4000 lines) along their existing semantic seams (GC: phases/fold/janitor; ledger: writer lanes/recovery/publication), gated by request-count/order equivalence goldens captured before any code moves and rerun unchanged afterward \u2014 no lifecycle or runtime boundary redesign.; Stage-B follow-up Task F2 (before upstreaming) is a sanitation sweep that strips branch-local references (task/review/finding numbers, BACKLOG, \"superpowers\") from production code and operator-visible strings, keeping the underlying reason but deleting the citation, verified by `git grep` measuring the baseline (~76 references) down to zero.", "current_evidence": "src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:1~CasGc translation unit and related comment sites show only references/comments, not execution of the F1 mechanical split or F2 sanitation sweep; notes confirm these matches are 'not evidence that either follow-up was executed'.", "results_md_hits": {"CasGc.cpp": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:170:Later, `Gc::fold` computes the physical gate in `Gc/CasGc.cpp`:", "docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:39:- The landed ref-log intake fold (`CasGc.cpp`, the frozen-tail walk around the", "docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md:195:(`CasGc.cpp:829,1033`, \"the durable cursor advances per FULLY folded log\" at `:1035-1037`; no"]}}, {"cluster_id": "C-1724", "verdict": "stale", "canonical_claim": "`cas_fold_seal` is `PinnedRaw` and `Strict`; its encoder emits fixed field and section order, sorts vector collections by run key inside the encoder, and must be byte-reproducible for `putDeterministicArtifact` adoption. Additionally, The `cas_fold_seal` body consists of a header, a metadata line, record sections in coverage, blob-target-runs, part-manifest-cleanup, condemned-summary, and namespace-cleanup order, followed by a `{\"n\":<total record count>}` trailer; record kinds are `cov`, `btr`, `pmc`, `cnd`, and `nsc`. Additionally, A fold-seal `nsc` record reconstructs its map key as `ns + newline + renderRefTxnId(remove_txn_id)`; a zero `{0,0}` transaction id is invalid for `nsc`, while a zero `last_folded_ref_id` is allowed for coverage records. Additionally, A cas_fold_seal contains rfl ref-life rows, sorted btr blob-target RunRef rows, and shard-sorted cnd condemned summaries, followed by a record-count trailer Additionally, cas_fold_seal is a write-once deterministic artifact produced by encodeFoldSeal and putDeterministicArtifact, so replay of the same attempt must reproduce identical bytes", "current_evidence": "not_found: 'pmc', 'ns + newline + renderRefTxnId(remove_txn_id)', '{0,0}', '{\\\"n\\\":<total record count>}' \u2014 notes state current encoding writes deterministic rfl, sorted btr, and shard-sorted cnd sections, while 'cov' and 'nsc' occur only in legacy test fixtures and 'pmc' is absent, contradicting the claimed cov/btr/pmc/cnd/nsc record-kind set. [also: src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Formats/CasFormat.cpp:167~format_traits]", "results_md_hits": {"last_folded_ref_id": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md:43:  `RefCoverage.last_folded_ref_id`, and `cursor == tail` is exactly this"]}}, {"cluster_id": "C-1734", "verdict": "stale", "canonical_claim": "A GC round enumerates the `cas/refs/` prefix twice: `Gc::preFoldRefScan` supplies the defer signal and `Gc::fold` performs the folding walk; only a hole in the fold walk reproduces the skipped-transaction defect.", "current_evidence": "not_found: 'Gc::preFoldRefScan' \u2014 the actual pre-scan method is src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:3773~Gc::listRefPrefix, not a symbol named preFoldRefScan as claimed.", "results_md_hits": {"Gc::fold": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:170:Later, `Gc::fold` computes the physical gate in `Gc/CasGc.cpp`:"]}}, {"cluster_id": "C-1895", "verdict": "stale", "canonical_claim": "The `CaGcDestructiveGateCore` model pins `UniverseAuthoritative == TRUE` because it models the post-flip posture; production's not-yet-knowable-universe posture suppresses destructive work and is covered by `CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault`.", "current_evidence": "not_found \"CaGcDestructiveGateCore\" and \"CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault\"; production instead uses `UniversePolicy::Authoritative` (CasGc.cpp:3024) and test suite `CASGCFrontierGate` (gtest_cas_gc_frontier_gate.cpp:688), not the claimed model/test names.", "results_md_hits": {"CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:204:`CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault`, not modelled"], "CaGcDestructiveGateCore": ["docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:6:title: CaGcDestructiveGateCore \u2014 TLA+ gate results", "docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:10:# `CaGcDestructiveGateCore` \u2014 TLA+ gate results {#ca-gc-destructive-gate-core-results}"]}}, {"cluster_id": "C-1903", "verdict": "stale", "canonical_claim": "`CaRefPreFoldDrainCore` is the sole owner of adopted-parent validation, catalog drain, conclusive CAS resolution, and fresh-cut ordering, and exports an immutable full-catalog token/value pair to the ref-plan consumer boundary.", "current_evidence": "not_found \"CaRefPreFoldDrainCore\"; production instead exposes `RefPlan`/`buildRefWalkPlan` (CasGc.h:217, CasGc.cpp:222), not a consolidated component of the claimed name.", "results_md_hits": {"CaRefPreFoldDrainCore": ["docs/superpowers/models/CaRefCatalogCore_RESULTS.md:7:`CaRefPreFoldDrainCore.tla` is the single owner of that cross-object A/B ordering.", "docs/superpowers/models/CaRefCatalogCore_RESULTS.md:63:cross-object obligations live only in `CaRefPreFoldDrainCore`.", "docs/superpowers/models/CaGcDestructiveGateCore_RESULTS.md:196:consumed from `CaRefPreFoldDrainCore` and"]}}, {"cluster_id": "C-1915", "verdict": "stale", "canonical_claim": "`CasOrphanManifestSweep::activeManifestKeys` must use exact-frontier recovery for owner protection and exact replay through `committed_through`; until that authority is available, catalog-named namespaces are retain-only.", "current_evidence": "not_found: \"CasOrphanManifestSweep::activeManifestKeys\" \u2014 Tier A notes state the helper is a namespace-scope function `activeManifestKeys` in CasOrphanManifestSweep.cpp, not a qualified class member as the claim asserts.", "results_md_hits": {"committed_through": ["docs/superpowers/models/CaRefTableSnapshotLogCore_RESULTS.md:18:`Installed`, and `Acknowledged`. The exact `_ckpt.committed_through` frontier is the only durable", "docs/superpowers/models/CaRefDeltaIntakeCore_RESULTS.md:16:- `BeginRound` freezes exact `_ckpt.committed_through` as `roundCte`.", "docs/superpowers/models/CaRefDeltaIntakeCore_RESULTS.md:96:| `_fix_ckptwitness` | checkpoint-snapshot coverage partially detects a LIST-hidden gap | `_ckpt.committed_through`, not `_ckpt.checkpoint`, is now the exact bound for every acknowledged key | `_sab_cleanupignorescursor` GREEN and `_v9_hold` GREEN |"]}}, {"cluster_id": "C-1917", "verdict": "stale", "canonical_claim": "At the unmatched `parent_ref_lives` branch, `buildRefWalkPlan` increments `CasGcUnmatchedAdoptedParentLives`, logs the exact `NamespaceLifePhysicalId`, drops the aggregate row, and emits no plan row.", "current_evidence": "not_found: \"CasGcUnmatchedAdoptedParentLives\" \u2014 Tier A notes state the branch and life-id type exist at src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Gc/CasGc.cpp:245~buildRefWalkPlan, but the actual metric name at HEAD is `CASGCUnmatchedAdoptedParentLives`, not the claimed spelling.", "results_md_hits": {"buildRefWalkPlan": ["docs/superpowers/models/CaRefPreFoldDrainCore_RESULTS.md:136:cut carries `{successor_life, Removing}`; `buildRefWalkPlan` admits exactly `successor_life`, counts"]}}]