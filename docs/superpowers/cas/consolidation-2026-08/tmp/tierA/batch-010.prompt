You are gathering EVIDENCE (not verdicts) for claims about the ClickHouse
content-addressed storage (CAS) implementation, against the code at HEAD in this
repository (/home/mfilimonov/workspace/ClickHouse/master).

For each cluster below, look at `canonical_claim`. For every backticked
identifier, symbol, setting, function/class/test name, or SQL fragment in the
claim, grep/read src/, tests/, programs/, and utils/ca-soak/ and record:
  - found_at: a list of "path:line~enclosing_symbol" strings where it actually
    appears (copy identifiers exactly as they occur in the claim; do not guess
    or normalize spelling)
  - not_found: identifiers you searched for and could not find anywhere in
    those directories
  - notes: one short sentence of context if useful (e.g. "renamed to X", "only
    referenced in a comment", "matches a TLA+ model file not code")

Do NOT decide done/rejected/stale/open/etc. Only report what you found or did
not find, and where. Search broadly (the identifier may have moved file or
been renamed) before declaring not_found.

Output: append exactly one JSON line per cluster to /home/mfilimonov/workspace/ClickHouse/master/docs/superpowers/cas/consolidation-2026-08/verdicts/evidence/batch-010.jsonl, in the form:
{"cluster_id": "C-NNNN", "found_at": ["path:line~Symbol", ...], "not_found": ["..."], "notes": "..."}

Every cluster_id below must produce exactly one output line. Do not skip any.

Clusters:
{"cluster_id": "C-0273", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-041"], "canonical_claim": "Phase-1 canonical text writers emit no whitespace outside JSON strings and terminate every line with `\n`; hashes and ids are lowercase fixed-width hex strings, unbounded `u64` values are decimal strings, and bounded counts, lengths, and millisecond timestamps are JSON numbers.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase1-formats-bootstrap.md#global-constraints"]}
{"cluster_id": "C-0274", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-042"], "canonical_claim": "`writeHeaderLine` stamps `currentCompatibilityVersion`, which equals `G_BUILD` and is 3; the `G_BUILD` 3-to-4 bump belongs to phase 2 rather than phase 1.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase1-formats-bootstrap.md#global-constraints"]}
{"cluster_id": "C-0275", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-043"], "canonical_claim": "Phase 1 freezes `FormatId::RefLog = 19`, `FormatId::RefSnapshot = 20`, `FormatId::BlobMeta = 21`, and `FormatId::GcHeartbeat = 22`; each receives a `BASELINE` change-point ladder and none receives a legacy binary magic.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase1-formats-bootstrap.md#task-2"]}
{"cluster_id": "C-0276", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-044"], "canonical_claim": "The phase-1 traits registry provides `traitsFor`, `traitsForType`, and `storedSuffix` over `TextFamily`, `KeyStrictness`, and `CompressionPolicy`, with an object cap and line cap for every non-reserved format; `FormatId::Roster` is reserved and has no traits.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase1-formats-bootstrap.md#task-3"]}
{"cluster_id": "C-0277", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-046"], "canonical_claim": "`JsonObjectReader` is a pull cursor for one canonical JSON object: `nextKey` rejects duplicate keys, `readHex128` requires exactly 32 lowercase hex characters, and `skipUnknown` rejects `!` keys with `UNKNOWN_FORMAT_VERSION`, rejects unknown keys in strict formats with `CORRUPTED_DATA`, and skips unknown keys in tolerant formats.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase1-formats-bootstrap.md#task-4"]}
{"cluster_id": "C-0278", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-047"], "canonical_claim": "`CasTextFormat` exposes `writeHeaderLine`, `writeTrailerLine`, `expectHeaderLine`, `sniffHeaderLine`, and `readLine`; `expectHeaderLine` validates type and compatibility without swallowing errors, while `sniffHeaderLine` returns no value for non-CAS or malformed input for fsck and dispatch.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase1-formats-bootstrap.md#task-5"]}
{"cluster_id": "C-0279", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-048"], "canonical_claim": "`sealObject` compresses only `Always` formats into one zstd frame, and `openObject` accepts raw bodies but accepts compressed input only for `Always` formats, checking the declared decompressed size against the cap before allocating output.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase1-formats-bootstrap.md#task-6"]}
{"cluster_id": "C-0280", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-053"], "canonical_claim": "Phase 2 keeps `G_BUILD = 3` and adds no `changePoints` entry because it introduces no breaking text-format generation; future breaking changes earn the version bump.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase2-control-plane.md#global-constraints"]}
{"cluster_id": "C-0281", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-054"], "canonical_claim": "CAS JSON writing pins `FormatSettings::JSON::escape_forward_slashes` to false, so slash characters remain unescaped and text bytes do not depend on ClickHouse-wide defaults.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase2-control-plane.md#global-constraints"]}
{"cluster_id": "C-0282", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-055"], "canonical_claim": "Phase-2 JSON encodes enum values as full words, hashes and UUIDs as 32-character lowercase hex strings, unbounded counters as decimal strings, bounded integers as JSON numbers, and `UINT64_MAX` sentinel fields as decimal strings.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase2-control-plane.md#object-file-map"]}
{"cluster_id": "C-0283", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-056", "B044-040"], "canonical_claim": "The shared `CasWireVocab` renders `Token` as sibling keys `tt` and `tv`, `BlobRef` as sibling keys `ha` and `h`, and maps token, blob-hash, and object-kind enums through fail-closed word maps.; Persisted `BlobRef` fields use `ha` for the hash algorithm and `h` for the digest hex, and the searched user-facing tools contain no `p` JSON/output key.", "issue_ids": [], "sources": [".superpowers/sdd/2026-08-02-cas-stage-b-remaining/shortkeys-report.md#L15-L16", "docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase2-control-plane.md#task1"]}
{"cluster_id": "C-0284", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B016-058"], "canonical_claim": "`PoolMeta::blob_header_len` must be at least 96, a multiple of 8, and at most 16384; `algos_used` must be non-empty, strictly increasing, and contain only valid `BlobHashAlgo` values.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase2-control-plane.md#task2"]}
{"cluster_id": "C-0285", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-014"], "canonical_claim": "Both refsnaplog formats belong to the `Control` family with `Tolerant` keys, `Always`/`.zst` storage, `object_cap = 64 MiB`, and `line_cap = 64 KiB`.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase3-refsnaplog.md#cruxes"]}
{"cluster_id": "C-0286", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-015"], "canonical_claim": "Refsnaplog text headers stamp `v = G_BUILD = 3`; this migration does not bump `G_BUILD` or append a `changePoints` entry.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase3-refsnaplog.md#global-constraints"]}
{"cluster_id": "C-0287", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-019"], "canonical_claim": "The `cas_ref_log` text shape is a `cas_ref_log` header, a meta line containing `ns`, `we`, and `rs`, one record line per `RefOp`, and a `{\"n\":count}` trailer.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase3-refsnaplog.md#text-shape"]}
{"cluster_id": "C-0288", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-020"], "canonical_claim": "The `cas_ref_snap` text shape is a `cas_ref_snap` header, a meta line containing lifecycle and optional removal/seal fields, committed and precommit record lines, and a `{\"n\":count}` trailer.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase3-refsnaplog.md#text-shape"]}
{"cluster_id": "C-0289", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-021"], "canonical_claim": "Refsnaplog text uses `we`/`rs` for the primary `RefTxnId`, `rte`/`rts` for `remove_txn_id`, `sfe`/`sfs` for `sealed_from`, and `me`/`mb`/`mo` for `ManifestRef` fields.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase3-refsnaplog.md#global-constraints"]}
{"cluster_id": "C-0290", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-022"], "canonical_claim": "`cas_blob_meta` is a three-scalar `Control` object containing `state`, `condemn_round`, and `size`, converted from the fixed 22-byte `CAMT` binary codec to one-line JSON text.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase4-blob-meta.md#cas-codecs-v3-phase4"]}
{"cluster_id": "C-0291", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-024"], "canonical_claim": "Blob-meta JSON encodes `state` as `clean` or `condemned` and encodes `condemn_round` and `size` as decimal strings.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase4-blob-meta.md#global-constraints"]}
{"cluster_id": "C-0292", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-028"], "canonical_claim": "`BlobMeta.version` remains in the in-memory struct with default value `1` for `CasInspect`, but is not serialized because the header `v` is the authoritative format version.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase4-blob-meta.md#version-field"]}
{"cluster_id": "C-0293", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-029"], "canonical_claim": "`loadMeta`, `putMetaIfAbsent`, `casMeta`, and `deleteMetaExact` remain in `Core/CasBlobMeta` while `MetaState`, `BlobMeta`, `encodeBlobMeta`, and `decodeBlobMeta` move to `Core/Formats/CasBlobMetaFormat`.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase4-blob-meta.md#task2"]}
{"cluster_id": "C-0294", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-032"], "canonical_claim": "Blob-meta text stamps `v = G_BUILD = 3` without adding a new format generation or `changePoints` entry.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase4-blob-meta.md#global-constraints"]}
{"cluster_id": "C-0295", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-042"], "canonical_claim": "The record-stream codec is backend-free and operates on caller-owned `ReadBuffer` and `WriteBuffer`; backend stream ownership stays in `Core/CasBlobInDegree`.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase5-runs.md#global-constraints"]}
{"cluster_id": "C-0296", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-045"], "canonical_claim": "A source-edge record maps to JSON keys `b` for the algo-prefixed lowercase blob reference, `s` for a 32-character lowercase-hex source ID, and `m` for `edge`, `zero`, or `condemned`.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase5-runs.md#kind-fields"]}
{"cluster_id": "C-0297", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-053"], "canonical_claim": "Each inline manifest entry is represented by an `il` length in its JSON record, while its arbitrary bytes are stored after the trailer as a banner line, exactly `il` raw bytes, and a newline; EOF must follow the last segment.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase6-manifest-stream.md#cruxes"]}
{"cluster_id": "C-0298", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-060"], "canonical_claim": "`encodePartManifest` returns canonical uncompressed text for text-size budgeting; backend PUTs seal that text, backend GETs open it before decoding, and exchange remains text-to-text without wrapping.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase6-manifest-stream.md#cruxes"]}
{"cluster_id": "C-0299", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-063"], "canonical_claim": "Part-manifest text stamps `v = G_BUILD = 3`, drops the binary `writer_version`, uses `Tolerant` JSON keys and `Always`/`.zst`, and has no dual-read path.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase6-manifest-stream.md#global-constraints"]}
{"cluster_id": "C-0300", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-065", "B040-166", "B040-168"], "canonical_claim": "The blob envelope is a fixed-length 256-byte JSON header: a JSON object, ASCII-space padding, and a newline at byte 255, with the payload beginning at the header length.; The v3 cas_blob header occupies exactly 256 bytes: JSON occupies [0,json_len), spaces occupy [json_len,255), and byte 255 is a newline Additionally, The v3 cas_blob envelope retains constant payload offset blob_header_len = 256 and does not use frame-level compression because payload identity is the raw bytes", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs_proposal_v3.md#blob-envelope", "docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase7-blob-envelope.md#budget"]}
{"cluster_id": "C-0301", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-067"], "canonical_claim": "The envelope's ref budget is `(L - 1) - PREFIX.size() - SUFFIX.size()`, with `L` equal to the pool's `blob_header_len`; the ref is incrementally escaped and truncated without splitting an escape.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase7-blob-envelope.md#budget"]}
{"cluster_id": "C-0302", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B017-069"], "canonical_claim": "Envelope JSON maps `tag`, `bld`, and `by` to 32-character lowercase hex strings, `ts` and `ch` to JSON numbers, `op` to a full word, and `ref` to the locally escaped diagnostic string.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-15-cas-codecs-v3-phase7-blob-envelope.md#fields"]}

