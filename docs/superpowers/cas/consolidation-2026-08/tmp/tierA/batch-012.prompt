You are gathering EVIDENCE (not verdicts) for claims about the ClickHouse
content-addressed storage (CAS) implementation, against the code at HEAD in this
repository (/home/mfilimonov/workspace/ClickHouse/master).

For each cluster below, look at `canonical_claim`. For every backticked
identifier, symbol, setting, function/class/test name, or SQL fragment in the
claim, grep/read src/, tests/, programs/, and utils/ca-soak/ and record:
  - found_at: a list of "path:line~enclosing_symbol" strings where it actually
    appears (copy identifiers exactly as they occur in the claim; do not guess
    or normalize spelling)
  - not_found: identifiers you searched for and could not find anywhere in
    those directories
  - notes: one short sentence of context if useful (e.g. "renamed to X", "only
    referenced in a comment", "matches a TLA+ model file not code")

Do NOT decide done/rejected/stale/open/etc. Only report what you found or did
not find, and where. Search broadly (the identifier may have moved file or
been renamed) before declaring not_found.

Output: append exactly one JSON line per cluster to /home/mfilimonov/workspace/ClickHouse/master/docs/superpowers/cas/consolidation-2026-08/verdicts/evidence/batch-012.jsonl, in the form:
{"cluster_id": "C-NNNN", "found_at": ["path:line~Symbol", ...], "not_found": ["..."], "notes": "..."}

Every cluster_id below must produce exactly one output line. Do not skip any.

Clusters:
{"cluster_id": "C-0333", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B027-120"], "canonical_claim": "`slotOccupy` performs exactly one conditional create and, only on conflict resolution, one raw exact GET; it never lists, never retries internally, and returns `Created`, `Occupied` with occupant bytes/token, or `Unresolved`.", "issue_ids": ["r9-6"], "sources": ["docs/superpowers/plans/2026-07-28-cas-ref-chain-stage-a-streams.md#task-2"]}
{"cluster_id": "C-0334", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B027-158"], "canonical_claim": "Fold-seal decoding rejects classifications outside `{0,1,2,4}` before narrowing, rejects classification-4 holds with zero `offending_position`, and rejects duplicate `(namespace, shard)` coverage rows; encoders reject the same malformed shapes.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-28-cas-ref-chain-stage-a-streams.md#task-16"]}
{"cluster_id": "C-0335", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B027-159"], "canonical_claim": "Within one `putIfAbsentControlled` call, `DefiniteFailure` is reported only when every attempt is definite; any earlier unresolved ambiguous attempt dominates the call verdict and preserves `ApplyPending`.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-28-cas-ref-chain-stage-a-streams.md#task-16"]}
{"cluster_id": "C-0336", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B029-029"], "canonical_claim": "Blob re-hashing establishes content identity, `HEAD`-before-`PUT` is the deduplication admission gate, and a condemned object is never revived by reading it back; revival requires a fresh upload.", "issue_ids": [], "sources": ["docs/superpowers/cas/2026-07-28-stage-a-retirement-verdicts.md#adversary"]}
{"cluster_id": "C-0337", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B032-074", "B051-035"], "canonical_claim": "The S3 conditional-write policy treats HTTP 412 as deterministic and non-retryable, exposed through `S3Exception::isPreconditionFailed` and `RetryStrategy`.; HTTP 412 `Precondition Failed` is deterministic for conditional requests and must not be retried, even when an SDK classifies a non-AWS error body as retryable `UNKNOWN`.", "issue_ids": [], "sources": ["docs/superpowers/cas/upstream.md#L265-L277", "docs/superpowers/plans/2026-08-03-cas-carve-script.md#L275-L299"]}
{"cluster_id": "C-0338", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B033-046"], "canonical_claim": "Identifiers belonging to compare-and-swap rather than the CAS feature are excluded from naming changes, including `casPut`, `CasOutcome`, `CasResult`, `CasOp::Cas`, `CasOp::CasConflict`, and the `CasRequestControl.h` family.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-08-03-cas-naming-unification.md#cas-naming-unification-implementation-plan"]}
{"cluster_id": "C-0339", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B033-072"], "canonical_claim": "During the incident, exact-key `GET` and `HEAD` remained truthful while LIST was not, and conditional writes were trusted because `putIfAbsent` cannot report `Created` over an existing key without data loss.", "issue_ids": [], "sources": ["docs/superpowers/cas/2026-08-03-list-trust-verdict.md#trusted-verbs"]}
{"cluster_id": "C-0340", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B034-015"], "canonical_claim": "`CasRequestController` provides single-attempt conditional writes, a budget, fence gating, and exact-key resolution for the ref lane, but `promoteStaged`'s `copyObjectConditional` is a separate retry mechanism and `casPutObject`/`casRemoveObject` still use the disk default retry policy.", "issue_ids": [], "sources": ["docs/superpowers/cas/BACKLOG.md#ref-protocol"]}
{"cluster_id": "C-0341", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B034-059"], "canonical_claim": "`casPutObject` retries with the same frozen payload after a token conflict, so a future concurrent appender requires a real `casAppendObject` that re-reads base content inside the retry loop.", "issue_ids": ["codex-26"], "sources": ["docs/superpowers/cas/BACKLOG.md#read-write"]}
{"cluster_id": "C-0342", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B035-039"], "canonical_claim": "The `EDGE-BEFORE-OBSERVE` writer order is body, `precommitAdd`, `putBlob`, then `promote`; it makes the precommit closure edge durable before blob adoption and is relied on by the two-phase GC deletion recheck.", "issue_ids": [], "sources": ["utils/ca-soak/scenarios/BACKLOG.md#L1673-L1713"]}
{"cluster_id": "C-0343", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B035-079"], "canonical_claim": "`ObjectStorageBackend::get` maps NoSuchKey/HTTP 404 to `std::nullopt`, allowing a legitimate live read in a HEAD-to-GET deletion window to surface cleanly instead of exposing raw Code 499.", "issue_ids": [], "sources": ["docs/superpowers/cas/CONSOLIDATION-COVERAGE.md#blocking-gaps", "docs/superpowers/cas/CONSOLIDATION-COVERAGE.md#gap-fill"]}
{"cluster_id": "C-0344", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B036-019"], "canonical_claim": "`supportsListTokens()` and `tokenForList` expose a LIST-derived incarnation token only for the blob-condemn exact-delete-versus-`HEAD` decision, never for skipping a manifest or owner-transition fold read.", "issue_ids": [], "sources": ["docs/superpowers/models/CaGcRootLocalPartManifestCore_RESULTS.md#verdict-listedtok-skip-premise-retired"]}
{"cluster_id": "C-0345", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B036-049"], "canonical_claim": "Once a successor's `SealSlot` occupies the key, `WedgeRetryOccupied` must reject the retry regardless of the presented generation; the seal is conclusive during the pre-seal race.", "issue_ids": ["r9-5"], "sources": ["docs/superpowers/models/CaCasMountCore_RESULTS.md#v9-traces"]}
{"cluster_id": "C-0346", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B037-053"], "canonical_claim": "Ref-log ids are per-namespace contiguous ids derived from state, and an id whose create outcome is ambiguous must not be reused under the every-attempt rule.", "issue_ids": [], "sources": ["docs/superpowers/models/CaRefTableSnapshotLogCore_RESULTS.md#L156-L163"]}
{"cluster_id": "C-0347", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B037-056"], "canonical_claim": "Every ref-log write is a create-if-absent operation; an unconditional PUT must not overwrite an epoch seal.", "issue_ids": [], "sources": ["docs/superpowers/models/CaRefTableSnapshotLogCore_RESULTS.md#L181-L187"]}
{"cluster_id": "C-0348", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B037-074"], "canonical_claim": "A namespace inactive while global writer epochs advance must close every skipped epoch in order with an empty sequence-1 `EpochSeal` before its later consumer can authorize recovery, fold, or destruction.", "issue_ids": [], "sources": ["docs/superpowers/models/CaWriterEpochBackfillCore_RESULTS.md#writer-epoch-backfill-results"]}
{"cluster_id": "C-0349", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B039-036"], "canonical_claim": "`Cas::Store` already exposes the protocol primitives `resolveRef`, `readManifest`, `locate`, `dropRef`, `updateRefPublishedAt`, and `dropNamespace` needed by the cache facade.", "issue_ids": [], "sources": ["docs/superpowers/cas/cache.md#current-fit-assessment"]}
{"cluster_id": "C-0350", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B039-106"], "canonical_claim": "`Cas::Backend` is the token-aware storage seam used by `Store`, `Build`, `Gc`, liveness keepers, capability probing, and fsck; its load-bearing contract is the token-to-content precondition.", "issue_ids": [], "sources": ["cas_disks_architecture_analysis.md#2-current-feature-map"]}
{"cluster_id": "C-0351", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B039-162"], "canonical_claim": "In `system.cas_log`, `object_hash` is lowercase hex and `token` is the involved incarnation `ETag` when applicable.", "issue_ids": [], "sources": ["docs/en/operations/system-tables/cas_log.md#columns"]}
{"cluster_id": "C-0352", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B040-077"], "canonical_claim": "Blob objects are stored at <prefix>/blobs/<first-two-hex-chars>/<32-hex-blob-id>, produced by Layout::blobKey", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs.md#blob-object-cabl"]}
{"cluster_id": "C-0353", "topic": "blob protocol contract", "target": "architecture/blob-protocol", "member_ids": ["B040-080"], "canonical_claim": "decodeEnvelopeHeader validates CABL magic, compatibility_version, critical extensions, header length, object-size arithmetic, and header_hash", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs.md#blob-object-cabl"]}
{"cluster_id": "C-0354", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B043-070"], "canonical_claim": "Mutable and control protobuf objects should use `CasHeader`; streamed blob payloads should keep a binary envelope, and GC data-plane runs should use `RunFile`.", "issue_ids": [], "sources": ["docs/superpowers/random_notes.md#lines-369-410"]}
{"cluster_id": "C-0355", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B044-021"], "canonical_claim": "CAS blobs and manifests are immutable, content-addressed, write-once objects, and GC is required to preserve no dangle, no returned token, and over-count rather than under-count.", "issue_ids": [], "sources": ["docs/superpowers/reports/reviews.md#L518-L525"]}
{"cluster_id": "C-0356", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B044-059"], "canonical_claim": "Setting `metadata_type` to `cas` makes an object-storage disk content-addressed, shares identical content across parts or tables, and enables background reclamation after references disappear.", "issue_ids": [], "sources": ["docs/en/operations/storing-data.md#L455-L466"]}
{"cluster_id": "C-0357", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B047-044"], "canonical_claim": "CAS integration configs use `<metadata_type>cas</metadata_type>` and the relink test asserts the wire literals `cas_pool_uuid`, `cas_relink=part_manifest_v2`, and `cas_source_token`.", "issue_ids": ["T6"], "sources": [".superpowers/sdd/2026-08-03-cas-naming/t6-report.md#L24-L28"]}
{"cluster_id": "C-0358", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B049-057"], "canonical_claim": "`uploadPendingBlobs` creates one `Cas::BlobUploadRequest` per referenced pending blob and delegates to the free function `DB::Cas::fanOutBlobUploads(build, requests, pool, hooks=nullptr)`.", "issue_ids": [], "sources": [".superpowers/sdd/task-5-report.md#what-changed"]}
{"cluster_id": "C-0359", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B049-060"], "canonical_claim": "Fan-out tasks write only their own pre-sized stable result slot; the runner is declared after the result vector so its destructor drains every task before the vector is destroyed, including a dispatch-loop throw (`B90`).", "issue_ids": ["B90"], "sources": [".superpowers/sdd/task-5-report.md#key-decisions"]}
{"cluster_id": "C-0360", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B049-061"], "canonical_claim": "`waitForAllToFinishAndRethrowFirstError` drains all tasks and then rethrows the first failure in ascending-ref order, and a rethrow bypasses merging results.", "issue_ids": [], "sources": [".superpowers/sdd/task-5-report.md#key-decisions"]}
{"cluster_id": "C-0361", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B049-067"], "canonical_claim": "Fan-out concurrency is bounded by the supplied pool size: pool 2 reaches peak concurrency 2, while pool 1 degenerates to serial execution with a bounded wait.", "issue_ids": [], "sources": [".superpowers/sdd/task-5-report.md#tests-all-six-routed-items"]}
{"cluster_id": "C-0362", "topic": "CAS blob protocol", "target": "architecture/blob-protocol", "member_ids": ["B049-068"], "canonical_claim": "`clickhouse-local` initializes the blob-upload pool from `content_addressed_blob_upload_pool_size` during `LocalServer::initialize` and shuts it down during `LocalServer::cleanup`.", "issue_ids": ["rev-s1-t5 Important"], "sources": [".superpowers/sdd/task-5-report.md#fix-round-1-rev-s1-t5-important"]}

