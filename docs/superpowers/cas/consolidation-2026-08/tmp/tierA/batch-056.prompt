You are gathering EVIDENCE (not verdicts) for claims about the ClickHouse
content-addressed storage (CAS) implementation, against the code at HEAD in this
repository (/home/mfilimonov/workspace/ClickHouse/master).

For each cluster below, look at `canonical_claim`. For every backticked
identifier, symbol, setting, function/class/test name, or SQL fragment in the
claim, grep/read src/, tests/, programs/, and utils/ca-soak/ and record:
  - found_at: a list of "path:line~enclosing_symbol" strings where it actually
    appears (copy identifiers exactly as they occur in the claim; do not guess
    or normalize spelling)
  - not_found: identifiers you searched for and could not find anywhere in
    those directories
  - notes: one short sentence of context if useful (e.g. "renamed to X", "only
    referenced in a comment", "matches a TLA+ model file not code")

Do NOT decide done/rejected/stale/open/etc. Only report what you found or did
not find, and where. Search broadly (the identifier may have moved file or
been renamed) before declaring not_found.

Output: append exactly one JSON line per cluster to /home/mfilimonov/workspace/ClickHouse/master/docs/superpowers/cas/consolidation-2026-08/verdicts/evidence/batch-056.jsonl, in the form:
{"cluster_id": "C-NNNN", "found_at": ["path:line~Symbol", ...], "not_found": ["..."], "notes": "..."}

Every cluster_id below must produce exactly one output line. Do not skip any.

Clusters:
{"cluster_id": "C-1772", "topic": "An unreleased binding becomes eligible when a successor epoch exists and", "target": "architecture/garbage-collection", "member_ids": ["B029-073"], "canonical_claim": "An unreleased binding becomes eligible when a successor epoch exists and is reclaimed by `sweepStalePrecommitsNow`; it is unbounded only while no successor mounts the namespace or every successor sweep fails, so a dedicated fsck class is unnecessary.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-part-write-release-seam.md#retention"]}
{"cluster_id": "C-1773", "topic": "The relink TLA model must name `CommittedEdgesAreGcVisible`", "target": "architecture/garbage-collection", "member_ids": ["B029-106"], "canonical_claim": "The relink TLA model must name `CommittedEdgesAreGcVisible`: a committed ref edge durable before a removal is appended must be observed by every GC fold that observes the removal; the v9 chain models are responsible for discharging it.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-relink-reoffer-redesign.md#tla-named-assumption"]}
{"cluster_id": "C-1774", "topic": "LIST may discover old namespace-file debris only for lazy cleanup", "target": "architecture/garbage-collection", "member_ids": ["B030-026"], "canonical_claim": "LIST may discover old namespace-file debris only for lazy cleanup; LIST omission may leak storage but must never affect file visibility, namespace rebirth, or deletion safety.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-stage-b-namespace-life-amendments.md#incarnation-key-namespace-files"]}
{"cluster_id": "C-1775", "topic": "Cleanup retains the checkpoint base's matching non-seal `_log` while `_c", "target": "architecture/garbage-collection", "member_ids": ["B030-030"], "canonical_claim": "Cleanup retains the checkpoint base's matching non-seal `_log` while `_ckpt` names that base; older covered logs remain deletable.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-stage-b-namespace-life-amendments.md#make-recovery-fully-list-independent"]}
{"cluster_id": "C-1776", "topic": "Namespace-file cleanup removes `_files` from mandatory namespace-removal", "target": "architecture/garbage-collection", "member_ids": ["B030-042"], "canonical_claim": "Namespace-file cleanup removes `_files` from mandatory namespace-removal LIST deletion, uses exact-token deletion with the deposited incarnation, and never re-derives incarnation from the current catalog entry during resumed cleanup.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-stage-b-namespace-life-amendments.md#namespace-file-requirements"]}
{"cluster_id": "C-1777", "topic": "`Gc", "target": "architecture/garbage-collection", "member_ids": ["B030-052"], "canonical_claim": "`Gc::foldManifestEdges` at `Gc/CasGc.cpp:1007-1018` performs one `backend.head(key)` followed by one `backend.get(key)` per manifest edge, and `CasRefManifestBodyFoldGets` counts the GET half.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-29-gc-perf-audit-soak.md#fold-cost-structure"]}
{"cluster_id": "C-1778", "topic": "The `Removing` to `RemovalReady` transition must refuse while a hold wit", "target": "architecture/garbage-collection", "member_ids": ["B031-018"], "canonical_claim": "The `Removing` to `RemovalReady` transition must refuse while a hold with `ShardCoverage::classification == 4` exists for the namespace.", "issue_ids": ["Q1"], "sources": ["docs/superpowers/reports/2026-07-31-removalready-review-fable.md#q1-monotone"]}
{"cluster_id": "C-1779", "topic": "A GC round whose catalog cut predates the `Removing` to `RemovalReady` C", "target": "architecture/garbage-collection", "member_ids": ["B031-019"], "canonical_claim": "A GC round whose catalog cut predates the `Removing` to `RemovalReady` CAS may write the cursor once more; the next round must prune it, and final deletion must revalidate the adopted seal and tolerate this one stale-cut round.", "issue_ids": ["Q1"], "sources": ["docs/superpowers/reports/2026-07-31-removalready-review-fable.md#q1-monotone"]}
{"cluster_id": "C-1780", "topic": "captured-namespace-life", "target": "architecture/garbage-collection", "member_ids": ["B031-053", "B031-059"], "canonical_claim": "Cleanup must capture `NamespaceLifeId` at deposition and never re-derive it from the catalog during a resumed or stale pass. Additionally, A deposited cleanup item stores `NamespaceLifeId` and `remove_txn_id`, and every resumed or stale cleanup pass uses those captured values directly.", "issue_ids": ["D1"], "sources": ["docs/superpowers/reports/2026-07-31-stage-b-strategic-review-fable-round3.md#dies", "docs/superpowers/reports/2026-07-31-task5-removalready-proposal.md#3-fold-and-deposit-cleanup"]}
{"cluster_id": "C-1781", "topic": "Terminal-owned manifests are cleaned from exact `ManifestId` values in t", "target": "architecture/garbage-collection", "member_ids": ["B031-061"], "canonical_claim": "Terminal-owned manifests are cleaned from exact `ManifestId` values in the terminal record after decrements are durable; the removal pass does not LIST and delete the shared manifest namespace prefix.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-31-task5-removalready-proposal.md#4-run-one-bounded-cleanup-attempt"]}
{"cluster_id": "C-1782", "topic": "GC may CAS `Removing` to `RemovalReady` only when the matching cleanup i", "target": "architecture/garbage-collection", "member_ids": ["B031-063"], "canonical_claim": "GC may CAS `Removing` to `RemovalReady` only when the matching cleanup item is durably `Completed`; it must not infer completion merely from item absence.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-31-task5-removalready-proposal.md#5-make-cursor-retirement-monotone"]}
{"cluster_id": "C-1783", "topic": "A `RemovalReady` round does not probe, fold, carry, or recreate the name", "target": "architecture/garbage-collection", "member_ids": ["B031-064"], "canonical_claim": "A `RemovalReady` round does not probe, fold, carry, or recreate the namespace cursor, and removes the matching completed cleanup item and shard-0 cursor together in the adopted seal.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-31-task5-removalready-proposal.md#6-retire-the-cleanup-item-and-cursor"]}
{"cluster_id": "C-1784", "topic": "The `_cleanup` marker can be removed because stale cleanup targets only ", "target": "architecture/garbage-collection", "member_ids": ["B031-066"], "canonical_claim": "The `_cleanup` marker can be removed because stale cleanup targets only the deposited life-qualified ref and `_files` prefixes or exact old terminal-owned manifests, never a namespace-wide prefix that a new life can create.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-31-task5-removalready-proposal.md#stale-cleanup-safety-without-cleanup"]}
{"cluster_id": "C-1785", "topic": "seal-capacity-accounting", "target": "architecture/garbage-collection", "member_ids": ["B031-070", "B042-008"], "canonical_claim": "The catalog admission reservation must cover non-per-namespace seal data such as `blob_target_runs` and `condemned_summary`; entry ordering does not remove those capacity terms. Additionally, The `btr` per-run-segment rows and `cnd` per-gc-shard rows are charged neither in `worstCaseEntryFoldReservationBytes`'s fixed term nor in its per-entry term.", "issue_ids": ["D7"], "sources": ["docs/superpowers/cas/deferred-docs-fixes.md#d7-reservation-doc-coverage", "docs/superpowers/reports/2026-07-31-task5-removalready-proposal.md#capacity-bound"]}
{"cluster_id": "C-1786", "topic": "An absent canonical life is inert debris and must never become a pool-su", "target": "architecture/garbage-collection", "member_ids": ["B031-075"], "canonical_claim": "An absent canonical life is inert debris and must never become a pool-suppressing damage anomaly.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-08-02-cas-stage-b-remaining.md#global-constraints"]}
{"cluster_id": "C-1787", "topic": "After probe A removal, each folding round must perform exactly one full ", "target": "architecture/garbage-collection", "member_ids": ["B031-084"], "canonical_claim": "After probe A removal, each folding round must perform exactly one full `cas/ns/stream/` enumeration; the bounded `cas/ns/` janitor page is counted separately.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-08-02-cas-stage-b-remaining.md#t5"]}
{"cluster_id": "C-1788", "topic": "Per-round GC work caps must leave excess work in the durable pipeline fo", "target": "architecture/garbage-collection", "member_ids": ["B031-090"], "canonical_claim": "Per-round GC work caps must leave excess work in the durable pipeline for later rounds, never drop or re-derive it, and the per-key row/life/lease validation before each exact delete must not be amortized or weakened.", "issue_ids": ["T6-1", "T6-2", "T6-3"], "sources": ["docs/superpowers/plans/2026-08-02-cas-stage-b-remaining.md#t6b"]}
{"cluster_id": "C-1789", "topic": "Orphan-planner budget exhaustion must fail closed by retaining candidate", "target": "architecture/garbage-collection", "member_ids": ["B031-091"], "canonical_claim": "Orphan-planner budget exhaustion must fail closed by retaining candidates, recording a counted retained cause, and leaving the cursor before the first undecided key; every round must either decide at least one candidate or advance the cursor.", "issue_ids": ["T6-1"], "sources": ["docs/superpowers/plans/2026-08-02-cas-stage-b-remaining.md#t6b"]}
{"cluster_id": "C-1790", "topic": "`deletePrefixWholesale` and cleanup families must consume the round budg", "target": "architecture/garbage-collection", "member_ids": ["B031-092"], "canonical_claim": "`deletePrefixWholesale` and cleanup families must consume the round budget, and `snap_pruned_through` may advance only after a prefix is fully drained.", "issue_ids": ["T6-3"], "sources": ["docs/superpowers/plans/2026-08-02-cas-stage-b-remaining.md#t6b"]}
{"cluster_id": "C-1791", "topic": "Injected holds or anomalies must leave every irreversible delete family ", "target": "architecture/garbage-collection", "member_ids": ["B031-097"], "canonical_claim": "Injected holds or anomalies must leave every irreversible delete family inert for their rounds while the round still completes.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-08-02-cas-stage-b-remaining.md#t8"]}
{"cluster_id": "C-1792", "topic": "A missing attribution in orphan nomination must suppress destructive wor", "target": "architecture/garbage-collection", "member_ids": ["B031-106"], "canonical_claim": "A missing attribution in orphan nomination must suppress destructive work rather than select a fallback owner.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-08-02-cas-stage-b-remaining-design.md#t4"]}
{"cluster_id": "C-1793", "topic": "The perpetual dead-life janitor owns namespace-life debris, while the or", "target": "architecture/garbage-collection", "member_ids": ["B032-009"], "canonical_claim": "The perpetual dead-life janitor owns namespace-life debris, while the orphan-manifest sweep owns orphan manifest bytes; both are cleanup mechanisms rather than identity or rebirth gates.", "issue_ids": [], "sources": ["docs/superpowers/cas/2026-08-02-r1-verbatim-file-aliasing-closure.md#physical-empty-lifecycle-proof"]}
{"cluster_id": "C-1794", "topic": "`INV_NO_RETIRE_UNCERTAIN_GRANT` prevents a writer cleanup duty from disa", "target": "architecture/garbage-collection", "member_ids": ["B032-014"], "canonical_claim": "`INV_NO_RETIRE_UNCERTAIN_GRANT` prevents a writer cleanup duty from disappearing while the grant outcome is unknown; successor cleanup remains the seal-derived path for durable precommits left by a dead predecessor.", "issue_ids": [], "sources": ["docs/superpowers/models/2026-08-02-stage-b-task8a-RESULTS.md#writer-cleanup-duties"]}
{"cluster_id": "C-1795", "topic": "cleanup-authority-revalidation-tests", "target": "architecture/garbage-collection", "member_ids": ["B032-034", "B045-074"], "canonical_claim": "Ref cleanup reads the authority from `folded.catalog_cut` rather than re-resolving it, and the `CasRefGcCleanupAuthority` tests cover catalog-token and GC-fence moves before the first delete and between two deletes. Additionally, The `CasRefGcCleanupAuthority` tests pin current-life destructive per-key `deleteExact` revalidation in the GC/cleanup layer; those class-3 catalog reads are not `CasRefLedger.cpp` sites.", "issue_ids": [], "sources": [".superpowers/sdd/2026-08-02-cas-stage-b-remaining/t1a-classification.md#L94-L107", "docs/superpowers/cas/2026-08-02-stage-b-midpoint-audit.md#t6-6-namespace-file-apis-and-hot-path-catalog-gets"]}
{"cluster_id": "C-1796", "topic": "CAS decommissioning rejects a `Removing` row without its `_ckpt` object ", "target": "architecture/garbage-collection", "member_ids": ["B032-037"], "canonical_claim": "CAS decommissioning rejects a `Removing` row without its `_ckpt` object with `CORRUPTED_DATA`, resumes a `Removing` row through `admin->dropNamespace(life)`, and leaves catalog deletion to GC.", "issue_ids": [], "sources": ["docs/superpowers/cas/2026-08-02-stage-b-midpoint-audit.md#t7-commit-exists-and-contains-the-implementation-confirmed"]}
{"cluster_id": "C-1797", "topic": "decommission-gc-round-callback", "target": "architecture/garbage-collection", "member_ids": ["B032-038", "B046-024"], "canonical_claim": "CAS decommissioning requests a GC round after partial progress via a `request_gc_round` callback guarded by `SCOPE_EXIT`, including fail-closed exits. Additionally, When decommission reaches a Removing state before a later namespace fails closed, a SCOPE_EXIT callback preserves the GC liveness signal by invoking request_gc_round; the callback only wakes the serialized GC worker and does not perform catalog work.", "issue_ids": [], "sources": [".superpowers/sdd/2026-08-02-cas-stage-b-remaining/t3-draft-report.md#L155-L177", "docs/superpowers/cas/2026-08-02-stage-b-midpoint-audit.md#t7-commit-exists-and-contains-the-implementation-confirmed"]}
{"cluster_id": "C-1798", "topic": "GC driving uses one leader via `gc", "target": "architecture/garbage-collection", "member_ids": ["B033-119"], "canonical_claim": "GC driving uses one leader via `gc.gc_round` or `gc.gc_drive_round`; callers must not drive both leaders concurrently because that can cause a concurrent-leader reclaim leak.", "issue_ids": [], "sources": ["utils/ca-soak/scenarios/framework/API.md#gc-lifecycle-framework-gc-framework-lifecycle"]}
{"cluster_id": "C-1799", "topic": "In `ca-fsck` vocabulary, `dangling` means referenced but missing and is ", "target": "architecture/garbage-collection", "member_ids": ["B034-002"], "canonical_claim": "In `ca-fsck` vocabulary, `dangling` means referenced but missing and is the INV-NO-LOSS data-loss class, whereas `unreachable`/`awaiting-gc` means present, unreferenced, and awaiting reclamation.", "issue_ids": [], "sources": ["docs/superpowers/cas/BACKLOG.md#round-gc-perf-and-stuck-blobs"]}
{"cluster_id": "C-1800", "topic": "A regular GC round is all-or-nothing", "target": "architecture/garbage-collection", "member_ids": ["B034-075"], "canonical_claim": "A regular GC round is all-or-nothing: `fold` produces one `gc/state` CAS followed by cleanup, and cleanup occurs only after the commit point because a durable `Removed` snapshot must precede deletion of covered logs.", "issue_ids": [], "sources": ["docs/superpowers/cas/BACKLOG.md#gc-throughput-collapse"]}
{"cluster_id": "C-1801", "topic": "`foldManifestEdges` returns false without deltas when a manifest body is", "target": "architecture/garbage-collection", "member_ids": ["B034-083"], "canonical_claim": "`foldManifestEdges` returns false without deltas when a manifest body is absent, and the caller is intended to clamp the table rather than abort the round or advance its cursor.", "issue_ids": [], "sources": ["docs/superpowers/cas/BACKLOG.md#unmatched-minus-one-retention-leak"]}

