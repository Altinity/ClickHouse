You are gathering EVIDENCE (not verdicts) for claims about the ClickHouse
content-addressed storage (CAS) implementation, against the code at HEAD in this
repository (/home/mfilimonov/workspace/ClickHouse/master).

For each cluster below, look at `canonical_claim`. For every backticked
identifier, symbol, setting, function/class/test name, or SQL fragment in the
claim, grep/read src/, tests/, programs/, and utils/ca-soak/ and record:
  - found_at: a list of "path:line~enclosing_symbol" strings where it actually
    appears (copy identifiers exactly as they occur in the claim; do not guess
    or normalize spelling)
  - not_found: identifiers you searched for and could not find anywhere in
    those directories
  - notes: one short sentence of context if useful (e.g. "renamed to X", "only
    referenced in a comment", "matches a TLA+ model file not code")

Do NOT decide done/rejected/stale/open/etc. Only report what you found or did
not find, and where. Search broadly (the identifier may have moved file or
been renamed) before declaring not_found.

Output: append exactly one JSON line per cluster to /home/mfilimonov/workspace/ClickHouse/master/docs/superpowers/cas/consolidation-2026-08/verdicts/evidence/batch-044.jsonl, in the form:
{"cluster_id": "C-NNNN", "found_at": ["path:line~Symbol", ...], "not_found": ["..."], "notes": "..."}

Every cluster_id below must produce exactly one output line. Do not skip any.

Clusters:
{"cluster_id": "C-1412", "topic": "Recovery from a complete stream LIST was rejected because completeness is the pr", "target": "architecture/design-history", "member_ids": ["B027-069"], "canonical_claim": "Recovery from a complete stream LIST was rejected because completeness is the premise under test; recovery performs zero stream LIST calls and uses enumeration only for leak-only GC/janitor discovery.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-27-cas-ref-chain-complete-cut-design.md#alternatives"]}
{"cluster_id": "C-1413", "topic": "ref-layer `materialization_grace` wait is expected to be deleted because arithme", "target": "architecture/design-history", "member_ids": ["B027-149"], "canonical_claim": "The ref-layer `materialization_grace` wait is expected to be deleted because arithmetic recovery no longer trusts listings for completeness and the in-band seal fences late stragglers deterministically; the setting must not remain parsed but inert.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-28-cas-ref-chain-stage-a-streams.md#task-12"]}
{"cluster_id": "C-1414", "topic": "series does not move namespace files into the ref log, add folder-state manifest", "target": "architecture/design-history", "member_ids": ["B028-008"], "canonical_claim": "This series does not move namespace files into the ref log, add folder-state manifests or a `_raw_files` object family, change `MergeTreeDeduplicationLog`, merge `Poisoned` into wedge, refactor `RefApplyState`, or remove the plain-object component.", "issue_ids": [], "sources": ["docs/superpowers/plans/2026-07-28-cas-ref-chain-stage-b-catalog.md#global-constraints"]}
{"cluster_id": "C-1415", "topic": "Adding `INVALID_STATE` to `isRetryableException` was rejected because it would r", "target": "architecture/design-history", "member_ids": ["B029-050"], "canonical_claim": "Adding `INVALID_STATE` to `isRetryableException` was rejected because it would reclassify 18 sites, including eight terminal CA sites and eight non-CA sites; recoding the three selected transient sites to `NETWORK_ERROR` fixes the required behavior with zero unintended reclassifications.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-29-ca-transient-classifier-audit.md#blast-radius"]}
{"cluster_id": "C-1416", "topic": "relink protocol deliberately sends no `ETag`, `If-None-Match`, `If-Modified-Sinc", "target": "architecture/design-history", "member_ids": ["B029-083"], "canonical_claim": "The relink protocol deliberately sends no `ETag`, `If-None-Match`, `If-Modified-Since`, or `If-Match`; standard conditional vocabulary was rejected because an intermediary could evaluate it and synthesize a response, including a forged 304, instead of consulting the sender.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-relink-reoffer-redesign.md#no-standard-conditional"]}
{"cluster_id": "C-1417", "topic": "recovery token bucket, cache-class segmentation, and per-peer fairness machinery", "target": "architecture/design-history", "member_ids": ["B029-111"], "canonical_claim": "The recovery token bucket, cache-class segmentation, and per-peer fairness machinery were rejected as overengineering; only the per-recovery work cap and hard-coded global concurrency limit remain, with cold-confirm share as the revisit tripwire.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-relink-reoffer-redesign.md#rejected"]}
{"cluster_id": "C-1418", "topic": "receiver-side ref verification", "target": "architecture/design-history", "member_ids": ["B029-112", "B030-081"], "canonical_claim": "Receiver-pays verification by having the receiver directly recover or verify the sender's ref state from the pool was rejected: although making the beneficiary pay would remove the DoS class, each confirmation would perform `LIST` plus replay and inherit the listing-trust questions/problems that the v9 chain is intended to remove.", "issue_ids": [], "sources": ["docs/superpowers/cas/2026-07-29-relink-confirm-per-ref-draft.md#decision", "docs/superpowers/specs/2026-07-29-cas-relink-reoffer-redesign.md#rejected"]}
{"cluster_id": "C-1419", "topic": "304/503 response-status encoding was rejected", "target": "architecture/design-history", "member_ids": ["B029-118"], "canonical_claim": "The 304/503 response-status encoding was rejected: `assertResponseIsOk` accepts only 200, 201, 202, 206 and redirects, so 304 and 503 would arrive as exceptions and require shared HTTP-client changes; the protocol keeps tri-state values in explicit headers on 200 responses.", "issue_ids": [], "sources": ["docs/superpowers/specs/2026-07-29-cas-relink-reoffer-redesign.md#status-variant"]}
{"cluster_id": "C-1420", "topic": "bounded re-`LIST` or retry triggered by an out-of-order key cannot replace catal", "target": "architecture/design-history", "member_ids": ["B031-029"], "canonical_claim": "A bounded re-`LIST` or retry triggered by an out-of-order key cannot replace catalog authority because finite scans can agree while omitting an entire namespace and therefore produce no retry trigger.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-31-stage-b-strategic-review-codex.md#verdict"]}
{"cluster_id": "C-1421", "topic": "Deleting a catalog entry immediately after folding the terminal record permits m", "target": "architecture/design-history", "member_ids": ["B031-072"], "canonical_claim": "Deleting a catalog entry immediately after folding the terminal record permits multiple cleanup items for repeated lives of one name and breaks the fold-seal reservation bound.", "issue_ids": [], "sources": ["docs/superpowers/reports/2026-07-31-task5-removalready-proposal.md#rejected-simpler-looking-alternatives"]}
{"cluster_id": "C-1422", "topic": "mount-time one-shot synthetic LIST probe cannot certify a backend because it can", "target": "architecture/design-history", "member_ids": ["B033-076"], "canonical_claim": "A mount-time one-shot synthetic LIST probe cannot certify a backend because it can pass on RustFS even though a rare load- and timing-dependent violation later occurs.", "issue_ids": [], "sources": ["docs/superpowers/cas/2026-08-03-list-trust-verdict.md#probe-limits"]}
{"cluster_id": "C-1423", "topic": "writer-GC simplification review rejected both unchanged-round promote HEAD skipp", "target": "architecture/design-history", "member_ids": ["B035-038"], "canonical_claim": "The writer-GC simplification review rejected both unchanged-round promote HEAD skipping and per-commit pinned-round acknowledgements as unnecessary variants after adopting the `EDGE-BEFORE-OBSERVE` design; the durable precommit edge plus two-phase delete recheck remains the load-bearing safety mechanism.", "issue_ids": [], "sources": ["utils/ca-soak/scenarios/BACKLOG.md#L1617-L1672"]}
{"cluster_id": "C-1424", "topic": "Raw appending of canonical ref names was rejected because `checkCanonicalRefName", "target": "architecture/design-history", "member_ids": ["B035-045"], "canonical_claim": "Raw appending of canonical ref names was rejected because `checkCanonicalRefName` does not reject quotes, control bytes, or U+2028/U+2029; only the fixed vocabulary words `owner` and `tombstone` were proven safe for raw append.", "issue_ids": [], "sources": ["utils/ca-soak/scenarios/BACKLOG.md#L2301-L2381"]}
{"cluster_id": "C-1425", "topic": "intermediate grace-based GC designs were intentionally superseded by the incarna", "target": "architecture/design-history", "member_ids": ["B035-096"], "canonical_claim": "The intermediate grace-based GC designs were intentionally superseded by the incarnation, pin, lease, and fence model; this is treated as intended design history rather than lost durable content.", "issue_ids": [], "sources": ["docs/superpowers/cas/CONSOLIDATION-COVERAGE.md#second-pass-pass"]}
{"cluster_id": "C-1426", "topic": "`INV_RECONCILE_SAFE_BRIEF` is not a valid gate because its proposed state formul", "target": "architecture/design-history", "member_ids": ["B037-011"], "canonical_claim": "`INV_RECONCILE_SAFE_BRIEF` is not a valid gate because its proposed state formula rejects the legitimate transient state where a creator fence is terminal before `_ckpt` creation.", "issue_ids": [], "sources": ["docs/superpowers/models/CaRefCatalogCore_RESULTS.md#carefcatalogcore-non-vacuity"]}
{"cluster_id": "C-1427", "topic": "retired `_sab_fix_ckptwitness` and `_witness_corruptgap` obligations are unconst", "target": "architecture/design-history", "member_ids": ["B037-021"], "canonical_claim": "The retired `_sab_fix_ckptwitness` and `_witness_corruptgap` obligations are unconstructible because `_ckpt.committed_through`, rather than `_ckpt.checkpoint` or a higher `LIST` witness, is now the exact bound and every missing key at or below it creates a hold.", "issue_ids": [], "sources": ["docs/superpowers/models/CaRefDeltaIntakeCore_RESULTS.md#retired-configurations"]}
{"cluster_id": "C-1428", "topic": "Gate 0 is only an availability filter and authorizes nothing", "target": "architecture/design-history", "member_ids": ["B037-073"], "canonical_claim": "Gate 0 is only an availability filter and authorizes nothing; safety belongs to gate 1, so modeling gate 0 would create a false impression of two independent safety sources.", "issue_ids": [], "sources": ["docs/superpowers/models/CaRelinkConfirmCore_RESULTS.md#L292-L299"]}
{"cluster_id": "C-1429", "topic": "EBR GC core was rejected because per-object epoch state did not scale", "target": "architecture/design-history", "member_ids": ["B038-072"], "canonical_claim": "The EBR GC core was rejected because per-object epoch state did not scale; GC uses incarnation-token identity instead.", "issue_ids": [], "sources": ["docs/superpowers/cas/ROADMAP.md#area-architecture"]}
{"cluster_id": "C-1430", "topic": "V1 rejects a generic folder-view API in `IMetadataStorage` because it would broa", "target": "architecture/design-history", "member_ids": ["B039-042"], "canonical_claim": "V1 rejects a generic folder-view API in `IMetadataStorage` because it would broaden the disk API, increase fork conflicts, and still not express CAS-specific freshness rules.", "issue_ids": [], "sources": ["docs/superpowers/cas/cache.md#api-changes-to-avoid-in-v1"]}
{"cluster_id": "C-1431", "topic": "`Cas", "target": "architecture/design-history", "member_ids": ["B039-043"], "canonical_claim": "`Cas::Store` must not own part-folder cache policy because that would mix protocol, metadata filesystem semantics, and operational tuning.", "issue_ids": [], "sources": ["docs/superpowers/cas/cache.md#api-changes-to-avoid-in-v1"]}
{"cluster_id": "C-1432", "topic": "V1 rejects generic path-result caching because the workload is specifically a pa", "target": "architecture/design-history", "member_ids": ["B039-044"], "canonical_claim": "V1 rejects generic path-result caching because the workload is specifically a part/projection folder load window and arbitrary path invalidation would complicate mutable files.", "issue_ids": [], "sources": ["docs/superpowers/cas/cache.md#api-changes-to-avoid-in-v1"]}
{"cluster_id": "C-1433", "topic": "architecture analysis does not recommend merging `CasPoolMeta` and `CasRootsRegi", "target": "architecture/design-history", "member_ids": ["B039-131"], "canonical_claim": "The architecture analysis does not recommend merging `CasPoolMeta` and `CasRootsRegistry` codecs because their create-once and mutable-CAS lifecycles differ.", "issue_ids": [], "sources": ["cas_disks_architecture_analysis.md#semantic-clusters"]}
{"cluster_id": "C-1434", "topic": "v2 rejects ClickHouse CompressedWriteBuffer for CAS bodies and chooses plain sin", "target": "architecture/design-history", "member_ids": ["B040-135"], "canonical_claim": "v2 rejects ClickHouse CompressedWriteBuffer for CAS bodies and chooses plain single-frame zstd so the protocol remains portable outside ClickHouse", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs_proposal_v2.md#compression"]}
{"cluster_id": "C-1435", "topic": "v2 rejects an all-protobuf design because unbounded GC data would lose bounded-m", "target": "architecture/design-history", "member_ids": ["B040-146"], "canonical_claim": "v2 rejects an all-protobuf design because unbounded GC data would lose bounded-memory streaming and ranged seek", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs_proposal_v2.md#alternatives-considered"]}
{"cluster_id": "C-1436", "topic": "v2 rejects FlatBuffers and Cap'n Proto because CAS metadata is small and object-", "target": "architecture/design-history", "member_ids": ["B040-147"], "canonical_claim": "v2 rejects FlatBuffers and Cap'n Proto because CAS metadata is small and object-storage network latency dominates parsing, while the dependencies add no needed portability", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs_proposal_v2.md#alternatives-considered"]}
{"cluster_id": "C-1437", "topic": "v3 rejects v2's framed protobuf design because it keeps the control plane behind", "target": "architecture/design-history", "member_ids": ["B040-182"], "canonical_claim": "v3 rejects v2's framed protobuf design because it keeps the control plane behind bespoke tooling and adds integrity guards whose failures change no decision", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs_proposal_v3.md#alternatives-considered"]}
{"cluster_id": "C-1438", "topic": "v3 rejects Avro and Parquet for cas_run because seek is dead in production, they", "target": "architecture/design-history", "member_ids": ["B040-183"], "canonical_claim": "v3 rejects Avro and Parquet for cas_run because seek is dead in production, they add dependencies, and their container or row-group machinery does not fit two-hash marker records", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs_proposal_v3.md#alternatives-considered"]}
{"cluster_id": "C-1439", "topic": "v3 rejects keeping per-block CRC and footer in cas_run because the seal checksum", "target": "architecture/design-history", "member_ids": ["B040-184"], "canonical_claim": "v3 rejects keeping per-block CRC and footer in cas_run because the seal checksum covers the whole file that all consumers actually read", "issue_ids": [], "sources": ["docs/superpowers/cas/codecs_proposal_v3.md#alternatives-considered"]}
{"cluster_id": "C-1440", "topic": "CAS mechanisms are rejected when they require Keeper for correctness, scale Keep", "target": "architecture/design-history", "member_ids": ["B043-003"], "canonical_claim": "CAS mechanisms are rejected when they require Keeper for correctness, scale Keeper or S3 work with data volume, let a stuck writer stall pool-wide reclamation, can accelerate deletion past safety gates, or couple generic `ReplicatedMergeTree` surfaces to the CA disk implementation.", "issue_ids": [], "sources": ["docs/superpowers/cas/how-we-got-here.md#thesis"]}
{"cluster_id": "C-1441", "topic": "`CasNamespaceFileDiskProfile", "target": "architecture/design-history", "member_ids": ["B045-052"], "canonical_claim": "The `CasNamespaceFileDiskProfile.SteadyStateFileOperationsTouchNoCatalogRefBlobOrManifestKey` test does not exercise the `listNamespaceFiles` arm; direct list-arm coverage is provided separately.", "issue_ids": [], "sources": [".superpowers/sdd/2026-08-02-cas-stage-b-remaining/t1b-review.md#L106-L136"]}

