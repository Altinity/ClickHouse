{"cluster_id": "chunk_012-105", "topic": "The renamed test `04328_reader_executor_modeled_cost_profile_event` verifies `Pr", "canonical_claim": "The renamed test `04328_reader_executor_modeled_cost_profile_event` verifies `ProfileEvents['ReaderExecutorModeledCostMicroseconds'] > 0` in `query_log`; it no longer tests asynchronous metrics."}
{"cluster_id": "chunk_012-106", "topic": "The `ca-fix` agent performs only one small, precisely specified change and must ", "canonical_claim": "The `ca-fix` agent performs only one small, precisely specified change and must stop and escalate when the fix is no longer primitive."}
{"cluster_id": "chunk_012-107", "topic": "A `ca-fix` change that touches more sites than expected or requires a decision m", "canonical_claim": "A `ca-fix` change that touches more sites than expected or requires a decision must be escalated instead of improvised."}
{"cluster_id": "chunk_012-108", "topic": "CAS prose should cite symbols rather than line numbers because symbols survive s", "canonical_claim": "CAS prose should cite symbols rather than line numbers because symbols survive source movement."}
{"cluster_id": "chunk_012-109", "topic": "CAS prose should not carry mutable counts as claims", "canonical_claim": "CAS prose should not carry mutable counts as claims."}
{"cluster_id": "chunk_012-110", "topic": "A test or fence claim must state exactly what the assertion checks and must not ", "canonical_claim": "A test or fence claim must state exactly what the assertion checks and must not imply coverage beyond that check."}
{"cluster_id": "chunk_012-111", "topic": "CAS comments should explain the reason for a non-obvious decision, a complex alg", "canonical_claim": "CAS comments should explain the reason for a non-obvious decision, a complex algorithm, or a non-local invariant, and should not replace clearer code or types."}
{"cluster_id": "chunk_012-112", "topic": "CAS comments must not reference plans, specs, ledgers, BACKLOG entries, review r", "canonical_claim": "CAS comments must not reference plans, specs, ledgers, BACKLOG entries, review rounds, finding IDs, or task numbers; they should preserve the reason without unstable provenance."}
{"cluster_id": "chunk_012-113", "topic": "The `ca-impl` agent treats the written brief as authoritative and asks before im", "canonical_claim": "The `ca-impl` agent treats the written brief as authoritative and asks before implementing when the brief is ambiguous or conflicts with the code."}
{"cluster_id": "chunk_012-114", "topic": "The `ca-impl` agent must disclose deviations from the brief and explain why any ", "canonical_claim": "The `ca-impl` agent must disclose deviations from the brief and explain why any requested work was skipped."}
{"cluster_id": "chunk_012-115", "topic": "The `ca-review` verdict is written to a file and returned completely in the fina", "canonical_claim": "The `ca-review` verdict is written to a file and returned completely in the final message."}
{"cluster_id": "chunk_012-116", "topic": "The docs consolidation project's own risk register names four risks and mitigati", "canonical_claim": "The docs consolidation project's own risk register names four risks and mitigations: docs describing a moving target (mitigated by HEAD-time verification and an experimental-format banner), false done/stale verdicts losing knowledge (mitigated by an evidence requirement plus a sampled adversarial audit at Gate M), map agents summarizing instead of extracting claims (mitigated by a one-claim-per-record rule and the Gate M missed-claim audit), and new docs bloating back into walls of text (mitigated by a per-page style gate with a volume limit and a diagonal-reading review test)."}
{"cluster_id": "chunk_012-117", "topic": "The asynchronous metric `ReaderExecutorModeledCostMsPerRequestedMiB` was removed", "canonical_claim": "The asynchronous metric `ReaderExecutorModeledCostMsPerRequestedMiB` was removed from `ServerAsynchronousMetrics::updateImpl` and `ServerAsynchronousMetrics.h`."}
{"cluster_id": "chunk_012-118", "topic": "The ProfileEvents `ReaderExecutorModeledCostMicroseconds` and `ReaderExecutorReq", "canonical_claim": "The ProfileEvents `ReaderExecutorModeledCostMicroseconds` and `ReaderExecutorRequestedBytes` remain defined even though the asynchronous modeled-cost metric was removed."}
{"cluster_id": "chunk_012-119", "topic": "Repository work by the `ca-fix` agent uses new commits only, never rebases, amen", "canonical_claim": "Repository work by the `ca-fix` agent uses new commits only, never rebases, amends, or pushes, and stages changed files by explicit path rather than `git add -A`."}
{"cluster_id": "chunk_012-120", "topic": "The `ca-arch` agent writes the complete decision answer to a file as well as ret", "canonical_claim": "The `ca-arch` agent writes the complete decision answer to a file as well as returning it in the final message."}
{"cluster_id": "chunk_012-121", "topic": "The `ca-impl` agent reports status, commit hashes, a one-line test summary, and ", "canonical_claim": "The `ca-impl` agent reports status, commit hashes, a one-line test summary, and concerns, and writes the full report to the dispatched path."}
