{"cluster_id": "chunk_005-299", "topic": "Under the current Stage-A posture, Gc", "canonical_claim": "Under the current Stage-A posture, Gc::runNamespaceJanitorPage passes the suppression flag to NamespaceJanitor::runOnePage, which visits unresolved physical life keys but skips every deleteExact when deletes are suppressed; canonical dead-life residue therefore remains pending until destructive rounds are enabled."}
{"cluster_id": "chunk_005-300", "topic": "parseNamespaceFileKey now throws on dirty _files keys", "canonical_claim": "parseNamespaceFileKey now throws on dirty _files keys; NamespaceJanitor::runOnePage catches the exception, records an anomaly, and advances the cursor without clearing page_decided, so malformed keys do not wedge the janitor."}
{"cluster_id": "chunk_005-301", "topic": "A rejected precommit body can be nominated and physically swept through ", "canonical_claim": "A rejected precommit body can be nominated and physically swept through real Gc::runRegularRoundReclaiming rounds after successor recovery closes the dead epoch with an arithmetic seal; genuine rejection means ChunkFaultBackend::Mode::Unresolved throws before CountingBackend::putIfAbsent."}
{"cluster_id": "chunk_005-302", "topic": "The production default UniversePolicy", "canonical_claim": "The production default UniversePolicy::kDefault is StageA_Suppressed; a suppressed runRegularRound() does not call planManifestCursorPage, emits an orphan_sweep phase with listed == 0, deleted == 0, and suppressed == 1, and leaves the candidate manifest intact."}
{"cluster_id": "chunk_005-303", "topic": "runFsckImpl classifies Creating, Live, and Removing catalog entries as p", "canonical_claim": "runFsckImpl classifies Creating, Live, and Removing catalog entries as protecting a physical candidate when CatalogLifeIndex::resolve returns a value, because the reverse index includes every catalog entry regardless of state."}
{"cluster_id": "chunk_005-304", "topic": "The orphan-sweep watermark floor is derived from the namespace's own pre", "canonical_claim": "The orphan-sweep watermark floor is derived from the namespace's own prefix segments and a live mount lease; a namespace rooted outside the pool's server_root_id is permanently ineligible for sweeping regardless of epoch or coverage."}
{"cluster_id": "chunk_005-305", "topic": "After probe-A deletion, `Gc", "canonical_claim": "After probe-A deletion, `Gc::RefScanSummary` retains `changed_shards`, `keys`, `listed_lives`, `logs_by_life`, and `max_log_by_life` because each field has consumers outside `sampleRefListQuality`."}
{"cluster_id": "chunk_005-306", "topic": "The frontier instrumentation adds no backend requests", "canonical_claim": "The frontier instrumentation adds no backend requests; its normal per-namespace cost is one `uint8_t` store, and formatting occurs only inside the already-suppressed path when the deficit is nonzero."}
{"cluster_id": "chunk_005-307", "topic": "When the graduation or redelete budget is exhausted, `settleEntry` carri", "canonical_claim": "When the graduation or redelete budget is exhausted, `settleEntry` carries the entry in `still_retired` unchanged: a condemned entry remains condemned, and a `delete_pending` entry remains `delete_pending` for a later round."}
{"cluster_id": "chunk_006-001", "topic": "One GcRoundWorkBudget is constructed per round", "canonical_claim": "One `GcRoundWorkBudget` is constructed per round in `Gc::runRegularRound` and shared by sequential shard folds; it is passed to all production `foldDeltasIntoGeneration` calls, while the rebuild path remains unbudgeted because it has no graduation or `delete_pending` inputs."}
{"cluster_id": "chunk_006-002", "topic": "deletePrefixWholesale reports out_fully_drained true only when", "canonical_claim": "`deletePrefixWholesale` reports `out_fully_drained = true` only when the LIST reaches its natural end; budget exhaustion reports false, and `pruneSupersededGenerations` advances `snap_pruned_through` only past fully drained prefixes."}
{"cluster_id": "chunk_006-003", "topic": "The post CAS generation prefix hand", "canonical_claim": "The post-CAS generation-prefix hand-off uses a separate budget from ordinary prune work because it is a one-shot event whose only existing backstop after a partial reclaim is `fsck`."}
{"cluster_id": "chunk_006-004", "topic": "The outcome entry cap limits only", "canonical_claim": "The outcome-entry cap limits only the `GcOutcomes` audit-log PUT: `settleEntry` still removes spared entries from `still_retired`, and `CasGcRetiredSpared`, `GcRecheckVerdict`, and `forgetCondemnMarker` run even when the `OutcomeEntry` push is skipped."}
{"cluster_id": "chunk_006-005", "topic": "The round budget implementation introduces no", "canonical_claim": "The round-budget implementation introduces no persisted object kinds and no protocol-step changes; the budget is stack-local, and the one-pass round still commits `gc/state` exactly once."}
{"cluster_id": "chunk_006-006", "topic": "RefPlan changedRows and shouldDeferRound can defer", "canonical_claim": "`RefPlan::changedRows` and `shouldDeferRound` can defer an entire fold round based on a listed tail observation, but the defer path performs no destructive work, advances no coverage cursor, and is bounded by `gc_fold_max_defer_rounds`. The DEFER decision is true exactly when changed-shard count is below `fold_threshold`, no destructive decision is due, and `rounds_since_last_fold` is below `gc_fold_max_defer_rounds`; otherwise the round FOLDs. The GC round DEFER mechanism bounds consecutive deferrals with `MaxDefer`; once the bound is reached, `GBegin` and weak fairness drive `GFold` so `unfolded` eventually returns to `{}`. FOLD and DEFER are mutually exclusive in an idle round: `DeferRound` is enabled only when `~GraduationDue` and its deferral condition hold, while `GBegin` is enabled for graduation or an exhausted honest defer budget."}
{"cluster_id": "chunk_006-007", "topic": "The CaGcDestructiveGateCore model pins UniverseAuthoritative TRUE", "canonical_claim": "The `CaGcDestructiveGateCore` model pins `UniverseAuthoritative == TRUE` because it models the post-flip posture; production's not-yet-knowable-universe posture suppresses destructive work and is covered by `CasGcFrontierGate.HiddenPlusOneInAnUnknownNamespaceIsRefusedByTheProductionDefault`."}
{"cluster_id": "chunk_006-008", "topic": "Normal rounds and healthy REBUILD apply", "canonical_claim": "Normal rounds and healthy `REBUILD` apply every returned runtime retirement before downstream work and proceed only for `{Authoritative, DrainComplete}`; damaged-state `REBUILD` uses a no-parent, zero-catalog-mutation path."}
{"cluster_id": "chunk_006-009", "topic": "The raw cas_gc_maintenance_state object at gc", "canonical_claim": "The raw `cas_gc_maintenance_state` object at `gc/maintenance_state` stores only a bounded opaque janitor cursor and uses one materialized read followed by one expected-token or expected-absence CAS."}
{"cluster_id": "chunk_006-010", "topic": "The maintenance state codec requires cur", "canonical_claim": "The maintenance-state codec requires `cur`, limits decoded cursors to 64 KiB, uses `FormatId::GcMaintenanceState = 25` with `{7, 7}` change points, and caps raw control objects at 512 KiB."}
{"cluster_id": "chunk_006-011", "topic": "Maintenance state CORRUPTED_DATA is classified as", "canonical_claim": "Maintenance-state `CORRUPTED_DATA` is classified as corrupt with its exact token, while future-version and backend failures propagate; CAS conflicts do not retry, reread, or adopt a winner."}
{"cluster_id": "chunk_006-012", "topic": "pre-fold catalog drain", "canonical_claim": "The catalog-only pre-fold drain runs after lease acquisition against the authoritative adopted parent and before `DEFER`, the hot stream LIST, the successor catalog cut, fold, `REBUILD` adoption, or successor seal adoption. A pre-fold deletion requires the complete exact `Removing` row, matching parent cleanup evidence, and no durable hold. A GC invocation performs a catalog-only pre-fold drain after acquiring its lease and before defer, stream enumeration, the fold catalog cut, folding, or successor-seal adoption; it deletes only complete observed `Removing` rows whose matching adopted life row has cleanup evidence and no durable hold."}
{"cluster_id": "chunk_006-013", "topic": "Full catalog tokens require a serial", "canonical_claim": "Full-catalog tokens require a serial rescan after every CAS outcome; for `N` eligible rows the drain costs one adopted-seal GET, at least `N+1` complete catalog GETs, and `N` exact catalog CASes, plus retries for conflict or ambiguity."}
{"cluster_id": "chunk_006-014", "topic": "Healthy REBUILD drains before taking its", "canonical_claim": "Healthy `REBUILD` drains before taking its fresh cut, while missing or undecodable `gc/state` adopts reconstructed authority only, performs zero catalog deletion, and leaves drain work to the next invocation."}
{"cluster_id": "chunk_006-015", "topic": "CaRefPreFoldDrainCore is the sole owner of", "canonical_claim": "`CaRefPreFoldDrainCore` is the sole owner of adopted-parent validation, catalog drain, conclusive CAS resolution, and fresh-cut ordering, and exports an immutable full-catalog token/value pair to the ref-plan consumer boundary."}
{"cluster_id": "chunk_006-016", "topic": "The pre fold witness requires an", "canonical_claim": "The pre-fold witness requires an actual token-advancing exact `Removing` deletion and verifies the drained row is absent from both the consumed cut and the plan."}
{"cluster_id": "chunk_006-017", "topic": "On a sibling upload failure successful", "canonical_claim": "On a sibling upload failure, successful sibling bodies remain ordinary GC-reclaimable debris while the poisoned sibling is never uploaded; result merging does not occur after failure."}
{"cluster_id": "chunk_006-018", "topic": "An unmatched adopted parent opaque life", "canonical_claim": "An unmatched adopted-parent opaque life remains observable but is excluded from the successor seal and does not suppress pool-wide destructive cleanup; a valid manifest candidate can be physically deleted in the same round."}
{"cluster_id": "chunk_006-019", "topic": "Every acquired ordinary GC invocation including", "canonical_claim": "Every acquired ordinary GC invocation, including `DEFER`, runs exactly one independently paced `cas/ns/` janitor page and one later catalog cut while the hot immutable `RefPlan` still uses exactly one `cas/ns/stream/` LIST plus one later catalog cut."}
{"cluster_id": "chunk_006-020", "topic": "Gc runNamespaceJanitorPage owns the bounded page", "canonical_claim": "`Gc::runNamespaceJanitorPage` owns the bounded page, post-page catalog cut, exact GC-fence check, warnings, and `namespace_cleanup` metrics, and never performs a catalog lifecycle transition or contributes to the hot walk plan."}
{"cluster_id": "chunk_006-021", "topic": "The folding path passes its FoldResult", "canonical_claim": "The folding path passes its `FoldResult::suppress_destructive` verdict to the janitor helper, while `DEFER` calls the same helper with deletion suppressed because it has no complete fold verdict."}
{"cluster_id": "chunk_006-022", "topic": "janitor cursor advancement", "canonical_claim": "A janitor page advances its cursor only when its catalog cut is unambiguous, deletion is globally admitted, and no candidate observes fence loss; suppressed, ambiguous, or fence-lost pages retain the old cursor for authoritative retry. Suppression, catalog ambiguity, and fence loss each retain the selected janitor page; fence loss stops subsequent deletes without rolling back earlier exact deletes, and a final fence recheck prevents progress after a retained-only page or authority loss following the last delete."}
{"cluster_id": "chunk_006-023", "topic": "Malformed keys NotFound and exact token", "canonical_claim": "Malformed keys, `NotFound`, and exact-token mismatch are decided per key and may advance the janitor page, while corrupt or backend-rejected cursor reset behavior remains exact-token based."}
{"cluster_id": "chunk_006-024", "topic": "The namespace janitor retains current Creating", "canonical_claim": "The namespace janitor retains current `Creating`, `Live`, and `Removing` life identifiers through the catalog reverse index; an absent identifier is a deletion candidate, duplicate current identifiers suppress the whole page, and malformed keys are reported and skipped."}
{"cluster_id": "chunk_006-025", "topic": "Corrupt or oversized janitor progress performs", "canonical_claim": "Corrupt or oversized janitor progress performs no LIST or delete and is reset only against its exact token; a backend-rejected cursor is likewise reset exactly before the error is surfaced, while cursor CAS conflicts are ignored."}
{"cluster_id": "chunk_006-026", "topic": "A fresh NamespaceJanitor resumes a durable", "canonical_claim": "A fresh `NamespaceJanitor` resumes a durable partial cursor and resets it at end-of-tree; LIST omissions defer work to a later cycle rather than coupling lifecycle progress to physical visibility."}
{"cluster_id": "chunk_006-027", "topic": "The current GC phase named namespace_cleanup", "canonical_claim": "The current GC phase named `namespace_cleanup` is a perpetual janitor phase and is not retired marker residue."}
{"cluster_id": "chunk_006-028", "topic": "Parsed listed keys can nominate malformed", "canonical_claim": "Parsed listed keys can nominate malformed or foreign stream objects for diagnostics or leak cleanup, but they can never install table state."}
{"cluster_id": "chunk_006-029", "topic": "Gc rebuildBaseline must pass the immutable", "canonical_claim": "`Gc::rebuildBaseline` must pass the immutable catalog cut and exact checkpoint, and a catalog-named life without usable frontier authority is retain/refuse rather than destructive."}
{"cluster_id": "chunk_006-030", "topic": "CasOrphanManifestSweep activeManifestKeys must use exact frontier", "canonical_claim": "`CasOrphanManifestSweep::activeManifestKeys` must use exact-frontier recovery for owner protection and exact replay through `committed_through`; until that authority is available, catalog-named namespaces are retain-only."}
{"cluster_id": "chunk_006-031", "topic": "planRefCleanup documents snapshot durable cursor and", "canonical_claim": "`planRefCleanup` documents snapshot, durable-cursor, and optional-checkpoint boundaries and does not describe cleanup items or a durable `Removed` snapshot."}
{"cluster_id": "chunk_006-032", "topic": "At the unmatched parent_ref_lives branch buildRefWalkPlan", "canonical_claim": "At the unmatched `parent_ref_lives` branch, `buildRefWalkPlan` increments `CasGcUnmatchedAdoptedParentLives`, logs the exact `NamespaceLifePhysicalId`, drops the aggregate row, and emits no plan row."}
{"cluster_id": "chunk_006-033", "topic": "C1 requires loop and heartbeatLoop to", "canonical_claim": "[C1] requires `loop()` and `heartbeatLoop()` to check `isVanished()` at each wake and self-exit, preventing new GC rounds after `Vanished`. The GC scheduler exits at its next wake on `Vanished`, on published `vanished_intent`, and on `IdentityLost`; it does not join from a callback."}
{"cluster_id": "chunk_006-034", "topic": "The modeled GcStoppedAfterForget invariant holds even", "canonical_claim": "The modeled `GcStoppedAfterForget` invariant holds even on idempotent FORGET and post-FORGET `GC START`, because the Admin gate refuses GC on a non-`Live` pool."}
{"cluster_id": "chunk_006-035", "topic": "During Stage A destruction suppression the", "canonical_claim": "During Stage-A destruction suppression, the janitor withholds `deleteExact`; its `namespace_cleanup` metrics record seen keys and a `suppressed` count, so six rounds with zero deletions do not prove permanent orphaning."}
{"cluster_id": "chunk_006-036", "topic": "Context provides getContentAddressedGarbageCollectionLog and getContentAddressedLog getters", "canonical_claim": "`Context` provides `getContentAddressedGarbageCollectionLog` and `getContentAddressedLog` getters for CAS system-log wiring."}
{"cluster_id": "chunk_006-037", "topic": "The SYSTEM query surface includes CONTENT_ADDRESSED_GARBAGE_COLLECTION", "canonical_claim": "The `SYSTEM` query surface includes `CONTENT_ADDRESSED_GARBAGE_COLLECTION`, `CONTENT_ADDRESSED_GC_REBUILD`, and `CONTENT_ADDRESSED_DROP_POOL_MEMBER`, with access checks and `runContentAddressed*` helpers."}
{"cluster_id": "chunk_006-038", "topic": "Interpreters SystemLog h registers the ContentAddressedGarbageCollectionLog", "canonical_claim": "`Interpreters/SystemLog.h` registers the `ContentAddressedGarbageCollectionLog` and `ContentAddressedLog` system logs."}
{"cluster_id": "chunk_006-039", "topic": "The parser and AST support CONTENT_ADDRESSED_GARBAGE_COLLECTION", "canonical_claim": "The parser and AST support `CONTENT_ADDRESSED_GARBAGE_COLLECTION`, `CONTENT_ADDRESSED_GC_REBUILD`, and `CONTENT_ADDRESSED_DROP_POOL_MEMBER`, including the `content_addressed_gc_rebuild_force` field and round-trip parser coverage."}
{"cluster_id": "chunk_006-040", "topic": "The magic_enum enum_range for ASTSystemQuery Type", "canonical_claim": "The `magic_enum::enum_range` for `ASTSystemQuery::Type` must be widened to `[0,255]` because the added CAS `SYSTEM` query types exceed the default range."}
{"cluster_id": "chunk_006-041", "topic": "A build heartbeat is durable in", "canonical_claim": "A build heartbeat is durable in the pool before the first blob PUT via the pool-wide gc/hb pulse plus the writer's mount object build-watermark floor; there is no separate builds/<build_id> object in the current layout."}
{"cluster_id": "chunk_006-042", "topic": "Regular GC deletion outcomes are classified", "canonical_claim": "Regular GC deletion outcomes are classified as deleted | absent | replaced (412 = resurrection won) | spared, and retire entries are dropped only on confirmed outcomes."}
{"cluster_id": "chunk_006-043", "topic": "resurrect-reupload token handling", "canonical_claim": "The force-Read override HEADs only fire from `closeBlob` on hashes touched in the current fold window (`cur_edges == 0`); `settleRetiredBelow`'s quiescent-entry path never issues a HEAD and must not be changed to do so. The resurrect-reupload fix keys blob condemnation on `(hash, current token)` instead of only on the hash, and re-condemns a current token when a touched hash has net in-degree zero and a prior retired token differs. The resurrect-reupload fix makes `closeBlob` HEAD the current token when a prior retired entry reaches zero in-degree and condemns the current token if it differs from the retired token, matching `CaIncarnationCore`'s `GRetire` behavior. The resurrect-replaced repair adds one HEAD only for a touched condemned transition; quiescent graduation through `settleRetiredBelow` takes no additional HEAD."}
{"cluster_id": "chunk_006-044", "topic": "On a resurrect supersede the fold", "canonical_claim": "On a resurrect supersede the fold keeps exactly one retired entry per hash: the stale entry is dropped, not kept alongside the fresh re-condemn, and the retired list stays hash-sorted."}
{"cluster_id": "chunk_006-045", "topic": "Sharded GC correctness was validated by", "canonical_claim": "Sharded-GC correctness was validated by S15: identical oracle checksums and `dangling=0` under both `gc_shards=1` (default) and `gc_shards=2`, with reducer memory flat (0.60->0.61 GB) and orphan backlog fully drained in both configurations."}
{"cluster_id": "chunk_006-046", "topic": "Under suppress_destructive both the manifest_deletes phase", "canonical_claim": "Under `suppress_destructive`, both the `manifest_deletes` phase and the orphan sweep are gated closed: `mf_cleanup_now` is an empty map, and the orphan-sweep delete loop consumes `folded.orphan_sweep.nominations`, which is empty because `planManifestCursorPage` is only invoked when `!suppress_destructive`."}
{"cluster_id": "chunk_006-047", "topic": "The frontier round s universe is", "canonical_claim": "The frontier round's \"universe\" is exactly every `Live`/`Removing` row of the round's own frozen catalog cut and nothing else; an older comment defining it as \"hint ∪ sealed cursors ∪ catalog Live/Removing entries\" is stale and contradicts the code, which implements only the catalog-cut definition."}
{"cluster_id": "chunk_006-048", "topic": "GC s raw baseline rebuild condemns", "canonical_claim": "GC's raw baseline rebuild condemns nothing: it only rebuilds cursors and edges. An earlier version ended the pass by LISTing `blobs/` and condemning every physically-present body outside the rebuilt edge set, but this was found unsafe (r5-finding-4) because both legs of the traversal (owner replay and the trimmed-but-live manifest pass) are listing-driven, so a store that omits a durable key from one enumeration hides a live owner while the same pass condemns the blob that owner pins — an observed real failure mode (the `0x1430c` shape), not a hypothetical."}
{"cluster_id": "chunk_006-049", "topic": "Patch parts lightweight deletes are first", "canonical_claim": "Patch parts / lightweight deletes are first-class GC reachability roots: a patch manifest references the base part's patched data columns plus its own delta blobs, and while a patch ref is active it transitively keeps alive both its own delta blobs and the base blobs it references, even if the base part is otherwise superseded, provided the MARK walk treats patch manifests as roots. When a patch is materialized into the base via merge, the patch ref is released and its uniquely-owned delta blobs become sweepable. Patch-on-content-addressed-base is flagged as not yet fully model-checked in the v3 design's residual-risk list."}
{"cluster_id": "chunk_006-050", "topic": "A stateless ref less reader that", "canonical_claim": "A stateless/ref-less reader that attaches by reading the catalog and registers no `/parts` ref is invisible to the per-process `use_count()==1` MARK-union fence, which is a valid cross-node fence only for replicas holding a `/parts` ref; owning replicas can release refs after `old_parts_lifetime` (480s) mid-SELECT, sweeping the manifest/blobs out from under the read. The preferred fix is an ephemeral reader pin (a Keeper node or equivalent) created at query start naming the parts/snapshot read, auto-released on session end/crash; this is distinct from the lost-replica timeout, which is a genuine leak needing a separate long-dead-replica reaping rule."}
{"cluster_id": "chunk_006-051", "topic": "clamp suppression", "canonical_claim": "`CaGcAckFloorCore.tla`'s `SabotageClampNoSuppress` config, which removes the `~clampedL` graduation guard, reproduces the 2026-07-03 night incident of 31 dangling blobs as an `INV_NO_DANGLE` counterexample; the shipped clamp-suppression guard (`suppress_destructive` gating `Gc::graduationDue`, `CasGc.cpp:1360`) blocks graduation and pending deletes while any shard is clamped, so held refs persist in `landed` until a later clamp-free pass consumes them. If any fold pass records a clamp anomaly, destruction is suppressed: no graduations or redeletes occur, pending entries carry `UNCHANGED`, condemn/spare continue, and deletes resume on the first clamp-free pass; the pass emits a warning and increments `CasGcClampSuppressedPasses`."}
{"cluster_id": "chunk_006-052", "topic": "putDeterministicArtifact is the write once helper", "canonical_claim": "`putDeterministicArtifact` is the write-once helper for GC objects whose bytes are a pure function of their inputs (source-edge runs, fold seals): a `putIfAbsent` precondition failure is resolved by reading the existing object back and comparing bytes — byte-equal means the writer's own replay landed and is adopted as a no-op, while divergent bytes throw `CORRUPTED_DATA` as an impossible case. It is deliberately not used for observation-bearing artifacts like GC outcome logs, which use first-durable-write-wins byte-adopt semantics instead, and never for blob bodies because the fresh-tag rule makes two attempts of the same logical create legitimately differ."}
{"cluster_id": "chunk_006-053", "topic": "In GC sharding a shard with", "canonical_claim": "In GC sharding, a shard with an empty delta bucket and no condemned entries in the parent summary copies the parent's run references verbatim (\"pure carry\", zero run I/O); a missing parent summary entry on a non-fresh pool is `CORRUPTED_DATA`, never silently treated as zero."}
{"cluster_id": "chunk_006-054", "topic": "generation pruning", "canonical_claim": "Generation pruning (GC phase 13) keeps the last `gc_snap_generations_to_keep` generations (default 3; 0 keeps everything for forensics) and deletes wholesale by LIST-and-delete-everything under the generation prefix, bounded to 64 generations per round. A generation still referenced by the live seal is skipped but the cursor still advances past it regardless, so leak-freedom depends entirely on the post-CAS hand-off reclaim in phase 14. Implemented retention skips at generation granularity and advances `snap_pruned_through` over every visited generation; when a reference moves off a skipped generation, hand-off deletes the whole `gc/gen/<g>/` prefix rather than one run, preventing leaks of the rest of that generation's artifacts."}
{"cluster_id": "chunk_006-055", "topic": "namespace removal cleanup", "canonical_claim": "Namespace removal has no physical-empty handshake: the writer changes the catalog row from `Live` to `Removing`, appends the exact ref removals plus a `RemoveNamespace` op, and deletes nothing itself. GC's fold attaches cleanup evidence to that opaque life row, and only a later GC invocation's pre-fold drain exact-CAS-deletes the matching `Removing` catalog row before any successor plan can be published; the perpetual namespace janitor and orphan-manifest sweep reclaim the physical debris independently, and a same-name birth waits only for the catalog row to disappear. Namespace removal deletes lifecycle metadata only; it has no `suppress_destructive` input and performs no physical deletion, leaving `_ckpt`, stream objects, `_files`, and other dead-life objects to the perpetual `cas/ns/` janitor. Catalog deletion does not require physical emptiness: `_ckpt`, stream, `_snap`, and `_files` residue may remain for the perpetual janitor or a family-specific sweep. Lifecycle physical cleanup is not part of the pre-fold drain: `suppress_destructive` does not govern it, and the perpetual `cas/ns/` janitor plus orphan-manifest sweep own physical debris. Removal completion performs no namespace-owned physical LIST, physical delete, physical-empty proof, or second deletion window; `_ckpt`, stream, and namespace-file residue is leak-only janitor work, while orphan manifests remain the orphan sweep's work."}
{"cluster_id": "chunk_006-056", "topic": "Attribution of the night s 11", "canonical_claim": "Attribution of the night's 11 concurrent GC clamps was: RustFS returned false 404s on HEAD during a metacache storm caused by the `rustfs#3231` overwrite-leak's directory bloat (00:37-00:55); the clamps rose with the storm and self-released when it subsided (~01:01), and the bodies the false 404s claimed were missing had never actually been deleted (no `tree_delete` for the pinned manifests). The full causal chain is: leak -> directory bloat -> metacache storm -> false 404s on HEAD -> GC clamps -> (pre-fix) graduation proceeding over unfolded landed events -> dangling blobs."}
{"cluster_id": "chunk_006-057", "topic": "CAS probe A the ref scan", "canonical_claim": "CAS probe A (the ref-scan disagreement detector) reports each hole with the ref-log id and its direction: holes missing from the fold's own (second) scan are explainable by concurrent deletion, but holes missing from the pre-fold (first) scan are not — the object existed at the second walk and its id sits below what the first walk had already observed, so the first walk should have returned it."}
{"cluster_id": "chunk_006-058", "topic": "The Stage B strategic review concentration", "canonical_claim": "The Stage B strategic-review concentration proposal's replacement namespace-removal sequence is: (1) catalog transitions Live to Removing; (2) the fenced owner appends the terminal record; (3) GC folds the terminal record and performs one bounded cleanup pass; (4) GC writes a stable retirement marker derived only from RootNamespace; (5) GC durably publishes a fold seal with that namespace's cursor pruned; (6) GC exact-deletes `_ckpt`; (7) catalog entry deletion happens last."}
{"cluster_id": "chunk_006-059", "topic": "Ordering the retirement marker before _ckpt", "canonical_claim": "Ordering the retirement marker before `_ckpt` deletion means even catalog loss cannot turn a namespace-removal window into a fresh birth; creation must check both the retirement marker and the namespace's `_ckpt` by exact key before admission and refuse if either exists; a listing omission can leak old objects but cannot make a retired namespace visible again because it is never re-admitted."}
{"cluster_id": "chunk_006-060", "topic": "Even under the no namespace reuse", "canonical_claim": "Even under the no-namespace-reuse simplification, catalog authority, the `Creating` publication exclusion, the `Removing` positive-ownership exclusion, the exact-next frontier probe, durable holds, and the delete-site in-degree re-read must not be cut, because they prevent data loss independently of rebirth; a temporary \"refuse while old objects survive, retry after GC\" rule does not substitute for them since LIST cannot certify old-object absence."}
{"cluster_id": "chunk_006-061", "topic": "C2 s widening past its three", "canonical_claim": "C2's widening past its three originally-named sites is justified because `marker_key` is the guard that the physical manifest and verbatim passes consult, so they cannot run without a resolved life; the still-sentinel-keyed `namespaceFilesPrefix` is correct because every writer of namespace files is also sentinel-keyed, making that layer Task 6's responsibility rather than a C2 leftover."}
{"cluster_id": "chunk_006-062", "topic": "The GC round s exact durable", "canonical_claim": "The GC round's exact durable key layout: `gc/state` (controller state, one per pool, strict fail-closed CAGT-magic proto), `gc/hb` (advisory heartbeat, fixed 24-byte binary), `gc/gen/<g>/attempt/<a>/fold_seal` (write-once fold seal), `gc/gen/<g>/attempt/<a>/.../<round>/<shard>` (retire outcome log), `cas/refs/<ns>/_log/<txn_id>` (ref log txn), `cas/refs/<ns>/_snap/<id>.proto` (ref snapshot)."}
{"cluster_id": "chunk_006-063", "topic": "source-edge reachability set", "canonical_claim": "Blob reachability is source-edge set membership, not a counter: one source edge is (BlobRef, source_id) where source_id = CityHash128(namespace + ref + path), with 0 reserved as the sentinel key for kCondemned rows. The edge set is idempotent under re-fold, so retries and competing attempts produce byte-identical runs. Blob in-degree is derived from a folded set of source-edge deltas keyed by `(kind, hash, source_manifest)` rather than stored as a mutable counter; set semantics can delay reclamation but cannot accelerate deletion."}
{"cluster_id": "chunk_006-064", "topic": "Spared means a condemned blob s", "canonical_claim": "\"Spared\" means a condemned blob's in-degree recovered because a publish re-referenced it; the retired entry is dropped and recovery wins even past the graduation floor. \"Forgotten\" has no live meaning in this subsystem — the memory-noted GForget/GcSnap::forget design is superseded by pruneSupersededGenerations (wholesale generation retention)."}
{"cluster_id": "chunk_006-065", "topic": "There is no crash resume step", "canonical_claim": "There is no crash-resume step in the GC round: the whole round commits in the single `gc/state` CAS at the end, so a crashed pass leaves only attempt-scoped debris under its own unadopted lease.seq, never adopted and later reclaimed by `pruneSupersededGenerations` or the Task-7 hand-off. Every pre-CAS destructive action is justified by previously-published durable state (`delete_pending`), making replay under a fresh attempt idempotent."}
{"cluster_id": "chunk_006-066", "topic": "The orphan manifest sweep gates deletion", "canonical_claim": "The orphan-manifest sweep gates deletion by `prefixEligible`, a durable watermark-only fact (`min_active > build_sequence`, or the retired sentinel `min_active==UINT64_MAX`, or an older epoch; a missing watermark means NOT eligible), and skips any key still in `activeManifestKeys(ns)` (committed + live precommits + manifests removed above the durable fold cursor). It never GETs a condemned body to revive it."}
{"cluster_id": "chunk_006-067", "topic": "Stage B Task 5 s restated", "canonical_claim": "Stage B Task 5's restated invariant: no same-name namespace entry can be created while any GC-held state (cursor, cleanup item, seal row) for the previous life survives; under this ordering, the catalog entry for a dropped table survives until the terminal record folds (a GC-round latency), so `CREATE TABLE` of a just-dropped name is refused — today as a `LOGICAL_ERROR` (`Pool/CasRefCatalog.cpp:236`) — until the next fold, which will burn stateless CI lanes that drop-and-recreate names constantly."}
{"cluster_id": "chunk_006-068", "topic": "Capture at deposition recording NamespaceLifeId at", "canonical_claim": "Capture-at-deposition (recording `NamespaceLifeId` at fold time rather than re-deriving it from the catalog) is mandatory because the stale-leader TLA+ sabotage shows that re-deriving the incarnation after rebirth targets the new life directly, causing data loss."}
{"cluster_id": "chunk_006-069", "topic": "A removed namespace whose only residue", "canonical_claim": "A removed namespace whose only residue is `_files` promotes its cleanup item to `Completed` on the first fold, so the janitor cannot key its reclaim logic off `Pending` state alone."}
{"cluster_id": "chunk_006-070", "topic": "Non per namespace fold seal capacity", "canonical_claim": "Non-per-namespace fold-seal capacity terms (`btr` per run segment, `cnd` per gc-shard) are untouched by the removal-ordering decision and do not disappear along with the Σ-index-set capacity argument."}
{"cluster_id": "chunk_006-071", "topic": "Probe A detects CAS ref prefix", "canonical_claim": "Probe A detects CAS ref-prefix LIST incompleteness via two independent enumerations per GC round (the pre-fold defer scan and fold's own enumeration); it compares per-namespace id sets and reports a hole only when the id is below the OTHER enumeration's own maximum for that namespace, which excludes ordinary in-flight appends between the two walks."}
{"cluster_id": "chunk_006-072", "topic": "Probe A has two documented blind", "canonical_claim": "Probe A has two documented blind spots it does not catch: a hole that reproduces identically in both enumerations, and a namespace dropped wholesale from one enumeration (no `ref_tables` entry, so the comparison loop never visits it); widening the witness rule to use the pre-scan's own maximum was considered and rejected because it would permanently block the cursor on a namespace whose logs were legitimately cleaned between the walks."}
{"cluster_id": "chunk_006-073", "topic": "stale_edge_verdict returns unchecked on any partial", "canonical_claim": "`stale_edge_verdict` returns `unchecked` on any partial scan, with positive findings still checked first, because a partial scan can only weaken a proof of absence, never invalidate evidence of presence."}
{"cluster_id": "chunk_006-074", "topic": "The chosen fix Fix 4 for", "canonical_claim": "The chosen fix (Fix 4) for the deposed-leader stray-Clean defect makes GC freshness metadata add-only: GC never transitions Condemned to Clean on a spare (the spare-side `clearSparedMeta` call and helper are removed); only a writer that has displaced the body with a fresh incarnation token publishes Clean, via `uploadFromSource` + `writeResurrectMetaClean`/`copyForwardFromCondemned`. Accepted cost: a spared hash stays Condemned until a writer resurrects it (write amplification on recurring-hash churn); normal reads never consult the meta so this is safe."}
{"cluster_id": "chunk_006-075", "topic": "The hot cold layout split changed", "canonical_claim": "The hot/cold layout split changed GC discovery to GET gc/state + LIST cas/refs/, making discovery proportional to ref shards rather than manifest backlog."}
{"cluster_id": "chunk_006-076", "topic": "GC runs as a background lease", "canonical_claim": "GC runs as a background lease-holder loop and its safety does not depend on leadership uniqueness; concurrent or stale leaders may duplicate work but must not mis-delete."}
{"cluster_id": "chunk_006-077", "topic": "The advisory GC heartbeat is stored", "canonical_claim": "The advisory GC heartbeat is stored in `gc/hb` as `{owner, hb_seq}` and is advanced independently of round progress to prevent a live slow leader from being falsely stolen."}
{"cluster_id": "chunk_006-078", "topic": "over-protective baseline rebuild", "canonical_claim": "A forced GC rebuild over-protects unowned manifests that are not provably build-dead, preferring bounded leaks over violating `INV_OVER_COUNT_ONLY` through under-counting. GC baseline rebuild is deliberately over-protective rather than under-protective: a build alive across journal trim has no journal evidence, so its manifests look unowned; the rebuild includes edges for every unowned manifest that is not provably build-dead, over-protecting rather than risking under-count. An unowned manifest that later dies with no journal evidence leaks its blob's edges for one bounded rebuild-to-rebuild window, `fsck`-visible as `unaccounted` — preferred to violating `INV_OVER_COUNT_ONLY`."}
{"cluster_id": "chunk_006-079", "topic": "GC safety is independent of the", "canonical_claim": "GC safety is independent of the advisory heartbeat; the heartbeat is the minimal addition needed to prevent a slow leader's frozen round sequence from triggering a false steal."}
{"cluster_id": "chunk_006-080", "topic": "The root local model shows that", "canonical_claim": "The root-local model shows that a target-sharded fold needs one global coordinator fence and paired old-binding `−1` deltas; per-reducer fencing or inferring the old target from the new ref is unsafe."}
{"cluster_id": "chunk_006-081", "topic": "The E3 GC redesign used root", "canonical_claim": "The E3 GC redesign used root-local part manifests and source edges identified by `(ManifestId, path)`, removed the registry, and added a shard-incarnation ABA guard."}
{"cluster_id": "chunk_006-082", "topic": "The E4 GC and writer settlement", "canonical_claim": "The E4 GC and writer settlement uses a one-pass heartbeat ack-floor round, writer point-reads of freshness metadata instead of a `RetireView`, retired rows inside the snapshot run, and a ref snapshot plus append log instead of a mutable root-shard manifest."}
{"cluster_id": "chunk_006-083", "topic": "retired sentinel rows", "canonical_claim": "Retired rows are represented as `kCondemned` sentinel rows inside the snapshot run rather than as a separate retired-list object. Retired entries can ride the source-edge snapshot as condemned-sentinel rows, reducing the GC merge from three cursors to two; a separate incremental LSM snapshot is an independent later optimization."}
{"cluster_id": "chunk_006-084", "topic": "The GC side ack floor one", "canonical_claim": "The GC-side ack-floor one-pass round is current, while the writer-side ack-floor and `RetireView` synchronization was deleted by `55b76af5998`; these are separate mechanisms."}
{"cluster_id": "chunk_006-085", "topic": "one-pass ack-floor GC round", "canonical_claim": "The regular GC round is one pass consisting of a heartbeat ack floor, one `LIST` discovery sweep, a three-cursor fold, pre-CAS deletes, and one `gc/state` CAS. The ack-floor GC redesign replaces O(universe) per-round fence and recheck phases with a merged per-server heartbeat, a current retired list, and a one-pass three-cursor merge. The planned one-pass ack-floor round computes the heartbeat floor before discovery, folds and merges deltas, deletes graduated entries by exact token, writes retired-list objects, and then performs one `gc/state` CAS; `fence` and `recheck` are removed. The ack-floor GC round replaces fence and recheck phases with a three-cursor streaming merge and one `gc/state` CAS; graduation requires `condemn_round < min_ack` and two-phase `delete_pending` handling. The current GC implementation (Gc class, CasGc.h) is a leader-paced ack-floor redesign (spec 2026-07-02): one pass per round consisting of a heartbeat ack floor over MountLease.observed_gc_round, a three-cursor merge fold, two-phase condemn/graduation deletion, and a single gc/state CAS; the older fence_round/recheck terminology survives only as fossil comments/variable names."}
{"cluster_id": "chunk_006-086", "topic": "The GC lease is work de", "canonical_claim": "The GC lease is work de-duplication rather than mutual exclusion: one `gc/state` CAS adopts a round, attempt-scoped artifacts isolate leaders, pre-CAS destructive work uses published state, and exact-token deletes protect newer incarnations."}
{"cluster_id": "chunk_006-087", "topic": "The D1 implementation plan replaces gc", "canonical_claim": "The D1 implementation plan replaces `gc/registry` discovery with `LIST(cas/refs/)` and reclaims empty ref-shard objects so `dropNamespace` no longer leaks a monotone registry and empty shard objects."}
{"cluster_id": "chunk_006-088", "topic": "The D1 design collapses the prior", "canonical_claim": "The D1 design collapses the prior fence coordinates into two orthogonal coordinates: immutable shard `incarnation` for identity and birth ordering, plus pool-global GC `round` for the writer retire-view floor."}
{"cluster_id": "chunk_006-089", "topic": "D1 makes LIST cas refs the", "canonical_claim": "D1 makes `LIST(cas/refs/)` the discovery authority, retains ambiguity detection across pages as a fail-closed condition, and removes `gc/registry`, `RootsRegistry`, `rootsRegistryKey`, `ensureRegistered`, the registry-fence sub-step, and the `_registry` cursor branch."}
{"cluster_id": "chunk_006-090", "topic": "dropNamespace appends an explicit tombstone as", "canonical_claim": "`dropNamespace` appends an explicit tombstone as the last journal event after ref removals, and GC deletes the shard only when it is empty, tombstoned, and folded past a completed fence using `deleteExact(rootShardKey(ns, shard), token)`."}
{"cluster_id": "chunk_006-091", "topic": "After registry removal GC fences only", "canonical_claim": "After registry removal, GC fences only discovered shards from `LIST(cas/refs/)`; an absent shard is not minted or fenced, and create ordering is supplied by the precommit gate."}
{"cluster_id": "chunk_006-092", "topic": "The consolidation target explicitly assigns GC", "canonical_claim": "The consolidation target explicitly assigns GC protocol documentation to cover lease/election, rounds, orphan and ref removal, shard reclaim, incarnation, registry removal, attempt-scoped generations, TLA+ references, and S3 budget links."}
{"cluster_id": "chunk_006-093", "topic": "The current retired list design adds", "canonical_claim": "The current retired-list design adds `RetiredEntry::condemn_round`, adds `GcState::retired_refs`, uses `cas_gc_state` version 4, rejects v3 fail-closed, and has `RetireView::refresh` GET `gc/state` followed by each referenced retired-list object without LIST."}
{"cluster_id": "chunk_006-094", "topic": "The redesigned regular GC round targets", "canonical_claim": "The redesigned regular GC round targets O(delta) plus O(servers) S3 requests; its only universe-proportional operation is the existing LIST sweep, which costs O(universe/1000) LIST requests."}
{"cluster_id": "chunk_006-095", "topic": "The retired list is one current", "canonical_claim": "The retired list is one current sorted candidate set referenced by `gc/state.retired_refs`; per-round retired sets keyed by `retiredKey(...)` and the multi-round `RetireView` fetch protocol are removed."}
{"cluster_id": "chunk_006-096", "topic": "gc state removes fence_version adds current", "canonical_claim": "`gc/state` removes `fence_version`, adds current retired-list references and checksums, and is updated with one CAS per pass containing the round, generation adoption, retired references, and coverage."}
{"cluster_id": "chunk_006-097", "topic": "fence_round is no longer bumped every", "canonical_claim": "`fence_round` is no longer bumped every round; it remains only as the birth floor stamped at root-shard creation for THM-NO-RETURN newborn ordering."}
{"cluster_id": "chunk_006-098", "topic": "Graduation is two phase a pass", "canonical_claim": "Graduation is two-phase: a pass marks fresh graduates with `delete_pending = true` and keeps them published, while `deleteExact` runs only for entries already pending in the previous published list."}
{"cluster_id": "chunk_006-099", "topic": "Snapshot run streaming scope includes T2", "canonical_claim": "Snapshot-run streaming scope includes T2 streaming reads and T0 reference-parent runs for empty-delta shards; T1 delta-runs are deferred to a subsequent specification."}
{"cluster_id": "chunk_006-100", "topic": "The rebuild plan uses attempt iteration", "canonical_claim": "The rebuild plan uses attempt iteration over batches of at most `rebuild_edge_budget` edges, feeding the previous attempt's runs back as `prior_runs`; this provides O(budget) memory without new spill/merge classes."}
{"cluster_id": "chunk_006-101", "topic": "GC trim uses mutateShard as a", "canonical_claim": "GC trim uses `mutateShard` as a `WholeShard` queue item, so trim rewrites no longer race writers even though trim still flushes alone."}
{"cluster_id": "chunk_006-102", "topic": "GC Lever A skips the expensive", "canonical_claim": "GC Lever A skips the expensive in-degree snapshot rebuild on rounds with no destructive decision; Lever B, an incremental point-updatable in-degree store, remains out of scope."}
{"cluster_id": "chunk_006-103", "topic": "Using a stale heartbeat acknowledgement for", "canonical_claim": "Using a stale heartbeat acknowledgement for a renewal is conservative for the GC floor because it can only lower `min_ack` and delay graduation; lease expiry is the failure that must be avoided."}
{"cluster_id": "chunk_006-104", "topic": "The phase 4 GC optimization skips", "canonical_claim": "The phase-4 GC optimization skips unchanged namespaces so an idle round approaches O(servers) reads and a small-delta round costs O(delta), with a mandatory TLA+ proof that owner-removal events are never skipped."}
{"cluster_id": "chunk_006-105", "topic": "The dangling precommit fix uses a", "canonical_claim": "The dangling-precommit fix uses a fold-side, watermark-keyed force-Read: `ShardCoverage` records the minimal live precommit binding, and `computeDiscoverDecisions` overrides `Skip` when that precommit is provably dead."}
{"cluster_id": "chunk_006-106", "topic": "Build abandon retires build_seq after its", "canonical_claim": "Build::abandon retires build_seq after its precommit-removal CAS, while alive is set false early."}
{"cluster_id": "chunk_006-107", "topic": "Graduation is re keyed from condemn_round", "canonical_claim": "Graduation is re-keyed from condemn_round < min_ack to condemn_round < current_round, with min_ack removed from the GC fold interface."}
{"cluster_id": "chunk_006-108", "topic": "After v3 the retired list is", "canonical_claim": "After v3, the retired list is GC-private: writers make condemned decisions through per-hash `.meta` point reads and do not read `RetiredSet`."}
{"cluster_id": "chunk_006-109", "topic": "Retired in snapshot preserves settlement semantics", "canonical_claim": "Retired-in-snapshot preserves settlement semantics including condemn, carry, round-paced graduation, `delete_pending`, redelete, clamp suppression, the pre-CAS single delete site, resurrect-supersede, `.meta` writes, B170 events, and GC-log counters."}
{"cluster_id": "chunk_006-110", "topic": "The two cursor merge settles a", "canonical_claim": "The two-cursor merge settles a pending row by exact-token redelete at d = 0, spares any row at d > 0, graduates a nonpending row when `condemn_round < current_round`, and otherwise carries it unchanged."}
{"cluster_id": "chunk_006-111", "topic": "In place journal trimming was replaced", "canonical_claim": "In-place journal trimming was replaced by cleanup of `_log` and superseded `_snap` objects only when both durable cursor coverage and observed snapshot coverage exist."}
{"cluster_id": "chunk_006-112", "topic": "CAS listing and exact delete handling", "canonical_claim": "CAS listing and exact-delete handling is intended to share a callback-based `forEachListedKey` iterator and a `classifyDeleteOutcome` classifier across GC, fsck, and orphan-manifest cleanup."}
{"cluster_id": "chunk_006-113", "topic": "GcState fence_seq is write only vestigial", "canonical_claim": "`GcState::fence_seq` is write-only vestigial state because GC outcome and retire keys use `(snap_generation, snap_attempt)`; its proto field is removed and field number 3/name `fence_seq` are reserved."}
{"cluster_id": "chunk_006-114", "topic": "ShardCoverage folded_cursor and ShardCoverage incarnation have", "canonical_claim": "`ShardCoverage::folded_cursor` and `ShardCoverage::incarnation` have no production readers and are removed from the fold-seal format while the `ShardIncarnation` type itself is retained."}
{"cluster_id": "chunk_006-115", "topic": "GC fold body reads should size", "canonical_claim": "GC fold body reads should size each fresh `ReadBufferFromS3` from the known listed/meta body size plus small slack, capped at the current default; the expected effect is approximately 99% less fold-buffer churn with no behavior change."}
{"cluster_id": "chunk_006-116", "topic": "Manifest debris must be drained before", "canonical_claim": "Manifest debris must be drained before deleting the victim mount slot because deleting the mount body removes `floorForNamespace` watermark authority and would make the debris ineligible forever; the decommission command is the epoch authority and can sweep old-epoch prefixes."}
{"cluster_id": "chunk_006-117", "topic": "The rev 6 GC defense audit", "canonical_claim": "The rev.6 GC-defense audit treats zero S3 requests spent fighting a foreign writer on hot paths as the compliance principle; incidental checks and CAS-linearized commits are compliant by definition."}
{"cluster_id": "chunk_006-118", "topic": "A new GC leader starts lease", "canonical_claim": "A new GC leader starts lease observation afresh, delaying fencing by one round without weakening safety because fencing is a liveness and cleanup mechanism."}
{"cluster_id": "chunk_006-119", "topic": "The rev 6 GC defense audit", "canonical_claim": "The rev.6 GC-defense audit keeps the `gc/state` CAS, orphan-sweep prior-epoch gate, and fold-lag machinery because they are GC or intra-node mechanisms rather than foreign-writer defenses."}
{"cluster_id": "chunk_006-120", "topic": "The recommended root cause fix for", "canonical_claim": "The recommended root-cause fix for the fetch-by-relink residual is a liveness re-check at the GC pre-CAS `deleteExact` site rather than restoring blanket promote-time HEAD and `loadMeta` probes."}
{"cluster_id": "chunk_006-121", "topic": "Reader retention is implemented as incremental", "canonical_claim": "Reader retention is implemented as incremental removal deferral rather than refolding the entire pinned closure each GC round."}
{"cluster_id": "chunk_006-122", "topic": "Fetch by relink handoff pins and", "canonical_claim": "Fetch-by-relink handoff pins and reader snapshot pins share only the GC retention-overlay primitive and are independently shippable features."}
{"cluster_id": "chunk_006-123", "topic": "Any future 5 revival must keep", "canonical_claim": "Any future §5 revival must keep spare tombstones uncleared until a writer resurrects the blob with a fresh token."}
{"cluster_id": "chunk_006-124", "topic": "CAS runs use sorted NDJSON with", "canonical_claim": "CAS runs use sorted NDJSON with no blocks, footer, or `seek`; `RunFileReader::seek`, `inDegreeInGeneration`, and `SourceEdgeKeyCodec::seekPrefix` are deleted because production consumers read runs sequentially, accepting approximately twice the bytes with re-binarizing `cas_run` as the localized fallback."}
{"cluster_id": "chunk_006-125", "topic": "cas_gc_outcomes is not deterministic it uses", "canonical_claim": "`cas_gc_outcomes` is not deterministic: it uses idempotent `putIfAbsent`, and replay adopts an existing durable object after decoding rather than treating byte inequality as a correctness failure."}
{"cluster_id": "chunk_006-126", "topic": "Only RunKind SourceEdge is live for", "canonical_claim": "Only `RunKind::SourceEdge` is live for standalone `cas_run`; `BlobDelta` and `TargetShardDelta` are dead, while `ManifestEntries` remains only for the embedded part-manifest stream."}
{"cluster_id": "chunk_006-127", "topic": "The production fold uses a hand", "canonical_claim": "The production fold uses a hand-written two-cursor merge of the prior run stream and scattered deltas; no k-way line-based merger is introduced."}
{"cluster_id": "chunk_006-128", "topic": "The superseded v1 protocol proposed a", "canonical_claim": "The superseded v1 protocol proposed a build-owned pin at `gc/fetch-pins/<receiver_srid>/<build_id>` containing the fetched part's blob-ref closure, created by the sender before releasing its part."}
{"cluster_id": "chunk_006-129", "topic": "Review finding 2 is by design", "canonical_claim": "Review finding №2 is by design at the harm level: unconditional `resurrectStaged` is safe because content-addressed payloads are byte-identical, promote dependencies are tokenless-on-ref, and fresh tags defeat stale exact-token deletes."}
{"cluster_id": "chunk_006-130", "topic": "CasDecommission ManifestDebrisPhaseFailuresWarnAndContinue should inject S3_ERROR not", "canonical_claim": "`CasDecommission.ManifestDebrisPhaseFailuresWarnAndContinue` should inject `S3_ERROR`, not `LOGICAL_ERROR`, because `sweepNamespace` catches `Exception` without inspecting the error code and the test models unavailable backend protection-view access rather than an invalid invariant."}
{"cluster_id": "chunk_006-131", "topic": "The sound S31 completeness check compares", "canonical_claim": "The sound S31 completeness check compares `previewDeletes` with the same-instant `fsck.pending_gc` and delete-pending classes, not cumulative `deleted_total`; no product-code change is required."}
{"cluster_id": "chunk_006-132", "topic": "The skipped transaction detector must detect", "canonical_claim": "The skipped-transaction detector must detect the effect that a fold cursor advanced past a ref-log transaction that was never applied, rather than assume that the cause was a hole in a `LIST` page."}
{"cluster_id": "chunk_006-133", "topic": "Probe A compares the two existing", "canonical_claim": "Probe A compares the two existing full enumerations of `cas/refs/` instead of performing another scan, so it adds a set comparison and no backend I/O."}
{"cluster_id": "chunk_006-134", "topic": "GcRoundLogRecord uses a fresh round_id for", "canonical_claim": "`GcRoundLogRecord` uses a fresh `round_id` for every round attempt and stamps it on Start, Phase, and Finish rows; `round` is unsuitable because it is zero on Start, unavailable on non-leader rounds, and known only after a folding CAS."}
{"cluster_id": "chunk_006-135", "topic": "The fix must not assume holes", "canonical_claim": "The fix must not assume holes are page-shaped or trust a particular store, must retain Probe A and its `HEAD` verdict, and should close Probe A's two blind spots with the authoritative chain rather than widen its witness rule."}
{"cluster_id": "chunk_006-136", "topic": "For each ref log GC performs", "canonical_claim": "For each ref log, GC performs one GET of the log body because the transaction must be read; for each edge it performs one HEAD and one GET of the manifest body because the reducer needs the blob list for both `-1` and `+1` edges."}
{"cluster_id": "chunk_006-137", "topic": "parallel GC fetching", "canonical_claim": "Per-log GETs and per-edge manifest GETs are independent fetches; their application order matters, but fetching can be parallelized subject to the existing pool concurrency limit. GC read parallelism is safe because tables fold independently and log reads can be prefetched while prior logs are applied; the ordering constraint applies to folding, not fetching. Parallel GC fetching should be bounded by the number of in-flight manifest bodies and should leave application strictly ordered while fetching log and manifest bodies ahead of use."}
{"cluster_id": "chunk_006-138", "topic": "The bounded walk fix freezes each", "canonical_claim": "The bounded-walk fix freezes each round's work set at the round-start LIST tail: if `tail_listed > cursor`, exact-walk only through `tail_listed`; if equal, skip the namespace; if below, skip and count the stale listing, with no new persistent state."}
{"cluster_id": "chunk_006-139", "topic": "A namespace reaching its frozen LIST", "canonical_claim": "A namespace reaching its frozen LIST tail is not counted as frontier-proven; quiet-namespace probes remain responsible for proving the frontier and detecting persistently hidden new records."}
{"cluster_id": "chunk_006-140", "topic": "The destructive GC round remains sequential", "canonical_claim": "The destructive GC round remains sequential in this series: it adds no `MultiDelete`, batch-delete API, parallel delete fan-out, or delete-side concurrency, so the soak establishes a per-operation baseline for a later optimization round."}
{"cluster_id": "chunk_006-141", "topic": "Probe A is deleted because catalog", "canonical_claim": "Probe A is deleted because catalog-authoritative discovery and LIST-independent recovery make its second full stream LIST irrelevant to correctness; B1/B2 accounting and the mount-time capability probe remain."}
{"cluster_id": "chunk_006-142", "topic": "The Stage A sampled store quality", "canonical_claim": "The Stage-A sampled store-quality detector was intended to suppress only its response: a nonzero `CasGcRefScanDisagreements` does not abort, hold, or gate a round whose ref intake reads exact keys."}
{"cluster_id": "chunk_006-143", "topic": "Task 15 bounded rounds are the", "canonical_claim": "Task 15 bounded rounds are the load-bearing GC scalability fix: making every round terminate restores cadence semantics for downstream work keyed on round completion."}
{"cluster_id": "chunk_006-144", "topic": "The C1 frontier fix removes both", "canonical_claim": "The C1 frontier fix removes both probing and durable holds for an uncataloged namespace because all probe and hold operations are reachable only inside the catalog-resolved loop."}
{"cluster_id": "chunk_006-145", "topic": "A RemovalReady namespace is excluded from", "canonical_claim": "A `RemovalReady` namespace is excluded from GC walking and cursor creation, making cursor absence monotone while the catalog row remains present."}
{"cluster_id": "chunk_006-146", "topic": "Canonical cursors for namespaces absent from", "canonical_claim": "Canonical cursors for namespaces absent from the catalog are retired metadata and should be omitted from the next seal, while the condition is counted and logged rather than used for pool-wide suppression."}
{"cluster_id": "chunk_006-147", "topic": "Writer cleanup duties own precommit owner", "canonical_claim": "Writer cleanup duties own precommit-owner settlement and the active-build-floor pin, not physical body deletion; settlement order is exact `OwnerTransition` append or conclusive absence, then `retireBuildSeq`, then duty removal, with failures retaining the duty for retry."}
{"cluster_id": "chunk_006-148", "topic": "Physical unreachable bodies from Rejected Uncertain", "canonical_claim": "Physical unreachable bodies from `Rejected`, `Uncertain`, and `Durable` attempts remain GC/orphan-sweep-owned; the writer path never exact-deletes a body, and missing attribution suppresses destructive fallback."}
{"cluster_id": "chunk_006-149", "topic": "Enabling production destruction may change only", "canonical_claim": "Enabling production destruction may change only the source of `frontier_complete` from hard-wired false to catalog-proven true; changing any other suppressor term is a protocol error."}
{"cluster_id": "chunk_006-150", "topic": "T6 cannot enable destruction until the", "canonical_claim": "T6 cannot enable destruction until the frontier-attribution verdict is `BENIGN-TRANSIENT` with convergence evidence or a real-gap fix has landed and reproduced a complete frontier."}
{"cluster_id": "chunk_006-151", "topic": "The ref GC subsystem has the", "canonical_claim": "The ref/GC subsystem has the highest documentation drift because Stage B landed on 2026-08-03 after the walkthrough, so current code takes precedence over the walkthrough for those pages."}
{"cluster_id": "chunk_006-152", "topic": "The LIST anomaly had a correct", "canonical_claim": "The LIST anomaly had a correct tail; the lie was omission of predecessors below the returned frontier rather than tail under-reporting."}
{"cluster_id": "chunk_006-153", "topic": "zero-trust LIST discovery", "canonical_claim": "The CAS GC trust chain is catalog cut, `_ckpt` acknowledged frontier, exact `GET` records, and conditional-write fences; LIST is only a hint for work bounds, witness sets, and genesis. CAS GC optimization must retain arithmetic middle intake everywhere, frontier probes for held or logless namespaces, and `tail < cursor` quality detection; only `tail == cursor` can prove a trusted frontier under a bucket passport. Under the amended v9 intake, `LIST` is a zero-trust hint; fold progress advances arithmetically and destructive work requires a per-namespace frontier proof. GC must not trust LIST completeness, independently of whether the object store is RustFS or AWS S3."}
{"cluster_id": "chunk_006-154", "topic": "Trusted LIST frontier mode may skip", "canonical_claim": "Trusted-LIST frontier mode may skip the exact `GET cursor+1` probe only for a per-backend certified store whose LIST is known to include keys completed before the LIST began; the mode must remain opt-in and fail closed to probe-always."}
{"cluster_id": "chunk_006-155", "topic": "A backend independent parallel exact key", "canonical_claim": "A backend-independent parallel exact-key walk can fan out namespace probes and `cursor+1..tail` record GETs over a bounded pool without changing the trust model, while fold application remains ordered or deterministic."}
{"cluster_id": "chunk_006-156", "topic": "The safe response to a GC", "canonical_claim": "The safe response to a GC post-CAS cleanup error is to retain leaked bodies for the orphan-manifest sweep rather than fail the already-committed decision; cleanup errors after `gc/state` CAS are data-safe but can redden the round."}
{"cluster_id": "chunk_006-157", "topic": "Expedited or GDPR deletion is intended", "canonical_claim": "Expedited or GDPR deletion is intended to confirm that no live ref remains under the GC lock and then bypass the normal two-phase graduation delay without changing the layout."}
{"cluster_id": "chunk_006-158", "topic": "A naive GC fold budget is", "canonical_claim": "A naive GC fold budget is unsafe: omitted `+1` edges must suppress deletion rather than permit it, so bounded rounds require durable progress state."}
{"cluster_id": "chunk_006-159", "topic": "The chosen interim response to the", "canonical_claim": "The chosen interim response to the LIST-hole hypothesis was mechanism-agnostic detection: compare two ref-prefix enumerations before advancing a cursor and compare intended-to-fold records with actually applied records; the permanent journal-chain fix remains required before release."}
{"cluster_id": "chunk_006-160", "topic": "attempt-scoped GC artifacts", "canonical_claim": "The concurrent-leader GC fix stores every per-round artifact, including `retired` and `outcomes`, under `gc/gen/<gen>/attempt/<a>/...`, where `<a>` is the folding leader's lease sequence; a lease-guarded adopt CAS publishes `(snap_generation, snap_attempt)`, and readers resolve only that adopted pair. All decision-bearing GC artifacts, including `retired` and `outcomes`, are under `gc/gen/<g>/attempt/<a>/`, and only the adopted attempt is reader-visible."}
{"cluster_id": "chunk_006-161", "topic": "GC cadence should be driven by", "canonical_claim": "GC cadence should be driven by per-shard journal pressure (event count, body size, or age), not changed-shard count, because one hot shard can accumulate thousands of mutations while still counting as one changed shard."}
{"cluster_id": "chunk_006-162", "topic": "To reclaim abandoned precommits on parked", "canonical_claim": "To reclaim abandoned precommits on parked shards, `Gc::computeDiscoverDecisions` force-reads a token-stable shard when its sealed minimal live precommit is dead according to the mount watermark, allowing `reclaimAbandonedPrecommit` to emit the owner removal."}
{"cluster_id": "chunk_006-163", "topic": "The GC wedge fix protects pending", "canonical_claim": "The GC-wedge fix protects pending removals of every owner kind: in `activeManifestKeys`, any removal with `transition_version > sealedFoldCursor` keeps its `old_binding` manifest body active until the removal `-1` is sealed; the sweep also emits a manifest-delete audit event."}
{"cluster_id": "chunk_006-164", "topic": "The B140 dangle fix requires both", "canonical_claim": "The B140 dangle fix requires both `TrimGated`, which limits journal trimming to the committed fold cursor, and `CursorInSnap`, which atomically commits the fold cursor with the snap edges."}
{"cluster_id": "chunk_006-165", "topic": "A DEFER invocation may inspect one", "canonical_claim": "A DEFER invocation may inspect one bounded namespace-janitor page under suppression, but it must retain a valid cursor so the next authorized fold retries that page."}
{"cluster_id": "chunk_006-166", "topic": "The root local part manifest design", "canonical_claim": "The root-local part-manifest design retired the `listedTok` token-diff discovery skip: a LIST match may not authorize skipping a manifest or owner-transition fold read."}
{"cluster_id": "chunk_006-167", "topic": "Indeg is read from the sealed", "canonical_claim": "`Indeg` is read from the sealed `folded` snapshot only; a delta added after the fold cut remains in `unfolded` and is protected by the pending-delta guard rather than by the in-degree check."}
{"cluster_id": "chunk_006-168", "topic": "The CAS worklog s observed S42", "canonical_claim": "The CAS worklog's observed S42 stale-edge defect occurs when a precommit's `+1` edges fold into in-degree, allocation then aborts, and the orphan-manifest sweep deletes the now-unowned manifest without decrementing those folded edges."}
{"cluster_id": "chunk_006-169", "topic": "The ref delta intake has no", "canonical_claim": "The ref-delta intake has no quiet or expected-next 404 frontier: every catalog target must resolve through its exact CTE."}
{"cluster_id": "chunk_006-170", "topic": "GFold and GSettle are separate steps", "canonical_claim": "`GFold` and `GSettle` are separate steps, so a `+1` durable after a round's fold cut is invisible to that round's settlement."}
{"cluster_id": "chunk_006-171", "topic": "Run file streaming is complete RunFileReader", "canonical_claim": "Run-file streaming is complete: `RunFileReader` uses ranged reads through `CasObjectStorageBackend::get` and `getStream`, and the whole-run `full` member was removed; delta-runs and compaction remain desirable follow-up work."}
{"cluster_id": "chunk_006-172", "topic": "The setting gc_round_manifest_cleanup_budget was removed entirely", "canonical_claim": "The setting `gc_round_manifest_cleanup_budget` was removed entirely because bounding the one-shot manifest cleanup phase converts a bounded burst into a permanent leak."}
{"cluster_id": "chunk_006-173", "topic": "A retained view may delay detection", "canonical_claim": "A retained view may delay detection of `INV-NO-DANGLE` after a live manifest body is physically deleted, so fsck and corruption probes must bypass retained views or use strict validation."}
{"cluster_id": "chunk_006-174", "topic": "The candidate RoundContext must preserve the", "canonical_claim": "The candidate `RoundContext` must preserve the GC invariant that round state is threaded and never re-read; adding a re-read convenience would be unsafe."}
{"cluster_id": "chunk_006-175", "topic": "Stage A must not be certified", "canonical_claim": "Stage A must not be certified or deployed as a standalone deletion-capable stage unless it has the authoritative catalog universe or all destructive work remains globally suppressed until Stage B is installed"}
{"cluster_id": "chunk_006-176", "topic": "Every destructive GC phase including handoff_reclaim", "canonical_claim": "Every destructive GC phase, including handoff_reclaim, must receive suppression state before execution and have a zero-delete test when the hold is active or the frontier is incomplete"}
{"cluster_id": "chunk_006-177", "topic": "With only per key deleteExact available", "canonical_claim": "With only per-key deleteExact available, catalog-token and fence credentials must be revalidated immediately before each delete rather than once per multi-key batch"}
{"cluster_id": "chunk_006-178", "topic": "The late plus one delete site", "canonical_claim": "The late-plus-one delete-site guard needs a test-only sabotage or fault seam that bypasses the final in-degree read, records an isolated RED test, and then restores the guard for GREEN"}
{"cluster_id": "chunk_006-179", "topic": "The production UniversePolicy default must remain", "canonical_claim": "The production UniversePolicy default must remain non-authoritative until a dedicated enablement task after Stage B Tasks 5 and 6, preferably Task 7, are green"}
{"cluster_id": "chunk_006-180", "topic": "The frontier test belongs in gtest_cas_universe_from_catalog", "canonical_claim": "The frontier test belongs in gtest_cas_universe_from_catalog.cpp, while the sole Stage-B edit of gtest_cas_list_liar_end_to_end.cpp belongs to Task 7b"}
{"cluster_id": "chunk_006-181", "topic": "GcHeartbeat advisory state", "canonical_claim": "The unversioned GcHeartbeat is acceptable only as disposable advisory state; if it affects cross-version safety it must be converted to protobuf with CasHeader If GcHeartbeat remains fixed binary, it must be documented as disposable advisory state with exact-length validation and permission for future readers to ignore or recreate it"}
{"cluster_id": "chunk_006-182", "topic": "Orphan manifest deletion for live namespaces", "canonical_claim": "Orphan-manifest deletion for live namespaces must be disabled unless a persisted owner-grant ref id or fenced quiescent reconstruction proves the cut"}
{"cluster_id": "chunk_006-183", "topic": "catalog capacity reservations", "canonical_claim": "Catalog admission must reserve each entry's worst-case Removing and fold-seal representation so capacity can never refuse the transition needed to remove an admitted entry. Catalog admission must reject creation unless the encoded catalog plus worst-case fold cursor and removal reservations fit within both the catalog cap and the 256 MiB fold-seal cap. Catalog admission must enforce the additive reservation predicate fold_fixed + Σ reserve_cursor(entry) + Σ reserve_removing_cleanup(entry) + Σ reserve_hold(entry) <= fold_seal_cap in addition to encoded_catalog(post).size <= catalog_cap."}
{"cluster_id": "chunk_006-184", "topic": "Every unsettled writer cleanup duty must", "canonical_claim": "Every unsettled writer cleanup duty must be persisted before the build is retired, with a defined adopter, exact-delete completion, and retirement transaction."}
{"cluster_id": "chunk_006-185", "topic": "Orphan manifest nomination must exact GET", "canonical_claim": "Orphan-manifest nomination must exact-GET and decode M, submit neutral current-count touches without B2 or unmatched-removal accounting, adopt them through the fold seal/gc/state CAS, and exact-delete M only afterward."}
{"cluster_id": "chunk_006-186", "topic": "Classification 4 wire fields require exact", "canonical_claim": "Classification-4 wire fields require exact bounded names and values, duplicate-field rejection, and a byte-exact worst-case reservation including escaped namespace text, framing, newline, and trailer growth."}
{"cluster_id": "chunk_006-187", "topic": "Arithmetic exact key intake is the", "canonical_claim": "Arithmetic exact-key intake is the intended mechanism for closing LIST omissions; the former Probe A seam was deleted rather than retained as a sampled store-quality detector."}
{"cluster_id": "chunk_006-188", "topic": "A proposed manifest cache is keyed", "canonical_claim": "A proposed manifest cache is keyed by `ManifestId`, scoped to one fold round, bounded in bytes, and dropped at round end; avoiding one redundant edge avoids both its HEAD and GET trips. The useful manifest-body cache scope is one GC round: an op-scoped or transaction-scoped cache saves nothing, while a round-scoped cache can avoid cross-transaction re-reads; the HEAD-before-GET pair remains vetoed from change. In the measured intake, 7,565 edge reads were re-reads across 4,573 distinct manifests, or 39.6%; all observed re-reads were cross-transaction and none were intra-transaction. In that fold window, 2,991 manifests carried both an add and a remove edge, and `CasManifestGet == CasRefEmittedEdges`, demonstrating that no manifest-body cache existed. 39.6% of GC intake manifest fetches are re-reads (7,565 edges over 4,573 distinct manifests): intra-transaction redundancy is exactly zero, and all of it is cross-transaction (2,991 manifests carry both an add and a remove edge in the window, so the same body is read once when the ref is published and again when it is dropped), making a round-scoped body cache — not an op- or transaction-scoped one — the effective lever."}
{"cluster_id": "chunk_006-189", "topic": "GC orphan sweep and fsck should", "canonical_claim": "GC, orphan sweep, and fsck should share a structured deletion outcome vocabulary: deleted, missing, token mismatch, unsupported, and failed."}
{"cluster_id": "chunk_006-190", "topic": "GC streams the three cursor merge", "canonical_claim": "GC streams the three-cursor merge but accumulates the round's `std::vector<BlobDelta>` in memory, so a large mass-drop delta can cause an OOM and repeated lack of progress for that shard."}
{"cluster_id": "chunk_006-191", "topic": "The later GCR 1 re triage", "canonical_claim": "The later GCR-1 re-triage concluded that `rebuildBaseline` computes the mount-lease ack-floor and installs `gc/state` with a fail-closed CAS, so floor-gated graduation protects post-snapshot writer objects without refusing rebuild while writers are mounted."}
{"cluster_id": "chunk_006-192", "topic": "The P3 B1 fix invokes an", "canonical_claim": "The P3-B1 fix invokes an `on_lease_acquired` hook immediately after `acquireOrRenewLease`, sets `i_am_leader`, and pulses the heartbeat before folding begins."}
{"cluster_id": "chunk_006-193", "topic": "sweepManifestCursorPage is test only code it", "canonical_claim": "sweepManifestCursorPage is test-only code: it was moved to src/Disks/tests/cas_sweep_test_support.h as sweepManifestCursorPageForTest, and no production translation unit calls it because exact-token deletion without source-edge retirement or gc/state adoption would recreate the accounting hole."}
{"cluster_id": "chunk_006-194", "topic": "T5 removes Gc sampleRefListQuality and its", "canonical_claim": "T5 removes `Gc::sampleRefListQuality` and its `ref_list_probe` call, so a regular GC round no longer performs a second full ref-prefix LIST for store-quality sampling."}
{"cluster_id": "chunk_006-195", "topic": "UniversePolicy kDefault is Authoritative the destructive", "canonical_claim": "`UniversePolicy::kDefault` is `Authoritative`; the destructive gate requires an authoritative universe, `frontier_namespaces > 0`, and `frontier_proven == frontier_namespaces`."}
{"cluster_id": "chunk_006-196", "topic": "After the destruction flip the dropped", "canonical_claim": "After the destruction flip, the dropped-pool-member integration test expects janitor residue to drain to `janitor_pending == 0`, while checking `lifeless_keys=0` on every poll and retaining final `dangling=0` and `unaccounted=0` checks."}
{"cluster_id": "chunk_006-197", "topic": "cas_gc_maintenance_state has no janitor invocation GcState", "canonical_claim": "`cas_gc_maintenance_state` has no janitor invocation, `GcState`, fold, catalog, DEFER, or round-publication coupling."}
{"cluster_id": "chunk_006-198", "topic": "No janitor state is stored in", "canonical_claim": "No janitor state is stored in `gc/state` or a fold seal, and there is no lifecycle-specific cleanup attempt."}
{"cluster_id": "chunk_006-199", "topic": "GC must not advance a cursor", "canonical_claim": "GC must not advance a cursor over records whose enumeration completeness it cannot prove from authoritative data rather than assuming a listing is complete."}
{"cluster_id": "chunk_006-200", "topic": "The in degree reducer is intentionally", "canonical_claim": "The in-degree reducer is intentionally not changed: a set cannot cancel an element it never received, and materializing an unmatched negative edge would create false deletion."}
{"cluster_id": "chunk_006-201", "topic": "The eager unlink helper isEagerContentAddressedUnlink and", "canonical_claim": "The eager-unlink helper `isEagerContentAddressedUnlink` and six per-method `remove*` eager branches are replaced by subclass dispatch, with durable deletion represented as a staged intent materialized at commit."}
{"cluster_id": "chunk_006-202", "topic": "Task A7 serializes the manual SYSTEM", "canonical_claim": "Task A7 serializes the manual `SYSTEM ... GC` round against the background GC loop using a dedicated gc_round_mutex rather than the scheduler's existing `mutex`, because loop() deliberately releases `mutex` before running a round so that stop() and heartbeatLoop() are not blocked; holding `mutex` across a long fold would freeze the GC heartbeat and stall shutdown."}
{"cluster_id": "chunk_006-203", "topic": "A destructive GC gate based only", "canonical_claim": "A destructive GC gate based only on `proven == total` accepts an empty universe vacuously because `0 == 0`; the fix adds an explicit nonempty-authority predicate so the gate cannot be satisfied by having nothing to prove."}
{"cluster_id": "chunk_006-204", "topic": "cas gc rebuild s security verdict", "canonical_claim": "`cas-gc-rebuild`'s security verdict: it cannot delete live data directly (deletions still flow through the normal round's ack-floor plus two-phase plus exact-token path), and a crafted owner state that makes rebuild under-count requires full owner-plane write access, which already implies total pool compromise (SEC-3) — the trimmed-history guard closes the naive truncation path. Residual risks: FORCE only skips the healthy-pool check (operational risk, not a safety bypass); rebuild does a full-universe discover-and-fold (O(all shards + all journals)), an expensive pool-wide sweep an attacker with SYSTEM access could trigger repeatedly (shares the SEC-4 decode/fold exhaustion surface); and a concurrent GC leader's round CAS and rebuild's `gc/state` install can contend, so rebuild must be verified to respect GC leadership or a split-baseline is possible."}
{"cluster_id": "chunk_006-205", "topic": "D7 the SQL SYSTEM CONTENT ADDRESSED", "canonical_claim": "D7: the SQL `SYSTEM CONTENT ADDRESSED GC REBUILD` verb and the offline `clickhouse-disks ca-gc-rebuild` command are NOT duplication — both call the same `Gc::rebuildBaseline` core through two deliberately inverse-gated surfaces (live/writable vs offline/read-only), and this dual surface should be kept as-is."}
{"cluster_id": "chunk_006-206", "topic": "There are two independent sharding axes", "canonical_claim": "There are two independent sharding axes in GC that are deliberately different and easy to confuse: `blobShard` (blob-hash high64 % gc_shards, for in-degree work) versus `Store::shardOf` (CityHash64(ref_name) % root_shards, for the owner journal). There are also two independent lease/watermark concepts: the GC lease inside `gc/state`, versus the mount lease / build watermark at `gc/server-roots/<srid>/mount`; `computeHeartbeatFloor` fences only the latter, never the GC lease."}
{"cluster_id": "chunk_006-207", "topic": "The RemovalReady state costs less wire", "canonical_claim": "The RemovalReady state costs less wire-format surface than a life-keyed-cursor alternative: cursor identity appears in eight files spanning the wire grammar, every builder/parser, REBUILD, fsck, and the sweep's coverage lookup, versus one enum value, one codec branch, one transition, and the five predicate sites for RemovalReady. Life-keyed cursors also create a new corner case (a reborn name's fresh-life cursor coexists with a dead-life cursor nothing prunes), so they only relocate the retirement-tracking problem rather than avoiding it."}
{"cluster_id": "chunk_006-208", "topic": "The Stage B strategic review s", "canonical_claim": "The Stage B strategic review's invariant rulings: weaken byte-exact fold-seal capacity reservation to a generous over-covering bound (count times a conservative constant, hard namespace cap ~10^4) instead of exact worst-case arithmetic; cut the `Creating`-forbids-publication write-path check as a separate production-path obligation since `resolveNamespaceLife` already loops until `Live` and is the sole life source; cut incarnation-scoped fold cursor re-keying once prune-before-entry-delete is the pinned invariant; weaken the `_ckpt` conflict-is-corruption join to a semantic-max merge (corruption only on genuinely incomparable fields)."}
{"cluster_id": "chunk_006-209", "topic": "Under strict removal ordering the worst", "canonical_claim": "Under strict removal ordering, the worst cases \"two cleanup rows for one namespace\" and \"a cleanup row with no catalog entry\" become unconstructible (both require rebirth before the first removal retired); the worst-case capacity test should be replaced by an anomaly check that fires if GC ever observes an entry-less cleanup row."}
{"cluster_id": "chunk_006-210", "topic": "Probe A is reported not gated", "canonical_claim": "Probe A is reported-not-gated: a soak can go GREEN with confirmed enumeration holes (`nonzero_in=28/88 reads`) because folding aborts and nothing is deleted, which is defensible while the product fails closed, but is flagged as a decision that should be made deliberate now that the holes are confirmed rather than suspected."}
{"cluster_id": "chunk_006-211", "topic": "The ack floor GC redesign drops", "canonical_claim": "The ack-floor GC redesign drops `process_epoch` as a separate merged-heartbeat field because the writable path already sets `process_epoch = writer_epoch` (`CasStore.cpp:157`) and the sweep consumes `writer_epoch` unchanged, so a distinct field would be redundant."}
{"cluster_id": "chunk_006-212", "topic": "The ack floor design renamed the", "canonical_claim": "The ack-floor design renamed the sabotage control `SabotageDeleteAtFloor` to `SabotageIgnoreAckFloor` because an `r < min_ack` vs `r ≤ min_ack` off-by-one is provably still safe under the publish-order invariant, so the load-bearing control is \"ignores the floor entirely\", not the exact boundary."}
{"cluster_id": "chunk_006-213", "topic": "An optional pre pass refresh for", "canonical_claim": "An optional pre-pass refresh for the promote copy-forward tokenless-blob race fix (spec §3) was deliberately deferred rather than implemented: it is a fast-path-only optimization, not required for correctness, so it was left unscheduled rather than turned into a task."}
{"cluster_id": "chunk_006-214", "topic": "Per round LIST hunting for deposed", "canonical_claim": "Per-round LIST hunting for deposed-leader GC debris had already been removed; stale generations are now reclaimed by ordinary retention pruning with zero extra hot-path requests."}
{"cluster_id": "chunk_006-215", "topic": "The three observed ref log keys", "canonical_claim": "The three observed ref-log keys were deleted at 16:53:59 after folding aborted and a later complete enumeration reclaimed them; this occurrence produced no leak."}
{"cluster_id": "chunk_006-216", "topic": "Before the redesign 100 000 tables", "canonical_claim": "Before the redesign, 100,000 tables with 8 root shards each required approximately 800,000 CAS-PUTs and 1,600,000 GETs per GC round because the fence and recheck traversed the entire root-shard universe."}
{"cluster_id": "chunk_006-217", "topic": "The redesigned steady state round budget", "canonical_claim": "The redesigned steady-state round budget is one LIST sweep plus O(servers) heartbeat GETs, O(D) changed-shard GETs, O(window) manifest GETs, O(gc_shards) run GET/PUTs, candidate HEADs, graduated DELETEs, O(D) coverage PUTs, and one `gc/state` CAS, with no O(universe) GET/PUT phase."}
{"cluster_id": "chunk_006-218", "topic": "Before T2 a snapshot run was", "canonical_claim": "Before T2, a snapshot run was materialized three times—by `CasObjectStorageBackend::get`, `RunFileReader::full`, and `readPriorEdges`—for peak memory of approximately 3 × O(active edges) per GC shard; the target example estimated about 24 GB RAM for one fold."}
{"cluster_id": "chunk_006-219", "topic": "Before T0 every pass rewrote every", "canonical_claim": "Before T0, every pass rewrote every GC-shard snapshot run even for an empty delta, causing an idle pool to pay two snapshot sizes per round; at the target scale and 5-minute rounds this was estimated as terabytes per day of churn."}
{"cluster_id": "chunk_006-220", "topic": "streaming run reads", "canonical_claim": "Opening a snapshot run in streaming mode uses a HEAD, a ranged tail GET for the footer, and a body `getStream`; resident run memory is O(block) while the wire bytes are unchanged. Streaming GC run reads use true ranged reads and `getStream`; opening a run costs three requests (`head`, tail `get`, and body `getStream`), or four when a large footer is present, and each `seek` costs one ranged GET per block."}
{"cluster_id": "chunk_006-221", "topic": "With T0 an idle round that", "canonical_claim": "With T0, an idle round that has no changes touches zero snapshot-run objects and consists of the LIST sweep, heartbeats, and one `gc/state` CAS."}
{"cluster_id": "chunk_006-222", "topic": "Idle GC over a static 20M", "canonical_claim": "Idle GC over a static 20M-row, 400-part pool used about 1,362 `CasGcGet`, 643 `CasBlobHead`, and 457 `CasRootGet` operations per round, roughly 2,500 S3 operations per round despite no changes."}
{"cluster_id": "chunk_006-223", "topic": "The observed GC round duration grew", "canonical_claim": "The observed GC round duration grew from 87 ms at 400 parts to 92.6 seconds at 10,000 tables and 398 seconds at 100,000 parts, demonstrating O(ref-universe/object-count) scaling; fsck also becomes O(objects) and timed out at 100,000 parts."}
{"cluster_id": "chunk_006-224", "topic": "Partitioned inserts in S11 require O", "canonical_claim": "Partitioned inserts in S11 require O(partitions) CAS commits, taking about 10 seconds for a 256-partition insert; deferred GC during 64 delete rounds allowed orphaned parts to outpace disk capacity."}
{"cluster_id": "chunk_006-225", "topic": "Gc fold performs a streaming two", "canonical_claim": "`Gc::fold` performs a streaming two-cursor merge over the entire prior generation's source-edge run, reading the whole prior generation and writing a whole new consolidated run even when one edge or no edge changed."}
{"cluster_id": "chunk_006-226", "topic": "The codecs v3 phase 3 soak", "canonical_claim": "The codecs-v3 phase-3 soak completed with `dangling=0`, equal oracle counts on both replicas, recovered from four faults and two restarts, and drained the pool from about 3 GB to 102 MB."}
{"cluster_id": "chunk_006-227", "topic": "The measured ref intake cost is", "canonical_claim": "The measured ref-intake cost is `logs + 2 × edges` round trips at approximately 0.9 ms per trip, making the phase request-bound rather than CPU-bound."}
{"cluster_id": "chunk_006-228", "topic": "The ref intake loop is serial", "canonical_claim": "The ref-intake loop is serial: approximately 1.9 million requests at 0.9 ms each imply about 1710 seconds, close to the observed 1830-second maximum and consistent with nearly all phase time being serial request latency."}
{"cluster_id": "chunk_006-229", "topic": "If the approximately 1710 second serial", "canonical_claim": "If the approximately 1710-second serial fetch floor were parallelized N-way, the ideal fetch component would be about 213.75 seconds at N=8 and 106.875 seconds at N=16, before merge cost."}
{"cluster_id": "chunk_006-230", "topic": "The ref chain design adds about", "canonical_claim": "The ref-chain design adds about two serial S3 round trips per committed chunk for `_ckpt`, one conditional PUT per touched namespace per epoch transition during recovery, and one catalog GET per fold round after the hot LIST."}
{"cluster_id": "chunk_006-231", "topic": "The dominant T15 GC cost was", "canonical_claim": "The dominant T15 GC cost was `defer_decision`: 5,096.39 seconds, 79.11% of GC time, including full LISTs of about 177,000 keys even when `changed_shards` was zero."}
{"cluster_id": "chunk_006-232", "topic": "The empty CaGcDestructiveGateCore universe configuration was", "canonical_claim": "The empty `CaGcDestructiveGateCore` universe configuration was green, with `CatalogUniverse = FrontierProven = {}` and physical actions suppressed despite the two sets being equal."}
{"cluster_id": "chunk_006-233", "topic": "CaGcAckFloorCore_empty_blobs cfg and CaGcAckFloorZombie_empty_blobs cfg both", "canonical_claim": "`CaGcAckFloorCore_empty_blobs.cfg` and `CaGcAckFloorZombie_empty_blobs.cfg` both passed as green with empty `Blobs` sets."}
{"cluster_id": "chunk_006-234", "topic": "CaGcLeaseCore_empty_actors cfg passed as green with", "canonical_claim": "`CaGcLeaseCore_empty_actors.cfg` passed as green with `Actors = {}`."}
{"cluster_id": "chunk_006-235", "topic": "CaGcRootLocalPartManifestCore_empty_namespaces cfg passed as green with", "canonical_claim": "`CaGcRootLocalPartManifestCore_empty_namespaces.cfg` passed as green with `Namespaces = {}` despite the model having configurable entity sets for writers, leaders, blobs, manifest instances, refs, builds, paths, build prefixes, and shards."}
{"cluster_id": "chunk_006-236", "topic": "CaGcRoundDeferCore_empty_blobs cfg passed as green with", "canonical_claim": "`CaGcRoundDeferCore_empty_blobs.cfg` passed as green with `Blobs = {}`."}
{"cluster_id": "chunk_006-237", "topic": "CaRetiredInRun_empty_blobs cfg and CaRetiredInRunFoldAbortWitness_empty_blobs cfg both", "canonical_claim": "`CaRetiredInRun_empty_blobs.cfg` and `CaRetiredInRunFoldAbortWitness_empty_blobs.cfg` both passed as green with empty `Blobs` sets."}
{"cluster_id": "chunk_006-238", "topic": "Across 65 263 ref log uploads", "canonical_claim": "Across 65,263 ref-log uploads in the affected namespace, partitioned by epoch, zero uploads were out of order."}
{"cluster_id": "chunk_006-239", "topic": "The GC cadence tradeoff is approximately", "canonical_claim": "The GC cadence tradeoff is approximately `A/interval + B·interval`, where `A` is per-fold universe work and `B` is writer amplification from journal growth; the proposed optimal interval scales as `sqrt(U) / r` for blob universe `U` and hottest-shard mutation rate `r`."}
{"cluster_id": "chunk_006-240", "topic": "GC folds remain O ref universe", "canonical_claim": "GC folds remain O(ref-universe) for large deltas: measured duration grew from about 87 ms at 400 parts to 93 seconds at 10,000 tables and 398 seconds at about 100,000 parts, even though correctness held with `dangling=0`."}
{"cluster_id": "chunk_006-241", "topic": "The merged m_merged configuration was green", "canonical_claim": "The merged `m_merged` configuration was green with `TrimGated = TRUE` and `CursorInSnap = TRUE`, while `m_both_buggy`, `m_cursorskip`, and `m_trimonly` violated `INV_NO_LOSS` under the same bounds."}
{"cluster_id": "chunk_006-242", "topic": "The C2 soak comparison recorded 112", "canonical_claim": "The C2 soak comparison recorded 112,518 skipped entries and 110,218 permanently unreachable objects with a cleanup cap of 5000, while disabling the cap drained 223,714 objects with zero unreachable objects. A cap on manifest cleanup was rejected because it converted a bounded burst into a permanent leak; measured cap=5000 left 112,518 entries skipped, while disabling the cap drained 223,714 entries in-round."}
{"cluster_id": "chunk_006-243", "topic": "The system cas_gc_log counters entries_condemned entries_graduated", "canonical_claim": "The `system.cas_gc_log` counters `entries_condemned`, `entries_graduated`, and `entries_redeleted` represent the three retired-cursor pipeline stages: newly condemned, floor-passed and republished `delete_pending`, and executed exact-token blob deletes."}
{"cluster_id": "chunk_006-244", "topic": "The outstanding reconciliation debt recorded 56", "canonical_claim": "The outstanding reconciliation debt recorded 56 already-leaked blobs that incremental rounds could never reclaim because their in-degree was pinned above zero; only rebuilding in-degree state could reclaim them."}
{"cluster_id": "chunk_006-245", "topic": "Before generation retention B174 was added", "canonical_claim": "Before generation retention (B174) was added, a 12h soak measured `gc/` bookkeeping at 82% of pool storage (2822 retained generations, roughly 3.7 MiB/shard/generation), dwarfing actual data 8:1."}
{"cluster_id": "chunk_006-246", "topic": "GC observability gained per phase timing", "canonical_claim": "GC observability gained per-phase timing during Stage B, which immediately found the fold seal being read five times per round where the design intended two."}
{"cluster_id": "chunk_006-247", "topic": "The measured GET cost formula for", "canonical_claim": "The measured GET-cost formula for a folding GC round is exact: total `S3GetObject` = ref-log body GETs + manifest body GETs, i.e. `1 + edges_per_log`, which climbs from 1.54 to 3.73 as backlog grows; round trips equal `logs + edges` at a flat roughly 0.5 ms per round trip across a 140x request range, and a measured 416-round `Success` sample cost roughly $0.007 in total S3 charges."}
{"cluster_id": "chunk_006-248", "topic": "GC s fold_ref_intake is linear in", "canonical_claim": "GC's `fold_ref_intake` is linear in the ref-log backlog at 256 logs/s sustained; the flat cost is per S3 request (~0.92 ms), decomposing as one GET per log body plus one per emitted manifest edge, so round trips equal `logs + 2*edges`, and edges-per-log climbs from 1.54 to 3.73 as backlog grows."}
{"cluster_id": "chunk_006-249", "topic": "Probe B s throw wedges GC", "canonical_claim": "Probe B's throw wedges GC until an operator runs the rebuild; on a pre-release soak stand this cost a disk-budget blowout, with the last observed run reclaiming 124 GB at one checkpoint once the wedge was cleared, which motivates the open question of whether the first N occurrences should suppress-and-continue with a counter, escalating to a throw only on recurrence."}
{"cluster_id": "chunk_006-250", "topic": "A sweep only defense that makes", "canonical_claim": "A sweep-only defense that makes `activeManifestKeys` judge precommits dead was deferred because it would delete the manifest body without releasing the precommit's pinned blob edges; the fold-side fix is required."}
{"cluster_id": "chunk_006-251", "topic": "Queueing an exact removal is not", "canonical_claim": "Queueing an exact removal is not a valid fix for the unmatched-minus-one leak because `RefTableState` throws `CORRUPTED_DATA` when the binding is absent, turning the leak into a permanent wedge."}
{"cluster_id": "chunk_006-252", "topic": "DeleteObjects cannot replace token conditional deleteExact", "canonical_claim": "`DeleteObjects` cannot replace token-conditional `deleteExact` because bulk delete has no per-object precondition and could delete a replaced incarnation; parallel individual conditional deletes preserve exactness."}
{"cluster_id": "chunk_006-253", "topic": "Moving pre CAS redeletes to execute", "canonical_claim": "Moving pre-CAS redeletes to execute only after round commit (to avoid idempotent re-execution on an aborted round) was rejected: the pre-CAS placement is a deliberate Task-9 ack-floor decision justified by durable state and safe at any leader staleness, re-execution is idempotent via exact-token deletes, and the P3-B1 heartbeat fix already removes the amplification that made aborts frequent; revisit only if a post-fix soak still shows re-execution churn."}
{"cluster_id": "chunk_006-254", "topic": "Accepting a condemned tokened blob without", "canonical_claim": "Accepting a condemned tokened blob without resurrecting it is UNSOUND and rejected in the promote/resurrect design, because the condemn→delete pipeline is exact-token two-phase and an accepted-but-condemned token can graduate to delete, dangling the manifest; only a fresh incarnation via uploadFromSource is safe."}
{"cluster_id": "chunk_006-255", "topic": "SYSTEM CAS GC RUN ca performs", "canonical_claim": "`SYSTEM CAS GC RUN ca` performs one synchronous GC round on the contacted node, and only the lease holder makes progress."}
{"cluster_id": "chunk_006-256", "topic": "The gc_anomaly audit event caps at", "canonical_claim": "The `gc_anomaly` audit event caps at 32 rows per round (one historical firing had 244,939 holes); the cap and the true total both travel in every row, and a truncated round logs an explicit line naming how many were counted but not detailed."}
{"cluster_id": "chunk_006-257", "topic": "On an empty CAS pool scratch_bytes", "canonical_claim": "On an empty CAS pool, `scratch_bytes` grew from about 1 MiB to 21 MiB on `ch1` and from 16 KiB to 21 MiB on `ch2` during idle GC, indicating a separate local staging-file cleanup question that does not explain the tracked-memory gate."}
{"cluster_id": "chunk_006-258", "topic": "The BACKLOG item clamp liveness scoped", "canonical_claim": "The BACKLOG item `[clamp liveness] scoped suppression under long persistent clamps` should make a clamped key reportable, either by emitting `ref_late_log_detected` or a dedicated clamped-key anomaly directly from the clamp path while the sweep skips the key."}
{"cluster_id": "chunk_006-259", "topic": "Task 5 must delete a catalog", "canonical_claim": "Task 5 must delete a catalog entry only after the cleanup marker and cleanup-item retirement are durable, and must prune the namespace cursor before deleting the entry."}
{"cluster_id": "chunk_006-260", "topic": "The fold cursor identity must include", "canonical_claim": "The fold cursor identity must include the full `NamespaceLifeId`, and its format, parser, consumers, and same-epoch low-sequence rebirth regression test must be updated together."}
{"cluster_id": "chunk_006-261", "topic": "RemovalReady producers", "canonical_claim": "The `RemovalReady` predicate must be enforced at five cursor or walk producers: the catalog-only frontier loop, parent-cursor carry, hint admission, REBUILD, and the `Removing` to `RemovalReady` no-hold precondition. The `RemovalReady` catalog-state proposal's monotonicity claim requires the predicate to be enforced at five distinct cursor/walk producer sites: the catalog-only frontier loop, the parent-cursor carry, hint-named namespaces (from the round's ref listing), REBUILD's cursor reconstruction, and carried holds (which need an explicit no-hold precondition on the Removing→RemovalReady CAS). The proposal's own text only explicitly covers the first and fifth; hint admission and REBUILD were flagged as not covered and must be added before implementation."}
{"cluster_id": "chunk_006-262", "topic": "Incremental point updatable in degree is", "canonical_claim": "Incremental point-updatable in-degree is intended to make non-idle small-delta rounds O(delta) and provide a global change signal that can eliminate the per-round `LIST(cas/refs/)` O(shards) sweep."}
{"cluster_id": "chunk_006-263", "topic": "Task 8 review F1 rev 6", "canonical_claim": "Task-8 review F1 (rev.6): Removed-lifecycle recoveries are not sealed — a late rebirth PUT from a dead epoch can transiently resurface in cold folds until GC namespace-cleanup; intended behavior is Live-only sealing, but extending the seal to Removed is a documented follow-up if this becomes observable."}
{"cluster_id": "chunk_006-264", "topic": "Task 11 review F1 rev 6", "canonical_claim": "Task-11 review F1 (rev.6): the anomaly-routing policy is only partially deployed — two foreign-bytes-signature sites need semantics analysis before routing through `reportImpossibleInterference`: `flushRefBatch`'s fresh-id resolve-before-reissue catch (a reviewed availability fix), and `stageManifest`'s ManifestId-collision path (possibly legitimate dedup)."}
{"cluster_id": "chunk_006-265", "topic": "The v7 ref chain design s", "canonical_claim": "The v7 ref-chain design's required go-red negative test controls under `HoleyListBackend` are: a wholly hidden A:+1 versus a visible B:-1 on the same blob; a carried hold followed by a round that omits the offending namespace; process death both before and after nomination adoption; many serialized catalog admissions exceeding aggregate fold reservation; decommission with a `Removing` catalog entry omitted by LIST; opening a legacy soak pool; and stale-handle rebirth between `namespaceFilesReadable` and the subsequent verbatim operation."}
{"cluster_id": "chunk_006-266", "topic": "The RemovalReady review identifies four claims", "canonical_claim": "The RemovalReady review identifies four claims the counter-proposal itself silently drops and that must return before implementation: (1) the perpetual `_files` janitor for foreign-incarnation debris (spec INV-3 still mandates it; without it the Task-4b leak interval becomes permanent), (2) the unauthorized terminal-append negative test, (3) the no-physical-empty gating proof reshaped as an op-journal test, and (4) Task 7's dead-root resume table needs two new rows for `RemovalReady`-without-`_ckpt` and `RemovalReady`-with-`_ckpt`."}
{"cluster_id": "chunk_006-267", "topic": "The RemovalReady proposal leaves the Removed", "canonical_claim": "The RemovalReady proposal leaves the `Removed` ref-snapshot's fate conditional even though it has a second consumer: `namespaceIsRemoved`'s cold-runtime recovery reads the `Removed` lifecycle state, which the reader-absence predicate rides on. The task must decide the mechanism explicitly: reader absence should come from the catalog cut, and the `Removed` snapshot plus the `_cleanup` gate should be rewired or deleted together."}
{"cluster_id": "chunk_006-268", "topic": "A dropped Task 5 test obligation", "canonical_claim": "A dropped Task 5 test obligation, `NamespaceRemovalDoesNotListOrDeleteFiles`, was the executable proof that removal has no physical-empty dependency (no `_files` LIST, no `_files` delete, entry deleted while files still exist); it must be restored in any rewrite."}
{"cluster_id": "chunk_006-269", "topic": "A dropped Task 5 test obligation", "canonical_claim": "A dropped Task 5 test obligation is janitor suppression: a janitor that deletes during a suppressed round would pass every test named in the rewrite, so a suppressed-round-deletes-nothing test must be added."}
{"cluster_id": "chunk_006-270", "topic": "The ideal fully invisible zero re", "canonical_claim": "The ideal, fully-invisible zero-re-upload fix for the promote/GC condemn race is a writer-triggerable synchronous fold that activates the precommit edge before revalidation, but no writer↔GC synchronous-fold API exists today (only a test-only foldManifestEdgesForTest); this is recorded as a follow-up, with the bounded in-closure resurrect used as the pragmatic fix instead."}
{"cluster_id": "chunk_006-271", "topic": "In the CAS GC observability skipped", "canonical_claim": "In the CAS GC-observability skipped-transaction-detector design, probe A's independence from probe B rests on the two enumerations being separate physical LIST walks; if a future optimisation merges them (listing the same prefix twice is an obvious target), probe A would silently become vacuous, and the design leaves open whether the guard against this should be a code comment forbidding the merge or a test asserting two distinct LIST walks occur."}
{"cluster_id": "chunk_006-272", "topic": "In the CAS GC per phase", "canonical_claim": "In the CAS GC per-phase observability row design, phase_metrics is typed as a Map, so there is no compile-time type checking on key names and a typo in a key is invisible; the design leaves open whether this is worth a compile-time key list or is over-engineering for an introspection table."}
{"cluster_id": "chunk_006-273", "topic": "The CAS GC per phase row", "canonical_claim": "The CAS GC per-phase row design treats its 17-phase split as a judgement call: fold_seal_read and fold_seal_write (one GET and one PUT) could fold into neighbouring phases, and round_commit mixes the heavy prune step with the trivial CAS step and arguably should be split."}
