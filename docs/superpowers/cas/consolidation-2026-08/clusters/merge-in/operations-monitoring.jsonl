{"cluster_id": "chunk_013-152", "topic": "unused ProfileEvents", "canonical_claim": "Six ProfileEvents—`ContentAddressedGenerationResurrectionsTotal`, `ContentAddressedDuplicateGenerationBytes`, `ContentAddressedTombstonesTotal`, `ContentAddressedGenerationsObserved`, `ContentAddressedHashesObserved`, and `ContentAddressedOrphanBytesEstimate`—have zero increment sites and describe a removed design. Risk register #9 (Medium, confirmed): six `ContentAddressed*` `ProfileEvents` (`ProfileEvents.cpp:876-881`) have zero increment sites, relics of the pre-rebuild generation/tombstone GC design; always-zero counters imply \"no resurrections/orphans\" on dashboards, which is actively misleading. Suggested action: delete them after verifying no dashboard depends on them. The zero-increment `ProfileEvents` for removed shard mutation queues, pre-rebuild GC, manifest backpressure, and part-folder-view evictions, plus the unused `GcSnapPersist` event, are removed after confirming no increment sites or dashboard consumers."}
{"cluster_id": "chunk_013-153", "topic": "dropped mount audit events", "canonical_claim": "Mount events emitted inside `Store::open` are dropped because the CA event sink is installed only after open completes. Risk register #15 (Low, confirmed): mount events emitted inside `Store::open` (`CasStore.cpp:331-337`) are dropped because the event sink is installed only after `open` returns, so first-mount claim/conflict decisions never reach `system.content_addressed_log`. Suggested action: pre-install the sink before `open` if first-mount audit matters."}
{"cluster_id": "chunk_013-154", "topic": "The previously observed S01 memory bug was caused by `Build::putBlob` ma", "canonical_claim": "The previously observed S01 memory bug was caused by `Build::putBlob` materializing a staged `BlobSource` into a `String`; streaming through `uploadFromSource` reduced a 2 GiB INSERT peak from 13 GiB to 4.33 GiB."}
{"cluster_id": "chunk_013-155", "topic": "D2 found that the S23 idle-pool `s3_ops` metric incorrectly summed `*Mic", "canonical_claim": "D2 found that the S23 idle-pool `s3_ops` metric incorrectly summed `*Microseconds` ProfileEvents as operation counts; the measured CAS counts were `CasRootList=0 CasRootGet=0 CasGcGet=71 CasGcPut=24 CasGcList=9` and `S3ReadRequestsCount=17`."}
{"cluster_id": "chunk_013-156", "topic": "Container-cgroup `memory", "canonical_claim": "Container-cgroup `memory.peak` includes page cache and was measured at 5–21 times the ClickHouse-tracked peak, so memory verdicts based on it are unreliable."}
{"cluster_id": "chunk_013-157", "topic": "The campaign harness hid sustained 10–20% S3 read-error rates because ve", "canonical_claim": "The campaign harness hid sustained 10–20% S3 read-error rates because verdict tables asserted operation counts but not error rates."}
{"cluster_id": "chunk_013-158", "topic": "CAS had no `CurrentMetrics` or `AsynchronousMetrics` gauges; its introsp", "canonical_claim": "CAS had no `CurrentMetrics` or `AsynchronousMetrics` gauges; its introspection surface consisted of historical log tables and monotone counters."}
{"cluster_id": "chunk_013-159", "topic": "Mount and lease state, including slot holder, renewal health, and fencin", "canonical_claim": "Mount and lease state, including slot holder, renewal health, and fencing, was visible outside `err.log` only after adding the proposed introspection package."}
{"cluster_id": "chunk_013-160", "topic": "retry-later error observability", "canonical_claim": "CAS retry-later failures were opaque because the message said `retry budget exhausted` even for a pre-attempt fence rejection, and the failures were logged at Information level without populating `system.replication_queue.last_exception`. The retry-later helper's message carries the precise cause, and changing the escaping error to non-exempt `NETWORK_ERROR` automatically populates `system.replication_queue.last_exception` and `last_exception_time` through existing merge handling."}
{"cluster_id": "chunk_013-161", "topic": "Review finding №30 is real for started disks: `system", "canonical_claim": "Review finding №30 is real for started disks: `system.content_addressed_mounts` skips listing exceptions and can make a failed listing appear as no mounts, although not-started disks are legitimately skipped."}
{"cluster_id": "chunk_013-162", "topic": "The initial S23 RSS failure was allocator retention and fresh-boot warmu", "canonical_claim": "The initial S23 RSS failure was allocator retention and fresh-boot warmup rather than a GC or log-flushing leak: the 86-second empty-pool run had sawtooth RSS, while `MemoryTracking` grew only about 50–70 MiB and the remainder was allocator dirty-page residency."}
{"cluster_id": "chunk_013-163", "topic": "The corrected S23 `MemoryTracking` failure was generic ClickHouse boot w", "canonical_claim": "The corrected S23 `MemoryTracking` failure was generic ClickHouse boot warmup: the non-leader `ch2` grew more than the leader despite running zero GC folds, and the CAS pool contained zero blobs, manifests, refs, and roots."}
{"cluster_id": "chunk_013-164", "topic": "S38 failed twice because no `ref_late_log_detected` event appeared durin", "canonical_claim": "S38 failed twice because no `ref_late_log_detected` event appeared during the 240-second post-injection poll window after injecting a dead-epoch late log at key `.../_log/0000000000000001-fffffffffffffffe`."}
{"cluster_id": "chunk_013-165", "topic": "`system", "canonical_claim": "`system.content_addressed_mounts` currently reports the querying server's `is_leader`, `pending_reclaim`, `last_success_age_seconds`, and `wedged_namespace_count` values on every peer row."}
{"cluster_id": "chunk_013-166", "topic": "vacuous GC verdicts", "canonical_claim": "The GC scenario harness once queried the removed `min_ack` column, converted the resulting `UNKNOWN_IDENTIFIER` into an empty list, and thereby made every GC verdict vacuously true until the observation error path was fixed. All scenario GC verdicts became vacuous when `framework/observe.gc_log_rows` queried the removed `min_ack` column, caught `UNKNOWN_IDENTIFIER`, and returned an empty row set; `assert_gc_no_failed` then passed on empty input."}
{"cluster_id": "chunk_013-167", "topic": "Deferred GC rounds previously logged as `round 0` because the defer path", "canonical_claim": "Deferred GC rounds previously logged as `round 0` because the defer path returned before assigning `report.round`, making skipped rounds indistinguishable from empty folds."}
{"cluster_id": "chunk_013-168", "topic": "`Gc::CasGc", "canonical_claim": "`Gc::CasGc.cpp` phase markers are already inconsistent: the audit found 17 `PHASE N/19` markers with 9 and 17 missing and 15 duplicated, while the test phase list has 19 names including `pre_fold_ref_drain`."}
{"cluster_id": "chunk_013-169", "topic": "Three hammer runs listing about 19 million keys produced zero holes befo", "canonical_claim": "Three hammer runs listing about 19 million keys produced zero holes before the rare LIST violation occurred live within four minutes of an ordinary soak."}
{"cluster_id": "chunk_013-170", "topic": "`system", "canonical_claim": "`system.content_addressed_mounts` can silently return partial results when obtaining the pool or listing mounts fails because the exception is caught and omitted."}
{"cluster_id": "chunk_013-171", "topic": "A GC scheduler retry loop used a fixed interval without backoff, failure", "canonical_claim": "A GC scheduler retry loop used a fixed interval without backoff, failure counting, or a circuit breaker, and `system.content_addressed_mounts` exposed only `last_success_age_seconds` for the resulting health state."}
{"cluster_id": "chunk_013-172", "topic": "`system", "canonical_claim": "`system.remote_data_paths` did not push down `WHERE disk_name = ...`, causing it to walk every disk and recursively LIST the shared CA-s3 ref namespace; the CA-s3 lane measured 872 LIST requests with 436 first-attempt timeouts."}
{"cluster_id": "chunk_013-173", "topic": "The mount-lease keeper can be silent at the default log level during a f", "canonical_claim": "The mount-lease keeper can be silent at the default log level during a fence-to-remount episode, leaving renewal failure, fence arming, remount phases, and completion absent from the server log."}
{"cluster_id": "chunk_013-174", "topic": "The consistency review identifies `CasDbgExistsHit`, `CasDbgExistsMiss`,", "canonical_claim": "The consistency review identifies `CasDbgExistsHit`, `CasDbgExistsMiss`, `CasDbgMetaHit`, and `CasDbgMetaMiss` plus the `casDbgSampleHeadMiss` sampler as throwaway debug instrumentation exposed through permanent `system.events` metrics and a hot S3 HEAD path."}
{"cluster_id": "chunk_013-175", "topic": "wedge soak criterion", "canonical_claim": "The soak criterion zero wedged-forever lanes contradicts the accepted possibility of a permanently quiet unacknowledged wedge until a later foreground flush or remount opportunity Soak failure is a wedge surviving a foreground flush or remount opportunity; quiet unacknowledged wedges may be reported, but no autonomous retry loop is allowed A wedge should fail the soak only if it remains after a foreground flush or remount resolution opportunity, and no background deadline-resetting retry loop may exist"}
{"cluster_id": "chunk_013-176", "topic": "Quarantine backoff bounds only anchor and repair point reads; every fold", "canonical_claim": "Quarantine backoff bounds only anchor and repair point reads; every fold still pays for two global ref-prefix enumerations, held-namespace grouping, Probe A comparisons, and larger fold seals"}
{"cluster_id": "chunk_013-177", "topic": "The `gc_anomaly` event must capture a `HEAD` verdict while a LIST/arithm", "canonical_claim": "The `gc_anomaly` event must capture a `HEAD` verdict while a LIST/arithmetic disagreement exists, because present and absent are opposite defects and cannot be distinguished reliably after the fact."}
{"cluster_id": "chunk_013-178", "topic": "`SYSTEM CONTENT ADDRESSED GC RUN` creates a fresh `Cas::Gc` and is unsyn", "canonical_claim": "`SYSTEM CONTENT ADDRESSED GC RUN` creates a fresh `Cas::Gc` and is unsynchronized with the background loop, so repeated manual calls cannot accumulate lease-steal observations and may run duplicate leaders."}
{"cluster_id": "chunk_013-179", "topic": "The `CasGcIsLeader` and `CasGcPendingReclaimEntries` `CurrentMetrics` ga", "canonical_claim": "The `CasGcIsLeader` and `CasGcPendingReclaimEntries` `CurrentMetrics` gauges are process-global rather than per disk, so one CA disk can hide another disk's stuck GC."}
{"cluster_id": "chunk_013-180", "topic": "detector phase naming", "canonical_claim": "`utils/ca-soak/soak/signals.py` lists `(\"fold_ref_list\", \"ref_folding_aborted\")` in `DETECTOR_METRICS`, but the server emits the metric under phase `fold_ref_group`; `summarize_phases` silently skips the missing phase, so `fold_ref_list.ref_folding_aborted` never appears in the detector summary or formatted operator line. `_DETECTOR_COLUMNS` still stores `ref_folding_aborted` as a scalar on the `fold_ref_group` phase row even though the detector-summary key is dead."}
{"cluster_id": "chunk_013-181", "topic": "`utils/ca-soak/scripts/soak_watch", "canonical_claim": "`utils/ca-soak/scripts/soak_watch.sh` still issued `SYSTEM FLUSH LOGS content_addressed_log`, but that table no longer exists after the CAS naming change."}
{"cluster_id": "chunk_013-182", "topic": "`CasObjectPut` has never existed in `src/Common/ProfileEvents", "canonical_claim": "`CasObjectPut` has never existed in `src/Common/ProfileEvents.cpp`; the soak query's `CasObjectPut` alternative was removed, leaving the working `CASBlobPut` metric name unchanged."}
{"cluster_id": "chunk_013-183", "topic": "S3 exception message format", "canonical_claim": "Formatting an error with `fmt::format(...)` before passing it to `S3Exception` loses the static `message_format_string`, degrading grouping in `system.text_log` and `system.errors`. `copyS3File.cpp` should construct `S3Exception` with `PreformattedMessage::create(\"Message: {}, Key: ...\", ...)` so the static message format remains available for error grouping."}
{"cluster_id": "chunk_013-184", "topic": "CAS system-log default", "canonical_claim": "The two new CAS system logs (content_addressed_log, content_addressed_garbage_collection_log) ship enabled-by-default, yet SystemLog.h documents content_addressed_log as \"off by default\" -- the comment and config.xml default disagree and need reconciling. The CA event log is on by default since `cbe0ffb7608`, and `system.content_addressed_garbage_collection_log` records per-round GC audit data. The `ContentAddressedLog` system-log description must reflect that its configuration is enabled by default for the experimental CA feature; the stale `SystemLog.h` off-by-default wording is corrected."}
{"cluster_id": "chunk_013-185", "topic": "On the studied Altinity build, `SYSTEM JEMALLOC FLUSH PROFILE` writes a ", "canonical_claim": "On the studied Altinity build, `SYSTEM JEMALLOC FLUSH PROFILE` writes a `.heap` file plus an empty `.heap.collapsed` and no `.symbolized` file, and `system.asynchronous_metrics`'s `jemalloc.prof.active` can read 1 even when `prof.thread_active_init=0` and zero backtrace samples are actually captured, making the async metric misleading for confirming profiler state."}
{"cluster_id": "chunk_013-186", "topic": "During the 4h chaos soak, the detail=False summary fsck exceeded its 180", "canonical_claim": "During the 4h chaos soak, the detail=False summary fsck exceeded its 180 s bound at ~183 GB / 2.14M objects at the gc_checkpoint, so the harness's `dangling==0` safety gate was skipped (`fsck_status: skipped`) rather than failing — the quadratic discovery/LIST cost blinds the correctness oracle at this pool size (tracked as B146/B154)."}
{"cluster_id": "chunk_013-187", "topic": "During the 4h chaos soak (SOAK-4H-DISK-INFEASIBLE), the `max_pool_gb=40`", "canonical_claim": "During the 4h chaos soak (SOAK-4H-DISK-INFEASIBLE), the `max_pool_gb=40` insert-pacing throttle could not hold the cap: the pool crossed 40 GB by ~t+30min and kept climbing (~5 GB/min decelerating to ~1.8 GB/min under full throttle) because RustFS performs no background compaction and MergeTree merge/mutation write-amplification plus non-reclaim outpace insert-pacing; growth stopped cleanly only when inserts stopped, at a ~183 GB plateau."}
{"cluster_id": "chunk_013-188", "topic": "During the definitive consistency soak, a GC round (round 3) did zero wo", "canonical_claim": "During the definitive consistency soak, a GC round (round 3) did zero work but still took 764 seconds, which was flagged as an observability gap (no signal distinguishing a genuinely idle round from a stalled one) rather than a correctness bug."}
{"cluster_id": "chunk_013-189", "topic": "D2 found S20 (replicated fetch and relink) failed because the follower's", "canonical_claim": "D2 found S20 (replicated fetch and relink) failed because the follower's `CasRootCas` counter read 0, noting the CAS counters may not be scoped per-node; recommended fix is to scope the CAS counter per node or make the verdict record-only."}
{"cluster_id": "chunk_013-190", "topic": "duplicate mount-conflict rows", "canonical_claim": "Risk register #18 (Low, confirmed): a single `SingleWriterSlot`/`MountLeaseKeeper` renew failure emits two separate `mount_conflict` audit rows (one classified, one a timeline marker), so a naive `count()` over the audit log over-reports conflict occurrences. A single mount-lease renewal failure emits two audit rows (a classified `mount_conflict` plus a separate `renew_failed` timeline marker), causing a naive count of `mount_conflict` events to over-report failures."}
{"cluster_id": "chunk_013-191", "topic": "An earlier revision of the soak audit had falsely declared `CasRootGet` ", "canonical_claim": "An earlier revision of the soak audit had falsely declared `CasRootGet` (a backend counter of GETs on the `roots/` prefix) removed; it was in fact still live, and the oracle's counter swap to `CasRefLogBodyGets` stands only on consumer-isolation grounds, not on `CasRootGet` being dead."}
{"cluster_id": "chunk_013-192", "topic": "Per-event CA audit logging is best-effort: a full SystemLog queue drops ", "canonical_claim": "Per-event CA audit logging is best-effort: a full SystemLog queue drops rows with a warning and never blocks the Core operation."}
{"cluster_id": "chunk_013-193", "topic": "`system", "canonical_claim": "`system.content_addressed_mounts` reports one row per mount slot and classifies state as `live`, `expired`, `terminated`, `fenced`, or `corrupt`."}
{"cluster_id": "chunk_013-194", "topic": "`CasInstrumentedBackend` is an operation-counting ProfileEvents decorato", "canonical_claim": "`CasInstrumentedBackend` is an operation-counting ProfileEvents decorator, not a fault-injection backend."}
{"cluster_id": "chunk_013-195", "topic": "GC start and finish rows", "canonical_claim": "A GC round emits one `Start` row and one `Finish` row in `system.content_addressed_garbage_collection_log`; a `Start` without a matching `Finish` indicates a hung or failed round. `system.cas_gc_log` stores a `Start` row and a `Finish` row for every GC round, with marked and deleted counts, duration, outcome, and a per-round `ProfileEvents` delta."}
{"cluster_id": "chunk_013-196", "topic": "Positive CAS scenarios must have no `Error` finish rows in `system", "canonical_claim": "Positive CAS scenarios must have no `Error` finish rows in `system.content_addressed_garbage_collection_log`; `NotALeader` rows on non-leaders are expected."}
{"cluster_id": "chunk_013-197", "topic": "The backup destination is a real CAS pool that runs standard GC over sha", "canonical_claim": "The backup destination is a real CAS pool that runs standard GC over shadow namespaces, and `clickhouse-disks ca-fsck` verifies it."}
{"cluster_id": "chunk_013-198", "topic": "GC observability includes heartbeat acknowledgement lag, B170 condemn/sp", "canonical_claim": "GC observability includes heartbeat acknowledgement lag, B170 condemn/spare/graduate/delete/fence-out events, retired-list size and oldest-entry age, and an invariant alert for changed shards whose pre-fold events are not folded."}
{"cluster_id": "chunk_013-199", "topic": "In the mount-recovery plan, `claimMountAwaitingExpiry` exposes an option", "canonical_claim": "In the mount-recovery plan, `claimMountAwaitingExpiry` exposes an optional `on_wait_start` callback that fires once with the observed lease and latched deadline so `Store::open` can emit an operator-visible log."}
{"cluster_id": "chunk_013-200", "topic": "Conditional deletes performed by `removeObjectIfTokenMatches` are record", "canonical_claim": "Conditional deletes performed by `removeObjectIfTokenMatches` are recorded by `BlobStorageLogWriter` for all outcomes, with the S3 error attached when applicable."}
{"cluster_id": "chunk_013-201", "topic": "The `gc_fold_clamp` event records namespace, shard, manifest, reason, an", "canonical_claim": "The `gc_fold_clamp` event records namespace, shard, manifest, reason, and `at_version`, making clamp forensics queryable instead of requiring archived server-log archaeology."}
{"cluster_id": "chunk_013-202", "topic": "The corrected GC-log observation includes `entries_condemned`, `entries_", "canonical_claim": "The corrected GC-log observation includes `entries_condemned`, `entries_graduated`, `entries_redeleted`, `fence_outs`, `min_ack`, and `anomalies` along with deletion, outcome, duration, and error fields."}
{"cluster_id": "chunk_013-203", "topic": "`s3_error_rates(node)` reports read/write error and request counters plu", "canonical_claim": "`s3_error_rates(node)` reports read/write error and request counters plus rates, returning `None` when counters are absent rather than fabricating zero; `end_checkpoint` stores the rates as an informational verdict."}
{"cluster_id": "chunk_013-204", "topic": "The harness field `cont_mem_peak_incl_cache` is explicitly cache-inclusi", "canonical_claim": "The harness field `cont_mem_peak_incl_cache` is explicitly cache-inclusive cgroup memory evidence; verdicts use `peak_mem_resident` for resident-memory claims."}
{"cluster_id": "chunk_013-205", "topic": "The dev-scale S13 smoke passed all safety checks, including `fsck dangli", "canonical_claim": "The dev-scale S13 smoke passed all safety checks, including `fsck dangling=0`, replica agreement, bounded abandoned-precommit residual, no failed GC rounds, and `dryrun ⊆ unreachable`; its lone inconclusive result was only an observability gate."}
{"cluster_id": "chunk_013-206", "topic": "The dangling-precommit regression scenario is S30, whose `no unbounded l", "canonical_claim": "The dangling-precommit regression scenario is S30, whose `no unbounded leftovers` verdict must show no `_manifests` leak after the fix."}
{"cluster_id": "chunk_013-207", "topic": "supersede observability", "canonical_claim": "The side-effect-free supersede peek must cause exactly one `blob_retire_replaced` event with `detail[\"superseded_token\"]`, one `CasGcRetireReplaced` increment, and no accompanying `blob_retire` or `CasGcRetiredCondemned` increment. The observability follow-up must make the supersede token peek side-effect-free and carry the old token so exactly one `blob_retire_replaced` event records the repair without a second `blob_retire` or `CasGcRetiredCondemned` increment. A resurrect supersede emits `blob_retire_replaced` and increments `CasGcRetireReplaced`."}
{"cluster_id": "chunk_013-208", "topic": "The facade `explain` surface is bounded test/log-only state with decisio", "canonical_claim": "The facade `explain` surface is bounded test/log-only state with decisions such as hit, mutable refresh, mismatch, miss, oversized bypass, strict bypass, force-fresh read, and invalidation; it has no SQL surface."}
{"cluster_id": "chunk_013-209", "topic": "`graduationDue` is computed from seal summaries as `pending_total > 0 ||", "canonical_claim": "`graduationDue` is computed from seal summaries as `pending_total > 0 || oldest_nonpending_condemn_round < current_round` and requires zero retired-list I/O."}
{"cluster_id": "chunk_013-210", "topic": "The audit classified lower coverage in `CasGc`, `CasStore`, `CasInspect`", "canonical_claim": "The audit classified lower coverage in `CasGc`, `CasStore`, `CasInspect`, and `CasOrphanManifestSweep` as mostly unreachable from the unit sweep, with CLI rendering, cursor/list-budget arms, and multi-process lease/fencing arms covered by integration or soak paths."}
{"cluster_id": "chunk_013-211", "topic": "local GC health columns", "canonical_claim": "`system.content_addressed_mounts` should expose per-disk `is_leader`, `pending_reclaim`, `last_success_age_seconds`, and `wedged_namespace_count` health columns. GC-health columns in `system.content_addressed_mounts` should be nullable and populated only on the local `server_root_id` row; peer rows should contain `NULL` rather than misleading local values."}
{"cluster_id": "chunk_013-212", "topic": "`system", "canonical_claim": "`system.content_addressed_mounts.server_uuid` is typed as `UUID`, while `started_at_ms` and `expires_at_ms` are typed as `DateTime64(3)`."}
{"cluster_id": "chunk_013-213", "topic": "Every new introspection counter must have at least one emit site and at ", "canonical_claim": "Every new introspection counter must have at least one emit site and at least one test asserting a nonzero counter delta; a declared counter without an emit site is considered the S13 INTROSPECTION-1 anti-pattern."}
{"cluster_id": "chunk_013-214", "topic": "The decommission audit is persisted as `member_decommission` begin, per-", "canonical_claim": "The decommission audit is persisted as `member_decommission` begin, per-namespace, and end events in `system.content_addressed_log`, while `system.content_addressed_mounts` shows the victim progressing from `fenced` or `terminated` to row disappearance after slot deletion."}
{"cluster_id": "chunk_013-215", "topic": "The 2026-07-13 soak recorded 1,128,370 PUTs and 5,704,432 GETs across bo", "canonical_claim": "The 2026-07-13 soak recorded 1,128,370 PUTs and 5,704,432 GETs across both nodes during the measured window."}
{"cluster_id": "chunk_013-216", "topic": "committed-part repoint observability", "canonical_claim": "An effective committed-part repoint emits a `system.content_addressed_log` event, a warning-level log, and a ProfileEvent. Every committed-part repoint emits `CasEventType::RefRepoint`, writes a `LOG_WARNING`, and increments a ProfileEvent."}
{"cluster_id": "chunk_013-217", "topic": "The all-tree soak recorded zero unreachable objects and zero dangling ob", "canonical_claim": "The all-tree soak recorded zero unreachable objects and zero dangling objects at both recovery checkpoints and the final converge checkpoint."}
{"cluster_id": "chunk_013-218", "topic": "`CasRefRepoint == 0` is a valid soak assertion for cards without `FREEZE", "canonical_claim": "`CasRefRepoint == 0` is a valid soak assertion for cards without `FREEZE`, `ATTACH`, `DETACH`, `MOVE`, or `REPLACE PARTITION`, while attach/detach lifecycle cards legitimately repoint."}
{"cluster_id": "chunk_013-219", "topic": "The permanent S39 regression is intended to exercise a degraded-but-aliv", "canonical_claim": "The permanent S39 regression is intended to exercise a degraded-but-alive object store, because prior chaos coverage faulted nodes but not S3 while nodes remained alive."}
{"cluster_id": "chunk_013-220", "topic": "The MOVE test gate includes `MOVE PART` and `MOVE PARTITION` in both dir", "canonical_claim": "The MOVE test gate includes `MOVE PART` and `MOVE PARTITION` in both directions, concurrent SELECTs, `fsck` with `dangling=0`, bounded GC reclamation, restart complete-or-rollback behavior, and dedup-on-TO-CA."}
{"cluster_id": "chunk_013-221", "topic": "The COW map's randomized property test must compare insert, update, eras", "canonical_claim": "The COW map's randomized property test must compare insert, update, erase, find, size, ordered iteration, and materialize operations against `std::map<String, RefCommittedRow>`, including copy-then-mutate isolation."}
{"cluster_id": "chunk_013-222", "topic": "The COW implementation must pass equivalence tests against `std::map`, a", "canonical_claim": "The COW implementation must pass equivalence tests against `std::map`, an O(1)-copy assertion, byte-identical `snapshotOf` output, the existing `Ca*:Cas*` battery, and a trace check that `__copy_construct_tree` is no longer a top ref-table stack."}
{"cluster_id": "chunk_013-223", "topic": "A failed S40 fault schedule or zero outage-induced insert failures makes", "canonical_claim": "A failed S40 fault schedule or zero outage-induced insert failures makes the run vacuous and must fail its anti-vacuity verdict instead of passing silently."}
{"cluster_id": "chunk_013-224", "topic": "The CA integrity oracle can remain clean during this loss because no dan", "canonical_claim": "The CA integrity oracle can remain clean during this loss because no dangling CAS ref exists; only a row-count or checksum oracle detects the missing acknowledged rows."}
{"cluster_id": "chunk_013-225", "topic": "The failpoint regression test must make the first deduplicated INSERT fa", "canonical_claim": "The failpoint regression test must make the first deduplicated INSERT fail before Keeper registration, retry the identical row after disabling the failpoint, and observe `count() = 1` rather than the pre-fix false-dedup result `0`."}
{"cluster_id": "chunk_013-226", "topic": "The stabilization campaign rule is that there are no tolerated known red", "canonical_claim": "The stabilization campaign rule is that there are no tolerated known reds; tactical tolerance requires a tracked return item, and standing exceptions require a complete RCA approved by the user."}
{"cluster_id": "chunk_013-227", "topic": "The S23 jemalloc growth is bounded rather than an unbounded leak because", "canonical_claim": "The S23 jemalloc growth is bounded rather than an unbounded leak because its rate decelerates from about 30 to 16.7 MiB per minute and live bytes correspond to a bounded set of active system-log parts reclaimed by merges or TTL."}
{"cluster_id": "chunk_013-228", "topic": "The 11 campaign INCONCLUSIVE scenarios were pre-existing `ci`-scale inst", "canonical_claim": "The 11 campaign INCONCLUSIVE scenarios were pre-existing `ci`-scale instrumentation limitations rather than product bugs, including short GC-log windows, blob-cache masking of fetch counts, unreachable manifest/RSS thresholds, and unavailable compose baselines; full scale is required for final answers."}
{"cluster_id": "chunk_013-229", "topic": "CAS ProfileEvent descriptions should use a lead sentence such as `Number", "canonical_claim": "CAS ProfileEvent descriptions should use a lead sentence such as `Number of ...` or `Total ... in microseconds`, followed by a separate interpretive sentence; event names and value types remain unchanged."}
{"cluster_id": "chunk_013-230", "topic": "The uncertainty exception includes the observed mount-holder identity an", "canonical_claim": "The uncertainty exception includes the observed mount-holder identity and the keeper's local sequence, while `MountConflict` carries the observed identity and distinguishes the branch through its `outcome` string; no new ProfileEvent is required."}
{"cluster_id": "chunk_013-231", "topic": "S42's detailed fsck oracle must fail closed when `stale_edge` is absent,", "canonical_claim": "S42's detailed fsck oracle must fail closed when `stale_edge` is absent, because `stale_edge` is detail-mode-only and a missing key can indicate a binary that predates the invariant."}
{"cluster_id": "chunk_013-232", "topic": "`CasUnresolvedReason::NoAttemptSent` distinguishes a provably unwritten ", "canonical_claim": "`CasUnresolvedReason::NoAttemptSent` distinguishes a provably unwritten key from an ambiguous unresolved PUT, and `CasRefAppendPreAttemptRefused` preserves observability after the no-wedge behavior removes the wedge as evidence."}
{"cluster_id": "chunk_013-233", "topic": "An unmatched remove delta is currently a silent no-op at the edge reduce", "canonical_claim": "An unmatched remove delta is currently a silent no-op at the edge reducer, so the instrumentation round added an unmatched-remove counter rather than changing the reducer's semantics."}
{"cluster_id": "chunk_013-234", "topic": "S42 consistency verdict", "canonical_claim": "The `S42` verdict treats a consistent on-disk and in-memory state as the deciding result; targeted post-durable-install counters are reported rather than gating, while the generic anti-vacuity guard remains gating. For S42, green means consistent on-disk and in-memory state; targeted poison/failpoint counters are reported rather than gating, while a generic allocation-fault anti-vacuity guard remains required."}
{"cluster_id": "chunk_013-235", "topic": "`Verdict", "canonical_claim": "`Verdict.reported` records a genuinely non-gating observation without affecting run status, and must not be used to soften an assertion or convert unavailable data into a pass."}
{"cluster_id": "chunk_013-236", "topic": "A successful force-claim must emit a warning and a `CasEvent` audit reco", "canonical_claim": "A successful force-claim must emit a warning and a `CasEvent` audit record naming the displaced uuid, the new uuid, and the death certificate used."}
{"cluster_id": "chunk_013-237", "topic": "Counters such as `CasGcProbeAHolePresent` and `CasGcProbeAHoleAbsent` ar", "canonical_claim": "Counters such as `CasGcProbeAHolePresent` and `CasGcProbeAHoleAbsent` are process-local and can be erased by chaos restarts, whereas `gc_anomaly` audit rows survive; both carriers are needed for field diagnosis."}
{"cluster_id": "chunk_013-238", "topic": "The S42 consistency verdict separately reports post-restart equality, cl", "canonical_claim": "The S42 consistency verdict separately reports post-restart equality, clean fsck `dangling`/`unaccounted`/`stale_edge` results, and the snapshot integrity oracle; `snapshot_oracle_checked == 0` is inconclusive rather than a pass."}
{"cluster_id": "chunk_013-239", "topic": "Every added ca-soak counter must be registered in `utils/ca-soak/soak/si", "canonical_claim": "Every added ca-soak counter must be registered in `utils/ca-soak/soak/signals.py` or the harness will not read it."}
{"cluster_id": "chunk_013-240", "topic": "The classified renewal mismatch reason is retained by `MountLeaseKeeper`", "canonical_claim": "The classified renewal mismatch reason is retained by `MountLeaseKeeper` before `onRenewMismatch` throws, so `onRenewFailed` can emit it in the fence warning; the reasons include `same_epoch_state_uncertain`, `superseded`, `foreign_writer`, and `vanished`."}
{"cluster_id": "chunk_013-241", "topic": "Stage A's soak PASS criteria require zero data loss, no wedge surviving ", "canonical_claim": "Stage A's soak PASS criteria require zero data loss, no wedge surviving a foreground flush or remount resolution opportunity, clean fsck, bounded `unaccounted` growth attributable to suppressed deletes, and no uninjected ERROR-severity log lines."}
{"cluster_id": "chunk_013-242", "topic": "The authoritative release counter and ERROR line are emitted only after ", "canonical_claim": "The authoritative release counter and ERROR line are emitted only after the final release attempt fails; intermediate INFO lines may still appear, so settled-late tests scope silence to the authoritative message class."}
{"cluster_id": "chunk_013-243", "topic": "A sender-side relink refusal must identify one of `fence`, `unrecovered`", "canonical_claim": "A sender-side relink refusal must identify one of `fence`, `unrecovered`, `wedge`, `poison`, `budget`, or `no-such-part`; the receiver logs the transient reason at INFO and acts only on the certified answer and identity pair."}
{"cluster_id": "chunk_013-244", "topic": "The T14 audit distinguishes salvage figures, which are re-derivable from", "canonical_claim": "The T14 audit distinguishes salvage figures, which are re-derivable from archived logs, from controller live-pass figures, which are unreproducible and ledgered verbatim; the two sources are not silently blended."}
{"cluster_id": "chunk_013-245", "topic": "S3 operation counts derived from rate-limited `WriteBufferFromS3` Trace ", "canonical_claim": "S3 operation counts derived from rate-limited `WriteBufferFromS3` Trace lines are lower bounds; ProfileEvents counts from `gc_phases` are exact."}
{"cluster_id": "chunk_013-246", "topic": "A healthy destructive soak must perform destructive work, leave `ca-fsck", "canonical_claim": "A healthy destructive soak must perform destructive work, leave `ca-fsck --detail` without dangling or stale edges, reach zero backlog, preserve hold/anomaly suppression of irreversible paths, avoid a second full ref LIST after probe-A removal, and expose phase timings and S3 counts."}
{"cluster_id": "chunk_013-247", "topic": "A `Removing` namespace with no terminal record must be surfaced after a ", "canonical_claim": "A `Removing` namespace with no terminal record must be surfaced after a configured number of GC rounds; GC must append nothing while it is stuck."}
{"cluster_id": "chunk_013-248", "topic": "A healthy round must perform nonzero work in every delete family that ha", "canonical_claim": "A healthy round must perform nonzero work in every delete family that has work; fsck must find zero dangling or stale edges; `pending_condemned` and cleanup backlog must reach zero and remain zero for at least three further rounds."}
{"cluster_id": "chunk_013-249", "topic": "The Stage B performance report must sample the preserved T8 specimen wit", "canonical_claim": "The Stage B performance report must sample the preserved T8 specimen without rerunning it, name every untimed span, and land no optimization; T9 is the mandatory closeout after the T8 verdict."}
{"cluster_id": "chunk_013-250", "topic": "The registered system table names are `cas_log`, `cas_gc_log`, and `cas_", "canonical_claim": "The registered system table names are `cas_log`, `cas_gc_log`, and `cas_mounts`; the corresponding C++ class names remain unchanged."}
{"cluster_id": "chunk_013-251", "topic": "The user-facing `system", "canonical_claim": "The user-facing `system.cas_mounts` columns are `started_at` and `expires_at`, both retaining type `DateTime64(3)`."}
{"cluster_id": "chunk_013-252", "topic": "indegree-zero event name", "canonical_claim": "The user-facing `system.cas_log` names are column `generation`, event type `indegree_zero`, detail key `prev_indegree`, and object kind `snapshot`. The CasEventType::IndegZero string representation is indegree_zero."}
{"cluster_id": "chunk_013-253", "topic": "GC log column names", "canonical_claim": "The user-facing `system.cas_gc_log` columns include `server_root_id` and `phase_duration_microseconds`. In system.cas_gc_log, the user-facing key srid is server_root_id, phase_duration_us is phase_duration_microseconds, and phase-metric keys txns_unapplied and txns_opened are transactions_unapplied and transactions_opened; internal C++/Python identifiers such as String srid, report.srid, and total_us remain unchanged."}
{"cluster_id": "chunk_013-254", "topic": "`Verdict", "canonical_claim": "`Verdict.inconclusive` represents unavailable data and must not be silently converted to pass; `Verdict.skipped` explicitly records that an assertion was not evaluated."}
{"cluster_id": "chunk_013-255", "topic": "`cluster_events_snapshot` and `cluster_events_delta` observe `Cas*`, `Di", "canonical_claim": "`cluster_events_snapshot` and `cluster_events_delta` observe `Cas*`, `DiskS3*`, and `S3*` events, while `ca_event_counts_all` classifies bad events including `read_missing`, `dangling_access`, `corrupt_dangle`, `corrupt_decode`, `snap_journal_incoherent`, and `exception`."}
{"cluster_id": "chunk_013-256", "topic": "Scenario cards must never silently skip an assertion and must emit `Verd", "canonical_claim": "Scenario cards must never silently skip an assertion and must emit `Verdict.inconclusive` or `Verdict.skipped` with a reason when evaluation is unavailable."}
{"cluster_id": "chunk_013-257", "topic": "`cas-gc-dryrun` previews condemned objects in the two-phase graduation p", "canonical_claim": "`cas-gc-dryrun` previews condemned objects in the two-phase graduation pipeline, so its candidates may be in the union of fsck classes `unreachable`, `pending-gc`, and `awaiting-gc`; a bounded `pending-gc` residual at fixpoint is expected rather than a leak."}
{"cluster_id": "chunk_013-258", "topic": "A converged GC verdict must use the final `fsck_final` after the end-che", "canonical_claim": "A converged GC verdict must use the final `fsck_final` after the end-checkpoint fixpoint and must count only reclaimable prefixes `blobs` and `_manifests`; non-reclaimable `other` bookkeeping is tracked separately."}
{"cluster_id": "chunk_013-259", "topic": "The S42 allocation-fault oracle must require a nonzero injected-failure ", "canonical_claim": "The S42 allocation-fault oracle must require a nonzero injected-failure count or report `inconclusive`; a generic nonzero `MEMORY_LIMIT_EXCEEDED` count does not prove that a post-durable-PUT in-memory-apply window was exercised."}
{"cluster_id": "chunk_013-260", "topic": "A 4-hour chaos soak can become blind when detail-less fsck exceeds its 1", "canonical_claim": "A 4-hour chaos soak can become blind when detail-less fsck exceeds its 180-second bound at about 2.14 million objects; long-run validation also needs a TTL-band oracle that does not hard-fail during an active fault window and a pool-size or compaction limit."}
{"cluster_id": "chunk_013-261", "topic": "CAS introspection is exposed through `clickhouse-disks ca-fsck`, `ca-gc-", "canonical_claim": "CAS introspection is exposed through `clickhouse-disks ca-fsck`, `ca-gc-dryrun`, `system.content_addressed_log`, and `system.content_addressed_garbage_collection_log`."}
{"cluster_id": "chunk_013-262", "topic": "`system", "canonical_claim": "`system.cas_log` emits one row per CAS protocol event and is the primary audit trail for investigating pool state."}
{"cluster_id": "chunk_013-263", "topic": "The CAS soak harness derives a deterministic operation ledger from one `", "canonical_claim": "The CAS soak harness derives a deterministic operation ledger from one `--seed` integer using a `splitmix64` stream, and checkpoints compare exact integer aggregates on both replicas and against an in-process oracle before running `clickhouse-disks cas-fsck`."}
{"cluster_id": "chunk_013-264", "topic": "Scenario reports must include `report", "canonical_claim": "Scenario reports must include `report.md`, `report.json`, `metrics.sqlite`, raw system-table extracts, pool-size samples, and container-resource samples, and missing observability data must fail loudly rather than being silently converted to `pass`."}
{"cluster_id": "chunk_013-265", "topic": "A scenario must reject `system", "canonical_claim": "A scenario must reject `system.cas_log` rows with `read_missing`, `dangling_access`, `corrupt_dangle`, `corrupt_decode`, `snap_journal_incoherent`, or `exception` outcomes unless it is an explicitly negative fail-closed test."}
{"cluster_id": "chunk_013-266", "topic": "GC phase rows", "canonical_claim": "`system.cas_gc_log` stores one `Phase` row for each GC phase reached, with that phase's duration, `ProfileEvents` delta, and phase-specific counts. Each GC phase is intended to emit its own row in `system.content_addressed_garbage_collection_log`, with further study deferred until metrics from real runs exist."}
{"cluster_id": "chunk_013-267", "topic": "All rows for one GC round share a `round_id`; rounds are emitted by the ", "canonical_claim": "All rows for one GC round share a `round_id`; rounds are emitted by the background scheduler with `trigger = 'Scheduled'` and by `SYSTEM CAS GC RUN` with `trigger = 'Manual'`."}
{"cluster_id": "chunk_013-268", "topic": "In `system", "canonical_claim": "In `system.cas_gc_log`, `outcome` is `Unknown` for `Start`, `Success` for a completed leader round, `NotALeader` when another replica holds the lease, `Deferred` for the skip-unchanged path, or `Error` when the round throws."}
{"cluster_id": "chunk_013-269", "topic": "`round_id` identifies every row of one round attempt, including attempts", "canonical_claim": "`round_id` identifies every row of one round attempt, including attempts that never committed or never led, whereas `round` is zero on `Start` rows."}
{"cluster_id": "chunk_013-270", "topic": "A round that defers or never acquires the lease emits only the phases it", "canonical_claim": "A round that defers or never acquires the lease emits only the phases it reaches, and a throwing round still emits the phase row in which it died."}
{"cluster_id": "chunk_013-271", "topic": "`system", "canonical_claim": "`system.cas_log` is the fine-grained per-decision complement to `system.cas_gc_log`, recording writer and GC events such as blob puts, root/ref transitions, in-degree changes, retire decisions, recheck verdicts, deletes, and dangling or corruption findings."}
{"cluster_id": "chunk_013-272", "topic": "The `system", "canonical_claim": "The `system.cas_log` `detail` map carries structured event-specific facts such as `condemn_round`, `superseded_token`, `code`, and `site`."}
{"cluster_id": "chunk_013-273", "topic": "`system", "canonical_claim": "`system.cas_mounts` reads directly from the CAS backend on every query and has no persisted log behind it."}
{"cluster_id": "chunk_013-274", "topic": "The `system", "canonical_claim": "The `system.cas_mounts` `pending_reclaim` value is the process-observed cumulative two-phase deletion backlog, computed as condemned entries minus executed exact-token deletes."}
{"cluster_id": "chunk_013-275", "topic": "The `is_leader`, `pending_reclaim`, `last_success_age_seconds`, and `wed", "canonical_claim": "The `is_leader`, `pending_reclaim`, `last_success_age_seconds`, and `wedged_namespace_count` columns in `system.cas_mounts` are process-local and populated only on the querying server's own mount row; peer rows contain `NULL` for them."}
{"cluster_id": "chunk_013-276", "topic": "Repair observability needs the same per-round cap as Probe A, with true ", "canonical_claim": "Repair observability needs the same per-round cap as Probe A, with true totals and truncation status carried in emitted gc_anomaly rows and aggregate counters for omitted rows"}
{"cluster_id": "chunk_013-277", "topic": "Probe A requires deterministic sampling cadence and durable sample due, ", "canonical_claim": "Probe A requires deterministic sampling cadence and durable sample due, performed, and skipped observability; its result must not be treated as a substitute for the mount-time store-quality gate."}
{"cluster_id": "chunk_013-278", "topic": "Version 26", "canonical_claim": "Version 26.3 adds the `SYSTEM WAIT BLOBS CLEANUP` privilege."}
{"cluster_id": "chunk_013-279", "topic": "`clampBefore` emits a `clamped` event row containing the clamped key and", "canonical_claim": "`clampBefore` emits a `clamped` event row containing the clamped key and shard, providing operator visibility into GC clamp conditions."}
{"cluster_id": "chunk_013-280", "topic": "A healthy-pool soak must require `frontier_proven == frontier_namespaces", "canonical_claim": "A healthy-pool soak must require `frontier_proven == frontier_namespaces` on every round, `unattributed == 0`, `probe_budget == 0` or a sufficiently raised `gc_frontier_probe_budget`, and a pending-reclaim backlog that drains to zero and stays there."}
{"cluster_id": "chunk_013-281", "topic": "system", "canonical_claim": "system.content_addressed_mounts types server_uuid as UUID and started_at_ms/expires_at_ms as DateTime64(3) (previously String and raw UInt64 unix-ms); the ContentAddressedLog system-log description was corrected from 'optional, off by default' to reflect that it is enabled by default while the CA disk feature is experimental."}
{"cluster_id": "chunk_013-282", "topic": "Risk register #17 (Low, confidence Med): `CasGc", "canonical_claim": "Risk register #17 (Low, confidence Med): `CasGc.cpp:169-203` and best-effort catch sites implement a broad deliberate fail-open surface (meta jobs swallow everything per the never-throw-on-404 rule; cleanup passes record-and-continue) that is correct by design, but chronic drift from this policy is observable only via `CasGcMetaWriteAnomaly`-class counters, so those counters need to be alerted on."}
{"cluster_id": "chunk_013-283", "topic": "The D1 plan requires new GC fold and discovery tests to run with both `g", "canonical_claim": "The D1 plan requires new GC fold and discovery tests to run with both `gc_shards=1` and `gc_shards>1`."}
{"cluster_id": "chunk_013-284", "topic": "The copy-forward observability work also records `retired_view_advance` ", "canonical_claim": "The copy-forward observability work also records `retired_view_advance` per-view events, `mount_remount` events, and GC round columns `entries_condemned`, `entries_graduated`, `entries_redeleted`, `fence_outs`, `min_ack`, and `anomalies`."}
{"cluster_id": "chunk_013-285", "topic": "The planned `system", "canonical_claim": "The planned `system.content_addressed_mounts` table exposes one point-in-time row per srid with `disk_name`, `srid`, `server_uuid`, `writer_epoch`, `expires_at_ms`, `min_active`, `observed_gc_round`, `gc_fenced`, and `state`."}
{"cluster_id": "chunk_013-286", "topic": "The introspection-first plan is pure instrumentation: it adds counters o", "canonical_claim": "The introspection-first plan is pure instrumentation: it adds counters only, changes no behavior, tables, or configuration, and places choke-point totals in `CasBlobMeta.cpp`, per-reason counters at `CasBuild.cpp` call sites, and GC counters inside the GC round or its bounded pool."}
{"cluster_id": "chunk_013-287", "topic": "The `content_addressed_log` allocation trim should pass `CasEvent` by va", "canonical_claim": "The `content_addressed_log` allocation trim should pass `CasEvent` by value so `makeCasEventSink` can move fields, and should change the templated `reason` column from `String` to `LowCardinality`; recorded rows remain behaviorally unchanged."}
{"cluster_id": "chunk_013-288", "topic": "S23 memory gate", "canonical_claim": "The S23 memory gate should key on per-node `MemoryTracking` growth rather than RSS, keep RSS informational, and if RSS remains gated use a settled per-node baseline instead of a cold-boot max-across-nodes comparison. For S23, baseline `MemoryTracking` after an initial idle round or settle period, measure later-window slope instead of cold-boot delta, and reserve a tight plateau assertion for `--scale full`; `dev` and `ci` windows are too short to reach steady state."}
{"cluster_id": "chunk_013-289", "topic": "The proposed `RefResolve` audit change preserves events for cold, forced", "canonical_claim": "The proposed `RefResolve` audit change preserves events for cold, forced, and strict resolves but suppresses the event for a warm `CachedForLoad` hit with a matching manifest."}
{"cluster_id": "chunk_013-290", "topic": "Phase timing is always enabled with no setting; `GcPhaseTimer` uses a sn", "canonical_claim": "Phase timing is always enabled with no setting; `GcPhaseTimer` uses a snapshot difference of the current thread's `ProfileEvents` rather than a nested `ProfileEventsScope`."}
{"cluster_id": "chunk_013-291", "topic": "The added fence/remount log lines are edge-triggered once per fence epis", "canonical_claim": "The added fence/remount log lines are edge-triggered once per fence episode and therefore do not need a rate limiter; if per-attempt failures later need throttling, `LogFrequencyLimiter` is preferred because it keys on logger name and format string, unlike logger-wide `LogSeriesLimiter`."}
{"cluster_id": "chunk_013-292", "topic": "Probe A is demoted from a fold-aborting hole detector to a sampled store", "canonical_claim": "Probe A is demoted from a fold-aborting hole detector to a sampled store-quality detector: it reports due/performed/skipped observations but aborts and gates nothing, while the round retains one strict hint enumeration."}
{"cluster_id": "chunk_013-293", "topic": "Under Stage-A deletion suppression, `--max-pool-gb` can pace inserts but", "canonical_claim": "Under Stage-A deletion suppression, `--max-pool-gb` can pace inserts but cannot bound the pool because reclamation never reduces it; therefore a growing-pool 90-minute soak cannot satisfy a final fsck-clean gate."}
{"cluster_id": "chunk_013-294", "topic": "The release-related lines from `abandonBuildBestEffort` and the caller s", "canonical_claim": "The release-related lines from `abandonBuildBestEffort` and the caller scope-guard `abort` are demoted to INFO with transient wording; `logCasWriteRetryLater` remains unchanged because it is a generic retry message rather than a release claim."}
{"cluster_id": "chunk_013-295", "topic": "The derived asynchronous metric `ReaderExecutorModeledCostMsPerRequested", "canonical_claim": "The derived asynchronous metric `ReaderExecutorModeledCostMsPerRequestedMiB` is removed, while ProfileEvents `ReaderExecutorModeledCostMicroseconds` and `ReaderExecutorRequestedBytes` remain available for observers to compute the ratio."}
{"cluster_id": "chunk_013-296", "topic": "The S12 ten-replica harness uses `docker-compose-10replicas", "canonical_claim": "The S12 ten-replica harness uses `docker-compose-10replicas.yml`, `Cluster(node_count=10)`, and one command to validate 10-replica byte-identical convergence, CA deduplication, and `fsck dangling=0/unreachable=0`."}
{"cluster_id": "chunk_013-297", "topic": "Suspected protocol-step costs must be instrumented before optimization d", "canonical_claim": "Suspected protocol-step costs must be instrumented before optimization decisions are made."}
{"cluster_id": "chunk_013-298", "topic": "CAS sanitizer lanes are sharded because an unsharded lane exceeds the si", "canonical_claim": "CAS sanitizer lanes are sharded because an unsharded lane exceeds the six-hour GitHub job timeout and is killed before it uploads results"}
{"cluster_id": "chunk_013-299", "topic": "CAS metric filters in `dump_cas_metrics", "canonical_claim": "CAS metric filters in `dump_cas_metrics.py`, `scenarios/framework/observe.py`, and `smoke_relink_validate.sh` use `LIKE 'CAS%'` after the metric-prefix normalization."}
{"cluster_id": "chunk_013-300", "topic": "The async CAS metric format strings use `CASGCIsLeader_{}`, `CASGCPendin", "canonical_claim": "The async CAS metric format strings use `CASGCIsLeader_{}`, `CASGCPendingReclaim_{}`, `CASGCLastSuccessAgeSeconds_{}`, and `CASGCWedgedNamespaces_{}` after the `CASGc` to `CASGC` normalization."}
{"cluster_id": "chunk_013-301", "topic": "detail[\"srid\"] in system", "canonical_claim": "detail[\"srid\"] in system.cas_log was renamed to server_root_id because it described the same fact already emitted under server_root_id by CasPool and exposed under system.cas_mounts and SYSTEM CAS DROP POOL MEMBER."}
{"cluster_id": "chunk_013-302", "topic": "The GC-wedge fix was validated by a 20-minute chaos run with dangling=0,", "canonical_claim": "The GC-wedge fix was validated by a 20-minute chaos run with dangling=0, no removal-fold clamps, and subsequent clean fsck with unreachable=0 and pending_gc=0."}
{"cluster_id": "chunk_013-303", "topic": "The full two-hour chaos soak completed with 38 chaos faults and no produ", "canonical_claim": "The full two-hour chaos soak completed with 38 chaos faults and no product-visible fatal or logical failures; the residual availability classes included 20 `aborted_persistent`, 3 `mount_fenced`, and 1 `s3_transient` outcome."}
{"cluster_id": "chunk_013-304", "topic": "Fix commit `77484196b0d` landed the generic `renameParts` transaction cl", "canonical_claim": "Fix commit `77484196b0d` landed the generic `renameParts` transaction close, and the validation reported S40 10/10, `dl_probe` LOST=0, S39 dev 11/11, S36 26/26, and a clean 20-minute seed-42 soak."}
{"cluster_id": "chunk_013-305", "topic": "Stage A's final gate counts were 1566/1566 release tests and 1570/1570 A", "canonical_claim": "Stage A's final gate counts were 1566/1566 release tests and 1570/1570 ASan tests, with zero sanitizer findings in the ASan run."}
{"cluster_id": "chunk_013-306", "topic": "The no-uninjected-ERROR soak criterion was admitted as a named exception", "canonical_claim": "The no-uninjected-ERROR soak criterion was admitted as a named exception for the pre-existing relink message `Code: 210 ... did not prove it still holds the manifest it offered for part ... by relink`; the retry path stores the entry, backs off, and re-selects a fresh source."}
{"cluster_id": "chunk_013-307", "topic": "ack-floor request cost", "canonical_claim": "The ack-floor round reduces the example 100,000-table per-round request cost from about 2.4 million requests to about 2,000–3,000 plus the discovery LIST sweep. For the 100,000-table by 8-root-shard slowly-changing example, the redesign estimates 2,000–3,000 requests costing approximately $0.001–0.01 and seconds per round, versus approximately 2,400,000 requests and $4.6 today."}
{"cluster_id": "chunk_013-308", "topic": "GC current metrics", "canonical_claim": "`CasGcIsLeader` is 1 while the local scheduler is leader, and `CasGcPendingReclaimEntries` reports the process-local reclaim backlog. The first CAS `CurrentMetrics` gauges are `CasGcIsLeader` and `CasGcPendingReclaimEntries`; the former reflects whether the latest round acquired the lease, while the latter is process-local cumulative condemned entries minus executed exact-token deletes."}
{"cluster_id": "chunk_013-309", "topic": "`CountingBackend` tracks per-key `head`, `get`, `getStream`, `put`, and ", "canonical_claim": "`CountingBackend` tracks per-key `head`, `get`, `getStream`, `put`, and `list` counts plus whole-read, ranged-length, and key-substring I/O metrics used to assert streaming-memory and idle-round behavior."}
{"cluster_id": "chunk_013-310", "topic": "Before the flat-combining queue, `casPut` conflicts measured 35% at `wor", "canonical_claim": "Before the flat-combining queue, `casPut` conflicts measured 35% at `workers=2`, with each conflict costing one extra conditional `PUT` and one `GET`."}
{"cluster_id": "chunk_013-311", "topic": "The shard-mutation queue achieved approximately 2", "canonical_claim": "The shard-mutation queue achieved approximately 2.3× CAS-write compression and reduced intra-server conflicts from approximately 257k per hour to approximately 11 per hour."}
{"cluster_id": "chunk_013-312", "topic": "The pre-P1/P2/P9 soak estimate was approximately 517 million read reques", "canonical_claim": "The pre-P1/P2/P9 soak estimate was approximately 517 million read requests and 73 million write requests per day, costing approximately 571 dollars per day in request pricing."}
{"cluster_id": "chunk_013-313", "topic": "In the 2026-07-19 post-fix chaos soak, a GC success round averaged appro", "canonical_claim": "In the 2026-07-19 post-fix chaos soak, a GC success round averaged approximately 5,305 GETs, 12,812 HEADs, and 4,156 DELETEs."}
{"cluster_id": "chunk_013-314", "topic": "In the 2026-07-19 post-fix chaos soak, CAS-internal housekeeping issued ", "canonical_claim": "In the 2026-07-19 post-fix chaos soak, CAS-internal housekeeping issued 1,128,865 PUTs, dominated by one small `.meta` sidecar PUT alongside each real content blob PUT."}
{"cluster_id": "chunk_013-315", "topic": "The CA-S3 lane's cacheless warm scan measured approximately three times ", "canonical_claim": "The CA-S3 lane's cacheless warm scan measured approximately three times local latency, 141 ms versus 47 ms, with 186 S3 GETs."}
{"cluster_id": "chunk_013-316", "topic": "For a folding round, the implemented GC cost includes one full LIST of `", "canonical_claim": "For a folding round, the implemented GC cost includes one full LIST of `cas/ns/stream/`, one LIST of `gc/server-roots/` plus one mount GET per mount, one HEAD per newly condemned blob, one DELETE per graduate, and one `gc/state` CAS."}
{"cluster_id": "chunk_013-317", "topic": "The CAS verification corpus includes 101 gtest files, 23 TLA+ models, 14", "canonical_claim": "The CAS verification corpus includes 101 gtest files, 23 TLA+ models, 14 adversarial scenarios, and 51 stateless tests referencing `content_addressed`."}
{"cluster_id": "chunk_013-318", "topic": "The planned ack-floor observability adds `CasGcRetiredGraduated`, `CasGc", "canonical_claim": "The planned ack-floor observability adds `CasGcRetiredGraduated`, `CasGcRetiredSpared`, `CasGcRetiredCondemned`, `CasGcHeartbeatFenceOuts`, and `CasGcFloorHeldByStaleAck` counters, plus `RoundReport` fields for graduated, spared, condemned, fence-outs, and `min_ack`."}
{"cluster_id": "chunk_013-319", "topic": "The D1 1-hour chaos soak ended with `table_rows` 1,765,870, `fsck reacha", "canonical_claim": "The D1 1-hour chaos soak ended with `table_rows` 1,765,870, `fsck reachable=0, unreachable=0, dangling=0, dryrun_count=0`, zero aborted-retried inserts, zero transport-retried operations, and no failed GC finish rows."}
{"cluster_id": "chunk_013-320", "topic": "During the D1 soak, memory remained flat with approximately 1", "canonical_claim": "During the D1 soak, memory remained flat with approximately 1.6–2.0 GB per ClickHouse replica and no OOM; disk usage returned to 507 GB free after teardown."}
{"cluster_id": "chunk_013-321", "topic": "The D1 soak observed physical pool usage peaking at approximately 39", "canonical_claim": "The D1 soak observed physical pool usage peaking at approximately 39.7 GiB while logical bytes were approximately 101 MiB, attributed for follow-up to full-GC/B140 debris and possible RustFS overwrite-version retention rather than a safety defect."}
{"cluster_id": "chunk_013-322", "topic": "The D2 scenario sweep had 14 PASS, 10 INCONCLUSIVE, and 8 FAIL results, ", "canonical_claim": "The D2 scenario sweep had 14 PASS, 10 INCONCLUSIVE, and 8 FAIL results, and reported zero D1 regressions; D1-specific scenarios S30, S34, and S35 passed."}
{"cluster_id": "chunk_013-323", "topic": "The live GCS acceptance cycle requires the capability probe to pass 8/8 ", "canonical_claim": "The live GCS acceptance cycle requires the capability probe to pass 8/8 plus versioning checks, then replication and checksums, churn with `OPTIMIZE FINAL`, two-phase reclaim observed in the bucket, clean fsck, and `DROP TABLE` to metadata-only."}
{"cluster_id": "chunk_013-324", "topic": "Live AWS S3 GC rounds took 30–40 seconds, compared with about 5–10 secon", "canonical_claim": "Live AWS S3 GC rounds took 30–40 seconds, compared with about 5–10 seconds on RustFS; the shipped default `gc_interval_sec=60` is sane for AWS, while a 10-second soak interval causes back-to-back rounds."}
{"cluster_id": "chunk_013-325", "topic": "The 24 GiB S01 rerun passed streaming, multipart, deduplication, replica", "canonical_claim": "The 24 GiB S01 rerun passed streaming, multipart, deduplication, replica agreement, fsck, and GC checks, but recorded 41,983 `S3HeadObject` operations for one table, 13,116 read errors out of 69,187 reads, and 717 write errors out of 8,330 writes."}
{"cluster_id": "chunk_013-326", "topic": "The prior soak recorded 8", "canonical_claim": "The prior soak recorded 8.7 million `CasBlobHead` operations, 380k landed `CasOtherCas` shard writes, 257k conflict attempts, and 1.29 million `CasOtherGet` operations per replica in one hour; shard bodies were 250–340 KB and 95% of a typical 3157-event body was journal tail, while later fsck timeouts belonged to the leak-bloated LIST family tracked by B146 and B154."}
{"cluster_id": "chunk_013-327", "topic": "The mount-lease acceptance event is `mount_conflict` with `branch = \"fen", "canonical_claim": "The mount-lease acceptance event is `mount_conflict` with `branch = \"fenced_by_gc\"` on the recoverable fence path."}
{"cluster_id": "chunk_013-328", "topic": "`ProfileEvents::CasGcPrecommitRevisitForced` counts forced re-folds of t", "canonical_claim": "`ProfileEvents::CasGcPrecommitRevisitForced` counts forced re-folds of token-stable ref-shards whose watermark-dead live precommit must be reclaimed."}
{"cluster_id": "chunk_013-329", "topic": "cache-over-CA read reduction", "canonical_claim": "The cache-over-CA integration test demonstrates a cold scan with positive `CachedReadBufferReadFromSourceBytes`, a warm scan with at least ten times fewer source bytes, positive `CachedReadBufferReadFromCacheBytes`, and populated `system.filesystem_cache`. A cache over CA eliminated repeated remote reads in the probe: warmed cached CA reached about 45 ms with zero S3 GETs versus about 141 ms and 186 GETs for raw CA."}
{"cluster_id": "chunk_013-330", "topic": "The manifest/precommit audit acceptance test requires one `manifest_put`", "canonical_claim": "The manifest/precommit audit acceptance test requires one `manifest_put` and one `precommit_removed` row for the corresponding lifecycle operations."}
{"cluster_id": "chunk_013-331", "topic": "Phase 3 registers eight `ProfileEvents`: `CasPartFolderViewHits`, `CasPa", "canonical_claim": "Phase 3 registers eight `ProfileEvents`: `CasPartFolderViewHits`, `CasPartFolderViewMutableRefreshes`, `CasPartFolderViewValidationMismatches`, `CasPartFolderViewMisses`, `CasPartFolderViewEvictions`, `CasPartFolderViewOversizedBypasses`, `CasPartFolderViewInvalidations`, and `CasPartFolderManifestGets`."}
{"cluster_id": "chunk_013-332", "topic": "Phase 3 registers `CurrentMetrics::CasPartFolderCacheBytes` and `Current", "canonical_claim": "Phase 3 registers `CurrentMetrics::CasPartFolderCacheBytes` and `CurrentMetrics::CasPartFolderCacheEntries` for retained view-cache size and entry count."}
{"cluster_id": "chunk_013-333", "topic": "`CasPartFolderManifestGets` is incremented at the successful manifest bo", "canonical_claim": "`CasPartFolderManifestGets` is incremented at the successful manifest body `GET` inside `Store::readManifestShared`, because the facade cannot distinguish a decode-cache hit from a body read."}
{"cluster_id": "chunk_013-334", "topic": "With retention disabled, five repeated `CachedForLoad` view builds perfo", "canonical_claim": "With retention disabled, five repeated `CachedForLoad` view builds perform one manifest body `GET` because the decode cache absorbs repeats and perform five mandatory manifest `HEAD`s."}
{"cluster_id": "chunk_013-335", "topic": "The acceptance metric is `CasPartFolderManifestGets` no greater than the", "canonical_claim": "The acceptance metric is `CasPartFolderManifestGets` no greater than the number of materialized load windows in controlled tests, rather than a server-wide inequality."}
{"cluster_id": "chunk_013-336", "topic": "Eight re-condemnations within one promote closure are considered unreach", "canonical_claim": "Eight re-condemnations within one promote closure are considered unreachable at a five-second GC interval, making the bounded loop effectively deterministic while remaining honestly bounded."}
{"cluster_id": "chunk_013-337", "topic": "The v3 meta design estimates three paid PUTs per steady-state blob lifet", "canonical_claim": "The v3 meta design estimates three paid PUTs per steady-state blob lifetime, roughly 150 bytes of metadata per live blob, and about 10 dollars per million blobs on AWS request pricing."}
{"cluster_id": "chunk_013-338", "topic": "GC condemn, spare, and delete meta operations must use a bounded paralle", "canonical_claim": "GC condemn, spare, and delete meta operations must use a bounded parallel pool because one million sequential operations at approximately 20 ms each would take about 5.5 hours per pass."}
{"cluster_id": "chunk_013-339", "topic": "For one productive ref handoff, the obsolete design models writer cost a", "canonical_claim": "For one productive ref handoff, the obsolete design models writer cost as `K` PUTs, GC log intake as `A·K` body reads plus `ceil(K/1000)` LIST pages, productive ref-fold reads as `3R + H`, and post-adoption trim as `ceil(K/1000)` DELETE requests."}
{"cluster_id": "chunk_013-340", "topic": "For a 1 MiB run block and a 16 MiB `gc_ref_fold_namespace_bytes` budget,", "canonical_claim": "For a 1 MiB run block and a 16 MiB `gc_ref_fold_namespace_bytes` budget, the obsolete Phase 1 ref-fold construction requires about 17 MiB plus small bounded buffers, independent of total live-ref cardinality."}
{"cluster_id": "chunk_013-341", "topic": "Before the retired-in-snapshot change, each GC folding round performs on", "canonical_claim": "Before the retired-in-snapshot change, each GC folding round performs one GET of the prior retired artifact and one PUT of the next retired artifact per GC shard."}
{"cluster_id": "chunk_013-342", "topic": "The add-only freshness-meta fix removes one spare-side meta GET and CAS ", "canonical_claim": "The add-only freshness-meta fix removes one spare-side meta GET and CAS per spared entry and adds no per-delete I/O."}
{"cluster_id": "chunk_013-343", "topic": "For one GC round, ref-specific request cost is approximately `LIST_pages", "canonical_claim": "For one GC round, ref-specific request cost is approximately `LIST_pages(Q) + 0.08 * (K + H)` under the stated S3 Standard request-price ratio, where `K` is new log GETs and `H` is manifest-body cache misses."}
{"cluster_id": "chunk_013-344", "topic": "CAS conditional-write observability separates attempts, SDK retries that", "canonical_claim": "CAS conditional-write observability separates attempts, SDK retries that must remain zero, and outcome counts by class."}
{"cluster_id": "chunk_013-345", "topic": "The ref snapshot/log implementation exposes counters for LIST pages, bod", "canonical_claim": "The ref snapshot/log implementation exposes counters for LIST pages, body GETs, post-snapshot logs per table, snapshot PUT bytes, `H`, emitted edges, cleanup backlog, restarts, resolutions, and wedges."}
{"cluster_id": "chunk_013-346", "topic": "The RelWithDebInfo coverage sweep ran the full CAS test selection with 8", "canonical_claim": "The RelWithDebInfo coverage sweep ran the full CAS test selection with 825 of 825 tests passing and produced 2.6 MB of profdata from a 174 MB profraw file."}
{"cluster_id": "chunk_013-347", "topic": "Coverage was 100% line and region for `CasRefIds", "canonical_claim": "Coverage was 100% line and region for `CasRefIds.h` and `CasRefSnapshotCodec.cpp`, 98.44% line and 97.06% region for `CasRefLogCodec.cpp`, and 99.53% line and 99.31% region for `CasRefStateMachine.cpp`."}
{"cluster_id": "chunk_014-001", "topic": "Coverage for CasGc.cpp, CasStore.cpp, CasFsck.cpp, CasInspect.cpp, and", "canonical_claim": "Coverage for `CasGc.cpp`, `CasStore.cpp`, `CasFsck.cpp`, `CasInspect.cpp`, and `CasOrphanManifestSweep.cpp` was respectively 90.06%, 84.60%, 86.68%, 62.15%, and 77.50% by line."}
{"cluster_id": "chunk_014-002", "topic": "After adding the five gap tests, the", "canonical_claim": "After adding the five gap tests, the full CAS sweep passed 825 tests with zero failures, compared with 820 baseline tests."}
{"cluster_id": "chunk_014-003", "topic": "CAS observability separates HTTP attempts, SDK retries", "canonical_claim": "CAS observability separates HTTP attempts, SDK retries, CAS-controlled retries, exact-key resolution outcomes, fence and lease-budget refusals, late responses, uncertain results, attempt and logical-operation latency, and effective timeout/lease configuration."}
{"cluster_id": "chunk_014-004", "topic": "Rollback failures swallowed by dropRefBestEffort must be", "canonical_claim": "Rollback failures swallowed by `dropRefBestEffort` must be logged with `tryLogCurrentException` and counted by `ProfileEvents::CasRefRollbackBestEffortDropFailed`."}
{"cluster_id": "chunk_014-005", "topic": "Process-global CasGcIsLeader and CasGcPendingReclaimEntries gauges are invalid", "canonical_claim": "Process-global `CasGcIsLeader` and `CasGcPendingReclaimEntries` gauges are invalid with multiple CAS disks because their values are clobbered; per-disk health belongs on `system.content_addressed_mounts`."}
{"cluster_id": "chunk_014-006", "topic": "ProfileEvents::CasRefLatePredecessorObserved counts each trySnapshotPublishOnce attempt whose snapshot_min_log_age_ms", "canonical_claim": "`ProfileEvents::CasRefLatePredecessorObserved` counts each `trySnapshotPublishOnce` attempt whose `snapshot_min_log_age_ms` grace window stops at a young tail entry."}
{"cluster_id": "chunk_014-007", "topic": "The sustained-load snapshot trigger collapsed to approximately", "canonical_claim": "The sustained-load snapshot trigger collapsed to approximately one full snapshot per publish latency, observed at roughly 6–8 publishes per second and 20–26 GB/hour per node, instead of one publish per 64 new logs."}
{"cluster_id": "chunk_014-008", "topic": "conditional S3 write accounting", "canonical_claim": "In the 2-hour chaos soak, S3 traffic totaled approximately 1.128 million PUTs, 5.70 million GETs, 9.58 million HEADs, 82 thousand LISTs, and 764 thousand DELETEs; `CasConditionalWriteAttempts` was approximately 1.133 million, so nearly every PUT was a controlled conditional write. Additionally, Every measured S3 PUT corresponded one-to-one with a controlled conditional write: 1,128,370 PUTs versus 1,133,550 `CasConditionalWriteAttempts` after chaos attempt inflation."}
{"cluster_id": "chunk_014-009", "topic": "The same soak attributed approximately 263 thousand", "canonical_claim": "The same soak attributed approximately 263 thousand blob-body PUTs, 175 thousand manifest PUTs, 82 thousand ref-log appends, and 598 thousand freshness-tag PUTs; ref-log batching compressed about 3.5 million logical ref operations at a 43:1 ratio."}
{"cluster_id": "chunk_014-010", "topic": "The .meta choke-point counters are CasMetaPut, CasMetaCas", "canonical_claim": "The `.meta` choke-point counters are `CasMetaPut`, `CasMetaCas`, and `CasMetaDelete`; the reason counters are `CasMetaCreateClean`, `CasMetaAdoptBackfill`, and `CasMetaResurrectClean`."}
{"cluster_id": "chunk_014-011", "topic": "The GC attribution counters are CasGcMetaOps for", "canonical_claim": "The GC attribution counters are `CasGcMetaOps` for freshness-meta operations on the round's bounded pool and `CasGcEnumerationPages` for physical-universe LIST pages; the former is pool-scoped and global-only, while the latter is round-scoped and should land in the GC log's `ProfileEvents` map."}
{"cluster_id": "chunk_014-012", "topic": "The dedup-cache instrumentation counters are CasDedupCacheHits and", "canonical_claim": "The dedup-cache instrumentation counters are `CasDedupCacheHits` and `CasDedupCacheMisses`, incremented at the lookup hit and miss branches without changing control flow."}
{"cluster_id": "chunk_014-013", "topic": "Manifest-trust relink adoption is expected to reduce", "canonical_claim": "Manifest-trust relink adoption is expected to reduce a relink fetch from roughly 36 GETs and 68 HEADs per part to one manifest read, targeting about 30% of the read-class workload."}
{"cluster_id": "chunk_014-014", "topic": "An unclean takeover without a GC fence", "canonical_claim": "An unclean takeover without a GC fence costs approximately `mount_lease_ttl_ms + materialization_grace_ms`, about 60 seconds with the stated defaults, before writable mount opens."}
{"cluster_id": "chunk_014-015", "topic": "Deduplication adoption was 69% in the soak", "canonical_claim": "Deduplication adoption was 69% in the soak, with about 598,000 adopts versus 264,000 uploads."}
{"cluster_id": "chunk_014-016", "topic": "A successful ch1 GC round averaged about", "canonical_claim": "A successful ch1 GC round averaged about 1,850 GETs, 4,720 HEADs, and 1,320 DELETEs."}
{"cluster_id": "chunk_014-017", "topic": "GC accounted for about 13% of GETs", "canonical_claim": "GC accounted for about 13% of GETs, 20% of HEADs, and nearly all DELETEs, estimated at about 6.5% of the AWS S3 request cost in the run."}
{"cluster_id": "chunk_014-018", "topic": "The soak measured about 1.96 GB of", "canonical_claim": "The soak measured about 1.96 GB of IO-buffer churn per GC round while useful `ReadBufferFromS3Bytes` reads averaged 24.4 MB per round."}
{"cluster_id": "chunk_014-019", "topic": "The AWS S3 Standard request-cost estimate was", "canonical_claim": "The AWS S3 Standard request-cost estimate was approximately 12.2 dollars for 72 minutes, or about 10 dollars per hour, excluding storage."}
{"cluster_id": "chunk_014-020", "topic": "The scenario stabilization dev campaign completed 35", "canonical_claim": "The scenario stabilization dev campaign completed 35 of 35 scenarios with zero skips, and S13 was the only product regression found and fixed across both campaigns."}
{"cluster_id": "chunk_014-021", "topic": "The all-tree T12 unit gate passed 922", "canonical_claim": "The all-tree T12 unit gate passed 922 tests across 160 suites with zero failures and two pre-existing disabled tests."}
{"cluster_id": "chunk_014-022", "topic": "The all-tree integration gate passed the RustFS", "canonical_claim": "The all-tree integration gate passed the RustFS relink test in 23.62 seconds and the CA transaction stateless test with zero failures."}
{"cluster_id": "chunk_014-023", "topic": "One long-running query on any reader holds", "canonical_claim": "One long-running query on any reader holds the global pool vacuum floor because the floor is the minimum across all reader pins."}
{"cluster_id": "chunk_014-024", "topic": "The planned retry-later warning uses LogSeriesLimiter with", "canonical_claim": "The planned retry-later warning uses `LogSeriesLimiter` with one allowed message per 30-second interval and logs at Warning level under the logger name `CasWriteRetryLater`."}
{"cluster_id": "chunk_014-025", "topic": "The codecs-v3 soak's dominant S3HeadObject activity and", "canonical_claim": "The codecs-v3 soak's dominant `S3HeadObject` activity and one fsck timeout were attributed to pre-existing B146/B154 fold and GC blob-existence probing, not to `.zst` parsing or codec CPU overhead."}
{"cluster_id": "chunk_014-026", "topic": "ref-table copy-cost hotspot", "canonical_claim": "A CA soak identified `__copy_construct_tree` over `std::map<string,Cas::RefCommittedRow>` as the top CPU stack for ref-table commit operations; it was treated as a pre-existing scalability smell and backlogged rather than a correctness defect. Additionally, The commit-heavy ref-table workload's dominant copy cost is deep-copying `std::map<String, RefCommittedRow>`, while a typical batch item touches about one ref."}
{"cluster_id": "chunk_014-027", "topic": "The S40 data-loss gate is lost ==", "canonical_claim": "The S40 data-loss gate is `lost == 0` for every server-acked insert, while cross-replica dedup-line counts are observation-only because legitimate client-timeout-then-commit retries also deduplicate."}
{"cluster_id": "chunk_014-028", "topic": "The deterministic build/dl_probe.py reproduction submitted and acknowledged", "canonical_claim": "The deterministic `build/dl_probe.py` reproduction submitted and acknowledged 1314 inserts, found 1116 present, and lost 198 acknowledged rows during a 105-second RustFS pause and replica kill."}
{"cluster_id": "chunk_014-029", "topic": "The permanent S40 gate requires a real", "canonical_claim": "The permanent S40 gate requires a real outage schedule, at least one outage-induced insert exception, a meaningful acknowledged volume, and zero acknowledged-but-absent rows; dedup log counts are non-gating observations."}
{"cluster_id": "chunk_014-030", "topic": "S23's tracked-memory growth decelerated after the first", "canonical_claim": "S23's tracked-memory growth decelerated after the first 90 seconds: `ch1` gained 56.6 MiB and then only 9.3 MiB during quiesce, while `ch2` gained 71.5 MiB and then only 4.7 MiB, which is a warmup curve rather than a constant-slope leak."}
{"cluster_id": "chunk_014-031", "topic": "The jemalloc study confirmed that S23 idle", "canonical_claim": "The jemalloc study confirmed that S23 idle growth was 100% generic `DB::SystemLog` part-flush machinery on the local `default` disk, with zero content-addressed or CAS symbols in either node's profile diff."}
{"cluster_id": "chunk_014-032", "topic": "The dominant S23 steady-state allocation spine was", "canonical_claim": "The dominant S23 steady-state allocation spine was `DB::SystemLog::savingThreadFunction` → `DB::SystemLog::flushImpl` → `DB::MergeTreeDataWriter::writeTempPart`, with `DB::ColumnsSubstreams::operator=` and `setColumns` as major allocation sites."}
{"cluster_id": "chunk_014-033", "topic": "The S23 profile driver was the soak", "canonical_claim": "The S23 profile driver was the soak harness's 10 ms query profiler: `system.trace_log` held 72,221 rows, including 63,258 `Real` rows, while system-log parts were stored on the `default` disk rather than the CA pool."}
{"cluster_id": "chunk_014-034", "topic": "After the S38 injection, ch2 completed 40", "canonical_claim": "After the S38 injection, ch2 completed 40 `Finish/Success` GC rounds while every round reported `CasGcClampSuppressedPasses: 1`, about 284 `DiskS3ReadRequestsErrors`, and `LogError: 1`."}
{"cluster_id": "chunk_014-035", "topic": "The #38 campaign produced a verdict for", "canonical_claim": "The #38 campaign produced a verdict for all 40 scenarios: 27 clean PASS results, 11 scale-limited INCONCLUSIVE results, one real product failure in S38, and the budget-only S08 disposition."}
{"cluster_id": "chunk_014-036", "topic": "After the filter and test fixes, the", "canonical_claim": "After the filter and test fixes, the complete CAS gtest battery ran with zero exclusions: 1030/1030 plain, 1034/1034 ASan, and 1034/1034 TSan, with four additional death tests present only under `DEBUG_OR_SANITIZER_BUILD`."}
{"cluster_id": "chunk_014-037", "topic": "The implemented CasJsonWriter migration reduced BM_EncodeRefLogTxn from", "canonical_claim": "The implemented CasJsonWriter migration reduced `BM_EncodeRefLogTxn` from 753 ns to 333 ns, a 2.26× speedup, while preserving byte-identical output."}
{"cluster_id": "chunk_014-038", "topic": "The pre-change BM_Admits benchmark measured 48.8 μs", "canonical_claim": "The pre-change `BM_Admits` benchmark measured 48.8 μs at N=100, 476 μs at N=1,000, 5,018 μs at N=10,000, and 55,976 μs at N=100,000, with an O(N log N) complexity fit."}
{"cluster_id": "chunk_014-039", "topic": "The 5-hour soak measured approximately 453 ms", "canonical_claim": "The 5-hour soak measured approximately 453 ms average caller wait per ref operation in `CasRefQueueWaitMicroseconds / CasRefBatchedMutations`, attributed to the O(N) `admits()` work."}
{"cluster_id": "chunk_014-040", "topic": "CAS recovery retry observability", "canonical_claim": "The retry implementation adds the `CasRefRecoveryRetries` ProfileEvent, distinct from `CasRefRecoveryRestarts`: retries count whole recovery attempts retried after transient object-store errors, while restarts cover a snapshot or log vanishing during reading. Additionally, Each CAS recovery retry should emit a `LOG_WARNING` containing attempt number, elapsed time, budget, and error, and increment the `CasRefRecoveryRetries` ProfileEvent."}
{"cluster_id": "chunk_014-041", "topic": "RefResolve audit events are emitted on every", "canonical_claim": "`RefResolve` audit events are emitted on every CA file access before the view-cache lookup, including warm cache hits, so their cost scales with read traffic rather than mutation rate."}
{"cluster_id": "chunk_014-042", "topic": "CasMountLeaseLost counts terminal mount-lease losses caused by", "canonical_claim": "`CasMountLeaseLost` counts terminal mount-lease losses caused by a vanished slot, a superseding incarnation, or a foreign server, but not a recoverable `fenced_by_gc` outcome."}
{"cluster_id": "chunk_014-043", "topic": "SYSTEM CONTENT ADDRESSED GC RUN is designed", "canonical_claim": "`SYSTEM CONTENT ADDRESSED GC RUN` is designed to expose `pending_candidates`, `pending_condemned`, and `pending_retired` pipeline counts from the state published by the round's single `gc/state` CAS."}
{"cluster_id": "chunk_014-044", "topic": "per-disk GC metric naming", "canonical_claim": "The four per-disk asynchronous metric names are `CasGcIsLeader_{disk}`, `CasGcPendingReclaim_{disk}`, `CasGcLastSuccessAgeSeconds_{disk}`, and `CasGcWedgedNamespaces_{disk}`. Additionally, The asynchronous CAS metric format strings are `CASGcIsLeader_{}`, `CASGcPendingReclaim_{}`, `CASGcLastSuccessAgeSeconds_{}`, and `CASGcWedgedNamespaces_{}`."}
{"cluster_id": "chunk_014-045", "topic": "The manifest-decode and dedup presence caches should", "canonical_claim": "The manifest-decode and dedup presence caches should register `CasManifestDecodeCacheBytes`, `CasManifestDecodeCacheEntries`, `CasDedupCacheBytes`, and `CasDedupCacheEntries` instead of passing `CurrentMetrics::end()`."}
{"cluster_id": "chunk_014-046", "topic": "The final RefTableState success criterion is that", "canonical_claim": "The final `RefTableState` success criterion is that blob hashing is the hottest CAS-attributed stack family and every other CAS-attributed family is at most one third of its sample count, with correctness invariants at zero hits."}
{"cluster_id": "chunk_014-047", "topic": "The benchmark plan covers BM_Admits, BM_ApplyRefLogTxn, BM_ReplayHistory", "canonical_claim": "The benchmark plan covers `BM_Admits`, `BM_ApplyRefLogTxn`, `BM_ReplayHistory`, `BM_ScratchCopy`, `BM_ManifestAlreadyOwned`, `BM_Materialize`, `BM_SnapshotEncode`, and `BM_MergedIteration` over table sizes from 100 to 100000 with complexity measurements."}
{"cluster_id": "chunk_014-048", "topic": "The E5 soak recorded CasRefMaterializeInPlace=81,467 versus CasRefMaterializeCopy=14", "canonical_claim": "The E5 soak recorded `CasRefMaterializeInPlace=81,467` versus `CasRefMaterializeCopy=14`, indicating that the in-place fast path handled 99.98% of production materializations."}
{"cluster_id": "chunk_014-049", "topic": "The definitive consistency soak found the main", "canonical_claim": "The definitive consistency soak found the main bottleneck in `CasRefLedger::appendRefOps` condition waits, with about 97,000 samples, rather than CPU; CAS CPU share was modest."}
{"cluster_id": "chunk_014-050", "topic": "500-partition INSERT baseline", "canonical_claim": "A 10M-row, 500-partition CAS-on-S3 INSERT measured 170.6 seconds versus 22.4 seconds for standard S3, a 7.6-times slowdown. Additionally, The measured INSERT produced `CasRefBatchFlushes=CasRefBatchedMutations=1026`, so the ref-ledger batch size was exactly 1.0, and `CasRefQueueWaitMicroseconds` was 36.2 seconds. Additionally, The CAS-on-S3 500-partition INSERT measured 170.6 seconds versus 22.4 seconds for standard S3, with `S3WriteMicroseconds` at 117.5 seconds, `CasRefQueueWaitMicroseconds` at 36.2 seconds, and `CasRefBatchFlushes=CasRefBatchedMutations=1026`. Additionally, A measured 500-partition wide INSERT was 170.6 seconds on CA versus 22.4 seconds on standard S3, despite fewer PUTs; the CA cost was dominated by serial per-part ref commits, serial per-blob uploads, manifest GET validation, and speculative `deduplication_head_first_min_bytes = 1 MiB` HEADs."}
{"cluster_id": "chunk_014-051", "topic": "The parallel-write validation success criterion is CasRefBatchFlushes", "canonical_claim": "The parallel-write validation success criterion is `CasRefBatchFlushes` much less than `CasRefBatchedMutations`, a sharp reduction in `CasRefQueueWaitMicroseconds`, and wall time materially closer to 22.4 seconds."}
{"cluster_id": "chunk_014-052", "topic": "Stage 1 intentionally leaves CasRefBatchFlushes and CasRefBatchedMutations", "canonical_claim": "Stage 1 intentionally leaves `CasRefBatchFlushes` and `CasRefBatchedMutations` at an average batch size near 1.0; reducing that serial cross-part metric belongs to stage 2."}
{"cluster_id": "chunk_014-053", "topic": "wide-insert stage-1 improvement", "canonical_claim": "Stage 1 reduced the measured CA INSERT from 58.41 seconds to 30.26 seconds, from 3.0× to 1.59× versus plain S3, while CPU/wall increased from 0.375 to 1.075 and the top-thread share fell from 72.3% to 14.5%. Additionally, Write-path stage 1 reduced CA wide-insert wall time from 58.41 seconds to 30.26 seconds and CA-vs-plain from 3.0x to 1.59x, while reducing top-thread Real share from 72.3% to 14.5%. Additionally, The wide-insert baseline improved from 58.41 seconds to 30.26 seconds and from 3.0x to 1.59x versus plain S3 after `CasBlobUploadPool` and `fanOutBlobUploads`; top-thread share fell from 72.3% to 14.5%."}
{"cluster_id": "chunk_014-054", "topic": "The S41 workload is a deterministic single-node", "canonical_claim": "The S41 workload is a deterministic single-node `MergeTree` INSERT of 10,000,000 rows, 30 mixed-type columns, 500 partitions, and one part per partition, with merges stopped during measurement."}
{"cluster_id": "chunk_014-055", "topic": "For the S41 baseline, plain S3 took", "canonical_claim": "For the S41 baseline, plain S3 took 19.44 seconds and CAS-on-S3 took 58.41 seconds for 500 parts and 3.53 GiB of logical data; CAS issued 119,432 PUTs, 134,400 HEADs, and about 54,360 GETs."}
{"cluster_id": "chunk_014-056", "topic": "In the CAS S41 trace, S3 PUT/upload", "canonical_claim": "In the CAS S41 trace, S3 PUT/upload response waits consumed 87.2% of Real wall time, HEAD-before-PUT consumed 12.6%, and serialization, hashing, ref-ledger/manifest encoding, and part writing together consumed under 0.2%."}
{"cluster_id": "chunk_014-057", "topic": "The S41 CAS upload waits were concentrated", "canonical_claim": "The S41 CAS upload waits were concentrated in one dominant thread holding 72.3% of Real samples; only three threads appeared, CPU-busy time was 37.5% of wall, and `CasRefBatchFlushes` equaled `CasRefBatchedMutations` at 1000 over 500 parts, an average batch size of exactly 1.0."}
{"cluster_id": "chunk_014-058", "topic": "On the fresh-pool S41 insert, CasBlobHeadFirst and", "canonical_claim": "On the fresh-pool S41 insert, `CasBlobHeadFirst` and `CasBlobBodyPutAvoided` were both 26,680, showing that deduplication avoided 26,680 blob-body uploads while still paying the HEADs."}
{"cluster_id": "chunk_014-059", "topic": "The S41 HEAD-before-PUT gate issued 134,400 HEADs", "canonical_claim": "The S41 HEAD-before-PUT gate issued 134,400 HEADs, or 268.8 per part, and accounted for 12.6% of CAS Real wall time; plain S3 issued no HEADs."}
{"cluster_id": "chunk_014-060", "topic": "The conservative fence-not-rescue behavior costs one self-remount", "canonical_claim": "The conservative fence-not-rescue behavior costs one self-remount cycle for an ambiguous landed renewal, including about 36.5 seconds of fenced writes with default settings; it requires a timed-out-yet-applied conditional PUT and is expected to be rare on healthy backends."}
{"cluster_id": "chunk_014-061", "topic": "The S41 quiescence checks passed 15/15 verdicts", "canonical_claim": "The S41 quiescence checks passed 15/15 verdicts, forced GC reduced residual `unreachable` to zero in one round, detailed `ca-fsck` reported `dangling = 0`, and peak `MemoryResident` was 1.99 GB."}
{"cluster_id": "chunk_014-062", "topic": "meta_pool_wait must report explicit jobs_scheduled and jobs_completed", "canonical_claim": "`meta_pool_wait` must report explicit `jobs_scheduled` and `jobs_completed` metrics because work scheduled on the meta pool runs on other threads and is absent from the fold thread's `ProfileEvents` delta."}
{"cluster_id": "chunk_014-063", "topic": "Across historical firings, 338,559 objects were missing", "canonical_claim": "Across historical firings, 338,559 objects were missing from the fold scan and 58 were missing from the pre-fold scan; only the latter direction is not explained by concurrent deletion because the fold scan demonstrably saw the objects."}
{"cluster_id": "chunk_014-064", "topic": "A gc_anomaly audit record carries the probe", "canonical_claim": "A `gc_anomaly` audit record carries the probe, ref transaction id, missing direction, `HEAD` verdict, both enumeration maxima, hole ordinal, row cap, namespace, and `outcome = ref_folding_aborted`; a bare anomaly count is insufficient forensic context."}
{"cluster_id": "chunk_014-065", "topic": "manifest re-read redundancy", "canonical_claim": "Measured GC performance found `fold_ref_intake` linear in the ref-log backlog at 256 logs/s, with round trips equal to `logs + 2 × edges`; 39.6% of intake manifest fetches were cross-transaction re-reads, making a round-scoped body cache a potential optimization. Additionally, A round-scoped cache could remove 39.6% of manifest-body edge re-reads, measured at approximately 597,000 redundant edges and about 600 seconds on the 404,000-log round. Additionally, Across 959 decoded ref-log transactions, the worklog measured 7,565 manifest edges over 4,573 distinct manifests; 2,991 manifests carried both an add and a remove edge, so 39.6% of intake manifest fetches were cross-transaction re-reads. Additionally, The measured 959-transaction residual pool contained 7,565 edges over 4,573 distinct manifests, so 39.6% of intake manifest fetches were re-reads; intra-transaction redundancy was exactly zero. Additionally, In one fold window, 39.6% of manifest fetches were re-reads: 7565 edges came from 4573 distinct manifests, with zero intra-transaction redundancy and cross-transaction add/remove overlap. Additionally, The measured manifest redundancy was 39.6%: 7,565 edges referenced 4,573 distinct manifests, and 2,991 manifests carried both an add and a remove edge in one window."}
{"cluster_id": "chunk_014-066", "topic": "GC round and pending-delete cost", "canonical_claim": "One observed `pending_deletes` phase lasted 77.2 seconds, compared with 243 milliseconds for the next-worst phase, making it a significant uninvestigated performance cost. Additionally, A measured GC round reached 1830 seconds in `fold_ref_intake` while folding 404,065 logs, and one `pending_deletes` occurrence reached 77.2 seconds. Additionally, A 404,065-log GC round took 1,829.6 seconds for 3,420,091 serial requests, with the large-round cost near 0.5 ms per request. Additionally, A `pending_deletes` occurrence took 77.2 seconds while the next-worst phase took 243 milliseconds, consistent with roughly 150,000 serial `deleteExact` calls at approximately 0.5 ms each. Additionally, One observed `pending_deletes` phase took 77.2 seconds, compared with 243 milliseconds for the next-worst phase in the same 20-minute run."}
{"cluster_id": "chunk_014-067", "topic": "mount-fence observability metrics", "canonical_claim": "Mode 1 observability uses `CasMountFenceArmed`, `CasSelfRemountCompleted`, and `CasSelfRemountMicroseconds`; Mode 2 uses `CasMountFenceExpired` and `CasMountFenceExpiredMicroseconds`. Additionally, The CAS mount-fence write-grace feature introduces the settings/metrics `write_grace_ms`, `waitForWriteGrace`, `CasMountFenceArmed`, `CasSelfRemountCompleted`, `CasSelfRemountMicroseconds`, `CasMountFenceExpired`, `CasMountFenceExpiredMicroseconds`, `fence_tripped_boot_ms`, and `fence_expiry_reported_boot_ms`."}
{"cluster_id": "chunk_014-068", "topic": "Mode 2 outage duration is measured from", "canonical_claim": "Mode 2 outage duration is measured from the true expiry instant stored in `fence_expiry_reported_boot_ms` until `setMountDeadline` re-arms the fence, rather than from the first refused write."}
{"cluster_id": "chunk_014-069", "topic": "The bounded fold walk adds tails_advanced, tails_unchanged", "canonical_claim": "The bounded fold walk adds `tails_advanced`, `tails_unchanged`, and `tails_below_cursor` phase metrics; an all-unchanged, hold-free round can skip per-namespace walks entirely."}
{"cluster_id": "chunk_014-070", "topic": "The bounded-walk follow-up measured approximately 444 logs", "canonical_claim": "The bounded-walk follow-up measured approximately 444 logs per second as the concurrent writer/walker throughput boundary, motivating a stop-at-round-start-tail design independent of concurrent writes."}
{"cluster_id": "chunk_014-071", "topic": "T15 GC validation results", "canonical_claim": "On the T15 re-validation binary, GC completed 64 `Success` folding rounds with an average duration of 100.7 seconds, a minimum of 2.8 seconds, and a maximum of 433.5 seconds; the preceding T14 run completed zero rounds in 42 minutes. Additionally, The T15 validation completed 64 successful and four deferred rounds in 118.7 minutes; round duration was bounded between 2.8 and 433.5 seconds, and `CasGcProbeADue` equaled `CasGcProbeAPerformed` at four."}
{"cluster_id": "chunk_014-072", "topic": "The T15 re-validation measured CasGcProbeADue = 4", "canonical_claim": "The T15 re-validation measured `CasGcProbeADue = 4`, `CasGcProbeAPerformed = 4`, `CasGcProbeASkipped = 0`, and `CasGcRefScanDisagreements = 0`; the document concludes that the sampling cadence was adequate once folding rounds were bounded."}
{"cluster_id": "chunk_014-073", "topic": "The v9 TLA phase passed 115 configurations", "canonical_claim": "The v9 TLA phase passed 115 configurations in 11 asserted batteries: all 115 received verdicts, with 26 green configurations and 89 expected reds; no configuration was skipped."}
{"cluster_id": "chunk_014-074", "topic": "The transient-classifier audit found 217 Formats/ throw", "canonical_claim": "The transient-classifier audit found 217 `Formats/` throw sites and zero transient sites there; across the CA tree it found approximately 58 transient sites, of which 50 already use upstream-retryable classes, five use `INVALID_STATE`, and three use `ABORTED` in GC."}
{"cluster_id": "chunk_014-075", "topic": "CasPrecommitReleaseUnproven counts precommits whose release is not", "canonical_claim": "`CasPrecommitReleaseUnproven` counts precommits whose release is not proven, not confirmed leaks; it is an upper bound on residue because an `Uncertain` removal may later settle and an unresolved promote may have committed without leaving a precommit."}
{"cluster_id": "chunk_014-076", "topic": "relink refusal storm", "canonical_claim": "The old `CasRefLedger::confirmExactRef` path proved relink availability only about 17% of the time under sustained writes, with approximately 92,000 refusals versus 19,531 proven operations; one run recorded 248,400 refusals in about 32 minutes and a peak of 9,219 per minute. Additionally, The T14 run produced 248,400 relink-refusal exceptions cluster-wide in about 32 minutes, with a peak of 9,219 per minute on one node; the refusal storm was essentially the whole `NETWORK_ERROR` population and was unrelated to GC correctness. Additionally, The T14 draft measured about 92,000 refusals versus 19,531 proven confirms, approximately 17% availability, and an independent GC audit measured 248,400 refusals cluster-wide with a peak of 9,219 per minute."}
{"cluster_id": "chunk_014-077", "topic": "The relink observability contract defines CasRelinkProven, CasRelinkChanged", "canonical_claim": "The relink observability contract defines `CasRelinkProven`, `CasRelinkChanged`, `CasRelinkUnknown`, `CasRelinkRecoveryBudgetRefused`, and `CasRelinkAnswerRejected`; `CasRelinkAnswerRejected` should be approximately zero in a healthy cluster."}
{"cluster_id": "chunk_014-078", "topic": "The query profiler samples every 10 ms", "canonical_claim": "The query profiler samples every 10 ms while the global background profiler samples every 10 s, so raw frame counts across the two sides are not comparable; the weighted 15-minute sample was about 332 s of query-side CPU versus 11,790 s of background CPU."}
{"cluster_id": "chunk_014-079", "topic": "The T14 GC leader on ch1 completed", "canonical_claim": "The T14 GC leader on ch1 completed zero rounds in 41 minutes 32 seconds and was still inside the same round when it was killed; ch2 recorded 248 lease rounds with `acquired=0` and `steal_allowed=248`."}
{"cluster_id": "chunk_014-080", "topic": "The unfinished T14 round reached 1,087,385+ CasRefManifestBodyFoldGets", "canonical_claim": "The unfinished T14 round reached 1,087,385+ `CasRefManifestBodyFoldGets`; with the 1:1 HEAD/GET pairing, it issued approximately 2.17 million manifest-side S3 requests."}
{"cluster_id": "chunk_014-081", "topic": "The T14 controller window measured approximately 949", "canonical_claim": "The T14 controller window measured approximately 949 `CasRefManifestBodyFoldGets` per second and 444 `CasRefLogBodyGets` per second, a ratio of 2.136 manifest-body GETs per ref-log body GET."}
{"cluster_id": "chunk_014-082", "topic": "A 1.53 ms S3 read round trip", "canonical_claim": "A 1.53 ms S3 read round trip implies a serial HEAD+GET ceiling of about 327 manifest edges per second, and the earlier measured approximately 313 edges per second reached 96% of that ceiling."}
{"cluster_id": "chunk_014-083", "topic": "An idle follower paid four S3 requests", "canonical_claim": "An idle follower paid four S3 requests and 6.77 ms per lease round at the 10-second cadence; 90.3% of the phase was `S3ReadMicroseconds`, and the request moved 108 bytes per GET."}
{"cluster_id": "chunk_014-084", "topic": "The T14 pool reached maximum block number", "canonical_claim": "The T14 pool reached maximum block number 74,672, peaked at 32,762 inactive parts on ch1, and ended at 31,143,704,824 bytes, approximately 29.0 GiB."}
{"cluster_id": "chunk_014-085", "topic": "system.content_addressed_log contributed 299 Loading Outdated part lines", "canonical_claim": "`system.content_addressed_log` contributed 299 `Loading Outdated part` lines during restart and 11,421 normal-operation logger lines, making the soak instrumentation itself the largest identified contributor to restart time."}
{"cluster_id": "chunk_014-086", "topic": "T15 fold throughput", "canonical_claim": "After T15 validation, `CasRefManifestBodyFoldGets` equaled `CasRefEmittedEdges` at 661,670, proving 1.000 GET per manifest edge; fold throughput was 751 edges/s versus approximately 313 edges/s before the HEAD removal. Additionally, T15 validation measured 661,670 fold edges in 880.78 seconds, or 751 edges/s, 2.40x the degraded run's approximately 313 edges/s; the comparison is observational because S3 latency and workloads differed."}
{"cluster_id": "chunk_014-087", "topic": "In T15 validation, the GC leader spent", "canonical_claim": "In T15 validation, the GC leader spent 90.5% of wall time in GC, with `defer_decision` consuming 79.11% of all GC time; the frozen-tail change bounded rounds but did not reduce the per-round cost."}
{"cluster_id": "chunk_014-088", "topic": "The follower lease check regressed from 6.77", "canonical_claim": "The follower lease check regressed from 6.77 ms to 53.5 ms per round in T15 validation, a 7.9x increase, while remaining 38 seconds over two hours in absolute cost."}
{"cluster_id": "chunk_014-089", "topic": "T15 validation measured 139.6 manifest edges per", "canonical_claim": "T15 validation measured 139.6 manifest edges per merge, 7.21 ref repoints per merge, 3.750 edges per ref-log entry, and a 0.18% GC share of all S3 GETs; `CasGc*` counters alone undercount fold reads because fold reads use `CasRefManifestBodyFoldGets`."}
{"cluster_id": "chunk_014-090", "topic": "T15 round accounting was 99.986% complete: 6,443.4", "canonical_claim": "T15 round accounting was 99.986% complete: 6,443.4 seconds of round wall time versus 6,442.5 seconds of phase time, leaving 0.9 seconds unaccounted."}
{"cluster_id": "chunk_014-091", "topic": "The destructive baseline must measure pending deletes", "canonical_claim": "The destructive baseline must measure pending deletes, owner-removed manifest deletion, orphan-manifest sweep, ref-object cleanup, namespace cleanup, generation pruning, and time and round count to fixpoint."}
{"cluster_id": "chunk_014-092", "topic": "CA ref-catalog timeout attribution", "canonical_claim": "The recorded S3 lane had 137 of 250 timeout lines naming `cas/ref_catalog` across 66 namespaces. Additionally, The pool-wide `content_addressed_s3/cas/ref_catalog` key accounted for 137 of 250 S3 timeout lines and 125 code-499 transient errors across 66 namespaces on the CA-s3 stateless lane."}
{"cluster_id": "chunk_014-093", "topic": "The destructive-baseline soak must use the sequential", "canonical_claim": "The destructive-baseline soak must use the sequential implementation only—no `MultiDelete`, parallel deletes, or delete-side concurrency—so its measured cost is an honest baseline."}
{"cluster_id": "chunk_014-094", "topic": "The omitted ref-log objects were durable for", "canonical_claim": "The omitted ref-log objects were durable for about 19 seconds before the anomaly, and an exact-key `HEAD` at the disagreement time confirmed that both were present."}
{"cluster_id": "chunk_014-095", "topic": "The default soak scale is dev, while", "canonical_claim": "The default soak scale is `dev`, while `--scale ci` is medium and `--scale full` targets specification-scale workloads such as S01 100 GiB, S03 1–10M objects, and S05 10000 tables."}
{"cluster_id": "chunk_014-096", "topic": "A forced GC fixpoint drive issues rounds", "canonical_claim": "A forced GC fixpoint drive issues rounds on both replicas until fsck unreachable count settles; for a non-abandoning scenario the expected final `unreachable` count is zero."}
{"cluster_id": "chunk_014-097", "topic": "GC ref-intake throughput", "canonical_claim": "Across 1,673 GC rounds, `fold_ref_intake` was linear in the ref-log backlog and sustained 256 logs per second. Additionally, The flat cost of ref intake was approximately 0.92 ms per request rather than per log, and requests per log ranged from 2.5 to 4.7 and increased with backlog size. Additionally, The GC bottleneck study measured an approximately 256-log-per-second service rate; its reproduction must sweep arrival rate across that service rate without chaos faults."}
{"cluster_id": "chunk_014-098", "topic": "orphan_sweep had the worst median duration at", "canonical_claim": "`orphan_sweep` had the worst median duration at 383 ms per round, while `lease` was approximately 2 ms at p50 across 2,255 rounds."}
{"cluster_id": "chunk_014-099", "topic": "Measured GC round cost grew with pool", "canonical_claim": "Measured GC round cost grew with pool size from 87 ms at 400 parts to 93 s at 10,000 tables and 398 s at 100,000 parts."}
{"cluster_id": "chunk_014-100", "topic": "Part removal generated 17,707 ref_repoint events, all", "canonical_claim": "Part removal generated 17,707 `ref_repoint` events, all on `delete_tmp_*` refs, costing approximately 53,000 PUTs or 22% of the writer PUT class in the measured soak."}
{"cluster_id": "chunk_014-101", "topic": "Empty-shard GC probes caused approximately 1,174 DiskS3ReadRequestsErrors", "canonical_claim": "Empty-shard GC probes caused approximately 1,174 `DiskS3ReadRequestsErrors` per round, growing by about four per round as new shards were touched."}
{"cluster_id": "chunk_014-102", "topic": "Insert queries accumulated 339.6 seconds of CasRefQueueWaitMicroseconds", "canonical_claim": "Insert queries accumulated 339.6 seconds of `CasRefQueueWaitMicroseconds` over 7,131 inserts, or approximately 48 ms of ref-lane queue wait per insert on average."}
{"cluster_id": "chunk_014-103", "topic": "The CA-s3 lane observed ConnectionGroup: Too many", "canonical_claim": "The CA-s3 lane observed `ConnectionGroup: Too many active sessions in group Disk` with 10,400 active sessions against an 8,000 warning limit; the test output itself remained correct."}
{"cluster_id": "chunk_014-104", "topic": "A shared-pool replica could re-merge and re-spill", "canonical_claim": "A shared-pool replica could re-merge and re-spill a deduplicated 100 GiB blob, consuming approximately 186 GiB of scratch across the two replicas."}
{"cluster_id": "chunk_014-105", "topic": "A 20,000-column OPTIMIZE FINAL generated O(columns) S3", "canonical_claim": "A 20,000-column `OPTIMIZE FINAL` generated O(columns) S3 operations and stalled in an S3 retry storm."}
{"cluster_id": "chunk_014-106", "topic": "A 256-partition insert took approximately 10 seconds", "canonical_claim": "A 256-partition insert took approximately 10 seconds because it performed O(partitions) CAS commits."}
{"cluster_id": "chunk_014-107", "topic": "Starting a server with 10,000 tables required", "canonical_claim": "Starting a server with 10,000 tables required approximately 152,000 S3 operations for LISTs and GETs, while recovery remained fast."}
{"cluster_id": "chunk_014-108", "topic": "A one-hour soak measured pool_bytes at approximately", "canonical_claim": "A one-hour soak measured `pool_bytes` at approximately 400 times `logical_bytes`, attributed to RustFS overwrite-version retention and CA debris rather than a dangling-data safety issue."}
{"cluster_id": "chunk_014-109", "topic": "The CA write path's allocation trace was", "canonical_claim": "The CA write path's allocation trace was dominated by `ContentAddressedTransaction::tryCreateWriteBuffer` and `writeFile`, while CPU traces were clean; the finding was allocation volume, not CPU or correctness."}
{"cluster_id": "chunk_014-110", "topic": "In the measured CA-s3 CI run, 63%", "canonical_claim": "In the measured CA-s3 CI run, 63% of ref-page volume was already-dropped namespaces: 97 of 128 namespaces had been dropped before the query began, and 9,538 of 15,123 namespaces were explicitly dropped."}
{"cluster_id": "chunk_014-111", "topic": "The retained-blob incident measured 56 blobs and", "canonical_claim": "The retained-blob incident measured 56 blobs and 104,755 bytes over one four-hour soak; the leak rate was unbounded over time under the observed mechanism."}
{"cluster_id": "chunk_014-112", "topic": "On the largest measured round, edge HEADs", "canonical_claim": "On the largest measured round, edge HEADs accounted for approximately 1.5 million of 3.42 million requests, or 44% and about 800 seconds of serial time."}
{"cluster_id": "chunk_014-113", "topic": "The S42 CI-scale run injected 2,184 allocation", "canonical_claim": "The S42 CI-scale run injected 2,184 allocation faults, observed zero `CasRefApplyPoisoned`, two wedged and two unwedged ref lanes, zero `CasRefAppendDefiniteFailure`, and 11,960 acknowledged blocks."}
{"cluster_id": "chunk_014-114", "topic": "GC serial request latency", "canonical_claim": "The GC duration study found round time dominated by serial object-store request latency: a 30-minute round represented about 3.42 million serial round trips at approximately 0.5 ms each, with repeated manifest work secondary. Additionally, A 30-minute GC round was measured as 3.42 million serial round trips at approximately 0.5 ms each, with phase time attributed to serial request latency rather than CPU or lock time. Additionally, The GC round-duration study measured 0.465–0.535 ms per total S3 request, making a 30-minute round equivalent to 3.42 million serial round trips; 39.6% were redundant manifest refetches."}
{"cluster_id": "chunk_014-115", "topic": "The scenario suite treats CASBlobPut, CASBlobPutDeduplicated, CASBlobHead", "canonical_claim": "The scenario suite treats `CASBlobPut`, `CASBlobPutDeduplicated`, `CASBlobHead`, `CASBlobHeadMiss`, `CASBlobHeadFirst`, `CASBlobBodyPutAvoided`, `CASBlobDeduplicationCacheHit`, `CASBlobDelete`, `CASBlobList`, `CASRootGet`, `CASRootHead`, `CASRootCompareSwap`, `CASRootCompareSwapConflict`, `CASRootList`, `CASGCGet`, `CASGCHead`, `CASGCCompareSwap`, `CASGCDelete`, and `CASGCList` as core CAS operation counters."}
{"cluster_id": "chunk_014-116", "topic": "A 10,000-row by 10 KB FixedString INSERT", "canonical_claim": "A 10,000-row by 10 KB `FixedString` INSERT measured peak CA memory at 144.5 MiB versus 135.5 MiB locally; the approximately 9 MiB delta came from `ContentAddressedTransaction::writeFile` staging and hash buffering, and test `03829` was raised from 150M to 170M."}
{"cluster_id": "chunk_014-117", "topic": "Ack-floor observability includes CasGcRetired{Condemned,Spared,Graduated,Redeleted}, CasGcHeartbeatFenceOuts, CasGcFloorHeldByStaleAck, a", "canonical_claim": "Ack-floor observability includes `CasGcRetired{Condemned,Spared,Graduated,Redeleted}`, `CasGcHeartbeatFenceOuts`, `CasGcFloorHeldByStaleAck`, a `gc_fence_out` audit event, and `RoundReport` fields for condemned, graduated, redeleted, fence-outs, and `min_ack`."}
{"cluster_id": "chunk_014-118", "topic": "On the CA-S3 lane, a warm cacheless", "canonical_claim": "On the CA-S3 lane, a warm cacheless read measured approximately 141 ms versus 47 ms locally and 186 S3 GETs; an opt-in file-cache disk made warm reads approximately local with zero S3 GETs but made the first cold read about twice as slow."}
{"cluster_id": "chunk_014-119", "topic": "The live AWS GC validation measured 73.8", "canonical_claim": "The live AWS GC validation measured 73.8 MB before merge churn, 37.3 MB after merge churn, and 13.9 KB after `DROP TABLE`, with `dedup_ratio=2`, zero anomalies or false 404s across more than 28 rounds, and 30–40 second round duration."}
{"cluster_id": "chunk_014-120", "topic": "The live GCS validation reclaimed 11 condemned", "canonical_claim": "The live GCS validation reclaimed 11 condemned objects through graduation and deletion, recorded a 51-object 20.8 KB metadata pool after DROP-to-zero, and achieved `dedup_ratio=2` with clean fsck results."}
{"cluster_id": "chunk_014-121", "topic": "The append-ordering measurement contained 65,157 uploads in", "canonical_claim": "The append-ordering measurement contained 65,157 uploads in epoch 1 and 106 uploads in epoch 2, with `out_of_order=0` in both epochs."}
{"cluster_id": "chunk_014-122", "topic": "The blob-storage audit contains Upload rows for", "canonical_claim": "The blob-storage audit contains Upload rows for ref-log keys ending `000000000001430c.zst`, `000000000001430d.zst`, and `000000000001430e.zst` at 16:47:19.211480, 16:47:19.212340, and 16:47:19.213680, followed by Delete rows for all three at 16:53:59.552282, 16:53:59.552902, and 16:53:59.553479."}
{"cluster_id": "chunk_014-123", "topic": "The cache observability surface includes CasPartFolderCacheHits, CasPartFolderCacheMisses", "canonical_claim": "The cache observability surface includes `CasPartFolderCacheHits`, `CasPartFolderCacheMisses`, `CasPartFolderCacheEvictions`, `CasPartFolderCacheOversized`, `CasPartFolderCacheBytes`, `CasPartFolderCacheEntries`, `CasPartFolderManifestGets`, and `CasPartFolderWriteThroughInvalidations`."}
{"cluster_id": "chunk_014-124", "topic": "The controlled acceptance metric is CasPartFolderManifestGets <=", "canonical_claim": "The controlled acceptance metric is `CasPartFolderManifestGets <= number of materialized active part-folder load windows` for eligible folders."}
{"cluster_id": "chunk_014-125", "topic": "The system.cas_gc_log counters candidates_marked, objects_deleted, objects_absent, objects_replaced", "canonical_claim": "The `system.cas_gc_log` counters `candidates_marked`, `objects_deleted`, `objects_absent`, `objects_replaced`, and `objects_spared` measure retirement, physical deletion, prior absence, `412` resurrection wins, and recheck saves respectively."}
{"cluster_id": "chunk_014-126", "topic": "The system.cas_gc_log ProfileEvents map is a per-round", "canonical_claim": "The `system.cas_gc_log` `ProfileEvents` map is a per-round delta on `Start` and `Finish` rows and a per-phase delta on `Phase` rows, so grouping `ProfileEvents['S3ListObjects']` by phase attributes list activity to the spending phase."}
{"cluster_id": "chunk_014-127", "topic": "Required cache diagnostics distinguish hits by mode", "canonical_claim": "Required cache diagnostics distinguish hits by mode, misses by cold or invalidation reason, invalidations by operation, manifest body `GET` count, and view inserts skipped by local generation mismatch."}
{"cluster_id": "chunk_014-128", "topic": "CAS ParamSet counts", "canonical_claim": "The CAS ParamSet amd_binary, cas s3 storage, parallel has count 1. Additionally, The CAS ParamSet amd_asan_ubsan, cas s3 storage, parallel, {b}/2 has count 2. Additionally, The CAS ParamSet amd_tsan, cas s3 storage, parallel, {b}/2 has count 2. Additionally, The CAS ParamSet amd_msan, cas s3 storage, parallel, {b}/3 has count 3. Additionally, The CAS ParamSet arm_binary, cas s3 storage, parallel has count 1. Additionally, The CAS ParamSet amd_binary, cas storage, parallel has count 1."}
{"cluster_id": "chunk_014-129", "topic": "The v3 hex NDJSON encoding is estimated", "canonical_claim": "The v3 hex NDJSON encoding is estimated at about twice the bytes of the retired binary encoding, roughly 1 GB versus 500 MB for 10M records."}
{"cluster_id": "chunk_014-130", "topic": "Creation costs at least three conditional writes", "canonical_claim": "Creation costs at least three conditional writes: catalog Creating, _ckpt creation, and catalog Live; _ckpt merge conflicts can require multiple GET/CAS attempts."}
{"cluster_id": "chunk_014-131", "topic": "ProfileEvents::PartsLockHoldMicroseconds and ProfileEvents::PartsLockWaitMicroseconds already instrument parts-lock hold", "canonical_claim": "ProfileEvents::PartsLockHoldMicroseconds and ProfileEvents::PartsLockWaitMicroseconds already instrument parts-lock hold and wait time, and DataPartsLock carries wait_watch and lock_watch."}
{"cluster_id": "chunk_014-132", "topic": "The soak phase-3 ticker should sample per-tick", "canonical_claim": "The soak phase-3 ticker should sample per-tick deltas of PartsLockHoldMicroseconds, PartsLockWaitMicroseconds, and ContextLockWaitMicroseconds, then gate runs on a calibrated per-tick hold budget."}
{"cluster_id": "chunk_014-133", "topic": "A latency canary should issue a cheap", "canonical_claim": "A latency canary should issue a cheap point SELECT and a small system.parts scan about every 100 ms, recording latency and failing when canary p99 outside chaos windows exceeds the threshold."}
{"cluster_id": "chunk_014-134", "topic": "The monitoring guide should alert on the", "canonical_claim": "The monitoring guide should alert on the server-wide per-table rate of PartsLockHoldMicroseconds and PartsLockWaitMicroseconds after enabling metric_log in the nightly image."}
{"cluster_id": "chunk_014-135", "topic": "The fsck detail report found reachable=17, dangling=0", "canonical_claim": "The fsck detail report found `reachable=17`, `dangling=0`, `unreachable=12`, `pending_gc=0`, `awaiting_gc=0`, `unaccounted=0`, `stale_edge=12`, and `corrupted_runs=0`."}
{"cluster_id": "chunk_014-136", "topic": "The same fsck report measured snapshot_oracle_mismatches=0, snapshot_oracle_checked=0", "canonical_claim": "The same fsck report measured `snapshot_oracle_mismatches=0`, `snapshot_oracle_checked=0`, `physical_bytes=21470665670`, `referenced_logical_bytes=75129138898`, `distinct_blobs=17`, `total_blob_refs=54`, and `dedup_ratio=3.17647`."}
{"cluster_id": "chunk_014-137", "topic": "Version 26.3 adds the DiskObjectStorageWaitBlobRemovalMicroseconds event for", "canonical_claim": "Version 26.3 adds the `DiskObjectStorageWaitBlobRemovalMicroseconds` event for measuring time waiting for object-storage blob removal."}
{"cluster_id": "chunk_014-138", "topic": "The final release CA hygiene gate ran", "canonical_claim": "The final release CA hygiene gate ran 1977 of 1977 tests across 277 suites with 2 disabled and exited 0; the final ASan gate ran 1982 of 1982 tests across 295 suites with 0 sanitizer reports."}
{"cluster_id": "chunk_014-139", "topic": "The S44 smoke run applied 54 mutations", "canonical_claim": "The S44 smoke run applied 54 mutations across six drop/recreate cycles with no mutation errors beyond the expected drop-window `UNKNOWN_TABLE` exceptions."}
{"cluster_id": "chunk_014-140", "topic": "The S44 smoke run's recreate latency stayed", "canonical_claim": "The S44 smoke run's recreate latency stayed stable at 2.349 seconds in the first half and 2.347 seconds in the second half."}
{"cluster_id": "chunk_014-141", "topic": "The S44 smoke run ended with CasRefApplyPoisoned=0", "canonical_claim": "The S44 smoke run ended with `CasRefApplyPoisoned=0`, `CasRefRecoveryStreamHole=0`, and `fsck --detail` reporting `dangling=0`."}
{"cluster_id": "chunk_014-142", "topic": "With the default cleanup budget, one T6b", "canonical_claim": "With the default cleanup budget, one T6b run accumulated 112518 `skipped_budget` nominations and ended with 110218 unreachable objects after 73 rounds."}
{"cluster_id": "chunk_014-143", "topic": "With gc_round_manifest_cleanup_budget=0, a T6b run had zero", "canonical_claim": "With `gc_round_manifest_cleanup_budget=0`, a T6b run had zero `skipped_budget` entries and both recovery and final convergence reached `unreachable=0`, `dangling=0`, and `stale_edge=0`."}
{"cluster_id": "chunk_014-144", "topic": "During the recorded interval, no ch1 or", "canonical_claim": "During the recorded interval, no `ch1` or `ch2` ref deletes occurred, while all-time `ch2` ref-object deletes also remained zero."}
{"cluster_id": "chunk_014-145", "topic": "The recorded ch2 activity included 45 touches", "canonical_claim": "The recorded `ch2` activity included 45 touches in the `ch1` namespace under the `cas/manifests` prefix."}
{"cluster_id": "chunk_014-146", "topic": "Across the stale-edge sample from rounds 109", "canonical_claim": "Across the stale-edge sample from rounds 109 through 254, `stale_edge` remained 15 and `unreachable` remained 15 while `pending_gc` and `awaiting_gc` remained zero for the constant keyset `c82df19e`."}
{"cluster_id": "chunk_014-147", "topic": "Each SYSTEM CAS GC RUN round is", "canonical_claim": "Each `SYSTEM CAS GC RUN` round is recorded in `system.cas_gc_log` as `Start` and `Finish` rows with `trigger = 'Manual'`."}
{"cluster_id": "chunk_014-148", "topic": "The snapshot-publish dispatch count is observable through", "canonical_claim": "The snapshot-publish dispatch count is observable through `ProfileEvents::CasRefSnapshotPublishDispatched`."}
{"cluster_id": "chunk_014-149", "topic": "The CAS suite generator reconciles 296 source", "canonical_claim": "The CAS suite generator reconciles 296 source Cas suites after excluding CascadeWriteBuffer to 278 runnable suites: 19 compile-guarded suites are excluded and CASBackendContract contributes both CASInMemory/ and CASLocal/ instantiation spellings."}
{"cluster_id": "chunk_014-150", "topic": "The fold_reduce phase publishes frontier_complete alongside suppress_destructive", "canonical_claim": "The `fold_reduce` phase publishes `frontier_complete` alongside `suppress_destructive`, allowing tests and operators to read the gate's verdict instead of recomputing `namespaces > 0 && proven == namespaces` from a tally."}
{"cluster_id": "chunk_014-151", "topic": "When a frontier deficit is nonzero, the", "canonical_claim": "When a frontier deficit is nonzero, the suppression warning appends `unproven: <bucket>=<count>, ...`; rounds with no deficit omit the clause."}
{"cluster_id": "chunk_014-152", "topic": "FoldResult::FrontierUnproven records a reason per unproven namespace", "canonical_claim": "`FoldResult::FrontierUnproven` records a reason per unproven namespace, while `FoldResult::FrontierDeficit` exposes nine counters and `count`/`total`/`describe` helpers; suppression logs append an `unproven:` clause when the deficit is nonzero."}
{"cluster_id": "chunk_014-153", "topic": "Each fan-out with work increments CasBlobUploadFanoutBatches once", "canonical_claim": "Each fan-out with work increments `CasBlobUploadFanoutBatches` once, and each unique-reference task dispatched increments `CasBlobUploadFanoutTasks` once."}
{"cluster_id": "chunk_014-154", "topic": "The namespace-cleanup phase publishes janitor_pages, janitor_keys, and", "canonical_claim": "The namespace-cleanup phase publishes `janitor_pages`, `janitor_keys`, and `janitor_deleted` metrics."}
{"cluster_id": "chunk_014-155", "topic": "Observed object-store enumeration holes were short adjacent", "canonical_claim": "Observed object-store enumeration holes were short adjacent runs of 2–15 keys rather than page-sized gaps of about 1000 keys."}
{"cluster_id": "chunk_014-156", "topic": "The T6a append-concurrency reproduction ran 935 rounds", "canonical_claim": "The T6a append-concurrency reproduction ran 935 rounds across 4 streams with zero deficits; a counterfactual old predicate fired 12 times out of 935."}
{"cluster_id": "chunk_014-157", "topic": "The RefCowMap design's correctness/testing plan requires the", "canonical_claim": "The `RefCowMap` design's correctness/testing plan requires the existing `Ca*:Cas*` gtest battery to stay green at 905/905 as evidence that behavior is byte-identical."}
{"cluster_id": "chunk_014-158", "topic": "A 2026-07-19 post-fix live chaos-soak measurement (workers=6", "canonical_claim": "A 2026-07-19 post-fix live chaos-soak measurement (workers=6, per-operation-unit attribution via ProfileEvents/part_log/content_addressed_garbage_collection_log) found a GC success round costs ~55-100x more per unit than a single INSERT/MERGE/MUTATE — not from PUTs (GC issues almost none) but from GET+HEAD volume in the retire/recheck verification pipeline ($0.00725/round vs $0.000085/INSERT); DownloadPart (replication fetch) was the cheapest unit at ~24x cheaper than an INSERT, confirming it as a lightweight metadata-level operation."}
{"cluster_id": "chunk_014-159", "topic": "In the same 2026-07-19 measurement window, aggregate", "canonical_claim": "In the same 2026-07-19 measurement window, aggregate request cost (request pricing only) was approximately $15.13, with CAS-internal housekeeping (per-blob `.meta` sidecar PUTs, cas/manifests, cas/refs, gc/gen attempt/outcome records) as the single largest line item at $5.64 (1,128,865 PUTs) -- larger than the INSERT bucket itself, and not previously broken out as its own cost driver."}
{"cluster_id": "chunk_014-160", "topic": "Cross-referencing generic S3-level counters (S3PutObject/S3GetObject/S3HeadObject/S3ListObjects/DiskS3DeleteObjects) against CAS's", "canonical_claim": "Cross-referencing generic S3-level counters (`S3PutObject`/`S3GetObject`/`S3HeadObject`/`S3ListObjects`/`DiskS3DeleteObjects`) against CAS's own semantic per-object-kind counters (`Cas{Blob,Gc,Manifest,Meta,Root,Other}{Put,Get,Head,Delete,List}`) in one soak session found the Cas-level counters covered PUT/GET/LIST far better than the query_log/part_log/GC-log attribution axis (77-96% vs 4-58%) because they are incremented at the S3-call site independent of ThreadGroup attribution, while HEAD stayed persistently under-attributed on both axes (~56%), left as an open question."}
{"cluster_id": "chunk_014-161", "topic": "Deep read-path profiling in the campaign found", "canonical_claim": "Deep read-path profiling in the campaign found `DownloadPart` (relink fetch) is the dominant read cost: 28.7k fetches cost 1.02M GET + 1.96M HEAD requests despite zero blob re-upload, driven by per-file `loadMeta` GET plus occupancy/adopt HEAD; the identified optimization lever is manifest-trust adoption without per-file probes."}
{"cluster_id": "chunk_014-162", "topic": "At soak checkpoints, both replicas must match", "canonical_claim": "At soak checkpoints, both replicas must match an independent SQL model, ca-fsck must report dangling=0, and ca-gc-dryrun must propose only genuinely unreachable objects."}
{"cluster_id": "chunk_014-163", "topic": "A CAS health check should inspect correctness", "canonical_claim": "A CAS health check should inspect correctness invariants and broad audit-log reasons before evaluating performance metrics."}
{"cluster_id": "chunk_014-164", "topic": "system.blob_storage_log does not log reads by default", "canonical_claim": "`system.blob_storage_log` does not log reads by default because `enable_blob_storage_log_for_read_operations` defaults to false and requires `enable_blob_storage_log`."}
{"cluster_id": "chunk_014-165", "topic": "On the AWS validation stand, reclaim progressed", "canonical_claim": "On the AWS validation stand, reclaim progressed from 11 condemned objects in round 24 to graduation in round 26 and deletion of all 11 in round 27; after reclaim fsck reported `dangling=0 unreachable=0 unaccounted=0`."}
{"cluster_id": "chunk_014-166", "topic": "Phase 4 soak validation compares one CA-local", "canonical_claim": "Phase 4 soak validation compares one CA-local plus CA-S3 run with retention enabled against one run with `cas_part_folder_cache_bytes = 0`, including anomaly baselines and `CasPartFolderView*` counters."}
{"cluster_id": "chunk_014-167", "topic": "explain(PartRefKey) is structured diagnostic state containing retention", "canonical_claim": "`explain(PartRefKey)` is structured diagnostic state containing retention status, the last decision, manifest reference, and estimated bytes, and is intended only for tests and log lines."}
{"cluster_id": "chunk_014-168", "topic": "Freshness-meta validation requires CAS gtests, a deterministic", "canonical_claim": "Freshness-meta validation requires CAS gtests, a deterministic GC-delete-versus-writer-resurrect race test, ASan, a CA-s3 lane with zero promote aborts and zero fsck dangles, and a soak with meta_without_body equal to zero."}
{"cluster_id": "chunk_014-169", "topic": "The retired-in-snapshot validation requires the S30 resurrect-churn/recurring-hash", "canonical_claim": "The retired-in-snapshot validation requires the S30 resurrect-churn/recurring-hash scenario and the S33 concurrent-leader scenario to pass; S33 guards the concurrent-leader reclaim-leak regression and S30 covers the resurrect-supersede path."}
{"cluster_id": "chunk_014-170", "topic": "The one-hour soak success criteria are two", "canonical_claim": "The one-hour soak success criteria are two consecutive final `fsck dangling=0 unreachable=0` reads, an empty `ca-gc-dryrun`, no logical-error exceptions, and bounded snapshot/log tails; host logs are archived before teardown."}
{"cluster_id": "chunk_014-171", "topic": "Round B measures each lever with 10-minute", "canonical_claim": "Round B measures each lever with 10-minute phase-3 comparative soaks using the same seed and one variable per run, classifying results from per-family `ProfileEvents` deltas summed through `metric_log` because `system.events` resets on chaos restarts."}
{"cluster_id": "chunk_014-172", "topic": "system.content_addressed_mounts must expose a non-gated lifecycle snapshot", "canonical_claim": "`system.content_addressed_mounts` must expose a non-gated lifecycle snapshot with `lifecycle`, `lifecycle_reason`, and `lifecycle_since` even when `store()` is refused for a not-live disk."}
{"cluster_id": "chunk_014-173", "topic": "A future CAS soak intended as a", "canonical_claim": "A future CAS soak intended as a performance specimen must dump system tables to `utils/ca-soak/logs/` before `compose down`, or persist `/var/lib/clickhouse` on a named volume, because the existing compose setup loses those tables with the container."}
{"cluster_id": "chunk_014-174", "topic": "A passing dev-scale soak is never treated", "canonical_claim": "A passing dev-scale soak is never treated as specification-scale validation because each card records its actual scale and verdict."}
{"cluster_id": "chunk_014-175", "topic": "For CPU, off-CPU, and memory investigations, scenario", "canonical_claim": "For CPU, off-CPU, and memory investigations, scenario reports must rank `system.trace_log` stacks separately for `CPU`, `Real`, and `Memory` trace types."}
{"cluster_id": "chunk_014-176", "topic": "The unattended campaign watchdog is intended to", "canonical_claim": "The unattended campaign watchdog is intended to run every 20 minutes and check fresh progress artifacts, disk usage, CPU starvation, Docker debris, and heavy-build versus soak overlap before intervening."}
{"cluster_id": "chunk_014-177", "topic": "Phase durations in system.cas_gc_log do not sum", "canonical_claim": "Phase durations in `system.cas_gc_log` do not sum to `duration_ms`; the `Finish` row's `duration_ms` is authoritative for total round time."}
{"cluster_id": "chunk_014-178", "topic": "Post-soak validation should mine system.query_log ProfileEvents for", "canonical_claim": "Post-soak validation should mine system.query_log ProfileEvents for PartsLockWaitMicroseconds, S3 request counters, and Keeper wait events to identify SELECT outliers."}
{"cluster_id": "chunk_014-179", "topic": "SYSTEM CAS GC RUN returns one result", "canonical_claim": "`SYSTEM CAS GC RUN` returns one result row per disk with the columns `disk`, `acquired_lease`, `deferred`, `round`, `candidates_marked`, `objects_deleted`, `objects_absent`, `objects_replaced`, `objects_spared`, `manifests_deleted`, `entries_condemned`, `entries_graduated`, `entries_redeleted`, `fence_outs`, and `anomalies`."}
{"cluster_id": "chunk_014-180", "topic": "A successful SYSTEM CAS GC REBUILD returns", "canonical_claim": "A successful `SYSTEM CAS GC REBUILD` returns `disk`, `performed`, `round`, `generation`, `namespaces`, `shards`, `committed_refs`, `live_precommits`, `unowned_alive_manifests`, `edges`, and `clamped_shards`."}
{"cluster_id": "chunk_014-181", "topic": "SYSTEM CAS DROP POOL MEMBER returns server_root_id", "canonical_claim": "`SYSTEM CAS DROP POOL MEMBER` returns `server_root_id`, `namespaces_removed`, `namespaces_already_removed`, `committed_refs_removed`, `precommits_removed`, `manifest_debris_removed`, `staging_objects_removed`, `mountpoint_objects_removed`, `slot_removed`, and `warnings`."}
{"cluster_id": "chunk_014-182", "topic": "The focused CasGcRefWalkPlan test for unmatched adopted", "canonical_claim": "The focused `CasGcRefWalkPlan` test for unmatched adopted parents verifies one event, one exact-id log line, and unchanged absence from plan, parent-state, and successor-state output."}
{"cluster_id": "chunk_014-183", "topic": "During the 4h chaos soak, the soak", "canonical_claim": "During the 4h chaos soak, the soak harness's own `pool_objects` telemetry returned `None` for the entire run and `pool_bytes` returned `None` on roughly half the ticks under write load; the throttle fails closed on `None` (treats it as `_THROTTLE_MAX`), and an independent `du`-based resource monitor gave ground truth instead — the soak's own pool telemetry is unreliable at scale."}
{"cluster_id": "chunk_014-184", "topic": "system.part_log's NewPart row shares the same underlying", "canonical_claim": "`system.part_log`'s `NewPart` row shares the same underlying ProfileEvents as the originating `INSERT` query's `system.query_log` row (confirmed via `sumMap(ProfileEvents)` cross-checks), so summing them together double-counts S3/Cas cost; `MergeParts`/`MutatePart`/`DownloadPart` are genuinely additive on top of `query_log` because their originating `Optimize`/`Alter` query_log rows show zero S3 ProfileEvents (background execution is captured only by part_log)."}
{"cluster_id": "chunk_014-185", "topic": "system.trace_log's three trace_types answer different questions and", "canonical_claim": "`system.trace_log`'s three trace_types answer different questions and must not be conflated: `CPU` shows where processor cycles are spent (group by full stack, not just leaf frame); `Real` shows wall-clock time including blocking waits, where most top entries are healthy idle capacity and the useful signal is a thread blocked inside the product's own internal queue/lock; `Memory` samples allocation events (not resident memory), and its leaf frame is always the profiler's own capture routine, so the first one or two frames must be skipped before reading the real allocation site."}
{"cluster_id": "chunk_014-186", "topic": "S23's initial idle-RSS-growth failure correlated with elevated", "canonical_claim": "S23's initial idle-RSS-growth failure correlated with elevated S3 error rates in the same window (read 8.6%, write 17.2% on one node); S3 retries allocate transient upload/download buffers that jemalloc keeps resident as dirty pages, inflating RSS without inflating tracked memory, which is one of two identified amplifiers of the outlier (the other being fresh-boot warmup variance)."}
{"cluster_id": "chunk_014-187", "topic": "part_log.ProfileEvents shows s3_put=0 while cas_cond>0 for a", "canonical_claim": "`part_log.ProfileEvents` shows `s3_put=0` while `cas_cond>0` for a given part operation because `S3PutObject` increments on upload-pool threads outside the part-op scope; use `CasConditionalWriteAttempts` to attribute per-part S3 writes instead."}
{"cluster_id": "chunk_014-188", "topic": "GC's per-hash .meta writes run on a", "canonical_claim": "GC's per-hash `.meta` writes run on a bounded side pool and escape the GC round's `ProfileEvents` scope, so round-level `cas_cond` undercounts GC's actual write volume; this is tracked as `CasGcMetaWriteAnomaly`."}
{"cluster_id": "chunk_014-189", "topic": "The CA stateless suite (~26 CA-specific tests", "canonical_claim": "The CA stateless suite (~26 CA-specific tests, `04278`-`05009`) covers black-box disk equivalence, correctness-under-active-GC, no-leftovers, GC introspection via `system.content_addressed_garbage_collection_log` and `system.content_addressed_log`, Replicated support, transactions, freeze/backup/restore/fetch-partition, mutations/lightweight-delete/patch-parts, projections, dedup window, and GC snap prune; 10 tests carry `no-content-addressed-storage` and are gated out under the CA-default job."}
{"cluster_id": "chunk_014-190", "topic": "Before multi-tenant deployment, the default grants for", "canonical_claim": "Before multi-tenant deployment, the default grants for `system.content_addressed_log` and `system.content_addressed_mounts` should be verified for low-privilege users."}
{"cluster_id": "chunk_014-191", "topic": "The first relink measurement is the steady-state", "canonical_claim": "The first relink measurement is the steady-state share of confirms hitting a cold table; a materially nonzero share falsifies the warm-by-construction premise and triggers reconsideration of the rejected recovery bounds."}
{"cluster_id": "chunk_014-192", "topic": "A long or wedged GC round currently", "canonical_claim": "A long or wedged GC round currently becomes visible only after the fact; the planned observability includes a duration watchdog, LIST/fold progress events, and an alert for an unbalanced `gc_fold_begin`/`gc_fold_end` pair."}
{"cluster_id": "chunk_014-193", "topic": "CA product operability still required a SYSTEM", "canonical_claim": "CA product operability still required a `SYSTEM` control surface for starting and stopping GC, putting a pool read-only, and running checks; product-side GC stop existed only as a soak-harness workaround."}
{"cluster_id": "chunk_014-194", "topic": "The server OOM finding of approximately 49", "canonical_claim": "The server OOM finding of approximately 49 GiB RSS at hour four had not been reproduced after the streaming `putBlob` fix and required a long soak to confirm resolution."}
{"cluster_id": "chunk_014-195", "topic": "The GC bottleneck study requires per-phase wall", "canonical_claim": "The GC bottleneck study requires per-phase wall time, request counts by verb, bytes, pages, retries, and timeouts; it also requires a namespace-churn reproduction rig with controllable arrival rate and an unsaturated endpoint."}
{"cluster_id": "chunk_014-196", "topic": "The mount fence-recovery implementation still requires live", "canonical_claim": "The mount fence-recovery implementation still requires live validation under induced S3 latency to prove renewal cadence remains within its period, a live lease does not expire, no spurious `gc_fence_out` occurs, and genuine fence-out recovers as a higher-`writer_epoch` incarnation."}
{"cluster_id": "chunk_014-197", "topic": "A distinct-manifest counter per GC round would", "canonical_claim": "A distinct-manifest counter per GC round would make the manifest re-read ratio continuously observable and test the large-window redundancy prediction."}
{"cluster_id": "chunk_014-198", "topic": "CAS introspection should expose pool format and", "canonical_claim": "CAS introspection should expose pool format and backend capabilities, namespace roots and generations, manifest lifecycle counts, blob reachability, GC cursors, decoded key/object explanations, and machine-readable dry-run reports."}
{"cluster_id": "chunk_014-199", "topic": "The T8 healthy-pool soak should check zero", "canonical_claim": "The T8 healthy-pool soak should check zero `no usable checkpoint` anomalies in addition to zero `unproven` and zero frontier-probe budget, because a namespace with no usable checkpoint now suppresses through an anomaly rather than silently."}
{"cluster_id": "chunk_014-200", "topic": "gc_round_sweep_namespace_budget = 20 should be observed in", "canonical_claim": "`gc_round_sweep_namespace_budget = 20` should be observed in T8 soak testing because it is a per-round throughput limit that can retain namespaces when more than 20 debris namespaces share one page."}
{"cluster_id": "chunk_014-201", "topic": "To make a CAS soak's tracked-memory gate", "canonical_claim": "To make a CAS soak's tracked-memory gate reflect CAS behavior rather than the profiler firehose, either disable the harness's 10 ms query profiler for S23 or subtract the `DB::SystemLog` flush working set from the tracked-memory measurement."}
{"cluster_id": "chunk_014-202", "topic": "The CA praktika job runs the complete", "canonical_claim": "The CA praktika job runs the complete stateless suite with a CA disk as the default MergeTree storage, using local object storage and RustFS-backed S3 variants."}
