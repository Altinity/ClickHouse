{"cluster_id": "chunk_004-151", "topic": "generation handoff leak", "canonical_claim": "The GC retention cursor can advance past a still-referenced generation before the best-effort hand-off or wholesale delete; if the process crashes after the retention cursor CAS and before deleting the `gc/gen/<g>/` prefix, a whole generation can leak until fsck, and a normal retry does not revisit the advanced cursor."}
{"cluster_id": "chunk_004-152", "topic": "pending manifest preservation", "canonical_claim": "The orphan sweep must preserve or skip a dropped but unsealed committed manifest body with a pending removal because the removal fold still needs that body to emit its decrement; this was the condition shown by GC-WEDGE-2026-07-10."}
{"cluster_id": "chunk_004-153", "topic": "abandoned precommit parked shard", "canonical_claim": "A watermark-dead abandoned precommit can remain permanently protected when token-diff discovery parks a content-static shard: `Skip` prevents `reclaimAbandonedPrecommit` from running again, so the fix forces a read when sealed coverage reports such a precommit."}
{"cluster_id": "chunk_004-154", "topic": "concurrent GC leader artifacts", "canonical_claim": "Concurrent explicit GC leaders could orphan unreachable content blobs and part-manifests while preserving safety (`fsck dangling=0`): a deposed leader wrote a final-key fold seal under `gc/gen/<gen>/fold_seal` before the lease-guarded `gc/state` CAS, a divergent `putIfAbsent` made the losing leader throw `ABORTED` without advancing state, and the winner's seal was never re-adopted so owner-removal events were not re-folded. Attempt-scoped generations keyed by `(gen, attempt = lease.seq)` hide unadopted artifacts and fix this."}
{"cluster_id": "chunk_004-155", "topic": "D1 originated because `dropNamespace` tombstoned shard contents but never remove", "canonical_claim": "D1 originated because `dropNamespace` tombstoned shard contents but never removed the namespace from `gc/registry`, causing registry entries and empty ref-shard objects to accumulate as the S30 `other` residual."}
{"cluster_id": "chunk_004-156", "topic": "GC rebuild initially missed physical blobs with zero rebuilt edges because they ", "canonical_claim": "GC rebuild initially missed physical blobs with zero rebuilt edges because they had no run row and never transitioned to zero; the rebuild now condemns such blobs directly using head-captured tokens, a minted round, and floor-gated graduation."}
{"cluster_id": "chunk_004-157", "topic": "GC safety bug allowed clamped shards to skip landed events", "canonical_claim": "A GC safety bug allowed clamped shards to skip landed events: a blob was deleted while its landed `+1` remained unfolded, yielding 31 dangling blobs and one dangling manifest; this violated INV-NO-LOSS."}
{"cluster_id": "chunk_004-158", "topic": "P2 reported 116 `delete_pending blob recovered in-degree — structurally impossib", "canonical_claim": "P2 reported 116 `delete_pending blob recovered in-degree — structurally impossible under the ack floor (spared anyway, fail-closed)` events in a four-minute window on ch1; the root cause was unknown in the campaign."}
{"cluster_id": "chunk_004-159", "topic": "P5 was GC round 31 stalling for about four hours under leader contention, with s", "canonical_claim": "P5 was GC round 31 stalling for about four hours under leader contention, with six of eight attempts reporting `manifests_deleted=0`, a 2:1 leadership skew, and chronic heartbeat acknowledgement lag."}
{"cluster_id": "chunk_004-160", "topic": "dryrun reachability divergence", "canonical_claim": "`ca-gc-dryrun` proposed or under-counted reachability incorrectly: it proposed 132 blobs as deletable in S18 even though fsck classified them reachable and real GC spared them, and across S18, S25, and S26 it over-proposed reachable blobs while real GC kept them and fsck reported `dangling=0`; the systematic defect affected frozen or shadow-referenced, non-Atomic, and verbatim-churn roots, while real GC remained safe with zero dangling blobs, and the intended invariant is `dryrun ⊆ (unreachable ∪ pending-gc)`."}
{"cluster_id": "chunk_004-161", "topic": "S30 initially measured `CasRootGet` growth from 75 to 190 across 30 create/drop ", "canonical_claim": "S30 initially measured `CasRootGet` growth from 75 to 190 across 30 create/drop cycles despite no live tables, but the later S34 correction attributed the apparent F5 defect to sampling reclaim backlog instead of steady-state discovery."}
{"cluster_id": "chunk_004-162", "topic": "Build", "canonical_claim": "Build::promote unconditionally overwriting root.refs[final_ref_name] can leave the old Committed binding in the journal without its releasing -1 owner event, pinning the old manifest and uniquely owned blobs."}
{"cluster_id": "chunk_004-163", "topic": "ABANDON-RETIRE-ORDERING allows GC to see a watermark-dead but not-yet-removed pr", "canonical_claim": "ABANDON-RETIRE-ORDERING allows GC to see a watermark-dead but not-yet-removed precommit and append a duplicate removal while abandon appends its own removal."}
{"cluster_id": "chunk_004-164", "topic": "GC liveness wedge was traced to a condemn-race retry that republished the same M", "canonical_claim": "A GC liveness wedge was traced to a condemn-race retry that republished the same ManifestIds while mf_cleanup retained removal-half entries, allowing a later removal fold to delete live bodies and clamp collection on missing bodies."}
{"cluster_id": "chunk_004-165", "topic": "Racing multipart copies can produce different multipart ETags, leaving the losin", "canonical_claim": "Racing multipart copies can produce different multipart ETags, leaving the losing writer's recorded token stale and making exact-token deletion unable to reclaim the object."}
{"cluster_id": "chunk_004-166", "topic": "manual GC observer serialization", "canonical_claim": "`CasGcScheduler::runOneRoundNow` constructs a throwaway `Cas::Gc` and does not serialize with `loop`, so manual GC loses stable observer state and can run concurrently under one `gc_id`."}
{"cluster_id": "chunk_004-167", "topic": "manual GC scheduler UAF", "canonical_claim": "A manual GC round can run through a raw `gc_scheduler` pointer captured outside `gc_scheduler_mutex`, leaving a use-after-free window against concurrent scheduler `reset()`; review finding №8 confirmed that `runGarbageCollectionRoundNow` snapshots `gc_scheduler.get()` under a mutex, unlocks, and can dereference it after shutdown resets the scheduler."}
{"cluster_id": "chunk_004-168", "topic": "Lazy scheduler-creation sites can resurrect a `gc_scheduler` after shutdown", "canonical_claim": "Lazy scheduler-creation sites can resurrect a `gc_scheduler` after shutdown; a full fix requires retaining a shared pointer for rounds and gating lazy creation with a started flag."}
{"cluster_id": "chunk_004-169", "topic": "S13 orphan-manifest result was a product regression", "canonical_claim": "The S13 orphan-manifest result was a product regression: successor cleanup removed the GC-side reclaim while the writer-side sweep was one-shot and silently failed, leaving two orphan `_manifests` after forced GC under kill-chaos."}
{"cluster_id": "chunk_004-170", "topic": "condemn marker write failures", "canonical_claim": "Swallowed or failed Condemned-marker writes can let a writer adopt a retired token or treat absent/Clean metadata as live, after which a later exact-token GC delete can remove a blob still referenced by a committed edge or newly referenced blob; task T1 fixed the load-bearing gate in commits `21a6051e8ff` and `12ae454e7f2`."}
{"cluster_id": "chunk_004-171", "topic": "parent-union generation pruning", "canonical_claim": "Pre-CAS generation or retention pruning based only on the proposed seal can delete a run or data still referenced by a winning concurrent leader's parent seal or by the currently adopted GC snapshot, permanently wedging GC with `CORRUPTED_DATA` in `PriorEdgeCursor`; task T3 fixed it by using the parent-union-proposed generation set in commit `883c8f92d66`."}
{"cluster_id": "chunk_004-172", "topic": "Review finding №18 is real", "canonical_claim": "Review finding №18 is real: emulated `get`/`head` tokens use `TokenType::Emulated` while list tokens use `TokenType::ETag`, so listed-token conditional deletes fail safely but generation-prune accounting can report deletion while objects leak."}
{"cluster_id": "chunk_004-173", "topic": "Review finding №19c is latent local-CA data loss across restart", "canonical_claim": "Review finding №19c is latent local-CA data loss across restart: a non-persisted `emu_seq` can re-mint a persisted condemn token value, allowing `deleteExact` to delete a live incarnation after restart."}
{"cluster_id": "chunk_004-174", "topic": "Review finding №28 is real", "canonical_claim": "Review finding №28 is real: `Pool::beginPartWrite` can register a build sequence before construction fails, permanently pinning the GC `minActive` retention floor until remount."}
{"cluster_id": "chunk_004-175", "topic": "S31's `ca-gc-dryrun` failure was caused by a card oracle comparing a single-roun", "canonical_claim": "S31's `ca-gc-dryrun` failure was caused by a card oracle comparing a single-round `dry_count` with cumulative multi-round `deleted_total`; `Gc::previewDeletes` already iterates every shard."}
{"cluster_id": "chunk_004-176", "topic": "late-log clamp starvation", "canonical_claim": "A poison late log causes repeated failing reads that clamp its key; every healthy GC round records `CasGcClampSuppressedPasses:1`, clamp suppression skips the orphan-manifest sweep pass that would call `reportLateLogsIfAny`, and late-log detection is therefore structurally starved while the clamp persists."}
{"cluster_id": "chunk_004-177", "topic": "Issue [Altinity/ClickHouse#2052](https", "canonical_claim": "Issue [Altinity/ClickHouse#2052](https://github.com/Altinity/ClickHouse/issues/2052) does not reproduce on HEAD because commit `318291fe5e5` moved orphan-manifest ownership recovery to `recoverRefTable`, which `CasOrphanManifestSweep.cpp` still uses through `recoverRefTableDetailed`."}
{"cluster_id": "chunk_004-178", "topic": "shipped TLA+ confirm model permits a dangle when one durable journal record is o", "canonical_claim": "The shipped TLA+ confirm model permits a dangle when one durable journal record is omitted from a paginated observation: `_sab_holeylist` violates the dangle property, and `_main` with `MaxHoles = 0` proves only that the confirm protocol adds no new dangle path under a completeness assumption."}
{"cluster_id": "chunk_004-179", "topic": "FSCK/GC residual blobs", "canonical_claim": "The FSCK/GC disagreement involved 56 blobs: `ca-fsck` classified them as `AwaitingGc` with `dangling=0`, while GC repeatedly reported zero candidates and never drained them; each present blob had no live reference and exactly one residual source edge in the adopted in-degree run, the corresponding manifest no longer existed, GC marked zero candidates across 1,062 rounds, and the edges traced to four `tmp-fetch_*` refs published and dropped within 43 ms."}
{"cluster_id": "chunk_004-180", "topic": "In the retention direction, omitting a ref-log removal record can advance the cu", "canonical_claim": "In the retention direction, omitting a ref-log removal record can advance the cursor past that record permanently, so the removal is never folded and the residual activation edge retains the blob after the listing recovers."}
{"cluster_id": "chunk_004-181", "topic": "In the deletion direction, omitting a second live owner's activation `+1` while ", "canonical_claim": "In the deletion direction, omitting a second live owner's activation `+1` while folding the first owner's removal can make GC compute zero in-degree for a blob still referenced by a committed manifest and delete that blob."}
{"cluster_id": "chunk_004-182", "topic": "`BlobDelta", "canonical_claim": "`BlobDelta::txn_ordinal` must remain the last member unless positional aggregate initializers are converted, because inserting it before `remove` silently rebinds `BlobDelta{ref, source_id, remove}` and turns removals into activations."}
{"cluster_id": "chunk_004-183", "topic": "residual `+1` can arise either because its `-1` ref-log was omitted and the curs", "canonical_claim": "A residual `+1` can arise either because its `-1` ref-log was omitted and the cursor sealed past it, or because the `-1` reached the reducer before its `+1`; the latter is counted by `CasGcUnmatchedRemoveDeltas` and is a separate investigation."}
{"cluster_id": "chunk_004-184", "topic": "initial arithmetic fold walk caused an unbounded-round liveness regression", "canonical_claim": "The initial arithmetic fold walk caused an unbounded-round liveness regression: concurrent appends made the walker chase the tail, producing zero completed GC rounds in 42 minutes on a hot approximately 30 GiB pool."}
{"cluster_id": "chunk_004-185", "topic": "REBUILD manifest-less orphans", "canonical_claim": "REBUILD cannot safely reclaim manifest-less orphan blobs while a live manifest can be hidden from `LIST`: it can currently condemn a physically listed blob when its live manifest is omitted, and a rebuild that condemns nothing cannot reclaim a blob whose manifest no longer exists anywhere because that blob has no row in the rebuilt baseline; such blobs remain fsck `unaccounted` until the authoritative R4 build/upload registry exists."}
{"cluster_id": "chunk_004-186", "topic": "Before incarnation qualification, `groupRefKeys` grouped listed ref keys by `par", "canonical_claim": "Before incarnation qualification, `groupRefKeys` grouped listed ref keys by `parsed->id.ns.string()` and discarded `parsed->id.incarnation`, merging old and reborn lives into one `RefTableListing`."}
{"cluster_id": "chunk_004-187", "topic": "orphan sweep folded edges", "canonical_claim": "The S42/orphan-manifest sweep premise that `unowned` implies no folded edges is false: a precommit's folded `+1` blob edges can already exist, and sweeping the orphan manifest body can strand those edges permanently, so orphan nomination must directly account for source edges."}
{"cluster_id": "chunk_004-188", "topic": "`CasGc", "canonical_claim": "`CasGc.cpp:718` codes an undecodable write-once outcome log as `ABORTED`, causing retry, although the immutable bytes indicate `CORRUPTED_DATA`; `CasGc.cpp:808` is a separate case where `ABORTED` remains correct because `gc/state` genuinely moved."}
{"cluster_id": "chunk_004-189", "topic": "uncataloged shard-0 cursor is re-added to every GC round, so its recurring anoma", "canonical_claim": "An uncataloged shard-0 cursor is re-added to every GC round, so its recurring anomaly permanently suppresses pool-wide reclamation, prevents cleanup-item retirement, leaves the name unrecreatable, and requires `SYSTEM CONTENT ADDRESSED GC REBUILD` to clear it."}
{"cluster_id": "chunk_004-190", "topic": "persisted fold cursor is keyed only by namespace and shard, so a recreated names", "canonical_claim": "The persisted fold cursor is keyed only by namespace and shard, so a recreated namespace can inherit a predecessor incarnation's sequence boundary and omit the new incarnation's first positive owner edges from the GC baseline."}
{"cluster_id": "chunk_004-191", "topic": "Deleting the perpetual `_files` janitor would make a single omitted `LIST` resul", "canonical_claim": "Deleting the perpetual `_files` janitor would make a single omitted `LIST` result leak dead-incarnation files permanently, because the one bounded cleanup attempt would be their only reclaimer."}
{"cluster_id": "chunk_004-192", "topic": "RustFS ref-log omission", "canonical_claim": "On RustFS, a ref-log enumeration returned sequence `0x1430e` while omitting predecessors `0x1430c` and `0x1430d`; the omitted keys were durable about nineteen seconds before enumeration began and were written 2.2 ms before the returned log, so the observed omission was a contract violation."}
{"cluster_id": "chunk_004-193", "topic": "Concurrent delete, stale-epoch minting, out-of-order appends, and a late `Unreso", "canonical_claim": "Concurrent delete, stale-epoch minting, out-of-order appends, and a late `Unresolved` PUT were excluded as explanations for the RustFS omission by evidence at three source levels."}
{"cluster_id": "chunk_004-194", "topic": "Resurrection of a condemned object can overwrite a live incarnation installed by", "canonical_claim": "Resurrection of a condemned object can overwrite a live incarnation installed by another writer because the rewrite is not bound to the observed token with `If-Match`."}
{"cluster_id": "chunk_004-195", "topic": "LIST-as-journal cursor hole", "canonical_claim": "GC discovers ref-log transactions by paginating `LIST(cas/refs/)` without a snapshot token, high-water mark, or contiguity/completeness proof, then advances the fold cursor over records it happened to see or over records merely observed in a paginated LIST; an omitted record is permanently skipped, creating the LIST-as-journal deletion hole tracked as a release blocker."}
{"cluster_id": "chunk_004-196", "topic": "GC-side recovered-in-degree warning was often a false alarm caused by a dedup-ad", "canonical_claim": "The GC-side recovered-in-degree warning was often a false alarm caused by a dedup-adopt-versus-condemn TOCTOU: a writer adopted a blob after GC read it as not condemned, and GC then spared it after recovering the fresh edge."}
{"cluster_id": "chunk_004-197", "topic": "GC collapse under mass table drops was a queue-stability failure", "canonical_claim": "GC collapse under mass table drops was a queue-stability failure: serialized rounds became longer than the arrival rate, so dropped namespaces persisted, the universe grew, and subsequent folds became slower."}
{"cluster_id": "chunk_004-198", "topic": "GC meta pool uses a queue of zero effective depth and `scheduleOrThrowOnError` b", "canonical_claim": "The GC meta pool uses a queue of zero effective depth and `scheduleOrThrowOnError` blocks the fold thread from the 17th in-flight meta operation onward, preventing fold LIST/GET work from overlapping with condemn PUTs."}
{"cluster_id": "chunk_004-199", "topic": "Completed namespaces retain `_cleanup` markers and their newest constant-size `R", "canonical_claim": "Completed namespaces retain `_cleanup` markers and their newest constant-size `Removed` snapshots, so the globally enumerated ref prefix grows linearly with historically unique namespaces."}
{"cluster_id": "chunk_004-200", "topic": "unmatched `-1` delta is silently ignored by the set-presence merge keyed by `(bl", "canonical_claim": "An unmatched `-1` delta is silently ignored by the set-presence merge keyed by `(blob_ref, source_id)`, so its corresponding `+1` can remain forever and retain a blob until a full `ca-gc-rebuild`."}
{"cluster_id": "chunk_004-201", "topic": "adopted fold seal once referenced a missing run under an older generation key, c", "canonical_claim": "An adopted fold seal once referenced a missing run under an older generation key, causing `zeroInDegree` to find no candidates and allowing GC to complete rounds while reclaiming nothing."}
{"cluster_id": "chunk_004-202", "topic": "scalar `suppress_destructive = !anomalies", "canonical_claim": "The scalar `suppress_destructive = !anomalies.empty() || !holds.empty() || frontier_incomplete` gate lets one namespace's anomaly stop reclamation for the whole pool; the intended Stage B shape is a per-namespace destructive gate."}
{"cluster_id": "chunk_004-203", "topic": "nonzero reclaimable residual in S33 is a real regression of the attempt-scoped g", "canonical_claim": "A nonzero reclaimable residual in S33 is a real regression of the attempt-scoped generation invariant: concurrent explicit GC leaders left 87 or 84 `_manifests` unreachable while `dangling=0`."}
{"cluster_id": "chunk_004-204", "topic": "GC per-round fanout grew across create/drop iterations even when no table remain", "canonical_claim": "GC per-round fanout grew across create/drop iterations even when no table remained live, violating the D1 registry-removal and dropped-shard-reclaim guarantee."}
{"cluster_id": "chunk_004-205", "topic": "resurrected-token orphan", "canonical_claim": "Under recurring content hashes, a resurrected blob incarnation B can replace condemned incarnation A at the same hash key; exact-token deletion correctly skips deleting B, but a touch-gated hash-keyed fold fails to re-condemn the replacement token, leaving an `unaccounted` physical orphan. The fix made `closeBlob` re-condemn the current token."}
{"cluster_id": "chunk_004-206", "topic": "GC removal-fold wedge can clamp a shard forever when `CasOrphanManifestSweep` de", "canonical_claim": "A GC removal-fold wedge can clamp a shard forever when `CasOrphanManifestSweep` deletes a promoted manifest body after `dropRef` but before the committed-removal `-1` is sealed; the fold then cannot read the body to compute edge deltas and pool-wide graduation stops."}
{"cluster_id": "chunk_004-207", "topic": "If `TrimGated` is enabled without `CursorInSnap`, separate `GCommitEdges` and `G", "canonical_claim": "If `TrimGated` is enabled without `CursorInSnap`, separate `GCommitEdges` and `GCommitCursor` steps can publish the durable cursor ahead of the edges it claims to cover, allowing a dangle."}
{"cluster_id": "chunk_004-208", "topic": "If `CursorInSnap` is enabled without `TrimGated`, a leader can trim past what an", "canonical_claim": "If `CursorInSnap` is enabled without `TrimGated`, a leader can trim past what any committed snap covers because the trim bound still consults an in-memory fold cursor."}
{"cluster_id": "chunk_004-209", "topic": "Advancing the janitor cursor from page `A` to `B` during a suppressed DEFER can ", "canonical_claim": "Advancing the janitor cursor from page `A` to `B` during a suppressed DEFER can form the fair `A-defer -> B-fold -> A` cycle, permanently skipping page `A` and violating `EventuallyDeadAReclaimed`."}
{"cluster_id": "chunk_004-210", "topic": "Removing the `deferCount < MaxDefer` guard permits a fair infinite DEFER sequenc", "canonical_claim": "Removing the `deferCount < MaxDefer` guard permits a fair infinite DEFER sequence in which nonempty `unfolded` deltas are never folded, violating `EventuallyFolded`."}
{"cluster_id": "chunk_004-211", "topic": "Graduating a blob without first checking that `unfolded` contains no pending del", "canonical_claim": "Graduating a blob without first checking that `unfolded` contains no pending delta for that blob can physically delete it after a protective `+1` lands after the fold cut, violating `NoOverDelete`."}
{"cluster_id": "chunk_004-212", "topic": "destructive gate based only on `proven == total` accepts an empty universe becau", "canonical_claim": "A destructive gate based only on `proven == total` accepts an empty universe because `0 == 0`; the gate therefore needs an explicit nonempty-authority predicate."}
{"cluster_id": "chunk_004-213", "topic": "single incomplete GC `LIST` page can permanently skip a durable receiver `+1`", "canonical_claim": "A single incomplete GC `LIST` page can permanently skip a durable receiver `+1`: the namespace cursor advances past the omitted transaction, the sender `-1` folds, and a deduplicated blob reaches physical deletion while a promoted manifest still references it."}
{"cluster_id": "chunk_004-214", "topic": "stale in-degree edges", "canonical_claim": "A `stale_edge` is a blob whose every source edge names a manifest that no longer exists; its positive in-degree is pinned forever, so incremental GC cannot reclaim it and only `ca-gc-rebuild` or an in-degree rebuild can clear the condition. One fsck report identified 12 unreferenced objects with exactly this condition."}
{"cluster_id": "chunk_004-215", "topic": "epoch-1 stale-edge root cause is a race in which a precommit `+1` folds, an allo", "canonical_claim": "The epoch-1 stale-edge root cause is a race in which a precommit `+1` folds, an allocation fault aborts the build, an orphan-manifest sweep deletes the now-unowned manifest without first decrementing its edges, and the folded `+1` remains permanently unreclaimable."}
{"cluster_id": "chunk_004-216", "topic": "precommit that records its blob closure lazily from a tree object can record an ", "canonical_claim": "A precommit that records its blob closure lazily from a tree object can record an empty closure after the tree disappears, causing a blob to leak forever."}
{"cluster_id": "chunk_004-217", "topic": "deposed leader's pre-CAS `clearSparedMeta` could leave stray `Clean` metadata ov", "canonical_claim": "A deposed leader's pre-CAS `clearSparedMeta` could leave stray `Clean` metadata over a `delete_pending` blob, allowing a writer to reuse the condemned exact token and a pending exact-token redelete to delete the live reuse; add-only GC freshness metadata fixed this INV_NO_LOSS risk."}
{"cluster_id": "chunk_004-218", "topic": "GC discovery no longer performs the roots-tree quadratic", "canonical_claim": "GC discovery no longer performs the roots-tree quadratic: it lists flat `cas/refs/` and uses streamed pagination with `start_after=cursor`."}
{"cluster_id": "chunk_004-219", "topic": "manifest cleanup budget durability", "canonical_claim": "A one-shot `gc_round_manifest_cleanup_budget` cap is unsafe: the ref-log cursor can consume an owner-removal entry in the same CAS as a cap-declined cleanup, making the manifest permanently unreachable; nominated-but-declined manifest bodies are not re-derived by later rounds, so durable retry is required before bounding this work, and an orphan-manifest sweep of about 100 objects per round cannot keep pace."}
{"cluster_id": "chunk_004-220", "topic": "Stage A residual missing-universe case is unsafe", "canonical_claim": "The Stage A residual missing-universe case is unsafe: an acknowledged hidden A:+1 in a namespace absent from both the hint and gc/state can coexist with visible B:-1, allowing the shared blob to reach zero and be deleted while A still owns it"}
{"cluster_id": "chunk_004-221", "topic": "plan lacks C++ coverage for SOURCE-BACKED/TOKENED adoption rematerialization, TO", "canonical_claim": "The plan lacks C++ coverage for SOURCE-BACKED/TOKENED adoption rematerialization, TOKENLESS receiver-plus-one durability before source release, and newly-condemned-not-deleted-in-the-same-round ordering"}
{"cluster_id": "chunk_004-222", "topic": "CasGc", "canonical_claim": "CasGc.cpp has a pre-suppression-gate handoff_reclaim wholesale generation-prefix delete in addition to prune, manifest-delete, and orphan-sweep paths"}
{"cluster_id": "chunk_004-223", "topic": "Stage B removal sequence omits the bounded suppression-aware best-effort cleanup", "canonical_claim": "The Stage B removal sequence omits the bounded suppression-aware best-effort cleanup pass required between terminal fold and _ckpt or catalog-entry deletion"}
{"cluster_id": "chunk_004-224", "topic": "late-plus-one delete-site test is insufficient when it only exercises behavior a", "canonical_claim": "A late-plus-one delete-site test is insufficient when it only exercises behavior already present in CasBlobInDegree::settleEntry and can pass for unrelated reasons"}
{"cluster_id": "chunk_004-225", "topic": "production path must not expose a mutable GcTestHooks", "canonical_claim": "The production path must not expose a mutable GcTestHooks::force_universe_authoritative hook or enable destructive work before Stage B installs removal and handle or pre-delete life and fence contracts"}
{"cluster_id": "chunk_004-226", "topic": "Stage B Task 1 still says RefNamespaceId", "canonical_claim": "Stage B Task 1 still says RefNamespaceId::stageATransition is deleted in Task 4 while Task 4 moves the zero-grep gate to Task 6, and no concrete compile-time-only test injection mechanism is specified"}
{"cluster_id": "chunk_004-227", "topic": "Stage A and Stage B plans remain inconsistent because Stage A retains kUniverseA", "canonical_claim": "The Stage A and Stage B plans remain inconsistent because Stage A retains kUniverseAuthoritative and GcTestHooks::force_universe_authoritative while Stage B preserves and flips kUniverseAuthoritative instead of changing UniversePolicy::kDefault"}
{"cluster_id": "chunk_004-228", "topic": "orphan-manifest sweep can delete a committed manifest when a hot LIST omits the ", "canonical_claim": "The orphan-manifest sweep can delete a committed manifest when a hot LIST omits the logs that grant its owner, because activeManifestKeys trusts only the incomplete reconstructed set"}
{"cluster_id": "chunk_004-229", "topic": "Snapshot cleanup can delete a recovery-seal chain anchor before durable_cursor r", "canonical_claim": "Snapshot cleanup can delete a recovery-seal chain anchor before durable_cursor reaches it because ordinary snapshot deletion is not gated by the cursor"}
{"cluster_id": "chunk_004-230", "topic": "namespace whose physical deletion remains Pending indefinitely causes every fold", "canonical_claim": "A namespace whose physical deletion remains Pending indefinitely causes every fold seal to carry and rescan an unbounded cleanup set"}
{"cluster_id": "chunk_004-231", "topic": "No current field binds a manifest owner grant to the ref-transaction cursor beca", "canonical_claim": "No current field binds a manifest owner grant to the ref-transaction cursor because build sequences and ref transaction sequences are independent and MountLease contains no ref cut"}
{"cluster_id": "chunk_004-232", "topic": "SYSTEM ", "canonical_claim": "SYSTEM ... GC REBUILD and fsck remain destructive or cleanliness-sensitive hot-LIST consumers: rebuild can condemn blobs from a wholesale-omitted namespace and fsck cannot know the namespace was omitted"}
{"cluster_id": "chunk_004-233", "topic": "Pinning every ordinary snapshot above a held cursor can create unbounded snapsho", "canonical_claim": "Pinning every ordinary snapshot above a held cursor can create unbounded snapshot storage, with each snapshot potentially reaching 64 MiB; only recovery-seal ids above the cursor need pinning"}
{"cluster_id": "chunk_004-234", "topic": "per-namespace hold is pool-wide because an unknown +1 may name any shared blob", "canonical_claim": "A per-namespace hold is pool-wide because an unknown +1 may name any shared blob; global destructive suppression prevents graduation, blob deletion, and ref cleanup for other namespaces too"}
{"cluster_id": "chunk_004-235", "topic": "ghost log V with prev=A can be folded before a later successor reveals recovery ", "canonical_claim": "A ghost log V with prev=A can be folded before a later successor reveals recovery seal S, and if the namespace stays quiet no anchor crossing occurs to detect the ghost"}
{"cluster_id": "chunk_004-236", "topic": "unresolved owner grant can land after its build is retired and min_active advanc", "canonical_claim": "An unresolved owner grant can land after its build is retired and min_active advances, so the premise that all grants below R* have settled is false unless R* means inclusive highest allocated or attempted ref id or unresolved builds remain active"}
{"cluster_id": "chunk_004-237", "topic": "member-wide highest-allocated R* can permanently retain a quiet namespace whose ", "canonical_claim": "A member-wide highest-allocated R* can permanently retain a quiet namespace whose chain ends far below another namespace's allocator high-water"}
{"cluster_id": "chunk_004-238", "topic": "REBUILD cannot prove all member leases are fenced by enumerating them through LI", "canonical_claim": "REBUILD cannot prove all member leases are fenced by enumerating them through LIST(gc/server-roots/) while the same hot-LIST trust boundary allows a live member to be omitted"}
{"cluster_id": "chunk_004-239", "topic": "Pending-cap check is not linearizable when writers append remove_namespace befor", "canonical_claim": "A Pending-cap check is not linearizable when writers append remove_namespace before GC creates Pending entries, and limiting Pending does not bound permanent lineage tombstones"}
{"cluster_id": "chunk_004-240", "topic": "proposed min_active acknowledgement does not contain the recovery generation", "canonical_claim": "The proposed min_active acknowledgement does not contain the recovery generation: GC can condemn a ghost, recovery can publish its pointer while down, and later rounds can delete before any pointer-aware fold occurs"}
{"cluster_id": "chunk_004-241", "topic": "Quarantine cannot be cleared by an empty or stale listing", "canonical_claim": "Quarantine cannot be cleared by an empty or stale listing; revalidation must exact-GET the persisted offender and anchor, reread pointer or generation, and carry quarantine through omission, 404, pointer change, or budget exhaustion"}
{"cluster_id": "chunk_004-242", "topic": "REBUILD can condemn a blob belonging to a live unowned manifest when manifest LI", "canonical_claim": "REBUILD can condemn a blob belonging to a live unowned manifest when manifest LIST omits that manifest but physical blob LIST includes the blob."}
{"cluster_id": "chunk_004-243", "topic": "maybeSweepStalePrecommits does not reclaim current-epoch manifest bodies, so rep", "canonical_claim": "maybeSweepStalePrecommits does not reclaim current-epoch manifest bodies, so repeatedly staging manifests and destroying builds without abandon can leak without bound during a long-lived mount."}
{"cluster_id": "chunk_004-244", "topic": "Normal orphan-manifest sweep cannot reclaim blobs from a deleted manifest when i", "canonical_claim": "Normal orphan-manifest sweep cannot reclaim blobs from a deleted manifest when it emits no blob deltas, because those blobs never transition to zero in-degree and later condemn-nothing REBUILD preserves the leak."}
{"cluster_id": "chunk_004-245", "topic": "v7 fold algorithm can delete a blob when namespace A has an acknowledged +1 at i", "canonical_claim": "The v7 fold algorithm can delete a blob when namespace A has an acknowledged +1 at its next exact slot but LIST hides all of A's new keys and the fold carries A's old cursor without probing it."}
{"cluster_id": "chunk_004-246", "topic": "hold recorded only as cursor C can be forgotten on a later round without a hint,", "canonical_claim": "A hold recorded only as cursor C can be forgotten on a later round without a hint, allowing a visible -1 in another namespace to delete a blob protected by an unaccounted acknowledged +1 behind the hold."}
{"cluster_id": "chunk_004-247", "topic": "Deleting orphan manifest M before its blob nominations are adopted can leave M u", "canonical_claim": "Deleting orphan manifest M before its blob nominations are adopted can leave M undiscoverable and its blobs without in-degree rows permanently."}
{"cluster_id": "chunk_004-248", "topic": "Forced REBUILD can discard a durable classification-4 hold and later certify the", "canonical_claim": "Forced REBUILD can discard a durable classification-4 hold and later certify the broken namespace as quiet, allowing a visible B:-1 to delete a blob still named by A:C+2."}
{"cluster_id": "chunk_004-249", "topic": "After process termination following nomination adoption, no durable manifest-del", "canonical_claim": "After process termination following nomination adoption, no durable manifest-delete debt is specified, so an incomplete LIST can omit the manifest forever; this causes a manifest leak rather than blob data loss."}
{"cluster_id": "chunk_004-250", "topic": "pool-wide-held baseline for a missing or undecodable prior fold seal is not repr", "canonical_claim": "A pool-wide-held baseline for a missing or undecodable prior fold seal is not representable by per-namespace ShardCoverage and has no defined operator-clear authority."}
{"cluster_id": "chunk_004-251", "topic": "GC anomaly Probe A recorded durable ref logs `0000000000000001-000000000001430c`", "canonical_claim": "GC anomaly Probe A recorded durable ref logs `0000000000000001-000000000001430c` and `0000000000000001-000000000001430d` as missing from the pre-fold scan while each had `head='present'`, `pre_max='0000000000000001-000000000001430e'`, `fold_max='0000000000000001-0000000000014a10'`, and `row_cap='32'`."}
{"cluster_id": "chunk_004-252", "topic": "Interrupted multipart uploads and lost-ACK `stageManifest` operations can leave ", "canonical_claim": "Interrupted multipart uploads and lost-ACK `stageManifest` operations can leave storage debris that depends on the orphan-manifest sweep and an S3 `AbortIncompleteMultipartUpload` lifecycle rule for reclamation."}
{"cluster_id": "chunk_004-253", "topic": "pool-wide clamp suppression", "canonical_claim": "A persistent clamp on one GC shard leaves `suppress_destructive` set for every pass and halts blob and manifest deletion and all GC reclamation across the entire pool, not just for the affected shard; the fail-closed behavior is retained, with recommended observability and an operator escape including clamp age, clamped key/shard, alerting, and a documented fsck/rebuild path, while scoped suppression is deferred."}
{"cluster_id": "chunk_004-254", "topic": "Before the P3-B1 fix, a new GC leader set `i_am_leader` only after `runRoundLogg", "canonical_claim": "Before the P3-B1 fix, a new GC leader set `i_am_leader` only after `runRoundLogged` returned, leaving its first long fold without heartbeat protection and allowing deterministic lease stealing."}
{"cluster_id": "chunk_004-255", "topic": "historical erasure-proof model found two GC-side windows in which a fresh schedu", "canonical_claim": "The historical erasure-proof model found two GC-side windows in which a fresh scheduler can create `gc/state` or `heartbeatLoop` can pulse `gc/hb` between the observer's final `LIST` and its in-flight check."}
{"cluster_id": "chunk_004-256", "topic": "reducer drops a remove that arrives before its matching add as an unmatched no-o", "canonical_claim": "The reducer drops a remove that arrives before its matching add as an unmatched no-op; if the later add survives, it can create a permanent retention leak."}
{"cluster_id": "chunk_004-257", "topic": "dropRefBestEffort swallows rollback failure with no logging", "canonical_claim": "dropRefBestEffort swallows rollback failure with no logging: a correlated backend outage during partial-commit rollback can leave a permanently-live phantom ref, since GC only reclaims unreferenced objects and won't clean it up. Proposed fix: add tryLogCurrentException (CachedPartFolderAccess.cpp)."}
{"cluster_id": "chunk_004-258", "topic": "`ObjectStorageBackend", "canonical_claim": "`ObjectStorageBackend::list` in `EmulatedSingleProcess` mode fills `lk.token` from `child->metadata->etag` (`CasObjectStorageBackend.cpp:781-783`) while emulated `head`/`get`/`putOverwrite`/`deleteExact` all use `emuObserveToken`; a consumer that reuses `listed.token` for `deleteExact` (the orphan-manifest sweep, `CasOrphanManifestSweep.cpp:300`) can get a token mismatch and leave GC debris undeleted in that test-only mode. Production `Native` S3 is unaffected because both `list` and `head` return the real S3 ETag."}
{"cluster_id": "chunk_004-259", "topic": "GC registered defect R2", "canonical_claim": "GC registered defect R2: `PartWriteTxn`'s destructor unconditionally retired its build sequence, advancing the GC floor while the precommit grant's outcome was still uncertain, making an uncertain owner grant look dead; the fix is a duty queue with the failure direction inverted (settle then retire then pop), where any throw retains the duty and a full queue pins the floor, since an advanced floor is an irreversible authorization while a pinned one only costs latency."}
{"cluster_id": "chunk_004-260", "topic": "rev", "canonical_claim": "The rev.8 disk-lifecycle whole-increment final review found two Important pre-release defects, both TOCTOU races around the `gc_scheduler_mutex`: (I-1) a manual `SYSTEM CONTENT ADDRESSED GC RUN` admitted while the pool was `Live` could queue behind FORGET's `gc_scheduler_mutex` and, once granted, resume and run a full GC round against a since-decommissioned (`Vanished`) pool; (I-2) `runGcRebuildNow` did not take `gc_scheduler_mutex` at all, making a GC rebuild invisible to FORGET and able to durably write after FORGET believed all GC threads were stopped. Both were fixed with a lock-then-gate pattern (matching `gcStart`) plus RED-demo regression tests."}
{"cluster_id": "chunk_004-261", "topic": "pre-release v3 data-loss hole was found while modeling the deposed-leader path i", "canonical_claim": "A pre-release v3 data-loss hole was found while modeling the deposed-leader path in TLA+: a deposed GC leader's pre-CAS `clearSparedMeta` call leaves stray-Clean freshness metadata over a `delete_pending` blob; a writer can then reuse the condemned blob's exact token, and the pending exact-token redelete for that token deletes the live reuse, violating `INV_NO_LOSS`. The fix made GC freshness metadata add-only (never `Condemned`->`Clean` on a spare leader; only a writer holding a fresh incarnation token may publish `Clean`), verified by a TLA+ witness plus 5 sabotage configurations including a `post_adoption_clear` case that produces an authentic 16-state 2-leader counterexample against the naive fix."}
{"cluster_id": "chunk_004-262", "topic": "post-soak leak-rca investigation verdict", "canonical_claim": "A post-soak leak-rca investigation verdict: the T8v2 soak's 169,837 vs 79 reachable/unreachable checkpoint reading was NOT a leak but an AwaitingGc fold backlog mid-churn that drained to 14 by run end; the real bug was the tripwire formula, fixed by moving the strict check to final-converge (mid-run track-only)."}
{"cluster_id": "chunk_004-263", "topic": "Opt-round §4 review Minor 2 (pre-existing, not introduced by the round)", "canonical_claim": "Opt-round §4 review Minor 2 (pre-existing, not introduced by the round): `CasBlobAdoptTrusted` increment and the manifest-trust audit-emit sit inside promote's `appendRefOps` closure, which `mutateShard` may re-invoke on a CAS retry, causing a possible counter over-count or duplicate manifest-trust event under contention; no correctness impact, same site/class as the older copy-forward increment."}
{"cluster_id": "chunk_004-264", "topic": "production regression where a stateless test (`05008`) went red was traced to `U", "canonical_claim": "A production regression where a stateless test (`05008`) went red was traced to `UniversePolicy::kDefault == StageA_Suppressed`, which suppresses destructive GC on every production round; the introducing commit swept 20+ gtest files and touched zero files under `tests/queries/`, so its stateless-test blast radius was invisible to its own test run."}
{"cluster_id": "chunk_004-265", "topic": "In the CA transient-classifier audit, `CasBlobInDegree", "canonical_claim": "In the CA transient-classifier audit, `CasBlobInDegree.cpp:289` versus `pruneSupersededGenerations` (`CasGc.cpp:3217`, called from `:802`) has a plausible residual window where a deposed leader mid-read of a superseded generation could see benign absence misreported as `CORRUPTED_DATA`; it is GC-plane and contained, but worth confirming impossible."}
{"cluster_id": "chunk_004-266", "topic": "P3-B1 root cause was a pre-existing GC lease/fold livelock, not a Phase-3 regres", "canonical_claim": "P3-B1 root cause was a pre-existing GC lease/fold livelock, not a Phase-3 regression: a large deletion pipeline makes each fold take minutes, the GC lease expires mid-fold, the other node legitimately adopts leadership, the first node's round CAS then aborts (`gc/state moved`) after its pre-CAS redeletes already executed, the pipeline never shrinks, and the cycle repeats as sustained dual-leader round fights; safety held throughout (dangling=0, unaccounted=0)."}
{"cluster_id": "chunk_004-267", "topic": "C1(b) fix was not addressed as originally stated", "canonical_claim": "The C1(b) fix was not addressed as originally stated: the frontier-completion predicate is byte-identical and the commit only changed the numerator's source; the equivalent guarantee holds only as an implicit invariant across three separate sites rather than the single explicit predicate that was requested, and this gap is exactly what NEW-1's incarnation-mismatch route exploits."}
{"cluster_id": "chunk_004-268", "topic": "D4 (structural duplication)", "canonical_claim": "D4 (structural duplication): delete-outcome classification (Deleted/NotFound/TokenMismatch/marker) is reimplemented with local accounting across GC's R3 phase, manifest cleanup, `cleanupRefObjects`, `runNamespaceCleanupPasses`, the orphan sweep, and the staging sweeper — same tolerated-outcome set, per-site counters/events, a semantic-drift risk though not yet a bug."}
{"cluster_id": "chunk_004-269", "topic": "D5 (structural duplication)", "canonical_claim": "D5 (structural duplication): manual LIST/cursor pagination loops are independently implemented in GC fold, `changedShardCount`, the orphan sweep, fsck, `listMounts`, and `recoverRefTable`."}
{"cluster_id": "chunk_004-270", "topic": "D6", "canonical_claim": "D6: GC (LIST `cas/refs/`), fsck (refs plus raw-listing diff), the orphan sweep (manifest paging plus watermark), and the frontend's `listNamespaces` (refs union roots) each discover the namespace/universe differently because each answers a different question (live set vs physical set vs eligibility); the definitions are not centrally owned, so fsck, GC, and the sweep can disagree — each grew independently in a different GC era."}
{"cluster_id": "chunk_004-271", "topic": "Risk register #7 (Medium, confidence Med)", "canonical_claim": "Risk register #7 (Medium, confidence Med): `CasGc.cpp`'s `suppress_destructive` flag is computed twice from the same mutable `report.anomalies` (at `:635` and `:1149`), with consistency between the two computations implicit rather than enforced; a future edit could make fold suppress deletes while the round itself doesn't (or vice versa), an over-delete class. Suggested action: compute it once in `fold` and thread it through `FoldResult`."}
{"cluster_id": "chunk_004-272", "topic": "Risk register #10 (Medium, confirmed)", "canonical_claim": "Risk register #10 (Medium, confirmed): `ContentAddressedMetadataStorage.cpp:203,316` lazily creates `gc_scheduler` without a mutex; two concurrent `SYSTEM ... GC` statements could race the `make_unique` call, producing a torn scheduler pointer if not otherwise serialized upstream. Suggested action: confirm interpreter serialization, or add a mutex."}
{"cluster_id": "chunk_004-273", "topic": "Under the RemovalReady proposal, a corrupt/unvalidatable terminal removal record", "canonical_claim": "Under the RemovalReady proposal, a corrupt/unvalidatable terminal removal record leaves the cleanup item permanently `Pending` (fail-closed) so the name is never recreatable until FORCE REBUILD or decommission — a deliberate posture flip from the old design's leak-only cleanup-failure behavior, but the review flags it as an unowned posture change needing an explicit unwedge path and operator-visible surfacing."}
{"cluster_id": "chunk_004-274", "topic": "High #1 was a GC regression where retire/preview hardcoded shard 0, causing shar", "canonical_claim": "High #1 was a GC regression where retire/preview hardcoded shard 0, causing shard-1..N blobs to leak; fixed in commit `5f5fa5f7906`."}
{"cluster_id": "chunk_004-275", "topic": "CLAMP-SUPPRESSION was the 2026-07-03 night-soak bug where 31 dangling objects ac", "canonical_claim": "CLAMP-SUPPRESSION was the 2026-07-03 night-soak bug where 31 dangling objects accumulated because a clamped fold cursor suppressed graduations and deletes pool-wide even though only one table was lagging."}
{"cluster_id": "chunk_004-276", "topic": "M1 requires the cross-round fold cursor to survive trim, otherwise a double-coun", "canonical_claim": "M1 requires the cross-round fold cursor to survive trim, otherwise a double-count over-pin/leak occurs."}
{"cluster_id": "chunk_004-277", "topic": "Blocking issue", "canonical_claim": "Blocking issue: cursor pruning is not monotone across crash-resume — if the process stops after the pruned seal is durable but before `_ckpt`/entry deletion, the catalog still shows `Removing`, and a later GC round adds every `Live`/`Removing` entry back to `walk_targets` and writes a cursor for it, reintroducing the pruned cursor; the resumed removal driver then has no safe-and-live choice between trusting the historical pruned seal or waiting on the current seal indefinitely."}
{"cluster_id": "chunk_004-278", "topic": "rewritten Task 5's claim that stale-resume re-derivation is unconstructible cont", "canonical_claim": "The rewritten Task 5's claim that stale-resume re-derivation is unconstructible contradicts both the spec's mandated capture-at-deposition rule and the existing TLA+ model `CaRefNsCleanupStaleLeaderCore.tla`, which constructs the exact interleaving (capture, stall, removal retires and deletes the entry, name is reborn, stale leader resumes and re-derives, targeting the live incarnation and violating `NoLiveDataDeleted`)."}
{"cluster_id": "chunk_004-279", "topic": "`clearSparedMeta` (`Core/CasGc", "canonical_claim": "`clearSparedMeta` (`Core/CasGc.cpp:106-113`) clears a blob's freshness meta from Condemned to Clean when a GC round spares it, scheduled on the spare verdict and completed BEFORE the round's single `gc/state` CAS; a GC leader that runs it and then loses the CAS leaves a durable stray-Clean meta over a blob still `delete_pending` in the durably-adopted run. Stray-Clean is the unsafe direction (unlike stray-Condemned, which only costs an unnecessary resurrect) because it defeats the writer's freshness gate, letting a writer reuse the exact condemned incarnation token; a later pending exact-token redelete then deletes the live reused incarnation."}
{"cluster_id": "chunk_004-280", "topic": "regular GC round folds ref logs into source-edge in-degree runs, performs pre-CA", "canonical_claim": "A regular GC round folds ref logs into source-edge in-degree runs, performs pre-CAS exact-token deletes of prior delete_pending entries, commits the round with one gc/state CAS, and performs cleanup after that CAS."}
{"cluster_id": "chunk_004-281", "topic": "Physical deletion requires a durable retire entry, fences at all root shards, a ", "canonical_claim": "Physical deletion requires a durable retire entry, fences at all root shards, a fold-through-fence zero-reachability recheck, and the exact observed token (INV-NO-LOSS)."}
{"cluster_id": "chunk_004-282", "topic": "GC is leader-paced and one-pass", "canonical_claim": "GC is leader-paced and one-pass: a stale leader may duplicate work but cannot roll back or mis-delete because operations are idempotent, monotone, or exact-token guarded."}
{"cluster_id": "chunk_004-283", "topic": "condemned blob has zero in-degree, an exact incarnation token observed by GC, a ", "canonical_claim": "A condemned blob has zero in-degree, an exact incarnation token observed by GC, a kCondemned sentinel row with source_id = 0, and Condemned per-blob metadata while its body may still exist."}
{"cluster_id": "chunk_004-284", "topic": "GC fold uses a three-cursor streaming merge of prior edges, kCondemned rows, and", "canonical_claim": "The GC fold uses a three-cursor streaming merge of prior edges, kCondemned rows, and current deltas, emitting at most one condemned sentinel per blob."}
{"cluster_id": "chunk_004-285", "topic": "crashed GC pass leaves only unadopted attempt-scoped artifacts", "canonical_claim": "A crashed GC pass leaves only unadopted attempt-scoped artifacts; there is no crash-resume step, and later pruning reclaims those artifacts."}
{"cluster_id": "chunk_004-286", "topic": "fold barrier treats a create-precommit as non-activating until its manifest body", "canonical_claim": "The fold barrier treats a create-precommit as non-activating until its manifest body is present; once present, manifest expansion contributes the activating +1 blob edges."}
{"cluster_id": "chunk_004-287", "topic": "GC subsystem is the only component authorized to delete content-addressed object", "canonical_claim": "The GC subsystem is the only component authorized to delete content-addressed objects."}
{"cluster_id": "chunk_004-288", "topic": "follower may steal the GC lease only after observing the incumbent `(owner, seq)", "canonical_claim": "A follower may steal the GC lease only after observing the incumbent `(owner, seq)` unchanged across the observation window and seeing no advancing heartbeat pulse."}
{"cluster_id": "chunk_004-289", "topic": "If `gc/hb` is absent or belongs to a different owner, the lease protocol falls b", "canonical_claim": "If `gc/hb` is absent or belongs to a different owner, the lease protocol falls back to observing `gc/state.seq`."}
{"cluster_id": "chunk_004-290", "topic": "authoritative pool-wide GC shard count is persisted in generation-8 `_pool_meta`", "canonical_claim": "The authoritative pool-wide GC shard count is persisted in generation-8 `_pool_meta`; `gc/state.gc_shards` mirrors it after GC state exists."}
{"cluster_id": "chunk_004-291", "topic": "GC discovery enumerates the live root-shard universe with `LIST(cas/refs/)`, and", "canonical_claim": "GC discovery enumerates the live root-shard universe with `LIST(cas/refs/)`, and this LIST sweep is the only universe-proportional operation in a regular round."}
{"cluster_id": "chunk_004-292", "topic": "GC fold processes only the root-shard journal window `(folded_cursor, current_sh", "canonical_claim": "A GC fold processes only the root-shard journal window `(folded_cursor, current_shard_version]`."}
{"cluster_id": "chunk_004-293", "topic": "GC must not advance a fold cursor past a live precommit whose manifest body is a", "canonical_claim": "GC must not advance a fold cursor past a live precommit whose manifest body is absent or fails `RefMatchesBody` or `ManifestNamespaceMatches` validation."}
{"cluster_id": "chunk_004-294", "topic": "Attempt-scoped GC artifacts are written under `gc/gen/<G_f>/attempt/<a>/` before", "canonical_claim": "Attempt-scoped GC artifacts are written under `gc/gen/<G_f>/attempt/<a>/` before adoption, and an unadopted attempt is invisible to readers."}
{"cluster_id": "chunk_004-295", "topic": "single `gc/state` CAS atomically publishes the adopted generation and attempt, f", "canonical_claim": "The single `gc/state` CAS atomically publishes the adopted generation and attempt, folded cursors, retired references, round, and prune cursor."}
{"cluster_id": "chunk_004-296", "topic": "When a GC-shard delta bucket is empty, the fold carries the parent generation's ", "canonical_claim": "When a GC-shard delta bucket is empty, the fold carries the parent generation's `RunRef` without run I/O; zero run I/O requires both the delta bucket and retired state to be empty."}
{"cluster_id": "chunk_004-297", "topic": "source-edge in-degree", "canonical_claim": "The persisted in-degree authority is the set of active source edges identified by `(ManifestId, path)`, with edge IDs computed by `sourceEdgeId(ManifestId, path)`; `in_degree(blob)` is the cardinality of that active source-edge set during a round and is not persisted as a separate counter."}
{"cluster_id": "chunk_004-298", "topic": "Only blobs with an explicit zero-transition marker in the current generation are", "canonical_claim": "Only blobs with an explicit zero-transition marker in the current generation are GC candidates; a blob never observed as a reference target is not a candidate."}
{"cluster_id": "chunk_004-299", "topic": "If a blob is already absent at condemnation time, GC records no candidate and ne", "canonical_claim": "If a blob is already absent at condemnation time, GC records no candidate and never fabricates a delete token from a 404."}
{"cluster_id": "chunk_004-300", "topic": "two-phase blob deletion", "canonical_claim": "Physical deletion is two-phase: a zero-in-degree or floor-eligible condemned entry is first published as `delete_pending` after surviving one full GC round, and the physical `deleteExact` runs only in a later pass for an entry pending in the previous published list; graduation requires confirmed `Condemned` evidence for the exact `(hash, token)`, recovery spares the entry, and a token mismatch leaves a newer blob untouched."}
{"cluster_id": "chunk_004-301", "topic": "Generation pruning skips generations still referenced by the adopted seal, while", "canonical_claim": "Generation pruning skips generations still referenced by the adopted seal, while a later parent-reference hand-off deletes the entire superseded generation prefix after the CAS."}
{"cluster_id": "chunk_004-302", "topic": "round uses `a = lease", "canonical_claim": "A round uses `a = lease.seq` as its attempt identifier, so competing leaders write to distinct attempt prefixes and only the lease-token `gc/state` CAS can adopt one attempt."}
{"cluster_id": "chunk_004-303", "topic": "Patch manifests are first-class reachability roots and keep both their own delta", "canonical_claim": "Patch manifests are first-class reachability roots and keep both their own delta blobs and referenced base-part blobs alive while active."}
{"cluster_id": "chunk_004-304", "topic": "incarnation-keyed fold cursors", "canonical_claim": "For a fixed namespace and shard, every recreated root shard has a strictly greater `incarnation`, and the D1 fold cursor is keyed by `(ns, shard, incarnation)`, so a recreated shard cannot match an old sealed cursor and is folded from `folded_cursor = 0`."}
{"cluster_id": "chunk_004-305", "topic": "`dropNamespace` appends a tombstone as its last journal event, and GC may reclai", "canonical_claim": "`dropNamespace` appends a tombstone as its last journal event, and GC may reclaim the shard object only after the tombstone and all prior journal events are folded; a token change from a revive prevents deletion."}
{"cluster_id": "chunk_004-306", "topic": "`putDeterministicArtifact(Backend&, key, bytes)` is limited to GC-internal deter", "canonical_claim": "`putDeterministicArtifact(Backend&, key, bytes)` is limited to GC-internal deterministic artifacts, and a pre-existing key is adopted only when its bytes are equal; divergent bytes cause `CORRUPTED_DATA`."}
{"cluster_id": "chunk_004-307", "topic": "`supportsListTokens()` is true only when LIST returns per-key incarnation tokens", "canonical_claim": "`supportsListTokens()` is true only when LIST returns per-key incarnation tokens; without it GC must GET every root-shard body."}
{"cluster_id": "chunk_004-308", "topic": "`gc-snap` remains a versioned binary-plus-zstd GC-internal format rather than be", "canonical_claim": "`gc-snap` remains a versioned binary-plus-zstd GC-internal format rather than being converted to protobuf."}
{"cluster_id": "chunk_004-309", "topic": "default test fixture uses one root shard, `gc_trim_min_events=0`, and `gc_fold_m", "canonical_claim": "The default test fixture uses one root shard, `gc_trim_min_events=0`, and `gc_fold_max_defer_rounds=8`."}
{"cluster_id": "chunk_004-310", "topic": "integration test `test_cas_gc_sharded` exercises disjoint-shard GC with `gc_shar", "canonical_claim": "The integration test `test_cas_gc_sharded` exercises disjoint-shard GC with `gc_shards=2` and `blobShard = high64(hash) % 2`."}
{"cluster_id": "chunk_004-311", "topic": "`CaGcLeaseCore", "canonical_claim": "`CaGcLeaseCore.tla` proves atomic lease-steal isolation and no false steal of an alive mid-round leader when the advisory heartbeat is enabled."}
{"cluster_id": "chunk_004-312", "topic": "incarnation registry-removal model proves that a durable per-shard never-reused ", "canonical_claim": "The incarnation registry-removal model proves that a durable per-shard never-reused incarnation and a newborn shard self-floor at the current GC round are both required; neither coordinate alone suffices."}
{"cluster_id": "chunk_004-313", "topic": "shard-incarnation model requires reclaim to wait until the shard journal, includ", "canonical_claim": "The shard-incarnation model requires reclaim to wait until the shard journal, including its tombstone, is fully folded."}
{"cluster_id": "chunk_004-314", "topic": "live GC-round graduation predicate is `condemn_round < current_round`, with `sup", "canonical_claim": "The live GC-round graduation predicate is `condemn_round < current_round`, with `suppress_destructive` independently blocking graduation on a clamped pass."}
{"cluster_id": "chunk_004-315", "topic": "`CaGcRoundDeferCore", "canonical_claim": "`CaGcRoundDeferCore.tla` permits a non-destructive round to defer rebuilding the in-degree snapshot, but a due graduation must force-fold before deleting and deferral must be bounded."}
{"cluster_id": "chunk_004-316", "topic": "disaster-recovery rebuild starts with an empty retired list, over-protects until", "canonical_claim": "A disaster-recovery rebuild starts with an empty retired list, over-protects until the normal condemnation pipeline catches up, and mints its round above every surviving mount acknowledgement."}
{"cluster_id": "chunk_004-317", "topic": "current regular GC round has request complexity O(delta) plus O(servers) and one", "canonical_claim": "The current regular GC round has request complexity O(delta) plus O(servers) and one `LIST` sweep, without an O(universe) GET or PUT phase."}
{"cluster_id": "chunk_004-318", "topic": "If a listed shard token matches the sealed folded token, GC skips the shard body", "canonical_claim": "If a listed shard token matches the sealed folded token, GC skips the shard body `GET`; clamped-coverage shards are never skipped."}
{"cluster_id": "chunk_004-319", "topic": "newly condemned candidate receives one `HEAD` to capture its exact token, while ", "canonical_claim": "A newly condemned candidate receives one `HEAD` to capture its exact token, while an already-condemned entry is re-verified from the fold and is not re-HEADed."}
{"cluster_id": "chunk_004-320", "topic": "Snapshot-run consumers use one `HEAD`, one ranged tail `GET`, and one streaming ", "canonical_claim": "Snapshot-run consumers use one `HEAD`, one ranged tail `GET`, and one streaming body `GET`; a large footer may add one exact-footer ranged `GET`."}
{"cluster_id": "chunk_004-321", "topic": "Snapshot-run streaming uses O(block) resident memory with a maximum current bloc", "canonical_claim": "Snapshot-run streaming uses O(block) resident memory with a maximum current block size of 1 MiB, independent of run size."}
{"cluster_id": "chunk_004-322", "topic": "fully idle GC round with no journal changes and no retired entries performs zero", "canonical_claim": "A fully idle GC round with no journal changes and no retired entries performs zero run-object GETs and PUTs."}
{"cluster_id": "chunk_004-323", "topic": "GC rebuild reconstructs cursors and edges but reclaims nothing", "canonical_claim": "A GC rebuild reconstructs cursors and edges but reclaims nothing; blobs not named by a surviving manifest are retained as `unaccounted`."}
{"cluster_id": "chunk_004-324", "topic": "reclaim test must advance the ack between rounds because the deletion pipeline i", "canonical_claim": "A reclaim test must advance the ack between rounds because the deletion pipeline is condemn at K, `delete_pending` at K+1, and physical delete at K+2 when all acks are current."}
{"cluster_id": "chunk_004-325", "topic": "Part-manifest cleanup must remain edge-driven by in-degree rather than namespace", "canonical_claim": "Part-manifest cleanup must remain edge-driven by in-degree rather than namespace-prefix ownership because shadow snapshots can reference manifests under dropped table prefixes."}
{"cluster_id": "chunk_004-326", "topic": "blob is live if a live manifest names it, and a manifest is live if a committed ", "canonical_claim": "A blob is live if a live manifest names it, and a manifest is live if a committed or precommitted ref names it."}
{"cluster_id": "chunk_004-327", "topic": "CAS has exactly one blob-body deletion site", "canonical_claim": "CAS has exactly one blob-body deletion site: the pre-CAS `pending_deletes` phase of a GC round, limited to entries marked `delete_pending` by a previously published round."}
{"cluster_id": "chunk_004-328", "topic": "GC never invents a ref transition", "canonical_claim": "GC never invents a ref transition; abandoned precommit cleanup is performed by the writer or its successor rather than by a synthetic GC event."}
{"cluster_id": "chunk_004-329", "topic": "GC condemnation marker is add-only", "canonical_claim": "The GC condemnation marker is add-only: GC may change `Clean` to `Condemned` and delete the marker after a confirmed body delete, but never changes `Condemned` back to `Clean`."}
{"cluster_id": "chunk_004-330", "topic": "blob `", "canonical_claim": "A blob `.meta` sidecar has only `Clean` and `Condemned` states; an absent sidecar reads as `Clean`, and the sidecar contains no token."}
{"cluster_id": "chunk_004-331", "topic": "pre-precommit orphan manifest has no ref-log owner edge and is reclaimed by the ", "canonical_claim": "A pre-precommit orphan manifest has no ref-log owner edge and is reclaimed by the orphan sweep, whereas a dangling precommit must first have its binding removed before GC can reclaim the body."}
{"cluster_id": "chunk_004-332", "topic": "Blob liveness is represented by a set of source edges", "canonical_claim": "Blob liveness is represented by a set of source edges; `precommitAdd` creates `+1` edges, owner removals create `-1` edges, `promote` creates no edge, and inline manifest entries create none."}
{"cluster_id": "chunk_004-333", "topic": "GC discovers the ref live set by listing `cas/ns/stream/`, joining opaque life i", "canonical_claim": "GC discovers the ref live set by listing `cas/ns/stream/`, joining opaque life ids to one catalog cut, and folding each admitted log only after its complete coverage is durable."}
{"cluster_id": "chunk_004-334", "topic": "implemented GC round is a single pass of 18 named phases ending in exactly one `", "canonical_claim": "The implemented GC round is a single pass of 18 named phases ending in exactly one `gc/state` CAS; the older `fold → retire → fence → recheck → trim` shape is superseded."}
{"cluster_id": "chunk_004-335", "topic": "`gc/state` is the durable round-adoption state and exactly one CAS per round pub", "canonical_claim": "`gc/state` is the durable round-adoption state and exactly one CAS per round publishes it; the fold seal records generation, parent generation, catalog-life coverage, source-edge runs, and condemned summaries."}
{"cluster_id": "chunk_004-336", "topic": "Condemned entries are stored as sentinel rows in the source-edge run", "canonical_claim": "Condemned entries are stored as sentinel rows in the source-edge run; there is no separate retired-list object and no run-file list outside the fold seal."}
{"cluster_id": "chunk_004-337", "topic": "blob candidate is HEAD-observed with an exact token when its edge set becomes em", "canonical_claim": "A blob candidate is HEAD-observed with an exact token when its edge set becomes empty; a carried candidate pays no HEAD, and missing bodies are non-throwing while present-but-invalid bodies are `CORRUPTED_DATA`."}
{"cluster_id": "chunk_004-338", "topic": "GC coordinator owns discovery, round visibility, the global fence, and generatio", "canonical_claim": "The GC coordinator owns discovery, round visibility, the global fence, and generation advancement; reducers own disjoint digest shards, and pure-carry shards copy parent run references with zero run I/O."}
{"cluster_id": "chunk_004-339", "topic": "planned incarnation-aware fold treats a sealed/live incarnation mismatch as an a", "canonical_claim": "The planned incarnation-aware fold treats a sealed/live incarnation mismatch as an absent prior cursor, restarts at `folded_cursor = 0`, drops carried edges from the old incarnation, and stamps the live incarnation into the new seal."}
{"cluster_id": "chunk_004-340", "topic": "planned ref-shard reclaim condition is empty refs, a last journal tombstone, com", "canonical_claim": "The planned ref-shard reclaim condition is empty refs, a last journal tombstone, complete folding past the fence at the current incarnation, and token-guarded `deleteExact`."}
{"cluster_id": "chunk_004-341", "topic": "Before D1, `discoverUniverse` enumerated `registry", "canonical_claim": "Before D1, `discoverUniverse` enumerated `registry.namespaces × shardsToVisit`, so GC fencing cost was proportional to namespaces ever created times `root_shards` and could mint fence-only manifests for absent shards."}
{"cluster_id": "chunk_004-342", "topic": "abandoned newborn is treated as an abandoned precommit and reclaimed by the exis", "canonical_claim": "An abandoned newborn is treated as an abandoned precommit and reclaimed by the existing `(writer_epoch, build_sequence)` watermark path; after it empties, tombstone reclaim handles its shard object."}
{"cluster_id": "chunk_004-343", "topic": "Successful copy-forward emits `CasEventType", "canonical_claim": "Successful copy-forward emits `CasEventType::BlobCopyForward` and increments `CasBlobCopyForward`; the old `(hash, t)` entry remains listed until exact-token settlement, while the new incarnation is protected from wrong-token deletion."}
{"cluster_id": "chunk_004-344", "topic": "GC min_ack semantics", "canonical_claim": "An ack-floor entry graduates only when `condemn_round < min_ack`, where `min_ack` is the minimum `observed_gc_round` among live heartbeats or live/expired-but-not-fenced heartbeats; terminated and fenced heartbeats are excluded, and if no live heartbeats exist then `min_ack = +∞`."}
{"cluster_id": "chunk_004-345", "topic": "ack-floor open ordering is claim heartbeat → load `gc/state` and the retired lis", "canonical_claim": "The ack-floor open ordering is claim heartbeat → load `gc/state` and the retired list → stamp the acknowledgement → enable mutations, and retired-list objects must be durable before the `gc/state` CAS publishes the round."}
{"cluster_id": "chunk_004-346", "topic": "three-cursor merge graduates entries with `condemn_round < min_ack`, keeps entri", "canonical_claim": "The three-cursor merge graduates entries with `condemn_round < min_ack`, keeps entries at `condemn_round == min_ack`, spares entries whose in-degree recovers, captures a token for new zero-in-degree condemnations, and skips absent blobs."}
{"cluster_id": "chunk_004-347", "topic": "current retired-candidate set is stored as per-GC-shard artifacts using the stri", "canonical_claim": "The current retired-candidate set is stored as per-GC-shard artifacts using the strict `cas_retired_set` protobuf codec (`RetiredSetProto`, magic `CART`), with entries sorted by `(kind, hash)` and each entry containing `(blob_hash, token, condemn_round)`."}
{"cluster_id": "chunk_004-348", "topic": "retired-list and `condemn_round` protobuf fields are additive fields in the same", "canonical_claim": "The retired-list and `condemn_round` protobuf fields are additive fields in the same compatibility regime as `snap_attempt`; no per-object version integer is bumped, and future `compatibility_version` values fail closed."}
{"cluster_id": "chunk_004-349", "topic": "Discovery performs one paginated LIST of `cas/refs/` and ETag-diffs it against p", "canonical_claim": "Discovery performs one paginated LIST of `cas/refs/` and ETag-diffs it against per-shard folded tokens; backends with `supportsListTokens = false` use per-shard HEAD/GET and remain fail-closed and read-only."}
{"cluster_id": "chunk_004-350", "topic": "GC merge processes the prior snapshot run, sorted edge deltas, and prior retired", "canonical_claim": "The GC merge processes the prior snapshot run, sorted edge deltas, and prior retired run with three cursors sorted by `blob_hash`, emitting the new snapshot run, retired run, and delete list in one streaming pass."}
{"cluster_id": "chunk_004-351", "topic": "retired candidate with recovered in-degree greater than zero is spared and remov", "canonical_claim": "A retired candidate with recovered in-degree greater than zero is spared and removed from the retired output; the implementation emits a B170 recheck-verdict event with the recovered in-degree."}
{"cluster_id": "chunk_004-352", "topic": "retired candidate with zero in-degree graduates only when `condemn_round < min_a", "canonical_claim": "A retired candidate with zero in-degree graduates only when `condemn_round < min_ack`; a zero-in-degree candidate with `condemn_round >= min_ack` remains in the retired list."}
{"cluster_id": "chunk_005-001", "topic": "A zero-in-degree candidate not already retired is condemned by HEADing i", "canonical_claim": "A zero-in-degree candidate not already retired is condemned by HEADing its blob, capturing its current token, and appending `(hash, token, K)` to the retired output; an absent blob is skipped."}
{"cluster_id": "chunk_005-002", "topic": "run-file-reader-streaming-bounds", "canonical_claim": "`RunFileReader` has borrowed-memory and streaming modes; streaming mode retains only the footer index and one current block no larger than `kRunHardCapBlockSize` = 1 MiB, while `seek` uses a ranged GET for the target block. Additionally, The snapshot-streaming implementation requires `RunFileReader` to have no whole-run member, keeps `cur_block.size() <= kRunHardCapBlockSize`, and bounds every ranged request by `kRunHardCapBlockSize + 64 * 1024` plus the footer allowance."}
{"cluster_id": "chunk_005-003", "topic": "pure-reference-carry-runs", "canonical_claim": "When a GC-shard delta bucket is empty, the new `fold_seal` carries the parent generation's `RunRef` key and checksum verbatim and performs no run GET or PUT for that shard. Additionally, `RunRef` has an explicit additive `shard` field so consumers associate a run with a GC shard without parsing key paths; run consumers resolve through seal references rather than constructing keys. Additionally, An empty-delta and empty-retired GC shard is pure ref-carry and performs zero run-object I/O; `RunRef` carries `shard` and `generation` and consumers resolve runs through seal refs."}
{"cluster_id": "chunk_005-004", "topic": "Retention must preserve any run object referenced by the live adopted se", "canonical_claim": "Retention must preserve any run object referenced by the live adopted seal; when a later pass replaces a parent reference, it deletes the superseded old-generation prefix after the CAS, with a crash window that can strand at most one small object per shard in the original design."}
{"cluster_id": "chunk_005-005", "topic": "rebuild-history-guard", "canonical_claim": "A fold using an empty baseline must throw `CORRUPTED_DATA` when a discovered ref shard proves trimmed history: `journal.front().transition_version > 1`, or an empty journal with `shard_version > 0`. Additionally, `SYSTEM CONTENT ADDRESSED GC REBUILD` reconstructs per-shard fold cursors and blob in-degree from owner shards and journals, and its baseline guard raises `CORRUPTED_DATA` when trimmed history cannot be accounted for."}
{"cluster_id": "chunk_005-006", "topic": "The baseline guard gates GC only", "canonical_claim": "The baseline guard gates GC only; it never blocks server startup or normal reads and writes, and a genuinely fresh pool whose journals start at version 1 passes."}
{"cluster_id": "chunk_005-007", "topic": "rebuild-gc-plane-only", "canonical_claim": "`Gc::rebuildBaseline` writes only the GC plane—runs, seals, and `gc/state`—and never modifies ref shards, manifests, or blobs or deletes content. Additionally, `SYSTEM CAS GC REBUILD` writes only the GC plane, namely `gc/state` and `gc/gen/*` artifacts, and does not touch ref shards, manifests, or blobs or delete anything during the rebuild itself."}
{"cluster_id": "chunk_005-008", "topic": "Rebuild edge accumulation is bounded by a memory budget", "canonical_claim": "Rebuild edge accumulation is bounded by a memory budget: per-GC-shard edges are sorted and spilled into temporary run segments, then merged into one run using the T2 streaming readers and `RunFileWriter`."}
{"cluster_id": "chunk_005-009", "topic": "The rebuild mints `new_round = max(observed_gc_round over all mount bodi", "canonical_claim": "The rebuild mints `new_round = max(observed_gc_round over all mount bodies, fence_round over all shards, and surviving gc/gen prefix numbers) + 1` so stale acknowledgements cannot float fresh condemnations past the floor."}
{"cluster_id": "chunk_005-010", "topic": "A successful rebuild publishes an empty `retired_refs` set, preserving t", "canonical_claim": "A successful rebuild publishes an empty `retired_refs` set, preserving the reconstructed snapshot and making all content pass through the normal condemn → acknowledgement floor → graduate → delete pipeline again."}
{"cluster_id": "chunk_005-011", "topic": "The rebuild's per-shard guard is stricter than a global empty-baseline c", "canonical_claim": "The rebuild's per-shard guard is stricter than a global empty-baseline check: it fires for any shard with no sealed cursor whose journal proves trimmed history, while a shard born after the baseline passes because its journal starts at `transition_version` 1."}
{"cluster_id": "chunk_005-012", "topic": "The intended streaming open profile is exactly one HEAD, one tail ranged", "canonical_claim": "The intended streaming open profile is exactly one HEAD, one tail ranged GET, and one body `getStream`; `seek` adds one ranged GET per touched block."}
{"cluster_id": "chunk_005-013", "topic": "A pure reference-carry path requires both an empty delta bucket and an e", "canonical_claim": "A pure reference-carry path requires both an empty delta bucket and an empty retired-merge input; an empty delta with prior retired entries still runs the merge so retired entries settle every pass."}
{"cluster_id": "chunk_005-014", "topic": "The raw rebuild API returns `RebuildReport` with `performed`, `refusal`,", "canonical_claim": "The raw rebuild API returns `RebuildReport` with `performed`, `refusal`, minted `round` and `generation`, counts for namespaces, shards, committed refs, live precommits, unowned-alive manifests, edges, and clamped shards."}
{"cluster_id": "chunk_005-015", "topic": "gc-defer-round-behavior", "canonical_claim": "DEFER leaves the sealed `(snap_generation, snap_attempt)` unchanged, so journals accumulate naturally from the old sealed cursor and a later FOLD processes the accumulated deltas without new persisted accumulator state. Additionally, The pre-fold DEFER decision uses only O(shards) `LIST(cas/refs/)` token-diff signals, O(retired-entries + servers) graduation-due signals, and an O(1) defer window; it does not read journals or inspect whether a changed shard contains `+1` or `−1` content. Additionally, `gc_fold_max_defer_rounds` bounds batching so every accumulated delta is folded within a bounded number of rounds; a due destructive decision also forces a FOLD. Additionally, A deferred GC round reports `RoundReport::deferred=true`, emits a round event with `outcome = \"deferred\"`, and leaves the sealed generation pinned without fold, delete, or round-commit CAS; a FOLD resets the leader-local defer counter. Additionally, Phase 4 Lever A lets a GC round re-adopt the sealed in-degree generation as DEFER when cheap pre-fold signals show no destructive decision; due graduation forces a fold, and the safety gate is `CaGcRoundDeferCore`."}
{"cluster_id": "chunk_005-016", "topic": "A deferred owner removal is conservative because the stale in-degree is ", "canonical_claim": "A deferred owner removal is conservative because the stale in-degree is too high and can only delay reclaim, whereas a deferred owner addition is dangerous if a pending blob graduates from a stale zero in-degree snapshot."}
{"cluster_id": "chunk_005-017", "topic": "A new zero-in-degree condemnation candidate discovered during DEFER is d", "canonical_claim": "A new zero-in-degree condemnation candidate discovered during DEFER is detected only at the next FOLD, delaying reclaim by at most the bounded defer window but never deleting an uncondemned blob."}
{"cluster_id": "chunk_005-018", "topic": "The GC acceptance criteria require idle rounds to perform no generation-", "canonical_claim": "The GC acceptance criteria require idle rounds to perform no generation-run reads or writes, small-delta rounds to amortize snapshot rebuilds, default behavior to remain unchanged except for idle skipping, and `dangling == 0` in unit and live validation."}
{"cluster_id": "chunk_005-019", "topic": "`graduationDue` returns true for a retired entry with `delete_pending` o", "canonical_claim": "`graduationDue` returns true for a retired entry with `delete_pending` or `condemn_round < min_ack`; a missing retired list also returns true to force the existing fail-closed fold path rather than silently deferring."}
{"cluster_id": "chunk_005-020", "topic": "`changedShardCount` is the number of present shards whose token differs ", "canonical_claim": "`changedShardCount` is the number of present shards whose token differs from the sealed generation, represented by the non-`DiscoverDecision::Skip` decisions from `computeDiscoverDecisions`."}
{"cluster_id": "chunk_005-021", "topic": "A lower `observed_gc_round` caused by syncer lag is conservative because", "canonical_claim": "A lower `observed_gc_round` caused by syncer lag is conservative because `computeHeartbeatFloor` takes the minimum acknowledgement across live heartbeats; lag can hold the floor back but cannot advance it beyond an installed round."}
{"cluster_id": "chunk_005-022", "topic": "At steady state after driving GC to an idle deferred round, S34 measured", "canonical_claim": "At steady state after driving GC to an idle deferred round, S34 measured zero discovery `CasRootGet` operations for iteration counts from 5 through 40 and flat `root_dirs = 2`."}
{"cluster_id": "chunk_005-023", "topic": "Under S27 duplicate-key and dropped-continuation-token LIST anomalies, G", "canonical_claim": "Under S27 duplicate-key and dropped-continuation-token LIST anomalies, GC tolerated 40 perturbed LISTs with zero round errors, replica agreement, `dangling=0`, and dropped-content reclamation; unstable listings increased `CasRootGet` from 74 to 1373 as GC conservatively re-read."}
{"cluster_id": "chunk_005-024", "topic": "`isPrecommitDead(uint64_t writer_epoch, uint64_t build_sequence, const M", "canonical_claim": "`isPrecommitDead(uint64_t writer_epoch, uint64_t build_sequence, const MountLease & w)` must be shared by reclaim and discover paths; a future epoch is live, an older epoch is dead, and an equal epoch is dead only when the retired sentinel or `min_active > build_sequence` applies."}
{"cluster_id": "chunk_005-025", "topic": "force-read-revisit", "canonical_claim": "The force-Read fix must preserve `Skip` for a shard with no live precommit or a live-but-not-yet-dead precommit, and must be idempotent after the removal changes the shard token. Additionally, The force-Read guard is self-terminating: reclaim appends a removal, the shard token changes, the next round reads normally, and the shard becomes `Skip`-eligible after coverage no longer contains a live precommit."}
{"cluster_id": "chunk_005-026", "topic": "Force-Read is strictly more reading than Skip and therefore cannot under", "canonical_claim": "Force-Read is strictly more reading than Skip and therefore cannot under-count in-degree; each abandoned precommit adds at most one forced revisit at the round its build sequence crosses the floor."}
{"cluster_id": "chunk_005-027", "topic": "The resurrect supersede produces one retired entry per hash, drops the s", "canonical_claim": "The resurrect supersede produces one retired entry per hash, drops the stale token entry, preserves hash order, and commits the replacement through the existing R5 round CAS."}
{"cluster_id": "chunk_005-028", "topic": "A fresh upload is condemned only when the hash has been referenced, the ", "canonical_claim": "A fresh upload is condemned only when the hash has been referenced, the current object exists, and its folded current in-degree is zero; never-referenced uploads are not condemned."}
{"cluster_id": "chunk_005-029", "topic": "The CA cache disk shares the base disk's metadata-bound disk identity fo", "canonical_claim": "The CA cache disk shares the base disk's metadata-bound disk identity for GC scheduling and `system.content_addressed_log` attribution, so two disk names correspond to one GC identity."}
{"cluster_id": "chunk_005-030", "topic": "`CachedPartFolderAccess", "canonical_claim": "`CachedPartFolderAccess::dropRefBestEffort` is `noexcept`, swallows cleanup failures, and relies on garbage collection to reclaim lingering debris."}
{"cluster_id": "chunk_005-031", "topic": "retained-views-not-reachability-state", "canonical_claim": "Mutable files are not reachability state, and retained views hold no tokens with authority, do not affect GC in-degree, and do not protect objects from garbage collection. Additionally, GC journal trimming and abandoned-precommit reclaim do not change committed `root.refs`, so they do not invalidate part-folder views. Additionally, Cached views are not owner state, do not affect in-degree, and do not protect objects from GC."}
{"cluster_id": "chunk_005-032", "topic": "GC journal-only mutations do not route through the facade and do not inv", "canonical_claim": "GC journal-only mutations do not route through the facade and do not invalidate retained views because they do not change committed refs or committed `RootRef::mutable_files`."}
{"cluster_id": "chunk_005-033", "topic": "After the precommit-removal mutation is durably committed, Build", "canonical_claim": "After the precommit-removal mutation is durably committed, Build::abandon retires build_seq; the staged-manifest debris cleanup remains best-effort afterward."}
{"cluster_id": "chunk_005-034", "topic": "Moving retireBuildSeq after precommit removal makes the precommit waterm", "canonical_claim": "Moving retireBuildSeq after precommit removal makes the precommit watermark-dead only after removal is durably committed, closing the double-removal window."}
{"cluster_id": "chunk_005-035", "topic": "dedup-adoption-gate", "canonical_claim": "The dedup-adoption condemnation check remains safety-critical because adoption does not mutate the existing incarnation and a graduated delete can race an adoption that was not checked. Additionally, The K1 dedup-adoption gate is safety-critical: adopting an existing incarnation is invisible to GC's in-flight delete decision, so a condemned adopted blob must be displaced with a fresh incarnation."}
{"cluster_id": "chunk_005-036", "topic": "A durable precommit closure cannot be deleted by GC once a per-shard sea", "canonical_claim": "A durable precommit closure cannot be deleted by GC once a per-shard seal after precommitAdd folds the closure edge, because d becomes at least one and the entry is spared."}
{"cluster_id": "chunk_005-037", "topic": "The per-shard seal is taken when the target shard is read, not once glob", "canonical_claim": "The per-shard seal is taken when the target shard is read, not once globally for the whole GC pass; the edge theorem still holds because the closure is stored in that target shard's journal."}
{"cluster_id": "chunk_005-038", "topic": "A blob condemned after precommitAdd is doomed to be spared at the next f", "canonical_claim": "A blob condemned after precommitAdd is doomed to be spared at the next fold, while a blob already graduated before precommitAdd must be detected by the writer and displaced with a fresh incarnation."}
{"cluster_id": "chunk_005-039", "topic": "GC condemn captures the body token and writes condemned meta, spare CASe", "canonical_claim": "GC condemn captures the body token and writes condemned meta, spare CASes condemned to clean, and delete removes the body by exact token before deleting the meta; there is no tombstone handshake in v3."}
{"cluster_id": "chunk_005-040", "topic": "Mass-DROP metadata operations must use a bounded parallel pool because o", "canonical_claim": "Mass-DROP metadata operations must use a bounded parallel pool because one million sequential metadata CASes at about 20 ms each would take roughly 5.5 hours."}
{"cluster_id": "chunk_005-041", "topic": "GC writes condemned metadata at condemn, clears it at spare, deletes it ", "canonical_claim": "GC writes condemned metadata at condemn, clears it at spare, deletes it after exact-token body deletion, and performs per-hash metadata operations on a bounded parallel pool."}
{"cluster_id": "chunk_005-042", "topic": "GC deletion in the raw-body design is a tombstone handshake", "canonical_claim": "GC deletion in the raw-body design is a tombstone handshake: transition meta from `condemned` to `tombstone`, delete the body, then delete the tombstone meta, using a bounded parallel pool."}
{"cluster_id": "chunk_005-043", "topic": "A GC delete may delete a body only after winning the `condemned` to `tom", "canonical_claim": "A GC delete may delete a body only after winning the `condemned` to `tombstone` CAS; the terminal tombstone prevents a writer from re-establishing the body after that claim."}
{"cluster_id": "chunk_005-044", "topic": "The destructive path fails closed", "canonical_claim": "The destructive path fails closed: GC never deletes a body without a won tombstone claim, while a 404 during fold is recorded and continued rather than throwing or fail-closing the fold."}
{"cluster_id": "chunk_005-045", "topic": "The committed-removal sweep must preserve a body after a `-1` removal is", "canonical_claim": "The committed-removal sweep must preserve a body after a `-1` removal is appended but before that removal is sealed by the GC fold, because the fold still needs the body to emit the decrement; the body becomes sweepable only after the removal is sealed."}
{"cluster_id": "chunk_005-046", "topic": "GC condemns a clean meta by CASing it to condemned and stores the result", "canonical_claim": "GC condemns a clean meta by CASing it to condemned and stores the resulting condemned-meta etag in `RetiredEntry.token`; it spares by CASing condemned to clean and ignores absent or tombstone metas during the condemn pass."}
{"cluster_id": "chunk_005-047", "topic": "graduation-round-boundary", "canonical_claim": "Graduation is determined by `condemn_round < current_round`, where `current_round` is `new_round = state.round + 1`; using `state.round` would prevent entries from graduating. Additionally, The rebuilt orphan rows use `CondemnedRow{token, size, condemn_round = minted_round}` and graduate when `condemn_round < current_round`, which is the next round after minting."}
{"cluster_id": "chunk_005-048", "topic": "GC supersede detection re-reads meta for net-zero-touched entries and co", "canonical_claim": "GC supersede detection re-reads meta for net-zero-touched entries and compares the current meta etag with the stored condemned-meta etag; a mismatch means the retired entry refers to a newer incarnation."}
{"cluster_id": "chunk_005-049", "topic": "The GC rebuild skips `", "canonical_claim": "The GC rebuild skips `.meta` keys in the `blobs/` listing, captures or repairs the corresponding meta for each body, and stores the condemned-meta etag rather than the body token in rebuilt retired state."}
{"cluster_id": "chunk_005-050", "topic": "After a successful ref-fold adoption, consumed `_log` objects may be del", "canonical_claim": "After a successful ref-fold adoption, consumed `_log` objects may be deleted in bounded batches; an interrupted post-CAS deletion leaves only physical debris and is not a second protocol phase."}
{"cluster_id": "chunk_005-051", "topic": "orphan-sweep-protection-view", "canonical_claim": "Orphan cleanup protects current owners and every manifest removed in the unadopted tail using the exact adopted `RefBaseSet` plus the complete validated writer tail. Additionally, An orphan-manifest sweep uses the newest snapshot plus every later log, including manifests removed in the tail, and additionally requires the durable `last_folded_ref_id` before deleting a retired manifest. Additionally, The orphan-sweep protection view combines the newest snapshot with the complete tail, protects owners and manifests removed anywhere in the tail, retains the young-manifest window, and requires the `last_folded_ref_id` condition."}
{"cluster_id": "chunk_005-052", "topic": "A live owner newly entering the fold requires a valid manifest body, an ", "canonical_claim": "A live owner newly entering the fold requires a valid manifest body, an inherited-owner removal requires that body to emit `-1`, a move preserving the same `ManifestRef` emits no delta, and a missing required manifest aborts the entire GC attempt."}
{"cluster_id": "chunk_005-053", "topic": "A productive ref handoff is forced by fold-delta threshold, ref-tail pre", "canonical_claim": "A productive ref handoff is forced by fold-delta threshold, ref-tail pressure, maximum defer age, destructive demand, or `remove_namespace`; a deferred round still replays the complete tail for destructive protection."}
{"cluster_id": "chunk_005-054", "topic": "After a successful `gc/state` CAS, GC deletes covered ref-log objects wi", "canonical_claim": "After a successful `gc/state` CAS, GC deletes covered ref-log objects with exact-key batch deletes of at most 1000 keys per request; if the CAS loses, it deletes no ref-log object."}
{"cluster_id": "chunk_005-055", "topic": "The orphan-manifest sweep proves build retirement, reads the exact adopt", "canonical_claim": "The orphan-manifest sweep proves build retirement, reads the exact adopted `RefBaseSet`, replays the complete contiguous tail, rechecks `gc/state`, and deletes only candidates absent from the complete protection set."}
{"cluster_id": "chunk_005-056", "topic": "retired-snapshot-unchanged-semantics", "canonical_claim": "Settlement semantics remain byte-for-byte unchanged in the retired-in-snapshot plan: condemn, carry, graduate with `delete_pending`, redelete, clamp suppression, pre-CAS single delete site, resurrect-supersede, `.meta` writes, B170 events, and GC-log counters are preserved. Additionally, The retired-in-snapshot design keeps B170 events, GC-log counters, `.meta` writes, and the `system.content_addressed_garbage_collection_log` schema unchanged."}
{"cluster_id": "chunk_005-057", "topic": "carried-condemned-settlement", "canonical_claim": "A carried condemned sentinel is settlement-only: it never sets the merge touch bit, emits no zero marker, and does not trigger `peek_head` when carry is the only activity. Additionally, Fresh condemns emit `kCondemned` rows, absent-at-condemn cases emit `kZeroMarker` as before, prior zero markers are dropped on carry, and carried condemned rows do not set `cur_touched`. Additionally, A carried `kCondemned` row is settlement-only and must not set the merge touch bit; only real edges or deltas touching a blob may trigger a zero marker or a resurrect-supersede `peek_head` probe."}
{"cluster_id": "chunk_005-058", "topic": "two-cursor-retired-settlement", "canonical_claim": "The 2-cursor settlement merge removes the separate `prior_retired` input: condemned rows are decoded from the prior source-edge run, settled at blob close-out, and emitted back at the blob's zero-sentinel key. Additionally, The retired-in-snapshot design stores condemnation state as `kCondemned` sentinel rows in the source-edge run and stores per-shard `condemned_summary` in the fold seal; the merge therefore uses two cursors instead of three."}
{"cluster_id": "chunk_005-059", "topic": "With `suppress_destructive`, pending rows carry unchanged and floor-pass", "canonical_claim": "With `suppress_destructive`, pending rows carry unchanged and floor-passed rows remain condemned-only, preserving the row bytes."}
{"cluster_id": "chunk_005-060", "topic": "condemned-summary-seal", "canonical_claim": "Every new fold seal contains a `condemned_summary` total over shards `0..gc_shards-1`; folding computes it from written condemned rows, while pure-carry shards copy the parent's entry verbatim. Additionally, `CasFoldSeal` contains a per-GC-shard `condemned_summary` with `condemned_total`, `pending_total`, and `oldest_nonpending_condemn_round`, where `UINT64_MAX` means no nonpending condemned row."}
{"cluster_id": "chunk_005-061", "topic": "graduation-due-summary", "canonical_claim": "For `snap_generation == 0`, `graduationDue` returns false; otherwise it reads the adopted seal and fails closed by returning true when the seal is missing, undecodable, or its summary is not total over `gc_shards`. Additionally, When the adopted seal summary is valid, `graduationDue` is true exactly when some shard has `pending_total > 0` or `oldest_nonpending_condemn_round < current_round`; it requires no backend GET beyond the seal read."}
{"cluster_id": "chunk_005-062", "topic": "GC rebuild must first traverse journals and accumulate per-shard deltas ", "canonical_claim": "GC rebuild must first traverse journals and accumulate per-shard deltas and `edge_bearing`, then list physical blobs and head zero-edge bodies, mint condemned rows, append them to shard runs, seal summaries, and only then CAS `gc/state`."}
{"cluster_id": "chunk_005-063", "topic": "A shard may be pure-ref-carried only when it has no deltas and `condemne", "canonical_claim": "A shard may be pure-ref-carried only when it has no deltas and `condemned_total == 0`."}
{"cluster_id": "chunk_005-064", "topic": "`RetiredMergeResult` remains the interface for `graduated`, `spared`, `r", "canonical_claim": "`RetiredMergeResult` remains the interface for `graduated`, `spared`, `redelete`, and `replaced` outcomes, which continue to drive outcome logs, `.meta` transitions, B170 events, and `entries_condemned`, `entries_graduated`, and `entries_redeleted` counters."}
{"cluster_id": "chunk_005-065", "topic": "A crash between run PUT and seal or state CAS leaves orphan attempt-scop", "canonical_claim": "A crash between run PUT and seal or state CAS leaves orphan attempt-scoped artifacts that retention prunes through `snap_pruned_through` under B174."}
{"cluster_id": "chunk_005-066", "topic": "Settlement has no algorithm loop", "canonical_claim": "Settlement has no algorithm loop: rows are self-describing, `blobShard` uses the first eight digest bytes in big-endian order modulo `gc_shards`, and the algorithm is ignored only inside the shard calculation."}
{"cluster_id": "chunk_005-067", "topic": "The fold seal remains total over `gc_shards` and gains no algorithm dime", "canonical_claim": "The fold seal remains total over `gc_shards` and gains no algorithm dimension or algorithm-driven partition iteration."}
{"cluster_id": "chunk_005-068", "topic": "Condemn-sweep and fsck derive algorithm and digest width from the `blobs", "canonical_claim": "Condemn-sweep and fsck derive algorithm and digest width from the `blobs/<algo>/...` path segment; a segment not in `algos_used` is skipped by sweep and reported as `unaccounted` by fsck."}
{"cluster_id": "chunk_005-069", "topic": "`blobShard` for variable-length digests is the big-endian uint64 represe", "canonical_claim": "`blobShard` for variable-length digests is the big-endian uint64 represented by the first eight digest bytes modulo `gc_shards`, preserving routing for existing 128-bit digests."}
{"cluster_id": "chunk_005-070", "topic": "GC stores one `last_folded_ref_id` cursor per table and manifest-edge de", "canonical_claim": "GC stores one `last_folded_ref_id` cursor per table and manifest-edge deltas, not complete table state or owners; the cursor map is O(number of tables), not O(number of refs)."}
{"cluster_id": "chunk_005-071", "topic": "The `_cleanup/<remove-txn-id>` object is a zero-byte completion marker p", "canonical_claim": "The `_cleanup/<remove-txn-id>` object is a zero-byte completion marker published with `putIfAbsent` after the exact namespace cleanup reaches `Completed`; Phase 1 never deletes markers."}
{"cluster_id": "chunk_005-072", "topic": "The namespace-removal transaction emits no additional manifest-edge delt", "canonical_claim": "The namespace-removal transaction emits no additional manifest-edge delta because its preceding exact owner transitions already emit all edge removals; its final operation creates the GC namespace-cleanup request."}
{"cluster_id": "chunk_005-073", "topic": "global-ref-list-intake", "canonical_claim": "GC performs one paginated global `LIST cas/refs/` per round for ref intake, snapshot observation, and cleanup planning; it does not perform per-table LISTs or snapshot HEADs. Additionally, `GC` performs one global paginated `LIST cas/refs/` with explicit start-after resume and persists per-table `last_folded_ref_id` cursors in the existing fold artifacts through the existing single `gc/state` CAS."}
{"cluster_id": "chunk_005-074", "topic": "Ref intake never directly deletes blobs or manifests", "canonical_claim": "Ref intake never directly deletes blobs or manifests; it only feeds appeared and disappeared edges to the existing multi-round GC settlement and delayed-delete protocol."}
{"cluster_id": "chunk_005-075", "topic": "ref-cleanup-coverage", "canonical_claim": "A ref log is eligible for deletion only when the durable per-table fold cursor covers it, a snapshot observed by the same scan covers it, and any namespace-removal cleanup work is durable. Additionally, A losing ref-fold commit adopts nothing, and cleanup requires both cursor coverage and observed snapshot coverage. Additionally, Ref-object cleanup requires observed-snapshot coverage, cursor coverage, and item durability for removal transactions; cleanup uses exact keys, batches no larger than 1000, and can delete snapshots older than the newest observed snapshot."}
{"cluster_id": "chunk_005-076", "topic": "namespace-cleanup-state", "canonical_claim": "Namespace cleanup has durable `Pending` and terminal `Completed` states keyed by `{namespace, remove_txn_id}`; `Completed` is written only after a pass observes no remaining objects to delete. Additionally, Namespace cleanup items transition from `Pending` to `Completed` in fold artifacts; after `Completed`, GC publishes the `_cleanup` marker and republishes the `Removed` snapshot idempotently."}
{"cluster_id": "chunk_005-077", "topic": "GC ref-intake memory is O(one LIST page plus a bounded decoded transacti", "canonical_claim": "GC ref-intake memory is O(one LIST page plus a bounded decoded transaction batch, emitted edge buffer, and retained exact cleanup keys), independent of table size."}
{"cluster_id": "chunk_005-078", "topic": "A crash between copy completion and staging deletion may leak a staging ", "canonical_claim": "A crash between copy completion and staging deletion may leak a staging object, which is reclaimed by a lease-fenced sweeper scoped to the mount's `staging/<mount_id>/` prefix and by GC age sweeps."}
{"cluster_id": "chunk_005-079", "topic": "GC discovers blobs by listing `blobs/`, so it excludes `staging/`", "canonical_claim": "GC discovers blobs by listing `blobs/`, so it excludes `staging/`; the staging sweeper is the only reclaimer of staging objects and GC must not exact-token-delete them as orphan blobs."}
{"cluster_id": "chunk_005-080", "topic": "`GC` never reads or GETs a condemned object to revive it", "canonical_claim": "`GC` never reads or GETs a condemned object to revive it; revival requires a fresh re-upload."}
{"cluster_id": "chunk_005-081", "topic": "A 404 during `GC` folding is recorded and processing continues where the", "canonical_claim": "A 404 during `GC` folding is recorded and processing continues where the specification permits; where the specification requires fail-closed behavior, only the current attempt is aborted rather than the process."}
{"cluster_id": "chunk_005-082", "topic": "Ref-delta intake uses paginated enumeration resumed after the last retur", "canonical_claim": "Ref-delta intake uses paginated enumeration resumed after the last returned key, atomically adopts per-table candidate cursors in one fold commit, and adopts every durable log delta exactly once using event-id idempotence."}
{"cluster_id": "chunk_005-083", "topic": "GC edge events are deterministic `+1/-1` events with `event_id={ns, txn_", "canonical_claim": "GC edge events are deterministic `+1/-1` events with `event_id={ns, txn_id, op_ordinal, edge_ordinal}`; same-manifest promotion has no net change, and add-plus-remove within one unfolded batch cancels."}
{"cluster_id": "chunk_005-084", "topic": "gc-scheduler-lifecycle-locking", "canonical_claim": "`CasGcScheduler` must share one persistent `Cas::Gc` between `loop` and `runOneRoundNow`, serialize rounds with a dedicated `gc_round_mutex`, and protect lazy `gc_scheduler` creation with `gc_scheduler_mutex`. Additionally, The CAS scheduler lifecycle fix must hold `gc_scheduler_mutex` across `runOneRoundNow`, reject lazy creation after shutdown, and synchronize `cas_store` and `part_access` reads with shutdown resets."}
{"cluster_id": "chunk_005-085", "topic": "The `suppress_destructive` decision for a GC round must be computed once", "canonical_claim": "The `suppress_destructive` decision for a GC round must be computed once in `Gc::fold`, stored in `FoldResult::suppress_destructive`, and reused by all destructive phases."}
{"cluster_id": "chunk_005-086", "topic": "token-stability-fence-out", "canonical_claim": "GC fence-out uses token-stability observation on the leader's monotonic clock: a non-terminated, non-fenced mount is fenced only after its token remains unchanged for `ttl + ttl / 20 + round_period_hint`, with a new leader starting with an empty observation map and therefore delaying fencing safely by one round. Additionally, GC fence-out uses token stability on the GC leader's own monotonic clock instead of comparing `MountLease.expires_at_ms` with the leader's wall clock."}
{"cluster_id": "chunk_005-087", "topic": "The orphan sweep can report a `materialization_grace_ms` violation from ", "canonical_claim": "The orphan sweep can report a `materialization_grace_ms` violation from a log below the seal without reading it to revive the object."}
{"cluster_id": "chunk_005-088", "topic": "The §4 promote gate proves a future GC fold will spare an adopted blob a", "canonical_claim": "The §4 promote gate proves a future GC fold will spare an adopted blob after the destination precommit edge is visible, but it does not prevent a previously captured pre-CAS `deleteExact` from firing."}
{"cluster_id": "chunk_005-089", "topic": "A resurrect displaces a condemned blob from token `t1` to fresh token `t", "canonical_claim": "A resurrect displaces a condemned blob from token `t1` to fresh token `t2`, whereas `adoptEvidence` keeps the original token `t1` and therefore does not make a stale `deleteExact(h,t1)` harmless."}
{"cluster_id": "chunk_005-090", "topic": "GC redelete runs before the round's `gc/state` CAS and does not re-check", "canonical_claim": "GC redelete runs before the round's `gc/state` CAS and does not re-check recovered in-degree before issuing `deleteExact` for a previously captured `delete_pending` row."}
{"cluster_id": "chunk_005-091", "topic": "A fresh GC leader's spare is add-only and cannot cancel a stale leader's", "canonical_claim": "A fresh GC leader's spare is add-only and cannot cancel a stale leader's already-captured pre-CAS delete."}
{"cluster_id": "chunk_005-092", "topic": "The GC defense audit found all six named GC-side sites compliant with th", "canonical_claim": "The GC defense audit found all six named GC-side sites compliant with the rule that foreign-writer defense spends no speculative S3 requests on hot paths."}
{"cluster_id": "chunk_005-093", "topic": "The `gc/state` round-ownership re-read is used only for destructive name", "canonical_claim": "The `gc/state` round-ownership re-read is used only for destructive namespace cleanup and checks both per page and per key to prevent mid-page namespace recreation."}
{"cluster_id": "chunk_005-094", "topic": "threaded-gc-state-anti-zombie", "canonical_claim": "Threading the already-held `(state, state_token)` pair to the terminal GC CAS avoids a zombie-steal re-read and costs zero requests. Additionally, A GC round threads `(GcState state, Token state_token)` through its phases, re-CASes that state, and never re-reads it to preserve the anti-zombie invariant."}
{"cluster_id": "chunk_005-095", "topic": "The orphan-sweep eligibility gate uses the durable mount watermark to id", "canonical_claim": "The orphan-sweep eligibility gate uses the durable mount watermark to identify dead epochs and treats a missing watermark as not eligible, so it deletes nothing on that fallback."}
{"cluster_id": "chunk_005-096", "topic": "Orphan-sweep `NotFound` and `TokenMismatch` outcomes are tolerated and l", "canonical_claim": "Orphan-sweep `NotFound` and `TokenMismatch` outcomes are tolerated and logged without retries or re-verification GETs."}
{"cluster_id": "chunk_005-097", "topic": "Fold-lag clamp barriers do not add request work", "canonical_claim": "Fold-lag clamp barriers do not add request work: they defer cursor advancement and suppress graduation when manifest bodies are not yet visible."}
{"cluster_id": "chunk_005-098", "topic": "When a listed ref-log body vanishes or fails to decode, GC aborts ref fo", "canonical_claim": "When a listed ref-log body vanishes or fails to decode, GC aborts ref folding for the round and resets cursor advances to already durable parents without issuing diagnostic requests."}
{"cluster_id": "chunk_005-099", "topic": "The Round-B plan defines `casSizedReadSettings` to shrink known-size fol", "canonical_claim": "The Round-B plan defines `casSizedReadSettings` to shrink known-size fold read buffers to body size plus 4096 bytes, capped at the caller's base buffer size, while leaving unknown size zero unchanged."}
{"cluster_id": "chunk_005-100", "topic": "The writer's GC retention floor is the minimum `S_min` over every non-ex", "canonical_claim": "The writer's GC retention floor is the minimum `S_min` over every non-expired reader pin and gates blob reclaim, ref-object cleanup, and namespace cleanup."}
{"cluster_id": "chunk_005-101", "topic": "A dropped table or dead pool-member decommission does not reclaim a read", "canonical_claim": "A dropped table or dead pool-member decommission does not reclaim a reader-pinned historical closure until the reader pin releases or expires."}
{"cluster_id": "chunk_005-102", "topic": "exact-token-delete-incarnation-safety", "canonical_claim": "Exact-token deletion protects against different-token resurrection but does not protect against same-token reuse. Additionally, RetiredSetProto records exact incarnation tokens, and GC deletes only the observed incarnation so a replacement object is not deleted accidentally Additionally, Each `deleteExact` carries its own token and must remove only the matching object incarnation, preserving GC safety against deletion of a replaced incarnation."}
{"cluster_id": "chunk_005-103", "topic": "`cas_gc_hb` is a full header'd text object with a header line and one JS", "canonical_claim": "`cas_gc_hb` is a full header'd text object with a header line and one JSON body line; its advisory owner and monotone `hb_seq` semantics remain unchanged, and the former 24-byte unversioned representation is removed."}
{"cluster_id": "chunk_005-104", "topic": "`GcState", "canonical_claim": "`GcState.gc_shards` is immutable and must be at least 1; decoding a zero value raises `CORRUPTED_DATA`."}
{"cluster_id": "chunk_005-105", "topic": "`cas_gc_outcomes` is line-structured as a header, one flat JSON record p", "canonical_claim": "`cas_gc_outcomes` is line-structured as a header, one flat JSON record per `OutcomeEntry` in insertion order, and a `{\"n\":<count>}` trailer; it is always zstd-compressed and stored under a `.zst` key suffix."}
{"cluster_id": "chunk_005-106", "topic": "fold-seal-encoding", "canonical_claim": "`cas_fold_seal` is `PinnedRaw` and `Strict`; its encoder emits fixed field and section order, sorts vector collections by run key inside the encoder, and must be byte-reproducible for `putDeterministicArtifact` adoption. Additionally, The `cas_fold_seal` body consists of a header, a metadata line, record sections in coverage, blob-target-runs, part-manifest-cleanup, condemned-summary, and namespace-cleanup order, followed by a `{\"n\":<total record count>}` trailer; record kinds are `cov`, `btr`, `pmc`, `cnd`, and `nsc`. Additionally, A fold-seal `nsc` record reconstructs its map key as `ns + newline + renderRefTxnId(remove_txn_id)`; a zero `{0,0}` transaction id is invalid for `nsc`, while a zero `last_folded_ref_id` is allowed for coverage records. Additionally, A cas_fold_seal contains rfl ref-life rows, sorted btr blob-target RunRef rows, and shard-sorted cnd condemned summaries, followed by a record-count trailer Additionally, cas_fold_seal is a write-once deterministic artifact produced by encodeFoldSeal and putDeterministicArtifact, so replay of the same attempt must reproduce identical bytes"}
{"cluster_id": "chunk_005-107", "topic": "blob-meta-state-codec", "canonical_claim": "`MetaState::Clean` means the blob is referenceable and present, while `MetaState::Condemned` means GC found zero in-degree but the body remains present and may be resurrected by a writer CAS. Additionally, The condemned-state gate remains correct when the text codec round-trips `state`, `condemn_round`, and `size`, because dedup and GC branch on decoded fields and CAS-swap by etag."}
{"cluster_id": "chunk_005-108", "topic": "cas-run-ndjson-format", "canonical_claim": "The `cas_run` object is the GC source-edge in-degree data plane and is converted from `CARN` block-framed binary to sorted NDJSON with a typed header, one record per line, and a {\"n\":count} trailer. Additionally, v3 cas_run uses sorted NDJSON records with marker words edge, zero, and condemned, followed by a trailer count n"}
{"cluster_id": "chunk_005-109", "topic": "The part-manifest-cleanup run has no reader", "canonical_claim": "The part-manifest-cleanup run has no reader: actual cleanup executes inline from the in-memory `folded.mf_cleanup` map, so the durable cleanup run and fold-seal `part_manifest_cleanup` field can be removed without changing the delete driver."}
{"cluster_id": "chunk_005-110", "topic": "The shared retention-overlay primitive folds current refs together with ", "canonical_claim": "The shared retention-overlay primitive folds current refs together with the edge sets of live-owner pins, and a blob is condemnable only when its source-edge set is empty across that union."}
{"cluster_id": "chunk_005-111", "topic": "Retention pins may encode their protected edges as either an explicit ma", "canonical_claim": "Retention pins may encode their protected edges as either an explicit manifest closure or a snapshot window; the primitive is agnostic to payload shape and distinguishes consumers by ownership and liveness."}
{"cluster_id": "chunk_005-112", "topic": "The proposed reaper deleted `gc/fetch-pins/<srid>/<build_id>` when `buil", "canonical_claim": "The proposed reaper deleted `gc/fetch-pins/<srid>/<build_id>` when `build_id < srid.min_active` or when the owning `srid` was terminated, fenced, or lease-expired."}
{"cluster_id": "chunk_005-113", "topic": "durable-condemned-confirmation", "canonical_claim": "The CAS GC graduation rule must require confirmed durable `Condemned` metadata for the exact `(hash, token)`; absent confirmation carries the entry to the next round instead of deleting, without changing the rule that a 404 during fold must not throw. Additionally, GC keeps its asynchronous advisory metadata model, but graduation to `delete_pending` or redelete requires confirmed durable `Condemned` metadata for the exact `(hash, token)`; missing evidence causes a fail-safe carry. Additionally, The planned GC marker confirmation can come from a successful marker write in the current or earlier round or from a fresh `loadMeta(hash)` observing `state == Condemned && token == entry.token`. Additionally, Graduation to `delete_pending` requires confirmed durable `Condemned` evidence from a write-completion callback or synchronous meta re-read; otherwise the entry carries to the next round."}
{"cluster_id": "chunk_005-114", "topic": "generation-pruning-parent-seal", "canonical_claim": "Pre-CAS generation pruning must protect the union of generations referenced by both the proposed seal and the parent seal; only generations referenced by neither may be deleted before the CAS. Additionally, Pre-CAS generation pruning must include every generation referenced by the parent seal as well as the proposed seal, because a losing leader must not delete a run referenced by the winning leader."}
{"cluster_id": "chunk_005-115", "topic": "The emulated backend should derive `TokenType", "canonical_claim": "The emulated backend should derive `TokenType::Emulated` token values from the underlying local-object etag so list and head agree and a post-restart fresh incarnation cannot match a persisted exact-token delete."}
{"cluster_id": "chunk_005-116", "topic": "The access type for the renamed command is `SYSTEM_CONTENT_ADDRESSED_GC_", "canonical_claim": "The access type for the renamed command is `SYSTEM_CONTENT_ADDRESSED_GC_RUN` with grant string `SYSTEM CONTENT ADDRESSED GC RUN`, and the interpreter method is `runContentAddressedGcRun`."}
{"cluster_id": "chunk_005-117", "topic": "`SYSTEM CONTENT ADDRESSED GC STOP` must stop future rounds and clear `i_", "canonical_claim": "`SYSTEM CONTENT ADDRESSED GC STOP` must stop future rounds and clear `i_am_leader`; `GC START` must resume rounds on the same scheduler while preserving `gc_id`."}
{"cluster_id": "chunk_005-118", "topic": "After a successful confirm, GC cannot delete the referenced blobs before", "canonical_claim": "After a successful confirm, GC cannot delete the referenced blobs before a later removal event because every fold that sees a post-confirm `-1` also sees the durable receiver `+1`, and a straddling fold spares positive in-degree rows."}
{"cluster_id": "chunk_005-119", "topic": "edge-set-in-degree-reducer", "canonical_claim": "CAS blob in-degree is a SET of source edges with last-wins semantics per key, not a counter; an unmatched removal is a per-key no-op and cannot remove a sibling manifest edge for the same blob. Additionally, The in-degree reducer models source edges as a set: an unmatched `-1` is dropped as a no-op and a duplicate `+1` collapses, so the reducer must not materialize negative edges merely to repair the retention leak. Additionally, The unmatched removal-fold `-1` is harmless under the current edge-set model because source edges are set membership keyed by `(ref, ManifestId, path)`, not a counter; erasing an already-absent edge cannot affect another manifest's edges. Additionally, In-degree is a set-presence merge keyed by `(blob, source_id)` and applied in strict transaction-id order; a `del` whose key is absent is a silent no-op. Additionally, The source-edge reducer treats in-degree as a set: an unmatched `-1` must be dropped rather than materialized as a negative edge, because a negative edge could cause a false deletion."}
{"cluster_id": "chunk_005-120", "topic": "The fsck `stale_edge` class is detail-mode only and should be emitted wh", "canonical_claim": "The fsck `stale_edge` class is detail-mode only and should be emitted when every in-degree edge for a blob names an absent manifest; a blob remains ordinary `AwaitingGc` when any edge still names a present manifest."}
{"cluster_id": "chunk_005-121", "topic": "A GC round enumerates the `cas/refs/` prefix twice", "canonical_claim": "A GC round enumerates the `cas/refs/` prefix twice: `Gc::preFoldRefScan` supplies the defer signal and `Gc::fold` performs the folding walk; only a hole in the fold walk reproduces the skipped-transaction defect."}
{"cluster_id": "chunk_005-122", "topic": "The `BlobDelta` row remains 64 bytes when `txn_ordinal` is placed after ", "canonical_claim": "The `BlobDelta` row remains 64 bytes when `txn_ordinal` is placed after `remove`, because the ordinal occupies existing tail padding; `kBlobDeltaSize` and a `static_assert` pin this hot-path size."}
{"cluster_id": "chunk_005-123", "topic": "Probe A reports a disagreement only when a ref-log id is present in one ", "canonical_claim": "Probe A reports a disagreement only when a ref-log id is present in one enumeration, absent from the other, and strictly below the other enumeration's maximum id for the same namespace; this witness condition excludes normal concurrent appends."}
{"cluster_id": "chunk_005-124", "topic": "Probe A cannot detect a hole reproduced identically in both enumerations", "canonical_claim": "Probe A cannot detect a hole reproduced identically in both enumerations or a namespace omitted wholesale from one enumeration, and widening the rule to use the pre-scan maximum as a witness was rejected because it can permanently block legitimate cleanup."}
{"cluster_id": "chunk_005-125", "topic": "probe-a-abort", "canonical_claim": "When Probe A detects a disagreement, ref folding is aborted for the round: cursors do not advance, accumulated deltas are discarded, parent cursors are carried into the seal, and destructive actions are suppressed. Additionally, When probe A detects a ref-scan disagreement, the fold must abort without advancing the cursor or deleting anything; the live detector stopped all seven observed folds this way."}
{"cluster_id": "chunk_005-126", "topic": "Probe B1 compares a running count of logs applied at the single `resolve", "canonical_claim": "Probe B1 compares a running count of logs applied at the single `resolved_through = log_id` cursor-advance site with a seal-time recomputation from sealed coverage and the ref listing; it is a control-flow assertion and is blind to a record missing from that same listing."}
{"cluster_id": "chunk_005-127", "topic": "`TxnApplyLedger` is round-local and never persisted", "canonical_claim": "`TxnApplyLedger` is round-local and never persisted; it tracks each opened ref transaction, whether it produced deltas, whether intake committed it, and whether a reducer consumed at least one delta."}
{"cluster_id": "chunk_005-128", "topic": "Probe B2 marks a transaction applied at reducer consumption rather than ", "canonical_claim": "Probe B2 marks a transaction applied at reducer consumption rather than run flush, because the in-degree model is a set and legitimate unmatched removals or duplicate activations may disappear during set merging."}
{"cluster_id": "chunk_005-129", "topic": "The garbage-collection log phase schema adds `EventType", "canonical_claim": "The garbage-collection log phase schema adds `EventType::Phase`, `round_id`, `phase`, `phase_duration_us`, and `phase_metrics` without removing or reordering existing fields."}
{"cluster_id": "chunk_005-130", "topic": "The ref-log cursor is sealed above every record a GC round observed, not", "canonical_claim": "The ref-log cursor is sealed above every record a GC round observed, not every record that exists; an omitted durable ref log can therefore never be folded by a later incremental round."}
{"cluster_id": "chunk_005-131", "topic": "When Probe A fired, the round aborted folding, held the cursor, and a la", "canonical_claim": "When Probe A fired, the round aborted folding, held the cursor, and a later complete enumeration folded the missing records; the three keys were deleted six minutes later and no leak resulted."}
{"cluster_id": "chunk_005-132", "topic": "The durable fix requirement is that GC must not advance a cursor over re", "canonical_claim": "The durable fix requirement is that GC must not advance a cursor over records whose enumeration it cannot prove complete; the committed direction is an authoritative per-namespace chain plus a complete-cut gate."}
{"cluster_id": "chunk_005-133", "topic": "Blob in-degree is pool-wide, so destructive GC rounds require a frontier", "canonical_claim": "Blob in-degree is pool-wide, so destructive GC rounds require a frontier proof for every catalog namespace rather than only namespaces returned by a listing."}
{"cluster_id": "chunk_005-134", "topic": "The pre-fold drain takes a complete catalog snapshot, selects one eligib", "canonical_claim": "The pre-fold drain takes a complete catalog snapshot, selects one eligible row, and restarts its complete scan after every CAS outcome; only a scan with no eligible row permits the invocation to continue."}
{"cluster_id": "chunk_005-135", "topic": "The dead-life janitor enumerates `cas/ns/` separately from the fold's ho", "canonical_claim": "The dead-life janitor enumerates `cas/ns/` separately from the fold's hot `cas/ns/stream/` LIST, takes one fresh catalog GET per page rather than per key, and deletes only after page-wide life revalidation and an exact fence/token check."}
{"cluster_id": "chunk_005-136", "topic": "Janitor progress is stored in the leak-only `<prefix>/gc/maintenance_sta", "canonical_claim": "Janitor progress is stored in the leak-only `<prefix>/gc/maintenance_state` object registered as `FormatId::GcMaintenanceState = 25`; losing or repeating this cursor can repeat cleanup work or leak bytes, but cannot affect GC authority, frontier completeness, `DEFER`, catalog lifecycle, or successor publication."}
{"cluster_id": "chunk_005-137", "topic": "A physical id returned by a completed LIST and absent from the correspon", "canonical_claim": "A physical id returned by a completed LIST and absent from the corresponding later catalog cut is inert dead-life debris: the fold counts and drops it, while only the janitor may nominate its physical deletion."}
{"cluster_id": "chunk_005-138", "topic": "snapshot-checkpoint-deletion", "canonical_claim": "Snapshots are deletable only strictly below `_ckpt.checkpoint`; a stale pointer can only under-clean, and a missing sampled base requires rereading `_ckpt` to distinguish a concurrent advancement from corruption. Additionally, Snapshots are deletable only strictly below `checkpoint_snapshot_id`; if a sampled checkpoint base is missing, a changed `_ckpt` token causes restart and an unchanged token causes `CORRUPTED_DATA`. Additionally, Snapshot cleanup may delete only snapshots strictly below the current `_ckpt` base; deleting the snapshot still named by `ckpt.base` must fail closed as corruption after exact-key revalidation."}
{"cluster_id": "chunk_005-139", "topic": "After the mandatory pre-fold drain, the fold performs one strict `LIST(c", "canonical_claim": "After the mandatory pre-fold drain, the fold performs one strict `LIST(cas/ns/stream/)`, then one fresh full-catalog cut, and builds its sole ref plan from that cut plus the completed LIST observations."}
{"cluster_id": "chunk_005-140", "topic": "arithmetic-ref-log-walk", "canonical_claim": "Each catalog-admitted life is folded arithmetically from its adopted cursor through exact `_ckpt.committed_through`; an omitted life receives the same finite exact walk, while hint holes such as `0x1430c`/`0x1430d` are not correctness gaps. Additionally, Fold arithmetic intake walks exact `expected = cursor + 1` keys rather than iterating `listing.logs`; a hint hole that GET finds is folded without anomaly, while a 404 with no higher witness marks the normal frontier. Additionally, The GC fold reads `expected = cursor + 1` by exact key, proves epoch crossings via `crossFromSeal`, treats a hint hole as a non-event, and turns an absent expected-next with a witness above it into a per-namespace HOLD with `ShardCoverage::classification == 4`. Additionally, The middle of a ref-log enumeration remains arithmetic on every backend because predecessor omission below a correct paginated LIST tail is legal under concurrent writes; held namespaces and namespaces with no listed logs must still use the probe. Additionally, The ref-log intake walk still issues the exact-key `GET` at `cursor + 1` even when `cursor == tail`; an absent expected-next key with no higher witness is the proof that sets `frontier_proven`."}
{"cluster_id": "chunk_005-141", "topic": "A destructive round may perform condemnation, graduation, deletion, swee", "canonical_claim": "A destructive round may perform condemnation, graduation, deletion, sweep deletes, or ref cleanup only after every `Live`/`Removing` catalog entry reaches its same-round `_ckpt.committed_through` frontier; missing checkpoints, incomplete walks, or budget exhaustion suppress all destruction."}
{"cluster_id": "chunk_005-142", "topic": "destructive-frontier-gate", "canonical_claim": "A classification-4 `ShardCoverage` hold stores a bounded reason, offending position, and retry/backoff data per `(namespace, incarnation)`; `suppress_destructive = anomalies || carried_holds || !frontier_complete` is recomputed before every destructive site. Additionally, `suppress_destructive` is true when anomalies exist, a hold is carried, or the frontier is incomplete, and it must be consulted at every destructive site including manifest deletion, generation prune, orphan sweep, handoff reclaim, and ref cleanup. Additionally, The destructive gate remains `suppress_destructive = anomalies || carried_holds || !frontier_complete`; the Stage B change permits `frontier_complete` to become true from catalog proof but does not weaken any suppression term. Additionally, Destructive GC is permitted only when `frontier_complete = true` and `suppress_destructive = false`; the suppression gate remains `anomalies || carried_holds || !frontier_complete`. Additionally, The destructive gate formula is `frontier_complete = universe_authoritative && frontier_namespaces > 0 && frontier_proven == frontier_namespaces` and `suppress_destructive = anomalies || carried_holds || !frontier_complete`. Additionally, The destructive gate has five independent suppressors: non-empty anomalies, non-empty carried holds, `!universe_authoritative`, `frontier_namespaces == 0`, and `frontier_proven != frontier_namespaces`. Additionally, The destructive frontier gate is complete only when `universe_authoritative` is true, `result.frontier_namespaces > 0`, and `result.frontier_proven == result.frontier_namespaces`; anomalies, carried holds, or any incomplete frontier suppress destructive work. Additionally, The physical GC destructive gate is open only when the universe is authoritative, the frontier namespace count is nonzero, the proven frontier equals the namespace universe, and neither anomalies nor carried holds suppress destruction. Additionally, When the frontier is not complete, destructive work is suppressed pool-wide because blob in-degree is pool-wide and there is no ownership partition of the blob space. Additionally, `suppress_destructive` is computed as `!report.anomalies.empty() || !carried_holds.empty() || frontier_incomplete`; the authoritative flip changes only the value of `universe_authoritative` in the `frontier_complete` expression. Additionally, An authoritative empty universe is not a complete frontier: `frontier_namespaces > 0` is required so `0 == 0` cannot open destructive work. Additionally, The production destructive gate is complete only when `universe_authoritative` is true, `frontier_namespaces > 0`, and `frontier_proven == frontier_namespaces`; `suppress_destructive` is then `!report.anomalies.empty() || !carried_holds.empty() || frontier_incomplete`. Additionally, At the destructive gate call site, `!suppress_destructive` implies `frontier_complete`, which implies `universe_authoritative`; therefore `planManifestCursorPage` receives an authoritative recovery argument whenever the gate opens."}
{"cluster_id": "chunk_005-143", "topic": "Every carried hold is pool-wide suppression because blob in-degree is po", "canonical_claim": "Every carried hold is pool-wide suppression because blob in-degree is pool-wide; a hold retries its exact offending position and clears only after that position is folded and the result is adopted in `gc/state`."}
{"cluster_id": "chunk_005-144", "topic": "With healthy authoritative `gc/state`, REBUILD carries all holds verbati", "canonical_claim": "With healthy authoritative `gc/state`, REBUILD carries all holds verbatim; with missing or undecodable `gc/state`, REBUILD has authority to reconstruct only, performs zero catalog deletions, and returns."}
{"cluster_id": "chunk_005-145", "topic": "A manifest from epoch E is deletable only after the cursor consumes epoc", "canonical_claim": "A manifest from epoch E is deletable only after the cursor consumes epoch E's seal and no unconsumed tail record above the cursor names it as a removal target; any uncertainty retains it."}
{"cluster_id": "chunk_005-146", "topic": "orphan-manifest-neutral-nomination", "canonical_claim": "Orphan-manifest deletion accounts for the manifest's exact blob/source edges through a separate accounting-neutral retirement input, consumes no ref-transaction ordinal, and does not require every affected blob to have zero total in-degree. Additionally, R3 orphan-manifest cleanup exact-GETs and decodes the manifest first, feeds its `BlobRef`s through a neutral nomination input, adopts nominations in the round's `gc/state` CAS, and only then performs exact-token deletion. Additionally, The orphan sweep's neutral nomination input is accounting-neutral: it does not consume ref-transaction ordinals or increment unmatched-remove accounting for an already-absent edge, while retaining exact source-edge identities. Additionally, Exact GET and decode nominate an orphan manifest as a neutral candidate without consuming a B2 ordinal or entering unmatched-remove accounting. Additionally, `CommitRound` adopts an orphan nomination in the round's `gc/state` CAS, and only an adopted nomination may reach exact-token manifest deletion. Additionally, If a process dies after orphan-nomination adoption and before manifest deletion, the manifest remains leak-safe and rediscoverable; the protocol does not promise that the dead process retries deletion. Additionally, Orphan-manifest nomination exact-GETs and decodes the captured body, validates `refMatchesBody` and `manifestNamespaceMatches`, derives `BlobSourceRetirement` entries, and performs no deletion during planning. Additionally, Orphan source retirements are passed to `foldDeltasIntoGeneration` as a vector distinct from the `BlobDelta` stream, preserving accounting neutrality; exact deletion occurs only after the round's `gc/state` CAS and only when destructive work is not suppressed."}
{"cluster_id": "chunk_005-147", "topic": "mandatory-catalog-frontier-targets", "canonical_claim": "Stage A hard-wires `frontier_incomplete = true` through `UniversePolicy::kDefault = StageA_Suppressed`, so `suppress_destructive` holds on every production round and all delete families remain inert until Stage B supplies the catalog universe. Additionally, The pool-wide ref-delta fold makes every catalog `Live`/`Removing` life a mandatory target, snapshots exact `_ckpt.committed_through`, walks arithmetically to that bound, and treats every missing required key as a durable hold independent of `LIST`."}
{"cluster_id": "chunk_005-148", "topic": "Epochs are crossed only by consuming a seal at the expected id", "canonical_claim": "Epochs are crossed only by consuming a seal at the expected id; an `EpochSeal` is counted as applied with `produced=false`, and a later-epoch record reachable before its seal is held rather than guessed across."}
{"cluster_id": "chunk_005-149", "topic": "Per-namespace fold failures such as corrupt bodies clamp or hold that na", "canonical_claim": "Per-namespace fold failures such as corrupt bodies clamp or hold that namespace, while the rest of the round continues; only a key unattributable to any namespace aborts the whole round."}
{"cluster_id": "chunk_005-150", "topic": "classification-four-hold-fields", "canonical_claim": "`ShardCoverage` classification 4 requires `HoldReason`, `offending_position`, `retry_count`, and `next_retry_round`, and those fields are forbidden for other classifications; malformed hold grammar is `CORRUPTED_DATA`. Additionally, Classification 4 requires bounded hold fields and forbids those fields for other classifications; unknown, duplicate, or invalid field combinations must be rejected."}
{"cluster_id": "chunk_005-151", "topic": "hold-clear-after-adoption", "canonical_claim": "A hold clears only after folding through its `offending_position` and adopting the result in `gc/state`; if an above-cursor witness disappears while the gap remains, the hold changes to `WitnessDisappeared` and remains held. Additionally, A successful frontier walk clears a hold only when folding through the offending position and adopting that result in gc/state; reaching another absent expected-next does not clear it."}
{"cluster_id": "chunk_005-152", "topic": "frontier-exact-get-and-budget", "canonical_claim": "The fold's `DestructiveGate` requires a frontier proof for every known namespace; a quiet namespace is probed with one exact GET of `expected_next(cursor)`, and budget exhaustion suppresses all destruction even if safe cursor progress is published. Additionally, Every destructive round must exact-GET cursor+1 for every Live or Removing catalog entry and suppress all destructive work if the common budget expires before every namespace has a frontier proof."}
{"cluster_id": "chunk_005-153", "topic": "manifest-deletion-premise", "canonical_claim": "The sweep's shared `manifestDeletionPremise` requires both consumption of the manifest's epoch seal and no unconsumed tail removal target; unreached frontier, budget exhaustion, or a hold retains the manifest. Additionally, The orphan-manifest sweep deletes only with seal-consumed and no-tail-removal evidence; when the evidence is uncertain, it retains the object."}
{"cluster_id": "chunk_005-154", "topic": "rebuild-orphan-blob-retention", "canonical_claim": "REBUILD removes the old blobs-prefix condemnation path and condemns nothing; this deliberately leaves manifest-less orphan blobs unreclaimable until the separate build/upload registry work in R4. Additionally, REBUILD does not condemn objects that its traversal did not reach; a manifest-less orphan blob remains unreclaimable until register R4 can enumerate in-flight build/upload operations, and is reported by fsck as `unaccounted`. Additionally, A physical blob LIST during REBUILD may nominate zero-edge blobs for condemnation, but omitted blobs remain leaked and are not treated as proof of absence."}
{"cluster_id": "chunk_005-155", "topic": "stage-a-invisible-namespace-safety", "canonical_claim": "Under the Stage A default, a namespace absent from both the hint and `gc/state` can hold an acknowledged shared-blob `+1` while another namespace exposes a `-1`, yet the required result is zero deletes because `StageA_Suppressed` prevents data loss. Additionally, Production destruction was suppressed in Stage A because pool-wide blob in-degree could contain an acknowledged `+1` from an invisible namespace while a visible `-1` was folded; suppression may be lifted only after the namespace universe is authoritative."}
{"cluster_id": "chunk_005-156", "topic": "The hot GC round completes one `LIST(cas/ns/stream/)`, then takes one au", "canonical_claim": "The hot GC round completes one `LIST(cas/ns/stream/)`, then takes one authoritative full-catalog `{token, value}` cut; `Live` and `Removing` entries in that cut are admitted, while listed ids absent from the later cut are inert dead-life debris and are not admitted or a reason to defer."}
{"cluster_id": "chunk_005-157", "topic": "pre-fold-drain-and-fresh-cut", "canonical_claim": "Every GC invocation acquires and validates the lease and adopted seal, conclusively drains eligible `Removing` rows, completes `LIST(cas/ns/stream/)`, takes one fresh full-catalog cut, builds the ref walk plan, and only then decides `DEFER` or performs fold, seal, and adoption. Additionally, Before any fold, `REBUILD`, or `DEFER` decision, the production GC actor must read the currently adopted parent seal, drain every eligible catalog row, conclusively resolve every exact CAS, and take a fresh catalog cut. Additionally, Before `DEFER`, ordinary fold adoption, or `REBUILD` adoption, GC actor B must read the authoritative parent, resolve every eligible exact catalog CAS, complete the hot-stream `LIST`, take one fresh authoritative catalog cut, and build the ref-walk plan. Additionally, A pre-fold GC invocation must resolve all eligible exact catalog CAS operations, complete hot `LIST`, take one fresh cut, and build the sole ref plan before fold, `REBUILD`, or `DEFER`; ambiguous results cannot be treated as completion."}
{"cluster_id": "chunk_005-158", "topic": "A complete `Removing` row may be exact-CAS-deleted only when the matchin", "canonical_claim": "A complete `Removing` row may be exact-CAS-deleted only when the matching adopted parent life row contains cleanup evidence and no durable hold; ambiguous or conflicting catalog CAS outcomes are resolved conclusively before successor publication."}
{"cluster_id": "chunk_005-159", "topic": "namespace-janitor-page", "canonical_claim": "The perpetual janitor scans `LIST(cas/ns/)` in bounded pages, stores one cleanup-only opaque pagination token in `GcMaintenanceState`, and takes a fresh catalog read after each page before classifying and exact-token-deleting candidates. Additionally, The perpetual `NamespaceJanitor::runOnePage` is invoked every GC round, is bounded by `page_budget`, is fenced by `gc/state` lease owner and sequence, and is leak-only."}
{"cluster_id": "chunk_005-160", "topic": "A valid janitor page under destructive suppression does not advance its ", "canonical_claim": "A valid janitor page under destructive suppression does not advance its cursor, preventing alternating `DEFER` and fold rounds from permanently skipping a dead page."}
{"cluster_id": "chunk_005-161", "topic": "Destructive cleanup of a current catalog life revalidates the life and f", "canonical_claim": "Destructive cleanup of a current catalog life revalidates the life and fence immediately before every per-key `deleteExact`; the dead-life janitor may use a page-level catalog cut because catalog-before-object publication and non-reused `life_id` provide the stronger ordering proof."}
{"cluster_id": "chunk_005-162", "topic": "In the fold model, a page scan is only a hint that may omit keys", "canonical_claim": "In the fold model, a page scan is only a hint that may omit keys; arithmetic `WalkStep` advances the cursor by expected transaction id and supplies the authoritative delta candidates."}
{"cluster_id": "chunk_005-163", "topic": "The fold's blob in-degree is pool-wide and is computed from fixed `EdgeO", "canonical_claim": "The fold's blob in-degree is pool-wide and is computed from fixed `EdgeOf` roles over folded keys; condemnation requires every table frontier proof and no hold, while deletion also requires no hold and two-phase pacing."}
{"cluster_id": "chunk_005-164", "topic": "The stale-leader cleanup model makes recreation safe structurally", "canonical_claim": "The stale-leader cleanup model makes recreation safe structurally: stale deletes capture and target the old incarnation, while reuse of an incarnation is the sabotage that violates rebirth safety."}
{"cluster_id": "chunk_005-165", "topic": "stage-a-universe-policy", "canonical_claim": "Stage A sets `UniversePolicy::kDefault` to `StageA_Suppressed`, so every destructive GC site is suppressed by construction and no object is deleted during Stage A. Additionally, `UniversePolicy::kDefault` remains `StageA_Suppressed`; `AuthoritativeForTest` is the test-only policy value read by `Gc::fold` to set `universe_authoritative`."}
{"cluster_id": "chunk_005-166", "topic": "The destructive-gate suppression covers nine GC deletion sites", "canonical_claim": "The destructive-gate suppression covers nine GC deletion sites: pre-CAS blob deletion, hand-off generation reclaim, owner-removed manifest bodies, generation pruning, orphan-manifest sweep invocation, covered-log and superseded-snapshot cleanup, namespace-cleanup pending work, `_ckpt` known-key deletion, and merge-side pending deletes."}
{"cluster_id": "chunk_005-167", "topic": "A 404 while folding a body the round already decided to read is treated ", "canonical_claim": "A 404 while folding a body the round already decided to read is treated as a concurrent delete and recorded while the round continues; this behavior remains required because throwing would wedge GC for the pool."}
{"cluster_id": "chunk_005-168", "topic": "`CaGcAckFloorCore`, `CaGcAckFloorZombie`, and `CaGcCondemnMarkerGate` as", "canonical_claim": "`CaGcAckFloorCore`, `CaGcAckFloorZombie`, and `CaGcCondemnMarkerGate` assume a complete fold cut; v9 turns that assumption into arithmetic discovery plus a durable classification-4 hold, so an undeclared skipped suffix becomes structurally unrepresentable without rewriting those orthogonal models."}
{"cluster_id": "chunk_005-169", "topic": "An unreleased binding becomes eligible when a successor epoch exists and", "canonical_claim": "An unreleased binding becomes eligible when a successor epoch exists and is reclaimed by `sweepStalePrecommitsNow`; it is unbounded only while no successor mounts the namespace or every successor sweep fails, so a dedicated fsck class is unnecessary."}
{"cluster_id": "chunk_005-170", "topic": "The relink TLA model must name `CommittedEdgesAreGcVisible`", "canonical_claim": "The relink TLA model must name `CommittedEdgesAreGcVisible`: a committed ref edge durable before a removal is appended must be observed by every GC fold that observes the removal; the v9 chain models are responsible for discharging it."}
{"cluster_id": "chunk_005-171", "topic": "LIST may discover old namespace-file debris only for lazy cleanup", "canonical_claim": "LIST may discover old namespace-file debris only for lazy cleanup; LIST omission may leak storage but must never affect file visibility, namespace rebirth, or deletion safety."}
{"cluster_id": "chunk_005-172", "topic": "Cleanup retains the checkpoint base's matching non-seal `_log` while `_c", "canonical_claim": "Cleanup retains the checkpoint base's matching non-seal `_log` while `_ckpt` names that base; older covered logs remain deletable."}
{"cluster_id": "chunk_005-173", "topic": "Namespace-file cleanup removes `_files` from mandatory namespace-removal", "canonical_claim": "Namespace-file cleanup removes `_files` from mandatory namespace-removal LIST deletion, uses exact-token deletion with the deposited incarnation, and never re-derives incarnation from the current catalog entry during resumed cleanup."}
{"cluster_id": "chunk_005-174", "topic": "`Gc", "canonical_claim": "`Gc::foldManifestEdges` at `Gc/CasGc.cpp:1007-1018` performs one `backend.head(key)` followed by one `backend.get(key)` per manifest edge, and `CasRefManifestBodyFoldGets` counts the GET half."}
{"cluster_id": "chunk_005-175", "topic": "The `Removing` to `RemovalReady` transition must refuse while a hold wit", "canonical_claim": "The `Removing` to `RemovalReady` transition must refuse while a hold with `ShardCoverage::classification == 4` exists for the namespace."}
{"cluster_id": "chunk_005-176", "topic": "A GC round whose catalog cut predates the `Removing` to `RemovalReady` C", "canonical_claim": "A GC round whose catalog cut predates the `Removing` to `RemovalReady` CAS may write the cursor once more; the next round must prune it, and final deletion must revalidate the adopted seal and tolerate this one stale-cut round."}
{"cluster_id": "chunk_005-177", "topic": "captured-namespace-life", "canonical_claim": "Cleanup must capture `NamespaceLifeId` at deposition and never re-derive it from the catalog during a resumed or stale pass. Additionally, A deposited cleanup item stores `NamespaceLifeId` and `remove_txn_id`, and every resumed or stale cleanup pass uses those captured values directly."}
{"cluster_id": "chunk_005-178", "topic": "A pending cleanup item performs one bounded, suppression-aware, best-eff", "canonical_claim": "A pending cleanup item performs one bounded, suppression-aware, best-effort pass with exact-token deletion; `NotFound`, token mismatch, or LIST omission retains or defers work and never becomes a safety conclusion."}
{"cluster_id": "chunk_005-179", "topic": "Terminal-owned manifests are cleaned from exact `ManifestId` values in t", "canonical_claim": "Terminal-owned manifests are cleaned from exact `ManifestId` values in the terminal record after decrements are durable; the removal pass does not LIST and delete the shared manifest namespace prefix."}
{"cluster_id": "chunk_005-180", "topic": "GC may CAS `Removing` to `RemovalReady` only when the matching cleanup i", "canonical_claim": "GC may CAS `Removing` to `RemovalReady` only when the matching cleanup item is durably `Completed`; it must not infer completion merely from item absence."}
{"cluster_id": "chunk_005-181", "topic": "A `RemovalReady` round does not probe, fold, carry, or recreate the name", "canonical_claim": "A `RemovalReady` round does not probe, fold, carry, or recreate the namespace cursor, and removes the matching completed cleanup item and shard-0 cursor together in the adopted seal."}
{"cluster_id": "chunk_005-182", "topic": "The `_cleanup` marker can be removed because stale cleanup targets only ", "canonical_claim": "The `_cleanup` marker can be removed because stale cleanup targets only the deposited life-qualified ref and `_files` prefixes or exact old terminal-owned manifests, never a namespace-wide prefix that a new life can create."}
{"cluster_id": "chunk_005-183", "topic": "seal-capacity-accounting", "canonical_claim": "The catalog admission reservation must cover non-per-namespace seal data such as `blob_target_runs` and `condemned_summary`; entry ordering does not remove those capacity terms. Additionally, The `btr` per-run-segment rows and `cnd` per-gc-shard rows are charged neither in `worstCaseEntryFoldReservationBytes`'s fixed term nor in its per-entry term."}
{"cluster_id": "chunk_005-184", "topic": "An absent canonical life is inert debris and must never become a pool-su", "canonical_claim": "An absent canonical life is inert debris and must never become a pool-suppressing damage anomaly."}
{"cluster_id": "chunk_005-185", "topic": "After probe A removal, each folding round must perform exactly one full ", "canonical_claim": "After probe A removal, each folding round must perform exactly one full `cas/ns/stream/` enumeration; the bounded `cas/ns/` janitor page is counted separately."}
{"cluster_id": "chunk_005-186", "topic": "Per-round GC work caps must leave excess work in the durable pipeline fo", "canonical_claim": "Per-round GC work caps must leave excess work in the durable pipeline for later rounds, never drop or re-derive it, and the per-key row/life/lease validation before each exact delete must not be amortized or weakened."}
{"cluster_id": "chunk_005-187", "topic": "Orphan-planner budget exhaustion must fail closed by retaining candidate", "canonical_claim": "Orphan-planner budget exhaustion must fail closed by retaining candidates, recording a counted retained cause, and leaving the cursor before the first undecided key; every round must either decide at least one candidate or advance the cursor."}
{"cluster_id": "chunk_005-188", "topic": "`deletePrefixWholesale` and cleanup families must consume the round budg", "canonical_claim": "`deletePrefixWholesale` and cleanup families must consume the round budget, and `snap_pruned_through` may advance only after a prefix is fully drained."}
{"cluster_id": "chunk_005-189", "topic": "Injected holds or anomalies must leave every irreversible delete family ", "canonical_claim": "Injected holds or anomalies must leave every irreversible delete family inert for their rounds while the round still completes."}
{"cluster_id": "chunk_005-190", "topic": "A missing attribution in orphan nomination must suppress destructive wor", "canonical_claim": "A missing attribution in orphan nomination must suppress destructive work rather than select a fallback owner."}
{"cluster_id": "chunk_005-191", "topic": "The perpetual dead-life janitor owns namespace-life debris, while the or", "canonical_claim": "The perpetual dead-life janitor owns namespace-life debris, while the orphan-manifest sweep owns orphan manifest bytes; both are cleanup mechanisms rather than identity or rebirth gates."}
{"cluster_id": "chunk_005-192", "topic": "`INV_NO_RETIRE_UNCERTAIN_GRANT` prevents a writer cleanup duty from disa", "canonical_claim": "`INV_NO_RETIRE_UNCERTAIN_GRANT` prevents a writer cleanup duty from disappearing while the grant outcome is unknown; successor cleanup remains the seal-derived path for durable precommits left by a dead predecessor."}
{"cluster_id": "chunk_005-193", "topic": "cleanup-authority-revalidation-tests", "canonical_claim": "Ref cleanup reads the authority from `folded.catalog_cut` rather than re-resolving it, and the `CasRefGcCleanupAuthority` tests cover catalog-token and GC-fence moves before the first delete and between two deletes. Additionally, The `CasRefGcCleanupAuthority` tests pin current-life destructive per-key `deleteExact` revalidation in the GC/cleanup layer; those class-3 catalog reads are not `CasRefLedger.cpp` sites."}
{"cluster_id": "chunk_005-194", "topic": "CAS decommissioning rejects a `Removing` row without its `_ckpt` object ", "canonical_claim": "CAS decommissioning rejects a `Removing` row without its `_ckpt` object with `CORRUPTED_DATA`, resumes a `Removing` row through `admin->dropNamespace(life)`, and leaves catalog deletion to GC."}
{"cluster_id": "chunk_005-195", "topic": "decommission-gc-round-callback", "canonical_claim": "CAS decommissioning requests a GC round after partial progress via a `request_gc_round` callback guarded by `SCOPE_EXIT`, including fail-closed exits. Additionally, When decommission reaches a Removing state before a later namespace fails closed, a SCOPE_EXIT callback preserves the GC liveness signal by invoking request_gc_round; the callback only wakes the serialized GC worker and does not perform catalog work."}
{"cluster_id": "chunk_005-196", "topic": "GC driving uses one leader via `gc", "canonical_claim": "GC driving uses one leader via `gc.gc_round` or `gc.gc_drive_round`; callers must not drive both leaders concurrently because that can cause a concurrent-leader reclaim leak."}
{"cluster_id": "chunk_005-197", "topic": "In `ca-fsck` vocabulary, `dangling` means referenced but missing and is ", "canonical_claim": "In `ca-fsck` vocabulary, `dangling` means referenced but missing and is the INV-NO-LOSS data-loss class, whereas `unreachable`/`awaiting-gc` means present, unreferenced, and awaiting reclamation."}
{"cluster_id": "chunk_005-198", "topic": "A regular GC round is all-or-nothing", "canonical_claim": "A regular GC round is all-or-nothing: `fold` produces one `gc/state` CAS followed by cleanup, and cleanup occurs only after the commit point because a durable `Removed` snapshot must precede deletion of covered logs."}
{"cluster_id": "chunk_005-199", "topic": "`foldManifestEdges` returns false without deltas when a manifest body is", "canonical_claim": "`foldManifestEdges` returns false without deltas when a manifest body is absent, and the caller is intended to clamp the table rather than abort the round or advance its cursor."}
{"cluster_id": "chunk_005-200", "topic": "The manifest-cleanup GC phase is a one-shot pipeline", "canonical_claim": "The manifest-cleanup GC phase is a one-shot pipeline: its `-1` edge is consumed in the same CAS that produces `mf_cleanup`, so a per-round cap can permanently skip entries rather than defer them to the next intake."}
{"cluster_id": "chunk_005-201", "topic": "Unadopted GC attempts are invisible to readers, resume, prune, and `Reti", "canonical_claim": "Unadopted GC attempts are invisible to readers, resume, prune, and `RetireView`, so a deposed leader cannot collide on a final-key seal; unadopted attempt debris is reclaimed by wholesale `gc/gen/<g>/` retention pruning."}
{"cluster_id": "chunk_005-202", "topic": "GC condemnation is fail-closed", "canonical_claim": "GC condemnation is fail-closed: a candidate absent at condemn time, including a HEAD 404, is skipped and no token is fabricated."}
{"cluster_id": "chunk_005-203", "topic": "A missing or invalid committed/promoted new-binding manifest body clamps", "canonical_claim": "A missing or invalid committed/promoted new-binding manifest body clamps the affected GC shard with `classification = 4`; precommit bindings are non-activating, old-binding removals use fold-sealed edges, and GC never reads a deleted manifest body."}
{"cluster_id": "chunk_005-204", "topic": "The GC publish-order invariant requires the retired-list runs for round ", "canonical_claim": "The GC publish-order invariant requires the retired-list runs for round K to be durable before the single `gc/state` CAS publishes `round := K` and `retired_refs`; refs and round are published together so round K always has a loadable retired list K."}
{"cluster_id": "chunk_005-205", "topic": "A patch-part manifest is a first-class reachability root that keeps both", "canonical_claim": "A patch-part manifest is a first-class reachability root that keeps both its delta blobs and the referenced base blobs alive transitively."}
{"cluster_id": "chunk_005-206", "topic": "The delete guard in `GComplete` must force a fold for any pending delta ", "canonical_claim": "The delete guard in `GComplete` must force a fold for any pending delta touching the candidate blob, and `unfolded` carries both `add` and `del` signs."}
{"cluster_id": "chunk_005-207", "topic": "A newborn shard is fenced to the current `gcRound` at birth, and GC disc", "canonical_claim": "A newborn shard is fenced to the current `gcRound` at birth, and GC discovers and fences every present shard by LIST each round."}
{"cluster_id": "chunk_005-208", "topic": "`CatalogTargets` must be derived only from the exact `Live`/`Removing` c", "canonical_claim": "`CatalogTargets` must be derived only from the exact `Live`/`Removing` catalog plan key set, and `BeginRound` must freeze exact `_ckpt.committed_through` as `roundCte`."}
{"cluster_id": "chunk_005-209", "topic": "`WalkStep` may exact-read only keys through the inclusive `roundCte` bou", "canonical_claim": "`WalkStep` may exact-read only keys through the inclusive `roundCte` bound."}
{"cluster_id": "chunk_005-210", "topic": "If a required key at or below `roundCte` is missing, the fold must reass", "canonical_claim": "If a required key at or below `roundCte` is missing, the fold must reassert a hold without consulting `maxSeen` or any `LIST` observation."}
{"cluster_id": "chunk_005-211", "topic": "`ScanComplete` may succeed only after every catalog target reaches CTE o", "canonical_claim": "`ScanComplete` may succeed only after every catalog target reaches CTE or resolves as held."}
{"cluster_id": "chunk_005-212", "topic": "A target held at the start of a fold round must follow the same resoluti", "canonical_claim": "A target held at the start of a fold round must follow the same resolution path even when `LIST` returns no information."}
{"cluster_id": "chunk_005-213", "topic": "`CteExact` requires `committedThrough[t] = MaxDurable(t)` for every tabl", "canonical_claim": "`CteExact` requires `committedThrough[t] = MaxDurable(t)` for every table, and `RoundCteNoRegression` requires `roundCte[t] <= committedThrough[t]`."}
{"cluster_id": "chunk_005-214", "topic": "`HoldReleaseRequiresFold` requires that no fold debt remains before a ho", "canonical_claim": "`HoldReleaseRequiresFold` requires that no fold debt remains before a hold can be released."}
{"cluster_id": "chunk_005-215", "topic": "physical-life-id-janitor-target", "canonical_claim": "The `cas/ns/` janitor must capture the exact physical life id encoded in a returned `LIST` key and continue targeting that id when a delayed delete resumes after same-name rebirth. Additionally, The janitor must not re-resolve a logical name at delete time; exact physical identity captured at nomination is the safety boundary. Additionally, The perpetual namespace janitor captures a physical life id from a `LIST` page and delayed deletion never derives its target from a reborn logical name."}
{"cluster_id": "chunk_005-216", "topic": "Fresh life-id allocation and physical-id capture are independent require", "canonical_claim": "Fresh life-id allocation and physical-id capture are independent requirements: reusing an id or re-deriving the target from the logical name can delete a live successor's data."}
{"cluster_id": "chunk_005-217", "topic": "A janitor nomination is valid only when it came from an actual listed ph", "canonical_claim": "A janitor nomination is valid only when it came from an actual listed physical id; `NominationCapturedPhysicalId` checks this provenance."}
{"cluster_id": "chunk_005-218", "topic": "An old catalog request that returns after lease takeover is safe only if", "canonical_claim": "An old catalog request that returns after lease takeover is safe only if the successor actor consumed the token first or no successor invalidated the proof under which the old request lands."}
{"cluster_id": "chunk_005-219", "topic": "A ready parent cannot proceed to normal fold, `REBUILD`, `DEFER`, or a f", "canonical_claim": "A ready parent cannot proceed to normal fold, `REBUILD`, `DEFER`, or a fresh-cut continuation while an eligible catalog deletion remains unresolved."}
{"cluster_id": "chunk_005-220", "topic": "If an exact catalog row is still `Removing` after an ambiguous or confli", "canonical_claim": "If an exact catalog row is still `Removing` after an ambiguous or conflicting storage result, the actor must refresh the full-catalog token and retry; only an absent row is conclusive completion."}
{"cluster_id": "chunk_005-221", "topic": "A missing or undecodable authority cannot be replaced by a seal discover", "canonical_claim": "A missing or undecodable authority cannot be replaced by a seal discovered through enumeration for catalog deletion; honest `REBUILD` restores authority and returns, leaving the drain to a later invocation."}
{"cluster_id": "chunk_005-222", "topic": "The authoritative catalog cut used for ref-plan intake must be taken aft", "canonical_claim": "The authoritative catalog cut used for ref-plan intake must be taken after the completed hot `LIST`; a cut taken before `LIST` can omit a current successor and classify it as dead debris."}
{"cluster_id": "chunk_005-223", "topic": "A predecessor listed later but absent from the authoritative cut is coun", "canonical_claim": "A predecessor listed later but absent from the authoritative cut is counted once, excluded from the life-id plan, and must never be used as a `DEFER` reason."}
{"cluster_id": "chunk_005-224", "topic": "`IntakeConsumesFreshPostDrainCut` requires ref-plan intake to consume th", "canonical_claim": "`IntakeConsumesFreshPostDrainCut` requires ref-plan intake to consume the exact token/value pair exported by `TakeFreshCut`, including life id and lifecycle state, and its plan projection must equal the walkable life-id set."}
{"cluster_id": "chunk_005-225", "topic": "An exact delete or external catalog resolution that invalidates the full", "canonical_claim": "An exact delete or external catalog resolution that invalidates the full-catalog token requires a complete serial rescan before the next candidate or successor decision."}
{"cluster_id": "chunk_005-226", "topic": "The all-row pre-fold drain may decide only when its remaining eligible-r", "canonical_claim": "The all-row pre-fold drain may decide only when its remaining eligible-row set is empty; `CompletionDrained` defines `phase = \"done\" => remaining = {}`."}
{"cluster_id": "chunk_005-227", "topic": "GC may delete a ref log only when `_ckpt` covers it", "canonical_claim": "GC may delete a ref log only when `_ckpt` covers it; deleting a durable id below an unchanged checkpoint token makes a 404 indistinguishable from the frontier and can cause recovery to seal over adopted data."}
{"cluster_id": "chunk_005-228", "topic": "The in-degree reducer must not be changed to fix stale edges", "canonical_claim": "The in-degree reducer must not be changed to fix stale edges; the defect is upstream in the orphan-sweep premise."}
{"cluster_id": "chunk_005-229", "topic": "The GC protocol includes leader election and lease, fold → retire → fenc", "canonical_claim": "The GC protocol includes leader election and lease, fold → retire → fence → recheck → trim/reclaim rounds, orphan removal through source-edge in-degree, exact-token deletes, shard incarnation, attempt-scoped generations, and snap pruning."}
{"cluster_id": "chunk_005-230", "topic": "The canonical GC core requires exact-token deletion, atomic cascade with", "canonical_claim": "The canonical GC core requires exact-token deletion, atomic cascade with deletion, registry fencing from the committed namespace universe, and fresh observation of stale dependency evidence before publication."}
{"cluster_id": "chunk_005-231", "topic": "clamp-suppresses-deletion", "canonical_claim": "A GC round that held back an unreadable shard must make no destructive decision, and the clamp-suppression guard prevents physical deletion while an unfolded delta could still touch the blob. Additionally, When any shard is clamped, GC performs no graduation and no pending deletes; a clamp-free pass resumes deletes and emits a `gc_fold_clamp` event."}
{"cluster_id": "chunk_005-232", "topic": "two-phase-graduation", "canonical_claim": "Two-phase graduation publishes `delete_pending` with a round CAS before physical delete, preventing a deposed leader's stale pass from deleting a live blob. Additionally, Two-phase graduation uses `delete_pending`: pre-CAS deletes apply only to previously published pending entries, and deletion lags condemnation by one pass."}
{"cluster_id": "chunk_005-233", "topic": "A durable never-reused per-shard incarnation plus a newborn shard fenced", "canonical_claim": "A durable never-reused per-shard incarnation plus a newborn shard fenced to the current GC round replaces the namespace registry's safety role, but reclaim still waits for the shard journal to be fully folded."}
{"cluster_id": "chunk_005-234", "topic": "A due GC graduation must force a fold before physical deletion, while a ", "canonical_claim": "A due GC graduation must force a fold before physical deletion, while a non-destructive round may re-adopt the sealed in-degree snapshot; deferral is bounded."}
{"cluster_id": "chunk_005-235", "topic": "CAS GC computes blob in-degrees from manifest-to-blob source edges, cond", "canonical_claim": "CAS GC computes blob in-degrees from manifest-to-blob source edges, condemns zero-in-degree blobs, and later deletes them with exact-token conditional deletes so a concurrent re-upload wins."}
{"cluster_id": "chunk_005-236", "topic": "A GC round takes or renews the leader lease, collects source edges, fold", "canonical_claim": "A GC round takes or renews the leader lease, collects source edges, folds per-shard in-degrees, condemns new zero-in-degree blobs, exact-token-deletes previously condemned blobs whose state is unchanged, and seals the round with one CAS on `gc/state`."}
{"cluster_id": "chunk_005-237", "topic": "After forced GC, `unreachable` must be zero unless a scenario deliberate", "canonical_claim": "After forced GC, `unreachable` must be zero unless a scenario deliberately abandons writes; any residual must be classified by object class and proven bounded and expected."}
{"cluster_id": "chunk_005-238", "topic": "S03 requires idle GC duration to scale with changed owner transitions ra", "canonical_claim": "S03 requires idle GC duration to scale with changed owner transitions rather than total live blobs, with `CASBlobList` equal to zero in regular journal-driven rounds unless an orphan sweep or `fsck` intentionally lists blobs."}
{"cluster_id": "chunk_005-239", "topic": "S05 requires idle tables not to dominate GC CPU or S3 GET counts", "canonical_claim": "S05 requires idle tables not to dominate GC CPU or S3 GET counts; ref body reads must be driven by new logs since per-table cursors rather than table count, and cursor regression must be reported."}
{"cluster_id": "chunk_005-240", "topic": "S16 requires resurrection to use writer-owned source bytes rather than a", "canonical_claim": "S16 requires resurrection to use writer-owned source bytes rather than a condemned object, and requires `blob_reuse_resurrect` to fire at least once during the hot-content cycle."}
{"cluster_id": "chunk_005-241", "topic": "Every per-round GC artifact is keyed by `(gen, attempt = lease", "canonical_claim": "Every per-round GC artifact is keyed by `(gen, attempt = lease.seq)`, so artifacts from a deposed leader remain under an unadopted attempt."}
{"cluster_id": "chunk_005-242", "topic": "After removal of `gc_round_manifest_cleanup_budget`, the `manifest_delet", "canonical_claim": "After removal of `gc_round_manifest_cleanup_budget`, the `manifest_deletes` phase drains all of `folded.mf_cleanup` unconditionally; the `skipped_budget` metric was dropped because it became structurally zero, while `attempted`, `deleted`, and `suppressed` remain."}
{"cluster_id": "chunk_005-243", "topic": "CAS garbage collection is a background reachability process driven by an", "canonical_claim": "CAS garbage collection is a background reachability process driven by an incarnation-token protocol using compare-and-swap on opaque storage tokens and failing closed."}
{"cluster_id": "chunk_005-244", "topic": "`Cas", "canonical_claim": "`Cas::Gc` runs leader-paced reachability rounds with lease handling, crash resume, and precommit reclaim across fold, retire, fence, recheck, cascade, and trim phases."}
{"cluster_id": "chunk_005-245", "topic": "`Cas", "canonical_claim": "`Cas::RetireView` is a thread-safe read-side mirror of condemned tokens consulted by the publish gate."}
{"cluster_id": "chunk_005-246", "topic": "`Cas", "canonical_claim": "`Cas::RootsRegistry` is the authoritative namespace universe and GC `fence_round` record stored at the key `gc/registry`."}
{"cluster_id": "chunk_005-247", "topic": "The GC round sequence is fold, retire, fence, recheck, cascade-and-persi", "canonical_claim": "The GC round sequence is fold, retire, fence, recheck, cascade-and-persist, and journal trim; physical content deletion occurs at the recheck delete site."}
{"cluster_id": "chunk_005-248", "topic": "The `lease` phase is the only phase emitted by a `NotALeader` round", "canonical_claim": "The `lease` phase is the only phase emitted by a `NotALeader` round."}
{"cluster_id": "chunk_005-249", "topic": "The `pending_deletes` phase is the single content-delete site for exact-", "canonical_claim": "The `pending_deletes` phase is the single content-delete site for exact-token deletes of previously published `delete_pending` entries."}
{"cluster_id": "chunk_005-250", "topic": "The `namespace_cleanup` phase performs bounded physical reclamation unde", "canonical_claim": "The `namespace_cleanup` phase performs bounded physical reclamation under `cas/ns/` and is not a lifecycle gate."}
{"cluster_id": "chunk_005-251", "topic": "In `system", "canonical_claim": "In `system.cas_log`, `round`, `generation`, and `at_version` are zero when not applicable, with `at_version` representing the driving manifest shard version."}
{"cluster_id": "chunk_005-252", "topic": "The GC round asserts snapshot and journal coherence with the `B140-dangl", "canonical_claim": "The GC round asserts snapshot and journal coherence with the `B140-dangle` fail-closed guard before retiring candidates."}
{"cluster_id": "chunk_005-253", "topic": "Stage A Task 9 must test same-round no-delete after a post-probe +1, SOU", "canonical_claim": "Stage A Task 9 must test same-round no-delete after a post-probe +1, SOURCE-BACKED/TOKENED rematerialization with stale-token delete refusal, and TOKENLESS receiver-plus-one-before-source-release ordering"}
{"cluster_id": "chunk_005-254", "topic": "The destructive-call inventory must include pre-gate handoff_reclaim, tr", "canonical_claim": "The destructive-call inventory must include pre-gate handoff_reclaim, transport suppression before it, and add an individual zero-delete-under-hold test for every discovered delete family"}
{"cluster_id": "chunk_005-255", "topic": "Terminal fold cleanup must make one bounded suppression-aware best-effor", "canonical_claim": "Terminal fold cleanup must make one bounded suppression-aware best-effort attempt before exact _ckpt and catalog deletion, defer failure as leak-only janitor work, and assert backend-journal ordering"}
{"cluster_id": "chunk_005-256", "topic": "Current per-key deleteExact operations require catalog-token and fence r", "canonical_claim": "Current per-key deleteExact operations require catalog-token and fence revalidation before each key, with a credential-change race injected between two keys"}
{"cluster_id": "chunk_005-257", "topic": "The late-plus-one test needs an isolated seam that bypasses the final in", "canonical_claim": "The late-plus-one test needs an isolated seam that bypasses the final in-degree read, records RED, restores the guard, and records GREEN"}
{"cluster_id": "chunk_005-258", "topic": "GcStateProto", "canonical_claim": "GcStateProto.retired_refs is semantically load-bearing for the ack-floor writer gate; an old reader cannot safely ignore it without a compatibility gate or documented mixed-version rule"}
{"cluster_id": "chunk_005-259", "topic": "GcHeartbeat is exactly 24 bytes", "canonical_claim": "GcHeartbeat is exactly 24 bytes: a 16-byte big-endian GC owner id followed by an 8-byte big-endian heartbeat sequence, with no magic or version field"}
{"cluster_id": "chunk_005-260", "topic": "Each RunRef btr row carries the whole-object chained CityHash128 checksu", "canonical_claim": "Each RunRef btr row carries the whole-object chained CityHash128 checksum, and GC verifies that checksum before acting on the referenced run"}
{"cluster_id": "chunk_005-261", "topic": "gc_shards > 1 uses target-sharded data layout and a local reducer loop i", "canonical_claim": "gc_shards > 1 uses target-sharded data layout and a local reducer loop inside one Gc::fold call; production does not schedule reducer shards across replicas or workers"}
{"cluster_id": "chunk_005-262", "topic": "GcOutcomeLogProto makes destructive GC decisions idempotent and auditabl", "canonical_claim": "GcOutcomeLogProto makes destructive GC decisions idempotent and auditable by recording delete and recheck outcomes for replay and observability"}
{"cluster_id": "chunk_005-263", "topic": "RunRefProto", "canonical_claim": "RunRefProto.checksum in v2 is CityHash128 over the full stored run object and is used for adoption and fsck identity, not as a read-path guard"}
{"cluster_id": "chunk_005-264", "topic": "Every cas_run consumer reads the whole file sequentially, accumulates Ru", "canonical_claim": "Every cas_run consumer reads the whole file sequentially, accumulates RunRef.checksum over the read, verifies it before acting, and uses trailer n to detect line-boundary truncation"}
{"cluster_id": "chunk_005-265", "topic": "v3 deterministic objects are exactly the fold seal and GC runs", "canonical_claim": "v3 deterministic objects are exactly the fold seal and GC runs; they are raw, strict-key, fixed-order, sorted-iteration artifacts passed through putDeterministicArtifact"}
{"cluster_id": "chunk_005-266", "topic": "Quarantine metadata must persist first-held round, retry generation, nex", "canonical_claim": "Quarantine metadata must persist first-held round, retry generation, next probe, and offending anchor, and suppress_destructive must remain true for carried quarantine until successful revalidation"}
{"cluster_id": "chunk_005-267", "topic": "Every manifest named by an unconsumed removal record must be retained un", "canonical_claim": "Every manifest named by an unconsumed removal record must be retained until the fold cursor passes that removal record, unless the blob-edge decrement is persisted independently of the manifest body."}
{"cluster_id": "chunk_005-268", "topic": "Removal-tail protection scans must share the common round budget, cache ", "canonical_claim": "Removal-tail protection scans must share the common round budget, cache decoded protection across candidates in a namespace, and retain every affected candidate when the authoritative frontier is not reached."}
{"cluster_id": "chunk_005-269", "topic": "A carried hold must persist {Held, reason-code, retry/backoff state} per", "canonical_claim": "A carried hold must persist {Held, reason-code, retry/backoff state} per (namespace, incarnation), force an exact retry without a hint, and suppress pool-wide destruction until a successful frontier walk or explicit operator repair clears it."}
{"cluster_id": "chunk_005-270", "topic": "A forced REBUILD with a readable prior seal must carry every hold verbat", "canonical_claim": "A forced REBUILD with a readable prior seal must carry every hold verbatim into the new baseline; a missing or undecodable prior hold authority must cause a pool-wide destructive hold or refusal, never an ordinary deletion-capable baseline."}
{"cluster_id": "chunk_005-271", "topic": "The destructive-frontier proof must cover a delayed +1 after probing dur", "canonical_claim": "The destructive-frontier proof must cover a delayed +1 after probing during condemnation, graduation, and exact deletion; Condemned metadata requires rematerialization and an old exact-token delete cannot remove the new incarnation."}
{"cluster_id": "chunk_005-272", "topic": "Every fold-seal PUT must verify encodeFoldSeal(", "canonical_claim": "Every fold-seal PUT must verify encodeFoldSeal(...).size() <= fold_seal_cap, with the reservation estimator accounting for JSON escaping and framing."}
{"cluster_id": "chunk_005-273", "topic": "REBUILD must refuse when its prior fold seal is missing or undecodable, ", "canonical_claim": "REBUILD must refuse when its prior fold seal is missing or undecodable, naming pool recreation as the recovery path, unless a separate authoritative non-auto-clearable pool hold and operator repair transition is defined."}
{"cluster_id": "chunk_005-274", "topic": "An immutable node that landed but was never installed into _head is iner", "canonical_claim": "An immutable node that landed but was never installed into _head is inert garbage with no owner semantics and is never folded."}
{"cluster_id": "chunk_005-275", "topic": "GC exact-reads ref_catalog and every namespace _head, follows each sampl", "canonical_claim": "GC exact-reads ref_catalog and every namespace _head, follows each sampled tip back to its exact cursor, and suppresses all destructive work when the cursor is not an ancestor or a node is missing, malformed, or over budget."}
{"cluster_id": "chunk_005-276", "topic": "A safe GC cursor equals a sampled authoritative head, and every node on ", "canonical_claim": "A safe GC cursor equals a sampled authoritative head, and every node on the unique path from the old cursor to that head has been exact-read and folded."}
{"cluster_id": "chunk_005-277", "topic": "Snapshot cleanup may delete a listed node or snapshot only when point-re", "canonical_claim": "Snapshot cleanup may delete a listed node or snapshot only when point-readable facts prove the GC cursor covers it, the current head checkpoint covers it, and it is neither the current tip nor current checkpoint."}
{"cluster_id": "chunk_005-278", "topic": "The orphan-manifest sweep protects every current committed or precommit ", "canonical_claim": "The orphan-manifest sweep protects every current committed or precommit manifest and every removed manifest named by exact chain nodes above the GC cursor, and deletes only a listed eligible manifest with its exact current token."}
{"cluster_id": "chunk_005-279", "topic": "REBUILD exact-reads the catalog and every head at a sampled tip, emits b", "canonical_claim": "REBUILD exact-reads the catalog and every head at a sampled tip, emits baseline +1 edges for current owners, sets baseline cursors to sampled tips, refuses missing committed manifests or broken chains, and publishes with destructive work suppressed."}
{"cluster_id": "chunk_005-280", "topic": "`Cas", "canonical_claim": "`Cas::Gc`'s fold LISTs `casRefsPrefix()` and never `rootsPrefix()`, so `_files` keys do not reach a GC round even though `ContentAddressedTransaction::removeRecursive` handles both the ref-`_log` and `_files` key families."}
{"cluster_id": "chunk_005-281", "topic": "`ns_cleanup_items` is keyed by `{ns, remove_txn_id}`, and a namespace ca", "canonical_claim": "`ns_cleanup_items` is keyed by `{ns, remove_txn_id}`, and a namespace can retain multiple `nsc` rows because rows retire only after a later round observes completion artifacts; a ref-folding abort skips retirement."}
{"cluster_id": "chunk_005-282", "topic": "`Pool", "canonical_claim": "`Pool::listNamespaces` remains LIST-based and was not changed by the catalog-authoritative `Gc::discoverUniverse` switch."}
{"cluster_id": "chunk_005-283", "topic": "In the T1/T6a frontier logic, the `checkpoint_unusable` anomaly bucket r", "canonical_claim": "In the T1/T6a frontier logic, the `checkpoint_unusable` anomaly bucket represents the no-cursor mechanism; an unreadable `_ckpt` when a cursor exists is reported as `held` instead."}
{"cluster_id": "chunk_005-284", "topic": "`GcRoundWorkBudget", "canonical_claim": "`GcRoundWorkBudget::max_sweep_namespaces` counts cumulatively per round, not per PAGE; the two interpretations coincide only while a sweep takes one page per round."}
{"cluster_id": "chunk_005-285", "topic": "The grounded `CasListLiarEndToEnd` fixture writes recoverable checkpoint", "canonical_claim": "The grounded `CasListLiarEndToEnd` fixture writes recoverable checkpoints for `hidden` with `committed_through = RefTxnId{1,1}` and `visible` with `committed_through = RefTxnId{1,2}`."}
{"cluster_id": "chunk_005-286", "topic": "The list-liar test's load-bearing property is that `Gc", "canonical_claim": "The list-liar test's load-bearing property is that `Gc::fold` reads each expected ref-log or checkpoint by exact key, so a LIST omission cannot make a durable edge appear absent."}
{"cluster_id": "chunk_005-287", "topic": "The `CasGcFrontierGate` exact-key arm finds a hidden edge and saves its ", "canonical_claim": "The `CasGcFrontierGate` exact-key arm finds a hidden edge and saves its blob on a complete frontier; the round is not refused merely because the edge was hidden from LIST."}
{"cluster_id": "chunk_005-288", "topic": "gc-phase-order", "canonical_claim": "The GC log's `phase` column has these 18 execution-order values: `lease`, `pre_fold_ref_drain`, `heartbeat_floor`, `defer_decision`, `parent_seal_read`, `fold_ref_group`, `fold_seal_read`, `fold_ref_intake`, `fold_reduce`, `fold_seal_write`, `pending_deletes`, `meta_pool_wait`, `round_commit`, `handoff_reclaim`, `manifest_deletes`, `namespace_cleanup`, `ref_object_cleanup`, and `orphan_sweep`. Additionally, The 18 GC phases execute in this order: `lease`, `pre_fold_ref_drain`, `heartbeat_floor`, `defer_decision`, `parent_seal_read`, `fold_ref_group`, `fold_seal_read`, `fold_ref_intake`, `fold_reduce`, `fold_seal_write`, `pending_deletes`, `meta_pool_wait`, `round_commit`, `handoff_reclaim`, `manifest_deletes`, `namespace_cleanup`, `ref_object_cleanup`, `orphan_sweep`."}
{"cluster_id": "chunk_005-289", "topic": "Logs for one table must be folded in strict ID order, and a clamp must s", "canonical_claim": "Logs for one table must be folded in strict ID order, and a clamp must stop that table at the correct log; prefetching may change only when data is available, never what is applied."}
{"cluster_id": "chunk_005-290", "topic": "Observed LIST holes are short adjacent runs of 2–15 keys rather than app", "canonical_claim": "Observed LIST holes are short adjacent runs of 2–15 keys rather than approximately 1000-key pages, so completeness logic must not assume page-shaped holes."}
{"cluster_id": "chunk_005-291", "topic": "The build watermark `build_seq` must be monotone", "canonical_claim": "The build watermark `build_seq` must be monotone; a non-monotone sequence can move `min_active` backward and re-protect a condemned blob, causing a leak."}
{"cluster_id": "chunk_005-292", "topic": "A regular GC round acquires a lease, computes a heartbeat floor, folds s", "canonical_claim": "A regular GC round acquires a lease, computes a heartbeat floor, folds shard journals into attempt artifacts, performs only previously published `delete_pending` deletes, publishes retired-list runs, and advances all state with one `gc/state` CAS."}
{"cluster_id": "chunk_005-293", "topic": "`computeHeartbeatFloor` treats transient or unreadable mounts as live an", "canonical_claim": "`computeHeartbeatFloor` treats transient or unreadable mounts as live and holds `min_ack` down; only causally dead mounts are excluded, so injected S3 faults cannot raise the graduation floor."}
{"cluster_id": "chunk_005-294", "topic": "`deleteExact` propagates transient, throttle, and authentication errors ", "canonical_claim": "`deleteExact` propagates transient, throttle, and authentication errors instead of forgetting the entry, while `TokenMismatch` and `NotFound` remain terminal outcomes for the exact-token deletion attempt."}
{"cluster_id": "chunk_005-295", "topic": "If a lost acknowledgment follows a committed GC `gc/state` CAS, the retr", "canonical_claim": "If a lost acknowledgment follows a committed GC `gc/state` CAS, the retry reports `ABORTED`, while the next round reads the already advanced state and treats the earlier exact-token deletes as terminal and safe."}
{"cluster_id": "chunk_005-296", "topic": "The decommission sweep retains manifests when epoch 1's closing seal is ", "canonical_claim": "The decommission sweep retains manifests when epoch 1's closing seal is not consumed, leaving them to normal GC; this can produce nonzero `unreachable` with `dangling=0` and no data loss."}
{"cluster_id": "chunk_005-297", "topic": "`SYSTEM CAS DROP POOL MEMBER` is a writer operation that emits ordinary ", "canonical_claim": "`SYSTEM CAS DROP POOL MEMBER` is a writer operation that emits ordinary ref-edge deltas and does not synchronously reclaim shared blob content; removing the edges makes those blobs eligible for a later ordinary GC round."}
{"cluster_id": "chunk_005-298", "topic": "The GC implementation must use the same complete catalog cut for a decis", "canonical_claim": "The GC implementation must use the same complete catalog cut for a decision and must not perform an independent catalog reread; the deleted `resolveLifeOrSentinel` helper was replaced in comments by this invariant."}
