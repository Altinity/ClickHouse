{"cluster_id": "chunk_009-101", "topic": "The isContentAddressed() whole-part transaction contract", "canonical_claim": "The isContentAddressed() whole-part transaction guard is duplicated across six MergeTree files, so a future caller that omits it could silently corrupt a CAS part. D2 (definitely duplicated): the projection whole-part-transaction guard `if (!isContentAddressed()) begin/commitTransaction()` is copy-pasted across six files (`MergeTask` x3, `MutateTask`, `MergeProjectionPartsTask`, `MergeTreeDataWriter`, `IMergeTreeDataPart`); forgetting it in a new call site is a silent-corruption class, tracked as issue B58."}
{"cluster_id": "chunk_009-102", "topic": "ContentAddressedTransaction::unlinkFile deliberately no-ops for committed content", "canonical_claim": "ContentAddressedTransaction::unlinkFile deliberately no-ops for committed content files; a future caller that expects surgical committed-file deletion would silently retain the bytes."}
{"cluster_id": "chunk_009-103", "topic": "Cross-namespace RENAME TABLE is non-atomic and", "canonical_claim": "Cross-namespace RENAME TABLE is non-atomic and can leave a table split across namespaces after a mid-loop exception; the operation is idempotent and re-drivable."}
{"cluster_id": "chunk_009-104", "topic": "B171 is protected by contract", "canonical_claim": "B171 is protected by a durable build-root `+1` edge for adopted blobs and a fail-closed commit path that aborts when the dependency closure cannot be proven. A durable precommit edge must make an adopter's build root structurally reachable before adoption, and commit must re-check the whole closure and fail closed on absence; each half is independently necessary."}
{"cluster_id": "chunk_009-105", "topic": "B151 requires a freshly contract", "canonical_claim": "B151 requires a freshly written part's final ref to publish at lock-free tmp-to-final `moveDirectory`, with rollback dropping the early-published ref when the transaction is abandoned. For a staged tmp-to-final part move, `ContentAddressedTransaction::moveDirectory` only re-keys the transaction view and does not call `publishStaging`; durable ref operations occur at commit."}
{"cluster_id": "chunk_009-106", "topic": "B59 occurred because projection temp blobs", "canonical_claim": "B59 occurred because projection temp blobs were recorded in an open transaction without a committed ref, so committed-only `resolveRef` returned `FILE_DOESNT_EXIST` during read-back."}
{"cluster_id": "chunk_009-107", "topic": "P6 was a suspected S09 mutation", "canonical_claim": "P6 was a suspected S09 mutation carry-forward failure: the failing run had 2876 MiB pool growth and 34.5% S3 write errors versus 1.4 MiB and 12.5% on the passing rerun, leaving open whether write failure caused a full re-upload."}
{"cluster_id": "chunk_009-108", "topic": "The non-Atomic part-path parser contract", "canonical_claim": "The non-Atomic part-path parser can anchor `data/<db>/<table>/detached/<part>/...` on the inner part-shaped component, producing a spurious table namespace that `DROP TABLE` never cleans. For an Ordinary database, detached part paths can be parsed into a sibling namespace instead of the table namespace, leaving refs and blobs that `DROP TABLE` never cleans up. For non-Atomic paths, `findPartDirComponent` must anchor on the first reserved `detached` component before scanning right-to-left for a part-shaped component, so detached paths fold into the table namespace with `part_name == detached`."}
{"cluster_id": "chunk_009-109", "topic": "MOVE PART or MOVE PARTITION to", "canonical_claim": "MOVE PART or MOVE PARTITION to a CA disk was architecturally unimplemented: `PartPathParser::findPartDirComponent` had no `MergeTreeData::MOVING_DIR_NAME` case, so files under `moving` resolved to the same literal ref `moving`."}
{"cluster_id": "chunk_009-110", "topic": "The deeper MOVE-to-CA failure was that", "canonical_claim": "The deeper MOVE-to-CA failure was that non-zero-copy `clonePart` used per-file `copyFile` with a fresh autocommit transaction for each file, so a whole part could not publish as one CAS manifest and the second file collided on ref `moving`."}
{"cluster_id": "chunk_009-111", "topic": "During the outage, Keeper committed the", "canonical_claim": "During the outage, Keeper committed the `block_id` znode and part-znode `all_1117_1117_0` even though the CA blobs and manifest did not durably reach paused RustFS and the committing replica was then killed."}
{"cluster_id": "chunk_009-112", "topic": "When RustFS is paused, uploadPendingBlobs in", "canonical_claim": "When RustFS is paused, `uploadPendingBlobs` in the post-Keeper `ContentAddressedTransaction::commit` hangs until the CAS budget and throws; the transaction destructor then rolls back the local part."}
{"cluster_id": "chunk_009-113", "topic": "The old ReplicatedMergeTreeSink::commitPart order contract", "canonical_claim": "The old `ReplicatedMergeTreeSink::commitPart` order was `renameParts` → Keeper multi → `transaction.commit`, with part disk transactions committed only inside `transaction.commit` after the Keeper decision and under the `data_parts` lock. In the failing interleaving, `ReplicatedMergeTreeSink::commitPart` builds Keeper ops, calls `transaction.renameParts()`, executes `tryMultiNoThrow(ops)` to commit block-id and part-znode, and only then calls `transaction.commit()` to upload blobs and publish the CA ref."}
{"cluster_id": "chunk_009-114", "topic": "Upstream plain S3 also violates part-before-Keeper", "canonical_claim": "Upstream plain S3 also violates part-before-Keeper durability on a process crash because blob data streams during write but local metadata, including tmp-to-final rename, remains queued until after the Keeper multi."}
{"cluster_id": "chunk_009-115", "topic": "PartWriteTxn::abandon can misreport a durably completed", "canonical_claim": "`PartWriteTxn::abandon` can misreport a durably completed operation if an unguarded `EventEmitter::emit` throws after `appendRefOps` succeeds."}
{"cluster_id": "chunk_009-116", "topic": "publishEntries can leak a durable live-epoch", "canonical_claim": "`publishEntries` can leak a durable live-epoch precommit when promote fails because its exception path does not call `abandon`, while the destructor only retires the build sequence and stale-precommit sweep and GC do not reclaim live precommits."}
{"cluster_id": "chunk_009-117", "topic": "MergeTreeData::Transaction::renameParts now commits every contract", "canonical_claim": "`MergeTreeData::Transaction::renameParts` now commits every part's disk-storage transaction before the Keeper commit decision, changing durability ordering for all `DiskObjectStorage`-backed MergeTree writes and requiring non-CA regression coverage before upstreaming. The required invariant is that a part is durable on its disk before its `block_id` and part-znode are registered in Keeper; parts-transaction commit only flips visibility, while rollback compensates with new operations over committed disk state. After `renameParts` returns, every part is durable at its final name; `Transaction::commit` only changes in-memory visibility, and rollback removes or compensates committed disk data. With the durability fix, a disk publish failure throws from `renameParts` before the Keeper multi, so no block-id znode exists and a retry inserts honestly. A crash between the Keeper multi and the later parts-transaction commit leaves the part durable at its final name, with a CA ref in the pool, so restart can see the part. If a later failure occurs after `renameParts`, rollback removes all preactive parts; published CA parts are reclaimed by ref removal and unpublished parts by temporary cleanup and destructor abandon, before any Keeper multi is reached. All audited `renameParts` call sites execute before an external commit decision or immediately before `commit` with no intervening decision, and paths bypassing it were found not to violate the durability invariant. The implementation closes each active part transaction in `renameParts` after deferred `renameTo`, while borrowed projection sub-parts do not independently commit because they share the parent's transaction. The generic durability invariant is that a part must be durably published before its Keeper block ID is registered; the proposed implementation closes the active disk transaction in `Transaction::renameParts` rather than adding CA-specific routing. The generic fix closes every part disk-storage transaction in `MergeTreeData::Transaction::renameParts` after deferred renames, with no CA-specific code change. The intended protocol is prepare durable and compensable storage before the Keeper decision, then apply visibility with a cheap finalize or compensate on abort; `renameParts` is the existing lock-free prepare slot. The agreed fix model closes the disk transaction at `renameParts`; a later parts-transaction commit has nothing left to do on disk, while rollback performs compensation as a new operation over committed state."}
{"cluster_id": "chunk_009-118", "topic": "A repoint path that abandons its", "canonical_claim": "A repoint path that abandons its scratch build must reset `st.build` after abandon or promote, because destructor re-abandonment raises `LOGICAL_ERROR` and can abort under `abort_on_logical_error` or ASan."}
{"cluster_id": "chunk_009-119", "topic": "The stage-1 fan-out originally scheduled a", "canonical_claim": "The stage-1 fan-out originally scheduled a task before appending its handle to an unreserved tracking vector, so a tracking allocation failure could leave a runnable task untracked and later dereferencing destroyed transaction or result-slot storage."}
{"cluster_id": "chunk_009-120", "topic": "If namespace birth publishes _ckpt and", "canonical_claim": "If namespace birth publishes `_ckpt` and then its ref-log `PUT` fails, the failed birth can leave durable `_ckpt` debris; the creation lifecycle must delete that `_ckpt` for a never-born namespace without deleting a live namespace's checkpoint."}
{"cluster_id": "chunk_009-121", "topic": "The existing ~PartWriteTxn unconditionally retires a", "canonical_claim": "The existing `~PartWriteTxn` unconditionally retires a build while an owner grant may be wedged-Unresolved, and staged-body cleanup can swallow failures until the epoch seals."}
{"cluster_id": "chunk_009-122", "topic": "removeDirectory, removeRecursive, and committed moveDirectory mutate", "canonical_claim": "`removeDirectory`, `removeRecursive`, and committed `moveDirectory` mutate durable refs immediately rather than through a rollback-capable transaction overlay, so aborting can leave durable refs changed and can publish a supposedly removed part."}
{"cluster_id": "chunk_009-123", "topic": "truncateFile reports success without changing bytes", "canonical_claim": "`truncateFile` reports success without changing bytes, so callers can observe successful truncation while old content remains."}
{"cluster_id": "chunk_009-124", "topic": "removeFile discards the if_exists flags and", "canonical_claim": "`removeFile` discards the `if_exists` flags and succeeds when a file is missing even for the non-`ifExists` API instead of throwing `FILE_DOESNT_EXIST`."}
{"cluster_id": "chunk_009-125", "topic": "Retrying a failed commit can report", "canonical_claim": "Retrying a failed commit can report success with refs absent because rollback removes published refs but leaves staging entries marked `published`, causing retry to skip them."}
{"cluster_id": "chunk_009-126", "topic": "Rename treats an unrelated existing destination", "canonical_claim": "Rename treats an unrelated existing destination as successful replay when the source is absent, even though no operation ID or content proof exists."}
{"cluster_id": "chunk_009-127", "topic": "Part-write construction failure can permanently pin", "canonical_claim": "Part-write construction failure can permanently pin the active-build watermark because the active sequence is inserted before `PartWriteTxn` construction and no destructor retires it after allocation or event-sink failure."}
{"cluster_id": "chunk_009-128", "topic": "Moving a part to a content_addressed", "canonical_claim": "Moving a part to a `content_addressed` disk through `ALTER TABLE ... MOVE PART ... TO DISK 'ca'` used `promote` for the `moving` ref and failed the unique-ref invariant when `moving` already named another manifest."}
{"cluster_id": "chunk_009-129", "topic": "Since TXN-ONE-PIPELINE Task 1.1, B151 CA", "canonical_claim": "Since TXN-ONE-PIPELINE Task 1.1, B151 CA durable publish operations ran inside MergeTreeData::Transaction::commit while holding the data_parts lock, causing concurrent queries needing that lock to stall behind S3 round trips."}
{"cluster_id": "chunk_009-130", "topic": "IDataPartStorage::precommitTransaction is called before final part", "canonical_claim": "IDataPartStorage::precommitTransaction is called before final part rename and therefore is not automatically a safe externally visible CAS publication boundary."}
{"cluster_id": "chunk_009-131", "topic": "A new CA part-storage implementation requires", "canonical_claim": "A new CA part-storage implementation requires a centralized factory because direct construction in fetch, part loading, and temporary text-index code could otherwise create two part-storage semantics on one disk."}
{"cluster_id": "chunk_009-132", "topic": "A CA tryCommit retry can lose", "canonical_claim": "A CA `tryCommit` retry can lose staged `writeFile` or `createHardLink` state because the reset `metadata_transaction` has no `operations_to_execute` entry to refill the in-memory staging maps."}
{"cluster_id": "chunk_009-133", "topic": "Build::abandon retires build_seq before appending the", "canonical_claim": "`Build::abandon` retires `build_seq` before appending the precommit-removal event, allowing GC precommit reclamation to race the abandonment removal."}
{"cluster_id": "chunk_009-134", "topic": "The clone/freeze path could perform per-file", "canonical_claim": "The clone/freeze path could perform per-file operations without an enclosing whole-part transaction, so a logical part clone was not represented as one disk transaction."}
{"cluster_id": "chunk_009-135", "topic": "On plain S3, mutation contract", "canonical_claim": "On plain S3, mutation finalization can make `loadProjections` see only committed metadata, silently skip a carried-forward projection, and leave the in-memory mutated part without that projection until reload. `createHardLink` must stage the hardlink eagerly so `loadProjections` during the same finalization can see a carried-forward projection."}
{"cluster_id": "chunk_009-136", "topic": "D8 (mostly duplicated): the frontend implements", "canonical_claim": "D8 (mostly duplicated): the frontend implements `existsDirectory` vs `listDirectory` path-shape routing twice with the same fixed dispatch order (`shadow -> atomic-shard -> table-uuid -> part -> subdir -> generic`), which must be kept in sync manually between the two implementations — the highest-value consolidation target after D9 in the author's own assessment."}
{"cluster_id": "chunk_009-137", "topic": "Risk register #4 (Medium, confirmed): ContentAddressedTransaction.cpp:1294-1336's", "canonical_claim": "Risk register #4 (Medium, confirmed): `ContentAddressedTransaction.cpp:1294-1336`'s `unlinkFile` case 3 is a deliberate fail-open no-op for unlinking a committed content file (load-bearing for MergeTree's file-by-file removal pattern, tracked as B123); any future caller that surgically deletes one committed file without a whole-part `removeDirectory` would silently keep the bytes. Suggested action: pin the MergeTree invariant with an assertion/flag or a dedicated test."}
{"cluster_id": "chunk_009-138", "topic": "B188 replaced an eager HEAD of", "canonical_claim": "B188 replaced an eager HEAD of a committed source with `adoptEvidence` and made precommit-first ordering gate reads as well as writes."}
{"cluster_id": "chunk_009-139", "topic": "A rebirth test using an Atomic", "canonical_claim": "A rebirth test using an `Atomic` database fixture silently gets a fresh UUID and never actually tests same-name rebirth; a valid rebirth test must pre-install a nonzero predecessor cursor, reuse the writer epoch, prove the same `RootNamespace` was reused, and interleave a post-prune GC round."}
{"cluster_id": "chunk_009-140", "topic": "ContentAddressedTransaction::commit compensates for a mid-commit exception", "canonical_claim": "ContentAddressedTransaction::commit compensates for a mid-commit exception by best-effort dropping newly created refs and then rethrows."}
{"cluster_id": "chunk_009-141", "topic": "moveDirectory publishes a CAS rename eagerly", "canonical_claim": "moveDirectory publishes a CAS rename eagerly, outside the later transaction commit, to avoid holding MergeTree locks during object-store I/O."}
{"cluster_id": "chunk_009-142", "topic": "CAS exposes supportsTransactionalMutableFiles separately contract", "canonical_claim": "CAS exposes supportsTransactionalMutableFiles separately from append support because transactions rewrite txn_version.txt and do not require append. `StorageMergeTree::supportTransaction` accepts a disk that reports `supportsTransactionalMutableFiles()` even when the disk does not support append, allowing CAS to persist per-part MVCC `txn_version.txt` in its per-ref sidecar."}
{"cluster_id": "chunk_009-143", "topic": "A new_binding event contributes +1 for", "canonical_claim": "A `new_binding` event contributes `+1` for each manifest blob, a true `old_binding` removal contributes `−1`, an equal old/new binding contributes zero, and a repoint contributes `−1` for the old manifest plus `+1` for the new manifest."}
{"cluster_id": "chunk_009-144", "topic": "A missing precommit body is non-activating", "canonical_claim": "A missing precommit body is non-activating; when the body appears, the fold activates it and emits its `+1` deltas, while an abandoned precommit emits no deltas."}
{"cluster_id": "chunk_009-145", "topic": "Part manifests are deleted only after", "canonical_claim": "Part manifests are deleted only after the adopted fold has sealed their blob decrements, and pre-precommit debris is handled by a bounded exact-token orphan sweep."}
{"cluster_id": "chunk_009-146", "topic": "A Cas::Build is never persisted and", "canonical_claim": "A `Cas::Build` is never persisted and has no precommit replay path; GC never invents a ref transition or removes a dead precommit."}
{"cluster_id": "chunk_009-147", "topic": "A CA MergeTree part contract", "canonical_claim": "A CA MergeTree part is modeled as many files to one manifest to one ref, so `freeze()` creates one whole-part `DiskTransaction` when the caller supplies none. When the caller does not provide a transaction, `src/Storages/MergeTree/DataPartStorageOnDiskBase.cpp:514` creates a whole-part transaction for cloning, and `metadata_version.txt` is written inside that transaction at line 553. `DataPartStorageOnDiskBase::freeze` wraps the whole clone in an owned transaction and writes `metadata_version.txt` inside it, preserving whole-part atomicity from many files to one manifest and one ref."}
{"cluster_id": "chunk_009-148", "topic": "A tree reference may be published", "canonical_claim": "A tree reference may be published only when all direct children are present and non-condemned at publish time."}
{"cluster_id": "chunk_009-149", "topic": "A root ref may own at", "canonical_claim": "A root ref may own at most one committed manifest; `Build::promote` must abort on a different existing committed manifest, while same-content republish is idempotent."}
{"cluster_id": "chunk_009-150", "topic": "The edge-before-observe model preserves contract", "canonical_claim": "The edge-before-observe model preserves the precommit → adopt/observe → promote order and K1 condemned-meta adoption gate; tokened leaves need no promote-time revalidation in the current code. With durable precommit closure before adopt/observe and same-pass GC recheck, promote-time revalidation of already-tokened leaves is redundant; dedup adoption, tokenless-leaf presence HEAD, condemned checks, and operation order remain load-bearing."}
{"cluster_id": "chunk_009-151", "topic": "The B59 in-flight overlay provides tryGetInFlightStorageObjects", "canonical_claim": "The B59 in-flight overlay provides `tryGetInFlightStorageObjects`, `tryReadFileInFlight`, and `tryGetInFlightFileSize`, with inline staged bytes served by `ReadBufferFromOwnString`."}
{"cluster_id": "chunk_009-152", "topic": "hasInFlightDirectory(path) returns true when staged entries", "canonical_claim": "`hasInFlightDirectory(path)` returns true when staged entries have a `<proj>.proj/` prefix, allowing `loadProjections` to discover an in-flight projection through `existsDirectory`."}
{"cluster_id": "chunk_009-153", "topic": "The durable part-write order contract", "canonical_claim": "The durable part-write order is `stageManifest` → `precommitAdd` → `putBlob` → `promote`, and adopting an existing blob is legal only after this build's precommit is durable. The canonical part publication sequence is `stageManifest` → `precommitAdd` → `putBlob` → `promote`; `promote` publishes no blob deltas because the manifest retains an owner. The write path is stageManifest → precommitAdd → putBlob → promote, and the precommit-to-blob edge protects a new blob only after GC folds the create-precommit event with the manifest body. The intra-part order `stageManifest → precommitAdd → uploadPendingBlobs → promote` is an invariant corresponding to EDGE-BEFORE-OBSERVE and must not change under parallelization."}
{"cluster_id": "chunk_009-154", "topic": "PartWriteTxn is in-memory only and is", "canonical_claim": "`PartWriteTxn` is in-memory only and is never resumed after restart; a dead precommit is removed rather than promoted."}
{"cluster_id": "chunk_009-155", "topic": "Promotion fails closed when a leaf", "canonical_claim": "Promotion fails closed when a leaf is neither token-protected nor backed by a durable manifest edge."}
{"cluster_id": "chunk_009-156", "topic": "A transaction never publishes a ref", "canonical_claim": "A transaction never publishes a ref before `commit`; its destructor abandons open builds."}
{"cluster_id": "chunk_009-157", "topic": "S10's authoritative delete oracle confirmed correct", "canonical_claim": "S10's authoritative delete oracle confirmed correct deletes; a bucket can be repopulated by later inserts because each insert writes all 100 buckets, so a final `ALTER DELETE ... mutations_sync=2` is required to test removal without that confound."}
{"cluster_id": "chunk_009-158", "topic": "PartRefKey identifies a committed part or", "canonical_claim": "`PartRefKey` identifies a committed part or projection folder by owning root namespace plus ref name, where the ref is either a part name or the folded `detached/<part>` name."}
{"cluster_id": "chunk_009-159", "topic": "ContentAddressedMetadataStorage::partAccess returns a reference so normal", "canonical_claim": "`ContentAddressedMetadataStorage::partAccess` returns a reference so normal committed-ref mutations use dot syntax; raw `->dropRef`, `->updateRefPayload`, `->dropNamespace`, and `->promote` calls are forbidden in wiring outside `CachedPartFolderAccess`."}
{"cluster_id": "chunk_009-160", "topic": "CachedPartFolderAccess::dropRefIfPresent treats an absent ref and", "canonical_claim": "`CachedPartFolderAccess::dropRefIfPresent` treats an absent ref and a concurrent `FILE_DOESNT_EXIST` race as successful no-ops, while propagating other exceptions."}
{"cluster_id": "chunk_009-161", "topic": "CachedPartFolderAccess::publishEntries performs startBuild, adoptEvidence for every", "canonical_claim": "`CachedPartFolderAccess::publishEntries` performs `startBuild`, `adoptEvidence` for every entry, `stageManifest`, `precommitAdd`, and `promoteBuild` as the shared committed-publish sequence."}
{"cluster_id": "chunk_009-162", "topic": "republishRef moves a committed source by", "canonical_claim": "`republishRef` moves a committed source by publishing a fresh destination manifest and then dropping the source; an absent source returns `false` without writing."}
{"cluster_id": "chunk_009-163", "topic": "dropNamespace removes retained views whose canonical", "canonical_claim": "`dropNamespace` removes retained views whose canonical cache keys start with the namespace plus the NUL separator and increments the invalidation metric."}
{"cluster_id": "chunk_009-164", "topic": "The facade's committed-ref write API includes", "canonical_claim": "The facade's committed-ref write API includes `promoteBuild`, `publishEntries`, `republishRef`, `updateMutableFiles`, `dropRef`, `dropRefIfPresent`, `dropRefBestEffort`, and `dropNamespace`; pass-through operations such as `listRefs`, `listNamespaces`, `listMirroredChildren`, namespace files, mountpoint objects, and `locate` remain on `Cas::Store`."}
{"cluster_id": "chunk_009-165", "topic": "The in-flight transaction overlay remains above", "canonical_claim": "The in-flight transaction overlay remains above the facade; committed-source `createHardLink` and `moveFile` use `ForceFresh` access, while in-flight overlay reads may continue to use raw read-only `locate` calls."}
{"cluster_id": "chunk_009-166", "topic": "Each level-1 facade mutation performs its", "canonical_claim": "Each level-1 facade mutation performs its underlying store or build operation and erases affected retained views only after success, except `dropRefBestEffort`, which erases even after swallowing a failure."}
{"cluster_id": "chunk_009-167", "topic": "publishEntries is the shared sequence startBuild", "canonical_claim": "`publishEntries` is the shared sequence `startBuild -> adoptEvidence per entry -> stageManifest -> precommitAdd -> promoteBuild`."}
{"cluster_id": "chunk_009-168", "topic": "Birth-completion is allowed only from an", "canonical_claim": "Birth-completion is allowed only from an absent meta, representing a crashed pre-meta birth; it is never allowed from a `tombstone` meta."}
{"cluster_id": "chunk_009-169", "topic": "A condemned object is contract", "canonical_claim": "A condemned object is never read or fetched to revive it; revival uses a fresh upload or recreation from source, except for the documented `INV-1` tokenless copy-forward of a committed-source dependency whose condemned body is still present. Only a writer that has displaced the body with a fresh incarnation token may publish `Clean`, through `uploadFromSource` with `writeResurrectMetaClean` or tokenless committed-source `copyForwardFromCondemned`. A spared hash may remain `Condemned` indefinitely; normal reads do not consult the marker, and the next `putBlob` must materialize or copy-forward a fresh incarnation before publishing `Clean`."}
{"cluster_id": "chunk_009-170", "topic": "The writer's meta state machine adopts", "canonical_claim": "The writer's meta state machine adopts a clean meta without a body `HEAD`, resurrects a condemned meta with `casMeta` and no body re-upload, and handles an absent meta by re-establishing the raw body from its own source before creating clean meta."}
{"cluster_id": "chunk_009-171", "topic": "A writer observing a tombstone waits", "canonical_claim": "A writer observing a tombstone waits through bounded fresh meta reads and then retries fresh upload after the meta becomes absent; if the bound is exhausted, `putBlob` throws `ABORTED` and never changes the tombstone to clean."}
{"cluster_id": "chunk_009-172", "topic": "Promote skips meta revalidation for tokened", "canonical_claim": "Promote skips meta revalidation for tokened leaves; for a non-tokened leaf it fails with `ABORTED` on absent meta, accepts clean meta, and copy-forwards only a condemned meta by CASing it to clean."}
{"cluster_id": "chunk_009-173", "topic": "copyForwardFromCondemned is a pure meta CAS", "canonical_claim": "`copyForwardFromCondemned` is a pure meta CAS for a condemned raw-body leaf and performs no body GET or PUT; tombstone or absent meta is fail-closed because the tokenless promote path has no source."}
{"cluster_id": "chunk_009-174", "topic": "A condemned row subsumes contract", "canonical_claim": "A condemned row subsumes the zero marker, while a plain `kZeroMarker` is emitted only when the object is absent at condemnation and remains transient for the generation. Fresh zero transitions mint a `kCondemned` row from the `head_blob` observation; an absent object produces only a plain `kZeroMarker`, and clamp suppression carries pending rows unchanged while keeping floor-passed rows condemned-only."}
{"cluster_id": "chunk_009-175", "topic": "A blob's (algo, digest) identity is", "canonical_claim": "A blob's `(algo, digest)` identity is immutable from upload through exact-token deletion, so changing the configured write algorithm does not rehash an existing blob."}
{"cluster_id": "chunk_009-176", "topic": "Deduplication is keyed by BlobRef and", "canonical_claim": "Deduplication is keyed by `BlobRef` and therefore works only within one algorithm subset; equal content written under different algorithms naturally has different keys and does not cross-deduplicate."}
{"cluster_id": "chunk_009-177", "topic": "The S3 staging lifecycle contract", "canonical_claim": "The S3 staging lifecycle is `precommitAdd(edge) → stage → promote(copy)+condemn-check → delete staging → promoteBuild(visible)`; a manifest cannot become visible before the blob exists at its content key. A staging object is deleted only after promotion and the condemn check complete, because it remains the resurrection source during that gate. For S3 staging, `publishStaging` must run the same `putBlob` condemn/resurrect gate as local staging; the only changed upload primitive is conditional server-side copy from the staging object."}
{"cluster_id": "chunk_009-178", "topic": "On a successful conditional copy, the", "canonical_claim": "On a successful conditional copy, the destination ETag from `CopyObject` or `CompleteMultipartUpload` becomes the blob incarnation token; on `PreconditionFailed`, the writer HEADs the destination and executes the existing condemn/resurrect gate."}
{"cluster_id": "chunk_009-179", "topic": "A failed current-writer build removes its", "canonical_claim": "A failed current-writer build removes its exact precommit before retirement; a fenced successor removes predecessor-epoch precommits in bounded batches after recovery."}
{"cluster_id": "chunk_009-180", "topic": "Borrowed projection storage shares contract", "canonical_claim": "Borrowed projection storage shares the parent whole-part transaction: `DataPartStorageOnDiskFull::beginTransaction` and `commitTransaction` must be no-ops when `has_shared_transaction` is true. A borrowed projection transaction makes `beginTransaction` and `commitTransaction` no-ops, with the parent transaction passed from `src/Storages/MergeTree/DataPartStorageOnDiskFull.cpp:41` and the no-op behavior implemented at lines 347 and 363. Projection build, merge, and mutation call sites can always invoke `beginTransaction` and `commitTransaction`; the storage decides whether it owns the transaction. Projection tasks may call `commitTransaction` unconditionally because a borrowed CAS projection rides the parent transaction and its `commitTransaction` is a no-op. The borrowed-projection transaction rule removes six duplicated `if (!isContentAddressed()) begin/commitTransaction()` guards from merge and mutate paths; the two sites that decide whether a projection borrows the parent transaction remain unchanged."}
{"cluster_id": "chunk_009-181", "topic": "Manifest-trust relink adoption relies on the", "canonical_claim": "Manifest-trust relink adoption relies on the source pinning the part for the whole download and the fetcher's durable precommit edge being established before adoption; trusted adoption uses `reason=\"manifest-trust\"`, an empty token, and the `CasBlobAdoptTrusted` counter without changing manifest format."}
{"cluster_id": "chunk_009-182", "topic": "Decommission order is gate and claim", "canonical_claim": "Decommission order is gate and claim, namespace enumeration, per-namespace ref removal, manifest-debris drain, staging sweep, roots sweep, and slot retirement strictly last; namespace removal emits ordinary negative edge deltas and does not make `GC` invent ref transitions."}
{"cluster_id": "chunk_009-183", "topic": "Recovery seals apply to Live-lifecycle tables", "canonical_claim": "Recovery seals apply to `Live`-lifecycle tables, while `Removed` recoveries are not sealed and rely on ordinary namespace cleanup for late rebirth debris."}
{"cluster_id": "chunk_009-184", "topic": "RemovePart generated zero S3 requests in", "canonical_claim": "`RemovePart` generated zero S3 requests in the soak because removal is a batched ref operation and physical deletion is deferred to GC."}
{"cluster_id": "chunk_009-185", "topic": "adoptEvidence records a tokenless, bodyless, edgeless", "canonical_claim": "`adoptEvidence` records a tokenless, bodyless, edgeless dependency during the transaction body, while the destination's durable `precommitAdd` edge is appended only at commit."}
{"cluster_id": "chunk_009-186", "topic": "A committed-source adopt is not a", "canonical_claim": "A committed-source adopt is not a pending blob, so it is never uploaded by `putBlob`; before `precommitAdd`, the destination contributes no durable in-degree edge to the adopted blob."}
{"cluster_id": "chunk_009-187", "topic": "grabOldParts removes a CA part only", "canonical_claim": "`grabOldParts` removes a CA part only when it is `Outdated` and its `DataPartPtr` is uniquely owned."}
{"cluster_id": "chunk_009-188", "topic": "A CA MVCC store event is", "canonical_claim": "A CA MVCC store event is intended to produce one manifest repoint, and `txn_version.txt.tmp` is never created on CA."}
{"cluster_id": "chunk_009-189", "topic": "A committed-file unlink stages contract", "canonical_claim": "A committed-file unlink stages a removal mark; if the whole part is also removed, the mark is superseded and the normal ref-drop path pays zero repoints. For a committed content-file unlink, `unlinkFile` stages the path in `st.content_removed`; publishing filters that path from carried-forward entries and repoints unless the same transaction drops the whole part ref, which supersedes and discards the marks."}
{"cluster_id": "chunk_009-190", "topic": "When publishStaging receives staged entries for", "canonical_claim": "When `publishStaging` receives staged entries for an already committed ref, it force-freshly reads the current manifest, drops entries whose paths are overwritten, carries forward all other entries, appends the staged entries, uploads pending blobs, and publishes once through the repoint path."}
{"cluster_id": "chunk_009-191", "topic": "Every CA disk transaction mutation updates", "canonical_claim": "Every CA disk transaction mutation updates one transaction-private overlay at call time, and the existing disk `commit` is the only operation that publishes the overlay into the durable CAS namespace."}
{"cluster_id": "chunk_009-192", "topic": "The transaction states are Open, Committed", "canonical_claim": "The transaction states are `Open`, `Committed`, and `Aborted`; in `Open`, writes, creates, hardlinks, deletes, moves, directory moves, and ref moves update the overlay immediately without publishing live refs."}
{"cluster_id": "chunk_009-193", "topic": "commit freezes the overlay, constructs manifests", "canonical_claim": "`commit` freezes the overlay, constructs manifests, runs the internal CAS build protocol, promotes blobs, publishes or moves refs, applies final overlay state, and is the only durable publication point."}
{"cluster_id": "chunk_009-194", "topic": "Blob bytes and metadata may be", "canonical_claim": "Blob bytes and metadata may be eagerly staged on a private local or S3 backend while a transaction is open, but they are not reachable through a live ref until commit."}
{"cluster_id": "chunk_009-195", "topic": "DiskObjectStorageTransaction::dispatch applies an operation immediately when", "canonical_claim": "`DiskObjectStorageTransaction::dispatch` applies an operation immediately when `transactionIsStagingOverlay()` is true and otherwise queues it for FIFO replay; CA uses the eager path and ordinary object storage keeps the deferred path."}
{"cluster_id": "chunk_009-196", "topic": "writeFileImpl uses the generic tryCreateWriteBuffer transaction", "canonical_claim": "`writeFileImpl` uses the generic `tryCreateWriteBuffer` transaction hook; CA supplies its hash-on-write staging buffer while the default implementation returns `nullptr` for unchanged metadata behavior."}
{"cluster_id": "chunk_009-197", "topic": "An autocommit write buffer contract", "canonical_claim": "An autocommit write buffer retains its owning disk transaction and invokes the existing `commit` from its finalize callback; it does not invoke a new `precommit` phase. The generic `tryCreateWriteBuffer` hook receives the owning `std::shared_ptr<IDiskTransaction>` so CA can keep the disk transaction alive for the buffer lifetime and commit from an autocommit finalize callback."}
{"cluster_id": "chunk_009-198", "topic": "The CA overlay covers part-file entries", "canonical_claim": "The CA overlay covers part-file entries, pending blob payloads, removals, projection and part-directory re-keying, committed-ref changes, and table-level or mountpoint mutations reached through the transaction."}
{"cluster_id": "chunk_009-199", "topic": "Before commit, undo or transaction destruction", "canonical_claim": "Before commit, `undo` or transaction destruction discards the overlay, abandons builds, and removes private staging without mutating the shared live namespace."}
{"cluster_id": "chunk_009-200", "topic": "The B183 text-index scratch ref is", "canonical_claim": "The B183 text-index scratch ref is published by a separate nested sub-storage and must still be dropped during staged-source `tmp`→final finalization even after the main transaction's publication moves to `commit`."}
{"cluster_id": "chunk_009-201", "topic": "The empty-covering-part commitTransaction() workaround must remain", "canonical_claim": "The empty-covering-part `commitTransaction()` workaround must remain: the rollback path does not enter `MergeTreeData::Transaction::commit`, so that call is the only publication point for the empty cover under a commit-only publication design."}
{"cluster_id": "chunk_009-202", "topic": "ALTER TABLE … MOVE contract", "canonical_claim": "`ALTER TABLE … MOVE PART|PARTITION TO DISK/VOLUME 'ca'` and background TTL/policy moves must copy a part to a content-addressed disk as one atomic unit: one part, one manifest, one ref, published atomically with deduplication. TO-CA MOVE must preserve deduplication, concurrent SELECT availability, clean `fsck`, bounded GC reclamation of the vacated side, and restart atomicity such that a part is fully moved or remains fully on its origin."}
{"cluster_id": "chunk_009-203", "topic": "The CA-specific clonePart branch is gated", "canonical_claim": "The CA-specific `clonePart` branch is gated by `dst_disk->isContentAddressed()`, while non-CA destinations retain the existing directory-copy path."}
{"cluster_id": "chunk_009-204", "topic": "MOVE PARTITION clones each part independently", "canonical_claim": "`MOVE PARTITION` clones each part independently through `moveParts`, so one-part-per-transaction is the correct granularity; `CA→local` remains untouched."}
{"cluster_id": "chunk_009-205", "topic": "rename_in_transaction controls the lock scope of", "canonical_claim": "`rename_in_transaction` controls the lock scope of part materialization, not durability: true defers rename to off-lock `renameParts`, while false performs immediate rename for plain-engine race protection."}
{"cluster_id": "chunk_009-206", "topic": "PartWriteTxn::abandon must set alive = false", "canonical_claim": "`PartWriteTxn::abandon` must set `alive = false` only after the correctness-bearing precommit removal succeeds so a caller can retry `abandon` after an append exception."}
{"cluster_id": "chunk_009-207", "topic": "IDataPartStorage::takeTransactionForBatchCommit moves out the owned transaction", "canonical_claim": "`IDataPartStorage::takeTransactionForBatchCommit` moves out the owned transaction, returns `nullptr` for shared or inactive transactions, fires the per-part commit failpoint, and leaves `hasActiveTransaction()` false."}
{"cluster_id": "chunk_009-208", "topic": "The CAS commit-worker pool must be", "canonical_claim": "The CAS commit-worker pool must be disjoint from `getThreadPoolWriter`; no worker may wait on work queued to its own pool."}
{"cluster_id": "chunk_009-209", "topic": "PreparedRelink is a move-only handle that", "canonical_claim": "`PreparedRelink` is a move-only handle that owns the receiver transaction, manifest, target ref, decoded entries, and exact source token; it uses a scope guard to append a durable abort on every non-promote exit."}
{"cluster_id": "chunk_009-210", "topic": "Relink promotion has three outcomes: Committed", "canonical_claim": "Relink promotion has three outcomes: `Committed`, `MechanismFallbackAllowed` only when non-commit is proven, and `Unresolved` when promotion may have committed; `Unresolved` requires retrying the whole fetch rather than sequential byte fallback."}
{"cluster_id": "chunk_009-211", "topic": "Detached relink targets are passed as", "canonical_claim": "Detached relink targets are passed as disk-relative part paths so the CAS side derives the namespace and ref itself; `detached/<name>` is folded into routing, while a non-live-part target throws instead of falling back to bytes at the wrong location."}
{"cluster_id": "chunk_009-212", "topic": "Stage 1 parallelizes blobs within each", "canonical_claim": "Stage 1 parallelizes blobs within each part, while part publication remains a serial loop; comments claiming Task 5 parallelizes cross-part publication are false for the as-built stage."}
{"cluster_id": "chunk_009-213", "topic": "The removal lifecycle is Creating →", "canonical_claim": "The removal lifecycle is `Creating → Live → Removing → absent`, and a pure walk-plan constructor creates exactly one `ref_lives` row for every catalog entry in `Live` or `Removing`, with no row for `Creating` or an absent id."}
{"cluster_id": "chunk_009-214", "topic": "A namespace rebirth receives a new", "canonical_claim": "A namespace rebirth receives a new opaque `life_id`, so an adopted predecessor row cannot be inherited by the successor and stale predecessor handles cannot target successor state."}
{"cluster_id": "chunk_009-215", "topic": "The catalog model mints a fresh", "canonical_claim": "The catalog model mints a fresh incarnation on each create, creates `_ckpt` before going live, allows stale-creator reconciliation only after the creator fence dies, and lets the janitor delete only objects whose incarnation is not current."}
{"cluster_id": "chunk_009-216", "topic": "Writer cleanup requires an in-memory duty", "canonical_claim": "Writer cleanup requires an in-memory duty queue while the mount lives and a successor-seal path for crash remnants; a build must not be retired while its owner-grant outcome is uncertain."}
{"cluster_id": "chunk_009-217", "topic": "An un-cataloged namespace during ordinary removal", "canonical_claim": "An un-cataloged namespace during ordinary removal is a legitimate transient state when removal evidence exists; anomaly reporting must distinguish that state from an un-cataloged namespace with no removal evidence."}
{"cluster_id": "chunk_009-218", "topic": "A ref recovery closes every writer", "canonical_claim": "A ref recovery closes every writer epoch below the live epoch, including an epoch that never wrote, with exactly one conditional create at the dead epoch's next id; the seal is paid lazily at first touch rather than at mount."}
{"cluster_id": "chunk_009-219", "topic": "A same-UUID recreated pool refuses to", "canonical_claim": "A same-UUID recreated pool refuses to bootstrap over a non-empty prefix lacking `_pool_meta`, emitting Code 668 and the message `missing _pool_meta over a non-empty pool prefix — refusing to bootstrap over residual data; recreate the pool or restore _pool_meta.`"}
{"cluster_id": "chunk_009-220", "topic": "CaDiskLifecycle contains no listing, scan, or", "canonical_claim": "`CaDiskLifecycle` contains no listing, scan, or enumeration and is driven by point reads and the `SYSTEM CONTENT ADDRESSED FORGET` protocol; v9 therefore changes none of its premises."}
{"cluster_id": "chunk_009-221", "topic": "PartWriteTxn stages the manifest contract", "canonical_claim": "`PartWriteTxn` stages the manifest and precommit and owes removal if the write never promotes; `PreparedPartWrite` owes exactly one terminal `promote` or `abort` and its destructor is the backstop; the caller owns the surrounding operation. `PreparedPartWrite` makes the receiver's durable `+1` and deferred promote an owned state: callers must explicitly `promote` or `abort`, because destruction of the handle alone is not cleanup."}
{"cluster_id": "chunk_009-222", "topic": "A staged precommit whose removal append", "canonical_claim": "A staged precommit whose removal append did not land pins its manifest and underlying blobs while the writer lives because the stale-precommit sweep is prior-epoch-scoped and GC does not touch a live precommit."}
{"cluster_id": "chunk_009-223", "topic": "PrecommitState::NotAttempted means no precommit append was", "canonical_claim": "`PrecommitState::NotAttempted` means no precommit append was entered, `Uncertain` means the append was entered with an unknown outcome and removal is owed, `Durable` means the append returned and removal is still owed, and `Settled` means the terminal operation discharged the duty."}
{"cluster_id": "chunk_009-224", "topic": "The authoritative release emission occurs once", "canonical_claim": "The authoritative release emission occurs once at `~PartWriteTxn` destruction, after `~PreparedPartWrite` has made the last release attempt: it emits iff the final state is `Uncertain` or uncommitted `Durable`, and a late success reaching `Settled` emits neither counter nor authoritative ERROR line."}
{"cluster_id": "chunk_009-225", "topic": "A consumer of the release seam", "canonical_claim": "A consumer of the release seam may rely on five guarantees: an unproven release is counted and logged once, the final attempt owns the result, proven no-send refusals are silent, missing proof fails closed by over-reporting, and the seam does not change consumer actions."}
{"cluster_id": "chunk_009-226", "topic": "The four relink receiver states are", "canonical_claim": "The four relink receiver states are `S0` publication proven absent, `S1` staged, `S2` publish uncertain, and `S3` committed; release cleanup is not a receiver state because only transaction destruction can observe the final release attempt."}
{"cluster_id": "chunk_009-227", "topic": "In S1, Committed promotion enters S3", "canonical_claim": "In S1, `Committed` promotion enters S3, `MechanismFallbackAllowed` enters S0 because rejection occurred before the ref-log append, and `Unresolved` enters S2 because the append was attempted without a verdict; none of these classifications reports release success."}
{"cluster_id": "chunk_009-228", "topic": "If a retry's conditional create returns", "canonical_claim": "If a retry's conditional create returns `Created` and the mount authority has moved before adoption, the lane enters `NeedsRecovery`; the newly durable transaction must not be discarded as an inert stale result."}
{"cluster_id": "chunk_009-229", "topic": "A post-durable install exception transitions Wedged", "canonical_claim": "A post-durable install exception transitions `Wedged` to `NeedsRecovery`, clears the wedge, rethrows, and does not raise a durable floor or poison the apply state."}
{"cluster_id": "chunk_009-230", "topic": "NeedsRecovery has only a complete-recovery exit", "canonical_claim": "`NeedsRecovery` has only a complete-recovery exit: `Recover` sets `cache_id' = durable_id`, and an incomplete recovery is rejected by `SabotageIncompleteRecovery` through `ReadyCaughtUp`."}
{"cluster_id": "chunk_009-231", "topic": "The apply-pending marker is armed immediately", "canonical_claim": "The apply-pending marker is armed immediately before the durable PUT, and an apply owed by a durable transaction requires the stored marker to be non-clean until installation or recovery completes."}
{"cluster_id": "chunk_009-232", "topic": "The resolveWedgeOnce mapping covers six WedgeResolution", "canonical_claim": "The `resolveWedgeOnce` mapping covers six `WedgeResolution` values, three `SlotOccupyResult::Kind` values, four occupant classes, seven `Reason` values, and two exception exits; inert rows are represented as no state transition."}
{"cluster_id": "chunk_009-233", "topic": "The Created arm of CasRefLedger::resolveWedgeOnce is", "canonical_claim": "The `Created` arm of `CasRefLedger::resolveWedgeOnce` is a separate durable retry step before `WedgeResolveInstall`; off-lock occupant classification makes the durable-but-unapplied interval observable."}
{"cluster_id": "chunk_009-234", "topic": "FloorReconcile retires a floor-covered wedge without", "canonical_claim": "`FloorReconcile` retires a floor-covered wedge without installing its transaction and retains both `Poisoned` apply state and the owed durable transaction until recovery re-derives the cache."}
{"cluster_id": "chunk_009-235", "topic": "Recovery performs no stream LIST: _ckpt.checkpoint_snapshot_id", "canonical_claim": "Recovery performs no stream LIST: `_ckpt.checkpoint_snapshot_id` is the sole recovery-snapshot authority, while LIST is reserved for GC/janitor garbage-nomination discovery."}
{"cluster_id": "chunk_009-236", "topic": "chooseRecoveryGrounding(catalog_state, ckpt) uses only checkpoint_snapshot_id; with", "canonical_claim": "`chooseRecoveryGrounding(catalog_state, ckpt)` uses only `checkpoint_snapshot_id`; with a base it walks from the base's successor, without a base it walks from `{life_epoch, 1}`, and it fails closed when required lifecycle information is absent."}
{"cluster_id": "chunk_009-237", "topic": "The ref lane state variable identifies", "canonical_claim": "The ref lane state variable identifies the owner of the next safe action, and an exact append attempt exists only while the writer or resolver owns it."}
{"cluster_id": "chunk_009-238", "topic": "Ready means no durable contract", "canonical_claim": "`Ready` means no durable transaction is missing from the cached table and permits append and certification; `Writing` owns an exact pre-I/O attempt; `Wedged` owns the same attempt with uncertain durability; `NeedsRecovery` knows durability but lacks a completed install; `Closed` and `Faulted` are terminal for one runtime. `Ready`, `NeedsRecovery`, `Closed`, and `Faulted` retain no append attempt, while `Writing` and `Wedged` retain exactly one attempt whose transaction id is the cached successor."}
{"cluster_id": "chunk_009-239", "topic": "A definite pre-send or non-durable refusal", "canonical_claim": "A definite pre-send or non-durable refusal clears the attempt and returns `Writing` to `Ready`, while an unresolved post-send outcome preserves the exact attempt and moves `Writing` to `Wedged`."}
{"cluster_id": "chunk_009-240", "topic": "Resolver outcomes are: exact durable plus", "canonical_claim": "Resolver outcomes are: exact durable plus current authority installs and returns `Wedged` to `Ready`; exact durable plus moved authority or a post-durable install exception enters `NeedsRecovery`; unknown remains `Wedged`; a successor seal enters `Closed`; foreign or malformed bytes enter `Faulted`; a result for another attempt causes no lane transition."}
{"cluster_id": "chunk_009-241", "topic": "ATTACH TABLE ... UUID '<same>' and", "canonical_claim": "`ATTACH TABLE ... UUID '<same>'` and explicit-UUID `CREATE TABLE` can reuse an old `RootNamespace`, and replicated database recovery can depend on preserving shared explicit UUIDs."}
{"cluster_id": "chunk_009-242", "topic": "A shadow backup namespace is derived", "canonical_claim": "A shadow backup namespace is derived from its literal shadow directory, so reusing a shadow name reaches the same `RootNamespace`."}
{"cluster_id": "chunk_009-243", "topic": "Creation during Removing or while a", "canonical_claim": "Creation during `Removing` or while a cleanup item is unretired must return a typed retryable refusal naming the wait, never `LOGICAL_ERROR`; creation succeeds and mints a fresh incarnation after entry removal."}
{"cluster_id": "chunk_009-244", "topic": "RemovalReady is transient rather than a", "canonical_claim": "`RemovalReady` is transient rather than a permanent tombstone, so it remains charged in catalog and fold-seal capacity reservations until the cleanup item and cursor are both retired."}
{"cluster_id": "chunk_009-245", "topic": "A removal lifecycle must contract", "canonical_claim": "A removal lifecycle must not require physical emptiness: omitted `_files` listings may leak bytes, but ref objects and namespace files are life-qualified and cannot affect later visibility or rebirth. Physical `_files` debris is outside namespace rebirth gating; incomplete `LIST` may defer reclamation and leak bytes, but cannot expose an old file through a new life or prevent the new life from being admitted."}
{"cluster_id": "chunk_009-246", "topic": "Final removal requires the exact expected", "canonical_claim": "Final removal requires the exact expected `RemovalReady` catalog row, an absent matching cleanup item, and an absent name-keyed shard-0 cursor; it then exact-deletes `_ckpt` and exact-CAS-deletes the complete observed row."}
{"cluster_id": "chunk_009-247", "topic": "The disk-transaction integration makes one logical", "canonical_claim": "The disk-transaction integration makes one logical part use one transaction, makes in-flight resolve read-your-writes state, and keeps projection sub-parts from committing before the parent whole-part transaction."}
{"cluster_id": "chunk_009-248", "topic": "The transaction ordering invariant is per", "canonical_claim": "The transaction ordering invariant is per state domain: operations touching in-memory part staging dispatch eagerly, while the deferred queue is reserved for durable non-staging effects; a total order across both domains is impossible because read-your-writes and abort safety impose opposite requirements."}
{"cluster_id": "chunk_009-249", "topic": "FREEZE must materialize real contract", "canonical_claim": "FREEZE must materialize real bytes into `shadow/` rather than create reference-only snapshots; `shadow/` is a GC reachability root and UNFREEZE releases its refs. FREEZE must materialize real bytes into the local shadow/ directory and cannot be reference-only, because shadow/'s documented contract is filesystem-readable hardlinks consumed by external backup pipelines; freeze/freezeRemote/clonePart are independent IDataPartStorage virtuals not built on createHardLinkFrom. A self-describing snapshots/<name> ref-set may exist as an additional in-pool reachability root for restore-by-reference but does not replace byte materialization."}
{"cluster_id": "chunk_009-250", "topic": "A fresh FETCH landing commits a", "canonical_claim": "A fresh FETCH landing commits a `detached` ref by folding the transaction's `recorded` blobs into the shared detached ref and rekeys `detached/tmp-fetch_<part>` to `detached/<part>` with `rekeyDetachedPartDir`."}
{"cluster_id": "chunk_009-251", "topic": "The transactional MVCC layer uses IMetadataStorage::supportsTransactionalMutableFiles", "canonical_claim": "The transactional MVCC layer uses `IMetadataStorage::supportsTransactionalMutableFiles` separately from `supportWritingWithAppend`; CA must not report `supportWritingWithAppend = true` because that would defeat the dedup-log no-append fallback and disarm the append guard."}
{"cluster_id": "chunk_009-252", "topic": "When a transaction has recorded empty", "canonical_claim": "When a transaction has `recorded` empty, `recorded_mutable` nonempty, and an existing ref, `commit` must update only the mutable sidecar in place while preserving the existing `part_id`, manifest, and ref; with no ref it must throw rather than publish a standalone empty sidecar."}
{"cluster_id": "chunk_009-253", "topic": "replaceFile routes a mutable-file destination to", "canonical_claim": "`replaceFile` routes a mutable-file destination to `recorded_mutable`, and the temporary name must itself satisfy `isMutablePerPartFile` as `<mutable>.tmp`; otherwise standalone autocommit can republish a one-file manifest and clobber the part."}
{"cluster_id": "chunk_009-254", "topic": "On CA, an uncommitted contract", "canonical_claim": "On CA, an uncommitted INSERT publishes a ref whose blobs remain GC-reachable but is governed-invisible through `txn_version.txt`; ROLLBACK removes the ref and makes the blobs GC-eligible, while `getLastModified` on a ref-less in-flight part must use the in-flight overlay. An uncommitted INSERT inside an open transaction still publishes a CAS ref whose blobs stay GC-reachable via a `WriteSession` pin until the ref lands; the part is logically invisible via its `txn_version.txt` creation CSN. ROLLBACK removes the ref, after which the blobs become GC-eligible."}
{"cluster_id": "chunk_009-255", "topic": "A multi-part disk transaction contract", "canonical_claim": "A multi-part disk transaction uses a per-part `std::map<{table_uuid, part_name}, PartStaging>` and can span a deferred `tmp_merge_X → X` rename plus `txn_version.txt` rewrites for covered source parts; CA publishes parts one at a time, with MVCC visibility providing logical atomicity and GC reclaiming crash-orphaned refs. A transactional multi-part commit is not filesystem-atomic on CAS: MVCC visibility is gated by the per-part CSN/TID in `txn_version.txt`, not by disk-op atomicity, so CAS may publish parts one at a time; a crash mid-commit leaves some refs published and some not, and orphan refs are GC-reclaimed exactly as any uncommitted-then-abandoned build (B122)."}
{"cluster_id": "chunk_009-256", "topic": "Gc::runRegularRound invokes Gc::drainCompletedRemoving immediately after lease", "canonical_claim": "`Gc::runRegularRound` invokes `Gc::drainCompletedRemoving` immediately after lease acquisition and before heartbeat work, DEFER, the hot stream LIST, the authoritative catalog cut, plan construction, or fold."}
{"cluster_id": "chunk_009-257", "topic": "The completed-removing drain uses CasRefCatalog::deleteCompletedRemovingAtSnapshot for", "canonical_claim": "The completed-removing drain uses `CasRefCatalog::deleteCompletedRemovingAtSnapshot` for an exact catalog CAS, resolves its outcome, and rescans until no eligible row remains; it performs no physical cleanup."}
{"cluster_id": "chunk_009-258", "topic": "Shard reclaim must wait until the", "canonical_claim": "Shard reclaim must wait until the shard journal is fully folded, including its tombstone, before token-guarded `deleteExact`; deleting earlier can orphan a folded edge and leak the target blob."}
{"cluster_id": "chunk_009-259", "topic": "The writer cleanup lifecycle precommits ownership", "canonical_claim": "The writer cleanup lifecycle precommits ownership, atomically replaces the precommit with the committed owner on promote under the current epoch, and cleans failed builds in resolve-grant → remove → retire order."}
{"cluster_id": "chunk_009-260", "topic": "The CAS write path hashes payloads", "canonical_claim": "The CAS write path hashes payloads, uploads or adopts blobs after a HEAD proves presence, stages a manifest, calls `precommitAdd`, and promotes the ref to committed in the ref log."}
{"cluster_id": "chunk_009-261", "topic": "S01 requires large-blob process memory to", "canonical_claim": "S01 requires large-blob process memory to stay bounded by buffers and overhead rather than blob size, scratch usage to return near baseline after commit, and large blobs to use multipart operations."}
{"cluster_id": "chunk_009-262", "topic": "S02 requires a repeated large content", "canonical_claim": "S02 requires a repeated large content blob to avoid remote body upload, with second-insert pool growth limited to manifests, refs, sidecars, and unique small metadata."}
{"cluster_id": "chunk_009-263", "topic": "S17 requires detached parts to remain", "canonical_claim": "S17 requires detached parts to remain reachable until explicitly dropped, after which dropped detached content becomes reclaimable by GC."}
{"cluster_id": "chunk_009-264", "topic": "CachedPartFolderAccess serves committed folders only; transaction", "canonical_claim": "`CachedPartFolderAccess` serves committed folders only; transaction overlays remain above it and are consulted first."}
{"cluster_id": "chunk_009-265", "topic": "Cas::Build implements the writer protocol for", "canonical_claim": "`Cas::Build` implements the writer protocol for blob puts, tree staging, evidence adoption, precommit, staged-tree upload, publish, and abandon."}
{"cluster_id": "chunk_009-266", "topic": "ContentAddressedMetadataStorage translates metadata operations into path", "canonical_claim": "`ContentAddressedMetadataStorage` translates metadata operations into path routing, namespace mapping, read-plan construction, GC scheduler lifecycle, and event-sink assembly, while `ContentAddressedTransaction` publishes one `Build` per part atomically at commit."}
{"cluster_id": "chunk_009-267", "topic": "A CAS part write stages blobs", "canonical_claim": "A CAS part write stages blobs and mutable or verbatim files, precommits the tree before uploading and publishing the ref, and on exception rolls back only refs created by that commit."}
{"cluster_id": "chunk_009-268", "topic": "In-flight transaction reads serve staged tree", "canonical_claim": "In-flight transaction reads serve staged tree entries, pending blobs, and mutable bytes from `PartStaging` before commit."}
{"cluster_id": "chunk_009-269", "topic": "Write-path source reads for hardlink, rename", "canonical_claim": "Write-path source reads for hardlink, rename, relink, or copy-forward use a fresh mode unless the view was just validated against the current root ref."}
{"cluster_id": "chunk_009-270", "topic": "Source-backed or tokened adoption rematerializes a", "canonical_claim": "Source-backed or tokened adoption rematerializes a condemned blob, while a tokenless adoptEvidence relink relies on a committed source edge and durably establishes the target edge before source removal."}
{"cluster_id": "chunk_009-271", "topic": "A current-epoch build prefix is eligible", "canonical_claim": "A current-epoch build prefix is eligible for orphan cleanup only after its build duty is conclusively settled; PartWriteTxn must not retire a build while an ownership-granting head CAS is ambiguous."}
{"cluster_id": "chunk_009-272", "topic": "Namespace removal commits one terminal node", "canonical_claim": "Namespace removal commits one terminal node containing every owner -1 and RemoveNamespace, forbids later operations in that incarnation, folds that terminal head, cleans incarnation-qualified manifests/files, and CAS-removes the catalog entry."}
{"cluster_id": "chunk_009-273", "topic": "IDataPartStorage provides part-relative operations including existsFile", "canonical_claim": "IDataPartStorage provides part-relative operations including existsFile, prepareRead, writeFile, createHardLinkFrom, rename, projection creation, cloning, removal, and transaction lifecycle."}
{"cluster_id": "chunk_009-274", "topic": "A typed CAS part-storage implementation can", "canonical_claim": "A typed CAS part-storage implementation can preserve table namespace, part ref, projection prefix, and relative file name as separate values from construction through publication."}
{"cluster_id": "chunk_009-275", "topic": "Cross-storage operations must define same-CAS manifest", "canonical_claim": "Cross-storage operations must define same-CAS manifest adoption, CA-to-ordinary copy, ordinary-to-CA copy, and movement between different CAS pools without concrete-type assumptions in fetch, clone, mutation, or storage-policy moves."}
{"cluster_id": "chunk_009-276", "topic": "The commitRefChunk removal_append arm rereads the", "canonical_claim": "The `commitRefChunk` `removal_append` arm rereads the catalog immediately before ID allocation and requires the exact catalog life to remain `Removing` before appending a terminal removal-class ref-log record."}
{"cluster_id": "chunk_009-277", "topic": "The commitRefChunk positive_append arm performs a", "canonical_claim": "The `commitRefChunk` `positive_append` arm performs a final catalog admission observation before ID allocation, requires the exact life to remain `Live`, and permanently closes the local positive lane when the row is not `Live`."}
{"cluster_id": "chunk_009-278", "topic": "dropNamespaceImpl performs a fresh exact-row catalog", "canonical_claim": "`dropNamespaceImpl` performs a fresh exact-row catalog read after the unbounded lane-drain wait and uses that observation to gate the `Live -> Removing` transition CAS."}
{"cluster_id": "chunk_009-279", "topic": "The literal plan vocabulary Poisoned refers", "canonical_claim": "The literal plan vocabulary `Poisoned` refers to `RefLaneState::NeedsRecovery`; there is no state literally named `Poisoned` in `CasRefLedger`."}
{"cluster_id": "chunk_009-280", "topic": "The real ref-lane states are RefLaneState::Ready", "canonical_claim": "The real ref-lane states are `RefLaneState::Ready`, `Writing`, `Wedged`, `NeedsRecovery`, `Closed`, and `Faulted`; there are no `Clean`, `ApplyPending`, or `Poisoned` states."}
{"cluster_id": "chunk_009-281", "topic": "Pool::drainWriterCleanupDuties appends the exact OwnerTransition removal", "canonical_claim": "Pool::drainWriterCleanupDuties appends the exact OwnerTransition removal through ref_ledger.appendRefOps, then calls retireBuildSeq(duty->build_seq), then pops the duty; an exception clears draining but retains the duty and does not retire its build sequence."}
{"cluster_id": "chunk_009-282", "topic": "An uncertain writer duty resolved as", "canonical_claim": "An uncertain writer duty resolved as reject is drained as a no-op only after the next attempt's resolve-before-reissue GET proves the key absent, and minActive advances past the rejected build."}
{"cluster_id": "chunk_009-283", "topic": "parentFoldStates contains only rows that existed", "canonical_claim": "`parentFoldStates` contains only rows that existed in the adopted parent seal and is used for baseline and cursor validation, preserving the distinction between no cursor and a default cursor state."}
{"cluster_id": "chunk_009-284", "topic": "successorFoldStates copies every catalog-admitted plan row", "canonical_claim": "`successorFoldStates` copies every catalog-admitted plan row into the mutable successor seal so later coverage and cleanup updates do not mutate the plan and every fold target has a successor row."}
{"cluster_id": "chunk_009-285", "topic": "Logical namespace removal does not depend", "canonical_claim": "Logical namespace removal does not depend on the physical `_files` object becoming empty, and the contract touches no `/_cleanup/` marker."}
{"cluster_id": "chunk_009-286", "topic": "RefTableState::Removed and its exact contract", "canonical_claim": "`RefTableState::Removed` and its exact `remove_txn_id` remain in-memory terminal state, including terminal log replay, removal ordering, mutation refusal, stale-handle behavior, and fold-cleanup evidence. `stateFromSnapshot` must explicitly initialize the returned in-memory state as `Live` because the default `RefTableState` is `Removed`. After Slice C, `RefTableState` still defaults to `RefLifecycle::Removed`, retains optional `remove_txn_id`, and terminal replay assigns the exact transaction id. `stateFromSnapshot` assigns `RefLifecycle::Live` explicitly rather than relying on the opposite-valued `RefTableState` default. In-memory `remove_txn_id`, `RefCleanupEvidence{remove_txn_id}`, and `RefLifecycle::Removed` remain intentional: they identify a terminal runtime, provide fold proof for pre-fold catalog drain, and distinguish terminal replay from an empty runtime."}
{"cluster_id": "chunk_009-287", "topic": "S43 performs its no-absorption verdict before", "canonical_claim": "S43 performs its no-absorption verdict before a recognizable life-2 insert; only that insert forces lazy namespace admission, and the inserted row must be the only visible content."}
{"cluster_id": "chunk_009-288", "topic": "Pool::dropNamespace consists of the Removing catalog", "canonical_claim": "`Pool::dropNamespace` consists of the `Removing` catalog transition, folded terminal evidence, exact catalog-row removal, and independent perpetual-janitor reclamation."}
{"cluster_id": "chunk_009-289", "topic": "The namespace-file contract fixture removes life", "canonical_claim": "The namespace-file contract fixture removes life 1 through `CasRefCatalog::deleteCompletedRemoving` with cleanup evidence and a held fence before admitting life 2 as `Live`."}
{"cluster_id": "chunk_009-290", "topic": "The decommission-to-heal flow is two-phase: the", "canonical_claim": "The decommission-to-heal flow is two-phase: the first call refuses with rows in `Removing`, a GC round deletes the catalog-only rows, and the second call retires the slot."}
{"cluster_id": "chunk_009-291", "topic": "DiskObjectStorage::supportsHardLinks returns true for CAS to", "canonical_claim": "`DiskObjectStorage::supportsHardLinks` returns true for CAS to enable mutation, lightweight `DELETE`, and data-`ALTER` capability checks without selecting a per-file autocommit path."}
{"cluster_id": "chunk_009-292", "topic": "For ContentAddressed disks, checkAlterPartitionIsPossible allows DROP", "canonical_claim": "For `ContentAddressed` disks, `checkAlterPartitionIsPossible` allows `DROP`, `ATTACH`, `REPLACE`, `MOVE`, `FETCH`, `FREEZE`, `UNFREEZE`, and `FORGET`, and rejects other partition commands fail-closed."}
{"cluster_id": "chunk_009-293", "topic": "MergeTreeDeduplicationLog::load treats a missing logs_dir as", "canonical_claim": "`MergeTreeDeduplicationLog::load` treats a missing `logs_dir` as normal for both Plain and `ContentAddressed` storage and still creates `current_writer`, because neither storage materializes empty directories."}
{"cluster_id": "chunk_009-294", "topic": "Per-part write-time files uuid.txt contract", "canonical_claim": "Per-part write-time files uuid.txt, txn_version.txt, and metadata_version.txt are ordinary PartManifest entries, not a separate mutable sidecar; a single-file change on an already-committed part resolves through a committed-ref repoint that carries every unchanged entry forward and republishes one manifest, and a byte-identical rewrite is a zero-pool-mutation no-op (repointRef). Since the all-tree part-files migration, filling in the creation CSN on COMMIT and locking/unlocking the removal TID on an already-committed part are ordinary committed-ref standalone writes through `repointRef`, not a special mutable-only case: every OTHER committed manifest entry is carried forward and a byte-identical rewrite resolves through `repointRef`'s byte-equal check with zero pool mutations."}
{"cluster_id": "chunk_009-295", "topic": "A SQL DROP PART/DROP PARTITION is", "canonical_claim": "A SQL DROP PART/DROP PARTITION is modeled as removal supersession: an additive commit creates a real part ref, a removal creates an empty covering tombstone ref promoted from MergeTreeData::dropPartImpl's local Outdated empty covering part into a persisted, Active, covering ref using the existing ActiveDataPartSet/getActiveContainingPart covering machinery. This persisted part ref is distinct from the D1 shard-object tombstone, which is a journal marker letting GC reclaim an empty shard object."}
{"cluster_id": "chunk_009-296", "topic": "DROP PART has a documented weak", "canonical_claim": "DROP PART has a documented weak guarantee under a mid-range race with a concurrent merge: the merge may produce a real covering part carrying the data, so DROP PART can end up merged into a bigger part instead of deleting it (ReplicatedMergeTreeLogEntryData::getVirtualPartNames returns {} for DROP_PART). The persisted tombstone for a mid-range single part is therefore conditional/reconciled, not unconditional: if a real covering part wins the race, the tombstone must not strand or double-cover."}
{"cluster_id": "chunk_009-297", "topic": "A transactional merge/mutation spanning multiple parts", "canonical_claim": "A transactional merge/mutation spanning multiple parts is keyed by a per-part staging map, `parts: (namespace, ref) -> PartStaging`, rather than a single `(table_uuid, part_name)`; a deferred rename (`tmp_merge_X -> X`) moves the staging entry to the destination key and merges it with any destination entry a deferred-window `txn_version.txt` write already created. `commit` takes the `gc_lock` once and iterates `parts`, applying the whole-part publish or the mutable-only branch per entry."}
{"cluster_id": "chunk_009-298", "topic": "CAS refuses BACKUP via temporary hard", "canonical_claim": "CAS refuses BACKUP via temporary hard links for Ordinary (non-UUID) databases with `SUPPORT_IS_DISABLED`, while Atomic databases back up normally; partition commands are refused outside an explicit allowlist (DROP_PARTITION, DROP_DETACHED_PARTITION, FORGET_PARTITION, ATTACH_PARTITION, REPLACE_PARTITION, MOVE_PARTITION, FETCH_PARTITION, FREEZE_*, UNFREEZE_*); `supportsChmod`/`supportsStat` are false and `getHardlinkCount` returns 0; bucket versioning must not be enabled and is probed and refused; and `<readonly>1</readonly>` disks take no mount claim, run no probe, no writes, and no GC."}
{"cluster_id": "chunk_009-299", "topic": "S06/S07 (10000/20000-column wide parts) confirmed the", "canonical_claim": "S06/S07 (10000/20000-column wide parts) confirmed the manifest encode/decode works correctly at scale: a 10000-column wide part commits under the manifest hard cap with no LIMIT_EXCEEDED needed, and the 1M-entry manifest cap is unreachable at any SQL-feasible column count (three orders of magnitude below 20000)."}
{"cluster_id": "chunk_009-300", "topic": "The removal driver resumes entry deletion", "canonical_claim": "The removal driver resumes entry deletion idempotently on the owning writer's next mount after a stop in the `Removing`-without-`_ckpt` window; it never re-creates `_ckpt`, and the dead-root branch belongs to the decommission path instead."}
{"cluster_id": "chunk_009-301", "topic": "namespaceIsRemoved gates its recreate-flip on the", "canonical_claim": "`namespaceIsRemoved` gates its recreate-flip on the `_cleanup` marker and `dropNamespace` publishes a constant-size `Removed` snapshot; both reader-side predicates must be re-derived from catalog state (`Removing`, then entry-absent) before the `_cleanup` marker class can be deleted."}
{"cluster_id": "chunk_009-302", "topic": "The writer no longer synchronizes a", "canonical_claim": "The writer no longer synchronizes a retired view or writer-side ack floor; it point-reads an add-only freshness meta at promote and copy-forward."}
{"cluster_id": "chunk_009-303", "topic": "The planned registry-free newborn protocol creates", "canonical_claim": "The planned registry-free newborn protocol creates a precommit-state shard fenced to the current `gcRound`, and gates `promote` on the shard's own `fence_round` rather than a registry fence."}
{"cluster_id": "chunk_009-304", "topic": "A first publish into a new", "canonical_claim": "A first publish into a new namespace uses the existing precommit machinery: the ref-shard becomes a precommit-state object, its `+1` is protected by the fold barrier, and there is no separate `pending-newborns` object."}
{"cluster_id": "chunk_009-305", "topic": "Phase 2 makes CachedPartFolderAccess the owner", "canonical_claim": "Phase 2 makes `CachedPartFolderAccess` the owner of freshness policy and terminal committed-ref primitives while `ContentAddressedMetadataStorage` retains path semantics and transactions retain staging orchestration."}
{"cluster_id": "chunk_009-306", "topic": "Write-through erases are cache hygiene rather", "canonical_claim": "Write-through erases are cache hygiene rather than a correctness dependency; `dropRefBestEffort` erases even when its underlying drop fails because durable ref state is then unknown."}
{"cluster_id": "chunk_009-307", "topic": "The two-level write API puts terminal", "canonical_claim": "The two-level write API puts terminal committed-ref primitives and `republishRef` in the facade, while `publishEntries` replaces the near-duplicate publish body of `adoptPartFromManifest` and transaction staging remains in the transaction."}
{"cluster_id": "chunk_009-308", "topic": "Writer cleanup removes stale precommit ownership", "canonical_claim": "Writer cleanup removes stale precommit ownership, and weak fairness of successor maintenance is expected to eventually clean pending stale ownership."}
{"cluster_id": "chunk_009-309", "topic": "Dead-precommit removal moved from GC to", "canonical_claim": "Dead-precommit removal moved from GC to the writer, which appends exact `owner_transition` removals; GC reclaims the resulting blobs through ordinary edge cancellation."}
{"cluster_id": "chunk_009-310", "topic": "Pool-member decommission performs full srid erasure", "canonical_claim": "Pool-member decommission performs full srid erasure of namespaces, manifest debris, staging, roots objects, and the slot, while subsequent normal GC rounds reclaim the now-unreferenced blob bytes."}
{"cluster_id": "chunk_009-311", "topic": "On atomic-write storages, VersionMetadataOnDisk writes version", "canonical_claim": "On atomic-write storages, `VersionMetadataOnDisk` writes version information with one `writeFile` instead of a temporary file plus rename."}
{"cluster_id": "chunk_009-312", "topic": "The freeze metadata_version_written_by_freeze special case is", "canonical_claim": "The freeze `metadata_version_written_by_freeze` special case is removed because an identical post-clone rewrite is absorbed by the byte-equal repoint no-op, while a differing rewrite may legally repoint."}
{"cluster_id": "chunk_009-313", "topic": "Pool/CasBlobUploader moves byte-delivery execution out of", "canonical_claim": "`Pool/CasBlobUploader` moves byte-delivery execution out of `CasBuild`, while admit/adopt/resurrect decisions and dependency-set recording remain on the transaction class."}
{"cluster_id": "chunk_009-314", "topic": "The proposed MOVE-to-CA fix is a", "canonical_claim": "The proposed MOVE-to-CA fix is a CAS-aware `clonePart` path that stages the whole part in one `ContentAddressedTransaction`; the generic per-file cross-disk copy path is insufficient."}
{"cluster_id": "chunk_009-315", "topic": "Whole-part atomicity sites such as freeze", "canonical_claim": "Whole-part atomicity sites such as `freeze`, restore-from-backup, and B58 projection commits remain single-`commit` transactions because this design deliberately introduces no `precommit` phase."}
{"cluster_id": "chunk_009-316", "topic": "Verbatim table-level and mountpoint files are", "canonical_claim": "Verbatim table-level and mountpoint files are treated as an immediate autocommit class: writes are durable on finalize and deletes or moves remain durable at call time, avoiding a second timeline for those files."}
{"cluster_id": "chunk_009-317", "topic": "The MOVE-to-CA design was reframed as", "canonical_claim": "The MOVE-to-CA design was reframed as one whole-part `ContentAddressedTransaction` threaded through the move, plus a layer-1 parser fix so the final part identity is used instead of `moving`; the earlier claim that a transactional copy would lack full build semantics was rejected."}
{"cluster_id": "chunk_009-318", "topic": "Review finding №12's call-time mutation of", "canonical_claim": "Review finding №12's call-time mutation of durable refs is by design: `renameParts` is the commit point and rollback compensates with new operations over committed disk state, but `removeDirectory` must discard staged entries when removing a part before commit."}
{"cluster_id": "chunk_009-319", "topic": "For CA part moves, the crash-atomic", "canonical_claim": "For CA part moves, the crash-atomic design uses a transient `moving/<part>` reference prefix: `clonePart` commits under the moving reference and the mover later repoints it to the final reference, so startup cleanup can reclaim an orphaned moving reference."}
{"cluster_id": "chunk_009-320", "topic": "The production INSERT topology contract", "canonical_claim": "The production INSERT topology commits one `ContentAddressedTransaction` per part, so cross-part batching must occur at the `MergeTreeData::Transaction::renameParts` seam rather than inside `ContentAddressedTransaction::commit`. A plain INSERT performs one `ContentAddressedTransaction::commit()` per part with `parts.size() == 1`, so inner-loop parallelism cannot improve INSERT; cross-part concurrency must be introduced above the disk transaction."}
{"cluster_id": "chunk_009-321", "topic": "The phased engine uses one precommit", "canonical_claim": "The phased engine uses one precommit ledger batch, one flattened cross-part upload wave, and one promote ledger batch; a 500-part INSERT is expected to reduce about 1026 serial ref-log PUTs to about two."}
{"cluster_id": "chunk_009-322", "topic": "The approved inner-loop design dispatches each", "canonical_claim": "The approved inner-loop design dispatches each part's unchanged `stageManifest → precommitAdd → uploadPendingBlobs → promote` sequence to bounded workers, preserving per-part ordering while overlapping parts."}
{"cluster_id": "chunk_009-323", "topic": "The CAS commit pool must use", "canonical_claim": "The CAS commit pool must use bounded worker-loop callbacks or a dedicated pool sized by `cas_commit_concurrency`; scheduling one task per part on a shared 100-thread pool would not enforce the bound and could starve unrelated users."}
{"cluster_id": "chunk_009-324", "topic": "The initial multi-commit design used commitTransactions", "canonical_claim": "The initial multi-commit design used `commitTransactions` as a transaction-centric seam: generic implementations loop sequentially, while `MergeTreeData::Transaction::renameParts` groups same-disk transactions without leaking `isContentAddressed` checks into generic MergeTree code."}
{"cluster_id": "chunk_009-325", "topic": "Move assignment for PreparedPartWrite is deleted", "canonical_claim": "Move assignment for `PreparedPartWrite` is deleted because overwriting a handle that still owes a terminal action may require a fallible `abandon`, which move assignment cannot report without dropping cleanup ownership."}
{"cluster_id": "chunk_009-326", "topic": "The part-write release seam is relink-independent", "canonical_claim": "The part-write release seam is relink-independent: any `PartWriteTxn` may leave a staged precommit whose release is unproven, and the accounting is intentionally not attributed to relink or another consumer."}
{"cluster_id": "chunk_009-327", "topic": "~PartWriteTxn, rather than ~PreparedPartWrite, owns the", "canonical_claim": "`~PartWriteTxn`, rather than `~PreparedPartWrite`, owns the final release word because `prepareEntries` can fail before constructing a handle; the transaction destructor therefore covers both handle and no-handle staging exits."}
{"cluster_id": "chunk_009-328", "topic": "Poisoned is not equivalent to a", "canonical_claim": "`Poisoned` is not equivalent to a wedge: it means a durable transaction may be missing from the cached view, so it continues to block snapshot publication and triggers re-recovery; making `ApplyPending` debug-only is a follow-up, not part of Stage B."}
{"cluster_id": "chunk_009-329", "topic": "The RemovalReady protocol is adopted as", "canonical_claim": "The `RemovalReady` protocol is adopted as a transient catalog state between `Removing` and entry deletion: it is entered only after the terminal record has folded and the life-scoped cleanup item is durably `Completed`."}
{"cluster_id": "chunk_009-330", "topic": "The final recommendation is to keep", "canonical_claim": "The final recommendation is to keep `NamespaceLifeId` and incarnation-qualified ref and namespace-file keys, avoid permanent `Retired` history and extra retirement markers, and delete a catalog entry only after terminal fold, cleanup/cursor retirement, and exact `_ckpt` deletion ordering is complete."}
{"cluster_id": "chunk_009-331", "topic": "The incarnation is retained because temporary", "canonical_claim": "The incarnation is retained because temporary refusal alone cannot make omitted old physical objects safe for eventual same-name reuse; the generation makes old ref objects and namespace files structurally inert."}
{"cluster_id": "chunk_009-332", "topic": "The RemovalReady state machine is Creating", "canonical_claim": "The `RemovalReady` state machine is `Creating -> Live -> Removing -> RemovalReady -> absent`; `RemovalReady` means no writer publication, recovery, ref fold, or new cursor is permitted, and only exact removal of the row is legal."}
{"cluster_id": "chunk_009-333", "topic": "Build::abandon must call retireBuildSeq only after", "canonical_claim": "`Build::abandon` must call `retireBuildSeq` only after the precommit-removal `mutateShard`, so the build becomes watermark-dead only after its removal is durable; this ordering mirrors `Build::promote`."}
{"cluster_id": "chunk_009-334", "topic": "The tokenless DETACH/freeze adopt path can", "canonical_claim": "The tokenless DETACH/freeze adopt path can race GC condemnation because `copyForwardFromCondemned` runs once before the CAS closure; the proposed fix is a bounded copy-forward and re-HEAD loop outside the closure, analogous to tokened resurrection."}
{"cluster_id": "chunk_010-001", "topic": "part lifecycle claim", "canonical_claim": "SQL DROP PART or DROP PARTITION is represented by a covering tombstone ref in the CAS model, distinct from the D1 shard-object tombstone."}
{"cluster_id": "chunk_010-002", "topic": "part lifecycle claim", "canonical_claim": "FREEZE uses one ref per frozen part rather than a shared container to avoid shared-`detached` read-modify-write and B66a torn-read hazards under concurrent freeze."}
{"cluster_id": "chunk_010-003", "topic": "part lifecycle claim", "canonical_claim": "Catalog-only pre-fold erasure of a proved `Removing` lifecycle row is independent of the later physical destructive gate and must not be suppressed by unrelated later-round anomalies or carried holds."}
{"cluster_id": "chunk_010-004", "topic": "part lifecycle claim", "canonical_claim": "Content-addressed storage requires a real transaction commit point, so `RegisterDiskObjectStorage.cpp` sets `needs_real_transaction=true` for CAS."}
{"cluster_id": "chunk_010-005", "topic": "part lifecycle claim", "canonical_claim": "The consistency review recommends using `TemporaryFileOnDisk` for CAS spill cleanup and considering `WriteBuffer`'s idempotent `finalize`/`cancel` state machine for the duplicated write-sink lifetime state."}
{"cluster_id": "chunk_010-006", "topic": "part lifecycle claim", "canonical_claim": "CAS should keep the logical part format Full; it changes physical representation and publication protocol, not the independent MergeTree files such as checksums.txt, columns, marks, projections, and metadata."}
{"cluster_id": "chunk_010-007", "topic": "part lifecycle claim", "canonical_claim": "A candidate DataPartStorageContentAddressed would carry typed CAS root namespace, ref, and optional projection-relative prefix, while file operations add only the part-relative file name."}
{"cluster_id": "chunk_010-008", "topic": "part lifecycle claim", "canonical_claim": "Build-root reachability and fail-closed commit are both required to prevent the `B140` dangle; either mechanism alone still has a counterexample."}
{"cluster_id": "chunk_010-009", "topic": "part lifecycle claim", "canonical_claim": "The `EDGE-BEFORE-OBSERVE` order is precommit with durable closure, then adopt/observe, then promote; under this order promote-time revalidation of tokened leaves is redundant."}
{"cluster_id": "chunk_010-010", "topic": "part lifecycle claim", "canonical_claim": "The `TXN-ONE-PIPELINE` design gives `IDiskTransaction::precommit` an explicit two-phase contract: CA precommit performs manifest publish through promote, CA commit materializes durable intent, and one `dispatch` funnel replaces the CA subclass."}
{"cluster_id": "chunk_010-011", "topic": "part lifecycle claim", "canonical_claim": "The T6 retirement fence is correct: slot retirement is forbidden while any entry owned by the root remains, so the proposed exemption for just-transitioned `Removing` rows was rejected."}
{"cluster_id": "chunk_010-012", "topic": "projection transaction sharing", "canonical_claim": "Projection sub-parts must use the parent part's whole-part transaction; commit `24f89d7ce78` routed merge/mutate projection sub-parts through that parent transaction, and commit `73b03c9a1e4` encapsulated the rule in borrowed projection storage. Additionally, `DataPartStorage` owns the rule for whether a projection storage opens its own transaction or borrows the parent transaction, so call sites do not need storage-specific transaction branches. Additionally, A logical clone of a part should be one operation represented by one disk transaction rather than independent autocommit file operations, especially for object-storage-like disks. Additionally, CAS cannot use early sub-part commits as a read-back mechanism because publishing a sub-part before its parent would expose a partial part; the sub-part must borrow the parent transaction and read staged files through its overlay. Additionally, `DataPartStorageOnDiskFull::beginTransaction` and `commitTransaction` are no-ops when `has_shared_transaction` is true, centralizing the rule that CA projections ride the parent whole-part transaction. Additionally, `getProjectionPartBuilder` sets `use_parent_transaction = !is_temp_projection || isContentAddressed()` so a CAS temporary projection sub-part shares the parent whole-part transaction. Additionally, `MergeTask` uses a `projection_uses_parent_transaction` flag and unconditional projection `beginTransaction`/`commitTransaction` calls so CAS projection sub-parts share the parent whole-part transaction."}
{"cluster_id": "chunk_010-013", "topic": "part lifecycle claim", "canonical_claim": "CAS unlink operations were eagerly staged in memory and program order to fix the `01603` column-TTL ordering inversion; under the two-domain invariant, unlink dispatch remains eager while the durable delete is materialized as an intent at commit."}
{"cluster_id": "chunk_010-014", "topic": "atomic transaction-version write", "canonical_claim": "When `supportsAtomicFileWrites` is true, `VersionMetadataOnDisk` writes `txn_version.txt` directly instead of using a temporary file and `replaceFile`, because the storage itself provides atomic publication. Additionally, Since Task 5's `supportsAtomicFileWrites` capability, `VersionMetadataOnDisk::storeInfoToDataPartStorage` writes `txn_version.txt` as a single atomic `writeFile` with no `.tmp` file and no `replaceFile` call; `ContentAddressedTransaction::moveFile`'s legacy mutable-per-part-file destination routing is dead code in production for CA, still present pending a later mechanical sweep but unreached by any current write path."}
{"cluster_id": "chunk_010-015", "topic": "part lifecycle claim", "canonical_claim": "The optional age-based trickle warmer pushes blobs of hot parts older than a threshold `A` into the pool ahead of any snapshot, on the reasoning that a part surviving the young-merge window will likely reach the cold tier eventually; with the warmer on, consolidation degrades into publishing metadata only, and TTL moves become nearly free."}
{"cluster_id": "chunk_010-016", "topic": "part lifecycle claim", "canonical_claim": "`kMutablePerPartFiles` was renamed to `kPerRefPartFiles` (with per-file rationale documented): `txn_version.txt` is genuinely mutable in-place (MVCC rewrites it after part publication) and stays a per-ref mutable sidecar; `uuid.txt` and `metadata_version.txt` are not mutated in-place under the single-owner-manifest model and were reclassified as ordinary immutable part files inside the manifest rather than sidecars, since the old dual-part-ref-sharing-one-manifest rationale for excluding them no longer applies once CAS stages a fresh `ManifestId` per ref."}
{"cluster_id": "chunk_010-017", "topic": "part lifecycle claim", "canonical_claim": "The alleged abandoned copy-forward orphan exists only in a raw `putOverwrite` shape; in the real `republishRef` flow, `stageManifest` and `precommitAdd` precede promote, and the watermark plus fresh-token lifecycle self-heals a writer that dies after the copy-forward PUT."}
{"cluster_id": "chunk_010-018", "topic": "part lifecycle claim", "canonical_claim": "The 56 retained blobs were traced to four `tmp-fetch_*` refs published and dropped within one 43 ms window; their `+1` was folded about three minutes after the drop and no `-1` was recorded."}
{"cluster_id": "chunk_010-019", "topic": "part lifecycle claim", "canonical_claim": "Normal `Atomic` recreation creates a fresh table UUID and therefore a fresh namespace; it does not wait for the prior namespace to be removed."}
{"cluster_id": "chunk_010-020", "topic": "part lifecycle claim", "canonical_claim": "Commit `6c495863667` introduced `rename_in_transaction` for lock-free part renames in `ReplicatedMergeTree`, and commit `3675c27fe9e` required explicit part renames in a transaction."}
{"cluster_id": "chunk_010-021", "topic": "part lifecycle claim", "canonical_claim": "Scenario S37 passed 22 of 23 verdicts; all non-MOVE multi-disk legs passed, including policy placement, insert-time `max_data_part_size` routing, truthful `disk_name`, restart re-attach, and mixed-disk merge, while only cross-disk `clonePart` to CA failed."}
{"cluster_id": "chunk_010-022", "topic": "part lifecycle claim", "canonical_claim": "Moving CA disk commit from `Transaction::commit` to `renameParts` moves S3 publish I/O off the `data_parts` lock without changing total I/O volume; the accepted cost is redo work when a later external decision rejects the part."}
{"cluster_id": "chunk_010-023", "topic": "part lifecycle claim", "canonical_claim": "Publishing a cloned CA part directly under the final `<part>` reference before the mover swaps it is rejected because the final directory already exists and a crash can leave a committed final reference that moving-reference cleanup cannot reclaim."}
{"cluster_id": "chunk_010-024", "topic": "part lifecycle claim", "canonical_claim": "Setting `alive = false` in `PartWriteTxn::promote` was rejected because existing promote-adjacent paths and the transaction destructor rely on current `abandon` no-op or soft-fail behavior."}
{"cluster_id": "chunk_010-025", "topic": "part lifecycle claim", "canonical_claim": "Nested sub-manifests per projection (each projection its own sub-PartManifest plus a projections footer) were rejected as a format/version change requiring a recursive resolver for identity projections never use, since projections are always read as children and rebuilt on merge (YAGNI). A separate projections/ ref namespace lifecycle-linked to the parent was also rejected as a second namespace plus GC coupling for no benefit."}
{"cluster_id": "chunk_010-026", "topic": "part lifecycle claim", "canonical_claim": "Task A5's detached-part-path fix anchors the reserved `detached` directory component left-to-right (first match), not the right-to-left scan the original spec sketched, because a right-to-left scan would incorrectly anchor on the inner part-dir-shaped component first."}
{"cluster_id": "chunk_010-027", "topic": "part lifecycle claim", "canonical_claim": "A thread-local \"active slot\" design for the part-folder cache was rejected as unnecessary v1 complexity with no measurable benefit over a bounded shared map lookup."}
{"cluster_id": "chunk_010-028", "topic": "part lifecycle claim", "canonical_claim": "The part-write crash-point table enumerates nine distinct crash windows (C1-C9) and their cleanup owner: C1 staging leaves local temp files (cleaned unconditionally) or S3 staging objects (cleaned only by the mount's own next-mount staging sweep, never on abort); C2 post-stageManifest-pre-precommitAdd leaves an unreferenced manifest cleaned by best-effort delete or the orphan sweep backstop; C3 an Unresolved precommitAdd leaves a possibly-live binding whose body is never writer-deleted; C4 between precommitAdd and promote has no resume path (INV-10) and is removed by abandon or a fenced successor's stale-precommit sweep; C5 mid blob fan-out leaves already-uploaded blobs as GC-reclaimable debris with nothing merged; C6 an Unresolved promote append leaves `CommitState::Uncertain`, mapped by the relink layer to \"retry the whole fetch\"; C7 a later part throwing after earlier parts published triggers precise rollback of only the refs this call created; C8 a destroyed uncommitted transaction has its destructor abandon every open build (INV-19); C9 a namespace dropped mid-build sets one atomic flag so every further op fails closed at `requireAlive`. Every row leaks at worst; no row loses data or dangles."}
{"cluster_id": "chunk_010-029", "topic": "part lifecycle claim", "canonical_claim": "Free carry-forward on mutation (re-referencing unchanged column blobs without recompute) applies only to Wide parts, whose default remote-disk threshold is 1 GiB; Compact parts are a single blob and are always fully rewritten on mutation."}
{"cluster_id": "chunk_010-030", "topic": "part lifecycle claim", "canonical_claim": "S36 is commissioned to test `ALTER TABLE ... MOVE PART|PARTITION TO DISK/VOLUME` in both local-to-CA and CA-to-local directions, including no dangling refs and round-trip deduplication."}
{"cluster_id": "chunk_010-031", "topic": "part lifecycle claim", "canonical_claim": "The removal design must retain a negative test proving that cleanup failure or `_files` residue does not gate lifecycle completion, even when the bounded pass performs a best-effort `_files` listing."}
{"cluster_id": "chunk_010-032", "topic": "part lifecycle claim", "canonical_claim": "The proposed spike should centralize DataPartStorageOnDiskFull construction, add a skeletal DataPartStorageContentAddressed for CA disks, implement one typed committed read and transactional write, test temporary-to-final rename and projection transaction sharing, and compare typed and path-derived routes."}
{"cluster_id": "chunk_010-033", "topic": "part lifecycle claim", "canonical_claim": "The `B92` adopt path must populate the correct `tree_size` instead of leaving it at zero."}
