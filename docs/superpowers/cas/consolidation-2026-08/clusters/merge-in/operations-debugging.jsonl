{"cluster_id": "chunk_012-122", "topic": "The scenario scratch high-water sampler was not reliable at scale because its 2-", "canonical_claim": "The scenario scratch high-water sampler was not reliable at scale because its 2-second interval missed the `OPTIMIZE FINAL` spike; direct measurement found 93 GiB while the sampler reported 0.100 GiB."}
{"cluster_id": "chunk_012-123", "topic": "A background merge can rewrite payload inside S09's mutation measurement window ", "canonical_claim": "A background merge can rewrite payload inside S09's mutation measurement window and falsely report pool growth; consolidating with `OPTIMIZE TABLE ... FINAL` before establishing the baseline isolates mutation carry-forward, while `SYSTEM STOP MERGES` also stops mutations because they use the merge scheduler."}
{"cluster_id": "chunk_012-124", "topic": "Consolidated After the per-server-tree relocation, manifests live under `cas/m", "canonical_claim": "After the per-server-tree relocation, manifests live under `cas/manifests/<srid>/...` and refs under `cas/refs/<srid>/...`; the old classifiers placed the tree in `other`, causing S08 to report 858081 objects and 138 GB as `other` and allowing a real manifest leak to pass the leftover verdict. Additionally, The harness `pool_shape` classifier was stale after per-server-tree relocation: S08 classified 858081 objects and 138 GB as `other` while `_manifests=0` for a 100k-part pool."}
{"cluster_id": "chunk_012-125", "topic": "Consolidated The harness `gc_log_rows` query used removed `forgotten_on_delete", "canonical_claim": "The harness `gc_log_rows` query used removed `forgotten_on_delete` and `forgotten_absent` columns, causing `UNKNOWN_IDENTIFIER` on every poll and making every GC verdict in the 2026-07-05 campaign vacuous. Additionally, The campaign harness `gc_log` query still referenced removed columns `forgotten_on_delete` and `forgotten_absent`, causing 213 `UNKNOWN_IDENTIFIER` errors per night and empty `gc_log` capture."}
{"cluster_id": "chunk_012-126", "topic": "Before scoped and partial fsck, the scenario campaign lost five verdicts because", "canonical_claim": "Before scoped and partial fsck, the scenario campaign lost five verdicts because fsck timed out at large object counts and returned no accumulated result."}
{"cluster_id": "chunk_012-127", "topic": "The S24 replica-agreement failure was a harness timing issue", "canonical_claim": "The S24 replica-agreement failure was a harness timing issue: the check ran before replication converged because it lacked `SYSTEM SYNC REPLICA`, while a post-run check showed identical 280-row replicas and checksum `8663329780789566770`."}
{"cluster_id": "chunk_012-128", "topic": "Before the observability package, `system", "canonical_claim": "Before the observability package, `system.content_addressed_log` could not show a manifest body birth or writer-side precommit removal, so a precommit created and never removed required raw-object decoding to diagnose."}
{"cluster_id": "chunk_012-129", "topic": "Before `ca-inspect`, diagnosing a CA bucket object required an ephemeral `mc` co", "canonical_claim": "Before `ca-inspect`, diagnosing a CA bucket object required an ephemeral `mc` container, an `od` hexdump, and hand parsing of protobuf or custom binary data even though decoders already existed."}
{"cluster_id": "chunk_012-130", "topic": "The S16 strict resurrect scenario can fail deterministically when `forced_gc_to_", "canonical_claim": "The S16 strict resurrect scenario can fail deterministically when `forced_gc_to_fixpoint` deletes bodies before reinsertion, because reinsertion then performs a plain `blob_put` instead of resurrecting a condemned-but-present token; this is scenario-cycle tuning rather than a product bug."}
{"cluster_id": "chunk_012-131", "topic": "The S06/S07 replica-agreement oracle sampled the second node roughly 0", "canonical_claim": "The S06/S07 replica-agreement oracle sampled the second node roughly 0.8–0.9 seconds before the fetch landed because it lacked `SYNC REPLICA` or a bounded wait; the product was clean after the RustFS file-descriptor issue was fixed."}
{"cluster_id": "chunk_012-132", "topic": "The S31 dry-run tool inspects only shard 0, so its result is a known tool-only g", "canonical_claim": "The S31 dry-run tool inspects only shard 0, so its result is a known tool-only gap rather than a CAS product verdict."}
{"cluster_id": "chunk_012-133", "topic": "The S38 harness could make `assert_gc_no_failed` vacuous because `observe", "canonical_claim": "The S38 harness could make `assert_gc_no_failed` vacuous because `observe.py` queried the removed `min_ack` column and swallowed `UNKNOWN_IDENTIFIER`."}
{"cluster_id": "chunk_012-134", "topic": "The `CasBlobInDegree", "canonical_claim": "The `CasBlobInDegree.h` documentation claiming that `previewDeletes` uses `inDegreeInGeneration` is false; `previewDeletes` uses `zeroInDegree` and a raw `kCondemned` scan."}
{"cluster_id": "chunk_012-135", "topic": "The B199 recovery test fixture produced a foreign seal by appending `\u0001_FOREIGN_D", "canonical_claim": "The B199 recovery test fixture produced a foreign seal by appending `\u0001_FOREIGN_DIFFERENT` to valid seal bytes; after the formats-v3 cutover, that byte sequence is no longer a valid seal object."}
{"cluster_id": "chunk_012-136", "topic": "Consolidated The ASan battery scripts `build/asan_battery", "canonical_claim": "The ASan battery scripts `build/asan_battery.sh` and `build/asan_battery2.sh` misidentified aborting tests because plain `grep` treated round logs containing embedded NUL bytes from CAS payloads as binary files. Additionally, The ASan battery's culprit-detection scripts used plain `grep` (not `grep -a`) on archived round logs containing thousands of embedded NUL bytes from CAS blob payloads in stdout, causing silent misattribution: rounds 27-66 (39 consecutive rounds) were all the same unfixed `CasRefSnapshotFormat.RejectsSnapshotIdBelowSealedFrom` abort, never actually excluded, so the peeling loop looped on one bug while accumulating ~30 wrong bystander test names in the known-debt list; the fix switched the scripts to `grep -a` and the exclusion list was corrected from 58 down to 28 verified real culprits. Additionally, Because of the binary-log parsing bug, `CasRefSnapshotFormat.RejectsSnapshotIdBelowSealedFrom` aborted rounds 27 through 66, a 39-round loop in which the battery accumulated false bystander exclusions instead of converging. Additionally, The ASan culprit detector must use `grep -a` for round logs, and the corrected `build/asan_battery_aborted.txt` contains 28 verified real culprits rather than the previous 58-name list; the bogus list is preserved as `build/asan_battery_aborted.txt.bak_bogus`."}
{"cluster_id": "chunk_012-137", "topic": "The seven ASan memory-safety findings from rounds 9, 10, 18, 19, 20, 21, and 23 ", "canonical_claim": "The seven ASan memory-safety findings from rounds 9, 10, 18, 19, 20, 21, and 23 were independently identified from symbolized AddressSanitizer stacks and are unaffected by the faulty grep-based culprit heuristic."}
{"cluster_id": "chunk_012-138", "topic": "`observe", "canonical_claim": "`observe.gc_log_all(cl, since_inject).summary.success` under-counted S38 by returning 0 even though the raw GC log contained 40 post-injection Success rows; a timestamp-format versus `since` comparison is the prime suspected harness cause."}
{"cluster_id": "chunk_012-139", "topic": "`quiesce_cluster` falsely marked S08 inconclusive because it failed immediately ", "canonical_claim": "`quiesce_cluster` falsely marked S08 inconclusive because it failed immediately on any transient non-empty `replication_queue.last_exception` instead of applying the sibling backlog-stall grace period; commit `35faaae182c` added the grace period and two regression tests."}
{"cluster_id": "chunk_012-140", "topic": "Consolidated Seven ASan gtest findings were test-lifetime bugs rather than pro", "canonical_claim": "Seven ASan gtest findings were test-lifetime bugs rather than product bugs: six stack-use-after-scope cases came from an event-sink vector declared after `Pool`, and `CasPoolShutdown.UnresolvedWedgeSkipsFarewell` had a heap-use-after-free from reading `store->layout()` after `store.reset()`. Additionally, An ASan battery run found 7 genuine memory-safety bugs among 49 peeled aborts: 6 stack-use-after-scope instances in `CasSweepLateLog`-family tests, root-caused to an event-sink lambda capturing a local `events` vector declared AFTER the `Pool` object, so `~Pool` emits terminate events into the already-destroyed vector (fixed by declaring the vector before the Pool); and 1 heap-use-after-free in `CasPoolShutdown.UnresolvedWedgeSkipsFarewell`, root-caused to a `const Layout &` held across `store.reset()` (fixed by taking it by value)."}
{"cluster_id": "chunk_012-141", "topic": "Consolidated The CAS gtest battery's established `Cas*", "canonical_claim": "The CAS gtest battery's established `Cas*:CA*:...` filter had never matched about 89–90 tests in the `CaWiring*`, `CaTransaction*`, `CaDedupCache*`, `CaInlinePlacement*`, and `CaPartPathParser*` suites; correcting the filter exposed three bugs in `gtest_ca_wiring.cpp`. Additionally, The obvious CAS gtest filter `Cas*:CA*` under-selects entire families such as `RefWriter*` and `CaWiring*`; the release gate must enumerate test families explicitly. Additionally, The CAS gate filter `Cas*:CA*` omitted `CaWiring`, `CaLifecycle`, several `RefWriter*` suites, and `RefTableCacheEviction`; twenty suites, including 17 CAS-relevant suites, were unclaimed by the gate."}
{"cluster_id": "chunk_012-142", "topic": "Consolidated Constructing `ReadBufferFromMemoryFileBase` with `owns_memory=tru", "canonical_claim": "Constructing `ReadBufferFromMemoryFileBase` with `owns_memory=true` and empty data can pass `nullptr` from `std::string_view::data()` to `memcpy` with length zero, violating the nonnull contract under UBSan. Additionally, The empty-file `ReadBufferFromMemoryFileBase` fix is to skip `memcpy` when `data.empty()`; no bytes need copying and the owned buffer must still construct at EOF."}
{"cluster_id": "chunk_012-143", "topic": "The mount-lease renewal classifier, GCS conditional dialect tests, and dedup-log", "canonical_claim": "The mount-lease renewal classifier, GCS conditional dialect tests, and dedup-log null-writer tests exposed `LOGICAL_ERROR` construction behavior that aborts debug or sanitizer builds before `catch` can run."}
{"cluster_id": "chunk_012-144", "topic": "The `DispatchThrowStillDrains` test was racy because it could cancel the first s", "canonical_claim": "The `DispatchThrowStillDrains` test was racy because it could cancel the first scheduled task before task entry; a latch must prove that already-running work is drained before dispatch unwinds."}
{"cluster_id": "chunk_012-145", "topic": "A pre-existing ref-writer test could spin forever because its polling loop waite", "canonical_claim": "A pre-existing ref-writer test could spin forever because its polling loop waited for a pending item that the throwing pre-carve hook had already erased; pinning execution to one CPU reproduced the hang 8/8 times while an idle 32-core machine passed 10/10."}
{"cluster_id": "chunk_012-146", "topic": "The CI scrape was post-mortem rather than concurrent", "canonical_claim": "The CI scrape was post-mortem rather than concurrent: `ClickHouseProc.dump_system_tables` stops the server before using `clickhouse local`, and the CA pool is opened under an unrelated identity against the same data directory."}
{"cluster_id": "chunk_012-147", "topic": "`CaBuildRootPrecommit", "canonical_claim": "`CaBuildRootPrecommit.tla:300` had an unparenthesized `dupFlag' = dupFlag / (...)` precedence bug that could leave `dupFlag'` unspecified; the bug was latent because both affected sabotage configurations reached their invariant violation first."}
{"cluster_id": "chunk_012-148", "topic": "The old TLA classifier pattern `[A-Za-z_]+` failed to recognize invariant names ", "canonical_claim": "The old TLA classifier pattern `[A-Za-z_]+` failed to recognize invariant names containing digits such as `I1ForgetTerminal` and `I2EarnedFarewell`; widening it to `[A-Za-z0-9_]+` was required to avoid false `error` outcomes and false passes based only on nonzero exit codes."}
{"cluster_id": "chunk_012-149", "topic": "The T15 trace dump SELECT aliased `'${tt}' AS trace_type`, and ClickHouse alias ", "canonical_claim": "The T15 trace dump SELECT aliased `'${tt}' AS trace_type`, and ClickHouse alias resolution in `WHERE` caused the literal to shadow the table column, so the dump was an unfiltered mix of trace types until fix `52f110e94b3`."}
{"cluster_id": "chunk_012-150", "topic": "`CaBuildRootPrecommit_empty_trees", "canonical_claim": "`CaBuildRootPrecommit_empty_trees.cfg` failed because the model's explicit assumption was false when `Trees = {}`; this is a model-domain restriction, not a generic empty-set checker failure."}
{"cluster_id": "chunk_012-151", "topic": "Consolidated `CaRefWriterCleanupCore_empty_builds", "canonical_claim": "`CaRefWriterCleanupCore_empty_builds.cfg` reported a temporal violation for `StalePrecommitEventuallyGone` even though `Builds = {}` makes `StaleExists` false and `NoStale` true in every behavior. Additionally, The empty-build temporal violation was a defect in supplied TLC 2.19 (2024-08-08, revision `5a47802`), not a model defect: the same jar falsely rejected the tautology `EventuallyTrue == <> TRUE`. Additionally, `CaRefWriterCleanupCore_empty_builds.cfg` violates the temporal property `StalePrecommitEventuallyGone` under TLC 2.19, although the same model's independent invariant `NoStale` passes on the four-state graph."}
{"cluster_id": "chunk_012-152", "topic": "`run_gclease", "canonical_claim": "`run_gclease.sh`, `run_gcshardincarnation.sh`, and `run_b140danglemerge.sh` accept any jar because they do not call `check_tlc_pin`; `run_gcrounddefer.sh` does call the pin check."}
{"cluster_id": "chunk_012-153", "topic": "`Mode", "canonical_claim": "`Mode::Native` tests ignore the unique storage root and write keys into the process working directory, making their namespace shared across all Native tests in the binary."}
{"cluster_id": "chunk_012-154", "topic": "Consolidated The pinned TLC runner had to recognize both `Temporal properties ", "canonical_claim": "The pinned TLC runner had to recognize both `Temporal properties were violated.` and the named-property form `Temporal property EventuallyFolded was violated.`; otherwise `sab_unbounded_defer` was misclassified as an error. Additionally, The pinned TLC runner classifier accepts both `Temporal properties were violated` and `Temporal property <Name> was violated` forms, and the latter singular form is emitted by the pinned `TLC2 Version 2026.07.18.145032` jar for a single `PROPERTY`."}
{"cluster_id": "chunk_012-155", "topic": "Stale-edge residue remained permanently nominated-free across 145 GC rounds", "canonical_claim": "Stale-edge residue remained permanently nominated-free across 145 GC rounds: the key-set fingerprint did not change and both `pending_gc` and `awaiting_gc` stayed zero."}
{"cluster_id": "chunk_012-156", "topic": "The epoch-1 cohort was confirmed six-for-six as orphan-sweep deletions of manife", "canonical_claim": "The epoch-1 cohort was confirmed six-for-six as orphan-sweep deletions of manifests carrying unmatched `+1` edges; the two epoch-4 attributions remain unreliable and must not be presented as eight-for-eight."}
{"cluster_id": "chunk_012-157", "topic": "The CAS suite gate initially missed `ContentAddressedSettings`, a six-test CAS s", "canonical_claim": "The CAS suite gate initially missed `ContentAddressedSettings`, a six-test CAS suite whose file `gtest_content_addressed_settings.cpp` matched neither `gtest_ca*.cpp` nor `gtest_cas*.cpp`; those six tests had never been gated."}
{"cluster_id": "chunk_012-158", "topic": "Renaming `CaWiring` to `CasWiring` would have merged it with the pre-existing ba", "canonical_claim": "Renaming `CaWiring` to `CasWiring` would have merged it with the pre-existing bare `CasWiring` suite and silently combined unrelated tests."}
{"cluster_id": "chunk_012-159", "topic": "The old README test filter contained dead components `CA*` and `RefSnapshotCodec", "canonical_claim": "The old README test filter contained dead components `CA*` and `RefSnapshotCodec*`, which matched no suite in either tested binary."}
{"cluster_id": "chunk_012-160", "topic": "On ClickHouse 26", "canonical_claim": "On ClickHouse 26.x, `SYSTEM JEMALLOC ENABLE PROFILE` can leave `prof.active` equal to `1` while producing dumps with zero stacks; a dump must contain `@ 0x` stack lines before the session is trusted."}
{"cluster_id": "chunk_012-161", "topic": "The soak harness watched the removed `CasRefApplyPoisoned` profile event even th", "canonical_claim": "The soak harness watched the removed `CasRefApplyPoisoned` profile event even though production had renamed it to `CasRefNeedsRecovery`, causing phase-3 preflight to abort until the watch list was updated."}
{"cluster_id": "chunk_012-162", "topic": "run_cas_gate_per_suite", "canonical_claim": "run_cas_gate_per_suite.sh cannot execute parameterized suites whose names contain / because it redirects to a slash-bearing suite-derived log path without creating the nested directory; it also has a fixed 60-second timeout below two passing suites' ASan runtimes."}
{"cluster_id": "chunk_012-163", "topic": "The first CAS suite enumeration missed a multiline INSTANTIATE_TEST_SUITE_P pref", "canonical_claim": "The first CAS suite enumeration missed a multiline INSTANTIATE_TEST_SUITE_P prefix, but the generator caught CasWinnerShape/CASGcCompletedRemovalFenceRace; the invariant must therefore be checked against the built binary rather than only line-based source enumeration."}
{"cluster_id": "chunk_012-164", "topic": "`utils/ca-soak/scenarios/tests/test_render_tuned_config", "canonical_claim": "`utils/ca-soak/scenarios/tests/test_render_tuned_config.py` was a red test because it still expected `<metadata_type>content_addressed</metadata_type>` after the setting value changed to `cas`; the assertion was corrected to `cas`."}
{"cluster_id": "chunk_012-165", "topic": "Consolidated `PutHookBackend", "canonical_claim": "`PutHookBackend::casPut` previously bypassed its immediate parent `HidingListBackend::casPut`, silently disabling `before_cas_put` and `ambiguous_cas_count` fault injection when composed. Additionally, `PutHookBackend : HidingListBackend : CountingBackend`; therefore `PutHookBackend::casPut` must route through `HidingListBackend::casPut` to compose both hook layers."}
{"cluster_id": "chunk_012-166", "topic": "`PostFoldUnreadableTerminalBackend", "canonical_claim": "`PostFoldUnreadableTerminalBackend::existsIgnoringFault` and the internal catalog probe in `CatalogAfterListBackend::list` bypassed `CountingBackend` counters, although only the former was behaviorally safe to route through its parent."}
{"cluster_id": "chunk_012-167", "topic": "Passing `*before", "canonical_claim": "Passing `*before.token` to `casPut` unnecessarily dereferenced an optional before wrapping it again; passing `before.token` directly removes the UB risk without changing the call contract."}
{"cluster_id": "chunk_012-168", "topic": "The tidy finisher found two broken `NOLINTNEXTLINE` placements and one missed `r", "canonical_claim": "The tidy finisher found two broken `NOLINTNEXTLINE` placements and one missed `readability-make-member-function-const` diagnostic; all three were corrected before the final verification gate."}
{"cluster_id": "chunk_012-169", "topic": "An fsck catalog-resolution ambiguity must be recorded as a finding rather than a", "canonical_claim": "An fsck catalog-resolution ambiguity must be recorded as a finding rather than aborting the entire audit, and stateless golden `05020` must reflect the new report columns."}
{"cluster_id": "chunk_012-170", "topic": "fsck's manifest-debris pass discovers namespaces via `store", "canonical_claim": "fsck's manifest-debris pass discovers namespaces via `store.listNamespaces` (`CasFsck.cpp:371`), which enumerates only `cas/refs/` and `roots/` (`CasStore.cpp:1640`), not `cas/manifests/`; a namespace whose only remnant is a staged `cas/manifests/...` body that `Build::abandon`'s best-effort cleanup missed (`CasBuild.cpp:958`) is not enumerated, so fsck under-reports it as unreachable even though the GC orphan sweep (which scans `cas/manifests/` directly) still reclaims it — a fsck diagnostics gap, not a leak."}
{"cluster_id": "chunk_012-171", "topic": "CI needed 6-hour-job-timeout sharding after a TSan lane finished 10,990/10,990 t", "canonical_claim": "CI needed 6-hour-job-timeout sharding after a TSan lane finished 10,990/10,990 tests but was killed 16 seconds before it could report the result."}
{"cluster_id": "chunk_012-172", "topic": "A CI fast-test build broke because `dbms` linked the dead `ch_contrib", "canonical_claim": "A CI fast-test build broke because `dbms` linked the dead `ch_contrib::crc32c` target left over from a phase-1a change, and the fast-test job does not initialize that submodule; the fix dropped the link so crc32c stayed google-cloud-cpp-only."}
{"cluster_id": "chunk_012-173", "topic": "CAS GC's `gc_fold_begin`/`gc_fold_end` audit rows write only an anomaly count (e", "canonical_claim": "CAS GC's `gc_fold_begin`/`gc_fold_end` audit rows write only an anomaly count (e.g. `{anomalies: '1'}`) as `detail`; `recordAnomaly` takes namespace, shard, manifest and reason but none of it reaches the audited row, so a firing cannot be identified or distinguished from other anomaly classes after the fact."}
{"cluster_id": "chunk_012-174", "topic": "B207 was a fsck phantom-dangle finding across a ref-walk/HEAD-confirm window", "canonical_claim": "B207 was a fsck phantom-dangle finding across a ref-walk/HEAD-confirm window."}
{"cluster_id": "chunk_012-175", "topic": "The `ca-fsck` summary previously omitted `corrupted_runs` from its rendered summ", "canonical_claim": "The `ca-fsck` summary previously omitted `corrupted_runs` from its rendered summary line and exit code even though it was counted and part of `clean()`, so a failing seal checksum was invisible twice over; the summary line was moved into `Cas::formatFsckSummary` (reachable from a unit test) with `corrupted_runs` added."}
{"cluster_id": "chunk_012-176", "topic": "Read-only CA mode skips capability probing, watermark and mount claims, and GC", "canonical_claim": "Read-only CA mode skips capability probing, watermark and mount claims, and GC; `fsck`, `ca-gc-dryrun`, `ca-gc-rebuild`, and `ca-inspect` require a read-only disk."}
{"cluster_id": "chunk_012-177", "topic": "`runFsck` recomputes reachability from authoritative refs rather than GC snapsho", "canonical_claim": "`runFsck` recomputes reachability from authoritative refs rather than GC snapshots and performs no writes."}
{"cluster_id": "chunk_012-178", "topic": "`runFsck` labels `Reachable`, `Dangling`, `Unreachable`, `PendingGc`, `AwaitingG", "canonical_claim": "`runFsck` labels `Reachable`, `Dangling`, `Unreachable`, `PendingGc`, `AwaitingGc`, `Unaccounted`, and `SnapshotOracleMismatch` objects, and `Unaccounted` is never used as a reachability input."}
{"cluster_id": "chunk_012-179", "topic": "The strongest whole-pool test oracle is `runFsck()", "canonical_claim": "The strongest whole-pool test oracle is `runFsck().dangling == 0` for no loss and `runFsck().unreachable == 0` after reclamation for no leak."}
{"cluster_id": "chunk_012-180", "topic": "`clickhouse-disks ca-fsck --disk <ca_disk_name>` opens the CAS disk read-only, w", "canonical_claim": "`clickhouse-disks ca-fsck --disk <ca_disk_name>` opens the CAS disk read-only, walks live refs and trees through the production read API, and enumerates the pool prefix with paginated `backend.list`."}
{"cluster_id": "chunk_012-181", "topic": "Under the ack-floor pipeline, `pending-gc` and `awaiting-gc` present-but-unrefer", "canonical_claim": "Under the ack-floor pipeline, `pending-gc` and `awaiting-gc` present-but-unreferenced objects are expected transient classes rather than leaks."}
{"cluster_id": "chunk_012-182", "topic": "`unchecked` means the audit could prove neither a healthy frontier nor a broken ", "canonical_claim": "`unchecked` means the audit could prove neither a healthy frontier nor a broken chain; it is a coverage result that does not make the report unclean or exit nonzero."}
{"cluster_id": "chunk_012-183", "topic": "A read-only `ca-fsck` records a per-namespace `CORRUPTED_DATA` failure and conti", "canonical_claim": "A read-only `ca-fsck` records a per-namespace `CORRUPTED_DATA` failure and continues scanning other namespaces instead of stopping at the first damaged namespace."}
{"cluster_id": "chunk_012-184", "topic": "Consolidated `ca-fsck` exits nonzero for `dangling`, `chain_broken`, `corrupte", "canonical_claim": "`ca-fsck` exits nonzero for `dangling`, `chain_broken`, `corrupted_runs`, or `lifeless_keys`, but never exits nonzero solely for `stale_edge`; a zero exit code therefore does not prove a clean pool without checking `stale_edge`. Additionally, `ca-fsck` was deliberately kept non-fatal for `stale_edge` findings pending a separate retention-leak decision."}
{"cluster_id": "chunk_012-185", "topic": "Consolidated `ca-gc-dryrun` requires read-only mode, calls `previewDeletes()` ", "canonical_claim": "`ca-gc-dryrun` requires read-only mode, calls `previewDeletes()` with fixed `Gc` ID `UInt128(1)`, and performs no writes. Additionally, `clickhouse-disks ca-gc-dryrun --disk <ca_disk_name>` is read-only and derives next-round delete candidates from `gc/snap` and `gc/state` without CAS writes or deletes. Additionally, `ca-gc-dryrun` is read-only and write-free, can over-report away from quiescence because it does not fold new owner events, and its output must never drive a real delete."}
{"cluster_id": "chunk_012-186", "topic": "No obsolete document may be deleted until a coverage matrix maps it to a canonic", "canonical_claim": "No obsolete document may be deleted until a coverage matrix maps it to a canonical section and a fresh sanity audit confirms that its load-bearing content is present."}
{"cluster_id": "chunk_012-187", "topic": "`claimMountAwaitingExpiry` accepts injectable wall-clock and sleep functions so ", "canonical_claim": "`claimMountAwaitingExpiry` accepts injectable wall-clock and sleep functions so unit tests can advance a fake clock without real sleeping; production uses a system-clock reader and `std::this_thread::sleep_for`."}
{"cluster_id": "chunk_012-188", "topic": "The corrected S02 workload uses deterministic incompressible `generateRandom('pa", "canonical_claim": "The corrected S02 workload uses deterministic incompressible `generateRandom('payload String', <seed>, per_row) LIMIT rows` data with `max_threads=1`, so duplicate inserts exercise the real large-blob `dedup_head_first_min_bytes` path."}
{"cluster_id": "chunk_012-189", "topic": "The CAS harness's transport probes are best-effort", "canonical_claim": "The CAS harness's transport probes are best-effort: failures return `None` or an empty sentinel rather than raising, and a scenario that requires a missing observation must be marked inconclusive rather than pass silently."}
{"cluster_id": "chunk_012-190", "topic": "`classify_pool_path` recognizes pool-relative and prefixed keys and returns one ", "canonical_claim": "`classify_pool_path` recognizes pool-relative and prefixed keys and returns one of `blobs`, `_manifests`, `refs`, `roots`, `gc`, `_files`, `_pool_meta`, or `other`; `RECLAIMABLE_UNREACHABLE_PREFIXES` remains `(`\"blobs\"`, `\"_manifests\"`)`."}
{"cluster_id": "chunk_012-191", "topic": "Consolidated `FsckReport` partial mode returns accumulated lower-bound counts ", "canonical_claim": "`FsckReport` partial mode returns accumulated lower-bound counts with `partial=true` and `partial_reason` when a deadline raises `TIMEOUT_EXCEEDED`; without partial mode the timeout exception is preserved. Additionally, A partial fsck scan may safely treat `dangling > 0` as a lower-bound failure, but it cannot use partial completion to prove absence because absence is the relevant waiter-side proof."}
{"cluster_id": "chunk_012-192", "topic": "`runFsck` namespace scoping uses `namespace_prefix`, checks only matching refere", "canonical_claim": "`runFsck` namespace scoping uses `namespace_prefix`, checks only matching references, performs dangling-only HEAD verification of named blobs, and intentionally skips pool-wide unreachable/pending/unaccounted classification."}
{"cluster_id": "chunk_012-193", "topic": "Consolidated `clickhouse-disks ca-inspect <key>` is a read-only command that d", "canonical_claim": "`clickhouse-disks ca-inspect <key>` is a read-only command that dispatches CA bucket keys by `Layout` and renders decoded objects as human-readable JSON. Additionally, `ca-inspect` recognizes `cas/refs`, `cas/manifests`, `gc/server-roots/<srid>/mount`, `gc/state`, fold seals, retired sets, and blob envelope headers; unknown keys fail closed with an error listing recognized layouts. Additionally, `ca-inspect` renders `u128`, token, and hash fields as lowercase hexadecimal and reports decode failures rather than emitting partial or guessed output. Additionally, `ca-inspect` requires a read-only-opened content-addressed object-storage disk and performs no CAS, delete, or mutation operation. Additionally, Under raw bodies, `ca-inspect` dispatches `.meta` keys to `BlobMeta` decoding and renders non-meta `blobs/` keys as raw blobs with size and a hash-match check rather than decoding an envelope. Additionally, Blob identities in inspect output and event logs render as `algo:hex`, such as `sha256:ab...`, rather than bare hexadecimal."}
{"cluster_id": "chunk_012-194", "topic": "The unit-testable `caInspectToJson` interface has signature `String caInspectToJ", "canonical_claim": "The unit-testable `caInspectToJson` interface has signature `String caInspectToJson(const Layout & layout, const String & key, std::string_view bytes)`."}
{"cluster_id": "chunk_012-195", "topic": "Consolidated ca-inspect must dispatch ", "canonical_claim": "ca-inspect must dispatch .meta keys before the blobs/ body branch, and ca-fsck reports meta_without_body as an invariant violation while body_without_meta is benign pre-birth debris. Additionally, `ca-fsck` reports `meta_without_body` for a meta with no body as an `INV-META-BODY` violation, reports `body_without_meta` for a body with no meta as benign pre-meta-birth debris, and `clean()` requires both `dangling == 0` and `meta_without_body == 0`."}
{"cluster_id": "chunk_012-196", "topic": "Consolidated `previewDeletes` emits a `PreviewEntry` for every `kCondemned` ro", "canonical_claim": "`previewDeletes` emits a `PreviewEntry` for every `kCondemned` row using the stored token, size, and condemn round; `delete_pending` maps to `reason = \"delete_pending\"`, otherwise to `reason = \"awaiting_graduation\"`, and these rows require no `HEAD`. Additionally, `previewDeletes` emits every carried `kCondemned` row using its stored token, token type, condemn round, and pending flag, classifying it as `delete_pending` or awaiting graduation without a HEAD probe."}
{"cluster_id": "chunk_012-197", "topic": "The existing zero-marker plus `HEAD` path remains the source of `PreviewEntry` r", "canonical_claim": "The existing zero-marker plus `HEAD` path remains the source of `PreviewEntry` records with `reason = \"unreachable\"`; the condemned-row output is a superset of the previous preview output."}
{"cluster_id": "chunk_012-198", "topic": "`fsck` collects condemned rows during the same streaming pass over seal-named so", "canonical_claim": "`fsck` collects condemned rows during the same streaming pass over seal-named source-edge runs that it uses for reachability, replacing the `retired_refs` loop."}
{"cluster_id": "chunk_012-199", "topic": "`ca-inspect` reports per-shard `condemned_summary` fields named `condemned_total", "canonical_claim": "`ca-inspect` reports per-shard `condemned_summary` fields named `condemned_total`, `pending_total`, and `oldest_nonpending_condemn_round` instead of dumping the `retired_refs` map."}
{"cluster_id": "chunk_012-200", "topic": "The targeted regression failpoint is `part_storage_fail_commit_transaction` in `", "canonical_claim": "The targeted regression failpoint is `part_storage_fail_commit_transaction` in `DataPartStorageOnDiskFull::commitTransaction`; the generic `disk_object_storage_fail_commit_metadata_transaction` is unsuitable because it also fires on autocommit temporary-directory operations."}
{"cluster_id": "chunk_012-201", "topic": "`SYSTEM CONTENT ADDRESSED FSCK <disk>` is dormant-only", "canonical_claim": "`SYSTEM CONTENT ADDRESSED FSCK <disk>` is dormant-only: it requires an unmounted, quiesced pool and runs against a temporary observe-only pool view."}
{"cluster_id": "chunk_012-202", "topic": "The offline disk-checking applet is renamed to `ca-fsck`, while `fsck` remains a", "canonical_claim": "The offline disk-checking applet is renamed to `ca-fsck`, while `fsck` remains as a deprecated alias."}
{"cluster_id": "chunk_012-203", "topic": "Consolidated `runFsck` has no coherent snapshot and can race any node's GC", "canonical_claim": "`runFsck` has no coherent snapshot and can race any node's GC; every hard finding must be revalidated against the authoritative current ref and the exact body via `HEAD` before reporting it. Additionally, CAS FSCK must re-resolve the authoritative ref and `HEAD` the exact body before reporting a missing committed manifest; `meta_without_body` is advisory and does not make `FsckReport::clean` false. Additionally, `runFsck` independently audits reachability by recomputing from refs, diffing against raw `LIST`, and confirming with `HEAD`. Additionally, fsck must use exact catalog and head state for logical integrity, retry a namespace if its head changes, and report missing catalog/head/node/checkpoint state as non-clean and unchecked rather than as an empty namespace."}
{"cluster_id": "chunk_012-204", "topic": "Consolidated Sanitizer/debug coverage for genuine `LOGICAL_ERROR` negative tes", "canonical_claim": "Sanitizer/debug coverage for genuine `LOGICAL_ERROR` negative tests should use `EXPECT_DEATH`, with one death assertion per invalid operation and the invalid teardown or worker-thread lifecycle contained inside the child process. Additionally, The PR #2073 sanitizer-abort fix keeps the production `LOGICAL_ERROR` guards unchanged for the GCS conditional dialect and dedup-log null-writer findings; only tests are changed for those findings. Additionally, Tests for `LOGICAL_ERROR` guards use `EXPECT_THROW` on release builds and `EXPECT_DEATH` under `DEBUG_OR_SANITIZER_BUILD`, with death suites named using a `DeathTest` suffix."}
{"cluster_id": "chunk_012-205", "topic": "The definitive CA gtest gate for stage 1 is `build_asan/src/unit_tests_dbms --gt", "canonical_claim": "The definitive CA gtest gate for stage 1 is `build_asan/src/unit_tests_dbms --gtest_filter='Cas*:CaLifecycle*:CaWiring*:ContentAddressed*:CountingBackendShape*:RefSnapshotCodec*:RefTableCacheEviction*:RefWriter*'`; the shorter `Cas*:CA*` filter is considered insufficient evidence."}
{"cluster_id": "chunk_012-206", "topic": "In `build_debug`, `DENY_ALLOCATIONS_IN_SCOPE` causes an allocation to throw rath", "canonical_claim": "In `build_debug`, `DENY_ALLOCATIONS_IN_SCOPE` causes an allocation to throw rather than abort because `DEBUG_OR_SANITIZER_BUILD` is not defined; allocation-negative tests must distinguish `EXPECT_ANY_THROW` from `EXPECT_DEATH` by build mode."}
{"cluster_id": "chunk_012-207", "topic": "A failed diagnostic command must not be converted to a confident zero with `|| e", "canonical_claim": "A failed diagnostic command must not be converted to a confident zero with `|| echo 0`; permission denied, a missing file, and no match are distinct outcomes, and container logs should be read with `docker exec` when host permissions deny access."}
{"cluster_id": "chunk_012-208", "topic": "The S42 allocation-fault scenario treats a run with `generic == 0` as `inconclus", "canonical_claim": "The S42 allocation-fault scenario treats a run with `generic == 0` as `inconclusive`, never as a green pass, because no client-visible allocation fault or `QueryMemoryLimitExceeded` delta occurred."}
{"cluster_id": "chunk_012-209", "topic": "Consolidated Behaviour-preserving extraction is verified with the full CAS gte", "canonical_claim": "Behaviour-preserving extraction is verified with the full CAS gtest gate and a soak run; a gtest-only gate is insufficient because lock discipline and ordering can fail only under production-like concurrency. Additionally, A per-task test battery should cover the same lane set that the stage gate certifies; running only a subset can leave a deliberate posture change colliding with unadapted assertions until the final gate."}
{"cluster_id": "chunk_012-210", "topic": "The completeness invariant for the produced series is that the diff between its ", "canonical_claim": "The completeness invariant for the produced series is that the diff between its tip and the refactored working branch, restricted to shipped paths, is empty."}
{"cluster_id": "chunk_012-211", "topic": "`UniversePolicy", "canonical_claim": "`UniversePolicy::AuthoritativeForTest` is passed explicitly only by tests with closed universes; the single production fold call uses `UniversePolicy::kDefault = StageA_Suppressed`, with no config, environment, or mutable-global override."}
{"cluster_id": "chunk_012-212", "topic": "The LIST-liar regression backend hides named keys only from LIST while serving G", "canonical_claim": "The LIST-liar regression backend hides named keys only from LIST while serving GET, HEAD, conditional PUT, CAS, and exact delete honestly, modeling the RustFS defect shape."}
{"cluster_id": "chunk_012-213", "topic": "The phase's 89 reds comprise 65 sabotage-class configurations, 20 reachability w", "canonical_claim": "The phase's 89 reds comprise 65 sabotage-class configurations, 20 reachability witnesses, and four findings; expected reds are not phase failures when their named invariant or temporal property is the expected outcome."}
{"cluster_id": "chunk_012-214", "topic": "Consolidated Asserted TLA runners must match the exact named invariant or temp", "canonical_claim": "Asserted TLA runners must match the exact named invariant or temporal property expected for each sabotage; an unknown outcome fails closed, a timeout is `incomplete`, and successful simulation is labeled `PROBE` rather than `PASS`. Additionally, A suite runner must assert each expected outcome by the name of the invariant or property it must break; a runner that checks no property is not evidence."}
{"cluster_id": "chunk_012-215", "topic": "TLC liveness expectations use a `temporal <Prop>` outcome that requires a tempor", "canonical_claim": "TLC liveness expectations use a `temporal <Prop>` outcome that requires a temporal violation, no invariant violation, and exactly the declared property; this covers `CaBuildRootPrecommit_lazyleak` and `CaDiskLifecycle_sab_nogcselfexit`."}
{"cluster_id": "chunk_012-216", "topic": "A green TLA configuration is evidence only after its corresponding property has ", "canonical_claim": "A green TLA configuration is evidence only after its corresponding property has been observed red under a sabotage; witness violations are intentional reachability evidence, not failures."}
{"cluster_id": "chunk_012-217", "topic": "For validator mismatches, the sender logs the components it hashed (`root_namesp", "canonical_claim": "For validator mismatches, the sender logs the components it hashed (`root_namespace`, `ref_name`, `ManifestRef`, and `disk_name`) beside both digests, while the receiver logs the sender-identity fields it retained from the offer; neither side uses those diagnostic fields for authorization."}
{"cluster_id": "chunk_012-218", "topic": "`CasGcScheduler", "canonical_claim": "`CasGcScheduler.cpp:306-312,334` emits a line for every completed or failed GC round, so the absence of an outcome line proves that the in-flight round did not finish."}
{"cluster_id": "chunk_012-219", "topic": "The official checker policy uses `tla2tools` 2026", "canonical_claim": "The official checker policy uses `tla2tools` 2026.07.18.145032, revision `30cc360`, SHA-256 `cc4803dce2a8ffaf0f5920a9dc39df4b5ee34ab4cb53fb58ac557277a7e516b3`, at `tmp/tla2tools.jar` for both safety and temporal runs."}
{"cluster_id": "chunk_012-220", "topic": "Consolidated The official jar completed the `<> TRUE` smoke test and accepted ", "canonical_claim": "The official jar completed the `<> TRUE` smoke test and accepted `CaRefWriterCleanupCore_empty_builds.cfg`, while the three named temporal sabotage rows still produced their expected violations. Additionally, Under the official checker, `CaBuildRootPrecommit_lazyleak`, `CaDiskLifecycle_sab_nogcselfexit`, and `CaGcRoundDeferCore_sab_unbounded_defer` each produced the named expected temporal violation rather than an unnamed checker failure."}
{"cluster_id": "chunk_012-221", "topic": "Consolidation artifacts live under `docs/superpowers/cas/consolidation-2026-08/`", "canonical_claim": "Consolidation artifacts live under `docs/superpowers/cas/consolidation-2026-08/` and are committed so every pipeline phase is restartable and auditable."}
{"cluster_id": "chunk_012-222", "topic": "The map gate requires every corpus file to be accounted for, every JSONL record ", "canonical_claim": "The map gate requires every corpus file to be accounted for, every JSONL record to be valid, every source path to exist, and every record ID to be unique."}
{"cluster_id": "chunk_012-223", "topic": "The CAS gtest suite prefix is `CAS*`, while `CascadeWriteBuffer` remains exclude", "canonical_claim": "The CAS gtest suite prefix is `CAS*`, while `CascadeWriteBuffer` remains excluded because its `Cas` substring is unrelated to CAS."}
{"cluster_id": "chunk_012-224", "topic": "The CAS test tag is `no-cas-storage`, and the runner flags are `--cas-storage` a", "canonical_claim": "The CAS test tag is `no-cas-storage`, and the runner flags are `--cas-storage` and `--cas-s3-storage`."}
{"cluster_id": "chunk_012-225", "topic": "The `clickhouse-disks` CAS commands are `cas-fsck`, `cas-gc-dryrun`, `cas-gc-reb", "canonical_claim": "The `clickhouse-disks` CAS commands are `cas-fsck`, `cas-gc-dryrun`, `cas-gc-rebuild`, `cas-inspect`, and `cas-drop-member`; the deprecated bare `fsck` alias is removed."}
{"cluster_id": "chunk_012-226", "topic": "A scenario card must only populate `result` and drive the cluster through framew", "canonical_claim": "A scenario card must only populate `result` and drive the cluster through framework helpers; it must not call `result.finalize()`, write reports, or modify `RUN_HISTORY` or `BACKLOG`."}
{"cluster_id": "chunk_012-227", "topic": "A registered scenario requires `name`, `title`, and `priority`", "canonical_claim": "A registered scenario requires `name`, `title`, and `priority`; optional flags include `abandons`, `expect_exception`, `compose_variant`, `needs_infra`, and `param_table`."}
{"cluster_id": "chunk_012-228", "topic": "`RunContext` exposes cluster nodes with `query`, `command`, `scalar`, `container", "canonical_claim": "`RunContext` exposes cluster nodes with `query`, `command`, `scalar`, `container`, and `ping`, and provides `params`, `seed`, `duration_s`, `scale`, logging, artifact paths, subdirectories, and JSON/text writers."}
{"cluster_id": "chunk_012-229", "topic": "Structural soak correctness uses `clickhouse-disks cas-fsck` with `dangling==0` ", "canonical_claim": "Structural soak correctness uses `clickhouse-disks cas-fsck` with `dangling==0` and the `cas-gc-dryrun ⊆ unreachable` oracle only at quiesced checkpoints."}
{"cluster_id": "chunk_012-230", "topic": "The `assert_dryrun_subset` oracle must accept `dryrun ⊆ (unreachable ∪ pending-g", "canonical_claim": "The `assert_dryrun_subset` oracle must accept `dryrun ⊆ (unreachable ∪ pending-gc ∪ awaiting-gc)` but must fail if a candidate is `reachable`, `dangling`, or absent."}
{"cluster_id": "chunk_012-231", "topic": "Harness observation helpers must distinguish a legitimately empty table or not-y", "canonical_claim": "Harness observation helpers must distinguish a legitimately empty table or not-yet-flushed log from a failed observation; schema/query errors must surface instead of becoming empty results, and row-set assertions need explicit non-vacuity guards."}
{"cluster_id": "chunk_012-232", "topic": "Each adversarial scenario should run independently against a fresh pool prefix `", "canonical_claim": "Each adversarial scenario should run independently against a fresh pool prefix `<scenario>/<seed>/<run_id>` and should end at a quiesced checkpoint with synchronized replicas, empty replication and mutation queues, no active merges, and forced GC rounds to a declared fixpoint."}
{"cluster_id": "chunk_012-233", "topic": "The facade debug surface `explainPartFolderAccess(PartRefKey)` reports presence,", "canonical_claim": "The facade debug surface `explainPartFolderAccess(PartRefKey)` reports presence, retention, the last decision, `manifest_ref`, estimated bytes, and generation."}
{"cluster_id": "chunk_012-234", "topic": "The CAS suite generator enforces that every non-excluded CAS suite name and ever", "canonical_claim": "The CAS suite generator enforces that every non-excluded CAS suite name and every parameterized `<Inst>/<Suite>` spelling starts with `Cas`, and that every CAS test file is covered by the source glob."}
{"cluster_id": "chunk_012-235", "topic": "The CAS suite gate determines whether a source file is CAS by checking for an in", "canonical_claim": "The CAS suite gate determines whether a source file is CAS by checking for an include under `MetadataStorages/ContentAddressed/` or `cas_test_helpers.h`, not by looking for a `Cas`-prefixed suite."}
{"cluster_id": "chunk_012-236", "topic": "The generator's source-vs-binary drift check and explicit `KNOWN_COMPILE_GUARDED", "canonical_claim": "The generator's source-vs-binary drift check and explicit `KNOWN_COMPILE_GUARDED` list remain part of the CAS gate, and any gate failure exits with status 1."}
{"cluster_id": "chunk_012-237", "topic": "Part-folder cache request-count tests measure CAS manifest loads directly and do", "canonical_claim": "Part-folder cache request-count tests measure CAS manifest loads directly and do not rely on `s3_cache`."}
{"cluster_id": "chunk_012-238", "topic": "The CAS read-only scrape substitutes metadata when it finds the <metadata_type>c", "canonical_claim": "The CAS read-only scrape substitutes metadata when it finds the <metadata_type>cas</metadata_type> marker, rather than matching disk names"}
{"cluster_id": "chunk_012-239", "topic": "The CAS read-only scrape keeps a fail-loud WARNING check unchanged while shorten", "canonical_claim": "The CAS read-only scrape keeps a fail-loud WARNING check unchanged while shortening its explanatory comment"}
{"cluster_id": "chunk_012-240", "topic": "Physical LIST results in fsck are only observed garbage candidates", "canonical_claim": "Physical LIST results in fsck are only observed garbage candidates; fsck must not claim that no unaccounted physical objects exist."}
{"cluster_id": "chunk_012-241", "topic": "The retirement-sweep regression test must run 32 rounds because the removed dete", "canonical_claim": "The retirement-sweep regression test must run 32 rounds because the removed detector enumerated every 16th folding round, and five rounds would not exercise that cadence twice."}
{"cluster_id": "chunk_012-242", "topic": "Jemalloc profiler absolute MiB are upper bounds because the profiler allocates i", "canonical_claim": "Jemalloc profiler absolute MiB are upper bounds because the profiler allocates its own sampling data, but site rankings remain useful; low-period trace profiling can instead dominate idle diffs through `trace_log` flushing."}
{"cluster_id": "chunk_012-243", "topic": "The fence-race test must assert the deposed leader fails with `DB", "canonical_claim": "The fence-race test must assert the deposed leader fails with `DB::ErrorCodes::NETWORK_ERROR` and a message naming `pre-fold drain lost authority`, rather than accepting any exception."}
{"cluster_id": "chunk_012-244", "topic": "Every `expectThrowsCode` or `EXPECT_THROW` involving `LOGICAL_ERROR` in `src/Dis", "canonical_claim": "Every `expectThrowsCode` or `EXPECT_THROW` involving `LOGICAL_ERROR` in `src/Disks/tests/` must be checked against the exact line ranges of `#ifndef DEBUG_OR_SANITIZER_BUILD` guards, not merely file-level guard presence."}
{"cluster_id": "chunk_012-245", "topic": "CasFsck", "canonical_claim": "CasFsck.AmbiguousLifeUnderAPhysicalKeyIsRecordedNotAborted drives a canonical _files object under life ID 777 into an ambiguity created by catalog rows bad/a and bad/b, ensuring the ambiguity path is exercised by a real physical candidate."}
{"cluster_id": "chunk_012-246", "topic": "Consolidated The 18-suite release-versus-ASan difference consists of ASan-only", "canonical_claim": "The 18-suite release-versus-ASan difference consists of ASan-only `Cas*DeathTest` suites listed in `KNOWN_COMPILE_GUARDED`; there are no release-only suites. Additionally, The compile-guarded death-test suites are absent from the release build, 18 are present in ASan, and all 19 are present in a debug build; CASRefInstallSafetyDeathTest is intentionally absent from ASan because MEMORY_TRACKER_DEBUG_CHECKS requires !NDEBUG while sanitizer builds define NDEBUG."}
{"cluster_id": "chunk_012-247", "topic": "The T7 model runner treats a temporal violation as valid only when the log names", "canonical_claim": "The T7 model runner treats a temporal violation as valid only when the log names the expected property and the config contains a singular `PROPERTY` line; a different property or plural `PROPERTIES` config falls through to failure."}
{"cluster_id": "chunk_012-248", "topic": "A global gtest listener ensures the CAS blob-upload pool before every test becau", "canonical_claim": "A global gtest listener ensures the CAS blob-upload pool before every test because `uploadPendingBlobs` uses fail-loud `Cas::blobUploadPool()` and the lifecycle suite can deliberately tear the pool down."}
{"cluster_id": "chunk_012-249", "topic": "`CasFsck` may use an independently listed stream as an oracle or diagnostic inpu", "canonical_claim": "`CasFsck` may use an independently listed stream as an oracle or diagnostic input, but listing omissions may affect storage-quality findings and may not affect the reconstructed committed table."}
{"cluster_id": "chunk_012-250", "topic": "`fsck` walk 1 silently skips an unparseable ref key while walk 2 throws `CORRUPT", "canonical_claim": "`fsck` walk 1 silently skips an unparseable ref key while walk 2 throws `CORRUPTED_DATA`; the asymmetry causes an abort rather than a hole and was classified as a consistency tidy-up rather than a correctness defect."}
{"cluster_id": "chunk_012-251", "topic": "The fsck janitor-pending implementation must use the shared canonical-key helper", "canonical_claim": "The fsck janitor-pending implementation must use the shared canonical-key helper, classify malformed shapes inline, and take the fresh cut after `LIST` using observe-then-cut ordering."}
{"cluster_id": "chunk_012-252", "topic": "`PoolConfig", "canonical_claim": "`PoolConfig::boot_ms_fn` feeds both `CasMountRuntime::bootMsNow()` and `CasRequestController::now_ms` through `CasPool.cpp`, making it the deterministic clock seam for CPU-bound CAS tests."}
{"cluster_id": "chunk_012-253", "topic": "A retry policy test for precondition failures should create `AWSError(CoreErrors", "canonical_claim": "A retry policy test for precondition failures should create `AWSError(CoreErrors::UNKNOWN, retryable=true)` with response code `PRECONDITION_FAILED` and verify that `ShouldRetry` returns false."}
{"cluster_id": "chunk_012-254", "topic": "Every positive CAS scenario in the S01-S35 suite must satisfy", "canonical_claim": "Every positive CAS scenario in the S01-S35 suite must satisfy: SQL correctness (all replicas match the scenario oracle's aggregates), storage correctness (`ca-fsck --detail` reports `dangling = 0`), GC safety (`ca-gc-dryrun` delete candidates are a subset of the `ca-fsck` unreachable set at quiescence), a clean event audit (`system.content_addressed_log` has no error-class event types), no `Failed` GC-round finish rows, no unbounded leftovers after forced GC, and no excessive resource growth beyond the scenario budget."}
{"cluster_id": "chunk_012-255", "topic": "The soak harness's chaos model applies seeded faults defined by `{t_offset, targ", "canonical_claim": "The soak harness's chaos model applies seeded faults defined by `{t_offset, target in {ch1,ch2,both,rustfs}, action in {kill-9, restart, pause/unpause}, duration}`; faults are bounded so a quiescent checkpoint is always reachable between them, and a server that does not recover within the bound is treated as a correctness failure, not a flake."}
{"cluster_id": "chunk_012-256", "topic": "`SYSTEM ", "canonical_claim": "`SYSTEM ... GC` reuses the scheduler's existing `Gc` instance (a per-call one-shot would acquire the lease once then back off forever, since the steal protocol needs a stable gc_id); only the log record's Trigger differs (Manual vs scheduled). `SYSTEM ... GC REBUILD [FORCE]` instead constructs a one-shot `Gc` and calls `rebuildBaseline`, which recomputes the in-degree snapshot from raw owner state, mints numbering above every surviving ack, publishes a fresh seal, and CASes gc/state — it is live-conservative, fail-closed on refusals, and never deletes content."}
{"cluster_id": "chunk_012-257", "topic": "The \"no-leftovers pool drain\" oracle requires that after DROP, `blobs/` and `par", "canonical_claim": "The \"no-leftovers pool drain\" oracle requires that after DROP, `blobs/` and `parts/` drain back to baseline, inspected directly on the object store (stateless `04290`, integration `test_content_addressed_gc_s3`, `test_content_addressed_shared_pool`)."}
{"cluster_id": "chunk_012-258", "topic": "`settle_fsck` stabilizes on `(reachable, dangling)` and intentionally excludes `", "canonical_claim": "`settle_fsck` stabilizes on `(reachable, dangling)` and intentionally excludes `unreachable`, because background GC legitimately churns unreachable objects; convergence of unreachable is owned by `forced_gc_to_fixpoint`."}
{"cluster_id": "chunk_012-259", "topic": "The B199 test fix adds `RefWriterTestBackend", "canonical_claim": "The B199 test fix adds `RefWriterTestBackend::corrupt_foreign_bytes`, allowing tests to supply a valid foreign object or deliberate garbage instead of always appending garbage to valid bytes."}
{"cluster_id": "chunk_012-260", "topic": "A Clang AST CI check can guard `StorageProxy` interface growth by comparing all ", "canonical_claim": "A Clang AST CI check can guard `StorageProxy` interface growth by comparing all virtual `IStorage` declarations with exact `StorageProxy` overrides and requiring rationale for allowlisted omissions."}
{"cluster_id": "chunk_012-261", "topic": "The SQL commands are renamed to `SYSTEM CAS GC RUN`, `SYSTEM CAS GC REBUILD`, `S", "canonical_claim": "The SQL commands are renamed to `SYSTEM CAS GC RUN`, `SYSTEM CAS GC REBUILD`, `SYSTEM CAS GC START`, `SYSTEM CAS GC STOP`, `SYSTEM CAS FSCK`, `SYSTEM CAS DROP POOL MEMBER`, and `SYSTEM CAS FORGET`, with access types renamed from `CONTENT_ADDRESSED_*` to `CAS_*`."}
{"cluster_id": "chunk_012-262", "topic": "The `MaxAdmissions` bound limits how many operations or recoveries a behavior ad", "canonical_claim": "The `MaxAdmissions` bound limits how many operations or recoveries a behavior admits for TLC state-space feasibility; it is a search bound, not a safety claim."}
{"cluster_id": "chunk_012-263", "topic": "For a wide or golden-literal sweep, the search tool is the coverage gate and `gr", "canonical_claim": "For a wide or golden-literal sweep, the search tool is the coverage gate and `grep` is only a hypothesis generator."}
{"cluster_id": "chunk_012-264", "topic": "A new behavior is evidenced only when its new test is shown failing before the c", "canonical_claim": "A new behavior is evidenced only when its new test is shown failing before the change and the test would fail if the behavior regressed."}
{"cluster_id": "chunk_012-265", "topic": "A sweep described as a product of dimensions must verify that every predicted ce", "canonical_claim": "A sweep described as a product of dimensions must verify that every predicted cell is reachable and that the classification parts form a partition."}
{"cluster_id": "chunk_012-266", "topic": "The `ca-review` agent verifies reports against code rather than trusting the rep", "canonical_claim": "The `ca-review` agent verifies reports against code rather than trusting the reports."}
{"cluster_id": "chunk_012-267", "topic": "The `ca-surgeon` agent treats exhaustiveness as the deliverable and enumerates a", "canonical_claim": "The `ca-surgeon` agent treats exhaustiveness as the deliverable and enumerates affected sites before and after editing."}
{"cluster_id": "chunk_012-268", "topic": "A debug guard should track whether the current thread holds DataPartsLock or Dat", "canonical_claim": "A debug guard should track whether the current thread holds DataPartsLock or DataPartsSharedLock and assert at synchronous CA or S3 network primitives that network I/O is not occurring under a hot parts lock."}
{"cluster_id": "chunk_012-269", "topic": "CAS gtest suite generation must cross-check source `TEST(", "canonical_claim": "CAS gtest suite generation must cross-check source `TEST(...)` discovery with `--gtest_list_tests`, resolve `TEST_P` instantiations, and fail on an unclaimed suite so a silently omitted list cannot appear green."}
{"cluster_id": "chunk_012-270", "topic": "The soak tooling's stale `CasRefApplyPoisoned` name was mapped to `CASRefNeedsRe", "canonical_claim": "The soak tooling's stale `CasRefApplyPoisoned` name was mapped to `CASRefNeedsRecovery`, and the stale executable needle `is POISONED at` was corrected to `NEEDS RECOVERY at` from the `CasRefLedger::requireRecovery` log text."}
{"cluster_id": "chunk_012-271", "topic": "The four `clang-analyzer-deadcode", "canonical_claim": "The four `clang-analyzer-deadcode.DeadStores` sites involving `plans_before`, `lost_before`, `remounts_before`, and `puts_before` are intentional values read after thread or join boundaries, not dead stores."}
{"cluster_id": "chunk_012-272", "topic": "For the three Class B tests whose `LOGICAL_ERROR` aborts fire incidentally at te", "canonical_claim": "For the three Class B tests whose `LOGICAL_ERROR` aborts fire incidentally at teardown rather than being the stated test subject (`CasGcRound.OrphanManifestCursorSweepDeletesAndPersistsCursor`, `CasMountStartup.StaleSelfMountReclaimedAfterWait`, `CasPoolRemount.ForeignOwnerIsNeverTakenOver`), the safest remediation is to keep the primary behavior test's cleanup valid and add or isolate an `EXPECT_DEATH` assertion for the release guard; if the existing test is retained as the release-guard test, the entire invalid setup plus explicit destruction must live inside the death statement."}
{"cluster_id": "chunk_012-273", "topic": "A `LOGICAL_ERROR`-under-sanitizer abort class gets a mandatory review trigger on", "canonical_claim": "A `LOGICAL_ERROR`-under-sanitizer abort class gets a mandatory review trigger on its fifth recurrence in one week across the CAS codebase, requiring a file-wide grep sweep, because such an abort hides every subsequent test in the same process from running."}
{"cluster_id": "chunk_012-274", "topic": "A Debug coverage build was invalid for measuring logical-error paths because `DE", "canonical_claim": "A Debug coverage build was invalid for measuring logical-error paths because `DEBUG_OR_SANITIZER_BUILD` made `LOGICAL_ERROR` abort the process; the audit rebuilt as RelWithDebInfo so those paths threw catchably."}
{"cluster_id": "chunk_012-275", "topic": "The unattended campaign classified an `unaccounted`/dangling-precommit orphan di", "canonical_claim": "The unattended campaign classified an `unaccounted`/dangling-precommit orphan discrepancy during chaos recovery as a harness or S30-family follow-up with no data loss observed."}
{"cluster_id": "chunk_012-276", "topic": "The v2 design status records Phases A-C, the TLA+ gate, and addenda as implement", "canonical_claim": "The v2 design status records Phases A-C, the TLA+ gate, and addenda as implemented on `cas-gc-rebuild`, with the `Cas*:CA*` gtest gate green; full live CAS-s3 lane validation remains a CI-push task."}
{"cluster_id": "chunk_012-277", "topic": "The source-layout mechanical phase produced intermediate commits that were not c", "canonical_claim": "The source-layout mechanical phase produced intermediate commits that were not clean-buildable because three external include fixes remained uncommitted; incremental-build success did not prove the committed tree built."}
{"cluster_id": "chunk_012-278", "topic": "The soak harness missed the lock-stall defect because it measured memory and cor", "canonical_claim": "The soak harness missed the lock-stall defect because it measured memory and correctness rather than query latency, and its all-INSERT/OPTIMIZE workload did not queue independent readers on the parts lock."}
{"cluster_id": "chunk_012-279", "topic": "When the model retained queued deletes during `GcSpareHeal`, TLC found 37 states", "canonical_claim": "When the model retained queued deletes during `GcSpareHeal`, TLC found 37 states violating `INV_ABSENCE_NO_QUEUED_DELETE`."}
{"cluster_id": "chunk_012-280", "topic": "The corrected B199 validation passed all nine `RefWriterRecoverySeal", "canonical_claim": "The corrected B199 validation passed all nine `RefWriterRecoverySeal.*` tests and the full selected Ca* battery with 907 tests run, 907 passed, and zero failures; two tests were pre-existing disabled tests."}
{"cluster_id": "chunk_012-281", "topic": "For `all_1117_1117_0`, `system", "canonical_claim": "For `all_1117_1117_0`, `system.part_log` recorded only `RemovePart`, and the fault-window blob log recorded 112 failed uploads, confirming that the phantom's CAS data never durably landed."}
{"cluster_id": "chunk_012-282", "topic": "The corrected logical-error triage population contained 21 tests", "canonical_claim": "The corrected logical-error triage population contained 21 tests: 1 generic fault injected as `LOGICAL_ERROR`, 19 genuine invariant violations, 0 confirmed unrelated sanitizer failures, and 1 unresolved termination; all 19 invariant paths use explicit production `throw ... ErrorCodes::LOGICAL_ERROR` and none uses `chassert`."}
{"cluster_id": "chunk_012-283", "topic": "The final CAS gtest suite passed as a single invocation without exclusions under", "canonical_claim": "The final CAS gtest suite passed as a single invocation without exclusions under ASan and TSan, with 1034/1034 tests reported for both sanitizer configurations."}
{"cluster_id": "chunk_012-284", "topic": "The captured disagreement involved namespace `ca_soak_ch1/store/3ba/3ba2c30d-d1b", "canonical_claim": "The captured disagreement involved namespace `ca_soak_ch1/store/3ba/3ba2c30d-d1b2-4b17-a536-d311f1e239d4@cas@`, missing ref transactions `epoch 1, seq 0x1430c` and `epoch 1, seq 0x1430d`, and pre-fold maximum `epoch 1, seq 0x1430e`."}
{"cluster_id": "chunk_012-285", "topic": "`src/Disks/tests/cas_test_helpers", "canonical_claim": "`src/Disks/tests/cas_test_helpers.h` is 1250 lines, included by 73 of approximately 101 test files, and pulls in five layers: `Primitives`, `Formats`, `Backend`, `Pool`, and `Gc`."}
{"cluster_id": "chunk_012-286", "topic": "The apply-marker audit covers sixteen writes in total", "canonical_claim": "The apply-marker audit covers sixteen writes in total: fifteen `armApplyPending`/`clearApplyPending`/`poisonApplyState` call sites plus the direct store at `CasRefLedger.cpp:1174`; the four production calls currently lacking the required lock are `:2808`, `:2827`, `:3072`, and `:3112`."}
{"cluster_id": "chunk_012-287", "topic": "`CANNOT_PARSE_INPUT_ASSERTION_FAILED` had a pre-restart count of 28,170, but its", "canonical_claim": "`CANNOT_PARSE_INPUT_ASSERTION_FAILED` had a pre-restart count of 28,170, but its provenance was not available from the live pass because neither node's warning-or-higher logs contained the error text."}
{"cluster_id": "chunk_012-288", "topic": "The user table loaded one active part in 15 ms with six outdated parts", "canonical_claim": "The user table loaded one active part in 15 ms with six outdated parts; the 178.9 seconds from process start to table usability consisted instead of a 40.0-second CAS mount-lease token-stability wait and 138.1 seconds reloading system-log tables' outdated parts."}
{"cluster_id": "chunk_012-289", "topic": "T15 validation explained `CANNOT_PARSE_INPUT_ASSERTION_FAILED` as one caught VAL", "canonical_claim": "T15 validation explained `CANNOT_PARSE_INPUT_ASSERTION_FAILED` as one caught VALUES-parser control-flow exception per INSERT: ch1 had 16,131 errors and 16,131 `InsertQuery` operations, while ch2 had 16,192 of each."}
{"cluster_id": "chunk_012-290", "topic": "The empty-set survey found no pre-existing config assigning an empty entity coll", "canonical_claim": "The empty-set survey found no pre-existing config assigning an empty entity collection; the only empty assignments at survey start were auxiliary `TreeHashes = {}` and `UniqueToBuildTree = {}` sets."}
{"cluster_id": "chunk_012-291", "topic": "The `CasGcStopStart` suite passed standalone in 69 seconds but had a 60-second p", "canonical_claim": "The `CasGcStopStart` suite passed standalone in 69 seconds but had a 60-second per-suite gate timeout, making the gate failure deterministic until the timeout, suite split, or runtime changes."}
{"cluster_id": "chunk_012-292", "topic": "The S42 reproduction showed `stale_edge` values of 12 before restart and 67 afte", "canonical_claim": "The S42 reproduction showed `stale_edge` values of 12 before restart and 67 after restart, while the run had zero environmental failures and 12,998 acknowledged blocks."}
{"cluster_id": "chunk_012-293", "topic": "The C2 removal passed the Release CAS gtest run with 2004 tests and zero failure", "canonical_claim": "The C2 removal passed the Release CAS gtest run with 2004 tests and zero failures, Release per-suite gate with `pass=278 fail=0 abort=0`, ASan CAS gtest run with 2009 tests and zero failures, and ASan per-suite gate with `pass=296 fail=0 abort=0`."}
{"cluster_id": "chunk_012-294", "topic": "Consolidated After normalization, the generator list and the `Cas*` filter wer", "canonical_claim": "After normalization, the generator list and the `Cas*` filter were identical in both release and ASan binaries: release had 279 suites and 1997 listed tests, while ASan had 297 suites and 2002 listed tests. Additionally, The release `Cas*` run executed 1995 passing tests and had two disabled tests, reconciling to the 1997 listed tests. Additionally, The suite normalization added exactly six gated tests in each build while whole-binary suite and test totals stayed unchanged."}
{"cluster_id": "chunk_012-295", "topic": "The GC wedge forensics snapshot for phase-A exit stand seed 991 recorded 63 clam", "canonical_claim": "The GC wedge forensics snapshot for phase-A exit stand seed 991 recorded 63 clamped manifests for namespace `ca_soak_ch1/store/b37/b3781ce3-af83-4732-8c64-e8d342672225@cas@`."}
{"cluster_id": "chunk_012-296", "topic": "In that wedge snapshot, the first-seen clamp interval was `2026-07-09 22", "canonical_claim": "In that wedge snapshot, the first-seen clamp interval was `2026-07-09 22:01:27` through `2026-07-10 02:37:21`, with the recorded value `56826`."}
{"cluster_id": "chunk_012-297", "topic": "The `CaGcRootLocalPartManifestCore` `live` configuration reached `74,147,107` ge", "canonical_claim": "The `CaGcRootLocalPartManifestCore` `live` configuration reached `74,147,107` generated and `17,845,340` distinct states, while `stage5_lazytrim` reached `1,333,723,653` generated and `233,198,128` distinct states before timing out with `24,395,446` queued."}
{"cluster_id": "chunk_012-298", "topic": "The completed CAS-scoped tidy diagnostic set contained 119 unique sites across 1", "canonical_claim": "The completed CAS-scoped tidy diagnostic set contained 119 unique sites across 12 production files and 30 test files."}
{"cluster_id": "chunk_012-299", "topic": "The direct listing hammer did not reproduce holes in about 19 million listed key", "canonical_claim": "The direct listing hammer did not reproduce holes in about 19 million listed keys, but it did not reproduce the real CAS conditions: it used boto3 against a healthy store, lacked the ClickHouse S3 client's retries and chaos, and used different concurrency and key shape."}
{"cluster_id": "chunk_012-300", "topic": "The archaeology report found no real-thread concurrency coverage beyond one sing", "canonical_claim": "The archaeology report found no real-thread concurrency coverage beyond one single-flight test; other concurrency evidence is deterministic simulation, TLA+ models, and the external soak harness."}
{"cluster_id": "chunk_012-301", "topic": "`ca-fsck --detail` adds tab-separated per-object rows, and `--timeout <seconds>`", "canonical_claim": "`ca-fsck --detail` adds tab-separated per-object rows, and `--timeout <seconds>` defaults to 600 seconds with 0 meaning unbounded."}
{"cluster_id": "chunk_012-302", "topic": "`system", "canonical_claim": "`system.errors` counters can be high while matching `system.text_log` rows are sparse because `LogSeriesLimiter` suppresses repeated log lines; `last_error_message`, `last_error_trace`, and `system.query_log` `exception_code != 0` must be checked."}
{"cluster_id": "chunk_012-303", "topic": "The mount-recovery plan restores real hard `docker kill -s KILL` for ClickHouse ", "canonical_claim": "The mount-recovery plan restores real hard `docker kill -s KILL` for ClickHouse replicas while retaining RustFS's B145 graceful-only fault scope."}
{"cluster_id": "chunk_012-304", "topic": "The promote regression tests use inline manifest entries with distinct inline by", "canonical_claim": "The promote regression tests use inline manifest entries with distinct inline bytes so they exercise manifest conflicts without requiring real blob objects."}
{"cluster_id": "chunk_012-305", "topic": "The post-mortem method for a torn-down CA pool is to run RustFS over the survivi", "canonical_claim": "The post-mortem method for a torn-down CA pool is to run RustFS over the surviving data directory and open it with clickhouse-local in readonly mode so owner-claim, mount-lease, and GC mutations are skipped."}
{"cluster_id": "chunk_012-306", "topic": "Concurrency tests for A1, A3, and A7 must avoid `sleep` and use deterministic fa", "canonical_claim": "Concurrency tests for A1, A3, and A7 must avoid `sleep` and use deterministic fault injection, instrumented backends, or an atomic-gated second thread."}
{"cluster_id": "chunk_012-307", "topic": "The recovery seal body records `sealed_from`, the greatest transaction ID listed", "canonical_claim": "The recovery seal body records `sealed_from`, the greatest transaction ID listed during recovery, so the sweep can identify logs materialized after the recovery LIST."}
{"cluster_id": "chunk_012-308", "topic": "Chaos restarts reset `system", "canonical_claim": "Chaos restarts reset `system.events`, so CAS run totals must be computed from summed per-second deltas in `system.metric_log`."}
{"cluster_id": "chunk_012-309", "topic": "`part_log` aggregates must be filtered by table because system-log-table parts o", "canonical_claim": "`part_log` aggregates must be filtered by table because system-log-table parts otherwise contaminate workload statistics."}
{"cluster_id": "chunk_012-310", "topic": "The failpoint `disk_object_storage_fail_commit_metadata_transaction` throws insi", "canonical_claim": "The failpoint `disk_object_storage_fail_commit_metadata_transaction` throws inside `DiskObjectStorageTransaction::commit()` before `metadata_transaction->commit()`, after the Replicated `Keeper` multi has returned `ZOK`; this tests recovery from a part present in the Keeper part set but absent on disk."}
{"cluster_id": "chunk_012-311", "topic": "After the CAS namespace and RefWriter file reshapes, a build directory with argl", "canonical_claim": "After the CAS namespace and RefWriter file reshapes, a build directory with argless `localize_rust_c_*` rules must be fully reconfigured before rebuilding the current branch head."}
{"cluster_id": "chunk_012-312", "topic": "The named CAS background workers are `CasGcSched`, `CasGcHeartbeat`, `CasRemount", "canonical_claim": "The named CAS background workers are `CasGcSched`, `CasGcHeartbeat`, `CasRemount`, and `CasLeaseKeeper`."}
{"cluster_id": "chunk_012-313", "topic": "S42 allocation-fault validation must distinguish a generic `MEMORY_LIMIT_EXCEEDE", "canonical_claim": "S42 allocation-fault validation must distinguish a generic `MEMORY_LIMIT_EXCEEDED` from exercising the post-PUT apply window by requiring a targeted failpoint or poison-transition counter; otherwise the result is inconclusive."}
{"cluster_id": "chunk_012-314", "topic": "The final mount-lease validation requires a full `Cas*", "canonical_claim": "The final mount-lease validation requires a full `Cas*:CA*` gate and must not claim the CAS-s3 stateless lane is green without actually running that lane or explicitly deferring it to CI."}
{"cluster_id": "chunk_012-315", "topic": "The operative CAS gtest gate is `build/src/unit_tests_dbms --gtest_filter='Ca*", "canonical_claim": "The operative CAS gtest gate is `build/src/unit_tests_dbms --gtest_filter='Ca*:CA*:ContentAddressed*:CountingBackendShape*:RefSnapshotCodec*:RefTableCacheEviction*:RefWriter*:*CasBackendContract*'`; the shorter `Cas*:CA*` filter omits parameterized `*/CasBackendContract` suites."}
{"cluster_id": "chunk_012-316", "topic": "A flat blob count cannot prove that relink ran because a byte fetch onto a conte", "canonical_claim": "A flat blob count cannot prove that relink ran because a byte fetch onto a content-addressed disk writes identical content and deduplicates; positive proof must use the receiver log line `Relink of part <p> … finished (no bytes transferred).`."}
{"cluster_id": "chunk_012-317", "topic": "Thread-allocation fault injection through `cannot_allocate_thread_fault_injectio", "canonical_claim": "Thread-allocation fault injection through `cannot_allocate_thread_fault_injection_probability` targets background GC scheduling, mount-lease renewal, self-remount, and snapshot publication; lease fencing during the fault is fail-closed behavior, but recovery after disarm must be verified."}
{"cluster_id": "chunk_012-318", "topic": "The raw `dropRefTransition` and `publishCommittedTransition` test helpers must n", "canonical_claim": "The raw `dropRefTransition` and `publishCommittedTransition` test helpers must not be mixed with the real writer on one namespace because the helpers allocate sequences by listing while the writer uses an in-memory sequence, causing collisions when the namespace is written again."}
{"cluster_id": "chunk_012-319", "topic": "A `HoleyListBackend` test must arm `omitFromNthListCall` after all seeding write", "canonical_claim": "A `HoleyListBackend` test must arm `omitFromNthListCall` after all seeding writes because writer-side namespace listing for sequence allocation consumes qualifying `LIST` calls and otherwise shifts the selected call."}
{"cluster_id": "chunk_013-001", "topic": "A ClickHouse gate's green result is evidence only about the binary that ", "canonical_claim": "A ClickHouse gate's green result is evidence only about the binary that gate executed; unit-test binaries can be fresh while `build/programs/clickhouse` remains stale if the build did not target it, so source-newer-than-binary checks are required before timing-sensitive validation."}
{"cluster_id": "chunk_013-002", "topic": "A counter that has never incremented may be absent from `system", "canonical_claim": "A counter that has never incremented may be absent from `system.events`; zero-counter assertions must query with `system_events_show_zero_values = 1` so an absent name can be distinguished from a true zero."}
{"cluster_id": "chunk_013-003", "topic": "CAS suite filter construction", "canonical_claim": "The full CA gate regenerates its suite list from sources with `utils/cas-gate/generate_cas_suites.sh build` and runs `build/src/unit_tests_dbms` using each bare suite name with a `.*` suffix. `build/cas_suites.txt` contains bare suite names, so a full gtest gate filter must append `.*` to each suite name; a colon-joined filter of bare names can run zero tests and still exit with status 0."}
{"cluster_id": "chunk_013-004", "topic": "TLC pin enforcement", "canonical_claim": "TLC model runners must reject any jar whose SHA-256 differs from `cc4803dce2a8ffaf0f5920a9dc39df4b5ee34ab4cb53fb58ac557277a7e516b3`; runners must not silently select another jar. The Task 8A model results use official TLC `2026.07.18.145032` with SHA-256 `cc4803dce2a8ffaf0f5920a9dc39df4b5ee34ab4cb53fb58ac557277a7e516b3` and one worker. The official TLC jar expected by the pin is `tmp/tla2tools-official.jar` with SHA-256 `cc4803dce2a8ffaf0f5920a9dc39df4b5ee34ab4cb53fb58ac557277a7e516b3`; the local `tmp/tla2tools.jar` symlink was instead identified as the TLAToolbox 1.7.4 jar with SHA-256 `936a262061c914694dfd669a543be24573c45d5aa0ff20a8b96b23d01e050e88`. The model suite uses the pinned official `tla2tools` jar revision `30cc360` with SHA-256 `cc4803dce2a8ffaf0f5920a9dc39df4b5ee34ab4cb53fb58ac557277a7e516b3`; the former TLC 2.19 revision `5a47802` is rejected. The four T7 Lane B model runners are pinned with `check_tlc_pin`, use TLC `2026.07.18.145032` revision `30cc360`, and require SHA-256 `cc4803dce2a8ffaf0f5920a9dc39df4b5ee34ab4cb53fb58ac557277a7e516b3` for `tla2tools.jar`."}
{"cluster_id": "chunk_013-005", "topic": "The Task 8A model runners are invoked from `docs/superpowers/models` wit", "canonical_claim": "The Task 8A model runners are invoked from `docs/superpowers/models` with `TLC_JAR=../../../tmp/tla2tools-official.jar ./run_refwcleanup.sh` and `TLC_JAR=../../../tmp/tla2tools-official.jar ./run_foldclamp.sh`."}
{"cluster_id": "chunk_013-006", "topic": "`standard_end` quiesces the cluster, settles fsck, drives forced GC to a", "canonical_claim": "`standard_end` quiesces the cluster, settles fsck, drives forced GC to a fixpoint, runs final fsck and dry-run checks, collects observations, dumps raw extracts, and runs common hard assertions."}
{"cluster_id": "chunk_013-007", "topic": "`insert_values` should use deterministic `SELECT ", "canonical_claim": "`insert_values` should use deterministic `SELECT ... FROM numbers(N)` content for deduplication tests because `randomString` is non-deterministic."}
{"cluster_id": "chunk_013-008", "topic": "Scenario table names use the scenario ID as a prefix, such as `s09_wide`", "canonical_claim": "Scenario table names use the scenario ID as a prefix, such as `s09_wide`, so cards do not collide and `table_filter` can scope quiescence."}
{"cluster_id": "chunk_013-009", "topic": "Soak runs bind-mount the host-built binary from `", "canonical_claim": "Soak runs bind-mount the host-built binary from `../../build/programs/clickhouse`, and record `version()` plus the repository Git SHA in `config.json`."}
{"cluster_id": "chunk_013-010", "topic": "`--no-reset` reuses the current pool, and its first run is not a clean-p", "canonical_claim": "`--no-reset` reuses the current pool, and its first run is not a clean-pool run."}
{"cluster_id": "chunk_013-011", "topic": "Completed unfiltered `unit_tests_dbms` logs can contain NUL bytes and om", "canonical_claim": "Completed unfiltered `unit_tests_dbms` logs can contain NUL bytes and omit the beginning of redirected stdout; use `grep -a`, keep completion markers in a separate small file, and rely on the surviving gtest summary for totals."}
{"cluster_id": "chunk_013-012", "topic": "`clickhouse-disks cas-inspect <key>` is a read-only CLI that decodes rec", "canonical_claim": "`clickhouse-disks cas-inspect <key>` is a read-only CLI that decodes recognized CA layouts using `decodeRootShard`, `decodePartManifest`, `decodeMountLease`, `decodeGcState`, `decodeFoldSeal`, `decodeRetiredSet`, and `CasEnvelope` for `blobs/`; unknown keys fail with `BAD_ARGUMENTS` rather than guessing."}
{"cluster_id": "chunk_013-013", "topic": "The B140 dangle model battery is reproduced with `docs/superpowers/model", "canonical_claim": "The B140 dangle model battery is reproduced with `docs/superpowers/models/run_b140danglemerge.sh`."}
{"cluster_id": "chunk_013-014", "topic": "The authoritative residual check is the in-degree run; counting `adds > ", "canonical_claim": "The authoritative residual check is the in-degree run; counting `adds > removes` is only a heuristic because in-degree is a last-wins presence set."}
{"cluster_id": "chunk_013-015", "topic": "The read-only `clickhouse-disks` CAS tools include `fsck`, `cas-inspect`", "canonical_claim": "The read-only `clickhouse-disks` CAS tools include `fsck`, `cas-inspect`, `cas-gc-dryrun`, `cas-gc-rebuild`, and `cas-drop-member`."}
{"cluster_id": "chunk_013-016", "topic": "All CAS unit-test suite names start with `Cas`, so `--gtest_filter='Cas*", "canonical_claim": "All CAS unit-test suite names start with `Cas`, so `--gtest_filter='Cas*'` runs the complete CAS suite; the suite generator fails loudly when a suite does not match."}
{"cluster_id": "chunk_013-017", "topic": "The 2026-07-15 reboot checkpoint recorded the all-tree part-files plan a", "canonical_claim": "The 2026-07-15 reboot checkpoint recorded the all-tree part-files plan as closed at 12/12 with approved reviews and a green milestone soak, codecs-v3 phases 2/4/5/7 committed, drafts for phases 2/4/7 parked in worktrees, and stateless gate #49 killed two minutes after dispatch."}
{"cluster_id": "chunk_013-018", "topic": "The sanctioned full CA-default stateless lane is `python -m ci", "canonical_claim": "The sanctioned full CA-default stateless lane is `python -m ci.praktika run \"Stateless tests (arm_binary, content_addressed storage, parallel)\"`."}
{"cluster_id": "chunk_013-019", "topic": "Codecs-v3 draft integration requires reading `DRAFT", "canonical_claim": "Codecs-v3 draft integration requires reading `DRAFT.md`, applying `DRAFT.patch` with `git apply --3way`, building and running named tests, committing using the draft split, reviewing the pinned diff, and then running phase exit criteria plus a milestone soak."}
{"cluster_id": "chunk_013-020", "topic": "Tests expecting `LOGICAL_ERROR` must be split for sanitizer builds becau", "canonical_claim": "Tests expecting `LOGICAL_ERROR` must be split for sanitizer builds because constructing one `ABORTS` under `DEBUG_OR_SANITIZER_BUILD`; the release arm uses a throw assertion and the sanitizer arm uses `EXPECT_DEATH` in a `Cas*DeathTest` suite."}
{"cluster_id": "chunk_013-021", "topic": "The intended sanitizer and release arms of a split `LOGICAL_ERROR` test ", "canonical_claim": "The intended sanitizer and release arms of a split `LOGICAL_ERROR` test must be verified with `--gtest_list_tests` on both builds."}
{"cluster_id": "chunk_013-022", "topic": "Every `ca-review` finding is labeled `CODE/TEST` or `PROSE`; prose findi", "canonical_claim": "Every `ca-review` finding is labeled `CODE/TEST` or `PROSE`; prose findings are graded `FALSE` or `IMPRECISE`."}
{"cluster_id": "chunk_013-023", "topic": "A `ca-review` sanitizer sweep checks every touched test file for `EXPECT", "canonical_claim": "A `ca-review` sanitizer sweep checks every touched test file for `EXPECT_(ANY_)?THROW` and `expectThrowsCode(.*LOGICAL_ERROR)` and compares each hit with the actual throw site's error code."}
{"cluster_id": "chunk_013-024", "topic": "The `meta_pool_wait` phase has an empty `ProfileEvents` delta because it", "canonical_claim": "The `meta_pool_wait` phase has an empty `ProfileEvents` delta because its work runs on other threads; use `phase_metrics` `jobs_scheduled` and `jobs_completed` with its duration to distinguish queue depth from endpoint slowness."}
{"cluster_id": "chunk_013-025", "topic": "The CAS read-only scrape must use grep -R and sed --follow-symlinks beca", "canonical_claim": "The CAS read-only scrape must use grep -R and sed --follow-symlinks because tests/config/install.sh symlinks configuration files into config.d"}
{"cluster_id": "chunk_013-026", "topic": "The fsck ENTRY gate can safely accept `--partial` for the positive direc", "canonical_claim": "The fsck ENTRY gate can safely accept `--partial` for the positive direction: a partial scan that finds `dangling > 0` still fails, while proof of absence must be reported as `unchecked`; the gate currently does not run on a 5.5 GB pool."}
{"cluster_id": "chunk_013-027", "topic": "Global jemalloc profiling is enabled with `<jemalloc_enable_global_profi", "canonical_claim": "Global jemalloc profiling is enabled with `<jemalloc_enable_global_profiler>1</jemalloc_enable_global_profiler>` in server XML and requires a server restart; sampling begins at boot."}
{"cluster_id": "chunk_013-028", "topic": "The symbolized jemalloc SQL dump is `SELECT line FROM system", "canonical_claim": "The symbolized jemalloc SQL dump is `SELECT line FROM system.jemalloc_profile_text SETTINGS jemalloc_profile_text_output_format = 'symbolized', jemalloc_profile_text_symbolize_with_inline = 1 FORMAT TSVRaw`; symbolized output needs no binary for `jeprof`."}
{"cluster_id": "chunk_013-029", "topic": "Growth attribution uses `jeprof --text --base=t1", "canonical_claim": "Growth attribution uses `jeprof --text --base=t1.sym t2.sym`; the diff represents allocations made between the two dumps that remain live at the later dump."}
{"cluster_id": "chunk_013-030", "topic": "A growth study should collect t0 immediately after boot, t1 after warmup", "canonical_claim": "A growth study should collect t0 immediately after boot, t1 after warmup settles, and t2 after the study window; `--base=t0 t1` attributes warmup and `--base=t1 t2` attributes steady state."}
{"cluster_id": "chunk_013-031", "topic": "SYSTEM CAS GC RUN behavior", "canonical_claim": "`SYSTEM CAS GC RUN [ON CLUSTER cluster_name] [disk_name]` runs one synchronous, node-local garbage-collection round for content-addressed `MergeTree` disks and reclaims objects no longer referenced by any part. When `disk_name` is omitted from `SYSTEM CAS GC RUN`, one round runs on every content-addressed disk configured on the node, and the command raises an exception if none are configured."}
{"cluster_id": "chunk_013-032", "topic": "The planned `Cas[A-Z]` cleanup gate was invalid because it passed a PCRE", "canonical_claim": "The planned `Cas[A-Z]` cleanup gate was invalid because it passed a PCRE lookahead to `grep -vE`, which does not support lookahead and could report zero lines after an error; a Python classifier was required for the actual sweep."}
{"cluster_id": "chunk_013-033", "topic": "`snapshotFramingSize` is property-tested against the full encoder after ", "canonical_claim": "`snapshotFramingSize` is property-tested against the full encoder after being narrowed to `(ns, snapshot_id, row_count)`."}
{"cluster_id": "chunk_013-034", "topic": "The Slice C final focused verification passed 10/10 tests across five su", "canonical_claim": "The Slice C final focused verification passed 10/10 tests across five suites and 99/99 tests across six focused suites, including raw decoder pins, live reconstruction, terminal refusal, framing equality, inspection JSON, and the generation-8 byte golden."}
{"cluster_id": "chunk_013-035", "topic": "The exact retired-identifier grep over `src`, `tests`, `programs`, `util", "canonical_claim": "The exact retired-identifier grep over `src`, `tests`, `programs`, `utils`, and `base` produced no matches."}
{"cluster_id": "chunk_013-036", "topic": "The namespace-file contract tests passed 2/2 under ASan without AddressS", "canonical_claim": "The namespace-file contract tests passed 2/2 under ASan without AddressSanitizer, LeakSanitizer, or runtime-error diagnostics."}
{"cluster_id": "chunk_013-037", "topic": "The final tidy CAS gate passed 278/278 release suites and 296/296 ASan s", "canonical_claim": "The final tidy CAS gate passed 278/278 release suites and 296/296 ASan suites, matching the standing suite-count baseline."}
{"cluster_id": "chunk_013-038", "topic": "`CasRefRecoveryCasWalk", "canonical_claim": "`CasRefRecoveryCasWalk.PutHookBackendComposesHidingListBackendCasPutFaultInjection` arms both hook layers and verifies that both fire for one `casPut` call."}
{"cluster_id": "chunk_013-039", "topic": "The `ThreadGroup` lifetime issue can be tested by creating a borrowed ch", "canonical_claim": "The `ThreadGroup` lifetime issue can be tested by creating a borrowed child group, destroying the parent, and then accessing child counters under ASan or a Debug build."}
{"cluster_id": "chunk_013-040", "topic": "The `LOGICAL_ERROR` ASan-abort triage found four coherent production-inv", "canonical_claim": "The `LOGICAL_ERROR` ASan-abort triage found four coherent production-invariant groups with their guard sites: mount single-writer/lifecycle guards in `CasServerRoot.cpp:695-711,807-865,941-944`; part-write lifecycle and dependency-closure guards in `CasPartWriteTxn.cpp:125-129,329-337,520-524,818-823,895-900,986-992`; ordered/reserved-identifier guards in `CasBlobInDegree.cpp:176-181`, `CasRecordStreamFormat.cpp:156-168`, `CasTypes.h:242-249`; and a snapshot seal-bound guard in `CasRefSnapshotFormat.cpp:93-101`."}
{"cluster_id": "chunk_013-041", "topic": "For sanitizer/debug-build-deterministic `EXPECT_DEATH` tests covering ge", "canonical_claim": "For sanitizer/debug-build-deterministic `EXPECT_DEATH` tests covering genuine `LOGICAL_ERROR` invariant violations, force `DB::abort_on_logical_error.store(true)` inside the death-test child process (an existing production knob, `Exception.h:34`) to get cross-build-type determinism and matchable abort message text."}
{"cluster_id": "chunk_013-042", "topic": "Running a CAS stateless test directly via `tests/clickhouse-test` agains", "canonical_claim": "Running a CAS stateless test directly via `tests/clickhouse-test` against a manually-started `clickhouse-server` binary (no configured praktika lane) requires exporting `CLICKHOUSE_USER_FILES` to match the server's actual data path; the harness's default `/var/lib/clickhouse/user_files` mismatch makes the pool directory invisible to the server, producing `Unknown disk` with `baseline=0 after_insert=0`."}
{"cluster_id": "chunk_013-043", "topic": "When asserting that a TLA+ sabotage config produces the expected counter", "canonical_claim": "When asserting that a TLA+ sabotage config produces the expected counterexample, the runner scripts must grep the TLC log for a NAMED violation (`Invariant/Property/Action property <X> is violated`) and never trust a bare nonzero exit code, because TLC also exits nonzero on a parse error, an unspecified successor state, or a deadlock report — exactly how a broken model can masquerade as a working sabotage; this exact failure mode hid a live parse bug in `CaBuildRootPrecommit.tla` on 2026-07-28."}
{"cluster_id": "chunk_013-044", "topic": "TLA+ models run from `docs/superpowers/models/` with the TLC jar expecte", "canonical_claim": "TLA+ models run from `docs/superpowers/models/` with the TLC jar expected at `../../../tmp/tla2tools.jar` (v2.19) and the Apalache binary at `../../../tmp/apalache/bin/apalache-mc` (v0.58.0+); runners are either suite runners that take no arguments and assert every expected outcome by invariant/property name (`run_refsnaplog.sh`, `run_mount.sh`, `run_buildrootprecommit.sh`, etc.) or single-config drivers that take a cfg as `$1` and assert nothing themselves (`run_tlc.sh`, `run_gc_partmanifest.sh`, `run_ackfloor.sh`)."}
{"cluster_id": "chunk_013-045", "topic": "Attributing S3 PUTs to MergeParts/MutatePart requires a query_id-decodin", "canonical_claim": "Attributing S3 PUTs to MergeParts/MutatePart requires a query_id-decoding technique because part_log's own ProfileEvents never captures a nonzero PUT for those event types: the upload count is recovered by matching blob_storage_log's synthetic `<table_uuid>::<part_name>` query_id against part_log by part name (documented in the analyzing-cas-health skill and utils/ca-soak/scenarios/BACKLOG.md's 2026-07-19 'S3 PUT budget' entry)."}
{"cluster_id": "chunk_013-046", "topic": "The soak harness's quiescence protocol before any checkpoint assertion i", "canonical_claim": "The soak harness's quiescence protocol before any checkpoint assertion is: pause all workers; `SYSTEM SYNC REPLICA` on both nodes and wait for `system.replication_queue` to be empty; wait for every `system.mutations` row to be `is_done` with no active `system.merges`; run `OPTIMIZE TABLE ... FINAL` plus `ALTER TABLE ... MATERIALIZE TTL`; then drive CA GC to fixpoint by polling until the pool object set and `ca-fsck`'s `unreachable` field stop changing across successive rounds."}
{"cluster_id": "chunk_013-047", "topic": "The acked-then-lost data-loss fix's regression test is `05014_insert_ded", "canonical_claim": "The acked-then-lost data-loss fix's regression test is `05014_insert_dedup_disk_commit_failpoint`, paired with the targeted failpoint `part_storage_fail_commit_transaction` in `DataPartStorageOnDiskFull::commitTransaction`."}
{"cluster_id": "chunk_013-048", "topic": "The praktika stateless job name that works for local CAS validation is `", "canonical_claim": "The praktika stateless job name that works for local CAS validation is `\"Stateless tests (arm_binary, parallel)\"`."}
{"cluster_id": "chunk_013-049", "topic": "A `gc_fold_clamp` event (fields: namespace, shard, manifest, reason, at_", "canonical_claim": "A `gc_fold_clamp` event (fields: namespace, shard, manifest, reason, at_version) was added per clamp occurrence to `content_addressed_log`, so that root-causing a GC clamp incident — previously an hour of event-log archaeology across archived server logs — becomes a single query."}
{"cluster_id": "chunk_013-050", "topic": "The CAS pool-member decommission SQL surface exposes `SYSTEM CONTENT ADD", "canonical_claim": "The CAS pool-member decommission SQL surface exposes `SYSTEM CONTENT ADDRESSED DROP POOL MEMBER`, backed by a disks facade command `ca-drop-member`; validated end-to-end on a 2-node RustFS integration test (kill -> lease expiry -> drop -> heal -> fsck-clean -> BAD_ARGUMENTS flow)."}
{"cluster_id": "chunk_013-051", "topic": "The scenario harness's `observe", "canonical_claim": "The scenario harness's `observe.pool_shape()` probe had a `timeout N cd <dir> && find ...` bug: `timeout` cannot exec the `cd` shell-builtin, so `&&` short-circuited and the probe silently returned no `_total`; fixed to `timeout N sh -c '<cd && find | xargs stat>'`."}
{"cluster_id": "chunk_013-052", "topic": "CAS's `contentAddressedFsckColumns` SQL row (the `SYSTEM ", "canonical_claim": "CAS's `contentAddressedFsckColumns` SQL row (the `SYSTEM ... FSCK` output) does not surface `lifeless_keys` even though the function's own comment states every term of `FsckReport::clean` must appear there; the comment claiming the summary is derived mechanically from `clean()`'s field set was false — the actual implementation is five hand-listed field names, so the enforcement mechanism the comment describes does not exist."}
{"cluster_id": "chunk_013-053", "topic": "A reviewer sub-agent must deliver findings BOTH as a durable file AND in", "canonical_claim": "A reviewer sub-agent must deliver findings BOTH as a durable file AND in its final message; message-only delivery risks the findings existing nowhere durable and never reaching the implementer, while file-only delivery has separately caused lost verdicts."}
{"cluster_id": "chunk_013-054", "topic": "The `utils/ca-soak/scenarios` adversarial card suite (S01-S35) stresses ", "canonical_claim": "The `utils/ca-soak/scenarios` adversarial card suite (S01-S35) stresses one hard condition per scenario; many gtest regression pins originated as soak findings, e.g. S04/S04b became `gc_undercount_repro.cpp` and the TRUNCATE op-450 stall became `truncate_reclaim.cpp`."}
{"cluster_id": "chunk_013-055", "topic": "S42 fault-injection coverage", "canonical_claim": "S42 arms `memory_tracker_fault_probability` through driver URL parameters and pairs it with `max_untracked_memory=0` so small allocations reach the memory tracker. S42 is intended to fault allocations through the per-query `memory_tracker_fault_probability` setting and thread creation through `cannot_allocate_thread_fault_injection_probability`, then verify that durable CAS state, replicas, fsck, and post-restart views remain consistent even when queries fail."}
{"cluster_id": "chunk_013-056", "topic": "fsck timeout scalability", "canonical_claim": "The D1 report identifies fsck scalability as follow-up work: `clickhouse-disks fsck` timed out after more than 180 seconds on a pool with approximately 1.7 million rows, although the final converge fsck completed cleanly. The flat 180-second fsck budget can time out once the ref space is large enough that the check for stuck blobs stops running at the scale where it is needed."}
{"cluster_id": "chunk_013-057", "topic": "`CasRefCodec", "canonical_claim": "`CasRefCodec.EncodeAllowsExactlyMaxRemovalBytes` could not be classified from the archived log because it ended after the `[ RUN      ]` line without an exception, sanitizer report, result, or exit code; it requires an isolated ASan run with preserved exit status, stderr, and host OOM or signal evidence."}
{"cluster_id": "chunk_013-058", "topic": "The relink unhappy-path test must drive a pending target-name mutation, ", "canonical_claim": "The relink unhappy-path test must drive a pending target-name mutation, a wedge, and a lost fence, then assert severity, transient-state messaging, no same-source byte re-request, and convergence of the retry cycle."}
{"cluster_id": "chunk_013-059", "topic": "The required Stage B tests include old-file hiding across drop and rebir", "canonical_claim": "The required Stage B tests include old-file hiding across drop and rebirth, stale readers and delayed writers after rebirth, resumed stale cleanup, zero catalog requests on hot paths, unchanged rewrite/append/remove/dedup-rotation request counts, LIST-independent recovery, missing and conflicting `_ckpt.life_epoch`, rejection of legacy keys, and compile-time absence of namespace-only key overloads."}
{"cluster_id": "chunk_013-060", "topic": "The T14 audit leaves the identity of the `pthread_mutex_lock` term open ", "canonical_claim": "The T14 audit leaves the identity of the `pthread_mutex_lock` term open because only the top frame was captured; the addendum must retain full symbolized stacks and filter `trace_type='Real'` with top frame `pthread_mutex_lock`."}
{"cluster_id": "chunk_013-061", "topic": "The T14 audit leaves the `mkdir` to `std::filesystem::create_directories", "canonical_claim": "The T14 audit leaves the `mkdir` to `std::filesystem::create_directories` attribution open; a deeper or focused background profile must determine whether the directory-creation signal survives bounded rounds and which layer creates the directories."}
{"cluster_id": "chunk_013-062", "topic": "The frontier-attribution spike must classify every unproven namespace as", "canonical_claim": "The frontier-attribution spike must classify every unproven namespace as post-LIST append above frozen tail, probe-budget exhaustion, effective hold, genuine gap, undecodable `_ckpt`, or whole-fold abort before destruction is enabled."}
{"cluster_id": "chunk_013-063", "topic": "The LIST-as-journal detector had fired 14 times but still needed lease i", "canonical_claim": "The LIST-as-journal detector had fired 14 times but still needed lease identity recorded at both ends of the enumeration to distinguish an actual listing hole from concurrent deletion."}
{"cluster_id": "chunk_013-064", "topic": "The 600-second product fsck deadline was insufficient for a roughly 29 G", "canonical_claim": "The 600-second product fsck deadline was insufficient for a roughly 29 GiB pool: a measured run took 731.1 seconds and returned `TIMEOUT_EXCEEDED` without a `reachable=` line."}
{"cluster_id": "chunk_013-065", "topic": "S22 requires a fault-injecting proxy between the CA disk endpoint and Ru", "canonical_claim": "S22 requires a fault-injecting proxy between the CA disk endpoint and RustFS to inject `503 SlowDown`, `429 Too Many Requests`, latency, and mid-response TCP resets; toxiproxy covers TCP faults but not HTTP status injection."}
{"cluster_id": "chunk_013-066", "topic": "S27 requires an instrumented S3 proxy that can duplicate root-shard LIST", "canonical_claim": "S27 requires an instrumented S3 proxy that can duplicate root-shard LIST keys, omit a continuation token, or change an otherwise stable list token so `listRootShardTokens` conservative rereads can be exercised."}
{"cluster_id": "chunk_013-067", "topic": "`CasRefInstallSafetyDeathTest` is absent from both release and ASan gate", "canonical_claim": "`CasRefInstallSafetyDeathTest` is absent from both release and ASan gate binaries because it is gated on `MEMORY_TRACKER_DEBUG_CHECKS`; covering it requires a plain debug build."}
{"cluster_id": "chunk_013-068", "topic": "GC verification must retain a strict grouped enumeration of all key clas", "canonical_claim": "GC verification must retain a strict grouped enumeration of all key classes and add negative controls for ambiguous-send reuse, hidden checkpoints, rebirth cleanup omissions, asymmetric REBUILD omissions, and current-epoch manifest growth."}
{"cluster_id": "chunk_013-069", "topic": "The verification model must include one shared blob and pool-wide in-deg", "canonical_claim": "The verification model must include one shared blob and pool-wide in-degree and retirement state, with controls for late +1, catalog transitions, forced REBUILD after a hold, classification-4 codec limits, stale reads, duplicate nomination, token ABA, and _ckpt-absent decommission finalization."}
{"cluster_id": "chunk_013-070", "topic": "Verification must force missing prior seals, disappearing held witnesses", "canonical_claim": "Verification must force missing prior seals, disappearing held witnesses, an incarnation-dropping key builder, and an old-generation wedge result after successor seal adoption; recovery and wedge installs need a generation recheck under the install lock."}
{"cluster_id": "chunk_013-071", "topic": "Audit every public operation in ContentAddressedMetadataStorage, Content", "canonical_claim": "Audit every public operation in ContentAddressedMetadataStorage, ContentAddressedTransaction, and object-storage entry points by latency class, S3 round trips, state dependence, and lock context."}
{"cluster_id": "chunk_013-072", "topic": "Audit every mutex, SharedMutex, and condition variable in the CA tree fo", "canonical_claim": "Audit every mutex, SharedMutex, and condition variable in the CA tree for protected state, longest critical-section operation, waiters, cutoff or indefinite-wedge behavior, and fencing interaction."}
{"cluster_id": "chunk_013-073", "topic": "`CasRefCatalogFormat", "canonical_claim": "`CasRefCatalogFormat.RemovalStartedRoundIsRequiredExactlyForRemoving` directly invokes a `LOGICAL_ERROR`-throwing site under `DEBUG_OR_SANITIZER_BUILD`, where the process aborts instead of throwing; the planned hygiene fix is to guard the throwing arm and add a `Cas*DeathTest` arm using `EXPECT_DEATH`."}
{"cluster_id": "chunk_013-074", "topic": "The recommended remediation order for the LOGICAL_ERROR ASan triage is: ", "canonical_claim": "The recommended remediation order for the LOGICAL_ERROR ASan triage is: (1) swap the one Class A hook from `LOGICAL_ERROR` to `S3_ERROR`; (2) convert the 19 direct Class B negative assertions to `EXPECT_DEATH`, including the worker-thread heartbeat case; (3) repair the three incidental teardown fixtures so the primary test cleans up validly while the release guard gets an isolated death assertion; (4) remove the Class D test from the logical-error list pending a captured, reproducible failure."}
{"cluster_id": "chunk_013-075", "topic": "The recommended CA transient-classifier fix (routing `ContentAddressedMe", "canonical_claim": "The recommended CA transient-classifier fix (routing `ContentAddressedMetadataStorage.cpp:1111`, `:1163`, and `CasMountRuntime.cpp:106` through a new `throwCasTransientUnavailable`) has carry-along updates: `utils/ca-soak/soak/cluster.py:215`; `gtest_cas_operation_gate.cpp:182-217` (must split its shared-code contract asserting both `TransientNotLive` and `IdentityLost` throw 668); `gtest_cas_fsck.cpp:669`; `gtest_cas_gc_stop_start.cpp:472`; and `docs/superpowers/specs/2026-07-22-cas-disk-lease-loss-throw-and-stop-verbs-design.md:56`."}
{"cluster_id": "chunk_013-076", "topic": "After the ASan-battery grep fix, of the 28 verified distinct real culpri", "canonical_claim": "After the ASan-battery grep fix, of the 28 verified distinct real culprits 7 were already-fixed memory-safety bugs and the remaining 21 needed LOGICAL_ERROR-abort classification (Class A/B/C/D); the battery should be relaunched from the corrected exclusion list once the Class A/B fixes land so it can progress past test ~700/910 instead of looping."}
{"cluster_id": "chunk_013-077", "topic": "The `utils/ca-soak` harness uses a deterministic `splitmix64` seed to dr", "canonical_claim": "The `utils/ca-soak` harness uses a deterministic `splitmix64` seed to drive SQL workload and in-memory aggregate oracle, checks both replicas against the oracle, and finishes with `clickhouse-disks fsck`."}
