{"cluster_id": "chunk_003-071", "topic": "design-history", "canonical_claim": "The old `gc_clear_on_spare` behavior and the post-adoption clear modeled by `post_adoption_clear` must remain red in the TLA gate; the latter is specifically unsafe because adoption does not fence pre-CAS side effects."}
{"cluster_id": "chunk_003-072", "topic": "design-history", "canonical_claim": "The report's earlier claim that `renameParts` performed the CA publish before the Keeper multi is explicitly corrected as false; on CA `renameParts` is only a tmp-to-final overlay re-key."}
{"cluster_id": "chunk_003-073", "topic": "design-history", "canonical_claim": "The `lazy_load_tables` proxy is considered unsafe for upstream promotion because it weakens type identity and has roughly sixty unforwarded virtuals, including a silent-empty `backupData` path."}
{"cluster_id": "chunk_003-074", "topic": "design-history", "canonical_claim": "The CAS path stages every blob locally and then uploads `pending_blobs` in a serial loop while committing parts one at a time, so one thread orchestrates and waits on each PUT or HEAD in turn."}
{"cluster_id": "chunk_003-075", "topic": "design-history", "canonical_claim": "The dead-precommit skip was not the S42 cause because `dead_precommits_skipped` was zero on both nodes; the skipped `-1` path never fired."}
{"cluster_id": "chunk_003-076", "topic": "design-history", "canonical_claim": "The previous design treated immutable log PUT as durability even though semantic application depended on later in-memory installation, creating possibly durable-but-not-applied state after an ambiguous PUT."}
{"cluster_id": "chunk_003-077", "topic": "design-history", "canonical_claim": "The intermediate `B167` blob-watermark design had a livelock in which GC could re-condemn a build's fresh upload during the non-atomic upload-to-publish gap, preventing publication forever."}
{"cluster_id": "chunk_003-078", "topic": "design-history", "canonical_claim": "The B140 dangle was caused by `reuseBlob` (the adopt operation) transferring the blob hash but not the `cas_owner` protection, so an adopted blob's protection stayed bound to the retired byte-writer and GC reclaimed it under a still-in-flight adopter; `system.content_addressed_log` (B170) pinned the precise dangle event with its token and timing."}
{"cluster_id": "chunk_003-079", "topic": "design-history", "canonical_claim": "An aggressive test GC cadence exceeded the fixed iteration cap of 8 in `resolveAndResurrectGeneration`, producing `CORRUPTED_DATA` failures in ordinary insert/mutation tests; the soak surfaced 33 failures from this single root cause and the fix was a one-line cap change from 8 to 256."}
{"cluster_id": "chunk_003-080", "topic": "design-history", "canonical_claim": "A 2026-06-20 soak reported 1.6M `S3WriteRequestsErrors` and 94 `fsck dangling` after a chaos window; forensic analysis showed all 922,772 GC deletes had `indeg_at_recheck=0`, the errors were ~1.57M by-design `PreconditionFailed` (412) CAS contention on root-shard manifests, and the 94 dangling was a transient read-unavailability false-positive (429/503 load shedding) whose 180s settle budget was too short under write load."}
{"cluster_id": "chunk_003-081", "topic": "design-history", "canonical_claim": "`CaGcIndegRefoldCore.tla` isolated an in-degree-underflow hazard the large safety model could not express: the C++ accumulated blob in-degree as a non-idempotent integer delta stream, which could reach -1 (throwing `CORRUPTED_DATA`) when a completion-seal cursor persisted at the wrong position; the fix adopted an idempotent two-cursor presence-set merge, making the underflow structurally impossible, and the model was then removed because it described a design the code had abandoned."}
{"cluster_id": "chunk_003-082", "topic": "design-history", "canonical_claim": "An external author review of rev.6 confirmed 3 substantive findings: F1 seal-skip-on-empty-dead-region combined with a blind detector, F2 a sticky-boundary-bool causing over-seal plus a fail-open allowlist, and F3 the seal PUT held under `state_mutex` for up to 90s; all three were filed as a fix-round task."}
{"cluster_id": "chunk_003-083", "topic": "design-history", "canonical_claim": "`CasInstrumentedBackend`'s classifier still contains a dead `_watermark` classification branch and a stale comment (`CasStore.cpp:263`) claiming the watermark writes a `roots/<server-hex>/_watermark` slot; the standalone watermark object was retired when the watermark was folded into the mount-lease beat by the ack-floor merge (`FormatId::Watermark=7` is retired)."}
{"cluster_id": "chunk_003-084", "topic": "design-history", "canonical_claim": "The stage-1 fix-verification pass found the writer-ledger memory regression guard incomplete: `LongTailReplaysUnderMemoryBound` exercises only the free `recoverRefTable`/`recoverRefTableDetailed` path, not `CasRefLedger::ensureRefTableRecovered`, so the ledger's own recovery loop could regress to whole-tail materialization while all existing bounds and the test-local control stay green; a later fix (commit 330d52e8c96) added a dedicated ledger-path test that verifies both an upper and lower peak bound."}
{"cluster_id": "chunk_003-085", "topic": "design-history", "canonical_claim": "Several GC-reclaim tests for abandoned precommits were deleted (not disabled) because reclaiming an abandoned precommit is now the writer's job on recovery (it appends the exact `owner_transition` removal), not GC's."}
{"cluster_id": "chunk_003-086", "topic": "design-history", "canonical_claim": "`gtest_cas_mount.cpp:101`'s comment \"No mount has been written in Task 4\" is stale and contradicts the same file's extensive mount writes; `gtest_ca_wiring.cpp`'s B188 predicate comment still references vestigial `/trees/` though the gate collects only `/blobs/`."}
{"cluster_id": "chunk_003-087", "topic": "design-history", "canonical_claim": "The final whole-branch review (base `d57a41f353d` to head `7771bb60c70`, 38 commits) found a Critical `partAccess` use-after-free (the same class task T9 meant to close but missed) plus a `gcHealth`-blocks-behind-round issue, fixed in `452d17af42f`."}
{"cluster_id": "chunk_003-088", "topic": "design-history", "canonical_claim": "The final whole-branch review found the S22 caller discarded `Committed`/`Conflict`/`Unresolved` outcomes (fixed `8068d8c5fe0`), the decommission owner-tombstone write was not resumable on an ambiguous outcome (fixed `bec89a9de95`), and `emu_token_state` had unbounded growth (fixed `08ea8d1200e`, closing the whole fix-wave and review cycle)."}
{"cluster_id": "chunk_003-089", "topic": "design-history", "canonical_claim": "S13's quiesce-wedge (cluster died ~6 minutes into the quiescing-cluster phase) was attributed to the same lifecycle-lock class as the final-review Critical `partAccess` use-after-free; after the fix, a re-run with sidecar instrumentation passed 13/13 with quiesce completing cleanly in ~7.5 minutes."}
{"cluster_id": "chunk_003-090", "topic": "design-history", "canonical_claim": "An early draft of the relink/seam TLA+ model omitted the WedgeRetryCreated action entirely, making the wedge protocol's own happy path unreachable: CasRefLedger.h:784-801 documents that a bounded conditional-create retry either lands the exact same transaction bytes or conflicts, so without modelling that retry as a durability-producing event, WedgeResolveInstall's sApplyOwed guard could never be satisfied and every state downstream of a resolved wedge was missing from the state space."}
{"cluster_id": "chunk_003-091", "topic": "design-history", "canonical_claim": "The mount crash-recovery change does not modify `claimMount`, the owner anchor, the epoch counter, or the watermark, and it adds no user-facing configuration."}
{"cluster_id": "chunk_003-092", "topic": "design-history", "canonical_claim": "The no-paranoid-mode decision rejects retaining deleted checks behind a configuration flag; detection remains with ca-fsck, soak assertions, and system.content_addressed_log."}
{"cluster_id": "chunk_003-093", "topic": "design-history", "canonical_claim": "The source-layout refactor preserves behavior by forbidding mutex coverage or lock-order changes, construction/destruction-order changes, persisted-byte/key changes, log/event/metric changes, and mixing file moves with content edits in one commit."}
{"cluster_id": "chunk_003-094", "topic": "design-history", "canonical_claim": "Renaming `Store` to `Pool` and `Build` to `PartWriteTxn` changes only C++ identifiers and file names; the protocol vocabulary `build_seq`, `buildSeq`, `BuildPrefix`, upload stamps, and related build-sequence identifiers remains unchanged."}
{"cluster_id": "chunk_003-095", "topic": "design-history", "canonical_claim": "Each `RefTableState` experiment must pass the full gtest and property gates, record benchmark numbers and an explicit elegance judgment, and retain reverted experiment results in the comparison report."}
{"cluster_id": "chunk_003-096", "topic": "design-history", "canonical_claim": "The fetch-handoff design and the publish-confirm and ref-lane changes do not modify GC folding, journal codecs, snapshot formats, sweeps, orphan protection, or anomaly/suppression semantics; every write remains in its owning domain, the ordinary INSERT/merge path is behaviorally unchanged, and Part A changes only how a committed transaction is installed."}
{"cluster_id": "chunk_003-097", "topic": "design-history", "canonical_claim": "The TLA phase summary is a pass only when every green configuration is green and every `_sab_*` configuration reports its expected violation; listing-trust audits record the exact action and premise for every model."}
{"cluster_id": "chunk_003-098", "topic": "design-history", "canonical_claim": "The conflicting-`life_epoch` behavior change is delayed until `_ckpt` is re-keyed, because different namespace lives could share the old key before incarnation separation."}
{"cluster_id": "chunk_003-099", "topic": "design-history", "canonical_claim": "`CaRefLaneCore`, `CaRelinkLaneComposition`, `CaRefCatalogCore`, and other listed models with fixed in-module universes have no applicable empty-entity-set configuration; fixed universes are intentional where the proof requires specific actors, rows, or namespace scenarios."}
{"cluster_id": "chunk_003-100", "topic": "design-history", "canonical_claim": "The shipped v12 gate intentionally reuses the name `SabotageStaleCache`: in `CaRelinkConfirmCore` it must be red because it drops v11 rule 3's pending and leader terms, while in `CaRelinkReofferCore` it selects the design predicate and must be green when the marker is intact."}
{"cluster_id": "chunk_003-101", "topic": "design-history", "canonical_claim": "`listedTok` in `CaGcRootLocalPartManifestCore.tla` models a LIST-observed live root-shard token as the value compared with persisted fold coverage to decide whether a shard body read can be skipped."}
{"cluster_id": "chunk_003-102", "topic": "design-history", "canonical_claim": "The historical `CaB140DangleMerge` fix requires journal trimming only up to the cursor inside the committed snapshot and atomic commitment of that cursor with the edges; either half alone can lose data."}
{"cluster_id": "chunk_003-103", "topic": "design-history", "canonical_claim": "A future persistent metadata cache would require separate model review because local manifest bytes could survive process restart outside the modeled object store."}
{"cluster_id": "chunk_003-104", "topic": "design-history", "canonical_claim": "The obscure CAS naming pass changed `CASGc` to `CASGC`, `CASRefCkpt` to `CASRefCheckpoint`, `UnappliedFoldedTxns` to `UnappliedFoldedTransactions`, `PutDedup` to `PutDeduplicated`, and `DedupCache` to `DeduplicationCache`; production classes `CasGc` and `CasRefCkpt` remain unchanged because the renames apply to metric and suite tokens rather than those class identifiers."}
{"cluster_id": "chunk_003-105", "topic": "design-history", "canonical_claim": "`fixture::fixtureLife` creates a deterministic namespace-derived identity rather than a fresh random life, `fixture::admitLive` creates a bare `Live` entry without `_ckpt`, and `fixture::writeRefLogRaw` writes ref-log bytes directly at the resolved fixture identity while bypassing the writer birth/append lane."}
{"cluster_id": "chunk_003-106", "topic": "design-history", "canonical_claim": "CascadeWriteBuffer is excluded from the CAS* suite set because its Cas prefix is accidental and unrelated to CAS tests."}
{"cluster_id": "chunk_003-107", "topic": "design-history", "canonical_claim": "The four token-diff model configurations `stage5_tokendiff`, `sab_skipchangedshard`, `sab_skipparksdeadprecommit`, and `fix_skipparksdeadprecommit` were retired because no production seam skips a manifest or owner-transition fold read based on a LIST-derived token match."}
{"cluster_id": "chunk_003-108", "topic": "design-history", "canonical_claim": "`Gc::newestFoldSealRef` uses listing absence as a virgin-by-enumeration verdict on the rebuild path, and this is documented as a named residual through `RebuildReport::virgin_by_enumeration`."}
{"cluster_id": "chunk_003-109", "topic": "design-history", "canonical_claim": "The exact retired identifiers are `namespaceIsRemoved`, `refCleanupMarkerKey`, `RefNsCleanupState`, `ns_cleanup_items`, `cleanup_markers`, `publishRemovedSnapshotNow`, and `runNamespaceCleanupPasses`."}
{"cluster_id": "chunk_003-110", "topic": "design-history", "canonical_claim": "The `RefTableState` closed-class optimization design lists three residual risks: E3's overlay-level transaction layer moves the strong-exception-safety guarantee into new code and needs an explicit test that state is byte-for-byte unchanged after a throwing transaction; E4's flat-vector base has different iterator-invalidation rules than the old node-based map, so the closed class's read API must not leak the old iterator-stability promises; and `cas-gc-rebuild` is a shared branch worked concurrently by another session, requiring commit discipline (verify `HEAD` after commit, one ninja at a time, never rebase/amend)."}
{"cluster_id": "chunk_003-111", "topic": "design-history", "canonical_claim": "The heart conjunct of the Apalache inductive invariant, `InflightCurrentUnreferenced` (a delete message in flight for a still-current token implies no folded root edge and no unfolded add past the cursor), was proven irredundant by a negative control; dropping the `W-REVALIDATE` re-observation conjunct from `WPublish` produces a counterexample breaking `NoDangle`, machine-checking the F1 re-observation requirement."}
{"cluster_id": "chunk_003-112", "topic": "design-history", "canonical_claim": "A TLA+ model is deleted, not kept, once keeping it would assert a guarantee the shipped code no longer makes; false comfort (an unfaithful model implying a safety property the code does not actually provide) is treated as worse than having no proof at all."}
{"cluster_id": "chunk_003-113", "topic": "design-history", "canonical_claim": "dedupTrace independently traced the CAS write ordering in `ReplicatedMergeTreeSink`: the CAS commit (`renameParts`→`stageManifest`, line 976) strictly precedes the atomic Keeper dedup-token multi-op (line 985), so a failed CAS write leaves no dedup token behind; this refuted the initially-suspected \"orphan-token\" mechanism for the R3 data-loss regression, though the acked-then-lost data loss itself remained confirmed via a separate mechanism."}
{"cluster_id": "chunk_003-114", "topic": "design-history", "canonical_claim": "codex_r6_findings.md's 'Checks that held' section confirms: a ref-log body whose decoded sequence differs from its key is already rejected; under a true state-derived next ID a dying lane that sees a seal at T+1 cannot create T+2 and must retry T+1; seal-on-every-transition (including clean transitions) is stated correctly; the S42 removal-tail retention rule is safety-correct (its missing piece is bounded execution, finding 6); and REBUILD condemn-nothing fixes the round-5 asymmetric-LIST data-loss scenario (its missing piece is orphan-blob reclamation, finding 9)."}
{"cluster_id": "chunk_003-115", "topic": "design-history", "canonical_claim": "`CasFormat`'s `G_BUILD=3` is the highest format generation the build understands (reader rule: `compatibility_version > G_BUILD` throws `UNKNOWN_FORMAT_VERSION`); retired `FormatId`s are frozen and never reused: `Tree(2)`, `GcSnap(4)`, `RetiredSet(6, magic CART)`, `Watermark(7)`, `RootsRegistry(10)`, `CompletionSeal(15)`."}
{"cluster_id": "chunk_003-116", "topic": "design-history", "canonical_claim": "Regression-pin test files are named after the invariant or mechanism they protect (e.g. `CasGcDangle`, `CasGcUndercount`, `CasGcLeak`), not after raw bug numbers except `b140`/`b171`; the absence of a `bNNN`-named file does not mean that bug is uncovered."}
{"cluster_id": "chunk_003-117", "topic": "design-history", "canonical_claim": "Only two tests in the entire CA gtest suite are DISABLED, both in `gtest_cas_protocol_scenarios.cpp`, pinning obsolete tree-model subtree-adopt behavior."}
{"cluster_id": "chunk_003-118", "topic": "design-history", "canonical_claim": "Restricting a deposed GC leader's advisory meta to condemn-only (add-only, never clearing to Clean) makes the TLA+ witness model GREEN (935,815 distinct states), confirming that stray-Condemned effects are safe as the v3 spec claims and that the SOLE violation source was the stray-Clean `clearSparedMeta`; `FoldRound`'s identical clean-on-spare write is safe only because it is coupled to adoption (the pending row is dropped in the same atomic step), a coupling the deposed-leader case lacks."}
{"cluster_id": "chunk_003-119", "topic": "design-history", "canonical_claim": "The source-layout refactor's phase-5 rename sweep (`Store`→`Pool`, `Build`→`PartWriteTxn`) must use word-boundary-anchored seds rather than blanket `s/Store/.../` or `s/Build/.../`, because a blanket substitution would incorrectly hit `IMetadataStorage` and the protocol vocabulary `build_seq`/`BuildPrefix`, which must survive the rename unchanged."}
{"cluster_id": "chunk_003-120", "topic": "design-history", "canonical_claim": "Each non-trivial implementation task uses a fresh implementer followed by independent specification and code-quality review subagents."}
{"cluster_id": "chunk_003-121", "topic": "design-history", "canonical_claim": "The `gc_lock` in-process mutex was rejected because it blocked writers and limited GC to one process; exact-token deletion, attempt-scoped generations, and the lease provide split-brain safety instead."}
{"cluster_id": "chunk_003-122", "topic": "design-history", "canonical_claim": "Content-addressed immutable data uses binary encoding, mutable control and ref objects use protobuf, and the earlier JSON codec family was abandoned."}
{"cluster_id": "chunk_003-123", "topic": "design-history", "canonical_claim": "The CA IO-layer extension points are CA-agnostic virtual interfaces with fail-closed defaults, while the MergeTree integration remains leaky through repeated `isContentAddressed()` branches and a seam header located inside the CA directory."}
{"cluster_id": "chunk_003-124", "topic": "design-history", "canonical_claim": "The canonical incarnation model establishes exact-token deletion, fold-through-fence ordering, registry commit-time universe fencing, and evidence re-observation as load-bearing safety rules."}
{"cluster_id": "chunk_003-125", "topic": "design-history", "canonical_claim": "The shipped writer uses lazy manifest-body edge discovery with a missing-body clamp barrier rather than the model's inline closure at precommit; this preserves the safety conclusion through a different mechanism."}
{"cluster_id": "chunk_003-126", "topic": "design-history", "canonical_claim": "The E1 structural swap introduced the incarnation-token backend model through `Cas::Backend` and `Cas::Store` and deleted the PoC GC, codec, and pool layer."}
{"cluster_id": "chunk_003-127", "topic": "design-history", "canonical_claim": "The rebuild implementation reuses `discoverUniverse`, `foldManifestEdges`, `foldDeltasIntoGeneration`, `computeHeartbeatFloor`, and `prefixEligible` rather than adding a new scanner or merge machinery."}
{"cluster_id": "chunk_003-128", "topic": "design-history", "canonical_claim": "The generation-binding plan does not modify `contrib/aws`; all GCS behavior is implemented in ClickHouse's HTTP, object-storage, and CAS layers."}
{"cluster_id": "chunk_003-129", "topic": "design-history", "canonical_claim": "The view-side names are `Store::syncRetiredView()` and `CasEventType::RetiredViewAdvance` with event string `retired_view_advance`; the lease-side names `MountLeaseKeeper`, `renewOnce`, `mount_renew_period`, and `SingleWriterSlot` remain unchanged."}
{"cluster_id": "chunk_003-130", "topic": "design-history", "canonical_claim": "A different-content overwrite is not treated as a legitimate repoint because MergeTree parts are immutable and mutations mint new names; the intended republish path handles re-drive conflicts explicitly."}
{"cluster_id": "chunk_003-131", "topic": "design-history", "canonical_claim": "The Phase A plan deletes the copy-forward pre-pass and accepts the rare in-closure GET+PUT because the in-closure gate is idempotent under CAS retry."}
{"cluster_id": "chunk_003-132", "topic": "design-history", "canonical_claim": "The writer-side RetireView, syncer, observed_gc_round, and ack-floor are deleted only after all writer metadata point-read sites replace RetireView reads."}
{"cluster_id": "chunk_003-133", "topic": "design-history", "canonical_claim": "The rejected raw-body plan defined a per-hash `.meta` descriptor as a three-state `{clean, condemned, tombstone}` register whose etag is the sole linearization token, while blob bodies are raw immutable content and the writer-side `RetireView`, syncer, `observed_gc_round`, and ack-floor are removed."}
{"cluster_id": "chunk_003-134", "topic": "design-history", "canonical_claim": "The separate `RetiredSet` object family and retired-in-snapshot design were deleted/removed in favor of `kCondemned` sentinel rows embedded in in-degree runs and a two-cursor source-edge merge: `retiredKey`/`Layout::retiredKey`, `GcState::retired_refs`, `FormatId::RetiredSet`, and magic `CART` were removed, the three-cursor settlement became a two-cursor merge, and `CART` is reserved never to be reused."}
{"cluster_id": "chunk_003-135", "topic": "design-history", "canonical_claim": "Removing the spare-side `scheduleMetaJob(... clearSparedMeta ...)` call requires no meta format change, no new I/O, and no delete-path change; `writeCondemnedMeta` and `deleteConfirmedMeta` remain."}
{"cluster_id": "chunk_003-136", "topic": "design-history", "canonical_claim": "The old `RootShardManifest`/`CasRootShardCodec` format has no compatibility scaffolding or dual-format reader: the CAS pool schema constant is bumped so old-format pools fail closed with a clear message through a pool-format bump, and every `RootShard` reference is deleted."}
{"cluster_id": "chunk_003-137", "topic": "design-history", "canonical_claim": "The deleted `RootShardManifest` codec tests are replaced by `gtest_cas_ref_codecs.cpp`, which covers canonical ID rendering/parsing, every ref operation kind, and ref log and snapshot validation."}
{"cluster_id": "chunk_003-138", "topic": "design-history", "canonical_claim": "The CAS stabilization iteration excludes large `CasGc.cpp` and `Cas::Store` structural refactors, new abstractions or configuration knobs without consumers, pre-emptive generality, and behavior changes justified only by taste."}
{"cluster_id": "chunk_003-139", "topic": "design-history", "canonical_claim": "The CAS fork uses three footprint rings: Ring 0 covers `ContentAddressed/` and `Core/`, Ring 1 covers CA-specific append-only lines in shared files, and Ring 2 covers shared upstream code that may be changed only when the delta shrinks, neutralizes, fixes, or simplifies the carried fork diff."}
{"cluster_id": "chunk_003-140", "topic": "design-history", "canonical_claim": "The `skip_access_check` fix reads the per-disk configuration directive in the CA factory; global `skip_access_check` plumbing through shared `IDisk::startupImpl` and generic `DiskSelector` isolation remain deferred Ring-2 work."}
{"cluster_id": "chunk_003-141", "topic": "design-history", "canonical_claim": "The stale `checkContentAddressedDiskRestrictions` no-op is removed from shared `MergeTreeData` because deleting it reduces the Ring-1 CA footprint rather than adding shared surface."}
{"cluster_id": "chunk_003-142", "topic": "design-history", "canonical_claim": "C5 is the only medium-risk behavioral change: it is a pure encapsulation of borrowed projection transactions, lands last with focused merge/mutate review, and can be reverted independently."}
{"cluster_id": "chunk_003-143", "topic": "design-history", "canonical_claim": "Removing `root_shards` and `fence_seq` is accepted because CAS is pre-release with no compatibility requirement; the affected pool-meta and GC-state codecs are covered by format tests and must have no remaining live consumers."}
{"cluster_id": "chunk_003-144", "topic": "design-history", "canonical_claim": "Changing run-file CRC coverage to include the block head, i.e. widening CRC coverage, changes the on-disk run-file framing; this is acceptable pre-release because run files are attempt-scoped and short-lived, old-format run files are never expected to survive, and writer and reader must be updated together."}
{"cluster_id": "chunk_003-145", "topic": "design-history", "canonical_claim": "The decommission design does not build a durable roster, does not auto-decommission inactive members, does not modify ReplicatedMergeTree ZooKeeper metadata, and provides no FORCE, per-namespace, or dry-run variant."}
{"cluster_id": "chunk_003-146", "topic": "design-history", "canonical_claim": "The rev.6 proposal specified a default `materialization_grace_ms` of 30000 ms for unclean handovers and no wait for a drained clean release."}
{"cluster_id": "chunk_003-147", "topic": "design-history", "canonical_claim": "The approved CAS codecs v3 design makes `docs/superpowers/cas/codecs_proposal_v3.md` the normative format reference and supersedes `codecs_proposal_v2.md`; `codecs.md` becomes a historical audit after `Formats/README.md` exists."}
{"cluster_id": "chunk_003-148", "topic": "design-history", "canonical_claim": "The refsnaplog conversion is a pre-release hard cutover with no legacy-binary-to-text dual-read fallback."}
{"cluster_id": "chunk_003-149", "topic": "design-history", "canonical_claim": "The blob-meta cutover deletes the `CAMT` binary codec in one pre-release commit and performs no binary-to-text dual read."}
{"cluster_id": "chunk_003-150", "topic": "design-history", "canonical_claim": "The streaming checksum changes the checksum value from the former one-shot `cityHash128(run_bytes)` algorithm because `object_cap = 0` forbids materializing a run for hashing."}
{"cluster_id": "chunk_003-151", "topic": "design-history", "canonical_claim": "The envelope uses a codec-local ref escaper that leaves `/` unchanged, escapes quotes, backslashes, and control characters, and makes byte-budget arithmetic independent of global JSON settings."}
{"cluster_id": "chunk_003-152", "topic": "design-history", "canonical_claim": "The source-layout refactoring includes the final renames `Cas::Store` → `Cas::Pool` and `Cas::Build` → `Cas::PartWriteTxn`, after layout and decomposition phases."}
{"cluster_id": "chunk_003-153", "topic": "design-history", "canonical_claim": "The refactoring fixes both dependency-direction violations rather than documenting around them; the backend-to-`CasTextFormat` violation disappears when v3 codecs own `sealObject` and `openObject`, so no `SealingBackend` decorator is planned."}
{"cluster_id": "chunk_003-154", "topic": "design-history", "canonical_claim": "The remaining post-v3 direction violation is `CasEvent.h` including the envelope header for `toEventKind`; the mapper is to move into `Formats/CasBlobEnvelopeFormat.h` so `CasEvent` can become dependency-free."}
{"cluster_id": "chunk_003-155", "topic": "design-history", "canonical_claim": "The refactoring merges files rather than classes, keeps type names and APIs unchanged during the merge phase, and places types/enums before codecs before logic in merged files."}
{"cluster_id": "chunk_003-156", "topic": "design-history", "canonical_claim": "Per-object `Formats/` files are deliberately not merged because their one-object-per-file organization is a settled codecs-v3 decision."}
{"cluster_id": "chunk_003-157", "topic": "design-history", "canonical_claim": "The revised one-pipeline design supersedes the staged-intent, subclass, and two-phase disk-transaction variants for `[TXN-ONE-PIPELINE]`."}
{"cluster_id": "chunk_003-158", "topic": "design-history", "canonical_claim": "If `NETWORK_ERROR` later proves too coarse, the documented escape hatch is a dedicated `CONTENT_ADDRESSED_WRITE_RETRY_LATER` error code, with optional additions to `checkDataPart::isRetryableException` and HTTP mapping."}
{"cluster_id": "chunk_003-159", "topic": "design-history", "canonical_claim": "DDL committed-ref drops, moves, and RENAME TABLE operations are gated for explicit design-owner confirmation because deferring them to commit requires a `pending_ref_ops` overlay and new read-path behavior; the motivation bugs are in the part-build path."}
{"cluster_id": "chunk_003-160", "topic": "design-history", "canonical_claim": "The #37 design selected existing `NETWORK_ERROR` through one `throwCasWriteRetryLater` helper, renewed while the lease deadline remained valid, and added an honest rate-limited Warning plus an S3-fault regression and fsck verification."}
{"cluster_id": "chunk_003-161", "topic": "design-history", "canonical_claim": "Same-pool `CA↔CA` moves are deferred for verification rather than special-cased; `CA→local` moves and insert-time policy routing are explicitly out of scope because they already work through a plain `DiskLocal` destination."}
{"cluster_id": "chunk_003-162", "topic": "design-history", "canonical_claim": "Same-pool `CA↔CA` movement is deferred because the target should deduplicate existing pool content, but it remains unverified whether the target final ref `<part>` collides with the source ref `<part>`."}
{"cluster_id": "chunk_003-163", "topic": "design-history", "canonical_claim": "The COW overlay is scoped to `RefTableState::committed`; `precommits` remains `std::set`, only `committed` changes, the base remains `std::map` in this cut, the codec remains based on `std::vector<RefCommittedRow>`, and an optional future `absl::btree_map` follow-up is allowed. It uses a sorted `std::map` because ordered merge iteration is required; a persistent immutable ordered tree is deferred because no drop-in implementation exists in the tree."}
{"cluster_id": "chunk_003-164", "topic": "design-history", "canonical_claim": "The triage fix wave applies confirmed real fixes with fail-closed behavior, while deferring the six user design decisions №14, №15, №17, №29, №30, and №31."}
{"cluster_id": "chunk_003-165", "topic": "design-history", "canonical_claim": "The CAS review left CityHash128 without byte verification, local/NFS shared-pool visibility, unknown GCS bucket versioning, the experimental gate for `metadata_type=content_addressed`, `system.content_addressed_mounts` listing errors, and `staging_backend=s3` fallback as open design decisions rather than silent fixes."}
{"cluster_id": "chunk_003-166", "topic": "design-history", "canonical_claim": "The accepted CasJsonWriter outcome is the measured 2.26× speedup plus exhaustive byte-identity verification; the memcpy floor remains as a documented benchmark reference, not a pass/fail gate."}
{"cluster_id": "chunk_003-167", "topic": "design-history", "canonical_claim": "Runtime reuse of the same CAS disk after a stop is intentionally dropped; tests use unique names, and decommission is handled by configuration plus restart or `SYSTEM CONTENT ADDRESSED FORGET`."}
{"cluster_id": "chunk_003-168", "topic": "design-history", "canonical_claim": "The rev.8 design permanently removes automatic erasure conclusion: a genuinely erased pool remains `IdentityLost` until an operator invokes `FORGET` or recreates it."}
{"cluster_id": "chunk_003-169", "topic": "design-history", "canonical_claim": "The rev.7 lifecycle plan rolls back the landed Dormant/UNMOUNT reuse lifecycle after switching tests to `DROP SYNC → FORGET → verify → rm -rf`; the abort hardening, coherent `poolAccess()` snapshot, atomic `startup()`, and `ca-fsck` rename are retained."}
{"cluster_id": "chunk_003-170", "topic": "design-history", "canonical_claim": "Stage 1 is limited to CAS-internal changes plus minimal pool server wiring, with no behavioral seam in `MergeTree` or generic-Disks code, and validates parallel blob upload within a part; cross-part `commitPart` concurrency/dispatch is deferred to stage 2 as the larger cross-part upload-concurrency opportunity."}
{"cluster_id": "chunk_003-171", "topic": "design-history", "canonical_claim": "The stage-1 A1 refinement costs one cheap COW copy per ref-log chunk and deliberately keeps the existing post-install in-place O(overlay) fold path; materializing the candidate before the PUT would create an O(n)-per-chunk regression."}
{"cluster_id": "chunk_003-172", "topic": "design-history", "canonical_claim": "The decomposition principle is to split decision logic from I/O and locking: pure functions operate on explicit state, while I/O and lock-holding remain in thin shells."}
{"cluster_id": "chunk_003-173", "topic": "design-history", "canonical_claim": "The TLA+ phase is a pre-C++ gate: every new invariant must be observed red under a dedicated `_sab_*` configuration before its green configuration is trusted."}
{"cluster_id": "chunk_003-174", "topic": "design-history", "canonical_claim": "The register's 2026-07-31 ruling for `_ckpt.life_epoch` was to reject decreases and let a higher monotone value win, because a lower contribution indicates a fenced-out writer whose contribution landed."}
{"cluster_id": "chunk_003-175", "topic": "design-history", "canonical_claim": "The ref-chain v9 TLA+ phase does not model the temporal lemma's SOURCE-BACKED/TOKENED adoption, TOKENLESS relink, condemned-meta rematerialization, or tokenless relink ordering arms, because these are properties of existing shipped code; they are recorded as C++-test obligations/scope notes rather than duplicated in TLA+ and are not a modeling gap."}
{"cluster_id": "chunk_003-176", "topic": "design-history", "canonical_claim": "The stopped relink model is historical; relink work must consume the replacement `Ready`-only certification contract from `2026-07-30-cas-ref-lane-state-machine.md` and the `CaRefLaneCore.tla`/`CaRelinkLaneComposition.tla` models."}
{"cluster_id": "chunk_003-177", "topic": "design-history", "canonical_claim": "A safety model keeps code-retained over-approximations, but an under-approximation is a defect; a state made unreachable by a guard must be recorded as a structural exclusion rather than claimed by an invariant."}
{"cluster_id": "chunk_003-178", "topic": "design-history", "canonical_claim": "The per-ref MutationScope index was retired before shipping; the chosen approach is re-offer confirm, because the index would add protocol complexity only to be deleted later and the observed busy-lane problem is availability/waste rather than correctness."}
{"cluster_id": "chunk_003-179", "topic": "design-history", "canonical_claim": "The previous `RefApplyState`, durable-floor bookkeeping, and separate wedge/apply-marker encodings are removed from the persistent lane state; request and occupant enums remain local computation results rather than persistent states."}
{"cluster_id": "chunk_003-180", "topic": "design-history", "canonical_claim": "The retired CAS lifecycle concepts `RemovalReady`, independent name-keyed cleanup cursors/maps, fictitious ref shard zero, permanent `Retired` catalog rows, `_cleanup`-driven removal handshakes, and `nsc` cursor grammar must not be reintroduced."}
{"cluster_id": "chunk_003-181", "topic": "design-history", "canonical_claim": "CAS subsystem commits are ordered bottom-up as `Primitives` → `Formats` → `Backend` → `Pool` → `Parts` → `Gc` → `Tools` → `benchmarks`, followed by integration, tests, CI/tags, and documentation."}
{"cluster_id": "chunk_003-182", "topic": "design-history", "canonical_claim": "The verification phase mechanically treats `history` claims routed to `none` as `ephemeral` or `unverifiable` because they cannot be checked against implementation code."}
{"cluster_id": "chunk_003-183", "topic": "design-history", "canonical_claim": "`manifests-and-refs.md` and `garbage-collection.md` receive strict HEAD verification because the ref/GC subsystem changed after the walkthrough, including the v5 invariant reset, contiguous streams, `_ckpt`, catalog, and Stage A/B work."}
{"cluster_id": "chunk_003-184", "topic": "design-history", "canonical_claim": "The full historical narrative remains in Git history for a future blog post, while the public `architecture/design-history.md` is a condensed version of `how-we-got-here.md`."}
{"cluster_id": "chunk_003-185", "topic": "design-history", "canonical_claim": "The naming effort does not add compatibility aliases for old SQL, metadata type, or wire-protocol names because the branch is unreleased."}
{"cluster_id": "chunk_003-186", "topic": "design-history", "canonical_claim": "The consolidation's second-pass audit found that mapping multi-part disk transactions to `03 §phase-upload (multipart)` and transaction machinery to `03 §mutable-vs-immutable` were incorrect; both belong in `03 §transactions-mvcc`."}
{"cluster_id": "chunk_003-187", "topic": "design-history", "canonical_claim": "The first coverage pass treated the transactional-commit machinery as covered, but the second-pass audit identified it as a significant load-bearing gap along with projection representation, committed projection listing, `pool_uuid` relink identity, fresh-fetch detached landing, and the per-part freeze rationale; all were later filled additively."}
{"cluster_id": "chunk_003-188", "topic": "design-history", "canonical_claim": "The cleanup simplification removed vestigial `trees/` and `ObjectKind::Tree`, the `_disk` namespace, the registry, and `Placement::Pack`, and unified configuration keys; the R1–R14 refactor task list is intentionally dropped as process history."}
{"cluster_id": "chunk_003-189", "topic": "design-history", "canonical_claim": "The relink seam depends only on the small `Ready` certification contract; the reference lane is modeled from semantic obligations rather than reconstructed from nested implementation decisions."}
{"cluster_id": "chunk_003-190", "topic": "design-history", "canonical_claim": "`CaRefNsCleanupStaleLeaderCore` deliberately excludes the adopted-parent/fresh-cut protocol, local catalog deletion evidence and holds, and synchronous physical cleanup from namespace removal."}
{"cluster_id": "chunk_003-191", "topic": "design-history", "canonical_claim": "The v9 regression pair treats enumeration as a zero-trust hint, advances fold work by `cursor + 1`, and requires per-namespace frontier proof for destructive work; `CaRefTableSnapshotLogCore_sab_scanistruth` remains red while `CaRefDeltaIntakeCore_v9_hintomission` is green."}
{"cluster_id": "chunk_003-192", "topic": "design-history", "canonical_claim": "The historical `_sab_holeylist` red is retained as a pre-v9 shipped-GC defect witness and must not be interpreted as a v9 implementation failure."}
{"cluster_id": "chunk_003-193", "topic": "design-history", "canonical_claim": "Generic MergeTree, Replicated, and Keeper code must not learn CAS concepts; every accepted exception to that minimal upstream footprint must retain its reasoning."}
{"cluster_id": "chunk_003-194", "topic": "design-history", "canonical_claim": "An additive format change introduces a tolerant key without bumping `v`, while a breaking change bumps `v`, records `changePoints`, writes down to the pool floor, and relies on the raised floor to fence old builds out."}
{"cluster_id": "chunk_003-195", "topic": "design-history", "canonical_claim": "The `ca-arch` agent must name the failure asymmetry of each architectural option before recommending one."}
{"cluster_id": "chunk_003-196", "topic": "design-history", "canonical_claim": "An architectural mechanism claim is incomplete until it predicts a minimal experiment and the experiment is run."}
{"cluster_id": "chunk_003-197", "topic": "design-history", "canonical_claim": "The `ca-arch` agent must refuse to choose when evidence is missing and state what evidence is needed."}
{"cluster_id": "chunk_003-198", "topic": "design-history", "canonical_claim": "An architectural conclusion must state what it does not cover."}
{"cluster_id": "chunk_003-199", "topic": "design-history", "canonical_claim": "The transaction wiring suite uses the collision-free name `CasTransactionWiring` to match the `CasTransaction*` test family."}
{"cluster_id": "chunk_003-200", "topic": "design-history", "canonical_claim": "The consistency review regards the parallel `Cas::Backend` seam as defensible for a narrowed token-aware contract and test isolation, but recommends documenting that rationale and considering whether decorators or fakes could reduce the duplicated operation surface."}
{"cluster_id": "chunk_003-201", "topic": "design-history", "canonical_claim": "The main CAS TLA+ models need changes if a later cache version persists metadata, serves manifest bytes after restart without remote validation, uses cached views as write evidence, or exposes cached state to GC decisions."}
{"cluster_id": "chunk_003-202", "topic": "design-history", "canonical_claim": "The Stage B review unit/Task 10 should split `listedTok` auditing, per-model-family driver expectations, runnerless-model decisions, and classifier-only phase reruns into independent units/tasks with separate commits and before-and-after evidence."}
{"cluster_id": "chunk_003-203", "topic": "design-history", "canonical_claim": "RunFile and PartManifest should rename format_version to compatibility_version at the next incompatible break or add a separate local format_version while reserving compatibility_version for G_BUILD gating"}
{"cluster_id": "chunk_003-204", "topic": "design-history", "canonical_claim": "Unused FormatId::Roster, unused RunKind values, TreeId without a live type boundary, and distributed sharded-GC scaffolding should be removed or demoted unless current producers and consumers exist"}
{"cluster_id": "chunk_003-205", "topic": "design-history", "canonical_claim": "v3 rejects base64 for inline manifest bytes because it blinds mostly-text sidecars and moves payload into the descriptor; inline payload remains a separate raw zone"}
{"cluster_id": "chunk_003-206", "topic": "design-history", "canonical_claim": "The inheritance of DataPartStorageContentAddressed is unresolved among overriding DataPartStorageOnDiskBase, extracting reusable mechanics, implementing IDataPartStorage directly, or incrementally wrapping Full behavior."}
{"cluster_id": "chunk_003-207", "topic": "design-history", "canonical_claim": "A future IDataPartStorageRoot would be a substantially larger upstream redesign touching startup discovery, reconciliation, storage-policy disks, detached/shadow enumeration, and replicated expected-part logic, and is not implied by the current concern."}
{"cluster_id": "chunk_003-208", "topic": "design-history", "canonical_claim": "Before TXN-ONE-PIPELINE becomes an implementation plan, a spike must determine whether a part-storage boundary removes enough shared patches and routing complexity to justify its larger implementation."}
{"cluster_id": "chunk_003-209", "topic": "design-history", "canonical_claim": "Non-executing CAS findings such as comments, documentation, reports, and commit-message claims are batchable prose work, while behavior defects remain in ordinary fix rounds."}
{"cluster_id": "chunk_003-210", "topic": "design-history", "canonical_claim": "CAS code comments must not cite branch-local plans, specifications, BACKLOG entries, review rounds, finding IDs, or task numbers; when surrounding code already expresses a rule, deleting the comment is preferred."}
{"cluster_id": "chunk_003-211", "topic": "design-history", "canonical_claim": "The highest-value low-risk refactoring pair is centralizing backend token policy and splitting `CasGc.cpp`; both are intended to preserve storage format and external behavior."}
{"cluster_id": "chunk_003-212", "topic": "design-history", "canonical_claim": "CAS metric names were normalized from the `Cas` prefix to `CAS`, while compare-and-swap tail identifiers were renamed to use `CompareSwap`."}
{"cluster_id": "chunk_003-213", "topic": "design-history", "canonical_claim": "The test-only namespace fixture seam is `DB::Cas::tests::fixture`, exposing `fixtureLife`, `admitLive`, and `writeRefLogRaw`; test migration maps `NamespaceLifeId::stageATransition(ns)` to `fixture::fixtureLife(ns)`, `casAdmitEntry` to `fixture::admitLive`, and `writeRefLogTxnRaw` to `fixture::writeRefLogRaw`."}
{"cluster_id": "chunk_003-214", "topic": "design-history", "canonical_claim": "The per-table snapshot-publish backoff driven by `RefTableRuntime::publish_backoff_ms` and `publish_backoff_until_ms` is distinct from the request controller's per-attempt retry backoff in `CasRequestControllerBackoff`."}
{"cluster_id": "chunk_003-215", "topic": "design-history", "canonical_claim": "The strings `CasGcIsLeader` and `CasGcPendingReclaimEntries` remain in two sites because they refer to retired process-global gauges, and changing them would invent a spelling for gauges that no longer exist."}
{"cluster_id": "chunk_003-216", "topic": "design-history", "canonical_claim": "prev_indeg, dropped_by, and cursor are not emitted CasEvent detail keys; the system.cas_log detail-key documentation therefore uses the emitted keys condemn_round, superseded_token, code, and site as examples instead of renaming nonexistent keys."}
{"cluster_id": "chunk_003-217", "topic": "design-history", "canonical_claim": "The Uncertain writer-duty transfer is implemented in PartWriteTxn::~PartWriteTxn, not enqueueWriterCleanupDuty; enqueueWriterCleanupDuty has no Uncertain branch and unconditionally builds and queues the duty."}
{"cluster_id": "chunk_003-218", "topic": "design-history", "canonical_claim": "CAS gtest suite renaming must change only the first argument of TEST, TEST_F, and TEST_P for colliding plain suites; all 62 production-symbol collisions are plain suites, while the two fixture suites do not collide and can be renamed with scoped whole-word edits."}
{"cluster_id": "chunk_003-219", "topic": "design-history", "canonical_claim": "The runtime proof is intentionally bounded to two runtime identities and one lane; it does not add unbounded retired-runtime history or change C++."}
{"cluster_id": "chunk_003-220", "topic": "design-history", "canonical_claim": "`CaRefDeltaIntakeCore` remains a pure downstream consumer of walk-plan key-set and fold semantics and does not own lifecycle protocol or fresh-cut provenance."}
{"cluster_id": "chunk_003-221", "topic": "design-history", "canonical_claim": "Snapshot codec fields and `RefLifecycle::Removed` are outside the Step 9 A/B catalog-cut slice and are owned by Step 9 slice C."}
{"cluster_id": "chunk_003-222", "topic": "design-history", "canonical_claim": "`CasRefWireVocab` and protocol contracts must not describe the retired snapshot-terminal surface; shared `RefTxnId` writer and validator helpers remain, and the retired namespace-cleanup marker and `Removed`-snapshot handshake are a zero-survivor audit target with no executable producer, consumer, DTO collection, or positive test in production."}
{"cluster_id": "chunk_003-223", "topic": "design-history", "canonical_claim": "The TLA+ model treats blocking joins as enabled-when conditions with weak fairness, proving absence of deadlock or livelock but not real-time join duration."}
{"cluster_id": "chunk_003-224", "topic": "design-history", "canonical_claim": "Probe A's two blind spots were a hole reproduced identically in both enumerations and a namespace dropped wholesale from one enumeration; the sampled Probe A mechanism was later superseded and deleted: `Gc::sampleRefListQuality`, `PoolConfig::gc_probe_a_period`, the `ref_list_probe` phase row, and all `CasGcProbeA*` counters were deleted, leaving one `cas/refs/` enumeration per fold round; Task T5 deleted Probe A rather than widening its witness rule."}
{"cluster_id": "chunk_003-225", "topic": "design-history", "canonical_claim": "The pre-CAS MergeTree disk-transaction contract was used as a batch of file operations rather than as one transaction for creating a logical part, allowing projections, clone/freeze, and mutable per-part files to use multiple independent transactions or per-file autocommit operations."}
{"cluster_id": "chunk_003-226", "topic": "design-history", "canonical_claim": "The non-CAS value of the transaction fixes is clarification of the `DataPartStorage`/`IDiskTransaction` responsibility boundary and removal of the implicit assumption that every sub-part can be safely committed independently."}
{"cluster_id": "chunk_003-227", "topic": "design-history", "canonical_claim": "Non-CA-gated changes to shared paths require separate upstream sign-off and regression coverage: `MergeTreeData::Transaction::renameParts` now commits each part's disk-storage transaction before the Keeper commit decision, the S3 client globally stops retrying HTTP 412, and the `ReadBufferFromFileView` fix changes the packed skip-index reader."}
{"cluster_id": "chunk_003-228", "topic": "design-history", "canonical_claim": "The `[TXN-ONE-PIPELINE]` refactoring removes patches caused by eager/deferred dispatch, the `moveDirectory` rename-window publish, read-your-writes gaps, and commit-position workarounds by introducing a `ContentAddressedDiskTransaction` and a two-phase `precommit`/`commit` contract."}
{"cluster_id": "chunk_003-229", "topic": "design-history", "canonical_claim": "Class-A patches are concentrated in `Disks/DiskObjectStorage/DiskObjectStorageTransaction.cpp` and `Storages/MergeTree/MergeTreeData.cpp`; the remaining inventory is classified as legitimate CAS integration or independently removable code."}
{"cluster_id": "chunk_003-230", "topic": "design-history", "canonical_claim": "The dangling-precommit TLA+ gate adds constant `SabotageSkipParksDeadPrecommit` and liveness property `LiveDeadPrecommitReclaimed` to `CaGcRootLocalPartManifestCore.tla`; the bug config must violate the property (a watermark-dead live precommit's manifest body stutters present forever under Skip) and the fix config must hold it."}
{"cluster_id": "chunk_003-231", "topic": "design-history", "canonical_claim": "The `RefCowMap` COW-overlay design for `RefTableState::committed` reuses the same immutable-base-plus-delta pattern Keeper already uses for `KeeperStorage::UncommittedState` (`Delta`/`applyDelta`/`rollback` over a committed `Container`), specialized to an ordered map."}
{"cluster_id": "chunk_003-232", "topic": "design-history", "canonical_claim": "In the `RefTableState` experiment matrix, E3 (transaction-layer instead of scratch copy) composes with E2 (COW owned-manifest index) or E4 (flat sorted-vector base) since a transaction layer works over either base, and E1 (relaxed replay) reduces the weight of what E2 must speed up; the comparison table is expected to judge combinations, not only single experiments."}
{"cluster_id": "chunk_003-233", "topic": "design-history", "canonical_claim": "The `FrontierUnproven::NoCatalogEntry` and `AppendAboveFrozenTail` buckets were removed outright (not retained with a comment) once they became unfireable-by-construction, because a zero-only bucket cannot change the invariant `sum(buckets) == frontier_namespaces - frontier_proven`, and retaining a dead enumerator would falsely imply a live exit path to a reader."}
{"cluster_id": "chunk_003-234", "topic": "design-history", "canonical_claim": "`WalkTarget::frozen_tail` was deleted as an unread field (written at three `push_back` sites, read nowhere) once the frozen-tail break arm it supported was removed as dead code, since keeping the field with prose claiming it bounds the round would have been a false statement about the code."}
{"cluster_id": "chunk_003-235", "topic": "design-history", "canonical_claim": "The GC in-degree model replaced a bare integer refcount with a source-edge multiset (`+`/`-` deltas keyed by `(kind, hash, source_manifest)`); the fold computes net in-degree from the edge multiset, and the `BlobInDegreeMatchesActiveManifests` TLA+ invariant asserts the multiset exactly equals the active manifests referencing the blob, turning the `+`-not-yet-durable race into a pending-fold problem rather than a guard problem."}
{"cluster_id": "chunk_003-236", "topic": "design-history", "canonical_claim": "The attempt-scoped-generation fix moved all decision-bearing round artifacts, including `retired` and `outcomes`, under `gc/gen/<g>/attempt/<a>/…` rather than the narrower original proposal of attempt-scoping only the four `gc/gen/<gen>/…` write-once artifacts, because `retired` is a writer-facing publish-gate input that `RetireView::refresh` LISTs and writers consult on the commit path, so a stale retired set from a deposed leader under its own `(round, fence_seq)` could otherwise influence live writers."}
{"cluster_id": "chunk_003-237", "topic": "design-history", "canonical_claim": "The B167 watermark models established that `build_seq` must be allocated from a monotone counter, not merely a unique one: a non-monotone allocation lets the active-set floor (`min_active`) be pulled back below a finished build's sequence, re-protecting an already-condemned blob (a leak); this monotone-`build_seq` floor lemma survives in shipped code for precommit-ref reclaim (`Gc::prefixEligible` / the `BuildPrefix` watermark floor in `CasGc.cpp`), even though the blob-protection use case it was built for was replaced."}
{"cluster_id": "chunk_003-238", "topic": "design-history", "canonical_claim": "CAS treats a plan as a hypothesis about how to proceed, not an authority: plans have been wrong in every round (a field placement that silently rebound positional initialisers, a prescribed proof that proved nothing, a step whose own authorization forbade it), so the person executing a plan is expected to notice when it is wrong and say so rather than execute it as written."}
{"cluster_id": "chunk_003-239", "topic": "design-history", "canonical_claim": "Mid-way through the rev.7 disk-lifecycle plan the user ordered a scope cut to a FORGET-only v1: the natural-erasure proof stack (~1500-2000 lines) was excised entirely (erasure is asserted, never machine-proven), documented as spec rev.8 with a 10-item excision list; TLA+ Model 1 (natural-erasure proof) was dropped while Model 2 (lifecycle/FORGET) remained the gating model, and the excision itself removed a net 1095 lines with a clean adversarial review."}
{"cluster_id": "chunk_003-240", "topic": "design-history", "canonical_claim": "E1 (relaxed replay of RefTableState) shipped as KEEP: the un-materialized overlay makes per-transaction scratch copies O(overlay) instead of O(N), reducing replay cost by 22-25% and strengthening the case for E3."}
{"cluster_id": "chunk_003-241", "topic": "design-history", "canonical_claim": "E2 (owned-manifest index for RefTableState) shipped as KEEP achieving flat O(1) BM_AdmitsAddPrecommit (~712-726 ns), at the honest cost of ScratchCopy +13 ns and BM_ReplayHistory regressing to 50,082 ns/row because the index overlay grows along the never-materializing replay tail; E3 was tasked to recover it."}
{"cluster_id": "chunk_003-242", "topic": "design-history", "canonical_claim": "E3 shipped as the simplified variant with no undo journal: `TrustedHistory` apply-mode does in-place mutation with poison-on-throw (replay-only, poison provably contained at all 3 callers), while `Full` keeps the verbatim two-phase copy; this cut BM_ReplayHistory from 50,082 to 1,725.58 ns/row (-96.6%, ~29x) with writer benches flat."}
{"cluster_id": "chunk_003-243", "topic": "design-history", "canonical_claim": "E5 (mutate-in-place-when-uniquely-owned materialize) shipped as KEEP, cutting unique-owner materialize from O(N) to O(overlay) for both COW containers."}
{"cluster_id": "chunk_003-244", "topic": "design-history", "canonical_claim": "The reviewer's (Mikhail) final disposition of the review findings: most \"findings\" and comments were judged not real or by-design, with a small set treated as genuine and prioritized: (1) GCR-1 rebuild mount-lease interlock — real, add the interlock; (2) `Build::promote` overwrite leak — the most concrete storage leak, fix by failing closed or repointing; (3) lost-ACK idempotency (W-N1/W-N2/J5) — reconcile by reading post-state instead of blindly replaying; (4) J1/J2/J3 writer-lease-is-not-a-fencing-token — add a SIGSTOP/SIGCONT test first, then re-check `mayMutate` immediately before the shard CAS as a short-term mitigation, `writer_epoch`-in-precondition as the longer-term fix; (5) G-N1 persistent clamp — keep fail-closed but add observability/operator escape. RPL-3 was judged safe in practice because the sender cannot remove a part until it receives an ack, which the receiver gives only after a successful write."}
{"cluster_id": "chunk_003-245", "topic": "design-history", "canonical_claim": "Reviewer disposition: the security finding labeled \"SEC-2 - no intra-pool authz\" in the reviewer's reply (matching the audit's SEC-3, no intra-pool authorization) is accepted as by-design: all bucket-credential holders share the same trust boundary. RPL-2 (ZK/CAS ref divergence on partial commit) is judged not a CAS-specific issue but general ReplicatedMergeTree partial-commit behavior that the project already knows how to resolve (via detached-parts handling). RPL-4 (`to_detached` relink disabled, always streams bytes) is accepted as a possible real performance issue to recheck, believed to use the same mechanics as a normal fetch. RPL-5 (no dedicated CAS integration tests for quorum/SYNC REPLICA/cloneReplica) is accepted as coverage debt comparable to other ClickHouse features, mitigated by all stateless tests passing successfully against a CAS-backed server."}
{"cluster_id": "chunk_003-246", "topic": "design-history", "canonical_claim": "The follow-up triage's \"Close As No-Action / By Design\" bucket: G-N3 (spurious ABORTED on lost-ACK round CAS) is a false positive because the next GC round reads the already-committed `gc/state`; cross-shard serializability findings (fractured read, read skew, phantoms) are accepted as by-design; and RPL-2 (ZK/CAS ref divergence) should follow ReplicatedMergeTree's existing detached-part/recovery semantics rather than invent CAS-only cross-registry transactionality."}
{"cluster_id": "chunk_003-247", "topic": "design-history", "canonical_claim": "The Stage B strategic review's concentration proposal, in dependency order: (1) a `FencedCatalogWriter` primitive constructed once per mount from `(admitted_generation, check_fence_or_throw)` that owns `casUpdate`/`casAdmitEntry`/`publishCkpt` dispatch and re-checks the fence inside every CAS attempt, replacing the two threaded fence parameters at every call site; (2) collapse namespace-life resolution to three named entry points — `lifeIfCataloged` (read), `resolveNamespaceLife` (the sole writer-mint path after the R12 fix), `liveUniverse` (GC/fsck/sweep/decommission) — retiring `resolveLifeOrSentinel`; (3) make removal ordering (prune GC-held state before entry delete) the load-bearing invariant so incarnation-scoped cursor re-keying and half the coexistence tests become unnecessary."}
{"cluster_id": "chunk_003-248", "topic": "design-history", "canonical_claim": "The standalone `tree` `ObjectKind` was excised on 2026-07-03; `ObjectKind` is now Blob-only, and `toEventKind` only handles `Blob`."}
{"cluster_id": "chunk_003-249", "topic": "design-history", "canonical_claim": "Lesson recorded from the deposed-leader stray-Clean investigation: a formal model that is safer than the code hides real bugs. The original `CaRetiredInRun` TLA gate did not model the deposed leader and stayed green; a first attempted fix made the model advisory-add-only and also went green, but this was a false-green that masked the code's actual behavior (`clearSparedMeta` really does clear-to-Clean pre-CAS) until the BLOCKED verdict and a not-weaken-invariants discipline caught the discrepancy."}
{"cluster_id": "chunk_003-250", "topic": "design-history", "canonical_claim": "The CAS merge-layout preparation's top identified risk (R1) is that an extraction changes lock discipline: the publish lane's correctness depends on the two-phase carve happening under one continuous lock hold and the install region not allocating; the mitigation is to extract only pure functions called from the same place under the same lock (so discipline is unchanged by construction) and to gate on a soak run, not just gtests."}
{"cluster_id": "chunk_003-251", "topic": "design-history", "canonical_claim": "The CAS merge-layout preparation's R2 risk is `Pool` facade churn, since B6 touches the class everything depends on; the mitigation is to do that extraction last and keep `Pool` as a delegating facade so call sites are untouched."}
{"cluster_id": "chunk_003-252", "topic": "design-history", "canonical_claim": "The CAS merge-layout preparation's R3 risk is that `docs/superpowers/cas/11-walkthrough.md` cites roughly 200 file:line references that workstream B invalidates; the mitigation is to refresh the walkthrough as the final step of workstream B, before carving, since it is the document reviewers read first."}
{"cluster_id": "chunk_003-253", "topic": "design-history", "canonical_claim": "The CAS merge-layout preparation's R4 risk is that workstream A3 may be reshaped in upstream review after it has already landed in the fork; the mitigation is to keep A3's fork commits identical to what is proposed upstream and treat upstream feedback as a follow-up to both."}
{"cluster_id": "chunk_003-254", "topic": "design-history", "canonical_claim": "A codex review of the publish-confirm/ref-lane-safety landing raised three footprint objections against the upstream-coupling-minimization rule; two were knowingly accepted as tradeoffs and recorded as accepted (not debt) rather than corrected: `DataPartsExchange.h` exposing `allow_ca_relink` and CAS-specific service helpers beyond the opaque enum, and the confirm/relink logic spanning more functions per side than the rule's ideal shape."}
{"cluster_id": "chunk_003-255", "topic": "design-history", "canonical_claim": "The publish-confirm/ref-lane-safety design deliberately uses two distinct types for the same confirm answer across the exchange boundary — `ConfirmAnswer` in the ledger and `CasConfirmAnswer` in the exchange wire header, and `PreparedPartWrite` (CAS-internal) exposed through the abstract `ICaPreparedRelink` — so that `DataPartsExchange.cpp` needs no CAS-internal headers, keeping the one type mapping confined to a single implementation file."}
{"cluster_id": "chunk_003-256", "topic": "design-history", "canonical_claim": "The branch-reconstruction carve script accepts, as a residual risk, that a file moved between source areas (e.g. a gtest moved into a layer directory) silently changes which commit carries it; this is accepted because the commit grouping is presentational only."}
{"cluster_id": "chunk_003-257", "topic": "design-history", "canonical_claim": "The ref-table snapshot-log Phase 1 plan has zero deliberate deviations from its spec; Phase 2 items are excluded per the plan's Global Constraints, not silently dropped."}
{"cluster_id": "chunk_003-258", "topic": "design-history", "canonical_claim": "Codex triage finding №18 was not given its own task because the root fix in Task 2 collapses it; the affected list-branch change is called out explicitly inside Task 2 rather than as a separate item."}
{"cluster_id": "chunk_003-259", "topic": "design-history", "canonical_claim": "The relink/seam TLA+ model deliberately introduces three controls the spec does not name (_ctl_v11nomarker, _ctl_distinctns, _ctl_skipidentity) because a flip with no control would be a coincidence and a green with no scoping statement would be a silent gap; it also models the full wedge lifecycle (hazards and happy path) beyond what section 12 of the spec asks for, because three drafts proved that modelling rule 3 without the wedge lifecycle produced sabotages that were red for unreachable states and greens resting on missing ones."}
{"cluster_id": "chunk_003-260", "topic": "design-history", "canonical_claim": "The current CAS code is the survivor of eras E0 through E4, and the C++ implementation progressed through an E0 reachability PoC, an E1 `Cas::Store` core rewrite, an E2 journal/epoch GC design, an E3 root-local part-manifest GC redesign, and an E4 ack-floor, freshness-meta, and ref snapshot-plus-log design; it ends in writer freshness metadata and the ref snapshot+log model, and `RootShardManifest` was removed on 2026-07-12."}
{"cluster_id": "chunk_003-261", "topic": "design-history", "canonical_claim": "The D6 write-ahead lease-intent mechanism was removed because it created data-proportional persistent Keeper traffic and collided across writers building identical content."}
{"cluster_id": "chunk_003-262", "topic": "design-history", "canonical_claim": "The standalone Merkle `treeId` design and `CasTreeCodec` are not implemented in the current code because the rev.15 `PartManifest` redesign replaced the standalone Tree object."}
{"cluster_id": "chunk_003-263", "topic": "design-history", "canonical_claim": "The two disabled protocol scenario tests for subtree adoption are obsolete because the tree model was removed."}
{"cluster_id": "chunk_003-264", "topic": "design-history", "canonical_claim": "`CaIncarnationCore.tla` is the architecture-independent safety spine, but its manifest-embedded journal and fence structure is superseded by ref-log objects, source-edge runs, fold seals, and heartbeat-floor fencing in shipped code."}
{"cluster_id": "chunk_003-265", "topic": "design-history", "canonical_claim": "The writer-heartbeat `min_ack` half of `CaGcAckFloorCore.tla` is superseded by v3 freshness metadata, while its GC-round rebuild and clamp-suppression portions remain current."}
{"cluster_id": "chunk_003-266", "topic": "design-history", "canonical_claim": "The current code trusts tokenless `adoptEvidence` leaves through durable manifest edges rather than performing the model's K3 presence HEAD and condemned check."}
{"cluster_id": "chunk_003-267", "topic": "design-history", "canonical_claim": "The original EBR/epoch GC model `CaGcCore.tla` was removed as superseded by the incarnation-token design."}
{"cluster_id": "chunk_003-268", "topic": "design-history", "canonical_claim": "The first CAS feature commit was `33d1b55b3e9`, dated 2026-06-02."}
{"cluster_id": "chunk_003-269", "topic": "design-history", "canonical_claim": "The E2 GC design used per-shard epoch compaction with a `gc/log` and `gc/snap` delta journal, build heartbeats, and a roots `_registry`; almost all of this design was later deleted."}
{"cluster_id": "chunk_003-270", "topic": "design-history", "canonical_claim": "The `Cas*` core versus `ContentAddressed*` frontend naming split began at the E1 swap commit `7133ba900b1`."}
{"cluster_id": "chunk_003-271", "topic": "design-history", "canonical_claim": "The retired-list-to-snapshot-run transition landed in `37813b4ea75` as the seven-task retired-in-snapshot series T1–T7."}
{"cluster_id": "chunk_003-272", "topic": "design-history", "canonical_claim": "The walkthrough records that older documentation describing mutable `RefSidecar`, a five-phase GC round, binary `CABL`/`CATR` envelopes, protobuf manifests, root shards, or a `mutable_files` sidecar has drifted from the current implementation."}
{"cluster_id": "chunk_003-273", "topic": "design-history", "canonical_claim": "The D1 design is partly superseded: registry removal and `LIST(cas/refs/)` discovery landed as D1, while the `RootShard` incarnation, incarnation-keyed cursor, and newborn-root-shard portions became moot after replacing the mutable root-shard model with per-table snapshot-plus-log state."}
{"cluster_id": "chunk_003-274", "topic": "design-history", "canonical_claim": "The D1 phase-0 model checked `INV_NO_DANGLING` and `INV_NO_ORPHAN_EDGE` across 724,944 distinct states, and its result was that `THM-NO-RETURN` holds without the registry; three sabotage controls demonstrated the need for the newborn floor, incarnation cursor, and fold-before-reclaim ordering."}
{"cluster_id": "chunk_003-275", "topic": "design-history", "canonical_claim": "The condemned-evidence copy-forward proposal is approved and explicitly constrained to the CA layer; it makes no `ReplicatedMergeTree` changes."}
{"cluster_id": "chunk_003-276", "topic": "design-history", "canonical_claim": "The implementation notes corrected the corruption outcome: a corrupt copy-forward payload throws `CORRUPTED_DATA`, while a 404 or lost race remains `ABORTED`."}
{"cluster_id": "chunk_003-277", "topic": "design-history", "canonical_claim": "The `FreshEvidenceDepWithViewHitIsResolvedByGate` test's post-fix meaning is promote success through displacement, with the condemned listed token never bound, rather than an `ABORTED` result."}
{"cluster_id": "chunk_003-278", "topic": "design-history", "canonical_claim": "The ack-floor redesign replaces the per-round all-shard fence R3, the fold-through-fence recheck R4, and the separate per-server watermark object."}
{"cluster_id": "chunk_003-279", "topic": "design-history", "canonical_claim": "The landed streaming implementation makes a large-footer run a fourth request instead of three: a tail probe that does not cover the footer triggers one exact-footer ranged GET, while the resident-memory bound is unchanged."}
{"cluster_id": "chunk_003-280", "topic": "design-history", "canonical_claim": "The flat-combining `Store::mutateShard` queue is obsolete because the per-table snapshot+log reference protocol replaced the root-shard mutation path; intra-server serialization is now the per-table `CasSingleWriterSlot` ref-append lane, and the `CasShard*` ProfileEvents were deleted during the 2026-07-12 stabilization iteration."}
{"cluster_id": "chunk_003-281", "topic": "design-history", "canonical_claim": "The GCS Generation binding was implemented with a GOOG4 signer, wire-boundary conditional dialect, `http_client = gcs_hmac`, `TokenType::Generation`, single-PUT guard, and fail-closed probe versioning; a live cycle passed probe, replication, reclaim, fsck, and logged 100 deletes with zero errors."}
{"cluster_id": "chunk_003-282", "topic": "design-history", "canonical_claim": "The shard-mutation queue implementation completed with 200,675 mutations compressed into 87,594 `casPut` operations, a 2.3x compression ratio, and only 11 intra-server reference conflicts; `CasOtherCas` equaled flushes."}
{"cluster_id": "chunk_003-283", "topic": "design-history", "canonical_claim": "The shard-mutation-queue documentation plan was intended to close the B157 contention axis by recording the queue design, conflict/rewrite budget, and completed roadmap status."}
{"cluster_id": "chunk_003-284", "topic": "design-history", "canonical_claim": "The CAS night-campaign fix plan approved an instrument-first approach with four independent work packages; phases 2–4 do not depend on phase 1, although phase-2 audit events improve phase-3.1 diagnosis."}
{"cluster_id": "chunk_003-285", "topic": "design-history", "canonical_claim": "The approved 2026-07-08 design spec supersedes the RFC's open questions; the RFC remains motivation and context, while the spec is normative."}
{"cluster_id": "chunk_003-286", "topic": "design-history", "canonical_claim": "Before the redesign, `ContentAddressedMetadataStorage` repeated route, ref resolution, manifest read, and path lookup across seven read methods; `readManifest` copied the cached manifest by value and performed a manifest-key `HEAD` on every call."}
{"cluster_id": "chunk_003-287", "topic": "design-history", "canonical_claim": "Before the redesign, `Store::lookupPath` and the manifest overload of `Store::listDirectory` were linear scans, while the encoder sorted entries but the decoder checked only adjacent duplicates and did not enforce ordering."}
{"cluster_id": "chunk_003-288", "topic": "design-history", "canonical_claim": "The tokened promote-condemn race was caused by GC condemning freshly put blobs before the precommit-to-blob edge had been folded, and the tokened fix re-uploaded from retained writer sources after the owner check."}
{"cluster_id": "chunk_003-289", "topic": "design-history", "canonical_claim": "The tokenless promote-condemn race occurred when the pre-refresh copy-forward pre-pass missed a condemnation revealed by the in-closure refresh; an in-closure copy-forward backstop closed that gap."}
{"cluster_id": "chunk_003-290", "topic": "design-history", "canonical_claim": "The raw-body and terminal-tombstone variants were rejected because removing the in-body incarnation tag makes the body etag content-derived and prevents exact-token deletion from missing a resurrected body."}
{"cluster_id": "chunk_003-291", "topic": "design-history", "canonical_claim": "Per-incarnation blob keys were rejected because they require a 404-to-LIST generation-resolution path, propagate incarnation into manifests, and need additional floors or pins."}
{"cluster_id": "chunk_003-292", "topic": "design-history", "canonical_claim": "The freshness-meta v3 direction keeps the in-body tag and uses per-hash metadata only as a writer freshness point-read, not as the delete linearizer."}
{"cluster_id": "chunk_003-293", "topic": "design-history", "canonical_claim": "Phase A migrates tokened promote tests from source resurrection to success with the token unchanged, while retaining the abandoned-precommit owner-check abort and tokenless/no-dep fail-closed tests."}
{"cluster_id": "chunk_003-294", "topic": "design-history", "canonical_claim": "The 2026-07-10 raw-body and terminal-tombstone CAS plan was superseded and rejected because it recreated the rejected generation-in-key design; the replacement named by the plan is `2026-07-10-cas-freshness-meta-v3.md`."}
{"cluster_id": "chunk_003-295", "topic": "design-history", "canonical_claim": "The 2026-07-10 CAS ref-log handoff design is obsolete and was superseded by `2026-07-11-cas-ref-table-snapshot-log-design.md` rev.5, which landed as Phase 1; rev.6 is a lease-boundary exclusivity proposal."}
{"cluster_id": "chunk_003-296", "topic": "design-history", "canonical_claim": "The obsolete ref-log design explicitly rejects a mutable `_head`, a `_snap` service area, a second ref adoption CAS, GC-generated writer records, cross-namespace atomic rename, and compatibility with unreleased `RootShardManifest` or decimal manifest-key layouts."}
{"cluster_id": "chunk_003-297", "topic": "design-history", "canonical_claim": "The retired-in-snapshot plan requires `05008_ca_gc_snap_prune` to remain unmodified because it is the settlement-semantics oracle for the refactor."}
{"cluster_id": "chunk_003-298", "topic": "design-history", "canonical_claim": "`CaB140DangleMerge.tla` remains the minimal historical counterexample for trimming before adoption and is not changed by the retired-in-snapshot refactor."}
{"cluster_id": "chunk_003-299", "topic": "design-history", "canonical_claim": "The retired-in-snapshot design explicitly scopes its no-divergence guarantee per attempt: pre-CAS `.meta` and event effects from a deposed leader may remain after an unadopted attempt, as in the prior implementation."}
{"cluster_id": "chunk_003-300", "topic": "design-history", "canonical_claim": "The deposed-leader stray-Clean defect was fixed by removing the spare-side clear; the recorded code gate passed the full `Cas*` battery at 542/542 and the TLA gate reported an honest green model with `post_adoption_clear` red."}
{"cluster_id": "chunk_003-301", "topic": "design-history", "canonical_claim": "The unattended campaign recorded that the S3-staging implementation was opt-in, capability-probed, and validated on RustFS after an end-to-end test caught missing `CABL` envelope bytes that in-memory tests did not expose."}
{"cluster_id": "chunk_003-302", "topic": "design-history", "canonical_claim": "The unattended campaign recorded Phase 2 SHA-256 support as a completed variable-length digest implementation with real-S3 validation of `blobs/sha256/<64hex>`, exact-token reclamation, and `dangling==0`."}
{"cluster_id": "chunk_003-303", "topic": "design-history", "canonical_claim": "The unattended campaign recorded the retired-in-snapshot refactor as soak-validated with `dangling==0` at every phase-3 checkpoint and the add-only deposed-leader fix as TLA-gated with `post_adoption_clear` red."}
{"cluster_id": "chunk_003-304", "topic": "design-history", "canonical_claim": "The coverage audit compared baseline `a39032d51fa` with HEAD `ea180d83d58` on branch `cas-gc-rebuild`, covering 28 test files with 2,946 additions and 3,559 deletions."}
{"cluster_id": "chunk_003-305", "topic": "design-history", "canonical_claim": "The pool-member decommission design is marked IMPLEMENTED on 2026-07-15, with tasks 1–6 landed in commits `03b3b95de44` through `e375fafa5e0` plus review-fix commits `1b5a7f5faf3`, `3d641996a1a`, and `51ff6879864`; the integration test and docs landed separately."}
{"cluster_id": "chunk_003-306", "topic": "design-history", "canonical_claim": "The rev.6 `_latedelivery` TLA configuration is GREEN at `MaxSeq = 5`, but that result is an abstraction-limit finding: the model freezes ordinary writes during reader recovery and cannot express the real system's in-flight `T_mat` transient, so it is not proof that the real transient cannot occur."}
{"cluster_id": "chunk_003-307", "topic": "design-history", "canonical_claim": "The rev.6 proposal was superseded by the approved rev.6 design because sealing at the greatest listed transaction ID was insufficient."}
{"cluster_id": "chunk_003-308", "topic": "design-history", "canonical_claim": "The unattended campaign abandoned the Round-B §5 absence-means-Clean optimization after its spare-clear transition was found unsafe; the user abandoned §5, no unsafe §5 code landed, and the shipped optimization round retained/shipped §§1–4 and §6."}
{"cluster_id": "chunk_003-309", "topic": "design-history", "canonical_claim": "Commit `8fe6331a431` widened the fetch-by-relink residual by removing promote-time `copyForwardFromCondemned`, which had displaced condemned content to a fresh token."}
{"cluster_id": "chunk_003-310", "topic": "design-history", "canonical_claim": "The all-tree change deletes the `DiskObjectStorageTransaction` eager dispatch hook for content-addressed mutable-part-file renames, identified as the B182 hook."}
{"cluster_id": "chunk_003-311", "topic": "design-history", "canonical_claim": "The corrected v3 inventory records that root shard `CARS` no longer exists because ref state is refsnaplog, retired set `CART` was removed on 2026-07-10, and the blob-meta sidecar had been omitted from earlier inventories."}
{"cluster_id": "chunk_003-312", "topic": "design-history", "canonical_claim": "`cas_blob_meta` was never a protobuf object; it used the `CAMT` fixed binary format, so its migration has no protobuf-graveyard work."}
{"cluster_id": "chunk_003-313", "topic": "design-history", "canonical_claim": "`Core/CasRunFile.{h,cpp}` remains after the `cas_run` cutover because `CasManifestCodec` still embeds a `RunKind::ManifestEntries` `CARN` stream; phase 6 owns and then deletes `Core/CasRunFile.{h,cpp}` after the part-manifest cutover because that embedded manifest stream is its last remaining `CARN` user."}
{"cluster_id": "chunk_003-314", "topic": "design-history", "canonical_claim": "The fetch-handoff retention-pin design was superseded on 2026-07-23 by a publish-confirm design in which the receiver confirms the sender's committed ref before promoting; the superseding design removes the pool-global pin and GC changes."}
{"cluster_id": "chunk_003-315", "topic": "design-history", "canonical_claim": "The detached/relink cluster recorded `B66b` as extending same-pool relink into `to_detached` and `B66a` as a separate local-backend atomicity issue for shared detached refs; both were orthogonal to the pin mechanism."}
{"cluster_id": "chunk_003-316", "topic": "design-history", "canonical_claim": "Until the pin or publish-confirm mechanism lands, the fetch-by-relink tail was explicitly accepted and documented; a bespoke relink handshake was not to be built in the interim."}
{"cluster_id": "chunk_003-317", "topic": "design-history", "canonical_claim": "The one-pipeline design removes B151 early publication and `rename_published_refs`, while B183 temporary text-index state must remain represented in the overlay so a scratch ref cannot overwrite the authoritative final manifest."}
{"cluster_id": "chunk_003-318", "topic": "design-history", "canonical_claim": "The original claim that CAS INSERT survives an object-store fault while CAS MERGE reruns the whole merge was refuted: both operations fail with the same CAS `Code 236 ABORTED` when the mount lease is lost."}
{"cluster_id": "chunk_003-319", "topic": "design-history", "canonical_claim": "The codecs-v3 migration exposed a cross-translation-unit ODR violation because old and new implementations defined identical `DB::Cas` codec functions; the safe resolution was an atomic cutover deleting `CasManifestCodec.{h,cpp}` before trusted shape tests."}
{"cluster_id": "chunk_003-320", "topic": "design-history", "canonical_claim": "The source-layout mechanical phase created the layered tree, completed eight merges, moved 107 files to 89, and reached a green CA battery of 905 tests before hand C++ decomposition."}
{"cluster_id": "chunk_003-321", "topic": "design-history", "canonical_claim": "Two independent consults validated three complementary CAS-side defects for #37: over-fencing on transient renewal errors, `ABORTED` defeating merge backoff, and opaque retry-later observability; they found no data-loss or recovery-wedge defect."}
{"cluster_id": "chunk_003-322", "topic": "design-history", "canonical_claim": "The F3-1a adoption-of-a-corrupt-seal tolerance finding is moot after formats v3 because an undecodable foreign seal now fails recovery closed via the zstd frame check."}
{"cluster_id": "chunk_003-323", "topic": "design-history", "canonical_claim": "`39cf3279652` removed CA publish-at-rename and moved publication into `ContentAddressedTransaction::commit`, creating the CA split-commit window; R3 changed the resulting ambiguity into a client-visible retry that amplified silent loss."}
{"cluster_id": "chunk_003-324", "topic": "design-history", "canonical_claim": "Every prior green S36 and S37 run was at `dev` scale; the two failures in `RUN_HISTORY.md` were the first runs at `ci` scale."}
{"cluster_id": "chunk_003-325", "topic": "design-history", "canonical_claim": "The T5 tombstone design supersedes only the owner half of the earlier exact-token tail-fencing recommendation; mount and epoch objects continue using exact-token fencing because their values naturally change between incarnations."}
{"cluster_id": "chunk_003-326", "topic": "design-history", "canonical_claim": "S36 and S37 passed in the #38 campaign after routing fix `4d457ec378a`, confirming that their prior failures were card scale-parameter errors rather than a product routing regression."}
{"cluster_id": "chunk_003-327", "topic": "design-history", "canonical_claim": "The only existing retry-on-touch path identified for a failed async table load is the opt-in `lazy_load_tables` mechanism via `StorageTableProxy` (PR #96283, 26.2)."}
{"cluster_id": "chunk_003-328", "topic": "design-history", "canonical_claim": "The mount-lease lifecycle plan records Tasks 4 through 8 as historical landed work scheduled for rollback by the later living disk-lease-loss design, while the renewal and terminate abort-hardening in Parts 1a and 1b remains."}
{"cluster_id": "chunk_003-329", "topic": "design-history", "canonical_claim": "`2026-07-22-cas-batched-part-commit-v2-design.md` is superseded by the stage-1 internal design and a future stage-2 sink-concurrency design; it must not be implemented as written."}
{"cluster_id": "chunk_003-330", "topic": "design-history", "canonical_claim": "`2026-07-22-cas-multicommit-phased-design.md` is superseded by the batched-part-commit v2 design and must not be implemented because its seam ownership, per-item result, leadership-exit, rollback, byte-admission, and topology assumptions were rejected."}
{"cluster_id": "chunk_003-331", "topic": "design-history", "canonical_claim": "The bounded-concurrency per-part design was approved after two independent concurrency reviews, with prior blocking findings closed, but it targets the inner `ContentAddressedTransaction` commit path."}
{"cluster_id": "chunk_003-332", "topic": "design-history", "canonical_claim": "The CAS write-path stage-1 effort targets the backlog item reporting CAS-on-S3 INSERT as approximately 7.6× slower than standard S3."}
{"cluster_id": "chunk_003-333", "topic": "design-history", "canonical_claim": "The original same-uuid/same-epoch self-race proposal is superseded by the v2 fence-not-rescue design because its claim that the pair proves the writer's own prior write was refuted by the absent-epoch re-mint hole."}
{"cluster_id": "chunk_003-334", "topic": "design-history", "canonical_claim": "The two earlier STID 3982-3b48 fixes replaced ordinary vanished or absent-at-release `LOGICAL_ERROR` paths with non-aborting exceptions, establishing the classifier-hardening pattern used by the later fix."}
{"cluster_id": "chunk_003-335", "topic": "design-history", "canonical_claim": "The force-claim CI motivation was removed by a one-line configuration-scrape fix, while force-claim remains a separate operator need; the relevant CI scrape runs after server termination and the force-claim tasks were intentionally left unstarted."}
{"cluster_id": "chunk_003-336", "topic": "design-history", "canonical_claim": "The ref-chain design resurrects the project's I7 contiguity idea from 2026-07-10 and closes the documented `Late Predecessor PUT` limitation from rev.4 by spending the `_ckpt` GET/CAS pair required for bounded crash recovery."}
{"cluster_id": "chunk_003-337", "topic": "design-history", "canonical_claim": "Stage B owns catalog and incarnation work (`INV-3`), while Stage A remains functional using today's unqualified namespace key shape and discovery mechanisms."}
{"cluster_id": "chunk_003-338", "topic": "design-history", "canonical_claim": "The Stage B catalog plan is superseded by `docs/superpowers/plans/2026-08-02-cas-stage-b-remaining.md`, which is the execution authority for remaining Stage-B work."}
{"cluster_id": "chunk_003-339", "topic": "design-history", "canonical_claim": "The TLA writer/recovery model retired its rev.4 and rev.6 configurations because v9 semantics superseded them, and `LatePredecessorPut` changed from expected-fail to a green case under the new seal and arithmetic-walk rules."}
{"cluster_id": "chunk_003-340", "topic": "design-history", "canonical_claim": "The lease-blip `INVALID_STATE` class was judged pre-existing: the archive contained 62 occurrences dated `2026.07.26` across 33 distinct parts before the Task 15/16 merge, and the CA throw site was introduced by commit `21d207734095` on 2026-07-23."}
{"cluster_id": "chunk_003-341", "topic": "design-history", "canonical_claim": "`CaErasureProof` models a full-prefix empty-`LIST` as part of its subject, but the natural `Vanished(erased)` promotion it once guarded was removed by lifecycle v1 and the mount-time LIST probe is outside v9 scope."}
{"cluster_id": "chunk_003-342", "topic": "design-history", "canonical_claim": "`CaRelinkConfirmCore_sab_holeylist` remains the permanent regression witness for the historical incomplete-page defect: one incomplete page with all confirm rules intact violates `ConfirmedRelinkNeverDangles`."}
{"cluster_id": "chunk_003-343", "topic": "design-history", "canonical_claim": "The original relink TLA+ gate plan was stopped because its cardinality product treated nested decisions as independent alternatives; six `WedgeResolution` values, three `SlotOccupyResult::Kind` values, four occupant classes, seven `Reason` values, and two exception exits do not prove a complete set of terminal paths."}
{"cluster_id": "chunk_003-344", "topic": "design-history", "canonical_claim": "The incremental survey compared the authoritative tracked module inventory with the results table, excluded generated `*_TTrace_*.tla` artifacts, and confirmed that `CaRefPreFoldDrainCore` and `CaRefPreFoldDrainAllRowsCore` use fixed in-module entity universes and only Boolean sabotage constants."}
{"cluster_id": "chunk_003-345", "topic": "design-history", "canonical_claim": "`CasRefCatalog` currently has no entry-deletion API, so comments describing a normal removal as an already-existing deletion path are future obligations rather than present behavior."}
{"cluster_id": "chunk_003-346", "topic": "design-history", "canonical_claim": "The captured LIST-incompleteness evidence establishes that a multi-page enumeration can omit durable objects, but no LIST violation has been observed on AWS S3; the incident is evidence about RustFS and legal enumeration concurrency, does not identify the store-side mechanism, and does not establish the behavior on AWS S3."}
{"cluster_id": "chunk_003-347", "topic": "design-history", "canonical_claim": "The Stage B strategic review found that catalog authority and incarnation protect different failure classes: the catalog closes hidden-universe ownership loss, while incarnation makes stale physical debris and deposed-writer writes inert across removal and rebirth."}
{"cluster_id": "chunk_003-348", "topic": "design-history", "canonical_claim": "The observed LIST omission was within a single enumeration call rather than explainable solely by cross-page snapshot isolation; its recency bound and RustFS mechanism remain unknown."}
{"cluster_id": "chunk_003-349", "topic": "design-history", "canonical_claim": "The earlier claim that same-name refusal would burn stateless CI lanes was retracted: under `Atomic`, drop-and-recreate uses a fresh table UUID and does not collide with the old namespace."}
{"cluster_id": "chunk_003-350", "topic": "design-history", "canonical_claim": "Stock `RESTORE` generates a fresh coordinated UUID, so it is not an exact-`RootNamespace` reuse route; shadow-name reuse and explicit-UUID replay remain supported reuse routes."}
{"cluster_id": "chunk_003-351", "topic": "design-history", "canonical_claim": "`enumerateRefPrefix` has three callers—`listRefPrefix`, the detector, and REBUILD—and probe A deletion removes only the detector caller while retaining the helper for REBUILD."}
{"cluster_id": "chunk_003-352", "topic": "design-history", "canonical_claim": "Three `known-model-error UnchangedCompositeVars` rows—`sab_crosssharddisplacement`, `sab_reducerownsfence`, and `stage5_sharding`—leave the model's Phase-4 sharding arm unproven and must remain explicitly recorded as `KNOWN` model debt."}
{"cluster_id": "chunk_003-353", "topic": "design-history", "canonical_claim": "The remaining-work plan supersedes the 2026-07-28 CAS catalog plan and is self-contained; the old plan and session handoff are historical provenance rather than execution authorities."}
{"cluster_id": "chunk_003-354", "topic": "design-history", "canonical_claim": "At baseline `ce312f547c3`, the namespace-file handle contract, hot-path zero-catalog behavior, ref cleanup token/fence races, Task 7 implementation, Task 8 production slices, and the `PENDING` gauge fix were already landed, while the ref-side life handle, list-arm pin, sentinel retirement, Task 6b tests, Task 7 closure evidence, Task 8 closure, Tasks 7a/7b, Task 10 closure, and Tasks 11/12 remained missing."}
{"cluster_id": "chunk_003-355", "topic": "design-history", "canonical_claim": "`CasRefCatalog::resolveLifeOrSentinel` still returns `NamespaceLifeId::stageATransition(ns)` when no catalog life exists, but all 17 call sites identified in the audit are under `src/Disks/tests/` and there are zero production callers."}
{"cluster_id": "chunk_003-356", "topic": "design-history", "canonical_claim": "The method name `tryPublishSnapshotAndAdvanceCheckpointOnce` is present in `CasRefLedger`, `CasPool`, and their implementations; commit `9d92c84ee37` renamed `trySnapshotPublishOnce` to `tryPublishSnapshotAndAdvanceCheckpointOnce` under the exact required commit subject, keeping the two durable effects in one retry unit, while the dedicated ordering-coverage file was still absent."}
{"cluster_id": "chunk_003-357", "topic": "design-history", "canonical_claim": "The stateless `PENDING` double-count was corrected in `04290_content_addressed_no_leftovers.sh` and `04295_content_addressed_mutation_no_leftovers.sh` by reading `pending_condemned` alone; the correction is recorded under commit `8e9b06c2a81`."}
{"cluster_id": "chunk_003-358", "topic": "design-history", "canonical_claim": "The Stage B document audit found that `Task 6` was partly landed despite being described as not started: two namespace-file contract tests were committed, while the ref-side contract, optionalisation of `resolveLifeOrSentinel`, snapshot seam, fixture seam, and sentinel retirement remained open."}
{"cluster_id": "chunk_003-359", "topic": "design-history", "canonical_claim": "The coverage matrix records that all second-pass gaps were filled additively in the consolidated docs, while the transactions-design iterative tail was intentionally excluded because it was a testing observation rather than a documented invariant."}
{"cluster_id": "chunk_003-360", "topic": "design-history", "canonical_claim": "The audited `cas-gc-rebuild` branch had no model/code safety risk: whenever a model diverged from the shipped mechanism, the code preserved the same safety conclusion through an equal-or-stronger mechanism."}
{"cluster_id": "chunk_003-361", "topic": "design-history", "canonical_claim": "The CAS suite normalization renamed 21 test suites and three parameterized instantiation prefixes so their exposed spellings begin with `Cas`."}
{"cluster_id": "chunk_003-362", "topic": "design-history", "canonical_claim": "The historical part_manifest_cleanup CARN run design is removed: current post-CAS manifest deletion uses the in-memory folded.mf_cleanup map and orphan sweep scans cas/manifests/ directly"}
{"cluster_id": "chunk_003-363", "topic": "design-history", "canonical_claim": "v3 deletes cas_format.proto and protobuf build dependencies, CARN blocks and footer seek machinery, the binary envelope codec and TLV parser, refsnaplog binary codecs, the heartbeat fixed-record codec, the part-manifest cleanup run, and most of CasInspect"}
{"cluster_id": "chunk_003-364", "topic": "design-history", "canonical_claim": "The previous design treated ref IDs as sparse because next_ref_sequence was pool-wide, rejected writes burned safe gaps, and replay validated strict increase rather than contiguity."}
{"cluster_id": "chunk_003-365", "topic": "design-history", "canonical_claim": "The CAS metric registry contained 165 `Cas*` metric names before the naming change: 156 from `ProfileEvents.cpp` and 9 from `CurrentMetrics.cpp`."}
{"cluster_id": "chunk_003-366", "topic": "design-history", "canonical_claim": "The live event is `CASRefNeedsRecovery`, emitted by `CasRefLedger::requireRecovery`; `CasRefApplyPoisoned` was never present in `src/Common/ProfileEvents.cpp` or `CurrentMetrics.cpp`."}
{"cluster_id": "chunk_003-367", "topic": "design-history", "canonical_claim": "`CasRefRecoverySealPublished` was absent from the registry and was inferred, for collection-only tooling, to correspond to `CASRefRecoveryEpochSealed`; the mapping was not code-stated and did not affect a card verdict."}
{"cluster_id": "chunk_003-368", "topic": "design-history", "canonical_claim": "Dated soak-history records retain stale names such as `CasRefApplyPoisoned` when they quote what an earlier run actually reported; historical evidence is not rewritten to current registry spelling."}
{"cluster_id": "chunk_003-369", "topic": "design-history", "canonical_claim": "The `readable_catalog_after_observation_hook_for_test` hook is used by three `RefWriterRuntimeIdentity` tests to inject catalog changes between the cold arm's two observations; it is not unused."}
{"cluster_id": "chunk_003-370", "topic": "design-history", "canonical_claim": "Production `CasRefCatalog::resolveLifeOrSentinel` and `NamespaceLifeId::stageATransition` were deleted, and source greps found no remaining non-documentation references to either name."}
{"cluster_id": "chunk_003-371", "topic": "design-history", "canonical_claim": "LIST_OF_CONTENT_ADDRESSED_SETTINGS and CONTENT_ADDRESSED_SETTINGS_SUPPORTED_TYPES remain as internal C++ macros, and the tests/clickhouse-test and tests/config/install.sh occurrences are intentionally deferred to Task 5."}
{"cluster_id": "chunk_003-372", "topic": "design-history", "canonical_claim": "`PoolConfig::gc_probe_a_period` was a `PoolConfig` field only: it had no `DECLARE` entry in `ContentAddressedSettings.cpp`, no XML or DDL binding, and was read from `store->poolConfig().gc_probe_a_period` only by test fixtures."}
{"cluster_id": "chunk_003-373", "topic": "design-history", "canonical_claim": "`GReclaimDeadPrecommit` and `LiveDeadPrecommitReclaimed` are now inert model artifacts because GC-side abandoned-precommit reclaim was removed; the production writer appends the exact `owner_transition` removal instead."}
{"cluster_id": "chunk_003-374", "topic": "design-history", "canonical_claim": "The Slice C implementation was based at commit `c13597c9d8348f87c408293c0fa394518cedfbf6` and its combined work was later represented by commits `224aacd8eb96f6da6e0b2457dad1b09ac563d3d5` and `6c0a5453bf72350e56022f3b6bf2dad4bdd21d5f`."}
{"cluster_id": "chunk_003-375", "topic": "design-history", "canonical_claim": "Historical documents retain the retired marker identifiers and `_cleanup` terminology as dated plans, reviews, telemetry, superseded specifications, or explicit deletion rationale rather than live API documentation."}
{"cluster_id": "chunk_003-376", "topic": "design-history", "canonical_claim": "The historical erasure-proof model found [D1] grace load-bearing: without it, a timed-out zombie request can land after the second empty sample and falsify the claim that a pool prefix is empty."}
{"cluster_id": "chunk_003-377", "topic": "design-history", "canonical_claim": "The first disk-transaction commit was `b557c1dd80db` on 2022-06-15 13:51 +0200 by alesapin, with subject `Interm stage`; it introduced `src/Disks/IDiskTransaction.h`, `src/Disks/FakeDiskTransaction.h`, `src/Disks/ObjectStorages/DiskObjectStorageTransaction.cpp`, and initial MergeTree integration through `src/Storages/MergeTree/IMergeTreeDataPart.cpp`; follow-up commits from 2022-06-16 through 2022-06-30 made the initial disk-transaction series workable, including `9a891543524` with subject `Fix incorrect order of operations in disk transactions`."}
{"cluster_id": "chunk_004-001", "topic": "statement that plain S3 did not break was misleading", "canonical_claim": "The statement that plain S3 did not break was misleading: plain S3 had a silent in-memory projection omission, while only the local-disk `Fake` transaction made hardlinked directories immediately visible."}
{"cluster_id": "chunk_004-002", "topic": "GC design moved from an epoch-based reclamation (EBR) core that encoded generati", "canonical_claim": "The GC design moved from an epoch-based reclamation (EBR) core that encoded generation in the object key (`blobs/<H>/<g>`) and required a `404→LIST` degraded read path, durable per-hash floors, and Keeper-fenced `safe_epoch`, to an incarnation model that moves the resurrection marker into the object body and deletion precision into the backend token, eliminating generation from any key and removing the `404→LIST` path entirely."}
{"cluster_id": "chunk_004-003", "topic": "2026-06-19 baseline determination against the pre-refactor binary found 68 uniqu", "canonical_claim": "A 2026-06-19 baseline determination against the pre-refactor binary found 68 unique failures in the `transactions` and `text-index` test families identically reproduced on the unrefactored binary, confirming they were pre-existing CA gaps (B182, B183) rather than regressions, so the refactor was declared regression-clean without any code change."}
{"cluster_id": "chunk_004-004", "topic": "`CaIncarnationProofCore", "canonical_claim": "`CaIncarnationProofCore.tla` used Apalache 0.58.0 to verify an inductive invariant `IndInv` (19 conjuncts) for a pre-B91, token-only fragment of the GC core: base case (`Init => IndInv`) and step check (`IndInv => IndInv'`) both held, which is stronger than TLC bounded checking because the step check quantifies over all states satisfying `IndInv` at fixed bounds regardless of trace depth; the model was removed 2026-07-22 as stale and unverifiable in an environment without an Apalache binary."}
{"cluster_id": "chunk_004-005", "topic": "`CaGcIndegRefoldCore", "canonical_claim": "`CaGcIndegRefoldCore.tla` modeled a bug (H1b) where an in-degree completion-seal cursor persisted at `foldCursor` instead of `max(foldCursor, fenceVersion)` caused a re-fold of an already-absorbed fence-window removal, driving the integer in-degree counter to -1 (`INV_INDEG_NONNEGATIVE` violated, mapping to `CORRUPTED_DATA` in C++); the design decision was that the `Seal` action must advance the persisted cursor to `fenceVersion`, not remain at `foldCursor`."}
{"cluster_id": "chunk_004-006", "topic": "`CaGcIndegRefoldCore", "canonical_claim": "`CaGcIndegRefoldCore.tla` was removed 2026-07-22 because the shipped in-degree design no longer accumulates a non-idempotent integer delta stream: `CasBlobInDegree.cpp` computes in-degree via an idempotent two-cursor presence-set merge keyed by `(blob_hash, source_id)` where the surviving-edge count is an unsigned integer that structurally cannot go negative, so the underflow hazard the model existed to catch is now structurally impossible."}
{"cluster_id": "chunk_004-007", "topic": "Three B167 build-watermark models (`CaResurrectLiveness", "canonical_claim": "Three B167 build-watermark models (`CaResurrectLiveness.tla`, `CaBuildWatermark.tla`, `CaBuildWatermarkNum.tla`), all removed 2026-07-21, proved that resurrect liveness requires a guard blocking GC from re-condemning a freshly-owned incarnation during the non-atomic upload-to-publish span (the B167 livelock: dedup hit on a stale incarnation is deleted, the fresh re-upload is re-condemned by GC without a guard, looping forever); their safety role fully migrated into `CaBuildRootPrecommit.tla`'s precommit-first reachability mechanism, which replaced the modeled per-candidate blob-guard and condemn-time `HeartbeatGuard` that were never fully implemented."}
{"cluster_id": "chunk_004-008", "topic": "codex_r6_findings", "canonical_claim": "codex_r6_findings.md's round-5 disposition table maps all 10 round-5 review findings against the v6 ref-chain redesign: fully resolved (later definite result after ambiguity; clean-transition sealing), resolved for safety only (REBUILD condemnation; cross-epoch removals), partially resolved (recovery snapshot authority; one enumeration/falsifiability), unresolved (namespace incarnation; current-epoch manifest bodies; catalog partial transitions/capacity), and resolved for the original mismatch only (CAS-walk controller mismatch) — each disposition cites which of the 13 new v6 findings covers the remaining gap. Overall verdict: REJECT."}
{"cluster_id": "chunk_004-009", "topic": "External bug report Altinity/ClickHouse#2052 (orphan-manifest sweep deletes live", "canonical_claim": "External bug report Altinity/ClickHouse#2052 (orphan-manifest sweep deletes live manifests) does not reproduce on HEAD: the reporter's build was ~330 commits behind and predated commit `318291fe5e5`, which switched the orphan-manifest sweep's ownership recovery onto `recoverRefTable` (the ref-log model) rather than the older `recoverRefTableDetailed` path."}
{"cluster_id": "chunk_004-010", "topic": "reftable COW-map plan's self-review found the actual access-site inventory for `", "canonical_claim": "The reftable COW-map plan's self-review found the actual access-site inventory for `RefTableState::committed` was 18 sites across 5 files (verified against the tree), not the ~15 sites the spec estimated; all but one site was a drop-in change."}
{"cluster_id": "chunk_004-011", "topic": "full-scale P0 campaign found six O(N) cost families", "canonical_claim": "The full-scale P0 campaign found six O(N) cost families: GC round O(pool objects), scratch equal to full part size, replicated `OPTIMIZE` double-spill, wide-part O(columns), partitioned INSERT O(partitions), and startup O(refs)."}
{"cluster_id": "chunk_004-012", "topic": "rev", "canonical_claim": "The rev.5 five-second grace floor was measured to cause approximately 20–26 GB/hour of ref snapshot traffic per node in the cited soak."}
{"cluster_id": "chunk_004-013", "topic": "In the measured 500-partition profile, `ReplicatedMergeTreeSink", "canonical_claim": "In the measured 500-partition profile, `ReplicatedMergeTreeSink::finishDelayed` commits partitions serially, the ref-ledger batch size is 1.0, and `CasRefBatchFlushes` equals `CasRefBatchedMutations` at 1026."}
{"cluster_id": "chunk_004-014", "topic": "S41 wide-insert baseline found CAS-on-S3 at 3", "canonical_claim": "The S41 wide-insert baseline found CAS-on-S3 at 3.0× slower than plain S3, with 87.2% of Real time in S3 network operations, 12.6% in the HEAD-before-PUT gate, and about 77 blob PUTs per part on the wide profile."}
{"cluster_id": "chunk_004-015", "topic": "wide S41 CA INSERT fell from 58", "canonical_claim": "The wide S41 CA INSERT fell from 58.41 seconds before stage 1 to 30.26 seconds after stage 1, a 48% reduction and 1.93x speedup; plain S3 remained 19.44 versus 19.05 seconds."}
{"cluster_id": "chunk_004-016", "topic": "S41 stage-1 workload produced 500 CA parts and 3", "canonical_claim": "The S41 stage-1 workload produced 500 CA parts and 3.53 GiB of logical bytes in both runs, while the CA-vs-plain factor fell from 3.0x to 1.59x."}
{"cluster_id": "chunk_004-017", "topic": "wide S41 profile averaged approximately 80 blob upload fan-out tasks per part (`", "canonical_claim": "The wide S41 profile averaged approximately 80 blob upload fan-out tasks per part (`40,150 / 500`), explaining why intra-part fan-out exceeded the narrow-case estimate of roughly two blobs per part."}
{"cluster_id": "chunk_004-018", "topic": "the CA INSERT, stage 1 reduced the top thread's Real-sample share from 72", "canonical_claim": "On the CA INSERT, stage 1 reduced the top thread's Real-sample share from 72.3% to 14.5%, increased distinct Real-trace threads from 3 to 33, and increased CPU-busy/wall from 0.375 to 1.075."}
{"cluster_id": "chunk_004-019", "topic": "CA write path remained S3-network-bound at about 87% of Real wall time after sta", "canonical_claim": "The CA write path remained S3-network-bound at about 87% of Real wall time after stage 1; stage 1 overlapped that network work without removing it, while the HEAD-before-PUT gate remained about 12%."}
{"cluster_id": "chunk_004-020", "topic": "For the 500-part S41 CA leg, PUTs stayed at 119,432, while HEADs fell from 134,4", "canonical_claim": "For the 500-part S41 CA leg, PUTs stayed at 119,432, while HEADs fell from 134,400 to 105,140 and GETs from 54,360 to 42,656 after stage 1."}
{"cluster_id": "chunk_004-021", "topic": "A1 benchmark gate kept the production-shaped `BM_FlushInstallUniqueOwner` within", "canonical_claim": "The A1 benchmark gate kept the production-shaped `BM_FlushInstallUniqueOwner` within baseline: at N=100,000 it measured 13,355 ns before A1 and 12,428 ns after A1, while the shared-base `BM_FlushInstall` measured 21,208,894 ns after A1."}
{"cluster_id": "chunk_004-022", "topic": "A1 benchmark's `BM_ScratchCopy` remained O(1) at about 58 ns after the restructu", "canonical_claim": "The A1 benchmark's `BM_ScratchCopy` remained O(1) at about 58 ns after the restructure, and `BM_ApplyRefLogTxn` remained within measurement noise at 770–831 ns versus 800–864 ns baseline."}
{"cluster_id": "chunk_004-023", "topic": "upstream patch inventory counts 12 logical class-A hunks, 67 class-B hunks, and ", "canonical_claim": "The upstream patch inventory counts 12 logical class-A hunks, 67 class-B hunks, and 3 class-C hunks."}
{"cluster_id": "chunk_004-024", "topic": "`RefTableState` closed-class experiments round's success criterion (Task 8 final", "canonical_claim": "The `RefTableState` closed-class experiments round's success criterion (Task 8 final gate) is: after a 20-minute soak, the hottest CAS-attributed CPU stack family in the `trace_log` profile must be blob hashing, and every other CAS-attributed family must be at most one third of blob hashing's sample count; a miss turns the residual hotspot into a new experiment row rather than closing the round."}
{"cluster_id": "chunk_004-025", "topic": "integer refcounts versus source-edge sets", "canonical_claim": "Mutable or persisted integer blob refcounts, including folding `prior_count + Σ ±1`, were rejected because a refolded removal can underflow the count, wedge GC with `CORRUPTED_DATA`, and non-atomic folding under concurrent leaders could undercount; they were replaced by an idempotent source-edge set folded from append-only +/- journal records."}
{"cluster_id": "chunk_004-026", "topic": "gc/registry namespace registry was removed because it grew monotonically, made G", "canonical_claim": "The gc/registry namespace registry was removed because it grew monotonically, made GC cost proportional to namespaces ever created times root_shards, and did not reclaim empty shards."}
{"cluster_id": "chunk_004-027", "topic": "ranged GETs for full scans", "canonical_claim": "Reading every run block with a separate ranged GET was rejected because a full 8 GB run would require about 32,000 requests per shard, or approximately 32,000 requests per shard per pass; ranged block GETs remain only for `seek`."}
{"cluster_id": "chunk_004-028", "topic": "Merkle tree layer rejection", "canonical_claim": "The separate intermediate/Merkle `treeId` tree-layer design—content-addressed `trees/<T>` objects forming a Merkle DAG of immutable folders, with tree hashes carrying `child_gen` inside the tree identity—was rejected because it added key-space and GC edge complexity without benefit beyond recursive subtrees and identity decoupling; `child_gen` inside tree identity forced ancestor rebuilds whenever a blob's generation floor moved, the 404->LIST degraded read path for generation resolution added latency and complexity, and the generation-in-key model required durable per-hash floors, epochs, per-build pins, quiescence machinery, and Keeper as a required component. Tree objects are manifest-internal, and the `trees/` key prefix and `ObjectKind::Tree` were removed entirely on 2026-07-03, leaving only `Blob` in `ObjectKind`."}
{"cluster_id": "chunk_004-029", "topic": "freshness-meta tombstone designs", "canonical_claim": "The raw immutable-body freshness-meta design with terminal tombstones, the v2 terminal-tombstone design, and the absence-means-Clean/meta-absence-means-clean tombstone variant were rejected, superseded, or abandoned because they coupled writer progress to GC, introduced writer–GC waiting and liveness coupling, and clearing the GC tombstone reopened a data-loss hole under the add-only freshness-meta proof; the abandoned §5 plan proposed pure tombstone meta where absent meta means Clean, fresh-body-first resurrect, and spare tombstone clearing, but no such code landed. The current design keeps one hash key with an in-body incarnation tag and exact-token body deletion, and the body tag changes the etag on resurrection, making the terminal tombstone unnecessary; the optimization round ships §§1–4 and §6 without §5."}
{"cluster_id": "chunk_004-030", "topic": "per-incarnation body keys", "canonical_claim": "Per-incarnation blob/body keys such as `blobs/xx/<hash>.<incarnation>` were rejected because they require generation resolution by `LIST` and a `404 → LIST` or `404-then-LIST` read path, recreate the generation-in-key race, propagate or leak incarnation into manifests and parents, and break pure-content manifests and the pure-content/FUSE-ready design."}
{"cluster_id": "chunk_004-031", "topic": "EBR or epoch GC core never existed in C++", "canonical_claim": "An EBR or epoch GC core never existed in C++; EBR appears only in TLA+ and design documents, while the C++ path went directly from PoC reachability to the `Cas::Store` token model."}
{"cluster_id": "chunk_004-032", "topic": "GcSnap::forget history", "canonical_claim": "The P9 `GcSnap::forget` cascade and its original 404-HEAD-storm node-pruning mechanism were short-lived: `GcSnap::forget` and `GForget` were added by `3243c6aec96` and `03e11643db3`, then the `CasGcSnap` unit was deleted by `3526407f1b` ten days later; the mechanism was later removed and superseded by the source-edge-set GC model, which has no persisted node registry to forget from, and the approximately 90%-of-read-ops savings credited to it are only a historical measurement basis, not a currently existing mechanism."}
{"cluster_id": "chunk_004-033", "topic": "Comparing hot snapshots through bare `checksums", "canonical_claim": "Comparing hot snapshots through bare `checksums.txt` equality or filesystem inode witnesses was rejected; the CAS-native identity primitive is streaming re-hashing and pool-side deduplication."}
{"cluster_id": "chunk_004-034", "topic": "CAS design rejects a Merkle `treeId` layer, EBR or epoch-based GC, integer refco", "canonical_claim": "The CAS design rejects a Merkle `treeId` layer, EBR or epoch-based GC, integer refcounts, `gc/registry` as discovery authority, endpoint-plus-prefix pool matching, and CAS-specific fields in `ReplicatedMergeTreePartHeader`."}
{"cluster_id": "chunk_004-035", "topic": "Writer deregistration at `dropNamespace` was rejected because removal events car", "canonical_claim": "Writer deregistration at `dropNamespace` was rejected because removal events carry the blob `-1` edges and removing the namespace from discovery before GC folds them could cause phantom in-degree leaks."}
{"cluster_id": "chunk_004-036", "topic": "GC deregistration based on an empty settled namespace was rejected because a fre", "canonical_claim": "GC deregistration based on an empty settled namespace was rejected because a freshly created live table with no inserts is indistinguishable from a dropped table by emptiness alone."}
{"cluster_id": "chunk_004-037", "topic": "Copy-forward rejects recreating from writer-local files because a CA part's file", "canonical_claim": "Copy-forward rejects recreating from writer-local files because a CA part's files are the pool blobs and no independent source exists on the move path."}
{"cluster_id": "chunk_004-038", "topic": "same-`ManifestId` owner-move rename with Δ=0 was rejected because cross-shard mo", "canonical_claim": "A same-`ManifestId` owner-move rename with Δ=0 was rejected because cross-shard moves would create two-owner or zero-owner windows that the source-edge set model cannot represent."}
{"cluster_id": "chunk_004-039", "topic": "cache-over-CA design rejects making the generic cache wrapper impersonate CA con", "canonical_claim": "The cache-over-CA design rejects making the generic cache wrapper impersonate CA concrete types and rejects peeling wrappers at every dynamic-cast site; direct CA metadata reuse has the smaller blast radius."}
{"cluster_id": "chunk_004-040", "topic": "`ManifestExpand`, `ManifestRetire`, and `ManifestStrip` are obsolete dead enum e", "canonical_claim": "`ManifestExpand`, `ManifestRetire`, and `ManifestStrip` are obsolete dead enum entries from the removed Merkle layer and must be deleted rather than implemented."}
{"cluster_id": "chunk_004-041", "topic": "rejected trust-until-invalidated generation-counter design would have depended o", "canonical_claim": "The rejected trust-until-invalidated generation-counter design would have depended on discovering every mutation site and a root-lease argument that does not cover pool-global shadow namespaces; validate-on-hit was chosen instead."}
{"cluster_id": "chunk_004-042", "topic": "Serving a retained view for `ForceFresh` after only a matching ref resolve was r", "canonical_claim": "Serving a retained view for `ForceFresh` after only a matching ref resolve was rejected because ref currency does not prove manifest-body existence; `ForceFresh` therefore always performs the mandatory `HEAD` through `readManifestShared`."}
{"cluster_id": "chunk_004-043", "topic": "four-value `Freshness` enum was rejected because the mutable-read versus write-e", "canonical_claim": "A four-value `Freshness` enum was rejected because the mutable-read versus write-evidence distinction is carried by the method: mutable reads call `resolve`, while write-evidence reads call `getView(ForceFresh)`."}
{"cluster_id": "chunk_004-044", "topic": "Materialized file and directory indexes were rejected because decoder-enforced c", "canonical_claim": "Materialized file and directory indexes were rejected because decoder-enforced canonical order makes binary search over the shared decode simpler and lighter."}
{"cluster_id": "chunk_004-045", "topic": "Relocating `shard_decode_cache` and `dedup_cache` into a `CasCacheState` was rej", "canonical_claim": "Relocating `shard_decode_cache` and `dedup_cache` into a `CasCacheState` was rejected as unnecessary verified-machinery movement with regression risk; the memory goal is met by byte-bounding `manifest_cache` in Phase 5."}
{"cluster_id": "chunk_004-046", "topic": "Comparing whole PartManifest objects is rejected because ref, root_namespace_id,", "canonical_claim": "Comparing whole PartManifest objects is rejected because ref, root_namespace_id, and payload_digest legitimately differ between source and destination manifests."}
{"cluster_id": "chunk_004-047", "topic": "raw-body v1 cut was rejected because dropping the in-body incarnation tag fixed ", "canonical_claim": "The raw-body v1 cut was rejected because dropping the in-body incarnation tag fixed the body etag to content and removed the protection that makes exact-token deletion miss a resurrected body."}
{"cluster_id": "chunk_004-048", "topic": "Retired-in-snapshot does not change round pacing or the one-round graduation gap", "canonical_claim": "Retired-in-snapshot does not change round pacing or the one-round graduation gap, because that gap is the racing-writer edge-surfacing window under EDGE-BEFORE-OBSERVE."}
{"cluster_id": "chunk_004-049", "topic": "Moving `clearSparedMeta` after the winning `gc/state` CAS is insufficient becaus", "canonical_claim": "Moving `clearSparedMeta` after the winning `gc/state` CAS is insufficient because an older leader can execute a stale pre-CAS `deleteExact` after a newer leader adopts a spare and clears the meta."}
{"cluster_id": "chunk_004-050", "topic": "Settlement is not partitioned by algorithm", "canonical_claim": "Settlement is not partitioned by algorithm: mixed rows remain in the same `gc_shards` runs, and schemas 1 and 2 plus digest-only settlement helpers are deleted without compatibility shims."}
{"cluster_id": "chunk_004-051", "topic": "Mixed-algorithm pools do not rehash existing blobs, perform cross-algorithm dedu", "canonical_claim": "Mixed-algorithm pools do not rehash existing blobs, perform cross-algorithm deduplication, support per-table or per-insert algorithm selection, or remove algorithms from `algos_used`."}
{"cluster_id": "chunk_004-052", "topic": "recommended default layout includes the `ch128` path segment rather than retaini", "canonical_claim": "The recommended default layout includes the `ch128` path segment rather than retaining the legacy no-segment cityHash128 layout, because CA has no persisted-data compatibility requirement."}
{"cluster_id": "chunk_004-053", "topic": "Phase 1 rejects a global ref-sequence watermark because lexical scan order diffe", "canonical_claim": "Phase 1 rejects a global ref-sequence watermark because lexical scan order differs from sequence order across tables and a global watermark could mark unseen concurrent appends as folded."}
{"cluster_id": "chunk_004-054", "topic": "optional memory fast path is a separable optimization", "canonical_claim": "The optional memory fast path is a separable optimization: small S3-mode blobs may be buffered in memory and written with `putIfAbsentStream`, while large blobs use S3 staging and conditional copy."}
{"cluster_id": "chunk_004-055", "topic": "late-predecessor TLA+ configuration intentionally retains an expected-fail count", "canonical_claim": "The late-predecessor TLA+ configuration intentionally retains an expected-fail counterexample in which an old-epoch log materializes after a successor scan and violates complete-recovery safety."}
{"cluster_id": "chunk_004-056", "topic": "token-diff `Skip` machinery and `listTokens` capability were removed because ref", "canonical_claim": "The token-diff `Skip` machinery and `listTokens` capability were removed because ref GC now detects changes from logs above each per-table cursor, and `listTokens` has no production callers at HEAD."}
{"cluster_id": "chunk_004-057", "topic": "Timeout control reduces but does not eliminate late-predecessor S3 completion", "canonical_claim": "Timeout control reduces but does not eliminate late-predecessor S3 completion; Keeper-based commit or another cross-object fencing mechanism remains a separate possible solution for strict cross-epoch authority."}
{"cluster_id": "chunk_004-058", "topic": "ObjectKind retention", "canonical_claim": "The single-value `ObjectKind` enum was deliberately retained and was not removed despite spec item D1f because it is threaded through about 100 consumers, including the parameter/field types of `observeAndAdmit`, `uploadFromSource`, `decodeEnvelopeHeader`, `magicFor`, `retiredLogicalSize`, `EnvelopeHeader::kind`, `RetiredEntry::kind`, the `CasGcOutcomes` codec, and `Fsck/GcObserved`; removing it would be a refactor rather than clean subtraction."}
{"cluster_id": "chunk_004-059", "topic": "CasEvent::round retention", "canonical_claim": "`CasEvent::round` was not removed despite the earlier premise that it was always zero and a dead field: at HEAD it has 20 write sites rather than the original stabilization spec's 5 write sites, is copied into `ContentAddressedLogElement::round`, and is a live serialized `system.content_addressed_log.round` column, so deleting it would be a user-visible schema change."}
{"cluster_id": "chunk_004-060", "topic": "design rejected a decommission-intent marker executed by `GC` because it would r", "canonical_claim": "The design rejected a decommission-intent marker executed by `GC` because it would require `GC` to invent ref transitions, and rejected raw prefix deletion followed by mandatory `ca-gc-rebuild` because it would break in-degree accounting until an O(universe) rebuild; the chosen design makes the command a temporary writer using existing writer machinery."}
{"cluster_id": "chunk_004-061", "topic": "rev", "canonical_claim": "The rev.5 `snapshot_min_log_age_ms` grace window could leave a late predecessor log visible to cold folds while permanently absent from the writer's state."}
{"cluster_id": "chunk_004-062", "topic": "proposal's recovery-seal-at-listed-maximum rule was later corrected to the dead-", "canonical_claim": "The proposal's recovery-seal-at-listed-maximum rule was later corrected to the dead-epoch upper-bound rule because an unresolved predecessor ID can exceed the greatest listed ID."}
{"cluster_id": "chunk_004-063", "topic": "read-only replica design rejects reader-to-writer promotion, an `H_max` snapshot", "canonical_claim": "The read-only replica design rejects reader-to-writer promotion, an `H_max` snapshot-age cap, per-blob read pins, and making the reader a full pool member as scope for this design."}
{"cluster_id": "chunk_004-064", "topic": "Owner-transition bindings use flat `o` and `n` key prefixes rather than nested J", "canonical_claim": "Owner-transition bindings use flat `o` and `n` key prefixes rather than nested JSON objects because the flat `JsonObjectReader` has no array or nesting support and extending it would increase the blast radius."}
{"cluster_id": "chunk_004-065", "topic": "`CasRecordStreamFormat` and the fixed-offset blob-envelope codec are not reused ", "canonical_claim": "`CasRecordStreamFormat` and the fixed-offset blob-envelope codec are not reused for part manifests because the former is source-edge-specific and the latter supports only one raw payload rather than multiple banner segments."}
{"cluster_id": "chunk_004-066", "topic": "Transport-level seals such as sender ACKs, response suffixes, HTTP trailers, and", "canonical_claim": "Transport-level seals such as sender ACKs, response suffixes, HTTP trailers, and full-duplex reverse channels were rejected because Poco interserver transport is half-duplex and response-side data cannot provide a reverse acknowledgement or backpressure."}
{"cluster_id": "chunk_004-067", "topic": "CA-specific disk `precommit` phase is rejected because the relevant `Keeper`/dis", "canonical_claim": "A CA-specific disk `precommit` phase is rejected because the relevant `Keeper`/disk ordering is general to real disk transactions, while a CA-only phase would add an extra visibility boundary and upstream conflict surface without solving the general problem."}
{"cluster_id": "chunk_004-068", "topic": "design rejects deferred delete intents, disk-queue replay before commit, replay ", "canonical_claim": "The design rejects deferred delete intents, disk-queue replay before commit, replay in a new `precommit`, reuse of `IDataPartStorage::precommitTransaction`, `publishStagedData`, publication from `moveDirectory`, and a CA-specific transaction subclass or split base."}
{"cluster_id": "chunk_004-069", "topic": "Preserving staged merged parts or implementing resume-upload is explicitly out o", "canonical_claim": "Preserving staged merged parts or implementing resume-upload is explicitly out of scope because content-addressed blob deduplication bounds recompute cost once lease renewal stops over-fencing and merge backoff engages."}
{"cluster_id": "chunk_004-070", "topic": "proposed `CasBlobUploader` split was descoped because `putBlob`, `observeAndAdmi", "canonical_claim": "The proposed `CasBlobUploader` split was descoped because `putBlob`, `observeAndAdmit`, and `uploadFromSource` form one mutually recursive writer-protocol lane whose decision state is coupled to byte delivery and shared mid-flight state."}
{"cluster_id": "chunk_004-071", "topic": "one-pipeline design adds no `IDiskTransaction", "canonical_claim": "The one-pipeline design adds no `IDiskTransaction::precommit`, `IMetadataTransaction::precommit`, or `IDataPartStorage::publishStagedData`, and does not change `IDataPartStorage::precommitTransaction` or the Replicated `Keeper` call sequence."}
{"cluster_id": "chunk_004-072", "topic": "cross-disk freeze copy", "canonical_claim": "The `freeze` transaction seam cannot provide the MOVE-to-CA or cross-disk copy primitive because its transactional `Backup()` path uses same-disk `transaction->copyFile`, `DiskObjectStorageTransaction::copyFile` calls `generateObjectKeyForPath`, which throws `NOT_IMPLEMENTED` on CA, and generic disk copy primitives use autocommit writes."}
{"cluster_id": "chunk_004-073", "topic": "Deferring `clonePart`'s commit into the swap is rejected because `clonePart` mus", "canonical_claim": "Deferring `clonePart`'s commit into the swap is rejected because `clonePart` must commit before `create(initialize=true)` reads the part back, and changing that contract would enlarge the generic mover surface."}
{"cluster_id": "chunk_004-074", "topic": "persistent ordered-tree approach, an `absl", "canonical_claim": "The persistent ordered-tree approach, an `absl::btree_map` base, and changes to `precommits` are out of scope for the COW-map cut; commit-path tracing and microbenchmark evidence are separate follow-up work."}
{"cluster_id": "chunk_004-075", "topic": "persistent immutable ordered tree is deferred, `absl", "canonical_claim": "A persistent immutable ordered tree is deferred, `absl::btree_map` is optional follow-up work, and `precommits` remains unchanged."}
{"cluster_id": "chunk_004-076", "topic": "Verify-on-dedup remains out of scope as the primary fix because it adds a read t", "canonical_claim": "Verify-on-dedup remains out of scope as the primary fix because it adds a read to every dedup hit and does not close merge-path ordering windows; CA-only publish-at-rename and a new `IDiskTransaction::precommit` phase were also rejected."}
{"cluster_id": "chunk_004-077", "topic": "T5 intentionally adds no new un-decommission CLI flag", "canonical_claim": "T5 intentionally adds no new un-decommission CLI flag; manual clearing of `retired_at_ms` is the only revival path in this fix."}
{"cluster_id": "chunk_004-078", "topic": "benchmark's `BM_MemcpyTxnBytes` floor was 30", "canonical_claim": "The benchmark's `BM_MemcpyTxnBytes` floor was 30.7 ns, making the implemented 333 ns encoder about 10.8× the floor rather than the planned ≤3× target; the target was rejected as physically unreachable because a correct encoder must still validate, format numbers, escape JSON, and allocate its buffer."}
{"cluster_id": "chunk_004-079", "topic": "incremental-budget design rejects a conservative over-estimate because byte-exac", "canonical_claim": "The incremental-budget design rejects a conservative over-estimate because byte-exactness is provable, rejects caching full encoded strings because it would retain two full strings and require line splicing, and rejects reducing `admits()` frequency because it changes per-operation validation granularity."}
{"cluster_id": "chunk_004-080", "topic": "E4 flat-vector `RefCowMap` base was reverted because only `BM_Materialize` met t", "canonical_claim": "The E4 flat-vector `RefCowMap` base was reverted because only `BM_Materialize` met the two-times target at N=100,000 while `BM_MergedIteration` improved by at most 1.28 times and `BM_SnapshotEncode` stayed within noise."}
{"cluster_id": "chunk_004-081", "topic": "landed Dormant/UNMOUNT lifecycle is unsound because its benign-absent behavior c", "canonical_claim": "The landed Dormant/UNMOUNT lifecycle is unsound because its benign-absent behavior can silently skip reclaim and make ATTACH load an empty table when data may still exist."}
{"cluster_id": "chunk_004-082", "topic": "Lazy `ensureMounted()` from `store()` is rejected because it inverts the `lifecy", "canonical_claim": "Lazy `ensureMounted()` from `store()` is rejected because it inverts the `lifecycle_mutex → gc_scheduler_mutex → pointer_mutex` lock order and can deadlock; a benign probe also cannot distinguish table binding from a sweep."}
{"cluster_id": "chunk_004-083", "topic": "original phased design's byte-estimate admission was rejected because undercount", "canonical_claim": "The original phased design's byte-estimate admission was rejected because undercounting could make whole-batch `checkBudget` throw; v2 replaces it with exact operation-count admission and a per-operation size cap."}
{"cluster_id": "chunk_004-084", "topic": "retention-pin design was rejected because it would add a pool-global GC overlay,", "canonical_claim": "The retention-pin design was rejected because it would add a pool-global GC overlay, union folding, removal deferral, and owner-liveness machinery to the most safety-critical component."}
{"cluster_id": "chunk_004-085", "topic": "reserved-precommit design was abandoned because it caused state-model sprawl, le", "canonical_claim": "The reserved-precommit design was abandoned because it caused state-model sprawl, let a foreign writer bypass `stageManifest` and `adoptEvidence` discipline, and mismatched the `tmp-fetch_<part>` contract."}
{"cluster_id": "chunk_004-086", "topic": "superseded branch was intentionally out of scope in the first proposal, but v2 l", "canonical_claim": "The superseded branch was intentionally out of scope in the first proposal, but v2 later classifies it as normal fencing because a different epoch indicates a distinct remount incarnation."}
{"cluster_id": "chunk_004-087", "topic": "Adopt-and-continue of the observed mount token was rejected because it violated ", "canonical_claim": "Adopt-and-continue of the observed mount token was rejected because it violated the contract that `onRenewMismatch` must throw and would require proof that an unwitnessed body is safe to adopt; fence plus self-remount already exists and is exercised."}
{"cluster_id": "chunk_004-088", "topic": "v2 design explicitly leaves body-nonce rescue, coherent-read machinery, resolve-", "canonical_claim": "The v2 design explicitly leaves body-nonce rescue, coherent-read machinery, resolve-on-ambiguity, and changes to the mount-lease format out of scope; the safety mechanism is fence plus remount."}
{"cluster_id": "chunk_004-089", "topic": "`Pool", "canonical_claim": "`Pool::openForDecommission` already provides a reversible alternative by reading the existing owner uuid and impersonating it, preserving pool identity while allowing a WRITE mount; its tradeoff is that two processes present the same owner uuid and rely on the mount lease to serialize them."}
{"cluster_id": "chunk_004-090", "topic": "Banning a database engine or restricting generation tokens to shadow paths was r", "canonical_claim": "Banning a database engine or restricting generation tokens to shadow paths was rejected because it would break supported same-name reuse and replicated-database recovery while leaving other rebirth routes unprotected."}
{"cluster_id": "chunk_004-091", "topic": "Serial exact `GET N+1` until 404 was rejected because every probe costs a full S", "canonical_claim": "Serial exact `GET N+1` until 404 was rejected because every probe costs a full S3 round trip, concurrent writing makes the stop unstable, and 404 proves object absence rather than acknowledged-history completeness."}
{"cluster_id": "chunk_004-092", "topic": "Recovery from a complete stream LIST was rejected because completeness is the pr", "canonical_claim": "Recovery from a complete stream LIST was rejected because completeness is the premise under test; recovery performs zero stream LIST calls and uses enumeration only for leak-only GC/janitor discovery."}
{"cluster_id": "chunk_004-093", "topic": "ref-layer `materialization_grace` wait is expected to be deleted because arithme", "canonical_claim": "The ref-layer `materialization_grace` wait is expected to be deleted because arithmetic recovery no longer trusts listings for completeness and the in-band seal fences late stragglers deterministically; the setting must not remain parsed but inert."}
{"cluster_id": "chunk_004-094", "topic": "series does not move namespace files into the ref log, add folder-state manifest", "canonical_claim": "This series does not move namespace files into the ref log, add folder-state manifests or a `_raw_files` object family, change `MergeTreeDeduplicationLog`, merge `Poisoned` into wedge, refactor `RefApplyState`, or remove the plain-object component."}
{"cluster_id": "chunk_004-095", "topic": "Adding `INVALID_STATE` to `isRetryableException` was rejected because it would r", "canonical_claim": "Adding `INVALID_STATE` to `isRetryableException` was rejected because it would reclassify 18 sites, including eight terminal CA sites and eight non-CA sites; recoding the three selected transient sites to `NETWORK_ERROR` fixes the required behavior with zero unintended reclassifications."}
{"cluster_id": "chunk_004-096", "topic": "relink protocol deliberately sends no `ETag`, `If-None-Match`, `If-Modified-Sinc", "canonical_claim": "The relink protocol deliberately sends no `ETag`, `If-None-Match`, `If-Modified-Since`, or `If-Match`; standard conditional vocabulary was rejected because an intermediary could evaluate it and synthesize a response, including a forged 304, instead of consulting the sender."}
{"cluster_id": "chunk_004-097", "topic": "per-ref `MutationScope` index was rejected as a pre-ship stopgap because it adds", "canonical_claim": "The per-ref `MutationScope` index was rejected as a pre-ship stopgap because it adds protocol complexity to narrow the lane terms and would undermine the surface-deletion goal; a refusal is retry-later and therefore an availability cost rather than a correctness hazard."}
{"cluster_id": "chunk_004-098", "topic": "recovery token bucket, cache-class segmentation, and per-peer fairness machinery", "canonical_claim": "The recovery token bucket, cache-class segmentation, and per-peer fairness machinery were rejected as overengineering; only the per-recovery work cap and hard-coded global concurrency limit remain, with cold-confirm share as the revisit tripwire."}
{"cluster_id": "chunk_004-099", "topic": "receiver-side ref verification", "canonical_claim": "Receiver-pays verification by having the receiver directly recover or verify the sender's ref state from the pool was rejected: although making the beneficiary pay would remove the DoS class, each confirmation would perform `LIST` plus replay and inherit the listing-trust questions/problems that the v9 chain is intended to remove."}
{"cluster_id": "chunk_004-100", "topic": "304/503 response-status encoding was rejected", "canonical_claim": "The 304/503 response-status encoding was rejected: `assertResponseIsOk` accepts only 200, 201, 202, 206 and redirects, so 304 and 503 would arrive as exceptions and require shared HTTP-client changes; the protocol keeps tri-state values in explicit headers on 200 responses."}
{"cluster_id": "chunk_004-101", "topic": "bounded re-`LIST` or retry triggered by an out-of-order key cannot replace catal", "canonical_claim": "A bounded re-`LIST` or retry triggered by an out-of-order key cannot replace catalog authority because finite scans can agree while omitting an entire namespace and therefore produce no retry trigger."}
{"cluster_id": "chunk_004-102", "topic": "Permanent exact-`RootNamespace` non-reuse would avoid generation-based aliasing ", "canonical_claim": "Permanent exact-`RootNamespace` non-reuse would avoid generation-based aliasing only by refusing supported exact-name reuse workflows, including shadow names and explicit UUID replay."}
{"cluster_id": "chunk_004-103", "topic": "Deleting a catalog entry immediately after folding the terminal record permits m", "canonical_claim": "Deleting a catalog entry immediately after folding the terminal record permits multiple cleanup items for repeated lives of one name and breaks the fold-seal reservation bound."}
{"cluster_id": "chunk_004-104", "topic": "mount-time one-shot synthetic LIST probe cannot certify a backend because it can", "canonical_claim": "A mount-time one-shot synthetic LIST probe cannot certify a backend because it can pass on RustFS even though a rare load- and timing-dependent violation later occurs."}
{"cluster_id": "chunk_004-105", "topic": "writer-GC simplification review rejected both unchanged-round promote HEAD skipp", "canonical_claim": "The writer-GC simplification review rejected both unchanged-round promote HEAD skipping and per-commit pinned-round acknowledgements as unnecessary variants after adopting the `EDGE-BEFORE-OBSERVE` design; the durable precommit edge plus two-phase delete recheck remains the load-bearing safety mechanism."}
{"cluster_id": "chunk_004-106", "topic": "Raw appending of canonical ref names was rejected because `checkCanonicalRefName", "canonical_claim": "Raw appending of canonical ref names was rejected because `checkCanonicalRefName` does not reject quotes, control bytes, or U+2028/U+2029; only the fixed vocabulary words `owner` and `tombstone` were proven safe for raw append."}
{"cluster_id": "chunk_004-107", "topic": "intermediate grace-based GC designs were intentionally superseded by the incarna", "canonical_claim": "The intermediate grace-based GC designs were intentionally superseded by the incarnation, pin, lease, and fence model; this is treated as intended design history rather than lost durable content."}
{"cluster_id": "chunk_004-108", "topic": "four `EnableTokenDiff = TRUE` configurations `stage5_tokendiff`, `sab_skipchange", "canonical_claim": "The four `EnableTokenDiff = TRUE` configurations `stage5_tokendiff`, `sab_skipchangedshard`, `sab_skipparksdeadprecommit`, and `fix_skipparksdeadprecommit` were retired because no production seam legitimately consumes a LIST-derived token as authority to skip the fold read."}
{"cluster_id": "chunk_004-109", "topic": "`INV_RECONCILE_SAFE_BRIEF` is not a valid gate because its proposed state formul", "canonical_claim": "`INV_RECONCILE_SAFE_BRIEF` is not a valid gate because its proposed state formula rejects the legitimate transient state where a creator fence is terminal before `_ckpt` creation."}
{"cluster_id": "chunk_004-110", "topic": "retired `_sab_fix_ckptwitness` and `_witness_corruptgap` obligations are unconst", "canonical_claim": "The retired `_sab_fix_ckptwitness` and `_witness_corruptgap` obligations are unconstructible because `_ckpt.committed_through`, rather than `_ckpt.checkpoint` or a higher `LIST` witness, is now the exact bound and every missing key at or below it creates a hold."}
{"cluster_id": "chunk_004-111", "topic": "Gate 0 is only an availability filter and authorizes nothing", "canonical_claim": "Gate 0 is only an availability filter and authorizes nothing; safety belongs to gate 1, so modeling gate 0 would create a false impression of two independent safety sources."}
{"cluster_id": "chunk_004-112", "topic": "original epoch-based reclamation GC with a global epoch and per-writer pins was ", "canonical_claim": "The original epoch-based reclamation GC with a global epoch and per-writer pins was superseded by the incarnation-token design because no epoch machinery exists in the shipped code."}
{"cluster_id": "chunk_004-113", "topic": "per-candidate minimum-active-build watermark mechanism was replaced by precommit", "canonical_claim": "The per-candidate minimum-active-build watermark mechanism was replaced by precommit-first reachability; the surviving requirement is that build sequence numbers come from a monotone counter."}
{"cluster_id": "chunk_004-114", "topic": "EBR GC core was rejected because per-object epoch state did not scale", "canonical_claim": "The EBR GC core was rejected because per-object epoch state did not scale; GC uses incarnation-token identity instead."}
{"cluster_id": "chunk_004-115", "topic": "V1 rejects a generic folder-view API in `IMetadataStorage` because it would broa", "canonical_claim": "V1 rejects a generic folder-view API in `IMetadataStorage` because it would broaden the disk API, increase fork conflicts, and still not express CAS-specific freshness rules."}
{"cluster_id": "chunk_004-116", "topic": "`Cas", "canonical_claim": "`Cas::Store` must not own part-folder cache policy because that would mix protocol, metadata filesystem semantics, and operational tuning."}
{"cluster_id": "chunk_004-117", "topic": "V1 rejects generic path-result caching because the workload is specifically a pa", "canonical_claim": "V1 rejects generic path-result caching because the workload is specifically a part/projection folder load window and arbitrary path invalidation would complicate mutable files."}
{"cluster_id": "chunk_004-118", "topic": "architecture analysis does not recommend merging `CasPoolMeta` and `CasRootsRegi", "canonical_claim": "The architecture analysis does not recommend merging `CasPoolMeta` and `CasRootsRegistry` codecs because their create-once and mutable-CAS lifecycles differ."}
{"cluster_id": "chunk_004-119", "topic": "v2 rejects ClickHouse CompressedWriteBuffer for CAS bodies and chooses plain sin", "canonical_claim": "v2 rejects ClickHouse CompressedWriteBuffer for CAS bodies and chooses plain single-frame zstd so the protocol remains portable outside ClickHouse"}
{"cluster_id": "chunk_004-120", "topic": "v2 rejects an all-protobuf design because unbounded GC data would lose bounded-m", "canonical_claim": "v2 rejects an all-protobuf design because unbounded GC data would lose bounded-memory streaming and ranged seek"}
{"cluster_id": "chunk_004-121", "topic": "v2 rejects FlatBuffers and Cap'n Proto because CAS metadata is small and object-", "canonical_claim": "v2 rejects FlatBuffers and Cap'n Proto because CAS metadata is small and object-storage network latency dominates parsing, while the dependencies add no needed portability"}
{"cluster_id": "chunk_004-122", "topic": "v3 rejects v2's framed protobuf design because it keeps the control plane behind", "canonical_claim": "v3 rejects v2's framed protobuf design because it keeps the control plane behind bespoke tooling and adds integrity guards whose failures change no decision"}
{"cluster_id": "chunk_004-123", "topic": "v3 rejects Avro and Parquet for cas_run because seek is dead in production, they", "canonical_claim": "v3 rejects Avro and Parquet for cas_run because seek is dead in production, they add dependencies, and their container or row-group machinery does not fit two-hash marker records"}
{"cluster_id": "chunk_004-124", "topic": "v3 rejects keeping per-block CRC and footer in cas_run because the seal checksum", "canonical_claim": "v3 rejects keeping per-block CRC and footer in cas_run because the seal checksum covers the whole file that all consumers actually read"}
{"cluster_id": "chunk_004-125", "topic": "CAS mechanisms are rejected when they require Keeper for correctness, scale Keep", "canonical_claim": "CAS mechanisms are rejected when they require Keeper for correctness, scale Keeper or S3 work with data volume, let a stuck writer stall pool-wide reclamation, can accelerate deletion past safety gates, or couple generic `ReplicatedMergeTree` surfaces to the CA disk implementation."}
{"cluster_id": "chunk_004-126", "topic": "`CasNamespaceFileDiskProfile", "canonical_claim": "The `CasNamespaceFileDiskProfile.SteadyStateFileOperationsTouchNoCatalogRefBlobOrManifestKey` test does not exercise the `listNamespaceFiles` arm; direct list-arm coverage is provided separately."}
{"cluster_id": "chunk_004-127", "topic": "SensitivityCheckOrderingComparisonDiscriminates is not a valid sensitivity check", "canonical_claim": "SensitivityCheckOrderingComparisonDiscriminates is not a valid sensitivity check because EXPECT_FALSE(*body_index > *ckpt_index) duplicates the scenario and is weaker than test 1's EXPECT_LT(*body_index, *ckpt_index); the recorded mutation run is the meaningful evidence."}
{"cluster_id": "chunk_004-128", "topic": "historical alternative of widening probe A was rejected", "canonical_claim": "The historical alternative of widening probe A was rejected; probe A is deleted outright rather than demoted or retained as a sampling mechanism."}
{"cluster_id": "chunk_004-129", "topic": "ordinary `clickhouse-disks write` verb currently does not reach `blobUploadPool(", "canonical_claim": "The ordinary `clickhouse-disks write` verb currently does not reach `blobUploadPool()` because plain files use `CaInlineWriteBuffer` and part-shaped autocommit writes are `NOT_IMPLEMENTED`; pool initialization there is defense-in-depth for other or future non-autocommit paths."}
{"cluster_id": "chunk_004-130", "topic": "`StorageReplicatedMergeTree", "canonical_claim": "The `StorageReplicatedMergeTree::checkAlterPartitionIsPossible` override that only delegates to the virtual base adds no behavior and should be deleted together with its header declaration."}
{"cluster_id": "chunk_004-131", "topic": "EBR (Epoch-Based Reclamation) GC core -- writers holding a pin epoch (an ephemer", "canonical_claim": "An EBR (Epoch-Based Reclamation) GC core -- writers holding a pin epoch (an ephemeral Keeper node), GC advancing through safe_epoch/commit_epoch/birth_epoch and waiting for quiescence, with generations on blob keys (blobs/<H>/<g>) as the ABA guard -- was rejected because a stuck or wedged writer held down safe_epoch and stalled reclamation of all dropped parts across all writers, Keeper was required for correctness, and a long-running INSERT could delay GC for its entire duration. It was replaced by the incarnation-token design, where regular GC candidates are restricted to journal-known nodes so fresh uploads are structurally invisible and can never be candidates, and a wedged writer has no pin to hold down."}
{"cluster_id": "chunk_004-132", "topic": "Whole-object copy-on-write for `RefTableState", "canonical_claim": "Whole-object copy-on-write for `RefTableState::committed` via `src/Common/COW.h` (clone the whole map on any write) was rejected as \"approach C\" because trial copies during a flush always write, so it gives no gain over the naive full-map copy."}
{"cluster_id": "chunk_004-133", "topic": "Three fixes for the GC-registry monotone-growth problem were rejected before the", "canonical_claim": "Three fixes for the GC-registry monotone-growth problem were rejected before the shard-incarnation redesign: (1) writer deregisters at `dropNamespace` — unsafe because the removal `-1` in-degree edges would be lost if the namespace vanished from discovery before GC folded that window; (2) GC hard-deregisters when 'empty + settled' — indistinguishable from a live table between CREATE and its first INSERT; (3) shard-object deletion on the path-keyed cursor — an ABA hazard where a recreated shard resets `shard_version` to 0 and an old sealed cursor silently skips the new incarnation's events."}
{"cluster_id": "chunk_004-134", "topic": "Rejected writer-protocol designs", "canonical_claim": "Rejected writer-protocol designs: `cas_owner` S3-metadata + `protectedByLiveBuild` guard (adopt did not transfer ownership, protection was revocable); a per-build heartbeat object at `builds/<build_id>` (leaked on successful publish, broke `DeletedCandidateDoesNotReappear`); `Build::resurrect` via GET-from-existing (raced GC delete in the HEAD-to-GET window); retaining full blob bodies in RAM (rejected on memory grounds for gigabyte column blobs); server-side copy/`UploadPartCopy` for bodyless re-stamp (dropped once the per-server watermark made bodyless re-stamp unnecessary; MinIO has known `UploadPartCopy` gaps); publish-at-precommit double-write (would add a second manifest PUT per part)."}
{"cluster_id": "chunk_004-135", "topic": "E4 (flat-vector base for RefTableState) was REVERTED after implementation", "canonical_claim": "E4 (flat-vector base for RefTableState) was REVERTED after implementation: it only cleared Materialize's asymptotic cost 2x (O(NlogN)->O(N)) on a cost that is per-flush, never trace-visible, and N-budget-bounded, so simplicity won over the marginal gain."}
{"cluster_id": "chunk_004-136", "topic": "Keeping a permanent `Retired` catalog row instead of the transient `RemovalReady", "canonical_claim": "Keeping a permanent `Retired` catalog row instead of the transient `RemovalReady` state was rejected because it grows the catalog with historical namespace births, eventually exhausts its object cap, and breaks supported exact-name reuse."}
{"cluster_id": "chunk_004-137", "topic": "Trusting only the historical pruned-seal observation (without a `RemovalReady` c", "canonical_claim": "Trusting only the historical pruned-seal observation (without a `RemovalReady` catalog state) was rejected because another round can recreate the name-keyed cursor while the catalog entry remains `Removing`."}
{"cluster_id": "chunk_004-138", "topic": "Keeping `_cleanup` as permanent removal evidence was rejected because a marker t", "canonical_claim": "Keeping `_cleanup` as permanent removal evidence was rejected because a marker that must outlive every possibly stalled actor is unbounded, and deleting it recreates the stale-leader race it was introduced to prevent."}
{"cluster_id": "chunk_004-139", "topic": "Inferring damage from entry-less debris was rejected because legal debris and a ", "canonical_claim": "Inferring damage from entry-less debris was rejected because legal debris and a fabricated missing catalog entry are physically indistinguishable; adding more classifier branches cannot create information that was never persisted."}
{"cluster_id": "chunk_004-140", "topic": "Life-scoped cursors were rejected as the primary Task 5 mechanism because they c", "canonical_claim": "Life-scoped cursors were rejected as the primary Task 5 mechanism because they change the fold-seal wire grammar, every cursor key builder/parser, GC intake, REBUILD, fsck, and manifest sweep coverage lookup, while `RemovalReady` changes only one catalog enum, its codec, one transition, and the GC universe predicate."}
{"cluster_id": "chunk_004-141", "topic": "first proposed fix for the deposed-leader stray-Clean defect (Fix 1", "canonical_claim": "The first proposed fix for the deposed-leader stray-Clean defect (Fix 1: couple `clearSparedMeta` to adoption by moving it after the winning `gc/state` CAS, plus a `condemn_round` guard) was proven insufficient by a second model consult: a stale, paused leader can issue the destructive exact-token `deleteExact(t1)` pre-CAS from an older adopted snapshot, so a fresh leader's legitimately-adopted post-CAS clear still lets a writer reuse `t1` and the stale leader's redelete kills the live reuse, because the final `gc/state` CAS fences adoption, not pre-CAS side effects."}
{"cluster_id": "chunk_004-142", "topic": "reviewer's objection that the `src/Common/ProfileEvents", "canonical_claim": "A reviewer's objection that the `src/Common/ProfileEvents.cpp` registration exceeded the approved failpoint scope was refuted as a conflation: the registration came from a separate task, not the failpoint-approval task, and a `ProfileEvent` registration is not protocol coupling; the failpoint approval's actual scope (only `src/Common/FailPoint.cpp` registration lines) was exercised exactly as recorded."}
{"cluster_id": "chunk_004-143", "topic": "later draft of the relink/seam TLA+ model omitted FloorReconcile entirely while ", "canonical_claim": "A later draft of the relink/seam TLA+ model omitted FloorReconcile entirely while leaving WedgeResolveInstall enabled on sWedge /\\ sApplyOwed, which admitted a poisoned-to-clean install as reachable when it should be impossible; a blanket ~sWedge exclusion in MarkerCoversDurableWindow was rejected because it hid the bug rather than describing the code — once the invariant's subject became the mount's own owed apply, no exclusion was needed for either the wedge or the fence."}
{"cluster_id": "chunk_004-144", "topic": "Five same-class defect hardening findings (H1-H5) followed the B85/B87 fixes", "canonical_claim": "Five same-class defect hardening findings (H1-H5) followed the B85/B87 fixes: H1 four unchecked `resolvePartGenKeyForRead` call sites in `getLastModified` could wedge `clearOldTemporaryDirectories` on DROP; H2/H3 two sites threw `LOGICAL_ERROR` for benign concurrent DROP/DETACH races that should have thrown recoverable `FILE_DOESNT_EXIST`; H4 `replaceFile` lacked the `is_part_dir` directory delegation `moveFile` had just received; H5 `ContentAddressedGC::listLivePartIds` did a LIST-then-read without swallowing a concurrently-dropped ref."}
{"cluster_id": "chunk_004-145", "topic": "architecture documents still describe the removed in-place trim mechanism as liv", "canonical_claim": "The architecture documents still describe the removed in-place trim mechanism as live, including the Lazy-trim gate in `docs/superpowers/cas/04-gc-protocol.md` and `01-architecture.md`; the references should be folded into the existing GC documentation-refresh backlog."}
{"cluster_id": "chunk_004-146", "topic": "architecture documents still present removed CAS mechanisms as current, includin", "canonical_claim": "Several architecture documents still present removed CAS mechanisms as current, including `/_precommits`, `root_shards`, `incarnation_*`, `fence_seq`, manifest-backpressure knobs, `folded_cursor`, lazy trim, and `shardOf`; these references require a documentation refresh."}
{"cluster_id": "chunk_004-147", "topic": "`StageA_Suppressed` remains the name of a live negative-policy enumerator even a", "canonical_claim": "`StageA_Suppressed` remains the name of a live negative-policy enumerator even after Stage A; a follow-up rename such as `NoUniverseSupplied` would touch about 12 sites."}
{"cluster_id": "chunk_004-148", "topic": "2026-06-22 model currency audit found no old-design cruft in any live TLA+ model", "canonical_claim": "A 2026-06-22 model currency audit found no old-design cruft in any live TLA+ model, but found three models stale against shipped code around the B171 precommit-reachability mechanism — `CaBuildWatermark.tla`, `CaBuildWatermarkNum.tla`, `CaResurrectLiveness.tla` — and documented dispositions (mark stale; repurpose the watermark floor lemma for precommit-ref reclaim liveness) rather than deleting the models, preserving proof history."}
{"cluster_id": "chunk_004-149", "topic": "Working-tree \"Task 13a\" coverage-gap tests add `InvalidRefLogBodyAbortsFoldNoPar", "canonical_claim": "Working-tree \"Task 13a\" coverage-gap tests add `InvalidRefLogBodyAbortsFoldNoPartialDelta`, `BaselineGuardRefusesWhenSnapshotSurvivesWithoutLogsOrCursor`, `RefWriterAppendLane.SameRefMutationsSplitAcrossFlushes`, and `MountpointObjectRoundTrip`, restoring contracts of tests retired with earlier ref-model changes."}
{"cluster_id": "chunk_004-150", "topic": "ref-lease-exclusivity rev", "canonical_claim": "The ref-lease-exclusivity rev.6 proposal left four open questions for review at draft time: whether a 30s `T_mat` unclean-handover penalty is acceptable in production given rustfs/MinIO/real-S3 request-lifetime differences; whether the eager recovery snapshot should also run on clean mounts with a large inherited tail; whether the seal's wedge/single-in-flight invariant may be hard-required via chassert plus release fail-close at the lane level; and confirming that the interim mechanical patch (aged trigger, copy-once replay, threshold 256) lands regardless of the proposal's outcome."}
