{"cluster_id": "chunk_012-094", "topic": "CasPool", "canonical_claim": "CasPool.cpp line 1412's statement that a function has four callers is stale because the classification lists at least six non-test call sites."}
{"cluster_id": "chunk_012-095", "topic": "Files ending in `", "canonical_claim": "Files ending in `.tla` or `.cfg` are never modified or deleted by the CAS documentation consolidation."}
{"cluster_id": "chunk_012-096", "topic": "The Gate D coverage matrix is reviewed by the user before deletion, and `", "canonical_claim": "The Gate D coverage matrix is reviewed by the user before deletion, and `.tla`, `.cfg`, and `KEEP-IN-PLACE` files remain untouched."}
{"cluster_id": "chunk_012-097", "topic": "Internal C++ names such as namespace `DB", "canonical_claim": "Internal C++ names such as namespace `DB::Cas`, classes `CasPool`, `CasGc`, `CasLayout`, and `CasMountRuntime`, and files such as `ContentAddressedLog.cpp` remain unchanged by user-facing naming unification."}
{"cluster_id": "chunk_012-098", "topic": "The TLA+ model sources, active D1 work, the consolidation design and plan, curre", "canonical_claim": "The TLA+ model sources, active D1 work, the consolidation design and plan, current D2 triage, the runfile streaming backlog, and the entire new `cas/` folder are explicitly retained rather than treated as deletion candidates."}
{"cluster_id": "chunk_012-099", "topic": "Until the design questions are answered, DataPartStorageContentAddressed remains", "canonical_claim": "Until the design questions are answered, DataPartStorageContentAddressed remains an architectural alternative rather than an implementation commitment."}
{"cluster_id": "chunk_012-100", "topic": "The standalone `poc/cas_mergetree/` PoC is orphaned, superseded by `ContentAddre", "canonical_claim": "The standalone `poc/cas_mergetree/` PoC is orphaned, superseded by `ContentAddressed/Core/`, referenced by nothing, and is deleted as repository hygiene."}
{"cluster_id": "chunk_012-101", "topic": "`docs/superpowers/cas/AGENTS", "canonical_claim": "`docs/superpowers/cas/AGENTS.md` is a persistent survivor that consolidates branch invariants, test recipes, delegation policy, known traps, and pointers for future agents."}
{"cluster_id": "chunk_012-102", "topic": "Still-valuable, still-actual CAS specifications remain where they are as `KEEP-I", "canonical_claim": "Still-valuable, still-actual CAS specifications remain where they are as `KEEP-IN-PLACE`; other old corpus files are deleted only after coverage review."}
{"cluster_id": "chunk_012-103", "topic": "The spelling indeg_zero in utils/ca-soak/scenarios/BACKLOG", "canonical_claim": "The spelling indeg_zero in utils/ca-soak/scenarios/BACKLOG.md:273 is retained because it is a dated transcription of a past soak run rather than a live event name."}
{"cluster_id": "chunk_012-104", "topic": "The user-visible exception texts containing B16/B34 in DataPartStorageOnDiskBase", "canonical_claim": "The user-visible exception texts containing B16/B34 in DataPartStorageOnDiskBase.cpp and MergeTreeData.cpp predate the naming effort and were intentionally left for a separate cleanup."}
