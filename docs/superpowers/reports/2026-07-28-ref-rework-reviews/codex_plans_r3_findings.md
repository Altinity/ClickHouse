1. NOT-RESOLVED — The Task 7b ordering, soak dependency, and kill-shot expectation are fixed, but Stage B Task 1 still says `RefNamespaceId::stageATransition` is deleted in Task 4, Task 4 says the deletion/zero-grep gate moved to Task 6, and Task 6 contains neither step; moreover, a test-only header cannot override a bare `constexpr` already compiled into production GC, and no test-only template/second-instantiation mechanism is specified. Add the deletion plus zero-grep gate to Task 6 and define a concrete compile-time-only test injection path.
2. NOT-RESOLVED — The per-namespace `life_epoch` semantics, initialization, recovery start, and requested tests are present, but Stage A Task 1 still advertises `validateEpochSealGrammar(const RefLogTxn &)` while defining the contextual overload, and it never replumbs `encodeRefLogTxn`/`decodeRefLogTxn` and their callers to supply `life_epoch` even though validation must run on both paths. Replace the stale signature and specify the context propagation.
7. NOT-RESOLVED — Stage A Task 6 now has both deterministic bump tests and Stage B's self-review correctly assigns the trio to Stage A, but Stage B Task 3 Step 1 still demands “the three fence-bump-between-sites tests,” contradicting its separate generation-plus-token framing. Replace that clause with the explicit generation-stale, token-stale, and both-stale catalog-site cases.
9. NOT-RESOLVED — Stage B Task 2 states the two correct independent `<=` predicates, but its failing-test step still says admission is refused exactly at the additive boundary and covers only that combined predicate. Require, separately for each predicate, accept-at-equality and refuse-at-cap-plus-one tests that assert the named failing predicate.
18. RESOLVED — `publishCkpt` now uses `const Layout &` plus the plumbed fence-check callback with no `CasMountRuntime &`, and the negative overload checks use a genuinely templated dependent concept before `static_assert`.

NEW-1 — Stage B Task 4 and Task 7b each call its modification of `gtest_cas_list_liar_end_to_end.cpp` the one intentional Stage-B edit, although both require distinct staged expectation changes. Describe the two edits explicitly or defer the kill-shot expectation change/removal entirely to Task 7b.

PLANS VERDICT: APPROVE-WITH-FIXES

The Round 3 revision supplies the missing architectural contracts: namespace-local genesis, deterministic three-site recovery fencing, independent capacity predicates, callback-only checkpoint publication, and safely ordered destruction enablement. The remaining problems are localized but must be corrected before execution because the current steps still prescribe incompatible APIs, boundary expectations, temporary-incarnation cleanup, and test edits; once those contradictions and the compile-time test-seam mechanism are made explicit, the plans are implementation-ready.
