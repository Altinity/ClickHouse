1. NEW — Stage A Task 9/13 now inventories and suppresses every GC delete family and its soak is honestly non-destructive, but `docs/superpowers/plans/2026-07-28-cas-ref-chain-stage-a-streams.md` Task 9 never makes `GcTestHooks::force_universe_authoritative` production-unreachable, while `docs/superpowers/plans/2026-07-28-cas-ref-chain-stage-b-catalog.md` Task 4 says “this task flips Stage A's `kUniverseAuthoritative` to `true`, re-enabling production destruction” before Task 5 installs removal and Task 6 installs handle/pre-delete life/fence contracts; Task 4 also requires deleting `RefNamespaceId::stageATransition` before Task 6 replumbs the readers that still need it. Keep the production path compile-time false with no mutable production hook, move the reader plumbing before the grep-clean gate, and flip in a dedicated enablement task only after Tasks 5-6 (preferably the same-rollout Task 7 too) are green.
2. NOT-RESOLVED — Stage A Tasks 1/4/6 now carry a recovered seal into ordinary sequence-1 appends, but they never define per-namespace genesis: `validateEpochSealGrammar(const RefLogTxn &)` has no `life_epoch`, `RefTableRuntime::last_epoch_seal` has no specified genesis initialization, and a brand-new namespace first born at global writer epoch `E > 1` has no predecessor seal to install. Define genesis as the namespace's `life_epoch`, make `last_epoch_seal = none` exactly there, validate sequence 1 contextually (`prev_epoch_seal` required iff `writer_epoch > life_epoch`), start recovery at that epoch, and test first birth at a non-1 writer epoch plus its first transition.
3. RESOLVED — Stage A Task 2 replaces the impossible helper composition with one raw conditional create, at most one raw exact `GET`, caller-side byte adjudication, and per-outcome backend operation-count assertions.
4. RESOLVED — Stage A Task 5 now modifies the real `CasRefLedger::trySnapshotPublishOnce` call path, publishes `_ckpt` after the snapshot body is durable, and requires race/no-op/conflict/fence tests through that call site.
5. RESOLVED — Stage A Task 9 now directly pins all three temporal-lemma arms: same-round no-delete after a post-probe `+1`, SOURCE-BACKED/TOKENED rematerialization with stale-token delete refusal, and TOKENLESS receiver-`+1`-before-source-release ordering.
6. RESOLVED — Stage A Task 9 makes the destructive-call inventory an implementation step, explicitly includes pre-gate `handoff_reclaim`, transports suppression ahead of it, and requires an individual zero-delete test for every discovered family.
7. NOT-RESOLVED — The generation/token distinction is corrected, but the actual trio's tests are not: Stage A Task 6 has only a generic “fence bumped during the walk → install refused,” while Stage B Task 3 vaguely promises “the three fence-bump-between-sites tests” in a creation-lifecycle file that cannot exercise recovery's slot-occupy → `_ckpt` → install trio. Put deterministic bumps after slot occupancy/before `_ckpt` and after `_ckpt`/before install in `gtest_cas_ref_recovery_cas_walk.cpp`, and correct Stage B's self-review claim that Task 3 completes the trio.
8. RESOLVED — Stage A Task 4 requires the exact stored generation/transaction/bytes recheck under `state_mutex` after I/O and adds the blocked-old-result-after-successor-seal regression with no ack, unwedge, or install.
9. NOT-RESOLVED — Stage A Task 8 correctly separates the 64-KiB line cap from the 256-MiB object cap and covers both regular and REBUILD PUTs, but Stage B Task 2 still applies “encoded catalog size + every entry's ... reservation” to both caps and explicitly refuses “exactly AT” the boundary. Specify and test the independent predicates `encoded_catalog <= catalog_object_cap` and `fold_fixed_bytes + sum(reservation) <= fold_object_cap`, accepting equality and refusing cap+1.
10. RESOLVED — Stage A Task 6 adds remount orchestration, a cancel-or-join barrier before rearm, cancellation polling at I/O boundaries, and a deterministic test proving rearm waits and no old `_ckpt`/install mutation follows.
11. RESOLVED — Stage A Task 3b adds an enforced quiescence precondition, terminal-owner check, normal-claim fencing on reuse, and the delayed old-writer refusal integration test required before `STAGE A: PASS`.
12. RESOLVED — Stage B Task 8 now mandates an R3 model gate before C++, with named delete-before-adoption and accounting-contamination sabotages, name-asserting runners, a green control, and separate model commits.
13. RESOLVED — Stage B Task 5 inserts one bounded suppression-aware cleanup attempt between terminal fold and exact `_ckpt`/catalog deletion, defers failures as leak-only janitor work, and requires backend-journal ordering.
14. RESOLVED — Stage B Task 6 now requires catalog-token and fence revalidation before each current per-key `deleteExact` and injects a credential change between two keys.
15. RESOLVED — Stage A Task 14 now fails only wedges surviving an actual foreground-flush/remount opportunity, permits and reports quiet unacknowledged wedges, and asserts that no autonomous retry loop exists.
16. RESOLVED — Stage A Task 9 adds a targeted seam that bypasses the final in-degree read, records the isolated test RED, restores the guard, and records GREEN.
17. RESOLVED — Stage B Task 10 is split into independent `listedTok`, per-model-family driver, runnerless-model, and classifier units with separate commits and before/after evidence.
18. NOT-RESOLVED — Paths are corrected, but Stage A Task 5 still declares `publishCkpt` with nonexistent `CasLayout` and a `CasMountRuntime &` that contradicts its callback-only `CasRefLedger` plumbing, while Stage B Task 1 calls its shown non-dependent `requires` expression “dependent,” so the negative lookup can be a hard compile error. Use `Layout`, make the helper consume the plumbed generation/check callbacks, and define a genuinely templated concept before `static_assert(!Concept<Layout>)`.

PLANS VERDICT: REJECT

The revisions resolve 13 of the 18 round-1 findings and make Stage A's no-destruction posture explicit and testably load-bearing, but they are still not implementation-ready. The ordinary predecessor hand-off remains undefined for a namespace whose first epoch is not global epoch 1, so the strict sequence-1 grammar still has an unusable first-life state. The new Stage B Task-4 flip creates a deletion-capable and even grep-clean-inconsistent intermediate before the lifecycle/read-side safety work that justifies it, and the load-bearing Stage-A test override is not isolated from production. The actual three-site fence tests, Stage-B capacity inequalities/boundaries, and executable type/callback checks also remain incomplete; keep destruction suppressed, repair the task ordering and genesis contract, then re-review before code begins.
