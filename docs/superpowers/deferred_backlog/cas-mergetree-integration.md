# Deferred Backlog — Content-Addressed Shared MergeTree integration

> **Groomed 2026-06-12 (M-W):** DONE and OBSOLETE items moved to
> [`cas-mergetree-integration-archive.md`](cas-mergetree-integration-archive.md) (institutional
> memory preserved there verbatim). This file holds only LIVE items.

Living list of work intentionally **deferred** out of the first milestone, captured during
brainstorming / planning / implementation. Each item records *what*, *why deferred*, and
**where it plugs in** — the last field is the "no architectural dead-ends" proof: choosing the
minimal first step must not foreclose any of these.

Companion to: `docs/superpowers/specs/` (design spec) and `docs/superpowers/plans/` (impl plans).
Source design: `../specs/2026-06-10-ca-incarnation-store-design.md` (incarnation-token protocol; supersedes the v3 PoC design — PoC history preserved in the archive file).

| ID | Item | Why deferred | Where it plugs in (dead-end check) |
|----|------|-------------|-------------------------------------|
| B1 | **Replicated / shared-storage**: replicas reference shared blobs; commit under the existing Keeper `/log`. **MUST coexist SIDE-BY-SIDE with zero-copy replication — zero-copy is NOT removed (backward compatibility, user directive 2026-06-03).** Content-addressing is opt-in per disk (`metadata_type=content_addressed`); existing zero-copy-replicated tables/disks keep working unchanged; the two replication-data-sharing mechanisms live alongside each other and must not interfere. Single-producer via the existing `execute_merges_on_single_replica` picker; a merge-race produces a **benign duplicate** (both blob sets reachable, GC-reclaimed when superseded) — a dedup-miss, **not** a correctness issue. **PROGRESS (CAS replication, M6): fetch-by-relink DONE; the B33 blanket rejection is LIFTED. Single-replica `ReplicatedMergeTree` on CA now does CREATE/INSERT/SELECT/merge/mutation/DROP — verified by 310/350 gated replicated stateless tests passing un-gated (CAS M6 triage, see B33 section). Queue-clone partition ops (B61(a)/Phase-3.2) and the `02486` reattach-readonly bug (B61(b)) are now DONE. RE-VALIDATION (CAS M6 re-triage, 2026-06-04): the STALE "uses ReplicatedMergeTree (B1)" gates on the replication/projection stateless tests were re-checked under the CA-default job — 13 candidates un-gated and re-run; 11 pass un-gated (`01511`, `02597`, `02691`, `03020`, `03047`, `03128`, `03571`, `03715`, `04004`, `04038`, plus `01705`/`01710` correctly un-gated but SKIPPED on CA via their pre-existing `no-object-storage` tag), confirming the B1 reason no longer holds; only `03822_attach_with_unknown_projection` still fails on CA (re-gated, new B64 — orphan-projection-dir lost across DETACH/ATTACH).** | Largest surface; touches commit + replication consensus; consumes B11 pool coordination | Catalog = existing per-replica Keeper `/parts` znode with the **full `ReplicatedMergeTreePartHeader` + `manifest_hash`** appended (header needed for cross-replica divergence detection); commit stays in `commitPart`/`getCommitPartOps` (ABOVE the metadata seam). M1's layout objects + format-version are reused unchanged. |
| B13 | **Migration & mixed-version rollout** | Adoption path for existing tables + replicated-rollout safety | Existing table → CAS: `ALTER TABLE … MOVE PART/PARTITION` (TTL move) to a `content_addressed` disk re-packs via normal-read + CAS-write (the `metadata_type` is per-disk, so a table migrates disk-by-disk), or `INSERT SELECT` into a new table. Replicated rollout: all replicas must run a min-version supporting `content_addressed` before any replica writes it (read-new-before-write-new); the `_pool_meta`/footer/ref **format-version self-check fails closed** to enforce it. |
| B8 | **REPLACE_RANGE multi-ref / MOVE PARTITION; DROP_PART vs concurrent-merge race** | Edge ops in the covering/removal model | Expressed via the existing `/log` entries (replicated) or multi-ref commits; the tombstone-covering primitive generalizes to them. |
| B10 | **#7 one-GET part open** (pack small files into the footer object, serve from an in-memory copy) | Needs a sub-object/in-memory-serve hook in the read path (new surface); `FileCache` amortizes cold opens meanwhile | Read-cost-at-scale optimization on the content-addressed metadata type; `getStorageObjects` resolution already in-memory, so only the byte-serving path changes. |
| B14 | **Expedited / compliance delete** (`DROP … SYNC`-style immediate reclamation bypassing `grace`, for GDPR / right-to-erasure) | M1's `grace` is short (the upload window), so normal deferred-delete is minutes; immediate reclamation is a compliance feature, not a correctness need | Under the GC lock, confirm no reachable footer references the objects, then delete bypassing grace — a second entry point into the same GC sweeper; no layout change. |
| B15 | **Operability**: introspection (`system.*` views for the pool, blob/part refcounts, GC status, outstanding frozen snapshots), GC metrics + coordinator-stall watchdog/alerts, and an optional **freeze TTL** to prevent forgotten-`FREEZE` cost leaks | Tooling; not needed to prove the model | Read-only views over `RefCatalog`/`BlobRefIndex`/GC state; freeze TTL is a retention policy on the `frozen/` ref namespace. No layout change. |
| B17 | **Encryption-at-rest × content-addressing** — dedup scope under encrypted disks (a content hash over ciphertext ⇒ dedup only within the same encryption key) | Edge interaction; not needed to prove the model | Key the blob by the deterministic on-disk (ciphertext) checksum; document that dedup is per-encryption-key. Local to key/hash derivation; no layout change. |
| B25 | **Refs keyed by `ServerUUID` — non-replicated robustness. REAL ISSUE, must be addressed (user directive 2026-06-12).** Namespaces live under `<server_id>/<table_uuid>/…` (the new wiring kept the PoC's `ServerUUID` prefix). If the server's UUID changes (lost/regenerated `server_uuid` file, a cloned/restored server), the refs are orphaned — blobs/trees are intact (global pool) but the table's parts "disappear" until a DR rebuild (list server roots + covering). **Decided direction (user, 2026-06-12): (1) make the server-root path USER-CONFIGURABLE** in the disk config (macros like `{shard}`/`{replica}` usable), replacing the implicit `ServerUUID` prefix — a stable configured root survives UUID regeneration by construction; **(2) add an ownership self-check** since a configured path can collide across servers: a marker subfolder under the configured server root (e.g. `<root>/serverid/`) where the server CREATES a node named by its current `ServerUUID` at startup, then LISTs — **more than one UUID present ⇒ refuse to start** (tell the user to give every server a unique CAS root) **and remove its own marker** before exiting. UUID regeneration then surfaces as a loud startup rejection (own stale marker looks foreign), never silent orphaning; remediation is removing the stale marker — refs are intact under the stable root. | Subtle operational footgun; surfaced by the audit; direction fixed 2026-06-12 | Open details for the design pass: marker lifecycle on crash (a crashed server leaves its marker — fine, same UUID re-claims it; a crashed-AND-regenerated server needs the manual stale-marker removal, or tie staleness to the existing build-heartbeat machinery); replicated case = each replica configures a unique root via macros and the same check enforces it; default when unconfigured stays `ServerUUID` (current behavior) so the check is what guards both modes. Plugs into disk config parsing + `ContentAddressedMetadataStorage` startup (the old PoC `_pool_meta` claim was deleted at M-W — this is its principled replacement at the SERVER-root level, not the pool level: the pool stays multi-server by design). |

_2026-06-13 design question (BLOCKS unattended implementation): the CA pool is SHARED across servers by design (namespaces are `SERVER_ID/TABLE_UUID` within one pool; blobs dedup across replicas — this replaces zero-copy replication). So a literal "reject if the `serverid/` dir holds >1 server UUID" guard would BREAK legitimate multi-server sharing. Need the operator's intent: is the guard meant to (a) detect ACCIDENTAL sharing of a root that was meant to be single-server (opt-in per-disk flag `cas_exclusive_root`?), or (b) just RECORD which servers mount a shared pool (a roster under `serverid/`, no rejection)? Recommend (b) as default + (a) as an opt-in flag. The path-configurability half (macros shard/replica in the disk `path`) is independent and non-controversial — can be done separately. Deferred pending this decision; do NOT implement the rejection guard blind._
| B26 | **Storage-backend orthogonality (local / NFS / shared-fs), explicitly supported & tested.** The content-addressed metadata storage is generic over `IObjectStorage` (the Phase 1–3 unit tests already run it over `LocalObjectStorage`), so `metadata_type = content_addressed` composes with **any** `object_storage_type` (`s3`, `local`, `azure`, …) — incl. a plain local directory or an NFS/shared-fs mount, giving content-addressed dedup/no-rename benefits without S3. Make this a **first-class, documented, tested** configuration, not just an incidental property. | Validated only at the unit level over `LocalObjectStorage`; not yet exercised in a running server or documented as supported | Caveat to harden: **put-if-absent atomicity** is read-after-write on S3 but a local existence-check+write on `LocalObjectStorage` (racy multi-writer; NFS create atomicity is weaker) → safe single-writer (M1), multi-writer needs B11/B7. Verify `LocalObjectStorage::writeObject` is crash-safe (write-temp-then-rename) for blob durability. No layout change — same objects, different backend. |
| B31 | **Explicit capability set (honest advertisement) + reject-unsupported at `CREATE`/`ATTACH`.** `DiskObjectStorage::supportsHardLinks`/`supportZeroCopyReplication` make `content_addressed` look more capable than it is. | Review P2; this IS the Phase-5 feature gate, sharpened — makes the disk honest+safe | Advertise explicitly: **no** zero-copy replication, projections, partition clone, external ATTACH, patch parts, or multi-mounter pool. Reject those at `CREATE`/`ATTACH` (incl. projection/nested table metadata — see B5). Enable each capability only when its storage contract (B30) exists. |

## M6-era live items (PoC-era findings still relevant on the new wiring) {#m6-era-live}

> Context for these rows (the M6 triage logs they came from) lives in the archive file's
> historical-narrative section.

| ID | Item | Why deferred | Where it plugs in (dead-end check) |
|----|------|-------------|-------------------------------------|
| B48 | **`clickhouse local` with a CA disk may not exit cleanly (shutdown hang).** During M6 triage, several `clickhouse local --queries-file <repro.sql>` invocations using an inline `content_addressed` disk were found hung for **3–21 hours** (orphaned processes), where the query had completed but the process never exited. Server-mode (praktika) tests shut down fine, so it is **local-mode-specific** — likely a background `BackgroundSchedulePool`/GC-thread or `_pool_meta` resource not joined/released on `clickhouse local` teardown (or a `DETACH PARTITION ALL` repro hang). | Surfaced while reaping orphaned processes after the overnight run; not caught by tests (they timeout-kill, masking a hang as a normal kill) | Investigate `LocalServer` shutdown vs the CA metadata storage's `shutdown()` (GC thread `task->deactivate()`); ensure the GC thread/pool task is reaped on local exit. Until fixed, treat `clickhouse local` + CA as needing an explicit process kill. Possibly relates to the GC-thread lifecycle (M5.2/Task A). |
| B54 | **Potential CA mutation bug (data-correct, pruning-efficiency only): secondary-index granule/part pruning after a mutation differs on CA.** Surfaced by `03702_alter_column_update_and_delete_with_secondary_index_rebuild` under the CA-default config: after an `ALTER … UPDATE`/`DELETE` that rebuilds a `set` skip index, `EXPLAIN indexes = 1` shows the index pruning `Parts: 1/2, Granules: 1/2` on CA vs the expected `0/2, 0/2` (one part is not pruned). The query result is CORRECT (`count() = 0` either way) — only the skip-index pruning differs, so a query reads one extra part. Likely cause: the rebuilt/ carried-forward skip-index file (`skp_idx_*.idx` + its marks) for a mutated part on CA is stale or not consulted for pruning on one of the two parts. Re-tagged `no-content-addressed-storage` pending investigation. **INVESTIGATED — NOT a CA bug (closed).** A controlled CA-vs-plain reproduction (same schema, same `alter_column_secondary_index_mode='rebuild'` mutation, same explicit settings, an inline `content_addressed` disk vs the default disk) produced **byte-identical** `EXPLAIN indexes = 1` output (`Skip idx_v → Parts: 1/2, Granules: 1/2`) and identical results — so a content-addressed part resolves and prunes a rebuilt secondary index exactly like a plain part. The `03702` failure under the CA-default config is **environment/part-layout sensitivity**: that config applies *random* merge-tree settings, which produce a different merge/part state than the committed `.reference` assumes, changing which parts the skip index prunes. Not content-addressing. The test stays `no-content-addressed-storage` (environment-sensitive under the random-settings CA-default config), like other layout-sensitive tests. | Found in CAS M7 Task 7; investigated post-M7 — controlled repro shows CA == plain, so reclassified from "potential CA bug" to "not a CA bug / config-sensitivity" | **RESOLVED:** no code change — CA index resolution matches plain. Tag retained as environment-sensitive. |
| B66b | **(CAS FETCH, 2026-06-04) Relink-into-detached deferred — the same-pool FETCH optimization (move no bytes).** A `to_detached` FETCH currently STREAMS bytes (B66 forces the byte path) even when the source part lives in the SAME content-addressed pool, where it could be published by REFERENCE (relink) at zero byte cost — exactly as a same-pool active fetch already does (CAS replication 2b, `relinkPartToDisk`). The relink path was wired for ACTIVE fetches: it stages at `getRelativeDataPath()` (the active part path) and ignores the `to_detached` parameter, so a detached relink would publish into the active namespace, not `detached/`. **Where it plugs in:** extend `Fetcher::relinkPartToDisk` / `MergeTreeData::relinkExistingPart` (the receive-side relink branch in `DataPartsExchange.cpp` ~L700-725) to honor `to_detached` — publish the relinked ref into the `detached/<tmp-fetch_part>/` namespace (mirroring `republishCommittedPartIntoDetached`'s detached-ref publish, but from the sender-advertised same-pool source part id rather than a local source manifest), then re-advertise the relink capability for `to_detached` (revert the `&& !to_detached` guard added in B66). Until then a detached fetch always byte-streams (correct, just not free). | Decided during the CAS FETCH design (byte-fetch first, relink-into-detached later) | **FIX:** teach the relink receive path to publish into `detached/` and lift the `!to_detached` advertise guard. Files: `src/Storages/MergeTree/DataPartsExchange.cpp` (`fetchSelectedPart` advertise + the relink-cookie receive branch / `relinkPartToDisk`), `src/Storages/MergeTree/MergeTreeData.cpp` (`relinkExistingPart`). |
| B66a | **OPEN (tracked 2026-06-05; promoted from a residual note inside B66).** Concurrent-FETCH fan-out torn read of the shared `detached` ref. `03350_alter_table_fetch_partition_thread_pool` (fetches a 100-part partition CONCURRENTLY via the FETCH thread pool) stays `no-content-addressed-storage`. The parallel fetches all REWRITE the one shared `detached` ref / sidecar-bundle pointer object; the READ side of that hot mutable object is not serialized against the truncating in-place rewrite of `LocalObjectStorage::writeObject` (open-with-`O_TRUNC`, no atomicity), so a concurrent reader (e.g. the per-part `loadColumnsChecksumsIndexes` right after its own commit, or `system.detached_parts`) can observe a TORN ref/manifest → `CANNOT_READ_ALL_DATA` (`partIdFromRefPayload` reading 0/4 header bytes) or `NO_FILE_IN_DATA_PART`. **Local-`LocalObjectStorage`-only — S3 PUTs are atomic** (an S3-backed pool would not tear). | Found running the un-gated FETCH tests under the CA-default job (CAS FETCH probe, 2026-06-04): `01650` (single-part) passes, `03350` (100-part concurrent fan-out) tears the shared detached ref | **FIX:** make the small mutable pointer-object publish ATOMIC so a concurrent reader sees old-or-new, never torn — write the ref + sidecar-bundle to a unique temp key then atomic-rename into place (a `moveObject`/rename primitive on the object storage; `LocalObjectStorage` backed by `std::filesystem::rename`), OR serialize the SHARED-`detached`-ref reads under the same per-pool `gc_lock` the commit RMW now holds (a non-recursive locked/unlocked split of `readRefPartId`/`loadPartManifestOrThrow`/`readRefSidecarIfExists`). Files: `src/Disks/.../ContentAddressed/ContentAddressedMetadataStorage.cpp` (reads), `ContentAddressedTransaction.cpp` (commit/republish), `src/Disks/DiskObjectStorage/ObjectStorages/Local/LocalObjectStorage.cpp` (atomic write). |
| B79 | **DEFERRED — CA-aware tiered offload: derive the blob key from the source `checksums.txt` and stream directly to S3 (no scratch spill, single read pass).** Today a cross-disk move TO a CA disk (`ALTER … MOVE PART/PARTITION TO DISK/VOLUME`, TTL move) goes `DataPartStorageOnDiskBase::clonePart` → `copyDirectoryContent` → `ContentAddressedWriteBuffer`, which (a) **recomputes** the CityHash128 content key from the bytes instead of reusing the source part's known per-file `file_hash`, and (b) **spills every file to a server-local scratch temp** (`local_scratch_path`) before uploading — because content addressing needs the hash *before* the object key, so it materializes locally to learn the key. For the canonical "offload to S3 to free a near-full local disk" case this is doubly wrong: the move transiently consumes local scratch on the very disk being drained (can `ENOSPC` the offload if scratch shares that disk) and does `read-local → write-scratch → read-scratch → PUT-S3` instead of a single `read-local → PUT-S3`. **Fix:** a CA-aware offload path that takes the blob key from the source `checksums.txt` `file_hash` (CityHash128 over the same stored bytes the blob gets) and streams each file **directly** to `blobs/<H>/0` — no scratch, no second read, single pass off the dying disk. **Verify-while-streaming is ON by default** (hash inline as the bytes stream and `chassert` the computed digest equals the claimed `file_hash` — still single-pass, no scratch, just CPU; preserves the self-validating "key always matches bytes" property); a config flag (e.g. `content_addressed_trust_source_checksums_on_move`, default OFF = verify) can disable verification to also skip the rehash CPU when the source checksums are trusted. **Coverage:** files NOT in `checksums.txt` (`checksums.txt` itself + a few small metadata files) still need an inline hash — cheap, they're tiny, hash in-memory without scratch; the mutable per-part files (`uuid.txt`/`txn_version.txt`/`metadata_version.txt`) go to the sidecar and are out of scope. **Same-pool variant:** when the move's source is ALSO this CA pool, prefer a metadata-only relink (reference the existing `part_id`) over any copy — the local analogue of the replication fetch-by-relink. | Surfaced during the post-review checksum discussion: the write path always computes its own hash + stages through local scratch (verified — `ContentAddressedWriteBuffer` ctor spills to `local_scratch_path`, `finalizeImpl` hashes then uploads), which is a genuine defect for the disk-offload use case (transient local space on the disk being freed + redundant local I/O), not just a CPU rehash | Plugs into the cross-disk move/clone path: `DataPartStorageOnDiskBase::clonePart` / `copyDirectoryContent` → a CA-aware offload that reads the source part's `IMergeTreeDataPartChecksums::file_hash` and streams to `blobs/<H>/0` via a direct (no-scratch) write into `ContentAddressedWriteBuffer` / `ContentAddressedTransaction`; verify-inline by default, `chassert` digest == claimed hash; same-pool case routes to the relink primitive (`ContentAddressedMetadataStorage::relinkExistingPart`, the replication-relink analogue). PREREQUISITE TO CONFIRM: that `file_hash` is byte-range-identical to the CA blob hash for every written file kind (the open question from the checksum discussion). |
| B80 | **DEFERRED — `DataPartStorageMergedIterator::isFile()` inaccurate for staged-only entries.** After the 2026-06-05 projection-dir read-back work, `DataPartStorageOnDiskFull::iterate` merges committed + in-flight staged children, but `DataPartStorageMergedIterator::isFile()` still uses the committed `disk->existsFile`, so a staged-only entry reports `isFile()==false`. Harmless today: the only consumer under an open txn (`getMarksTypeFromFilesystem`) reads only `name()`/`isValid()`. Tighten (consult the txn's in-flight staging) or `chassert` if another caller iterates a part dir under an open transaction and relies on `isFile()`. | Review follow-up (Phase A projection-dir read-back, 2026-06-05) | `DataPartStorageMergedIterator::isFile()` — consult the held transaction's in-flight staging like the sibling overlays. | [TRIAGED 2026-06-13: latent, not green-blocking, deferred — see unattended report]
| B82 | **DEFERRED (known limitation) — CA `tryCommit` retry loses in-memory staging.** On a `tryCommit` retry the disk layer resets `metadata_transaction`, but CA staging (`recorded`/`recorded_mutable`) lives in the transaction's in-memory maps with NO `operations_to_execute` entry to refill it (true for CA `writeFile`, and now CA `createHardLink` after the 2026-06-05 eager-staging change). A retried CA commit would publish empty/partial staging. Pre-existing for `writeFile`; document as a known CA-commit-retry limitation or make CA staging replayable across a `tryCommit` reset. | Review follow-up (Phase A); pre-existing, shared with CA `writeFile` | `DiskObjectStorageTransaction::tryCommit` retry vs CA in-memory staging (no `operations_to_execute` refill). |
| B89 | **DEFERRED (refinement of H3) — the mutable-only-commit concurrent-ref-drop still `std::terminate`s via the `afterCommit` `noexcept` boundary.** H3 (commit `c289069f126`) downgraded `commitOnePart`'s mutable-only "no existing ref" throw from `LOGICAL_ERROR` to `FILE_DOESNT_EXIST` (the recoverable code, removing the explicit CI abort). But this path is reached via `MergeTreeTransaction::afterCommit` (and `setAndStoreCreationCSN`), which is `noexcept` — so ANY exception thrown there (old `LOGICAL_ERROR` or new `FILE_DOESNT_EXIST`) calls `std::terminate`. So a genuine concurrent DROP/DETACH/merge unlinking the ref mid-mutable-commit still terminates the server (same as the plain disk, which would `ErrnoException`-terminate there too). LOW probability (did not trigger the full CA-local run). **True fix:** in the mutable-only branch, on a concurrent-ref-drop treat it as a recoverable NO-OP (the part is being removed; the `txn_version` rewrite is moot) RATHER than throwing into the `noexcept` boundary — OR detect the dropped ref before entering the `noexcept` commit path. Needs care (a silent no-op could mask a genuine non-race ref inconsistency; weigh vs the terminate). | B87-completion + hardening review (2026-06-06): H3 removed the explicit LOGICAL_ERROR but the `noexcept afterCommit` still terminates on this race | `ContentAddressedTransaction::commitOnePart` mutable-only branch (the `FILE_DOESNT_EXIST` throw) vs `MergeTreeTransaction::afterCommit` `noexcept`; make the concurrent-ref-drop a recoverable no-op or pre-check the ref before the noexcept commit. |

## Incarnation-core era (M-C1..M-C3, 2026-06-10..12) {#incarnation-core-backlog}

**SUPERSESSION NOTE:** the incarnation-token core (`docs/superpowers/specs/2026-06-10-ca-incarnation-store-design.md`,
implemented under `src/Disks/.../ContentAddressed/Core/` through milestones M-C1..M-C3) REPLACES the
s1-s4 GC design (now in the archive file's historical-narrative section). Old items that live inside the generation/epoch machinery (notably **B85**
read-path 404-repair — the new read path is INV-NO-DANGLE fail-closed, no repair; the `gc_lock`/
write-session/epoch-fold items in B88-B90) are superseded: their *intent* (no dangles, no lost writes)
is carried by the new protocol's gates, their *mechanisms* are deleted at M-W with the PoC. Items below
are the new core's own deferred work. Milestones: **M-W** = cutover (wiring rewrite, PoC deletions,
RustFS CI), **M-F** = full-GC walk / debris / packs / observability.

| ID | Item | Why deferred | Where it plugs in (dead-end check) |
|----|------|-------------|-------------------------------------|
| B104 | **Verification residuals after the B91 refresh.** (a) `CaIncarnationProofCore.tla` (Apalache inductive-invariant rung) predates the registry/evidence amendments and `ViewableRound` — STALE; re-derive `IndInv` against the refreshed semantics or mark the §12 Apalache paragraph historical. (b) Registry × split-brain and registry × evidence combined TLC configs not run (state space); each amendment is checked against the full single-leader round only. (c) One model shard = one namespace; per-namespace `root_shards > 1` not modeled. (d) The model's hit checks read the live `retired` set — weaker than the implementation's frozen LIST snapshot (conservative direction, documented in the README). | Each is a bounded-verification scope cut, not a code gap | Cheapest first: re-run the Apalache recipe after porting the amendments into the proof core; combined configs may need ladder-style reduced bounds. |
| B92 | **M-W: adopt-published `tree_size` = 0** (`CasBuild.cpp` `adoptTree`, FOLLOW-UP comment). FREEZE / detached-reattach / relink parts publish `RefPayload.tree_size = 0`. Harmless in-core (no read consumes it; `readTree` GETs whole, `locate` uses per-entry sizes) but `Resolved.tree_size` exists so the wiring can build `StoredObject`s without a HEAD (spec §6). | Needs nothing until the wiring consumes it | Recover the size on the adopt path (subtree entries already carry it; whole-tree adopt = one HEAD or caller-supplied) + an adopt-republish round-trip test. Pinned at the code site. |
| B93 | **M-W: RustFS replaces MinIO in CI** (user directive 2026-06-11). MinIO OSS silently ignores `If-Match` on DELETE (probed; archived upstream); RustFS 1.0.0-beta.8 passes the full conditional battery (quoted + unquoted ETags; rustfs#1458 fixed). Also covers the only e2e-unexercised seam: `ObjectStorageBackend` Native-mode streaming 412 (typed `S3Exception::getExceptionName` catch is unit-tested; the true S3 streaming path is not). | Test-infra change scoped into the cutover milestone | Stateless CA-default job + CA integration fixtures switch images; the fail-closed capability probe gates any backend swap. Fixture details (image pin, compose, credentials) decided at M-W. **DEGRADATION FINDING (T13 full lane, 2026-06-12; NOT "overload shedding" — request load is roughly constant while symptoms GROW):** two distinct RustFS failure modes under the 19-way-parallel suite. (a) Transport-level: a constant, monotonically growing stream of `Broken pipe` failures on blob GET/PUT (62/min at run start → ~550/min after an hour; 18k+ total; ClickHouse retries up to attempt 60+, some queries stall past the 600 s test timeout — the hung clients' SIGTSTP dumps look like crashes in the lane log but the server never died). (b) Periodic burst windows (~every 5–10 min, 1–3 min long) of `503 ServiceUnavailable` whose per-request durations are UNIFORM within a burst (6.6 s / 8.5 s / up to 18 s) — deadline-shaped, i.e. an internal lock/queue wait timing out, not gradual saturation (binary strings: `NamespaceLockQuorumUnavailable`, "Failed to acquire lock"). RustFS process state after 1 h: 121% CPU sustained, 1.2 GB RSS, pool at 177k objects / 5.5 GB. Big GC delete rounds do NOT correlate with the bursts (largest rounds fell in quiet windows). Prime suspect for both the CPU burn and the periodic lock stalls: the MinIO-heritage data-scanner/heal subsystems walking the growing object set. **ROOT-CAUSE CONFIRMED + FIX APPLIED (2026-06-12):** an idle RustFS at `RUST_LOG=info` logs `Background services configuration: scanner=true, heal=true`, starts a `HealManager` whose `start_auto_disk_scanner` runs every **10 s** (the periodic-503 cadence), and a `rustfs_scanner` that walks the whole object tree (the CPU-grows-with-objects part). Setting `RUSTFS_SCANNER_ENABLED=false RUSTFS_HEAL_ENABLED=false` (the `RUSTFS_ENABLE_*` forms are deprecated in beta.8) makes it log `scanner=false, heal=false` and `Both scanner and heal are disabled, skipping AHM service initialization` — no auto-disk-scan, no tree walk. Safe for a single-disk ephemeral test pool (no parity to heal; bitrot/usage accounting on throwaway data is pointless; client GET/PUT/LIST/DELETE never consult either service). FIX: `start_rustfs` in `ci/jobs/scripts/clickhouse_proc.py` now prefixes the launch with both env vars. Validation: full CA-S3 lane re-run with B115 + this fix (compare in-situ vs the documented unfixed numbers — 572 503s / ~120% CPU / ~50 timeouts). **GARAGE EVALUATED + REJECTED (2026-06-12):** considered Garage as a lighter alternative (no MinIO-heritage scanner). Ran the CA conditional-op battery against single-node Garage v2.0.0 (`tmp/garage_probe.sh`); a byte-identical battery against standalone RustFS was the positive control (`tmp/rustfs_probe_control.sh`). Garage **accepts but IGNORES** `If-None-Match: *` and `If-Match` on both PUT and DELETE — a wrong-`If-Match` DELETE returned 200 and **destroyed the object** (RustFS returns 412 and the object survives, on the identical calls). Garage is therefore a NON-STARTER for the CA backend, same class as MinIO but worse (silent data loss on a wrong-token GC delete would violate INV-NO-RETURN). RustFS stays. Decision is closed — do not revisit Garage unless a release lands enforced conditional DELETE. NOTE/lesson: do not run heavy host-side experiments (Garage bootstrap, extra RustFS instances, large `strings` scans) DURING a validation lane — RustFS 503s are 10 s lock-acquisition deadline timeouts, so host CPU starvation induces them; an in-progress validation lane showed 503s during the experiment window that then ceased and did not recur once the host went quiet. | [LANE-STABILITY RESOLVED 2026-06-13: RustFS migrated + scanner/heal disabled (2a1f62bcf59); the timeout/broken-pipe storm that motivated B93 is fixed by B116 (read pipeline) + B118 (Expect:100-continue) — full CA-S3 lane 10388 OK / 20 FAIL (all non-CA). RustFS-side robustness tracked separately in B119.]
| B94 | **M-F: full-GC walk + debris reclaim + checkpoints.** LIST-driven authoritative walk (coherent cut vector), debris classification via build heartbeats (GC-observed staleness, no writer clocks), `gc/checkpoint/<n>` + `checkpoint_generation` (CAGS field reserved), outcome-log trimming after checkpoint inclusion (logs currently accumulate per round — small JSON objects, unbounded count). | Regular GC structurally cannot see debris/drift; spec §8 is designed, API slot reserved in `CasGc.h` | The retire→fence→recheck tail is SHARED (no second deletion path, spec §8); the walk feeds the same machinery. Also sweeps: orphaned snap generations (abandoned probe-upward partials), epoch-orphan retired sets (B95), registry entries of dropped namespaces (`dropNamespace` leaves registration; removal goes with final-manifest deletion). |
| B95 | **M-F: epoch-orphan retired sets.** After a leadership steal (`fence_seq` bump), the old epoch's still-undropped retired sets are never probed by the new leader's resume — they linger, condemning their hashes (conservative: forces resurrect-on-reuse per publish, a cost/liveness leak, never a wrong delete). Documented at `Gc::tryResumeIncompleteRound`. | Cross-epoch cleanup needs the full-GC walk's authority (or a bounded epoch scan) — out of regular-round scope | Full GC lists `gc/retired/` wholesale and drops sets whose outcome logs exist (or re-runs their tail). The lingering direction is fail-safe meanwhile. |
| B96 | **M-F: `snap_shards > 1` needs a displacement design.** Last-op-wins root-edge re-pointing is intra-shard; with the displaced and displacing trees in different snap shards a republish would leak the old edge forever. `Gc::fold` refuses `snap_shards != 1` (NOT_IMPLEMENTED, fail-closed). | Default 1 shard is correct-by-construction for M-C3 scale; cross-shard re-point needs per-ref displacement records or ref-identity-sharded root edges | The sharding axis (target-hash prefix) and all key shapes are already wired; only the fold's re-point logic generalizes. Spec §4 carries the constant. |
| B97 | **M-F: packs production + repack.** `PackSlice` placement is encoded, validated, folded (pack edges), and locatable since M-C1 — produced by nobody. Repack = a normal writer operation (spec §3). | Middle-band optimization; needs no protocol change | `Build` gains pack assembly; GC already accounts one edge per distinct referenced pack. |
| B98 | **M-F: server-side copy-to-self resurrect for huge blobs** (`CasBuild.cpp` resurrect, FOLLOW-UP comment). Current resurrect is GET + header-rewrite + `putOverwrite` — correct for all sizes, expensive for huge blobs. | Optimization; `x-amz-copy-source-if-match` hygiene path per spec §5 step 2 | Drop-in replacement inside `Build::resurrect`; semantics identical (fresh tag, exact-token guard). |
| B99 | **M-F: GC observability.** `system.*` views over `RoundReport` (deleted/absent/replaced/spared/cascaded), retire backlog depth, 412-save rate (the health metric, spec §7), registry/universe size, snap generation lag. | Needs the wiring layer (M-W) for table plumbing | All counters already exist in `RoundReport`/outcome logs; pure surfacing. |
| B101 | **Keeper acceleration (post-M-F, optional).** Ephemeral-sequential leader election instead of lease polling; per-build ephemerals instead of heartbeat objects. Zero protocol divergence; total Keeper loss degrades to the S3-only baseline (INV-S3-COMPLETE). | Spec decision: S3-only is the v1 baseline and is complete without Keeper | Swappable inside `acquireOrRenewLease` + `HeartbeatKeeper`; nothing else changes. |
| B102 | **Size-bookkeeping consistency (minor).** Tree retired-entry `size` = whole object; `RefPayload.tree_size` = payload only; `dep.size` = 0 for trees/adopted (all documented as forensics-only — no protocol decision reads sizes). Matters when B99 aggregates bytes-reclaimed. | Non-load-bearing in M-C3 | Normalize when outcomes feed observability (one definition: logical payload bytes). |
| B103 | **Fence/persist round-cost optimizations (minor).** Each round: registry CAS + (#namespaces × root_shards) manifest CASes (even idle) + write-all-snap-shards on changed generations. Noted at the code sites: skip-unchanged-namespace fencing needs care (the fence IS the ordering primitive — only skip when provably no publish since the last fence), skip-byte-identical snap shards is safe. | Rounds are minutes apart; correctness-first v1 | Pure `Gc::fence`/persist internals; no format change. |
| B105 | **FIXED by B116 (`d8fd11659df`, 2026-06-13).** CA blob reads now route through the STANDARD object-storage pipeline (gather + FileCache/page-cache + async prefetch) bounded by a `ReadBufferFromFileView` stage, so cache stages DO see CA blob reads (the in-manifest tiny-bytes path stays correctly memory-served, uncached). Full CA-S3 lane green (no wrong-results from caching). Original below. `ContentAddressedMetadataStorage::prepareReadPipeline` sets a custom buffer source (in-manifest bytes from memory; blob payloads through a `ReadBufferFromFileView` skipping the 256B CHCA header), so FileCache / page-cache stages never see CA reads. Parity with the PoC (the CA-S3 disk ran cacheless, B86), but now structural: the proper fix is the upstream `StoredObject` range plumbing (spec §13 integration delta) — once a StoredObject can carry (offset, length), CA returns plain ranged objects and the standard pipeline (incl. caches) applies. | StoredObject has no range field upstream; touching the generic read stack was out of M-W scope | Revisit when CA-S3 perf wants caching; one focused upstream PR (StoredObject + gather + cache key shape). |
| B106 | **AUDITED — not a bug (2026-06-13).** The in-manifest placeholder (`StoredObject{remote_path="", size}`) is correct for size consumers and fail-LOUD for byte readers, and the real consumers never read bytes from it: BACKUP reads via `BackupEntryFromImmutableFile::getReadBuffer` -> `disk->readFile` -> CA `prepareRead` (in-manifest bytes served from memory, blob bytes via `getBlobViewPlan`), NOT via the placeholder; `system.remote_data_paths` only reports the (empty) path. EVIDENCE: the CA BACKUP tests `04284_content_addressed_backup_pointer_holding` and `05005_content_addressed_backup_restore` PASS in the full CA-S3 lane. The only path that would touch the placeholder is checksum-from-remote (`allow_checksums_from_remote_paths`), which the immutable-file entry computes from `readFile` when no remote checksum is available. No fix needed. Original note below. Mutable per-part files / inline entries / verbatim namespace files return `StoredObject{remote_path="", size}` — correct for size consumers, fail-loud for any byte reader that bypasses `prepareReadPipeline`. Consumers to audit at/after T13: BACKUP of a CA disk, `system.remote_data_paths`, checksum collectors. | No second read mechanism exists for manifest-embedded bytes; loud beats silently wrong | If a legitimate consumer surfaces, either teach it `tryGetInManifestBytes` or land B105 and inline-as-objects. |
| B107 | **M-W: verbatim-file mtimes are epoch 0.** The new wiring keeps no per-object mtime for namespace files (`getLastModified` on table-level/generic files returns 0; parts use the `.ca_mtime` publish stamp). The PoC reported the object's real mtime. Anything age-gating table-level files (stale `tmp_mutation_*` cleanup?) sees "infinitely old". Watch at T13. | No metadata channel kept for verbatim files; the stamp pattern covers parts only | If a real consumer needs it: stamp verbatim writes (a sibling `.ca_mtime` map per namespace, or object metadata reads). |
| B108 | **M-W: thin spots to exercise at T13 (recorded, not fixed).** (a) The `DiskObjectStorageTransaction` CA glue — autocommit mutable one-shots via `WriteBufferWithFinalizeCallback`-wrapped commit, the buffer-capture commit flow — is exercised only at the SQL level (no unit harness for the disk transaction); (b) `prepareReadPipeline` itself first runs under a real server; (c) Native-mode (S3) conditional writes e2e only against RustFS (recorded in core comments); (d) empty-content-file round trip untested at the wiring tier; (e) deliberate fail-loud stubs remain: `generateObjectKeyForPath`, `createMetadataFile`, `chmod` (NOT_IMPLEMENTED — T13 surfaces any real caller). | The unit tier cannot reach these seams; T13 is their gate by design | Triage loop at T13; add wiring tests for whatever it surfaces (the (d) empty-file test is cheap — add with the first T13 fix batch). |
| B109 | **M-W: independent review debt.** The whole M-W wiring delta (T1-T12, ~15 commits incl. the −16,951-line structural swap) has had NO independent review — the org spend limit killed the only subagent attempt (B91's reviewer died at 0 tokens), and everything since was self-review. The B91 model refresh got the user's external review; the M-W wiring has not. | Spend limit; direct-execution mode | Before or alongside T13: an external review of the wiring delta (`git diff 1022cbd251f..` over src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed + the disk/exchange seams), focused on PoC-semantics parity (the dispatch table) and the new-layout changes (per-part detached refs, shadow namespaces, in-manifest reads). |

_2026-06-13: PARTIAL — the session's high-stakes changes (B113 caches, B116 read pipeline, B117 cancellation, B118 Expect:100-continue) got an independent adversarial subagent review; it confirmed 12 areas safe and surfaced 3 findings (2 fixed `c1a26be818b`, 1 → B120). The broader M-W wiring delta (T1–T12) still lacks independent review — the write/commit path (`ContentAddressedTransaction`) ALSO got a focused adversarial review (2026-06-13): it VALIDATED autocommit one-shots (content blobs can't autocommit; finalize-once; cancel-never-publishes; truncated-blob guard), createHardLink carry-forward (gate re-validates, no wrong-bytes carry, no re-upload), same-name-rewrite-in-one-txn (clean replace), and the no-throw destructor; and surfaced commit-atomicity / verbatim-RMW gaps now tracked as B122/B123/B124. The rest of the M-W wiring (reads, GC scheduler, path parsing) is exercised by the green lane but not yet independently line-reviewed._
| B110 | **M-W: verbatim Append reads existing bytes at writeFile time, not finalize.** The read-modify-rewrite for `WriteMode::Append` (MVCC mutation-entry CSN append) captures the existing bytes when the buffer is CREATED; a concurrent append between create and finalize would be lost. Single-writer-per-table in practice (mutation entries are written under table locks), and `putNamespaceFile` is token-conditional so the race would surface as a retry-overwrite, not corruption — but the window exists. | Matches the practical single-writer contract; finalize-time read needs the buffer to re-read inside the callback | Move the existing-bytes read into the finalize callback (cheap) with a CAS loop on the observed token. | [TRIAGED 2026-06-13: latent, not green-blocking, deferred — see unattended report] _(write-path review F3 review note 2026-06-13: the append branch is reached for ANY non-part file opened Append — table-level mutation entries AND the genericNamespace fall-through — not only mutation entries; it is the one CA write with NO token/CAS guard, fail-open lost-update.)_
| B111 | **M-W: RENAME TABLE republishes ref-by-ref with one Build each.** `moveDirectory(table)` calls `republishRef` per part — each opens a Build (heartbeat object churn) and does adoptTree+publish+dropRef. A 10k-part table rename = 10k Builds + 30k+ ops. Correct but heavy. | Correctness-first; renames are rare | One Build for the whole rename (adoptTree per part under it; publish is per-ref CAS anyway), or a core-level bulk-republish. |
| B112 | **The lease/pacing layer is outside the TLA+ model — by design for SAFETY, but its LIVENESS contract is unverified.** `CaIncarnationCore.tla` proves the round safe with NO leadership assumption (stage 5 split-brain: concurrent leaders are safe), so the lease was deliberately unmodeled. But `NoLeakForever` grants fairness DIRECTLY to GC actions — it assumes rounds run; the lease layer can starve that assumption invisibly (the M-W retro bug: a fresh `gc_id` per call never accumulates the observation window ⇒ every tick backs off ⇒ zero rounds, silently). The caller obligation ("the observer must be stable across calls") lives only in `CasGc.h` comments. Current guards: the strengthened wiring test (rounds 2-3 must run), the core lease battery (protocol-as-contracted), and scheduler back-off observability (periodic INFO when never leading). | Safety needed no leadership; liveness modeling was scoped out with the rest of NoLeakForever's bound limits | Option: a small focused `CaLeaseLiveness.tla` (lease {owner,seq}, contenders with remembered observations, renew/observe/steal/die; liveness: dead leader ⇒ eventually some contender leads; negative control: an identity-forgetting contender never steals — exactly the retro bug as a counterexample). Cheap (tiny state space); decide alongside B104's verification residuals. |
| B113 | **FIXED (`d1590609597` shard cache + `47ec3241d53` tree cache, 2026-06-13).** Per-read shard-manifest + tree decode (~2x on read-heavy). FIX: token-validated decode cache for shard manifests (`Store::readShardDecoded`, head-validated, immutable shared_ptr; resolveRef/listRefs use it, writers stay uncached) + content-addressed tree decode cache (immutable, bounded). MEASURED CA-local read-heavy: 2.66x -> 1.51x vs plain (read time -44%). New gtest `ResolveDecodeCacheInvalidatesOnWrite`; CA battery 224/224. Residual 1.51x is inherent per-read overhead (head probe, per-file blob GETs), not decode. Original below. Every wiring read resolves ref->tree via `resolveRef` (GET + strict-JSON decode of the WHOLE shard manifest - which holds many parts' refs) + `readTree` (GET + decode). `00139_like`: 228s plain vs 456s CA-local. The PoC decoded one compact per-part binary manifest. Compounds with B105 (no cache stages). | Correctness-first wiring; no caching layer yet | A small wiring-side resolve cache keyed (ns, shard) -> decoded RootShard, invalidated by shard_version (one HEAD or carried from the last CAS); plus tree-decode memoization per TreeId (immutable - safe forever). Natural M-F optimization. |
| B114 | **Debt/consider: selective post-upload size check for CONTENT objects.** The T13 fix (`1f0c61bac73`) skips `check_objects_after_upload` for ALL CA Native writes because on CAS-MUTABLE keys (manifests, gc/state, registry) it races a concurrent conditional PUT by design (the observed server terminate). But CONTENT objects (blobs/trees/packs) have a DETERMINISTIC size — `blob_header_len + payload` — and even a racing resurrection preserves it (fresh tag, identical payload, fixed header), so the check would be race-safe there and could be re-enabled selectively (one `casWriteSettings` variant for content uploads). Doubtful value (user assessment): the conditional PUT's 2xx already confirms the upload; corruption is caught on read by the envelope `header_hash` + compressed-block checksums; the check costs one HEAD per upload. | The blanket skip was the correct hotfix for the terminate; the selective question needs a cost/benefit pass, not a code fix | If ever revisited: split `casWriteSettings` into mutable-key (skip) vs content-upload (keep) variants in `Cas::ObjectStorageBackend` (`putIfAbsentStream`/blob paths keep the check; casPut/putOverwrite skip). Also note the generic upstream robustness issue the crash exposed: the check throwing on an upload WORKER escalates to std::terminate instead of failing the query - worth an upstream report independent of CA. | [TRIAGED 2026-06-13: latent, not green-blocking, deferred — see unattended report]
| B115 | **FIXED (`440871098a9`, 2026-06-12). WRONG RESULTS (T13 CA-S3 lane): `optimize_read_in_order` + experimental reverse key on a CA-S3 disk returned wrong rows.** ROOT CAUSE: `ReadBufferFromFileView` tracked its buffer-end offset incrementally and broke when the inner buffer DISCARDS its working buffer in `setReadUntilPosition` (which `ReadBufferFromS3` legitimately does on a range change, rebasing to the consumer position) — the view's stale buffer-end got clamped to the new until-bound, teleporting the reported position forward by the discarded bytes; `CompressedReadBufferFromFile`'s "already at position"/"within working buffer" shortcuts then re-served a stale block (duplicated + missing granules, checksum-clean). The in-order reader triggers it by adjusting the right mark per granule with pending buffered data. FIX: rebase `file_offset_of_buffer_end` from the impl's own post-op accounting (`getPosition() + available()` inside the swap window) after EVERY impl op — also repairs the truncate-then-extend drift in `nextImpl`. Also affected upstream `PackedFilesReader` (packed parts on remote disks) — the fix + new gtest battery (`gtest_read_buffer_from_file_view.cpp`, first-ever coverage for the view; 14/36 red pre-fix) are upstream material. VERIFIED: minimal repro green, exact test file 5/5 MATCH, view battery 36/36, CA battery 241/241, `03257` triple `Failed: 0, Passed: 3` on the CA-S3 praktika job. Original analysis preserved below. Deterministic minimal repro (single process, `clickhouse local`, fresh pool, even `max_threads = 1`): table `(i, j)` `ORDER BY (i, j DESC)` with `allow_experimental_reverse_key = 1`, `index_granularity = 2`, 100 rows; `SELECT * FROM x2 ORDER BY i, j DESC LIMIT 5` returns `1090,1080,1050,1040,1030` (expected `1090..1050`), the ASC variant returns rows UNSORTED — duplicated + missing granules, block-aligned (off-by-granules). Data is intact (`count`+checksum OK); `optimize_read_in_order = 0` is correct. ISOLATION MATRIX: CA-local correct, plain-s3 correct, CA-S3 wrong — only the combination fails. Plans identical (`EXPLAIN PIPELINE` and `EXPLAIN indexes = 1` byte-equal CA-local vs CA-S3), so the bug is in read EXECUTION: blob payloads are served via `ReadBufferFromFileView` (256 B envelope-skipping view) over the backend's `readObject` buffer — plain `ReadBufferFromFile` on local (correct) vs `ReadBufferFromS3` on S3 (wrong), so the view's seek/swap/resize state machine over a LAZY-seek inner buffer is the prime suspect (`ReadBufferFromFileView` has ZERO test coverage). Surfaced as `03257_reverse_sorting_key_simple` failing in the T13 diff-retest (the full-lane `03257_reverse_sorting_key` diff was the same bug). Repro script: `tmp/03257_min4.sql` (needs RustFS on 127.0.0.1:11122, bucket `test`). | The ONE confirmed real CA bug from the T13 full-lane triage; everything else was env (B93) or pre-existing | NEXT: (1) a semantics gtest battery for `ReadBufferFromFileView` over an inner buffer with lazy/buffer-resetting seek (mimic `ReadBufferFromS3`) — random seek/read/setReadUntilPosition sequences vs golden bytes; (2) debug-build run of the minimal repro to log the view's seek/next offsets; (3) fix + un-gate/re-run the 03257 pair; suspect sites: `ReadBufferFromFileView::seek` (`file_offset_of_buffer_end = result + available()` after the swap dance), `resizeWorkingBuffer` interaction with the inner buffer's in-buffer-seek bookkeeping. |
| B116 | **FIXED (read path `d8fd11659df` + write path `5b69a99fd5b`, 2026-06-13). The CA-S3 lane timeout driver.** Two independent roots, both fixed: (READ) CA blob reads bypassed the standard pipeline and issued whole-blob GETs that were torn mid-response on right-mark changes → broken-pipe churn; fixed by routing blob reads through the standard gather/cache/async pipeline bounded by a `ReadBufferFromFileView` stage (right-mark bounds now reach `ReadBufferFromS3`, GETs drain and pool). (WRITE) the conditional-PUT retry storm — see [[B118]]. VERIFIED: the 4 read-in-order tests that timed out at 600s now pass in <4s in the CA-S3 lane. Original analysis below. CA-S3 read connection-churn + broken-pipe retry storm (T13, 2026-06-12). The lane's ~50 test timeouts are this, not scanner/heal (B93) or concurrency.** End-to-end chain, all evidence-backed: (1) **Root — connection churn.** The CA read path serves blob bytes via `ReadBufferFromFileView` over `ReadBufferFromS3`; read-heavy queries (read-in-order, `LIMIT`, merges) open a GET per granule range and ABORT it before draining the response (the reader took what it needed and moved on / `LIMIT` stopped early). An aborted-before-drain GET cannot be returned to the HTTP keep-alive pool, so it is hard-closed. `ss` during a live lane: **ClickHouse-side `TIME-WAIT` ~100 every sample, RustFS-side `TIME-WAIT` = 0, `ESTAB` ~2** — i.e. ClickHouse does all the active closes; near-zero reuse; ~100-connection/s churn. (2) RustFS is the victim of the mid-response close: it logs `rustfs_ecstore: get_object_with_fileinfo .../blobs/<h> err Io(Kind(BrokenPipe))` (broken pipe writing the half-sent GET body). (3) Under that churn a fraction of ops surface to ClickHouse as `Poco::IOException: Broken pipe` (18,365 in one lane, growing 327->502/min). (4) ClickHouse RETRIES broken-pipe (correctly classified retryable) but with NO cancellation bound in the outer `ReadBufferFromS3` loop (see B-retry-cancel below), so a query whose client was killed ZOMBIES — observed retrying one blob 521x over 42 min (client killed at +10 min). (5) Churn latency + retries push read-heavy queries past the 600 s test timeout. REPRODUCES at `--workers 3` (NOT load-dependent) and the broken-pipe count is identical with scanner/heal disabled (NOT B93). AWS S3 / MinIO tolerate mid-response aborts silently (RST, no retry); RustFS logs them and the churn destabilises it. | The actual blocker for "green CA-S3 lane without timeouts"; everything else (B93, B115) was secondary | FIX DIRECTIONS, in order of leverage: **(a) B105 proper ranged-read plumbing** — issue exact `Range` GETs that the buffer fully drains, so connections return to the keep-alive pool and the abort-churn disappears (the real root fix; upstream `StoredObject` range field). **(b) drain-or-bounded-abort policy** in the CA read path / `ReadBufferFromS3` so an abandoned GET with little remaining is drained (poolable) rather than hard-closed. **(c) retry-cancellation fix** (below) to kill the 42-min zombie amplifier. **(d) RustFS-side**: investigate whether RustFS can be made to not error/destabilise on client aborts. Pin tooling: `tmp/capture_who_closes.sh` (ss-based; no host CAP_NET_RAW so tcpdump only works inside the --privileged praktika container, which lacks the binary —  install or use ss). **UPDATE 2026-06-12: RustFS-tuning REJECTED, ClickHouse-side root CONFIRMED.** (1) Re-ran the 4 read-in-order tests with `RUSTFS_RUNTIME_WORKER_THREADS=24 RUSTFS_RUNTIME_MAX_BLOCKING_THREADS=2048 RUSTFS_OBJECT_MAX_CONCURRENT_DISK_READS=4096` -> all 4 still timed out at 600 s. NOT RustFS runtime/Tokio starvation; fix-direction (d) is out. (2) Mechanism = ClickHouse reads FEWER bytes than the GET response holds and never drains it: each blob read builds a `ReadBufferFromFileView` sized to the WHOLE blob payload (`readBlobPayload`: `[location.offset, location.offset+location.length)`), but the consumer (a granule / `LIMIT` / a column slice in a merge) reads only part and the buffer is destroyed, OR `ReadBufferFromS3::seek` hits a backward / >`remote_read_min_bytes_for_seek`(=4 MB default) forward seek - both call `impl.reset()` / close the GET mid-response (`ReadBufferSeekCancelConnection`) instead of draining. RustFS logs the half-sent response as `BrokenPipe`; a fraction surface to ClickHouse and retry (B117). ACTUAL fix = (a)/(b): make CA blob reads request+drain only the consumed bytes (B105 ranged-read plumbing - bound the GET to the granule range so it drains and pools) or drain-on-close in the CA read path. NB `readBigAt` already drains (`istr.ignore(INT64_MAX)`); the streaming `nextImpl`/`seek` path does not. |
| B117 | **FIXED (`dd408fef7ba`, 2026-06-13).** Outer `ReadBufferFromS3` retry loop ignored query cancellation. FIX: `processException` now returns false (stop retrying) when `CurrentThread::get().isQueryCanceled()`, mirroring the SDK `RetryStrategy` check, so a killed query's reads no longer zombie-retry a transient error. Largely upstream-general. Original below. `Client::RetryStrategy::ShouldRetry` (SDK layer, `src/IO/S3/Client.cpp:107`) bails on `isQueryCanceled()`, but the OUTER ClickHouse loop in `ReadBufferFromS3::nextImpl` (`src/IO/ReadBufferFromS3.cpp:160-203`) and `processException` (~L320-352) have NO cancellation check — they retry broken-pipe with exponential backoff up to `s3_max_single_read_retries`, compounding with the SDK retries (observed "attempt 107/50"). So when a test's client is killed at the 600 s deadline the server query does NOT abort: it keeps retrying broken-pipe reads for tens of minutes (observed 42 min), piling up zombie queries that add load and worsen the churn (B116). Broken-pipe itself is correctly retryable (transient network drop). | Turns a transient drop into a 600 s+ zombie; makes the lane tail (and resource use) far worse; largely upstream-general, not CA-specific | Add an `isQueryCanceled()` / connection-alive check to the outer `ReadBufferFromS3` retry loop (and the write path), mirroring the SDK strategy at `Client.cpp:107`; optionally bound the broken-pipe retry budget. Upstream-relevant. |
| B118 | **FIXED (`5b69a99fd5b`, 2026-06-13; regression test `05006_content_addressed_dedup_blob_insert`). CA-S3 INSERT hang.** ROOT CAUSE: a conditional blob PUT (`If-None-Match: *`) that loses its precondition (e.g. two byte-identical columns dedup to one blob) shipped its full multi-MB body before reading the response; RustFS rejects the large doomed body with a mid-upload close / `500 UploadStreamError` (RETRYABLE), so the AWS SDK retried the upload up to `s3_retry_attempts`(=500)× with backoff while `WriteBufferFromS3::finalizeImpl → TaskTracker::waitAll` blocked the INSERT (~40 min). FIX: `Expect: 100-continue` on conditional PUTs (`PocoHTTPClient`: `setExpectContinue` + `peekResponse`) — the server evaluates the precondition before the body, returns a clean 412, the body is never sent, no retry storm. Verified on the deterministic repro (count 1000000, exit 0, zero broken-pipe/500/retry) and in the CA-S3 lane (Failed: 0, Passed: 5 incl. the 4 read-in-order tests that previously 600s-timed-out). Same root family as the lane write-storm — now resolved. Original analysis below. (distinct from the lane broken-pipe issue; relates to B48).** A single-process `clickhouse local` with a `content_addressed` disk over standalone RustFS: `CREATE TABLE` succeeds, logs `Start to refresh statistics`, then the process BLOCKS during the first large INSERT (`numbers(1e6)`) — killed at 300 s, never committed a part; **RustFS logs ZERO broken pipes** (so this is a pure ClickHouse-side block, NOT a RustFS drop). A 1000-row insert finishes in <10 s, so it is volume-triggered. `ptrace_scope` blocks `gdb` attach without root, so no stack captured yet. Likely the same family as B48 (`clickhouse local` + CA lifecycle hang). Repro: `tmp/ca_hang_stack.sh` (full 2M-row workload). | Found while trying to reproduce B116 in `clickhouse-local`; it reproduces a DIFFERENT failure, so `clickhouse-local` is not a faithful vehicle for B116 (which needs server mode) | Capture a stack (enable `ptrace_scope=0` or run under root/`strace`, or use the server's `system.stack_trace`); determine if it is the CA write path (manifest CAS loop / GC heartbeat) or statistics (`PackedFilesReader`) blocking. Server-mode INSERTs DO work (the lane inserts fine), so it may be local-mode-specific. |
| B119 | **OPEN — RustFS errors/destabilises on benign client mid-response aborts (the broken-pipe storm; upstream to RustFS, not ClickHouse).** When an S3 client closes a GET before draining the response body — a normal, expected event for range/streaming reads that stop early (`LIMIT`, granule slices, seek-cancel) — AWS S3 and MinIO treat it as benign: they drop the connection (RST) and log nothing. **RustFS instead logs `rustfs_ecstore: get_object_with_fileinfo .../blobs/<h> err Io(Kind(BrokenPipe))` for every such abort and the resulting churn destabilises it** (a fraction of subsequent ops then fail on the ClickHouse side, see B116). This is the RustFS-side half of the B116 chain: even after ClickHouse is fixed to drain/bound its GETs (B105/B116), a conforming S3 store should tolerate mid-response client aborts silently. NB this is the ONLY behaviour difference; RustFS's enforced conditional ops (the reason we use it over MinIO) are correct. | Splits B116 fix-direction (d) into its own tracked item; the ClickHouse-side fix (B116/B105) is the primary lever, this is the defence-in-depth / upstream-report side | Report upstream to RustFS (broken-pipe on client abort should be Debug/ignored, not an error that destabilises the store); confirm against `rustfs 1.0.0-beta.8`. Optionally test a newer RustFS build. Tooling: `tmp/capture_who_closes.sh`. Related: [[B116]], [[B117]]. |
| B120 | **OPEN (follow-up to [[B118]]) — `Expect: 100-continue` has no graceful fallback for a store that ignores it.** The B118 fix sets `Expect: 100-continue` on conditional PUTs and calls `peekResponse` before sending the body. A well-behaved S3 store (AWS S3, MinIO, RustFS — all verified) answers `100 Continue` or a final response; but a store that IGNORES the `Expect` header (waits for the body) makes `peekResponse` block until the socket first-byte timeout, then the request fails after SDK retries. Bounded (not infinite — the session timeout caps it) and CA-only (the change is gated on `If-None-Match`/`If-Match`, used solely by the content-addressed backend, whose stores are already capability-probe-validated for conditional ops). Curl's client degrades gracefully by sending the body after a short (~1s) 100-continue timeout. FOLLOW-UP: either (a) mirror curl — use a SHORT peek timeout and proceed to send the body if no interim response arrives; and/or (b) add an `Expect: 100-continue` honoring check to `runCapabilityProbe` so a non-honoring store disables the optimization. Low priority while the only CA stores in use honor it. **[REVIEW 2026-06-13, rated HIGH by independent reviewer.]** Confirmed a real (if narrow) regression: a store/proxy that IGNORES `Expect` makes `peekResponse` block until the first-byte timeout, then throw -> SDK retries (re-applying Expect) -> repeats; bounded but a stall+fail where pre-B118 there was none. FEASIBILITY: the clean curl-style fallback (poll the socket, send the body if no interim response arrives in ~1s) is NOT doable via the public Poco API — `HTTPClientSession` exposes no `socket()` accessor and `peekResponse` CLOSES the connection on timeout, so we cannot send the body after a timed-out peek. RECOMMENDED FIX (attended): probe `Expect: 100-continue` support in `runCapabilityProbe` and set a per-S3-client flag (carry it on `PocoHTTPClientConfiguration`) that gates the optimization; OR add a per-disk config escape hatch `cas_use_expect_100_continue` (default true). NOT done unattended: both are cross-layer changes to the shared S3 client and a buggy version is worse than the bounded edge-case stall. **[BLAST-RADIUS CORRECTION 2026-06-13: the Expect gate is NOT CA-only.** `if-none-match`/`if-match` are also set by **Iceberg** (`Storages/ObjectStorage/DataLakes/Iceberg/Utils.cpp::writeMessageToFile`, catalog/metadata conditional commits), so the Expect optimization now also applies to Iceberg conditional writes. SAFE for Iceberg: the precondition OUTCOME (412/200) is identical with or without Expect — only the wire behavior differs (body not pre-streamed), which is correct and beneficial on S3/MinIO (both Expect-compliant, Iceberg's stores). But the graceful-fallback follow-up MUST therefore cover the Iceberg path too, not just CA. The reviewer's Finding 3 ('only CA conditional writes set Expect') was incorrect.]** Keep Expect ON meanwhile — it is REQUIRED to fix B118 and all CA stores in use (RustFS/S3/MinIO) honor it. | Found during self-review of the B118 fix | Implement the short-timeout-then-send fallback in `PocoHTTPClient` (distinguish peek-timeout from peek-error), or probe-gate the feature. |
| B121 | **OPEN (perf) — `stateful, long` heavy `test.hits` reads time out (600s) on the CA-S3 lane; marginal + flaky.** Across two full CA-S3 lane runs the only CA-attributable failures are heavy stateful reads of the huge `test.hits` table timing out — and the SET shuffles run-to-run (run 1: `03927_autopr_input_bytes_estimation_prewhere_filter`; run 2: `00091_prewhere_two_conditions`, `03582_pr_read_in_order_hits`, `03634_autopr_input_bytes_estimation`, `03800_autopr_reuse_index_analysis`), i.e. they sit near the 600s boundary and which ones tip over varies with host load — not a systematic correctness regression. ROOT: a CA-S3 read of a massive table issues a GET per column-blob per part (gigabytes of data transfer); the metadata-decode cost is already fixed (B113, 2.66x->1.51x), but the per-blob DATA GET overhead is inherent. NOT caused by B113/B116/B117 (those speed reads up). FIX DIRECTIONS (future, larger): read-coalescing across granules/columns, fewer/larger blobs, or packing small files into a footer object ([[B10]]) / packs (M-F). Until then these `stateful,long` tests are marginal on CA-S3. Consider whether stateful tests should even run in the CA-S3 STATELESS lane. | The only CA-attributable non-green tests after the 2026-06-13 fixes; everything else green or non-CA | Profile a `test.hits` CA-S3 scan (GET count, bytes, wall-clock); evaluate read-coalescing / larger remote-read buffers / packing. |
| B125 | **OPEN — CA integration tests (`test_content_addressed_s3`, `test_content_addressed_gc_s3`) use minio + don't run in the local integration env.** Two issues, BOTH pre-existing (not regressions): (1) the local integration run errors at the SHARED minio bring-up fixture (`cluster.py` 'Trying to connect to Minio' -> Connection refused at the container's :9001, before the clickhouse node/CA code runs) — a local-harness minio connectivity/TLS issue affecting ALL minio integration tests here, not CA-specific; (2) even with working minio, these tests configure CA over **minio**, which the CA incarnation capability probe REJECTS (minio ignores If-Match on DELETE — the exact B93 reason RustFS replaced minio in the stateless lane), so they were silently broken by the probe-enforcement landing after they were written (commits 22d247b796f/df580adad76). FIX: update these integration tests to use RustFS (mirror the stateless-lane B93 migration) — needs RustFS support in the integration docker framework (the framework has minio built in; adding rustfs is non-trivial). Until then CA-over-S3 is validated by the STATELESS CA-S3 lane (RustFS, 3 green full runs, zero CA-correctness regressions) + the CA gtest battery (224) + live GC-delete validation. |  CONFIRMED 2026-06-13 (2nd run, minio connectivity worked): the CA tests error at CA-disk load with `CasProbe: deleteExact with a wrong token was not TokenMismatch — backend does not enforce conditional deletes (NOT_IMPLEMENTED)` (`CasProbe.cpp:146` -> `Store::open`) — i.e. CA's capability probe correctly FAIL-CLOSES on minio (RELEASE.2024-09-13 still honors a mismatched-token DELETE). This is the safety probe working AS DESIGNED, pre-existing, NOT a regression. Same run: the non-CA `test_merge_tree_s3` + others = 22 passed (no regression on non-CA s3 integration). | Found running integration per request 2026-06-13 | Add RustFS to the integration framework + repoint the CA integration tests at it; or gate them no-integration-without-rustfs. |
| B122 | **FIXED 2026-06-13 (`08add5c0d4f`) — `commit` partial-commit rollback.** `ContentAddressedTransaction::commit` published N refs SEQUENTIALLY and flipped `committed=true` only at the end, with NO rollback of already-published refs if a later `publish` threw → a multi-part txn (`parts` keyed by `{ns,ref}`, size>1 reachable) that failed mid-loop left earlier refs LIVE+visible while the disk layer saw the txn as failed. Five umbrella reviewers flagged this as the top blocker. FIX: track the refs THIS commit creates and, on any exception in the loop, best-effort `dropRef` them before rethrowing (all-or-nothing). **Fail-closed:** only refs ABSENT before publish are rolled back (a pre-existing ref is never destroyed on the error path — publishing over a live ref does not occur in the MergeTree write path, unique part names); `updateRefPayload` mutations (autocommit one-shots) are individually durable by design and deliberately NOT rolled back. **GUARANTEES/TLA+ (user Q 2026-06-13):** a partial commit is NOT a CAS/incarnation protocol violation — each `publish` and the rollback's `dropRef` are individually gate-checked + journalled, leftover uploads are GC-reclaimable debris; spec §9 invariants (INV-NO-LOSS/NO-RETURN/JOURNAL-COVERAGE) all hold. It is a wiring-layer transaction-atomicity gap at the IDisk boundary, ABOVE the model's abstraction line (the model's atomic action is a single publish/dropRef; a sequence where one fails is already covered as independent events). The fix adds NO new model obligation → correct coverage tier is a wiring-layer fault-injection gtest, NOT TLA+. TDD: `CaWiringWrite.PartialCommitRollsBackPublishedParts` (`FaultyLocalObjectStorage` fails the 2nd shard-manifest publish) — verified failing pre-fix, passing post-fix; full Cas*/Ca* battery green (225). | Top write-path review finding | DONE. REMAINING sub-item: RENAME TABLE `move_namespace` (`:567`) still has the cross-ref/verbatim multi-namespace gap (+ may leave a table split across two namespaces on a mid-loop throw) — a longer, separate multi-namespace move not covered by this commit-loop rollback; tracked as [[B126]]. |
| B123 | **OPEN (LOW/MEDIUM, latent) — unguarded read-modify-write hazards on the verbatim plane (write-path review F4/F6).** `moveFile` verbatim (`:678-696`, `tmp_mutation_N.txt`->`mutation_N.txt`) does get->putNamespaceFile->removeNamespaceFile with NO token/CAS — re-driven rename can clobber a concurrently-updated dest; ordering is crash-safe (dup, not loss). `unlinkFile` (`:766-784`) silently NO-OPs for a committed (non-staged, non-mutable) content file — fail-OPEN: if the MergeTree layer ever unlinks a committed content file expecting it gone, stale bytes survive. Both tolerable under strict single-writer but unguarded/fail-open unlike every other CA write. FIX: CAS-guard the verbatim RMW; add a fail-closed assertion to `unlinkFile`'s no-op shape. | Latent, single-writer-mitigated | Token-guard verbatim RMW; assert unlink no-op shape. |
| B124 | **OPEN (MEDIUM, NEEDS-DISCUSSION) — move paths use OPPOSITE mutable-file collision policies (write-path review F5).** `moveDirectory` staged-merge uses `emplace` so DEST bytes win on a mutable-file collision (`:638-639`), while `moveFile` overwrites so SOURCE wins (`:719`). Each asserts a 'newer side' invariant without enforcement; if the assumed operation order is ever wrong, a just-written mutable file (e.g. `txn_version.txt`) is silently dropped (lost update). | Internal inconsistency in the move collision policy | Decide + document the correct policy (likely source-wins = the part being carried forward), align both paths. |
| B126 | **OPEN (MEDIUM, latent — split out of [[B122]] 2026-06-13) — RENAME TABLE `move_namespace` has no cross-ref/verbatim atomicity.** `ContentAddressedTransaction::moveDirectory`'s `move_namespace` lambda (`:567`) republishes every live + detached ref and every verbatim file under the new table identity, then `dropNamespace`s the old one — all sequentially, no rollback. A mid-loop throw leaves the table SPLIT across two namespaces (some refs/files moved, the old `dropNamespace` never reached). Unlike [[B122]] (the commit loop, now rollback-guarded), this is a multi-namespace move spanning refs AND verbatim files AND a terminal `dropNamespace`, so the simple created-refs compensation does not directly apply — needs its own design (idempotent re-drive on retry, or a move journal, or republish-all-then-drop with compensation). NOT green-blocking (single-writer; RENAME TABLE is rare and not in the lane's failure set). Same protocol-vs-wiring framing as B122: not a CAS-invariant violation, a wiring-layer atomicity gap. | Remaining half of the B122 write-path finding | Design an atomic/idempotent multi-namespace table move (re-drivable on partial failure). |
| B127 | **OPEN (LOW, latent — review triage 2026-06-13, deliberately NOT fixed unattended) — empty-token-on-Done in the Native conditional-write sinks.** `ObjectStorageBackend::nativeConditionalPut` (`CasObjectStorageBackend.cpp:112`) and `NativeStreamingSink::finalize` (`:148`) observe the just-written token via a head-after-put and fall back to an EMPTY `Token{}` if the head reports the object absent — returning `PutOutcome::Done` + empty token. Callers (e.g. `putTree`/`putBlob` dep recording) store that empty token unchecked; a later gate/revalidate using an empty dep token can misbehave (fail-OPEN). The theoretical trigger is the object vanishing between our successful conditional PUT and the HEAD — which does not occur for content-addressed blobs/trees (immutable, heartbeat-gated) and for manifests a concurrent CAS REPLACES (head still `exists`, different token), never deletes-to-absent. **WHY NOT FIXED UNATTENDED:** converting the empty-token fallback into a fail-closed throw adds a NEW throw on the head-after-put result of EVERY Native conditional PUT in the CA-S3 lane — directly contradicting the documented decision (`casWriteSettings` comment, `CasObjectStorageBackend.cpp:326-332`) to NOT post-HEAD-check CAS keys because they "race by design" and false-positive; that class of check is exactly what destabilised the lane pre-B116/B118. A buggy fail-closed check is worse than the bounded latent fall-open. | Review triage; lane-stability risk | Attended: assert/throw on empty token ONLY for content objects (blobs/trees, which truly cannot vanish), leave manifest writes on the tolerant path; or carry the ETag out of the conditional write so no head-after-put is needed at all. |
| B128 | **OPEN (LOW, Emulated-only — review triage 2026-06-13) — in-flight `StoredObject` uses the bare pool key, not `physicalKey`.** `ContentAddressedTransaction::tryGetInFlightStorageObjects` (`:234`) and `ContentAddressedMetadataStorage::getStorageObjects` (`:636`) emit `StoredObject(location.key, ...)` with the bare pool key, while the authoritative byte-reading plan wraps it as `physicalKey(location.key)` (`getBlobViewPlan`, `:723/:735`). `physicalKey` is identity in **Native** mode (production: `physical_key_prefix` empty) and only rewrites in **EmulatedSingleProcess** mode (local/tests), so there is NO production impact; an Emulated consumer that byte-reads these `StoredObject`s directly (instead of via the read plan) would hit the wrong key. NOT fixed unattended: changing it risks DOUBLE-applying the prefix if a consumer already re-applies it — needs a check of every consumer first. | Review triage; test-only impact | Verify in-flight/getStorageObjects `StoredObject` consumers; wrap with `physicalKey` (identity in Native) once confirmed no double-prefix. |
| B129 | **OPEN (LOW perf/robustness — review triage 2026-06-13) — roots registry grows monotonically, never pruned.** `Store::ensureRegistered` (`CasStore.cpp:497`) inserts a namespace on first publish; `dropNamespace` tombstones shards but never removes the namespace from `roots/_registry`. The `namespaces` set grows forever (one entry per live/detached/shadow namespace ever created), with no size guard, and GC R3 fences EVERY registry entry each round (`CasGc.cpp:82-83`) so rounds get slower over time. Bounded in practice by table count (thousands), not truly pathological. A size GUARD is safe/additive; PRUNING is correctness-sensitive (a namespace missing from the fence-time registry can miss its fence floor — see the ordering comments at `CasStore.cpp:506-510`, `CasGc.cpp:469-472`), so pruning must run under the GC fence protocol. | Review triage; perf/robustness | Add a registry hard-size guard (minimal); or fence-gated registry pruning on `dropNamespace` (proper, larger). |
| B130 | **OPEN (architecture, MEDIUM — umbrella review #2, 2026-06-13) — `dynamic_cast`-to-concrete instead of virtual dispatch on the metadata-storage/exchange seam.** Several call sites reach CA-specific behaviour by `dynamic_cast<ContentAddressedTransaction&>` / `dynamic_cast<IContentAddressedExchange*>` on the `IMetadataTransaction` / metadata-storage pointer (e.g. the wiring tests, the DataPartsExchange facade path). The umbrella reviewers flagged this as a layering smell — the polymorphic intent should be expressed via a virtual interface method rather than a downcast to the concrete type, so non-CA storages and future backends don't depend on the concrete class. NOT done unattended: it is a cross-cutting refactor of the IDisk/metadata-transaction interface (touches the abstract base + every implementor), higher blast radius, and needs a design pass on which operations deserve first-class virtual slots vs staying CA-internal. | Umbrella review (architecture/#2) | Promote the CA-specific operations consumed via `dynamic_cast` to virtual methods on the relevant interface (or a dedicated optional capability interface), and remove the concrete downcasts from shared code paths. |
| B131 | **OPEN (repo hygiene — umbrella review BLOCKER #2, 2026-06-13; operator said SKIP for now) — diff vs upstream/master carries non-shippable working files.** The branch diff includes `docs/superpowers/**` (specs/plans/reports/backlog — internal working docs), assorted top-level scratch files (`*.md` notes, `*.svg`/`*.folded` flamegraphs, `tmp/`, `disks/`, `keeper-bench-*`, `ssh_key`, stray `tests/queries/0_stateless/00408_*.tmp.*`, etc.), and the PoC CA code that M-W will delete. Two reviewers rated the diff-hygiene a merge blocker: a reviewer/maintainer cannot separate the real change from the scaffolding. **Operator decision 2026-06-13: SKIP for now** (the working docs are actively used during the unattended effort). Before any upstream PR: move `docs/superpowers/**` out of the PR (keep locally / separate branch), gitignore or remove the scratch artifacts, and land the PoC deletion (M-W) so the diff is the new core + wiring only. | Umbrella review (repo-impact/#2 blocker) | At PR-prep time: strip working docs + scratch artifacts from the branch diff; ensure `ssh_key`/credentials never reach a public PR; complete the M-W PoC deletion. |
| B132 | **RESOLVED 2026-06-14 (soak-test Phase-1 GREEN exercised the populated-pool fsck path).** The soak harness runs `Cas::runFsck` (via the mounted, fixed binary) at every quiescent checkpoint against the live, POPULATED shared pool, asserting `dangling=0` and `{dryrun} ⊆ {fsck unreachable}` — exactly the sub-project-D smoke this item asked for. Phase-1 GREEN (seed=20260613, 1500 ops, 6 workers, 5 checkpoints) confirmed it end-to-end: every checkpoint had counts matching both replicas, `dangling=0` (HEAD-confirmed, see [[B141]]), `dryrun_subset=ok`, residual `unreachable` logged as B140 M-F debris. Note from the original — fsck/read-only mode needs a POPULATED pool; live CLI smoke deferred to the soak harness:** The read-only CA disk mode + `clickhouse-disks fsck`/`ca-gc-dryrun` (DONE — see [[project_ca_fsck_readonly_subproject_a]] / commits T1-T7 on `cas-mergetree-poc`) are gtest-covered at the storage level (`Cas::runFsck`, `Gc::previewDeletes`, read-only fail-closed — 232 Cas*/Ca* green), and the CLI commands' build/register/dispatch/guards + the `--query` nonzero-exit-on-failure are verified. NOT yet exercised: the happy-path CLI run of `fsck`/`ca-gc-dryrun`/`list`/`read` against a POPULATED read-only pool. Reason: opening a FRESH/empty CA pool read-only fails at mount (correct — `PoolMeta::createOrValidate` cannot write the absent pool-meta on a read-only backend), and populating a pool needs a running writable server. So the live smoke belongs in soak-test **sub-project D** (running replicas populate the shared pool; then fsck/ls/cat run at quiescent checkpoints). | Sub-project A wrap-up | In sub-project D: at a quiescent checkpoint, run `clickhouse-disks fsck --detail` + `ca-gc-dryrun` + `list --recursive`/`read` against the live shared pool opened read-only; assert dangling=0, exit codes, and `{preview} ⊆ {fsck unreachable}`. |
| B133 | **OPEN (soak-test sub-project A finding, 2026-06-13) — WORM read-only mounter is server-scoped; cross-server live-part reads go through fsck, not the IDisk read path.** Live namespaces are `server_id + "/" + table_uuid` (CAMS `liveNamespace`), so a read-only mounter opened AS A DIFFERENT server cannot see another server's live parts via `listDirectory`/`readFile` (it resolves its own empty namespace). This is fine for `fsck` (pool-global — walks ALL namespaces via `listNamespaces("")`) and for the same-server read-only reopen (the tested WORM case), but means the "serve queries as a read-only replica of another writer" story is NOT delivered by the read-only mode alone. | Surfaced implementing sub-project A T2 | If a cross-server read-only query replica is ever wanted, design shared/non-server-scoped live namespaces (or a read-mount that unions namespaces) — separate from this sub-project. Document the WORM scope as inspection (fsck) + same-server reopen for now. |
| B134 | **OPEN (LOW, soak-test sub-project A minor follow-ups, 2026-06-13) — grouped optional hardening.** (a) `ContentAddressedMetadataStorage::runOneGcRoundForTest` is a public mutator NOT guarded by the read-only check (reachable only from gtests today, not from any command/disk surface — final-review Minor); add a `read_only` throw for defense-in-depth so the class has no unguarded public mutator. (b) Make `clickhouse-disks fsck` force the observe-only open at disk-build time so it works on any CA disk WITHOUT requiring `<readonly>true</readonly>` in config (currently it requires the disk be configured read-only; cross-layer change to the disks disk-builder, deferred). (c) `Gc::previewDeletes` / `ca-gc-dryrun` could expose the in-grace vs leaked `reason` split (needs reading retire-grace/fence state) — currently only emits `reason="unreachable"` (spec §5 deferred this as a debugging aid). (d) `fsck --detail` does not print `FsckObject.kind` (cosmetic). | Sub-project A wrap-up (final review Minors) | Apply piecemeal when convenient; none blocks the soak test. |
| B135 | **OPEN (MEDIUM, robustness — surfaced by the soak-test T2 bring-up 2026-06-13) — concurrent shared-pool mounts race on the FIXED capability-probe key.** `runCapabilityProbe` (`CasStore.cpp` `Store::open`, probe prefix `pool_prefix + "/_probe"`) writes FIXED keys `<pool>/_probe/token` and `<pool>/_probe/cas`. When two servers mount the SAME shared pool simultaneously (e.g. both replicas start at once), they race: one `putIfAbsent(_probe/token)` wins, the other gets `PreconditionFailed` and ABORTS with `CasProbe: putIfAbsent on a fresh key returned PreconditionFailed` (exit 48); the winner's step-8 cleanup delete can also cascade into the loser. Found standing up the soak harness (two ch replicas sharing one RustFS CA pool); worked around there by gating ch2 on ch1's healthcheck (serialized mounts). Real gap: production replicas CAN start concurrently. FIX (sub-project A adjacent): use a PER-MOUNT unique probe key (e.g. `_probe/<random-or-incarnation>/...`) so each mounter validates conditional-op support independently without racing; OR skip the probe when `PoolMeta` already exists (the backend was validated at pool creation) — but a per-mount unique key is safer (keeps per-mounter validation). | Soak harness T2 finding | Make the capability probe use a per-mount unique key prefix; add a gtest with two concurrent Store::open over one InMemoryBackend asserting both succeed. |
| B136 | **OPEN (MEDIUM, write-availability race — surfaced by soak-test Phase 1, 2026-06-13; reproducer below) — `Build::resurrect` GET races GC `deleteExact` (INSERT fails, NOT data loss).** During an INSERT's dedup path, `observeAndAdmit` (`CasBuild.cpp:156-196`) does `head(blobKey)` (exists) → sees the token is condemned (in the retire view) → calls `resurrect` → `get(blobKey)` (`:193-196`). GC's `runRegularRound` does `backend.deleteExact(objectKey, entry.token)` (`CasGc.cpp:219-234`) on that EXACT condemned token. If GC's delete lands in the writer's HEAD→GET window, `get` returns nothing → `Build::resurrect: object <blob> vanished between HEAD and GET` (`FILE_DOESNT_EXIST`, Code 107) → the INSERT fails with HTTP 500. **NOT data loss / NOT replica divergence** — every soak checkpoint that completed matched the model exactly on both replicas with a clean pool (dangling=0, unreachable=0); this is a transient write FAILURE under a GC-vs-write timing race. Reproducer: soak harness `utils/ca-soak`, `./scripts/run_phase1.sh` (seed=20260613, ~op 300-650, full 1500-op run hits the window; a short `--until-op 650` may miss it). DIAGNOSED FIX DIRECTION: in the failing branch the writer is in `putBlob`'s `PreconditionFailed` path and STILL HOLDS the full content body, so the correct recovery is to RE-`putIfAbsent` the content (re-materialize the blob it was trying to resurrect) rather than abort — turning a lost race into a re-upload. Needs care + adversarial review (it's the resurrect/GC-delete interaction; confirm re-putIfAbsent is safe vs the incarnation token / retire view, and that it cannot resurrect a genuinely-retired-and-should-stay-dead blob — the resurrect path already intends to revive a still-referenced blob, so re-uploading the same content under a fresh incarnation should be correct, but verify against the spec's INV-NO-RETURN). | Soak Phase 1 finding (the harness working as intended) | Make `resurrect` (or its `observeAndAdmit` caller) re-`putIfAbsent` the held body on a vanished-between-HEAD-and-GET, instead of throwing; gtest the race via fault injection (delete between head and get). Blocks soak Phase-1 green. |
| B137 | **OPEN (MEDIUM, write-availability — soak Phase-1 validation 2026-06-13; the bodyless half of [[B136]]) — publish-time `revalidateDeps`/`gateCheckDeps` → `resurrect` GET races GC `deleteExact`, surfacing as a hard `FILE_DOESNT_EXIST` (HTTP 500) instead of a retryable error.** B136 fixed the `Build::putBlob` dedup arm (re-PUT the held body). But the SAME race is reachable at PUBLISH time from the bodyless callers `gateCheckDeps` (`CasBuild.cpp:431`) and `revalidateDeps` (`:552`): a part's blob dep is condemned (retire-view hit) → `resurrect` → `head` then `get`, and GC's exact-token `deleteExact` (`CasGc.cpp:~234`) lands in the HEAD→GET window → `resurrect` throws `FILE_DOESNT_EXIST` "vanished between HEAD and GET" → the INSERT fails (HTTP 500, observed under async-insert `WaitForAsyncInsert`). NOT data loss (the blob was legitimately CONDEMNED — no live reference at GC recheck — and the build is establishing a NEW reference; checkpoints showed dangling=0). DIAGNOSIS: this is a benign GC-vs-publish race; the correct semantics is fail-closed-but-RETRYABLE — throw `ABORTED` ("dependency vanished under GC; retry the operation"), matching the SIBLING lost-dependency branches already at `CasBuild.cpp:527`/`:569`, so the INSERT retries (re-runs `putBlob` from source → re-uploads the now-absent blob fresh → re-publishes; the fresh blob is journal-invisible to GC so the retry converges). FIX (needs care — touches shared `resurrect`, the GC-vs-publish core): in `resurrect`'s vanished-between-HEAD-and-GET branch, throw `ABORTED` instead of `FILE_DOESNT_EXIST` (fail-closed-retryable). OPEN QUESTION before fixing: confirm an `ABORTED` from the publish path is actually RETRIED for async INSERT (`WaitForAsyncInsert`) the way the sibling 527/569 branches assume — if async insert does NOT retry on ABORTED, a deeper fix (retry inside the flush, or block GC delete against in-flight publish deps) is needed. Reproducer: `utils/ca-soak` `./scripts/run_phase1.sh` (seed=20260613) fails ~op 541 after rebuilding `build/programs/clickhouse` with the B136 fix. Blocks soak Phase-1 green. | Soak Phase-1 validation (B136 incomplete) | Make publish-time resurrect-vanish throw ABORTED-retry (match :527/:569); first confirm async-INSERT retry semantics; gtest the bodyless path via the same one-shot head-hook used for B136 but through gateCheckDeps/revalidateDeps. |
| B138 | **RESOLVED 2026-06-14 — CONCLUSION (1): NOT a CA data-loss bug; it was the harness's unsafe ASYNC-insert retry.** Decisive A/B (seed=20260613, same 1500-op workload, fresh cluster each): with **SYNC inserts** (`async_insert=0`) every count checkpoint matched the model EXACTLY (op 300/600/900/1200 = 13463/7951/21716/32640, dangling=0) — including the ops that lost rows under async; with **ASYNC inserts** the op-627 resurrect-`ABORTED` retry was followed by a 20-row deficit at checkpoint 900 (both nodes 21696 vs model 21716). So the CA core does NOT lose data; retrying an async insert is non-idempotent (the async dedup token commits before the part publish fails, so the retry is deduped-away and the rows never land). FIX: soak runs SYNC inserts by default (retry then truly re-inserts). The async-insert+dedup-token-vs-part atomicity is split to [[B139]] (lower priority). The sync run's remaining stop was a DIFFERENT, non-loss issue (`fsck unreachable=61 at fixpoint`, dangling=0) — a GC-fixpoint-vs-grace timing matter in the checker, fixed harness-side (drive GC to unreachable==0 with a grace-aware bound). | Soak Phase-1 A/B | DONE (conclusion 1). |
| B139 | **OPEN (LOW/MEDIUM — split from [[B138]] 2026-06-14) — async-insert dedup-token vs part-commit atomicity on a CA publish failure.** When an async INSERT's publish fails (e.g. the resurrect-`ABORTED` GC race, [[B137]]) AFTER its deduplication token was committed to Keeper, a retry of the same async insert is deduped-away and its rows never land (silent loss) — confirmed by the B138 A/B (async lost rows where sync did not). UNKNOWN whether this is CA-specific or general ClickHouse async-insert behavior (token/part not atomic across a flush failure). Lower priority: the soak uses sync inserts; async-insert+CA is an advanced scenario. Investigate with preserved server logs (logs/b138_async_server.log) — does the dedup token commit before the part on the CA disk, and is that different from non-CA disks? | Split from B138 | Determine if CA-specific; if so, make the dedup-token registration and the part commit atomic across publish failure (or don't register the token until the part is durable); add an async-insert variant to the soak once understood. |
| B140 | **OPEN (MEDIUM — REAL core GC-completeness bug; no data loss). Re-investigated 2026-06-14: the earlier "harness/timing artifact" hypothesis is REFUTED.** The orphaned-blob leak is reproducible and STABLE even with the GC interval at 2s and the background `CasGcScheduler` confirmed ticking every ~2s and reaching its OWN fixpoint. Soak Phase-1 sync (seed=20260613, 1500 ops): checkpoint op-300 clean (`unreachable=0`); checkpoint op-600 (quiesced: 1 active part, both replicas 7951 rows, model match, `dangling=0` — NO loss) is STUCK at `unreachable=2473` for the full 495s backlog-scaled bound (flat history). Decisive evidence: **ALL 2473 unreachable objects are `blobs/`; ZERO unreachable trees** (fsck detail: reachable blobs=3607/trees=226, unreachable blobs=2473/trees=0); the lease leader logged `candidates=0 deleted=0 cascaded=0` for 150+ consecutive rounds and `ca-gc-dryrun count=0`; only 6 productive (`deleted>0`) rounds in the whole run. So a full-sweep fsck sees 2473 orphan blobs while the journal-driven GC believes nothing is collectable. **ROOT CAUSE (real):** the GC learns a tree's child blob edges ONLY by EXPANDING the tree the first time its `Add` is folded (`CasGc.cpp:735-795`, `addTreeEdge`+`markExpanded`). The `displaced_later`/`FILE_DOESNT_EXIST` skip path (`CasGc.cpp:743-774`) — taken when the tree object is already gone at fold time because a competing COMPLETED round deleted a displaced tree — sets `tree_present=false`, calls NO `markExpanded`, and adds NO blob edges. Under concurrent 2-server churn (rapid OPTIMIZE/merge superseding parts, lease handoff mid-fence: server log shows "lease lost during fence (stolen by …)" / "lease held by another mounter"), trees are routinely added-and-displaced within one fold window, so their blobs' edges are NEVER recorded and the blobs are invisible to the GC forever. NOT TRUNCATE-specific — at op 600 the orphans came from OPTIMIZE/merge churn (the first cliff is later); DROP/TRUNCATE is just one trigger of root removal. **Why gtest `CasTruncateReclaim.*` passes anyway:** it deliberately interleaves GC rounds with the publishes so EVERY tree is expanded BEFORE removal (its header comment, `gtest_cas_truncate_reclaim.cpp:16-20`), removing the exact precondition for the bug; it does NOT cover the displaced-before-expansion path. So the gtest's "core reclaims" claim holds only under expand-before-remove discipline, not under concurrent two-leader churn. (Also confirmed: `content_addressed_gc_grace_sec` is INERT — the core never reads it; the only throttle is the GC interval. The harness `gc_grace_sec` assumption has been removed.) **CONFIRMED + verdict 2026-06-14 (read-only investigation):** root cause confirmed at the code level. The skip branch sets `tree_present = false` (`CasGc.cpp:771-773`) and the entire expansion block `if (tree_present) { ... addTreeEdge/addPackEdge ...; markExpanded }` (`CasGc.cpp:776-796`) is bypassed, so NO edge is recorded and the marker stays unset. Two compounding effects make the leak permanent: (1) the child blobs are inserted into the snap's `known` set ONLY via `addEdge` (`CasGcSnap.cpp:104` `known.insert(target)`), reached only through `addTreeEdge`/`addPackEdge` — skipped here — so `zeroInDegreeKnown` (`CasGcSnap.cpp:233-241`) can never even consider them as candidates; (2) the cascade `stripTree` frees only edges recorded at expansion ("the strip needs no tree read: the snap recorded the child edges at expansion ... a never-expanded tree strips as a no-op", `CasGc.cpp:347-349`), so the dead tree's strip frees nothing. The blobs are invisible to the incremental GC forever. **VERDICT: NEEDS M-F FULL SWEEP (incremental GC is NOT complete by design for this case).** Spec evidence: §8 opens "The rare tier, for the two things regular GC structurally cannot see: **debris** ... and **drift** (snap diverging from truth)" (`2026-06-10-ca-incarnation-store-design.md:494`); the Full-GC walk is the authority that reads manifests, streams tree reachability, and sorted-stream-joins against a LIST of `blobs/`/`trees/`/`packs/`, classifying `present-but-unreferenced → debris candidates` (`:507-514`) which then enter the same retire→fence→recheck→exact-token-delete tail (`:522`). A displaced-and-vanished tree's blobs are exactly this "drift" class: an Add referenced them transitively (so they are not pure debris), but the snap never recorded them. The liveness invariant `LIVE-RECLAIM` (`:554`) only promises that "every journal-known node that becomes permanently unreachable is retired and deleted within ~2 regular rounds" — and a blob whose parent vanished before expansion is journal-known-in-principle but never entered the snap, so regular GC structurally cannot reclaim it; the design's intended backstop is the §8 full reachability walk. An ordering fix (option (a), expand-before-displace) is NOT generally achievable incrementally: once the tree object is GONE the fold cannot read its children, and `Build::retained_trees`/`recreateTree` (`CasBuild.cpp:404`,`:457`) live in the per-writer `Build`, not in the GC, so the leader has no source for a vanished tree's payload. **The minimal robust fix is the M-F full mark-sweep backstop (option (b)); a partial mitigation could persist child edges into the durable journal/snap at publish time so the fold never needs to re-read the object, but that is a protocol change requiring its own adversarial review.** **Failing regression gtest added:** `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` (`src/Disks/tests/gtest_cas_gc_leak.cpp`) — publishes a ref to treeA (unique blobs, no GC interleave), re-publishes the ref to treeB (last-op-wins displacement, two `Add`s one fold window apart, no `Remove`), `deleteExact`s treeA's object (simulating a competing round's landed delete so `readTree` 404s), drives `runRegularRound` to a true fixpoint, and asserts `runFsck().unreachable == 0`. RED today: `unreachable=2` (treeA's `data-A`/`mark-A`), `dangling=0`, `reachable=3` (treeB + its 2 blobs), 0 productive rounds — exactly the soak shape. `CasTruncateReclaim.*` stays green (3/3). **Fix-safety obligations (when (b) lands):** the full-sweep deletes via the SAME retire→all-shard-fence→fold-through-fence-recheck-(in-degree 0)→exact-observed-token tail, so `INV-NO-LOSS` (`:534`) and `INV-NO-RETURN` (`:536`) are preserved by construction; the candidate set must be computed from a COHERENT cut (`cut_version[shard]`, `:497-505`) so a concurrent publish that re-references a blob is re-seen by the deletion tail's own fence/recheck (`:502`); `referenced-but-absent` must re-read the ref's shard and alert loudly on `INV-NO-DANGLE` rather than auto-repair (`:511-513`); debris classification must be heartbeat-gated, never `mtime`/writer-clock (`:516-524`) — fail closed (skip) on any ambiguity, preserving `INV-OVER-COUNT-ONLY` (`:540`). | Soak Phase-1 sync (seed=20260613; `utils/ca-soak/logs/phase1_green*.{log}`, `phase1_ch{1,2}_gc.log`, `failure.json` op 599) | **Core fix needed (blocks Phase-1 green) — verdict: M-F full mark-sweep backstop (incremental GC is not complete by design for the displaced-before-expansion case).** Implement the §8 Full GC reachability walk over `roots/`+`blobs/`/`trees/`/`packs/` as a periodic backstop that reconciles the incremental snap with the physical object set and enqueues present-but-unreferenced blobs through the existing retire→fence→recheck→exact-token-delete tail (preserving INV-NO-LOSS / INV-NO-RETURN / INV-OVER-COUNT-ONLY; coherent-cut publication; loud INV-NO-DANGLE on referenced-but-absent; heartbeat-gated debris, never mtime). Option (a) expand-before-displace is NOT incrementally achievable once the tree object is gone (GC has no `retained_trees`); a publish-time edge-persistence change is a possible partial mitigation but a separate protocol change. Failing regression: `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` (`src/Disks/tests/gtest_cas_gc_leak.cpp`). Keep `CasTruncateReclaim.*` green. |
| B140-dangle | **OPEN (HIGH — DATA LOSS; the severe DANGLING variant of [[B140]], found 2026-06-17 in the P9 instrumented soak). NOT covered by B140's M-F-backstop verdict.** Same root cause as B140 (the GC fold fails to record a tree's `tree→blob` edges into the snap, so `zeroInDegreeKnown` surfaces the blob at in-degree 0 and the single content-delete site deletes it), but here the under-recorded tree is **LIVE** (a current manifest ref still resolves to it), so GC deletes a **referenced** blob → `fsck dangling>0` = **INV-NO-LOSS violation (data loss)**, not a harmless leak. **The M-F full-sweep backstop (B140's verdict) does NOT fix this**: the blob is already physically deleted by the time a sweep runs — a sweep reclaims debris, it cannot restore a deleted live blob. So this needs a PREVENTIVE fix (the wrong delete must never happen), distinct from B140-leak. **Soak evidence (2-node, RustFS, 64 root_shards, seed 20260617):** reproduced `dangling=25` (and earlier `=55`) at the `gc_checkpoint`, PRE-chaos (`last_fault=null`). Instrumented (`CAGCDEL`/`CAREUSE` audit logs): all 25 blobs ch2 `CAREUSE adopt` round 334 → ch1 `CAGCDEL Deleted` round 340, EXACT token match; `reachable_from` = a merged-then-detached broken part (ch2 merge 19:27:03 reused the 25 blobs → GC deleted them round 340 → next merge read 404 → part broken → detached). So GC deleted a live freshly-merged part's blobs. **NOT P9** (P9 prunes `everEdged`, not fold edge-recording; verified separately). **Mechanism (subagent-verified — built+ran 9 natural single-process interleavings, none dangle):** the fold is NOT buggy in single-process sequential operation (the `displaced_later` skip is fail-closed for any live ref — rethrows `FILE_DOESNT_EXIST` when no later same-ref record; the cascade advances the cursor in the SAME CAS that clears the marker, with a fold-through-fence recheck). The broken invariant — **"a `markExpanded` tree that is still LIVE (in-degree≥1) has ALL its `tree→blob` child edges in the durable snap"** — arises only across rounds/leaders (cross-node): mechanism (b), the cascade-vs-recreate marker race the spec comment at `CasGc.cpp:402-407` warns about. **Likely PRODUCER on the leader: the cross-node replication relink** (`adoptFromTree` tokenless-evidence, `CasBuild.cpp:607-630`) — audit with the fold invariant. **Failing regression gtest:** `CasGcDangle.MarkedExpandedWithoutEdgesDeletesLivePinnedBlob` (`src/Disks/tests/gtest_cas_b140_dangle.cpp`) — injects the corrupt durable snap via the public `GcSnap` API, asserts `runFsck().dangling==0`; RED today (`dangling=1`). Companion `CasReuseGcRace.ReuseOfBlobDeletedBeforePublish` PASSES (the LOCAL reuse-vs-GC publish gate is correctly fail-closed — the bug is the fold edge-recording invariant, not the reuse gate). **Fix-safety:** preserve INV-NO-LOSS/INV-NO-RETURN/INV-OVER-COUNT-ONLY; candidate fixes = re-expand a live tree on a folded Add when a child edge is missing / never advance `folded_cursor` past an Add whose tree is not fully edge-recorded / refuse to retire a candidate whose live parent is `markExpanded` / coherent re-verify at the delete site; model the producer + fix in `CaIncarnationCore.tla`. **2026-06-18 CORRECTION (ground truth — supersedes the "marker-without-edges" mechanism above):** decoding the REAL saved soak snaps (`/tmp/snapdump/snap_67{1,2,3}/0`) with the production codec found `markers_with_ZERO_child_edges = 0` in EVERY generation ⇒ the live-markExpanded-without-edges state DOES NOT occur in reality (the hypothesis, the fix spec's fold-watermark fix, and the `CasGcDangle` injection test's injected state are all REFUTED). The 25 dangling blobs are `known=Y, inDeg=0` while a LIVE tree references them ⇒ REAL bug = **shared/deduplicated-blob in-degree under-count** (a blob shared by a deleted/stripped tree and a still-live tree, whose live-tree edge was never present in the folded snap). A FAITHFUL TLA+ model (`CaB140DangleFaithful.tla`) exhausts 9.1M states with NO dangle (non-vacuity verified) ⇒ producer is UPSTREAM in the real `foldShardRecords` edge-construction (`displaced_later` skip applicability for a still-live ref / shared child, or cursor/shard-version windowing), below the abstract model — STILL UNPINNED. Next: natural shared-blob fold gtest + cross-node/cursor repro; correct the injection test; rewrite the fix spec once pinned. | P9 instrumented soak 2026-06-17 (`utils/ca-soak/logs/p9_instr_FAILURE_dangling25.json`, `p9_instr_correlation.txt`, `p9_instr_fsck_detail.txt`; round-340-era `gc/snap` gens 671-673 + outcomes/340 + state preserved at `/tmp/snapdump`). | **Preventive fix needed (data loss) via the proper flow (brainstorm→spec→TLA+→plan→implement).** Audit `CaIncarnationCore.tla` `GFold`/expansion + the relink producer (`adoptFromTree`) for the same invariant class; durable fix proven in TLA+ + TDD gtest (`CasGcDangle.*` green); add sibling-scenario tests; re-soak (quick → 12h chaos) confirming `dangling=0`. Related: [[B140]] (leak variant), [[B160]] (lease/leadership churn = the cross-leader trigger), [[B164]] (manifest). |
| B141 | **RESOLVED 2026-06-14 (found by soak Phase-1, fixed + verified GREEN) — fsck HEAD-confirms `dangling`; LIST-lag false-positive eliminated.** `Cas::runFsck` built the REACHABLE key set from the authoritative ref-walk (listNamespaces→listRefs→readTree→locate) and the PRESENT set from a raw `backend.list` of `blobs/`/`trees/`/`packs/`, then classified any reachable key absent from the LIST set as `dangling` (an INV-NO-LOSS violation → nonzero fsck exit). But an S3/object-store LIST can LAG for recently-written objects (eventual consistency / mid-churn), so a reachable, ACTUALLY-PRESENT object missing from a lagging LIST was FALSELY flagged dangling. Confirmed false-positive in the soak: counts matched both replicas, re-fsck showed `dangling=0`, the object was a superseded short-lived intermediate, no server `FILE_DOESNT_EXIST`. FIX: a reachable key not found in the LIST(present) set is now `backend.head`-confirmed — `head` is AUTHORITATIVE for presence, LIST is advisory. If HEAD finds it → reclassify `Reachable` (and add its HEAD size to physical accounting, since the LIST scan missed it); only a HEAD-ALSO-absent reachable object is truly `Dangling`. We HEAD only the suspected-dangling set (reachable∖LIST, ~0 in the healthy case), so it stays cheap. `unreachable` stays LIST-based (M-F debris metric, not safety-critical — not HEAD-confirmed). Regression gtests: `CasFsck.ListLagDoesNotFalseFlagPresentObjectAsDangling` (present-on-HEAD + absent-from-LIST → reachable, not dangling) and `CasFsck.HeadAbsentReachableIsStillDangling` (HEAD-absent reachable still dangling — fix does not mask real loss), via a `ListLaggingBackend` wrapper that omits a key from `list` while `head`/`get` still see it. Verified by soak Phase-1 GREEN (seed=20260613, 1500 ops): 5/5 checkpoints `dangling=0`. | Soak Phase-1 finding (the harness working as intended) | DONE — `src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Core/CasFsck.cpp` (HEAD-confirm branch) + `src/Disks/tests/gtest_cas_fsck.cpp`. |
| B142 | **RESOLVED 2026-06-14 (HARNESS robustness gap — NOT a CA bug; found by soak Phase-2 T11 chaos integration). A graceful `docker restart`/shutdown cancels an in-flight query server-side (`QUERY_WAS_CANCELLED`, code 394) instead of dropping the socket; the workload's node-down retry only caught connection-level errors, so the cancelled INSERT killed the run.** First Phase-2 run (seed=20260613, chaos_seed=20260613, mean_interval=90s): two checkpoints passed CLEAN (op-400 count=16439 / op-800 count=18315, both replicas == model, `dangling=0`), then fault #1 (`both restart dur=56s`) fired and the in-flight ops correctly rerouted/retried on `ConnectionResetError`/`Broken pipe`, BUT INSERT op-898 on node2 came back as HTTP 500 `Code: 394 QUERY_WAS_CANCELLED` — a SERVER-SIDE exception body, not a dropped connection. The harness classified that as a `QueryError` (a real workload error), not a transport failure, so `retry_on_transport` did not catch it and the driver exited `WORKLOAD FAILURE`. **DIAGNOSIS: harness-side, not CA.** No checkpoint failed, no divergence, `dangling=0` throughout — the CA core was fine. A graceful shutdown is the node-down twin of a dropped connection: the in-flight query is cancelled (394) or hits a mid-query network error (`NETWORK_ERROR` 210 / `SOCKET_TIMEOUT` 209) instead of the TCP connection simply going away. **FIX (harness):** added `QueryError.is_node_down` + `is_node_down(exc)` (`soak/cluster.py`) recognising `NODE_DOWN_CODES={394,209,210,735}` and the connection-level `is_transport_error`; `retry_on_transport` now retries/reroutes on EITHER. Kept the B137 ABORTED (236) on its own `retry_on_aborted` path (excluded from node-down so it is not double-handled) and logic errors (e.g. `UNKNOWN_TABLE`) propagate immediately. Idempotency holds: the rerouted retry of a sync INSERT is deduped by RMT block-dedup if the original committed (same B138 sync-insert guarantee), else it re-inserts. Unit tests: `tests/test_transport_retry.py` (394/210 classified node-down; ABORTED + logic errors excluded; cancelled-then-recovers retry). | Soak Phase-2 T11 finding (`utils/ca-soak/logs/phase2_*_server.log`, `failure.json` op 898; reproducer: `utils/ca-soak/scripts/run_phase2.sh` seed=20260613 chaos_seed=20260613) | DONE (harness) — `utils/ca-soak/soak/cluster.py` (`is_node_down`/`NODE_DOWN_CODES`/`QueryError.is_node_down`), `utils/ca-soak/soak/run.py` (resilient insert/optimize/barrier reroute), `utils/ca-soak/tests/test_transport_retry.py`. |
| B143 | **RESOLVED 2026-06-14 (HARNESS-CONFIG gap surfaced by soak Phase-2 T11 restart chaos — NOT CA data loss; the pool was clean, `dangling=0`). On a server RESTART, the table fails to ATTACH with `UNKNOWN_DISK` (code 479) because the harness defines a second, read-only CA disk (`ca_ro`) aliasing the SAME pool, and the orphaned-parts scan treats the policy parts under `ca_ro`'s same-pool path as orphans.** Second Phase-2 run (after the B142 fix; seed=20260613, chaos_seed=20260613): checkpoints op-400 (count=16439) and op-800 (count=18315) passed CLEAN (both replicas == model, `dangling=0`); fault #1 (`both restart dur=56s`) fired and the B142 node-down reroute correctly carried the workload through (45 node-down retries incl. 394 `QUERY_WAS_CANCELLED`); but when ch1 came back up its async table-load failed: `Code: 722 ASYNC_LOAD_WAIT_FAILED` → `695/696 load table` → `Code: 479 UNKNOWN_DISK: Part '20260614_724_724_0' (store/.../...) was found on disk 'ca_ro' which is not defined in the storage policy 'ca' or broken (defined disks: [ca], skipped disks: [])`. The driver surfaced this as `WORKLOAD FAILURE` at op-893; `failure.json` shows `fsck dangling=0 reachable=10376 unreachable=52 exit=0` — **the CA pool is intact; this is a table-ATTACH config issue, not loss**. **ROOT CAUSE (code-confirmed):** `MergeTreeData::loadDataParts` runs an orphaned-parts safety scan on ATTACH/CREATE (`MergeTreeData.cpp:2302-2355`) that iterates EVERY disk in `getContext()->getDisksMap()` — not just the storage policy's — and for any non-policy disk eligible under `search_orphaned_parts_disks` (default `ANY`, `MergeTreeSettings.cpp:2177`; `isDiskEligibleForOrphanedPartsSearch` at `:2269`) that `existsDirectory(relative_data_path)` and holds a parseable part name, it throws `UNKNOWN_DISK` (`:2347-2352`). The harness's `ca_ro` (read-only S3 view over the SAME RustFS endpoint as `ca`, present in the server config because the offline `clickhouse disks fsck` applet reads that same config — `soak/fsck.py` `run_fsck(disk='ca_ro')`) is remote, not-broken, not-custom → eligible under `ANY` → its same-pool path holds the very parts policy disk `ca` owns → throws. **Phase-1 never restarted a server (table loaded once at fresh CREATE, no part-discovery scan), so it never surfaced.** **FIX (harness, minimal + semantically correct):** the table DDL now sets `search_orphaned_parts_disks='local'` (`soak/run.py` `DDL_TEMPLATE`), so the orphan scan is limited to LOCAL disks and skips the remote `ca_ro` alias (a false positive — `ca_ro` is the same pool as the policy disk `ca`, not orphaned storage), while still catching genuinely-orphaned local parts. A cleaner-but-larger alternative (deferred) is to stop defining `ca_ro` as a SERVER disk altogether and give the fsck applet its OWN config file (config/compose restructuring). **CA-relevance note (worth a look, not blocking):** a read-only CA disk that is an alias of a writable policy disk's SAME pool is indistinguishable from an orphan-bearing foreign disk to this generic MergeTree scan; if CA read-only aliases become a supported production pattern, the scan (or the CA disk) may want to recognise a same-pool read-only alias and skip it rather than rely on the table setting. | Soak Phase-2 T11 finding (`utils/ca-soak/failure.json` op 893, last_fault=both/restart/56s; reproducer: `utils/ca-soak/scripts/run_phase2.sh` seed=20260613 chaos_seed=20260613 — fails on ch1 restart after fault #1 WITHOUT the DDL setting) | DONE (harness) — `utils/ca-soak/soak/run.py` `DDL_TEMPLATE` (`search_orphaned_parts_disks='local'`). Follow-up (optional): split `ca_ro` into a fsck-applet-only config; consider CA-side same-pool-alias recognition in the orphaned-parts scan. |
| B144 | **OPEN (MEDIUM — needs confirmation; HARNESS-gap most likely, but a REAL object-store-durability finding is NOT excluded). Surfaced by soak Phase-2 T11 chaos. After a fault that restarts the RustFS OBJECT STORE, the recovery checkpoint's HARD dryrun-subset assert fired with fsck `dangling=10` (all `trees/`, size 0) while BOTH replicas + the model agreed EXACTLY (no SQL loss).** Third Phase-2 run (after B142+B143; seed=20260613, chaos_seed=20260613): faults #1 `both restart` / #2 `rustfs kill` / #3 `rustfs kill` all had CLEAN recovery checkpoints (`dangling=0`, count==model both replicas — counts 22464/22748/23185). Fault #4 `rustfs restart dur=44s` (op-972, a DELETE): the recovery checkpoint failed `CHECKPOINT FAILURE: dryrun key 'soak_pool/trees/05/0536…' not in fsck unreachable set (dryrun must be a subset of unreachable); dryrun_count=41`. `failure.json` (op 972, last_fault=rustfs/restart/44s): node1==node2==model EXACTLY (count=25057, all 7 aggregates identical — **NO SQL data loss / NO replica divergence**), but `fsck dangling=10 reachable=6512 unreachable=4599 exit=36`; ALL 10 dangling keys are `trees/…` with `size=0`, and the dryrun-violating key `trees/05/0536…` was fsck-classified REACHABLE (neither dangling nor unreachable). So three independent reads of the pool DISAGREED right after the object-store restart: fsck ref-walk+HEAD saw some trees reachable / some HEAD-absent (dangling), and the journal/snap-driven GC dryrun wanted to delete a tree fsck thought reachable — a VIEW-INCOHERENCE during/after backend recovery. **DIAGNOSIS (most likely HARNESS): the recovery wait gated only ClickHouse `/ping` (`wait_for_healthy`), NOT the RustFS backend's recovery, so the checkpoint asserted CA POOL invariants against a still-settling object store.** Same fault TYPE (rustfs down) was CLEAN at #2/#3 and dirty at #4 → timing-dependent, the signature of a transient post-restart window (recently-written tree objects not yet visible/durable through a FRESH `ca_ro` fsck mount, while the running server reads them from cache). Note B141 made fsck `dangling` HEAD-confirmed/authoritative, so a transient HEAD-absent right after restart still classifies dangling — the gate must WAIT for the backend to settle before trusting it. **NOT EXCLUDED (could be REAL): a durability gap where CA acknowledged a tree write that RustFS lost on restart (esp. with `RUSTFS_HEAL_ENABLED=false`) — that would be HEAD-permanently-absent and the running server reads only from its in-memory cache (a CH restart would then lose it). The earlier `both restart` (cold caches) reading CLEAN with dangling=0 argues AGAINST a permanent loss, but is not conclusive for these specific trees.** **HARNESS FIX (lands now; makes the run self-diagnosing): the Phase-2 checkpoint now `wait_for_pool_consistent` — polls a FRESH `run_fsck` until `dangling==0 && exit_code==0` for K stable reads (bounded) BEFORE the hard dryrun-subset assert. If the pool reaches `dangling==0`, the prior failure WAS the transient backend-recovery window (harness gap, confirmed). If `dangling>0` PERSISTS past the generous bound, the gate raises a CheckpointFailure that EXPLICITLY escalates it as a REAL crash-recovery / object-store-durability finding (INV-NO-LOSS) — i.e. the harness no longer papers over it; it either clears or it is proven real.** Re-run pending to classify. Unit tests: `tests/test_recovery_wait.py` (transient-dangling-clears → returns; persistent-dangling → raises with the REAL-finding message). | Soak Phase-2 T11 finding (`utils/ca-soak/failure.json` op 972 preserved at `/tmp/ca_soak_phase2c_failure.json`; reproducer: `utils/ca-soak/scripts/run_phase2.sh` seed=20260613 chaos_seed=20260613 — fault #4 rustfs restart) | Re-run with the `wait_for_pool_consistent` gate (`utils/ca-soak/soak/run.py`). If GREEN → confirmed harness gap (object-store recovery not waited for); if dangling persists → STOP, escalate as a real CA/RustFS durability bug (capture the persistent dangling trees + a fresh-mount fsck vs running-server read A/B). Possible deeper fix: make the recovery wait probe RustFS readiness directly (HTTP) and/or A/B a fresh-mount fsck against the running server to distinguish cache-masked loss from transient invisibility. |
| B145 | **OPEN (LOW-MEDIUM — classified 2026-06-14 by a decisive durability probe; NOT a durable-loss bug; chaos surface scoped; one ordering question deferred to a durable-store re-test). An INSERT's CA read path hit `S3_ERROR Code 499 "The specified key does not exist"` for a `blobs/` key during a RustFS-restart chaos window.** ⟶ **DURABILITY PROBE VERDICT (2026-06-14): the "RustFS-beta is not crash-durable" hypothesis is REFUTED. RustFS does NOT lose acked objects on `docker kill -s KILL`.** Probe method (cluster up, no CH involvement): write N distinct objects to the `test` bucket via `mc`, record LIST count, `docker kill -s KILL ca-soak-rustfs1-1`, `docker start`, re-LIST + read-back each acked key. **5 runs, 0 acked-but-lost every time:** (1) 200 PUT then kill → before=200/after=200; (2) 200 PUT then IMMEDIATE kill (no flush window) + read-back → before=200/after=200/readback-missing=0; (3) CONTINUOUS write stream, kill MID-STREAM → acked=403/after=403/acked-but-lost=0; (4) 1 MB objects, continuous, kill mid-stream → acked=486/after=487(one in-flight completed durably, safe direction)/acked-but-lost=0; (5) kill→restart→KILL-DURING-STARTUP→restart (mirrors the soak's #2→#3→#4 rustfs sequence) → acked=335/after=335/acked-but-lost=0. So a successfully-acked S3 PUT survives a hard kill in `1.0.0-beta.8` even with `RUSTFS_HEAL_ENABLED=false`/`RUSTFS_SCANNER_ENABLED=false`, even back-to-back kills during recovery. **Corroborating evidence from the original B145 capture:** `failure.json` op 953 had `fsck dangling=0` (and exit_code=0) — `runFsck` walks the manifest→blob graph and reports referenced-but-missing blobs as `dangling`; `dangling=0` means the blob `61/612346a2…` was NOT permanently missing at fsck time. (That blob is also a fsck-`reachable` entry in the same-seed Phase-1 logs — a legitimately-referenced deterministic-payload blob, not garbage.) **CLASSIFICATION: the 499 was a TRANSIENT post-restart read-VISIBILITY window in the RustFS test backend (an object-store recovery artifact), NOT a durable blob loss and NOT a CA durability defect.** It is the read-path twin of B144's transient `dangling` window — same backend-recovery cause, surfaced server-side as `499 S3_ERROR` on the INSERT-dedup read (correctly NOT in `NODE_DOWN_CODES`, so the B142 reroute didn't mask it). **ACTION TAKEN: scope the Phase-2 chaos so RustFS faults never inject this backend artifact** — `generate_chaos_schedule` (`utils/ca-soak/soak/chaos.py`) deterministically downgrades any `RUSTFS`+`KILL` slot to `RUSTFS`+`RESTART` (graceful flush), keeping schedule length/timing; CH replicas KEEP `KILL` (the CA-relevant crash: does CA recover when a ClickHouse SERVER crashes over a durable-enough store). Unit tests `test_rustfs_never_killed` + `test_ch_replicas_still_killed` (`tests/test_chaos_schedule.py`) assert this. **CAVEAT / DEFERRED FOLLOW-UP (does NOT close the deeper question): the probe rules out RustFS losing acked PUTs, but it does NOT rule out a CA blob-durability-ORDERING bug — CA must never make a part/manifest reference a blob before the store has DURABLY acked that blob's PUT. That property can only be cleanly tested against a KNOWN-crash-durable store (real S3 / MinIO-with-fsync), where a CH-server `kill -9` is the crash under test. Re-test B145's ordering aspect there as a follow-up.** ⟶ ORIGINAL CONTEXT BELOW. Phase-2 run with the B144 `wait_for_pool_consistent` gate (seed=20260613, chaos_seed=20260613): the gate WORKED — fault #4 `rustfs restart` recovery checkpoint now PASSED (count=23839, dangling=0, dryrun=0), CONFIRMING B144 was the transient post-restart window (harness gap). Faults #1–#4 all had CLEAN recovery checkpoints. Then during fault #5 `ch1 pause` (just after #4 `rustfs restart`), INSERT op-951/953 on ch1 failed: `Code: 499. DB::Exception: The specified key does not exist ... while reading key: soak_pool/blobs/61/612346a2408408af89b5f90b46842be4, from bucket: test. (S3_ERROR)`. `failure.json` (op 953, last_fault=ch1/pause/32s): counts were node1=24063(max_op 952)/node2=23993(max_op 949)/model=24301(max_op 953) — **these DISAGREE only because the failure was captured MID-FLIGHT (un-quiesced: ch1 paused, replication lagging, model always ahead of un-synced replicas), NOT at a quiesced checkpoint — so this is NOT a divergence finding**; `fsck dangling=0` at capture. **DIAGNOSIS: this is the READ-PATH manifestation of the same object-store-restart durability window as B144** — a `blobs/` object written shortly before the RustFS hard-restart was lost/invisible on restart, so a later INSERT-dedup/resurrect read of it returns NoSuchKey. It is surfaced as a SERVER-SIDE `QueryError` (code 499 S3_ERROR), NOT a transport/connection error, so the B142 node-down reroute did not catch it (correctly — 499 is NOT in `NODE_DOWN_CODES`). It is the same family as [[B136]]/[[B137]] (resurrect-vs-GC race → FILE_DOESNT_EXIST/ABORTED on a referenced-but-vanished object), now triggered by a backend restart rather than a GC race. **DELIBERATELY NOT auto-retried (fail-close): adding 499/NoSuchKey to the harness retry set would risk MASKING a genuine CA referenced-blob loss — per the fail-close principle we surface it instead.** **Most likely root: RustFS `1.0.0-beta.8` is not crash-durable for un-fsync'd objects on a hard restart (`RUSTFS_HEAL_ENABLED=false`); CA correctly references the blob, the backend lost it — an environment/durability property, analogous to B144, NOT a CA logic defect. NOT EXCLUDED: a real CA path that references a blob the core itself failed to persist/that GC removed across the restart.** Needs a live A/B to classify (the pool was torn down): re-run, on a 499-NoSuchKey-on-read during a chaos window, A/B whether the blob reappears (transient/backend) vs stays gone (real loss), and whether a fresh-mount fsck reports it dangling. | Soak Phase-2 T11 finding (`utils/ca-soak/failure.json` op 953 preserved at `/tmp/ca_soak_phase2d_failure.json`; reproducer: `utils/ca-soak/scripts/run_phase2.sh` seed=20260613 chaos_seed=20260613). Durability-probe verdict 2026-06-14: RustFS keeps all acked objects on `kill -9` (5 runs, 0 acked-but-lost; counts 200/200, 200/200, 403/403, 486/487, 335/335); B145 capture had `fsck dangling=0` → transient read-visibility, not loss. | CLASSIFIED + SCOPED (2026-06-14): transient backend read-visibility window, not a CA durability defect → RustFS chaos scoped to graceful (`chaos.py` KILL→RESTART downgrade + unit tests). Did NOT add 499 to the harness retry set (fail-close preserved). DEFERRED follow-up (still OPEN, separate from the kill-9 durability question): re-test the CA blob-durability-ORDERING property (no part may reference a blob before its PUT is durably acked) against a crash-durable store (real S3 / MinIO-with-fsync), with a CH-server `kill -9` as the crash under test. |
| B146 | **OPEN (gates efficient LONG/24h soak; classification UNKNOWN — leans toward "CA pool legitimately grows to tens of thousands of distinct objects", NOT RustFS versioning). The offline `clickhouse disks fsck` / `ca-gc-dryrun` applet is O(pool objects): a single offline fsck over RustFS takes >120s once the read-only LIST it scans holds tens of thousands of objects, so a full-duration phase-3 self-check's STAGE checkpoints cannot complete in time.** SYMPTOM: with a leftover idle cluster (tens of thousands of objects) an offline `clickhouse disks fsck`/`ca-gc-dryrun` call ran >120s; when fsck DID complete it reported `dangling=0`/`unreachable=0` — so this is a test-backend I/O-SPEED limitation, NOT a CA-correctness defect (NO data-loss implication). **SUSPECTED-CAUSE PROBE (2026-06-14, T14 self-check, seed=20260613): the prompt's "RustFS retains object VERSIONS on the CA dedup re-PUT path, inflating LIST" hypothesis is REFUTED by a direct `mc` probe.** On a FRESH `docker compose up` (after `down -v`) the `test` bucket reports `mc version info ca/test` = **`un-versioned`**, and `mc ls --recursive` (current) ≈ `mc ls --recursive --versions` (24386 vs 24387 — the +1 is a list artifact, NOT retained versions); the `soak_pool/blobs/` prefix held 8221 objects current AND 8221 with `--versions`. So RustFS is NOT keeping per-key versions across the CA re-PUT/`putOverwrite`/resurrect path. **ACTUAL CAUSE (probe-confirmed):** the pool is dominated by DISTINCT content-addressed `blobs/` objects sharded over 256 two-hex prefixes (~85 each ⇒ ~22k blob objects after a short run), plus `roots/`/`trees/`/`builds/`/`gc/outcomes`/`snap` (305 snapshot objects). The metrics ticker showed `pool_objects` climbing 420 → 14535 → 23959 within the first ~23s of the self-check and hovering ~40k mid-run (dropping back as GC ran). fsck's read-only ref-walk + full `blobs/`/`trees/`/`packs/` LIST therefore costs O(distinct objects), which is genuinely large even at small `--ops`, because deterministic-payload inserts + merge/mutation churn mint many distinct blobs fast. So fsck-at-scale slowness is REAL but its driver is object COUNT, not versioning. **SELF-CHECK OUTCOME (NOT `PHASE3 OK`, but NOT a CA defect either):** seed=20260613 phase 3 `--duration 300 --workers 4 --ops 400`. Reached STAGE warmup→steady→mutations→ttl_pressure→chaos; the chaos-stage `§8 checkpoint+GC` completed; then a `SYSTEM SYNC REPLICA ca_stress` timed out (HTTP 408, `Code: 159 TIMEOUT_EXCEEDED`, `receive_timeout`) on node2 → raised as a `QueryError` → `WORKLOAD FAILURE`, abort. The failure-DUMP path (`soak/run.py:1004-1007`) ran a SINGLE bare `run_fsck` on the NON-quiesced, mid-chaos cluster (no `quiesce`, no `wait_for_pool_consistent` settle — unlike the clean-checkpoint path at `:475`), which reported `dangling=115`/`unreachable=8399` with an `INV-NO-LOSS` (`BAD_ARGUMENTS` exit 36). **That `dangling=115` is the documented transient post-restart/chaos fsck-incoherence window (HEAD-absent reachable trees the running server still reads correctly — see B141/B144/B145, and the `wait_for_pool_consistent` rationale at `soak/run.py:538-542`), surfaced because the diagnostic dump fsck does NOT run through that settle gate. It is NOT a checkpoint assertion failure and NOT confirmed loss.** The metrics DB (`/tmp/soak_selfcheck_small.db`, 92 rows, full schema incl. `pool_objects`/`pool_bytes`/`parts_*`/`repl_queue`/`mutations_pending`) and the degraded TSV plot (`/tmp/soak_curve_small.tsv`, 46 timestamp rows; matplotlib absent so `scripts/plot.py` correctly degraded to TSV) were produced — the phase-3 TIMELINE + per-minute METRICS + STAGE-checkpoint machinery work e2e; what blocked a clean `PHASE3 OK` at 300s/4 workers is (a) the chaos-stage SYNC-REPLICA reconcile not completing within `receive_timeout` over a ~40k-object pool, and (b) fsck cost at that scale. **IMPACT:** makes large/24h soak STAGE checkpoints slow and risks SYNC-REPLICA timeouts under chaos at scale; gating item for running a long (multi-hour/24h) soak efficiently. NO data-loss implication (`dangling=0`/`unreachable=0` whenever fsck completes on a settled pool). **CLASSIFICATION: UNKNOWN — most likely a test-infra/cost property (CA pool legitimately accumulates tens of thousands of distinct objects + offline fsck is a full O(N) LIST+resolve), NOT a CA-correctness defect and (now refuted) NOT RustFS versioning.** Fix directions: (1) make offline fsck/dryrun INCREMENTAL or cheaper (cache the prior LIST/snap and reconcile a delta; or have the applet read the live core's in-memory snap instead of a cold full LIST); (2) for the self-check, raise the SYNC-REPLICA `receive_timeout` and/or quiesce-before-fsck on the failure-dump path so the diagnostic fsck runs after a settle (it would then likely read `dangling=0`); (3) bound pool growth in the self-check (lower `--ops`/payload entropy so distinct-blob count stays small); (4) re-confirm versioning stays OFF in the compose/bucket setup (already OFF — no action needed unless a future RustFS bump changes the default) — i.e. the prompt's "disable bucket versioning" fix is a no-op here because it is already un-versioned. NEEDS a follow-up: run the small self-check with a quiesce-before-dump fsck (or larger `receive_timeout`) to demonstrate a clean `PHASE3 OK`, and prototype an incremental/cheaper offline fsck for the 24h run. | Soak T14 phase-3 self-check (seed=20260613, `--duration 300 --workers 4 --ops 400`; `utils/ca-soak/logs/phase3_selfcheck_small.log`, `phase3_selfcheck_small_server.log`; metrics `/tmp/soak_selfcheck_small.db`, plot TSV `/tmp/soak_curve_small.tsv`; versioning probe via `mc version info`/`mc ls --versions` on a fresh `test` bucket = un-versioned, current≈all-versions) | OPEN — leans test-infra/cost (incremental/cheaper offline fsck + quiesce-before-dump-fsck + bound self-check pool growth); RustFS versioning REFUTED (bucket un-versioned, current≈all-versions, blobs are distinct content-addressed objects). Re-run a small self-check with quiesce-before-dump-fsck (or larger `receive_timeout`) to show `PHASE3 OK`; prototype an incremental offline fsck before attempting the 24h run. |
| B147 | **ROOT-CAUSED + CONFIRMED (MEDIUM, GC scalability — soak 24h run hour-2; profiled at scale 2026-06-14) — the GC lease-holder re-serializes the WHOLE-pool in-degree snap (`gc/snap`) on EVERY round, an O(pool) JSON decode+encode+S3-upload that becomes CPU-bound and starves the shared object-store I/O path, timing out storage-touching queries.** ⟶ **CONFIRMED ROOT CAUSE (live `system.stack_trace` profiling of `ca-soak-ch1-1` at 64k→247k pool objects, GC interval 2s).** The per-round cost is NOT the candidate/observe scans as originally suspected — it is `Cas::Gc::loadSnap` → `decodeGcSnap`, which **JSON-parses the entire pool's `gc/snap` document into a Poco JSON DOM every round, then `fold`/`cascadeAndPersist` re-serialize it via `encodeGcSnap` and re-upload it with `putIfAbsent`/`casPut`.** The snap is ONE document holding `edges` (every live blob/tree/pack reference), `known` (every object ever edged) and `expanded`/`indeg` for the whole pool (see `CasGcSnap.h`: `std::map<String,EdgeRec> edges; std::set<NodeKey> known; std::map<NodeKey,uint64_t> indeg;`). **Measured snap blob growth:** 23 MB @ 64k objects → 55 MB @ 227k → 58 MB @ 247k (multipart). **Measured GC round duration growth:** ~4.7s @ 50k → ~8s @ 227k → ~9–12s @ 247k, so with a 2s interval the GC thread runs back-to-back with no idle gap. **Dominant hot stack (30+ samples; 17/18 GC-phase samples were in `loadSnap`):** `Poco::JSON::ParserImpl::parseImpl <- DB::Cas::parseJsonDocument <- DB::Cas::decodeGcSnap <- DB::Cas::Gc::loadSnap <- DB::Cas::Gc::fold <- DB::Cas::Gc::runRegularRound <- CasGcScheduler::loop`, with the CPU spent in `Poco::Dynamic::Var` map-node construct/destruct + `MemoryTracker::alloc/free` + `je_sallocx`/`operator delete` (millions of tiny allocations building the DOM, then the `std::map`/`std::set`). Secondary cost: `Gc::retire` does one S3 `head` PER candidate (`ObjectStorageBackend::head <- Gc::retire`), and `encodeGcSnap <- cascadeAndPersist` + the 55 MB multipart `putIfAbsent` round-trip (`PocoHTTPClient` poll frames). **The asymmetry is the lease, confirmed:** whichever node holds the GC lease bears the whole cost — observed ch1 pegged 471–558% while ch2 sat ~150–190% when ch1 led, and the spike FOLLOWED the lease to ch2 (ch2 hit 769% → 1277% once leadership shifted, ch1 dropping to ~5%). **Unresponsiveness reproduced:** at 227k objects, `SELECT count() FROM system.parts` on ch1 spiked to **23.95 s** (≈ the timeout) and stayed elevated (4.6 s, 2.1 s on the next rounds) while `SELECT 1` stayed at 0.07 s — exactly the reported symptom (`/ping` instant, storage-touching queries time out). The spike coincided with the GC round's 55 MB snap multipart S3 I/O monopolizing the shared S3 connection pool + object-store callback-runner threads that `system.parts`/`SYNC REPLICA` also use; `system.parts` reads CA metadata through the same `S3ObjectStorage`. NOT a correctness bug (`candidates=0`/`deleted=0` throughout — nothing was wrongly reclaimed; no dangling/loss). Cost side of [[B140]] (debris inflates `known`, which never shrinks while parts stay referenced) + same O(pool) family as [[B146]] (offline fsck full LIST). **CONCRETE FIX (code-level, evidence-backed):** the snap must NOT be re-decoded/re-encoded/re-uploaded in full every round. Options, in priority order: (1) **keep the decoded `GcSnap` resident in the `Gc` instance across rounds** (the scheduler already holds ONE long-lived `Gc` per thread — `CasGcScheduler::loop`) and apply only the journal-delta fold incrementally, eliminating `loadSnap`/`decodeGcSnap` from the steady-state path entirely (the durable snap is still re-derivable on crash via the cursor); (2) **persist the snap in a binary, append/delta-friendly format** instead of one monolithic JSON DOM (Poco `Var` parsing is the allocation hotspot) and **only re-upload changed shards/pages**, not the whole 55 MB blob, when `snap_changed`; (3) **shard the snap** (`snap_shards > 1`, currently pinned to 1 by `fold`'s M-C3 limitation) so each round touches O(delta) shards and GC leadership can be split per-shard across both replicas; (4) **batch/skip the per-candidate `head` in `retire`** (LIST-or-cache) and rate-limit/yield the GC thread's CPU. Items (1)+(2) attack the measured 67%-of-round-time `decodeGcSnap`+`encodeGcSnap`+upload directly and make the steady-state round O(journal delta) instead of O(pool). Soak mitigation until fixed: lighter load / larger GC interval so a round finishes before the next tick. | Soak 24h run (hour 2); profiled 2026-06-14 on the `utils/ca-soak` cluster (`python3 -m soak.run --seed 20260614 --phase 1 --ops 100000 --workers 8`, GC interval 2s) via `system.stack_trace` sampling with `allow_introspection_functions=1`; snap sizes from `find …/gc/snap/<gen>/0 -name part.*`; round durations from `CA GC round` log lines; latency from timed `system.parts` vs `SELECT 1` probes. | ROOT-CAUSED (2026-06-14): O(pool) per-round `decodeGcSnap`+`encodeGcSnap`+full-snap S3 re-upload on the lease-holder. Fix: keep the `GcSnap` resident across rounds in the long-lived `Gc` (fold only the journal delta), persist a binary/delta-friendly snap and re-upload only changed shards, shard the snap (`snap_shards>1`) for per-shard GC leadership, and batch the per-candidate `head` in `retire`. |
| B147-validate | **B147 part-1 VALIDATED + remaining cause refined (soak validation 2026-06-14).** Rebuilt with the binary gc/snap codec (commit 11fd32b2854) and re-ran the SAME aggressive config (6 workers, 25 GB) that pegged a node at 686% CPU / ~24s system.parts. RESULT (measured, stack-sampled): the JSON-snap-parse CPU hot path is GONE (no `decodeGcSnap`/`Poco::JSON` in any hot stack); GC-leader CPU **686% → ~66–109%** (no node pegged); ch1 fully responsive (system.parts 0.00s). BUT the GC-leader node's `system.parts` STILL times out (>30s) at only ~1 core CPU — the bottleneck MOVED from CPU to **S3 I/O contention**. Hot stacks: many threads blocked in `S3::PocoHTTPClient::receiveResponse`/poll; dominant CA paths = **`Cas::RetireView::refresh()` called from `Build::publish`** (an S3 GET of gc/state per ref publish — inserts + merges + part-removal `republishRef`→`publish`) + the per-round whole-snap upload + per-candidate `head` in retire. At high publish throughput the shared S3 connection pool + single-disk RustFS saturate → pool-touching queries (system.parts, SYNC REPLICA) starve while `SELECT 1` stays instant. NO correctness issue (no dangling/loss). **Part-1 (CPU) DONE+validated; remaining work = cut S3 ops per operation:** (2a) cache RetireView / refresh only on fence-version advance, not effectively-per-publish — likely the BIGGEST S3-op reducer; (2b) resident-snap O(delta) fold + no whole-snap re-upload per round (the original part-2); (2c) batch/skip per-candidate HEAD; (2d) consider a separate S3 connection pool / rate-limit for GC+CA-metadata so the query path isn't starved. | Soak validation (B147 part-1) | Profile/confirm RetireView::refresh S3-GET frequency under load; cache it (fence-version-gated); then snap-delta + candidate-HEAD batching. |
| B148 | **OPEN (HIGH perf — soak detailed profiling 2026-06-14) — CA S3-op-COUNT amplification (HEAD storm) is the dominant scalability bottleneck, not snap bytes.** Live profiling of the hour-2 degraded GC-leader node (S3 event deltas + system.stack_trace + code): HEAD ~5,100/s (16.1M cumulative) ≫ GET ~610/s ≈ PUT ~690/s; **66% of read requests ERROR (11.7M)** → an S3 retry storm that single-disk RustFS (16.9GB RSS, 300-520% CPU) can't absorb → `system.parts` (which `resolveRef`s→HEADs) stalls behind it. HEAD sources (stack+code confirmed): (1) **`Store::resolveRef`→`readShardDecoded` HEADs the shard manifest on EVERY call** (`CasStore.cpp:163`) to validate the head-token decode cache — a cache HIT still costs a HEAD; resolveRef is on the hot read path (`prepareInManifestRead`) + everywhere → the biggest amplifier; (2) **`Gc::retire` HEADs every zero-in-degree candidate every round** (scales with candidate set); (3) head-after-put per conditional write ([[B127]]). ROOT FIX = cut S3 op-count per logical op: (a) resolveRef decode cache — bounded-TTL / version-pushed validation so a warm cache needs NO HEAD (read-path twin of the RetireView cache); (b) GC retire — use the resident snap's known token/size instead of per-candidate HEAD (the [[B147]] incremental-GC resident-snap design delivers this + kills the snap GET/PUT); (c) head-after-put. NOTE: part of the error rate is single-disk RustFS failing at an op rate real S3 would absorb (test-store limit) — but 16M HEADs is genuine op-count waste (real S3 bills per request), so op-count reduction is a real CA efficiency win. No correctness issue (no dangling/loss). perf/strace unavailable (need passwordless sudo); ClickHouse event counters + stack_trace sufficed. | Soak detailed profiling | Two pillars: resident-snap incremental GC (B147 — covers retire-HEAD + snap I/O) + resolveRef decode-cache no-HEAD-on-hit (the biggest single op-count reducer). |
| B150 | **EXPLAINED (source-verified 2026-06-14) — "why so many read failures?" The 14.9M `S3ReadRequestsErrors` (66% read-error rate) are overwhelmingly EXPECTED `404`/`NoSuchKey` responses to existence-checking HEADs, NOT genuine I/O failures.** ClickHouse's S3 metric code counts ANY non-2xx, non-429/503 response — including `404` — as an error: `PocoHTTPClient.cpp:787-789` does `else if (status_code >= 300) addMetric(request, S3MetricType::Errors)`. The `Expect404ResponseScope` (`getObjectInfo.cpp:143/168/198`, used by `objectExists`/`getObjectInfo`) only suppresses the **error log line** (`PocoHTTPClient.cpp:720`, `AWSLogger.cpp:79`, `Client.cpp:857`) — it does NOT gate the metric increment, so "expected" 404s still bump `S3ReadRequestsErrors`. Numeric confirmation from the soak (ch2): `S3ReadRequestsErrors`=14.9M vs `S3HeadObject`=20.3M ≈ **73% of HEADs returned 404** — exactly the CA existence-check miss pattern (`resolveRef`→`readShardDecoded` HEADing not-yet/​no-longer-present shards, `Gc::retire` HEADing condemned candidates, head-after-put). Proof these are benign: they are NOT surfaced as exceptions — `system.errors S3_ERROR`=1.21M which matches `WriteBufferFromS3RequestsErrors`=1.21M **exactly** (the read 404s are absorbed by the existence-check callers as "not exists"; the real exception set is write-side). **Genuine** failures are a much smaller, separate bucket: `S3WriteRequestsThrottling`=19,284 (real 429/503 overload from single-disk RustFS), the 1.21M write-buffer errors (conditional-PUT `412` + `500`s under load), `NETWORK_ERROR`=370, `RECEIVED_ERROR_FROM_REMOTE_IO_SERVER`=111. CONCLUSION: the "read failure" count is an accounting artifact of the HEAD-heavy existence-checking pattern, not a correctness or transport problem — and it reinforces [[B148]]: killing the per-access HEAD (Pillar A resident-snap retire + Pillar B decode-cache no-HEAD-on-hit) removes the bulk of BOTH the 20.3M HEADs and the 14.9M "errors" in one move. | Soak read-failure diagnosis | Covered by [[B149]] op-count reduction; no separate fix. Optional: a CA-level ProfileEvent distinguishing expected-404 existence-misses from real read errors would make future soak triage unambiguous. |
| B153 | **REGRESSION FOUND + FIXED (2026-06-14) — Pillar B bounded-TTL decode cache broke read-your-writes; fix = invalidate the shard decode cache on write.** The Pillar B opt-in TTL decode cache (`Store::readShardDecoded(..., allow_stale=true)`, default 200ms) was opted into by existence/read callers (`existsDirectory`/`existsFile`/`getStorageObjects`/`getLastModified` via `resolveRouted`), but `Store::mutateShard` (the single chokepoint for ALL shard-manifest writes — `dropRef`/`updateRefPayload`/`dropNamespace`/`Build::publish` at `CasBuild.cpp:650` all route through it) did NOT invalidate the `shard_decode_cache` entry on a committed `casPut`. So a same-`Store` write followed by an `allow_stale` read within the TTL served the STALE pre-write decode → a just-dropped ref still appeared present, or a just-written ref appeared absent. **FIX (commit `cf71b1f059c`):** in `mutateShard`, after the committed `casPut`, `shard_decode_cache.erase(key)` (cache mutex scoped, not held across the network op). A LOCAL write is now reflected immediately; cross-node staleness stays bounded by the TTL (acceptable per the spec's caller audit). **Impact correction:** this regressed **7 unit tests** — 5 `CaWiring*` (`CommittedPartRenameMovesTheRef`, `DetachAttachRoundTrip`, `RemovalsDropRefsAndNamespaces`, `DroppedPartIsReclaimedByRounds`, `AdoptPartPublishesOwnRef`) PLUS 2 I had MISLABELED as pre-existing baseline failures (`CasProtocol.DropReattachThroughDetachedNamespace`, `CasTruncateReclaim.PerRefDropOfSharedBlobsReclaimsToZero`). All 7 now pass. **The ONLY genuine pre-existing red test is `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak`** (the B140 M-F-deferred guard). **Process lesson:** the op-count tasks' gate used `--gtest_filter=CaWiring.*` (matches only the `CaWiring` suite, NOT `CaWiringOps`/`CaWiringGc`/`CaWiringExchange`), so the regression was invisible during Pillar B execution; future CA test gates must use `CaWiring*` (glob) + the full `Cas*` suite. | Surfaced during B151-T1 (full `CaWiring*` run) | DONE — `mutateShard` invalidates the decode cache on write (`cf71b1f059c`). Re-validate under soak (B151-T4). | 
| B151-VALIDATED | **B151 lock-scope fix VALIDATED by soak (2026-06-15).** Two 12h soak runs on the B151-fixed binary (seed 20260614, 6 workers, 25 GB). Across EVERY pulse and EVERY reached stage — warmup, steady, mutations, ttl_pressure (delete-heavy), gc_checkpoint, and early-chaos (with REAL docker kill/restart/pause faults causing genuine ZK-session loss + recovery) — `system.parts` stayed responsive (**0% slow polls, max 0.01–0.08s**) and `system.trace_log` showed **0 `WriteBufferFromS3::finalizeImpl` under `MergeTreeData::Transaction::commit(DataPartsLock&)` on both nodes**. This is decisive vs the B151/T9 BASELINE (same config, pre-fix): ~12% slow polls, **max 220s** `system.parts`, 427% CPU saturation, with the under-lock manifest publish as the dominant exclusive-lock holder. The fix DECOUPLES responsiveness from the op-count/HEAD pressure: even under a 28M-HEAD GC storm at 90%+ CPU (run #1 pulse #2), readers stayed at 0.02s. No correctness regression (dangling=0 in every recovery checkpoint; no loss). NOTE: neither run reached the full 12h — run #1 wedged on [[B154]] (fsck), run #2 PHASE3-FAILED on [[B155]] (SYNC-REPLICA readonly) — but BOTH are soak-HARNESS chaos-robustness gaps, NOT B151/server issues; B151's responsiveness goal is met across the hard stages it did reach. | B151 soak validation (runs #1+#2) | B151 RESOLVED/validated. Optional: a clean full-12h run after the B154+B155 harness fixes (in progress). | 
| B161 | **Soak-harness notes from the #1/#3/#4 night (2026-06-16): two startup pitfalls + a #1 HEAD-reduction nuance.** (a) **reaper sidecar wiring deferred:** mounting a shared `rustfs_data` named volume on rustfs1 (so a Debian reaper sidecar could share it) was the FIRST suspected startup destabilizer; reverted — the reaper now runs as a host-side `docker exec` loop (busybox-compatible `orphan_reaper.sh`, launched alongside the soak). Re-evaluating the sidecar+named-volume approach is a backlog item (it may have been fine; the real culprit was (b)). (b) **createbucket `#`-comment bug (root cause of the ch1 "NoSuchBucket"/dependency-fail loop):** a `#` comment placed INSIDE the YAML `>`-folded `/bin/sh -c "…"` string folds onto one line and comments out the rest (incl. the closing quote) → `sh` exit 2 → createbucket fails → ch1's `service_completed_successfully` dependency fails. Fixed (no inline comments in folded `sh -c`); added a `mc ls`-until-visible verify loop so ch1 never probes a not-yet-visible bucket. (c) **#1 nuance on RustFS:** RustFS DOES return a usable PutObject ETag matching HEAD, so #1 eliminates the head-after-put on the Done path (new blobs + every `casPut`); but the soak's dedup-able payloads make most blob writes DEDUP HITS → `observeAndAdmit` HEADs (which #1 does not remove) remain ≈ writes. So on a dedup-heavy RustFS workload the net HEAD reduction is partial; reducing the dedup-reuse `observeAndAdmit` HEAD is a candidate follow-up op-count item. | #1/#3/#4 soak bring-up 2026-06-16 | (a) re-evaluate reaper sidecar + named volume (deferred; host-exec loop used meanwhile); (b) DONE; (c) candidate follow-up: cut dedup-path observeAndAdmit HEADs. |
| B165 | **OPEN (2026-06-16, soak #7) — a CA ClickHouse server grew to ~49 GiB RSS and was OOM-killed at ~hour-4, ending soak #7 (`PHASE3 FAILED rc=1`).** Kernel: `07:56:54 Out of memory: Killed process 476715 (clickhouse-serv) anon-rss:51327944kB`. The B162 driver fix HELD (driver did not OOM) — this is **server-side**. **Key clue (soak7.db metrics):** at OOM time the table was **nearly empty** (`table_rows≈0`, `parts_active≈7`, `parts_inactive≈833` — the TTL live-set→0 dynamic), yet RSS was 49 GiB ⇒ memory is **NOT proportional to live table data** ⇒ GC working set (the `fold`/`loadSnap` in-memory reachability `std::map` over the whole ~67 GiB unreclaimed-blob backlog — grows because GC is behind, [[B160]]) OR a leak in the CA metadata/GC path. **Could NOT confirm the allocator: the harness destroyed the evidence** — no host log-volume mount (`down -v` wiped in-container ClickHouse logs), the metrics ticker records no per-node RSS, and there is NO `max_server_memory_usage` cap (so the kernel OOM-killed instead of ClickHouse failing-closed with a logged top consumer). **REQUIRED harness instrumentation before re-run (sub-project D):** (1) mount `/var/log/clickhouse-server` to host so logs survive teardown; (2) set `max_server_memory_usage_to_ram_ratio` (e.g. 0.35 per node) so ClickHouse throws `MEMORY_LIMIT_EXCEEDED` + dumps top consumers instead of OOM-kill — keeps the node alive and names the culprit; (3) add per-node server RSS + `MemoryTracking` async-metric to the ticker; (4) optionally memory `trace_log` sampling. Then re-run to pin the allocator. Strongly motivates [[B160]]/[[B163]] (bound the GC working set) and possibly a streaming/sharded fold that doesn't hold the whole graph in RAM. No data-loss/dangle observed. **B165 follow-up (validated working in soak #8 — logs DID survive teardown and the RSS metric recorded peak 2.28 GiB): the host-mounted ch logs are written `syslog:syslog` mode 640 (container uid 101 == host `syslog`), so the host user can't read them without sudo; workaround = read via a throwaway root docker container (`docker run --rm -v logs:/logs --entrypoint sh clickhouse/clickhouse-server:25.8 -c 'grep …'`). Optional fix: make the log dir/files world-readable (umask or a post-run chmod via the same docker-root trick).** **MEMORY TRACE (soak #10, clean run, 2026-06-16): the per-run RSS peaks (~2–4.5 GiB) are TRANSIENT write-buffer churn, NOT a leak — on a healthy run live is modest: jemalloc.allocated ~788 MiB (== MemoryTracking 839 MiB), jemalloc.resident ~1 GiB, RSS ~1.36 GiB; concurrent merges=1 (12 MiB), pk-indexes in RAM=2 KiB, caches ~0; jemalloc.retained 4.83 GiB is VIRTUAL (decayed, not resident). trace_log Memory allocation FLOW is 100% write buffers: `WriteBufferFromFile` (67.9 GiB cum), `CompressedWriteBuffer`/`MergeTreeWriterStream` (58.8), `CaContentWriteBuffer` (41.8), + a read-back `ReadBufferFromFile` (2.64 — CA writeFile writes locally then reads back to hash+upload). **CA-specific overhead: the CA write path allocates an extra content buffer + a read-back buffer ON TOP OF vanilla MergeTree's compressed+file write buffers — roughly DOUBLING write-buffer memory under high write concurrency. Transient/bounded, not a leak; a candidate optimization (stream-through hashing without the read-back, or a smaller CA buffer).** This means the soak #7 49 GiB OOM is a DIFFERENT regime (4h, ~67 GiB pool, GC behind) NOT reproduced on a small/healthy pool — prime suspect remains the GC fold snap over the unreclaimed backlog (or merge concurrency on a huge part set); confirm via a long instrumented run where the mem-cap fail-closes and names it.** | Soak #7 server OOM 2026-06-16 | Instrument harness (logs-to-host + mem-cap + RSS metric), re-run, pin allocator (GC fold snap vs leak); bound GC in-memory working set. Related [[B160]],[[B163]],[[B164]]. |
| B167 | **IN PROGRESS (2026-06-16): spec + TLA+ DONE (writer-side fix; C rejected); C++ impl pending. Ships WITH B160.** Spec `2026-06-16-ca-resurrect-reupload-design.md`: `resurrect`/gate-revalidation always RE-UPLOADS a fresh incarnation from the caller's recreatable bytes (never GET-from-existing) → fresh incarnation is fold-invisible until referenced → GC-safe by construction; thread the recreatable source through `deps`; NO GC change (C/back-link rejected per user). TLA+ `CaResurrectLiveness.tla` (commit `b7319bb2c05`) TLC-green: the LIVENESS dimension the safety-only model omitted — `<>published` HOLDS with the recreatable fix, VIOLATED under SabotageBodyless (lasso counterexample = the B167 livelock matching soak #11's recreate/condemn/delete churn). REMAINING: C++ (DepEntry carries recreatable source; resurrect/gate re-upload; remove GET-from-existing for recreatable deps) + unit tests + soak (B160+B167 → 0 broken parts, fsck dangling=0). ORIGINAL (corrected) writeup below. — CA LIVENESS/robustness gap (NOT a safety/data-loss bug) found by the B160 soak validation (soak #11): the bodyless publish-gate `Build::resurrect`-vs-GC-delete race makes merges fail to CONVERGE under productive GC → broken detached parts. B156-family; UNMASKED by B160, NOT caused by it.** WHY IT'S NOT A SAFETY BUG (and why TLA+ didn't "catch" it): `CaIncarnationCore` proves SAFETY (INV-NO-LOSS/NO-DANGLE/NO-RETURN) and that HELD — soak #11 had NO data loss (active table intact: 7 parts / 809,779 rows; the broken part is a FAILED merge whose source data stays in the active parts; the 2 "missing" blobs are referenced ONLY by the quarantined detached-broken part). The model's writer retries the resurrect-vanish UNBOUNDED-until-converged (the "bytes in hand → re-PUT" arm) and proves THAT safe; but the real PUBLISH-GATE dep-revalidation is BODYLESS (no bytes to re-PUT) → propagates ABORTED, and the real retry is BOUNDED → under productive GC + RustFS-412 churn the GC keeps winning the race so the merge never converges. A safety model doesn't check "writer eventually publishes under an adversarial GC" — that's LIVENESS. The incremental GC (M-C) does NOT honor the build heartbeat (per `CasHeartbeat.h`, heartbeats gate only the UNIMPLEMENTED full GC M-F; the publish GATE is the safety mechanism); a never-referenced blob is safe (not everEdged), but a dedup-reuse of an ALREADY-CONDEMNED blob (referenced→dropped→condemned→re-reused) is the gap. Also a SECONDARY fsck false-positive: fsck counts `detached/broken_*` refs as reachable-must-exist → inflates a quarantined failed-merge into an INV-NO-LOSS alarm. Mechanism (ch2 server log): a merge dedup-hits an existing blob (conditional PUT → 412 PreconditionFailed = "exists"), but that blob is a CONDEMNED GC candidate; `Build::resurrect` tries to re-reference it (HEAD→GET), and the now-productive GC **deletes it between the HEAD and the GET** → `Code 236 ABORTED: "Build::resurrect: condemned incarnation of <blob> deleted by GC between HEAD and GET; retry"`. The retry re-races (GC keeps deleting, amplified by the RustFS-412 dedup churn [[B158]]/[[B166]]) → the merge ultimately produces a BROKEN part referencing GC-deleted blobs → detached as `broken_<part>`. fsck then counts the detached part's refs as reachable → `dangling=2 … INV-NO-LOSS` (stable on the quiescent pool). Evidence: soak #11 ch2 part `20260616_2881_3917_13`, blobs `2b/2bfab…`,`d1/d144…`; **29 broken detached parts**; 7 `resurrect…deleted by GC` aborts (ch1:1, ch2:6). **Why B160 unmasked it:** pre-B160 the GC livelocked (~0 productive rounds, [[B160]]), so it barely deleted → the resurrect-vs-delete race almost never fired; with B160 the single leader runs 225 productive rounds and actively deletes condemned blobs → the race fires hard. **NOT live-data loss:** the active table was intact (7 parts / 809,779 rows); the broken part was a FAILED merge (never active — its source data stays in the active parts); the INV-NO-LOSS is on quarantined detached-broken refs. **TWO things to fix:** (1) the resurrect path must WIN against GC for a body-recreatable blob — e.g. on the ABORTED race, fall back to writing the blob FRESH (the merge has the bytes) instead of re-resurrecting the condemned incarnation; OR GC must defer deleting a blob a writer is actively resurrecting (a writer-pin / resurrect grace, like the B156 fix for `reuseBlob`); needs design + TLA+ (B137/B156 precedent). (2) fsck should NOT count `detached/broken_*` parts' refs as reachable-must-exist (they are quarantined, not live) — otherwise every broken part inflates into an INV-NO-LOSS alarm. **SEQUENCING:** B160 is correct in isolation (TLA+ + unit tests + 0 contention), but enabling productive GC without B167 creates broken parts under dedup+GC load — so B160 should ship WITH B167 (or B167 first). | Soak #11 B160 validation 2026-06-16 | Design + implement the resurrect-wins-or-GC-grace fix (B156-family) + fsck detached-broken exclusion; model-check. Blocks B160 merge. |
| B166-CORRECTED | **CORRECTION (2026-06-16, soak #9 live data): the 412 is NOT retried and is NOT the stall cause — I was wrong in the first B166 writeup below.** DECISIVE evidence (soak #9 ch1, ~5 min, chaos-off): **186,649× 412 `PreconditionFailed` but 0 retried**, vs **680 connection failures (`HTTP response code: -1`) and EXACTLY 680 SDK retries** (`before attempting again`) — retries are 1:1 with connection failures, never with 412s. Verified in the SDK: `PreconditionFailed` is NOT in the AWS C++ error-name map (only a doc comment in `aws-cpp-sdk-s3/.../Error.h`), so `AWSErrorMarshaller::Marshall` hits line 247 → `UNKNOWN`, **`isRetryable=false`**, name preserved; and `IsRetryableHttpResponseCode(412)`=false. So a 412 is NEVER retried — the CA code (`finalizeConditionalWrite`/`WriteBufferFromS3`) matches it by NAME and handles it. **The 412 is benign: an SDK name-mapping GAP (the user's "old aws client" guess) → log noise only, NOT a RustFS bug.** The REAL `SYNC REPLICA`-stall cause = **connection-level failures (-1 / NETWORK_CONNECTION) from single-disk RustFS overwhelmed by the CA op volume**, which ARE correctly retried (backoff to 5000 ms) → upload stall → queue stall → timeout. The 412 storm is a SYMPTOM of that volume (heavy CAS/dedup conditional PUTs), not the mechanism. **The committed fix `b566f00ef4f` (412→non-retryable in `ShouldRetry`) is therefore INERT defensive hardening (correct, but 412 was already non-retryable) — kept, not the stall fix.** REAL mitigations: cut S3 op/conditional-PUT volume ([[B164]] manifest binarization, [[B148]] op-count, [[B160]] GC contention) + the single-disk RustFS test-rig limit; OPTIONAL cleanup: suppress the 186k expected-412 WARN log lines (an "Expect412" scope mirroring `Expect404ResponseScope`) to stop log bloat. **OPEN QUESTION (do we have an old SDK?): the vendored AWS C++ SDK is `1.11.771` (fork `ClickHouse/aws-sdk-cpp`, pinned 2026-03-24); `S3Errors.h/.cpp` has NO `PreconditionFailed` mapping (only a doc comment in `model/Error.h`). Investigate whether a newer SDK maps the `PreconditionFailed` S3 error name to a typed value — if so, an SDK bump removes the "Unable to parse ExceptionName" log noise without a ClickHouse-side Expect412 scope. CAVEAT: even upstream S3Errors may leave conditional-write errors UNKNOWN-mapped (S3's typed error enum is narrow), so the bump may not help and the name-match handling stays the authoritative path regardless. Low priority (benign noise).** | Soak #9 corrected diagnosis 2026-06-16 | 412 is benign (SDK-gap log noise); stall = connection-failure retries under RustFS saturation → load reduction (B164/B148/B160). Optional: Expect412 log-suppression scope; investigate AWS SDK 1.11.771 vs a newer version for PreconditionFailed name mapping. |
| B166-original-WRONG | **(SUPERSEDED by B166-CORRECTED above — left for the record; this writeup incorrectly blamed 412 retries.)** the AWS SDK RETRIES RustFS's 412 `PreconditionFailed` (it can't parse the error body), amplifying a normal CAS/dedup precondition into a part-upload stall that hangs `SYSTEM SYNC REPLICA`.** Sharpens [[B158]] with the mechanism. Evidence (host-mounted ch1 log, B165): in 08:25–08:29 alone, **46,910× `Message: Unable to parse ExceptionName: PreconditionFailed`** (HTTP 412) + some `HTTP response code: -1` (connection drops), each followed by `AWSClient: Request failed, now waiting {25..5000} ms before attempting again` on the SAME part (escalating backoff). Merge/mutate parts (`20260616_0_1306_9_*`, `…_3452_4381_8`) upload content via conditional PUT (`If-None-Match` dedup, `If-Match` CAS); RustFS returns 412 with a body the AWS SDK can't parse as a terminal `PreconditionFailed`, so the SDK **generic-retries** it instead of returning it to the CA caller (which would treat 412 as dedup-hit/CAS-conflict and handle it WITHOUT retry, as on real S3). The trapped uploads stall the 9 in-flight replication-queue entries → `SYSTEM SYNC REPLICA ca_stress` (issued 08:26:05) waited the full **673 s** → `Code 159 TIMEOUT_EXCEEDED` at 08:37:19 → soak `WORKLOAD FAILURE`. NOT chaos (ch2 never paused — no log gap), NOT readonly (ch1 kept committing to Keeper throughout), NOT a B164 regression (codec is on a different path). **FIX:** classify 412 as TERMINAL/non-retryable on the CA conditional-PUT path — an "expected-412" scope mirroring the existing `Expect404ResponseScope` (`getObjectInfo.cpp`) — so `casPut`/dedup gets the 412 immediately and the SDK never retries it. Secondary: RustFS's 412 error body is non-S3-conformant (the SDK can't extract `ExceptionName`); a conformant body would also stop the misclassification. Validate via a re-run after the fix (SYNC REPLICA should drain promptly). Related: [[B158]], [[B150]] (expected-404 metric artifact), [[B148]]. | Soak #8 SYNC-REPLICA-timeout diagnosis | Classify 412 terminal on the CA conditional-PUT path (Expect412 scope); re-run to confirm SYNC REPLICA drains. |
| B164 | **B164a IMPLEMENTED 2026-06-16 (commits 3f4f5ea2/d1c92f71/26d60f86): the RootShard manifest codec is now Protobuf.** `cas_root_shard.proto` (proto3) → `clickhouse_cas_proto` lib (mirrors BuzzHouse); `encodeRootShard` emits protobuf (16-byte raw tree_id, deterministic serialization, `codec_version=1`); `decodeRootShard` is **protobuf-only — no JSON back-compat** (user decision 2026-06-16: fresh pools only; the JSON encoder+decoder+dispatch were removed). `codec_version==0`→CORRUPTED, `>1`→NOT_IMPLEMENTED; journal invariants (at_version≤shard_version, non-decreasing) enforced. 9/9 codec tests pass; full CA suite 254/255 (the 1 red = pre-existing B140 `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak`). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-16-ca-rootshard-protobuf-codec*`. **STILL TODO:** (B164b) bound the journal — needs GC throughput ([[B160]]/[[B163]]); (B164c) journal/refs split — REJECTED by user (doubles hot-path PUTs). Soak-validate the CPU/byte win after a full `clickhouse` binary rebuild + instrumented re-run. ORIGINAL FINDING below. — the RootShard manifest is ~280KB JSON that is 98% UNTRIMMED JOURNAL, read-modify-written on the WHOLE workload write path. The dominant CPU bottleneck.** Hard data (ch2 GC-leader, ~3h in): one shard manifest = 287601 bytes JSON; `num_refs=38` (refs_bytes ~6.5KB, the live state) vs `journal_len=2430` (journal_bytes ~300KB — **98%**); `shard_version=22222`, `fence_round=165`. CPU profiler (`allow_introspection_functions=1`, trace_type='CPU', 900s): #1 leaf `DB::WriteBuffer::write(char)` (char-by-char `writeJSONString`); top full stacks all `encodeRootShard`/`decodeRootShard`/`Cas::checkNoUnknownKeys` from `Store::mutateShard` ← `Build::publish` (INSERT) and ← `dropRef`/`republishRef` ← `moveDirectory`/`removeDirectory` ← `IMergeTreeDataPart::remove()` (TTL/merge part removal). Each ref op: GET ~280KB → parse JSON (Poco `Var` map + validate every key) → modify 38 refs → re-serialize ~280KB → PUT. ProfileEvents confirm RMW: `S3PutObject`=7.73M ≈ `S3GetObject`=7.70M; plus `S3HeadObject`=27.67M (3.6×). **Why 98% journal:** ~135 mutations/round/shard × should trim to ~135, but journal=2430 ≈ 18 rounds of trim backlog → GC `trim` not keeping pace ([[B160]]) → journal bloat → every publish/drop pays 300KB of dead journal → positive feedback (hotter → slower → GC further behind). **This is the "comp is hot" mechanism + a big chunk of the RustFS saturation (300KB × 7.7M PUTs).** FIX DIRECTIONS: (a) **binarize the `RootShard` codec** (extend B147's binary snap codec to the manifest) — kills the JSON constant factor (`writeJSONString` char-by-char, Poco `Var` allocs, `checkNoUnknownKeys`) on the hottest path; (b) **bound the journal independently of GC** (trim more eagerly / cap length + force a local fold) so a behind GC can't inflate the write path; (c) deeper: **stop rewriting the whole manifest per mutation** — separate the append-journal from the refs object so a publish/drop doesn't re-serialize the whole journal. Reframes [[B160]] priority: not GC hygiene but whole-system write-path cost. No correctness issue. Evidence: soak #7 live profiling 2026-06-16. | Soak #7 CPU/manifest profiling | (a) binary RootShard codec (biggest constant-factor win, contained); (b) journal cap/eager-trim; (c) journal/refs object split. Related: [[B147]] (binary snap codec precedent), [[B148]] (op-count), [[B160]] (trim lag = the amplifier). |
| B163 | **NEW milestone (2026-06-16, brainstorm) — prefix-sharded PARALLEL GC ("M-G").** Separate, larger effort split out of the B160 leader fix (user decision: fix B160 leader-election first, shard GC as its own spec). **Groundwork already in code:** the GC is internally hash-prefix-sharded — `hashPrefixShard(hash, snap_shards)` routes every blob/tree/pack; snap/retired/outcomes persist per snap-shard (`gcSnapKey`/`retiredKey`/`outcomesKey(... , snap_shard)`); and the load-bearing invariant `CasGc.cpp:723` "edges live in the TARGET's snap shard — in-degree is intra-shard" makes the **retire→recheck→delete phase independent per snap-shard** (different nodes could own disjoint prefixes with zero coordination). **The hard part = fold:** `foldShardRecords` reads ALL root-shard journals and `readTree`s every expanded tree, routing edges into the CHILDREN's shards (cross-shard); computing a prefix's in-degree needs every tree that may point into it. So a fully-independent per-prefix fold needs either redundant `readTree`s (a tree with children across prefixes read by several workers) or pre-routing journal edges by TARGET prefix (journal-format change). Two design shapes: (I) **shared fold** (one folder builds the global per-shard snap) + **parallel per-shard retire/delete** (smaller; fold still single point); (II) **fully independent per-prefix GC** with per-shard leases (bigger; subsumes B160 — contention gone by construction). NOTE `snap_shards` appears to default to **1** today (`CasGc.cpp:819` "intra-shard while snap_shards == 1") — infra exists, not yet exercised at >1. Motivation: even at ~77% OK rounds in soak #7 the pool still grows ~22G/h → single-leader **throughput** is a real limit independent of contention. Needs its own TLA+ (per-shard ownership + cross-shard fold edges). | Brainstorm 2026-06-16 | Separate spec/milestone after the B160 leader fix ships. Likely start with shape (I) (parallel retire/delete over a shared fold), then evaluate (II). |
| B162 | **FIXED (2026-06-16, `fac220cf3c9`) — soak-driver OOM (run #6 ended ~3h, `rc=137`); harness bug, NOT a CA fault.** Kernel oom-killer (conclusive): `Out of memory: Killed process 935198 (python3) total-vm:13618192kB anon-rss:12864068kB ... task=python3` @ 03:25:06. The python `soak.run` driver grew to **~12.86 GiB** in 3h (swap 7G/7G also exhausted; Zoom co-resident; disk NOT full, 375G free). The CA cluster was healthy throughout (0 no-ref/0 broken, both nodes 0.01s responsive, replLag ≤6s). **Root cause:** `Model.rows` (the authoritative per-rid oracle, `soak/model.py`) gained ~100 entries per INSERT op and **never evicted TTL-expired rows**; the table sheds them via `TTL … DELETE` (active parts fell 311→172→80) but the dict only grew. With `ts = base_time + (op_id % TS_WINDOW)` anchored to a fixed ~2h window at start, after the TTL horizon ~every inserted row is permanently expired yet was retained → monotonic growth (~12.8 GiB at 3h, ~43 GiB at the full stream). **Fix:** `Model.prune_expired(now)` evicts rows `_expired`/`live_rows` already exclude (safe: `now` monotonic, `ts` fixed → once-expired-always-expired → can't change any aggregate at this/any later `now`); called from `checkpoint` after the aggregate assert (workload drained, `now` quiesced — no worker race). Also stopped storing the unused 256 B `payload` in the model row (only the INSERT SQL emitter needs it, recomputed independently from `row_for_rid`) — it dominated the per-row footprint. Tests: `test_prune_expired_removes_only_expired`, `test_prune_expired_does_not_change_live_view`, `test_model_row_carries_no_payload`; full driver suite 142 pass. | Run #6 OOM diagnosis 2026-06-16 | DONE (`fac220cf3c9`). Relaunch the 12h soak on the fixed driver. |
| B160 | **IN PROGRESS (2026-06-16): spec + TLA+ DONE; C++ impl pending. Fix = clock-free lease + advisory heartbeat (Approach A).** Spec `2026-06-16-ca-gc-lease-heartbeat-design.md`. TLA+ `CaGcLeaseCore.tla` (commit `b27f183e89e`) TLC-green: NoEpochCollision (safety) holds WITH and WITHOUT the heartbeat (atomic single-CAS steal + fence_seq make every steal safe regardless of timing — the heartbeat does not weaken safety); NoFalseSteal holds WITH the heartbeat and is VIOLATED under SabotageNoHeartbeat (7-state counterexample reproduces the false-steal). C++ IMPL DONE (commit `5a56962a7cc`): `gc/hb` key + `GcHeartbeat` codec, `Gc::pulseHeartbeat` (static), heartbeat-gated steal in `acquireOrRenewLease` (`last_seen_hb_*`), heartbeat thread in `CasGcScheduler` (H=interval/4, `i_am_leader` flag). Tests: `GcHeartbeatRoundTrip`, `CasGcLease.HeartbeatBlocksFalseStealOfAliveLeader` + `FailoverStealOnceHeartbeatStops`; existing lease/steal tests green; CA suite 253/254 (1 red = pre-existing B140). REMAINING: soak validation (rebuild `clickhouse`, two-replica soak, confirm contention ~70-80% → ~0 and `gc/state.round` advances steadily). ORIGINAL FINDING below. — GC multi-mounter retire-contention LIVELOCK (real CA bug; the cause of the run #4 80%-404 HEAD storm).** Both replicas run the `CasGcScheduler` against the SHARED pool and race at `retire`: ~70–80% of GC rounds FAIL with `Code 236 ABORTED: "CAS gc retire: gc/state moved during retire (another leader advanced it); retry next round"` (`CasGc.cpp:709`). Measured run #5: ch1 300 failed / 84 ok / 250 retire-contention; ch2 294 / 141 / 245. The scheduler loop checks `report.acquired_lease` and backs off if not acquired (`CasGcScheduler.cpp:69-84`), but at the aggressive `gc_interval_sec=2` BOTH replicas acquire+fold+recheck+`retire` and one loses the `gc/state` CAS every round. **Consequence (instrumented + model-consistent):** the loser burns a full round (lease acquire, `loadSnap`, `fold`, `recheck` — each HEAD-ing candidates) then throws it away; `gc/state` never advances; the candidate set never shrinks; the GC re-scans the SAME + growing stale set every 2 s, HEAD-ing objects the OTHER replica already deleted → **the 80%-404 HEAD storm** (confirmed: run #5 fresh-pool 404-HEAD stacks are 100% `Gc::retire`; the storm grows with pool age as the un-retired backlog grows). Reclamation barely progresses (deleted≈1046 over the run). **NOTE:** this is the *regular incremental* GC (M-C); the lease is a liveness optimization, NOT the correctness mechanism (the publish gate is — per `CaHeartbeat.h`), so the livelock is wasteful + load-amplifying, not a loss/dangle. **FIX DIRECTIONS (needs design + TLA+ check, like B136/B137):** (a) a real single-leader gate so a non-leader backs off BEFORE the expensive fold/recheck (cheap lease-peek first), not after a failed `retire`; and/or (b) lengthen/jitter the lease + `gc_interval` so rounds don't overlap (the implementation's lease observation-window vs the 2 s cadence is mistuned for 2 mounters); and/or (c) only ONE replica runs the GC scheduler for a shared pool (config: `content_addressed_gc_enabled` on one node), with lease failover. Evidence: `docs/superpowers/reports/2026-06-15-ca-soak-opcount-and-rustfs-findings.md` §A2. Related: [[B158]] (the storm's surface symptom), [[B148]]. | Run #5 GC-round-outcome measurement | DEFERRED (backlog only per user 2026-06-15). Fix = single-leader gate / lease-cadence retune / one-scheduler-per-pool; design + model-check before implementing. |
| B159 | **CA disk is NOT traversable top-down via `clickhouse-disks` — need a browsable/diagnosable (even if synthetic) tree. (Introspection gap, sub-project A follow-up; usability, not correctness.)** Repro: `clickhouse-disks --disk ca` → `ls /` returns EMPTY and `cd store` → `Code 36 BAD_ARGUMENTS "Path store at disk ca is not a directory"` even though the pool has 444k objects / live tables. ROOT CAUSE: `ContentAddressedMetadataStorage::listDirectory` (`ContentAddressedMetadataStorage.cpp:486`) and `existsDirectory` only answer for RECOGNIZED path shapes — a table-UUID dir (`store/<3char>/<uuid>` or `data/<db>/<tbl>`), a part dir, detached/shadow dirs, table subdirs — and fall through to `return {}` for everything else, INCLUDING the intermediate scaffolding `/`, `store`, `store/<3char-prefix>`. Those levels are never materialized (the store only knows namespaces keyed by table UUID), so root/`store` list empty and `cd` fails the `existsDirectory` check. Today the only way in is to `cd` straight to a full table path (`store/<prefix>/<uuid>`) whose UUID you got from `system.tables`/`system.parts.data_paths` — you cannot discover it by walking down from `/`. **WANTED:** a nice way to traverse/diagnose the (even fake) tree — e.g. make `listDirectory`/`existsDirectory` SYNTHESIZE the `/` → `store` → `<prefix>` → `<uuid>` levels by reverse-mapping `Store::listNamespaces("")` (live + detached + shadow) to their disk-path forms, so `ls /`, `ls store`, `ls store/<prefix>` work like a normal disk; and/or a dedicated diagnostic command (e.g. `clickhouse-disks` `ca-tree` / extend `ca-gc-dryrun`) that dumps the namespace→refs→tree structure. Keep it READ-only and tolerant of a live/churning pool. Related: [[B132]] (live CLI smoke), [[B134]] (introspection hardening). | clickhouse-disks ls/cd on `ca` disk (user, 2026-06-15) | TODO: synthesize root/`store`/`<prefix>` levels in `listDirectory`+`existsDirectory` (reverse-map `listNamespaces("")`); add gtest for `ls /` → `store`, `ls store` → prefixes, `ls store/<prefix>` → table UUIDs; optional dedicated tree-dump diagnostic command. |
| B158 | **ENV/SCALE (NOT a CA correctness bug, NOT disk load): RustFS per-object 64-permit I/O cap collides with the CA root-shard fanout under the aggressive soak → a 503/412/broken-pipe "storm". Run #5 diagnosis (split out of [[B157]] where it was first mis-lumped as "RustFS saturation").** RustFS CPU = **3.64% (idle)** throughout — it is NOT I/O load. PINNED from `/logs/rustfs.log` (level=`warn`; logs to a FILE, not stdout — first grep missed it): RustFS enforces a **per-object I/O permit cap of 64** and the hot root-shard keys hit it — `WARN I/O queue congestion detected, key soak_pool/roots/.../<n>, permits_in_use 58–63/64, queue_utilization 84–98%` — then sheds with **503 `ServiceUnavailable` "Please retry"** (4,223 in ~9 min; CH saw ~9,940 service-unavailable mentions and retried — 503 is retryable). MECHANISM: the CA design funnels EVERY manifest mutation (INSERT/merge/mutation/GC `dropRef` + every CAS retry) through `root_shards=8` keys/table; 6 workers + merges + GC drive per-key in-flight ops past the 64-permit cap → 503 + connection teardown (`connection closed before message completed` ×869, `ConnectionReset on BodyWrite` ×481) → CH retries → more pile-up on the SAME 8 keys → the compound storm. **412 breakdown** (target keys): blobs=303,758 / roots=12,141 / trees=1,599 — the BULK (blobs) is `putIfAbsentStream` `If-None-Match:*` (`CasBuild.h:36`) returning 412 = **content-addressed dedup hit, benign by design**; only the 12k root `If-Match` 412s are real `casPut` CAS contention (retried, converges). **Broken pipe** (roots=30,248 / blobs=1,000) is connection-level on the high-churn root path; NOT caused by the 412 (counts refute: roots have more broken pipes than root 412s; 304k blob 412s yield only 1k broken pipes) and NOT 1:1 with the 503 either (CH ~30k root broken pipes vs RustFS ~1.3k logged mid-message teardowns → a chunk is plain pooled-connection closure). NOT a correctness risk (broken-pipe/503/412 PUTs never committed; retries converge). SEPARATE in the same log: 2,817 `list_path_raw revjob canceled` + 636 `scan_dir BrokenPipe` = GC/fsck LIST ops canceled by client timeout ([[B154]] family). **SECOND area — TWO verified findings, kept SEPARATE (my earlier conflation of them into "fsck hangs listing roots/" was WRONG and is retracted: the CA layer never recursively lists `roots/` — `listNamespaces` is a registry GET, `runFsck`'s `listAll` covers only `blobs/`/`trees/`/`packs/`, `listNamespaceFiles` lists the verbatim files — so `roots/` recursive-list pathology can NOT be what hung fsck).**
**(A) VERIFIED — 74 GB overwrite-orphan LEAK on `roots/` (storage + recursive-listability), largely self-inflicted.** The CA design overwrites ~18 root-shard keys ~14,900× each (`root_shards=8`); RustFS writes a fresh `roots/<t>/<ns>/<shard>/<uuid>/part.1` per overwrite and defers deleting the old data dir to its background scanner; our `configs/rustfs.env` sets **`RUSTFS_SCANNER_ENABLED=false`** on purpose (to dodge the write-path 503 bursts above) → orphans never reclaimed. RESULT: `roots/` = **74 GB for 18 live keys** (228k orphan `part.1` dirs; `xl.meta` points at the current one) vs `blobs/` = 2.8 GB for 184k immutable blobs. A *recursive* `ListObjectsV2` of `roots/` then 500s (`Io error: timeout`) walking ~228k dirs (3.5 s for ONE shard dir); a *shallow/delimiter* list is instant (0.025 s, returns the 8 shard objects). Fix: raise `root_shards`, op-count reduction ([[B148]]/[[B149]]), or re-enable the scanner; real S3 reclaims overwrites so wouldn't leak. NOTE: fsck doesn't list `roots/`, so this is a STORAGE/scale issue, not the fsck hang.
**(B) VERIFIED — RustFS intermittently 500s on LIST under sustained load, and the AWS SDK retry-loops it.** A boto3 probe (`tmp/s3list_probe.py`, `retries=0`, per-page timing, host→live pool DURING writes) lists `blobs/` (immutable) fully at **~7,640 obj/s** — listing-during-writes is NOT broken — but a whole-pool list drew a `500 InternalError: "Io error: timeout"` at ~300k. ClickHouse's S3 client retries 500s (up to 501×, the same as the write path), so a `fsck` `listAll(blobs/|trees/)` that draws a transient 500 under load retry-loops and APPEARS hung. gdb on the wedged `fsck` (PID 5762, 30+ min) confirmed it was parked in `ListObjectsV2 → AttemptExhaustively → receiveResponse → poll` — but the process exited before I captured the request PREFIX, so WHICH list it stalled on is UNCONFIRMED (NOT `roots/` per the code; most likely a transient-500 retry-loop on `blobs/`/`trees/`). TO PIN: re-run `fsck` + gdb-print the in-flight request URI. Tool follow-up: bounded per-LIST timeout (surface the 500 instead of retry-looping) + progress output; run `fsck` against a QUIESCED pool ([[B154]]). **LEVERS (write path):** blob 412 needs NO fix (dedup). The write storm → **raise `root_shards`** (8→256 cuts per-key concurrency ~32×, keeping each key well under the 64-permit cap; cheap creation-time pool config) + op-count reduction ([[B148]]/[[B149]]). | Run #5 RustFS-log diagnosis | Falsifiable next experiment: a follow-up run at `root_shards=256` should make the congestion/503/storm vanish. Then revisit op-count reduction ([[B148]]/[[B149]]). NOT a B157/correctness blocker. |
| B157 | **CONFIRMED CORRECTNESS BUG in my own Pillar B work + FIXED (2026-06-15, soak run #4 ~1h): the `allow_stale` TTL decode-cache has a read-your-writes COHERENCE RACE that makes a freshly-adopted part transiently unresolvable → marked broken → replication GET_PART livelock. (Initial 2026-06-15 attribution to RustFS saturation was WRONG — corrected below. RustFS saturation is REAL but CONCURRENT/separate, not the cause of these specific broken parts.)** **Smoking gun (user zgrep of `20260615_17792_17792_0`):** `relink`-adopt CLEAN → merge read of `op_id.bin` → `getStorageObjects` line 645 throws `FILE_DOESNT_EXIST "ContentAddressed: no ref for …/op_id.bin"` → "Part looks broken" → detach (`broken_20260615_17792_*`, `broken_21212_*`). The `no ref for {path}` there fires when `resolveRouted` → `resolveRef(ns, ref, allow_stale=true)` returns nullopt — i.e. the **part ref itself** is missing from the decode (NOT a file-within-tree miss; `op_id.bin` is just the file being read at that instant). **Mechanism (CONFIRMED by code reading + a deterministic gtest repro, no soak needed):** `Store::loadShardDecoded` does HEAD→GET→decode→populate. An in-flight reader can HEAD/GET a shard whose bytes predate a concurrent `Build::publish`; the writer's `mutateShard` `casPut` commits the new ref and erases the decode-cache entry (B153), but the in-flight reader then POPULATES the TTL cache with its STALE (pre-publish) decode AFTER that erase. A subsequent `allow_stale` read (the merge's `getStorageObjects` → `resolveRouted`, `ContentAddressedMetadataStorage.cpp:314`) hits the poisoned entry within the TTL window and sees no ref for the just-published part → broken. The adopt's publish happens-before the merge read on the SAME thread, so the merge's own GET would be fresh — the poisoning comes from a CONCURRENT reader, which is why it only surfaced under heavy soak churn. **FIX (commit pending):** per-key monotonic `shard_write_seq` bumped in `mutateShard`'s invalidation; `loadShardDecoded` captures it before its GET and only populates the decode cache if it is unchanged at populate-time (a write that landed during the GET means the decode may be superseded and its invalidation already ran → skip caching, never poison). Keeps the Pillar B op-count win (steady-state: no concurrent write → seq stable → caches normally) AND closes the race. Test `CasStoreDecodeTtl.ConcurrentWriteDuringGetDoesNotPoisonStaleEntry` (GatedGetBackend: snapshot-at-entry + gated return; root_shards=1) reproduces RED→GREEN. **SUB-FINDING (real, still worth a fix, separate):** once a part IS broken, the REFETCH livelocks — `GET_PART 21212` postponed **353,984×** (`postpone_reason: "…we still have broken part with that name. Waiting for the broken part to be removed first"`) → replication stuck (~60min lag, NO_REPLICA storm ch1 171k / ch2 624k). A healthier recovery path should bound/break that spin. **The 412/503/broken-pipe "storm" that I first (wrongly) lumped in here as "RustFS saturation" is a SEPARATE env-side issue — MOVED OUT to [[B158]]** (RustFS per-object 64-permit cap × root-shard fanout; NOT this bug, NOT disk load, NOT a correctness risk). NOT confirmed data loss (dangling=0; the other replica holds a good copy). | B151 soak run #4 live diagnosis (user-driven `system.replication_queue` + `system.detached_parts` + `system.text_log` + zgrep) | DONE (correctness): coherent decode cache (per-key write-seq) — see B157-impl. REMAINING (separate from the fixed coherence race): (a) CA recovery-robustness — bound the broken-part-refetch livelock (the `GET_PART` 353,984× postpone spin); (b) the env-side 412/503/broken-pipe storm is its own item → [[B158]]. |
| B157-impl | **IMPLEMENTED + TDD (2026-06-15) — B157 coherence-race fix shipped. Commit `e1f69b696e6`.** Per-key monotonic `shard_write_seq` in `Store` (guarded by `shard_decode_cache_mutex`): bumped in `mutateShard`'s invalidation block (alongside the existing B153 `shard_decode_cache.erase(key)`); `loadShardDecoded` captures it just before `pool_backend->get(key)` and only inserts into `shard_decode_cache` if the counter is unchanged at populate time (else the just-decoded bytes may be superseded by a write whose invalidation erase already ran → skip caching, never poison; the reader still returns its own point-in-time decode). Steady-state behaviour is unchanged (no concurrent write → seq stable → caches as before), so the Pillar B op-count win is preserved — verified by the still-green `CasStoreDecodeTtl.WarmHitWithinTtlSkipsHead`. Counter kept monotonic across the wholesale cache clear (never reset) so a captured value can't spuriously match. Test `CasStoreDecodeTtl.ConcurrentWriteDuringGetDoesNotPoisonStaleEntry` + `GatedGetBackend` (snapshot-at-entry + gated return; `root_shards=1` so `part_1`/`part_2` collide on shard 0) was RED before the fix (`resolveRef(part_2, allow_stale)` → false) and GREEN after. Full `Cas*`/`CaWiring*` (246 tests) green except the pre-existing baseline `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` ([[B140]] guard). **NOT yet re-validated under soak** (run #4 still up demonstrating the bug; a fresh run should show no `no ref`-driven broken parts). The B157 recovery-robustness sub-finding (broken-part-refetch livelock) and the RustFS-saturation env issue remain open + separate. | B157 fix (TDD) | DONE (code+unit); soak re-validation pending. |
| B156-FIXED | **B156 FIXED + adversarially-reviewed (Approved) (2026-06-15).** `Build::reuseBlob` now takes `body_recreatable` and converts `observeAndAdmit`'s HEAD-absent `FILE_DOESNT_EXIST` → retryable `ABORTED` ONLY when the reused blob is body-recreatable (it was `putBlob`'d by a build in this txn → tokened dep → an INSERT retry re-writes the content → converges). For ADOPTED-from-committed (tokenless W-EVIDENCE) reuses, it stays **fail-loud `FILE_DOESNT_EXIST`** — a HEAD-absent referenced blob there is a genuine INV-NO-LOSS signal that MUST surface, not be masked. Discriminator = new `Build::depIsTokened(hash)` (`putBlob`→tokened, `adoptFromTree`→tokenless `nullopt`, no-dep→false=fail-loud safe default). Per-site `body_recreatable`: createHardLink (`src_st->build->depIsTokened`), moveDirectory-merge (`dst||src build`), moveFile (`src_st.build`). Commits: `cf454abd826` (initial blanket fix — adversarial review caught it MASKED the adopted-blob loss + didn't converge) → `94ff4f7c9cb` (the discriminator fix). Tests: `VanishedRecreatableBlobIsRetryableAborted` (putBlob'd→ABORTED), `VanishedAdoptedBlobStaysFailLoud` (adopt→FILE_DOESNT_EXIST, proves no masking), `DepIsTokenedDiscriminatesPutBlobVsAdopt`; `observeAndAdmit`/`putBlob`/`resurrect`/`adoptTree` byte-unchanged; CA suite green except baseline `CasGcLeak`. **Minors (non-blocking):** `hasDep` helper is test-only (no prod caller); **residual follow-up** — the deeper root is GC condemning an in-flight build's blob after a heartbeat LAPSE under load (the B156-fixed retry recovers it, but the heartbeat-renewal-cadence-vs-GC-condemnation-grace under heavy load is worth a separate look). | B156 fix + 2-round adversarial review | DONE; re-run the 12h soak with the fix (run #4). | 
| B156 | **REAL CA dedup-vs-GC robustness bug (2026-06-15, soak run #3 ~1.25h, steady stage) — `reuseBlob`/`observeAndAdmit` HEAD-absent branch is NOT retry-safe against the dedup-reuse-vs-GC vanish race; surfaces as a fatal `FILE_DOESNT_EXIST` INSERT failure. NOT data loss, NOT introduced by this session, NOT B151.** Error: `Code 107 DB::Exception: Build: object soak_pool/blobs/ab/ab065611... absent — cannot reuse (caller must upload it). (FILE_DOESNT_EXIST)` on an INSERT (op_id=31655) → `WORKLOAD FAILURE` → `PHASE3 FAILED`. Mechanism: `Build::observeAndAdmit` (`CasBuild.cpp:193-196`, shared by `reuseBlob`) HEADs the dep blob; **HEAD-absent ⇒ hard `FILE_DOESNT_EXIST`**. The code comment assumes HEAD-absent = "genuinely-never-existed" and is explicitly **"untouched"** by the B136/B137 fixes — but the soak shows a blob the writer DEDUP-REUSED (existed at decision time) can be GC-deleted (exact-token `deleteExact`) BEFORE the reuse-HEAD, a benign race. The sibling condemned-token→`resurrect`→GET-vanish branch correctly surfaces ABORTED (retryable; `putBlob`'s body-holding retry re-uploads), but the HEAD-absent branch does NOT — and `reuseBlob` callers (`ContentAddressedTransaction.cpp:595/737/822` — projection hardlink, merge re-key, adopt) hold no body, so they can't re-upload → the INSERT fails hard. **Not loss** (the insert never committed; no dangling ref created). **Pre-existing latent** (observeAndAdmit/reuseBlob unchanged by B151/B153; surfaced only once the soak sustained heavy insert+dedup+GC churn, which runs #1/#2 didn't reach before their harness wedges). Family: [[B136]]/[[B137]] (resurrect race, fixed) + B139 (async+dedup). **FIX DIRECTION (needs B136/B137-level care + adversarial review):** make the dedup-reuse HEAD-absent case retry-safe — either (a) surface it as retryable `ABORTED` ("dep deleted by GC between dedup-decision and reuse; retry") so the writer retries and, on retry, the now-absent blob is re-uploaded via `putBlob` from source instead of reused; or (b) have the reuse callers retain/re-derive the body and fall back to `putBlob` on vanish. Must NOT mask a genuinely-never-existed dep (a real corruption/loss signal) as retryable — distinguish "was dedup-reused (we expected it)" from "never existed". | Soak run #3 diagnosis | Fix the reuseBlob/observeAndAdmit HEAD-absent dedup-vs-GC race to be retry-safe (re-upload fallback), with adversarial review (INV-NO-LOSS: don't mask a real never-existed dep); then re-run the soak. Decision pending (user). |
| B155 | **SOAK HARNESS chaos gap (2026-06-15, run #2 ~4.9h) — recovery-checkpoint `SYSTEM SYNC REPLICA` is intolerant of the transient `TABLE_IS_READ_ONLY` a replica shows while recovering its ZK session after a chaos fault → spurious `PHASE3 FAILED`. NOT a server/CA bug, NOT data loss.** Run #2 progressed past gc_checkpoint into the chaos stage (B154 fsck-timeout degrade worked); a chaos fault (ch2 restart / rustfs pause) dropped node2's Keeper session — Keeper log: `Found dead session 2` → ~30s later `Received session ID 3` (the replica RECOVERED). During that readonly window the harness's recovery convergence issued `SYSTEM SYNC REPLICA ca_stress` against node2 → `Code: 242 TABLE_IS_READ_ONLY` → the harness treated it as a hard WORKLOAD FAILURE → `PHASE3 FAILED (rc=1)`. This is the same class as [[B154]]: the harness isn't robust to an EXPECTED transient (readonly during ZK-session recovery), exactly as it already retries transport-failures/`ABORTED`. **FIX (harness):** retry `SYSTEM SYNC REPLICA` (and any post-fault convergence op) on `TABLE_IS_READ_ONLY` (Code 242) with bounded backoff until the replica is read-write or a timeout, then proceed — do not count a transient readonly as a workload failure. | B151 soak run #2 diagnosis | Make the recovery-checkpoint SYNC REPLICA tolerate/retry transient readonly; re-run the 12h soak. |
| B154 | **SOAK HARNESS WEDGE (2026-06-15, found during B151 12h soak ~pulse #3) — a checkpoint `clickhouse-disks fsck --detail` hangs the driver; `run_fsck` has NO subprocess timeout. NOT a B151 or server issue.** [B154 fsck-timeout fix VALIDATED in run #2: the gc_checkpoint summary fsck timed out at 180s and DEGRADED GRACEFULLY (`WARNING [B146/B154] ... SKIPPING ... soak continues`), no wedge — the driver progressed into the chaos stage.] The 12h B151 soak driver wedged at ~00:51 (early-steady stage, ~1.5h in): `soak.run` main thread blocked in `do_wait` on `docker exec ca-soak-ch1-1 clickhouse disks --disk ca_ro --query fsck --detail` (pid running **42+ min, not completing**); all worker + ticker threads gone (Threads:1); no log output / metrics tick since #47 (00:51). Root: `soak/fsck.py:98` `run_fsck` does `subprocess.run(cmd, capture_output=True, text=True)` with **no `timeout=`**, and the checkpoint (`run.py:455-457`) runs the `--detail` fsck whenever `unreachable>0` (ALWAYS true — M-F debris). `--detail` is an O(pool) full LIST + per-object HEAD reachability scan; over the **342k-object / 3.6 GB** pool on the single-disk RustFS that was ALSO serving the live GC HEAD storm (28M HEADs), it crawls to effectively-hung, and the un-timed `subprocess.run` wedges the whole driver. This is the [[B146]] offline-fsck O(pool) cost surfacing as a harness hang. **The SERVER stayed healthy + responsive (system.parts 0.02s) throughout** — only the harness's Python driver wedged. **FIX (harness):** add a `timeout_s` to `run_fsck`/`run_dryrun`'s `subprocess.run`; on timeout, raise a typed exception the checkpoint catches → log a loud B146/B154 slow-fsck finding + degrade gracefully (skip the `--detail`-dependent dryrun-subset assert for that checkpoint; keep the cheap summary `dangling==0` loss gate) + CONTINUE, instead of wedging. (Deeper: [[B146]] make offline fsck not O(pool) — incremental/sharded.) | B151 12h soak pulse #3 diagnosis | Add the fsck subprocess timeout + graceful checkpoint degradation; then re-run the 12h soak at full scale to exercise the later delete/TTL/truncate/chaos stages. |
| B152 | **T9 soak verdict (2026-06-14): NOT data loss, but `PHASE3 FAILED` on a post-fault consistency-SETTLING gate — plus a checker MESSAGE BUG that mislabels it as INV-NO-LOSS.** The 2h re-validation soak (seed 20260614, 6w/25GB, on the Pillar B+A1+zstd binary) failed at a chaos-recovery checkpoint: `wait_for_pool_consistent` (`run.py:554-567`) requires `stable`(3) CONSECUTIVE `dangling==0 && exit_code==0` fsck reads within 180s after a fault window; it timed out. **The final read was `dangling=0, exit_code=0` (CLEAN), reachable=129341, unreachable=17430.** So there is **NO loss / NO dangling** — `dangling=0`. The failure is that the pool kept **FLAPPING** in/out of the documented transient post-restart fsck-incoherence window (B141/B144/B145: HEAD-absent reachable trees while the object store is mid-recovery) for >180s under the aggressive load, so the gate never saw 3 consecutive stable-clean reads. The 17430 `unreachable` are M-F-deferred debris (over-count, INV-OVER-COUNT-ONLY-permitted, GC-backlog), explicitly NOT loss per `checker.py:88`. **Two findings:** (1) **Checker message bug** — `run.py:561-566` interpolates `PERSISTENT dangling={last.get('dangling')}` (=0 here) and calls it "a REAL crash-recovery / object-store durability finding (INV-NO-LOSS)"; a `dangling==0` timeout is a SETTLING/flap failure, NOT an INV-NO-LOSS finding. The message must distinguish persistent-`dangling>0` (real loss escalation) from never-stabilized-but-clean (recovery still settling). (2) **Recovery-settling-time signal** — that the post-fault incoherence window stays open >180s under load is plausibly LINKED to [[B151]] + the HEAD storm: slow under-lock manifest writes + 20M-HEAD RustFS overload delay post-fault object-store coherence (manifest visibility lag), keeping fsck flapping. Fixing B151 (publish out of lock) + op-count should let recovery settle faster. Responsiveness over the run: `system.parts` slow(>1s) in 27/220 polls (~12%), recovered to 0.00s at the end. | T9 re-validation soak | (a) Fix the checker message to not cry INV-NO-LOSS on `dangling==0`; gate on `unreachable` stabilization for the settling case. (b) Re-validate post-fault settling after [[B151]] + op-count land. NOT a data-loss bug (dangling=0). |
| B151-impl | **IMPLEMENTED + adversarially-reviewed (2026-06-14) — B151 lock-scope fix shipped (Mechanism B, zero `src/Storages`).** Per spec `2026-06-14-ca-manifest-commit-lock-scope-design.md` + plan `2026-06-14-ca-manifest-commit-lock-scope.md`. Commits: `864d10c11ab` (extract `publishStaging` + `PartStaging.published`), `cf71b1f059c` (B153 read-your-writes cache invalidation), `6f5e38667104` (publish final ref at the lock-free `moveDirectory`/rename; `commit()` skips published), `a9dba473b36` (rollback safety: drop rename-published refs on abandon). The CA manifest publish now happens at the lock-free tmp→final rename for all replicated INSERT/merge/mutation paths; under-lock `commit` does no S3 write. Adversarial review (2 rounds) found + got fixed a **Critical** rollback gap (a part published at the rename before the ZK multi would orphan its ref on ZK-failure rollback → resurrection/unexpected-part risk; fixed by the path-agnostic drop-on-abandon in `~ContentAddressedTransaction` when `!committed`). Final review verdict: **Approved**. Tests: new `CaTransactionLockScope.*` (4 tests: publish-at-rename, no-spurious-publish-on-committed-move, drop-on-abandon, survive-on-commit) + all 27 `CaWiring*` green; only `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` red (genuine B140 guard). **Remaining (minor, non-blocking):** optional `chassert(!dst_st.published)` before the staged-source merge in `moveDirectory` (defense-in-depth for a theoretical merge-into-already-published-dst; no reaching sequence found). **Pending:** soak re-validation (B151-T4) — confirm `system.parts` responsive + no `WriteBufferFromS3` under `DataPartsLock` in trace_log. | B151 implementation | DONE (code); soak re-validation pending. |
| B151 | **ROOT CAUSE of CA `system.parts` unresponsiveness — CONFIRMED by trace + code (2026-06-14 T9 re-validation soak). The unresponsiveness is a LOCK-SCOPE bug, NOT S3 op-count: the CA manifest S3 write happens while holding the exclusive MergeTree `data_parts` lock.** After Pillar B + A1-min + zstd landed (op-count down, GC-leader CPU healthy ~<250%, no 427% saturation), `system.parts` STILL stalls intermittently up to the 60s+ client timeout (~35% of 60s-spaced polls slow; observed server-side `query_duration_ms` up to **220s**). `system.query_log` for those slow `SELECT count() FROM system.parts WHERE active` queries shows **`S3HeadObject=0, S3GetObject=0, OSIOWaitMicroseconds=0`, and only 6–12 ms of CPU** for a 60–220s wall-clock query → the query did NO work and NO I/O; it was BLOCKED ON A LOCK. `system.trace_log` (Real, 220/220 samples) parks it entirely in `DB::SharedMutex::lock_shared() <- DB::DataPartsSharedLock <- DB::MergeTreeData::getDataPartsVectorForInternalUsage <- DB::StorageSystemParts::processNextStorage` — i.e. waiting for the SHARED `data_parts` lock, blocked by an exclusive holder. Server-wide Real traces show the dominant wall-time is CA manifest writes blocked in `WriteBufferFromS3::finalizeImpl <- TaskTracker::waitAll <- future::get` (the multipart PUT to the overloaded single-disk RustFS) across `mutateShard`→`casPut` from publish/commit/dropRef/republishRef/heartbeat. **The smoking gun:** the exclusive-lock holders blocked on that S3 write are `DB::MergeTreeData::Transaction::commit(DataPartsLock&) <- ReplicatedMergeTreeSink::commitPart` (INSERT commit) and `<- StorageReplicatedMergeTree::checkPartChecksumsAndCommit <- ReplicatedMergeMutateTaskBase` (merge/mutation commit). **Code confirms it:** `MergeTreeData::Transaction::commit()` (`MergeTreeData.cpp:8790`) does `auto lock = data.lockParts();` then `commit(lock)`, which at `MergeTreeData.cpp:8805-8807` calls `part->getDataPartStorage().commitTransaction()` — for the CA disk this is `ContentAddressedTransaction::commit` → the manifest CAS S3 PUT — **all under the held exclusive `data_parts` lock**. Under a slow store each commit holds the lock for seconds; a continuous stream of part commits (INSERT/merge/mutation at load) starves `system.parts` and every other `data_parts` reader for 60–220s. **This is an architectural mismatch:** the MergeTree commit path assumes metadata-commit is a fast LOCAL op (true for the standard S3 disk — only DATA on S3, metadata is a local file), but the CA disk puts the MANIFEST (metadata) on S3, turning the in-lock metadata commit into a network round-trip. **Op-count work (B147/B148/B149: Pillar B+A1+zstd) is still valuable** (it cut CPU + HEAD storm + RustFS load, changing the failure from sustained 28s degradation to intermittent stalls) **but does NOT address this root** — neither A1b (snap checkpoint) nor A2 (retire-HEAD) touch the commit lock-scope. **FIX DIRECTION (do NOT implement without scope approval; architectural):** get the CA manifest network write OUT from under the `data_parts` lock — e.g. perform the durable manifest CAS BEFORE acquiring `lockParts` (the manifest publish is the CA commit point; the in-memory part-state swap under the lock should be a fast pointer flip), or split `IDataPartStorage::commitTransaction` into a slow durable phase (S3, lock-free) + a fast in-memory phase (under lock). Must preserve commit atomicity (manifest durability vs visible part state) and crash-consistency. No correctness issue observed (no dangling/loss; counts bounded). Evidence: T9 soak (seed 20260614, 6 workers, 25 GB) `query_log`+`trace_log` with `allow_introspection_functions=1`; poller TSV `utils/ca-soak/logs/revalidate_poll_*.tsv`. | T9 re-validation trace analysis (user-directed: query_log ProfileEvents + trace_log Real) | Move the CA manifest CAS write out of the exclusive `data_parts` lock (durable-publish-before-lock, or split commitTransaction into durable+in-memory phases); re-validate `system.parts` stays responsive under commit load. This is the top responsiveness lever, above A1b/A2. |
| B149 | **DESIGN DIRECTION (agreed 2026-06-14) — "Reduce CA S3 op-count" (the root performance fix; supersedes the patch-level framing of B147/B148).** The soak proved the CA disk's per-operation S3 round-trip count (esp. HEAD) is the dominant scalability bottleneck (B148: 16M HEADs, 66% S3 read-error/retry storm melting a single-disk store; B147: per-round whole-pool snap GET+PUT). Agreed root design = THREE elements (spec: `docs/superpowers/specs/2026-06-14-ca-reduce-s3-op-count-design.md`): **(Pillar A) resident-snap incremental GC** — keep the snap resident in the leader's `Gc` across rounds, fold only the journal delta, persist the snap only at periodic checkpoints (not every churned round), recover by re-folding from `folded_cursor`; this removes the per-round whole-snap GET+PUT AND lets `retire` use the resident snap's known token/size instead of a per-candidate HEAD. **(Pillar B) `resolveRef` decode-cache: no HEAD on a warm hit** — `Store::readShardDecoded` HEADs the shard manifest on every call to validate the head-token decode cache (`CasStore.cpp:163`); make a warm hit skip the HEAD via bounded-TTL + single-flight coalescing (with a force-fresh path for callers that need strict freshness, e.g. the publish gate) — the biggest single op-count reducer (read path). **(+ zstd) compress the snap blob** with ClickHouse's existing zstd infra (on top of the B147-part1 binary codec) — smaller checkpoint/recovery bytes. Delete safety unchanged (retired/outcome sets keyed by round remain the durable authority; snap is a derived candidate-selector). Adversarial-review gate on both pillars (INV-NO-LOSS / INV-NO-RETURN; Pillar B staleness must not let the publish gate pass a condemned dep). | Soak root-perf design (B147+B148 consolidated) | Implement per the spec: Pillar A (incremental GC) + Pillar B (resolveRef decode-cache) + zstd snap; re-validate the aggressive soak stays responsive (system.parts bounded, HEAD-rate down, no error storm). |
| B167a | **DEFERRED (B167 watermark follow-up) — wire `WatermarkKeeper::farewell()` to a clean-shutdown hook.** The per-server watermark spec calls for a farewell (`min_active = UINT64_MAX`) on graceful shutdown so GC can reclaim that epoch's `everEdged∧InDeg=0` blobs immediately. Currently `~Store()` only calls `stopBackground()` (no farewell) — there is no clean-shutdown hook plumbed, and calling farewell from the dtor would wrongly retire the epoch on a transient drop/re-open. Without it, a cleanly-shut-down server's leftover in-flight debris waits for the K=2 frozen-`seq` crash detection instead of being reclaimed at once. Correctness preserved (frozen-seq handles it), just slower reclaim. | No clean-shutdown signal reaches `Store`; dtor-farewell is unsafe on transient drop | `Core/CasStore.cpp` `~Store`/a new shutdown hook; `WatermarkKeeper::farewell()` already implemented |
| B167b | **DEFERRED (B167) — `LocalObjectStorage` drops S3 user-metadata, so local-disk CA mode does not persist the watermark owner triple.** `LocalObjectStorage::writeObject` ignores `attributes` and never populates `ObjectMetadata::attributes`, so a CA pool on a local disk (test/dev mode) writes no `cas_owner` → GC treats every blob as unprotected (pre-watermark behavior) → the B167 livelock can recur on local-disk CA under productive GC. The publish-gate backstop still prevents data loss (liveness only). Production/soak uses S3 (RustFS), which round-trips metadata, so the load-bearing path is covered. | Local mode is test/dev only; fixing needs a metadata sidecar in `LocalObjectStorage` | `src/Disks/.../Local/LocalObjectStorage.cpp:253` writeObject + getObjectMetadata |
| B167c | **DEFERRED (B167) — remove the now-redundant per-build `HeartbeatKeeper`.** The watermark redesign supersedes the per-build `builds/<build_id>` heartbeat for incremental-GC liveness, but `Store::startBuild` still creates it (kept additive to avoid breaking the heartbeat tests + the deferred full-GC debris path that reads the header `build_id`). Once full-GC debris classification is reconciled with the watermark (read `cas_owner` + the server watermark instead of `builds/<build_id>`), drop the per-build keeper entirely. | Removing now would break heartbeat tests + the full-GC debris path; needs full-GC reconciliation first | `Core/CasStore.cpp` `startBuild`; `Core/CasBuild.{h,cpp}`; full-GC debris in `CasGc.cpp` §8 |
| B167d | **MINOR (B167) — GC re-HEADs an absent-watermark server once per candidate within a round.** `Gc::watermarkOf` returns nullptr (caches nothing) when `servers/<id>` is absent, so a candidate set owned by a server with no watermark costs one HEAD per candidate per round. In practice servers always have a watermark (anchored by `Store::open`), so this only bites for pre-watermark objects or a truly-removed server. Cache a "missing" sentinel per round if HEAD volume shows up in op-count metrics. | Rare in practice (servers anchor their watermark) | `Core/CasGc.cpp` `watermarkOf` |
| B167e | **NOTE (B167) — convergence depends on the live build's background watermark renewer running.** A build whose renewer thread is wedged while its commit path lives can have its watermark `seq` freeze; the K=2 detector then declares the server dead and GC may reclaim the in-flight blob. The publish gate backstops this (abort+retry, no data loss), but the build will not CONVERGE until the renewer recovers — the same "wedged heartbeat" caveat as the original protocol. `background_heartbeats` is on in production (`ContentAddressedMetadataStorage.cpp:218`). A wedged-renewer-but-live-commit watchdog (mirrors B160 risk #3) could escalate. | Inherent to the liveness mechanism; matches the spec's wedged-heartbeat trace | `Core/CasWatermark.cpp` background loop; potential watchdog |
| B167f | **FINDING (B167 short-soak validation, 2026-06-17, seed=20260613 phase-3 10m no-chaos) — CORRECTNESS CLEAN, but a checkpoint quiescence-drain HANG + very high GC HEAD volume.** Results: fsck `dangling=0/unreachable=0`, `broken=0`/`noref=0` on both replicas, both replicas + the oracle agree EXACTLY (604895 rows), watermark live+renewing (`servers/<id>`), GC reclaimed 114k→39k objects, B160 contention 1/233 rounds, mem peak 1.6 GiB. **The run still exited 1**: the stage-boundary checkpoint's quiescence "initial drain" stuck at 8 ops for 600s ("genuine hang") at the `gc_checkpoint` stage (pool ~114k objects); the B146/B154 entry-gate fsck also timed out (>180s). The server executed every op correctly (oracle + both replicas match) and replicas converged the instant the driver exited, so the drain hang is a HARNESS-drain/op-count issue, NOT a CA/watermark correctness regression. **HEAD/PUT = 3.1M/278k (~5200 HEAD/s)** — dominated by the GC observe scan; PLAUSIBLY amplified by the watermark itself: a protected (live-build-owned) `everEdged∧InDeg=0` blob is SKIPPED (not condemned), so it remains a zero-in-degree candidate and is re-HEAD'd every GC round until its build finishes — more candidates lingering → more observe HEADs → S3 pressure → slow quiescence. Mitigations to evaluate: (a) cache the per-candidate observe result / skip re-HEAD of recently-protected candidates within a few rounds; (b) have GC drop watermark-protected hashes from the candidate set for a cooldown; (c) the B157 op-count reductions. | Surfaced by the post-implementation soak; correctness unaffected; gates a clean 12h run | `Core/CasGc.cpp` retire observe loop (HEAD per candidate per round); ties into B157/B146/B154 |
| B167g | **BLOCKER for the 12h soak (2026-06-17, hour 1 of the B167 12h run) — RustFS on-disk VERSION amplification on the mutable `roots/` manifests fills the disk.** At t=73min the pool's `roots/` prefix held **996,289 objects / 234 GB** (vs `blobs/` 943k objects / 18 GB), growing ~3.2 GB/min; host had 155 GB free ⇒ disk-full ETA ~48 min, so a 12h run is physically impossible on this config. ROOT CAUSE (confirmed by `find`): each root-shard manifest dir (`roots/<srv>/<table>/<shard>/`) accumulates one on-disk version subdir per `casPut` overwrite (`…/<uuid>/part.1`), and the soak sets `RUSTFS_SCANNER_ENABLED=false`/`RUSTFS_HEAL_ENABLED=false` (rustfs.env, to avoid 503 bursts / B161) — but the scanner is also what COMPACTS overwritten versions, so they leak forever. The CA layer is CORRECT throughout (S3 API returns the current version; `broken=0`/`noref` benign tmp-fetch; oracle+replicas matched in the short soak); this is purely RustFS-storage + harness-config, exacerbated by the B167f high CAS/op rate. The CA design's "DELETE is free, backend compacts" assumption does NOT hold for RustFS-scanner-off. Mitigations: (a) re-enable `RUSTFS_SCANNER_ENABLED=true` (accept the 503 noise — CA retries it; heal can stay off) so versions compact → sustainable disk; (b) reduce the manifest CAS-rewrite RATE (B157/B149 op-count) — fewer publishes per unit work; (c) periodic RustFS restart/compaction in the harness; (d) a backend that doesn't retain overwrite versions. 60x503 + replLag 8s at hour 1 corroborate RustFS saturation. | Surfaced by the B167 12h soak; gates ANY multi-hour CA soak on RustFS | `utils/ca-soak/configs/rustfs.env` (scanner flag); `roots/` casPut rate in the publish path; B146/B157/B161 |
| B167g-update | **UPDATE (2026-06-17): the roots/ on-disk version amplification is LOAD-DEPENDENT.** Re-run with WORKERS=2 (vs 6), scanner OFF: roots/ stays BOUNDED at ~4-5 MB (sawtooth) over 25+ min, disk ~0.4 GB/min total (mostly GC-managed blobs) — a sustainable 12h. So RustFS's own internal version cleanup keeps pace at the workers=2 publish rate but NOT at workers=6 (where roots/ hit 234 GB / 1M objects in 73 min). Conclusion: B167g is a THROUGHPUT limit of RustFS version-reclaim vs the publish/casPut rate, addressable by op-count reduction (B157) OR lower load — NOT a hard blocker, and NOT a watermark/CA-correctness issue. Scanner-ON is NOT the fix (it broke bring-up: createbucket mc signature error). Multi-hour soaks: run at reduced WORKERS + the disk watchdog until B157 lands. | n/a (finding) | WORKERS knob in run_24h.sh; B157 op-count |
| B167h | **FINDING (B167 12h soak, 2026-06-17, workers=2 scanner-off chaos run) — chaos-stage RECOVERY-checkpoint oracle assertion `dryrun ⊆ unreachable` failed on 1 blob; NOT a correctness violation.** The run was clean for ~4.9h (warmup→steady→mutations→ttl→gc_checkpoint, INCLUDING the gc_checkpoint quiescence-drain that killed the workers=6 run — confirming B167g/B167f are load-specific), then a post-chaos-fault recovery checkpoint failed: `clickhouse disks ca-gc-dryrun` predicted blob `2bc9925c…` reclaimable, but the separate (non-quiesced, fsck-gate-timed-out per B146) `fsck` classified it reachable, violating the harness invariant "dryrun ⊆ fsck.unreachable". `dangling=0`/`broken=0`/`noref=0` throughout — NO INV-NO-DANGLE/NO-LOSS violation. ROOT CAUSE (probable): a dryrun-vs-fsck SNAPSHOT race on a live pool during chaos — a content-addressed blob that was unreachable when dryrun ran got re-referenced by a concurrent insert (dedup) before fsck ran, so it is legitimately reachable now; the actual GC delete-time fence/recheck (not the dryrun) is the safety mechanism, so no real loss is possible (and dangling=0 confirms it). The dryrun⊆unreachable assert is only sound under quiescence, which the B146 fsck-timeout prevented here. Fix: make the harness dryrun-subset assert quiescence-gated (skip when the fsck gate timed out), or account for content-addressed re-reference. Restart-recovery itself WORKED (no connection-refused; the watermark epoch/restart path handled chaos faults). | Harness oracle limitation under chaos + B146 fsck-timeout; not a product bug | `utils/ca-soak/soak/` checkpoint dryrun-subset assert; B146/B154 fsck speed |
| B167-DONE | **VALIDATED & DONE (2026-06-17) — per-server build watermark.** Implemented (13-task plan), TLA+-verified (`CaBuildWatermark` guard+sabotages, `CaBuildWatermarkNum` numeric multi-server+monotone-build_seq finding, + safety invariants), unit-green 265/266 (only pre-existing B140 red). Soak: short soak fully clean (`dangling=0`/`broken=0`/`noref=0`, oracle+both replicas exact 604,895); 12h attempts ran clean through warmup→steady→mutations→ttl→**gc_checkpoint** (the drain that killed the workers=6 run — load-specific, B167g) at workers=2, incl. a ~4.9h chaos run exercising node restarts (the epoch/restart path). All soak failures were infra/op-count (B167f/g) or a harness oracle race (B167h), never the watermark. Replaces the reverted per-build heartbeat existence guard. Commits `15c77af`..`02e5023a` + spec/TLA+/plan. | DONE | n/a |
| B168 | **OPTIMIZATION TRACK — CA S3 op-count reduction (the real cost lever).** Living proposals doc: `docs/superpowers/reports/2026-06-17-ca-s3-opcount-optimization-proposals.md`. Measured steady cost ≈ **$571/day** (workers=2 sample), ~57% pure waste: GC 404-HEAD storm (~$147/day, ~367M 404-HEADs/day, ALL on the GC leader — re-HEADing already-deleted candidates) + 412 CAS/dedup retries (~$178/day). Proposals P0-P9: **P0** instrument all CA disk events per (namespace,op,outcome) at the backend chokepoint (`part_log` shows put=0 — PUTs ride the bg `threadpool_writer`); **P9** prune deleted keys from the GC observe set (kill the 404 storm); **P1/P2** local dedup cache + HEAD-before-PUT; **P3** CA fetch-by-relink (replication re-reads shared blobs); **P4** batch publishes / fewer manifest casPut; **P5** widen root shards; **P6** dirty-only fence; **P7** audit mutation LISTs; **P8** coalesce CAS retries. Independent of the watermark. STARTING with P0 + a short soak to collect per-event stats. | OPEN (active) | `Core/CasObjectStorageBackend.cpp` chokepoint; `Core/CasGc.cpp` (P9); B157/B149 |
