# Archived Backlog — Content-Addressed Shared MergeTree integration

Items moved out of the live backlog (`cas-mergetree-integration.md`) during the 2026-06-12
M-W grooming pass: DONE items needing no further attention, and items made OBSOLETE by the
incarnation-core rewrite (M-C1..M-C3) + the M-W cutover that deleted the PoC wholesale.
Rows are preserved verbatim (plus an ARCHIVED note where the resolution was not already in
the text) — they carry the project's institutional memory: root-cause analyses, fix shapes,
and verification evidence.

## Done — verified, no further attention needed {#done}

| # | Item | Status / why deferred | Notes |
|---|---|---|---|
| B4 | **DONE (2026-06-04). FREEZE/UNFREEZE PARTITION and FREEZE/UNFREEZE ALL now work on a content-addressed disk.** Mechanism: each frozen part is published as its own ref in a GC-rooted `shadow/` namespace that **mirrors** the physical `store/<uuid[:3]>/<uuid>` table tree (`shadow/<backup>/store/<uuid[:3]>/<uuid>/refs/<part>`), sharing the live part's blobs zero-copy (same content-only `part_id`). The shadow ref is an independent GC root — `listLivePartIds` scans `shadowRefsRootPrefix` — so a snapshot survives the live part being merged or dropped. `SYSTEM UNFREEZE` / table-scoped `UNFREEZE` enumerate and remove via the CA metadata-storage shadow routing. **Gate lifted:** `checkAlterPartitionIsPossible` no longer rejects `FREEZE`/`FREEZE_ALL`/`UNFREEZE` on CA. **7 FREEZE stateless tests un-gated (all pass under the CA-default job):** `00952_part_frozen_info`, `01325_freeze_mutation_stuck`, `01414_freeze_does_not_prevent_alters`, `01417_freeze_partition_verbose`, `01417_freeze_partition_verbose_zookeeper`, `03611_freeze_partition_parallel_verbose`, `03612_freeze_partition_parallel_verbose_zookeeper`. New inline-CA oracle `05003_content_addressed_freeze` passes on both the CA-default and plain jobs. **Deferred (B4a): `freezeRemote`** (cross-disk FREEZE to a DIFFERENT destination disk) is NOT handled — only same-disk FREEZE. The plug-in point is `DataPartStorageOnDiskBase::freezeRemote`; no test exercises it on CA. ——— ORIGINAL: **FREEZE: two flavors.** (i) native cheap freeze = a `frozen/<snapshot>/<part>` **ref-set that is a GC root** (no copy; blobs stay in the pool; survives table drop). (ii) **materialized self-contained export** = real bytes copied out (filesystem-backup contract). | M1 reserves the `frozen/` namespace and makes GC **root-aware** of it; the `ALTER … FREEZE` DDL (i) and the materialized export (ii) are deferred. As of M9 W2 (2026-06-03) this was the only remaining gated partition op on CA (`checkAlterPartitionIsPossible` rejected `FREEZE`/`FREEZE_ALL`/`UNFREEZE` with `Code: 344`); ATTACH/REPLACE/MOVE/DETACH all work (B21 RESOLVED). | (i) is just another root namespace via `RefCatalog`. (ii) overrides `freeze`/`freezeRemote` (independent `IDataPartStorage` virtuals) to copy bytes. Neither changes the pool layout. |
| B6 | **DONE / already covered (2026-06-04 review).** Manifest determinism & golden tests. `computePartId` (`PartManifest.cpp:105`) is purely content-derived: a `SipHash` over the sorted (std::map) `(logical_file, checksum)` stream with the mutable per-part files (`isMutablePerPartFile`) excluded — it takes NO `server_id`, NO `key_prefix`, NO `uuid`/`txn_version`/`metadata_version`. So the part_id is deterministic and **server-independent by construction**, which is exactly the property the shared-pool cross-server dedup and the replication fetch-by-relink rely on (two servers writing identical content → identical part_id → one blob set). The serializer is canonical little-endian (B19). Locked by existing gtests: `StableHashIsCanonical` (order/canonicalization), `GoldenBytes` + `GoldenPartIdUnchanged` (cross-version golden bytes for the manifest + the id), `RejectsUnknownVersion` (forward-compat fail-closed), `MutablePerPartFile.PredicateMatchesPartIdExclusion` (the exclusion set), plus ref-payload + sidecar golden tests. A "part_id independent of server_id" test would be vacuous (the function cannot take a server_id). No code/test gap; no structural impact. | Quality gate, matters for dedup/relink not correctness | Pinned by the manifest/part_id golden + canonicalization gtests above. |
| B16 | **DONE (2026-06-05).** `BACKUP`/`RESTORE` integration & validation on content-addressed disks. | Reuses the normal read path (`getStorageObjects`) and the CAS write path on restore, but needs explicit end-to-end validation (the `ATTACH_PART` path, interaction with `frozen/`) | **DONE (2026-06-05).** BACKUP-read already worked; RESTORE now routes each part-write through one whole-part `ContentAddressedTransaction` (`restorePartFromBackup`, `d384298602b`). All 13 BACKUP/RESTORE tests un-gated + passing on CA, zero re-gates (`868c4f869ef`). Oracle `05005_content_addressed_backup_restore` (CA + plain green). See the "B16/B34 … DONE" note below. |
| B19 | **DONE (M5.4). Little-endian on-disk format encoding** — the persisted `PartManifest` (de)serialization used host-byte-order `memcpy` of integers; since manifest bytes are content-addressed, host order would cause cross-arch divergence. **Fixed in M5.4:** `PartManifest::serialize`/`deserialize` re-implemented on the shared codec (`MAGIC(4)+version(1)` + `writeBinaryLittleEndian`/varint/length-prefixed strings via `IO/WriteHelpers.h`+`ReadHelpers.h`), fail-closed on a bad magic / unknown version. `RefSidecar` and `_pool_meta` moved onto the same `FormatHeader`/codec. `part_id` is over `(filename, checksum)` NOT manifest bytes, so identities/dedup are unchanged (pinned by a golden `part_id` test). | **DONE** (M5.4); unit `ContentAddressed*` 47→59 incl. golden manifest/ref/sidecar/`_pool_meta` byte tests + unknown-version-rejection + golden `part_id`-unchanged; stateless 04278–04282 `[ OK ]` | (done) `Codec.h` (new shared LE/varint/header codec), `PartManifest.{h,cpp}`, `PoolMeta.{h,cpp}`. Relates to B6 (determinism). |
| B21 | **Partition-cloning ALTERs corrupt parts (P1, review).** `MergeTreeData.cpp:6519` grants `ContentAddressed` the full `PlainRewritable` ALTER-PARTITION set. `MOVE`/`REPLACE PARTITION` and `ATTACH PARTITION … FROM` reach `cloneAndLoadDataPart`/Backup → `DiskObjectStorage::createHardLink` **once per file with no enclosing transaction** → each autocommits a one-file manifest/ref and overwrites the destination ref → the clone keeps only its LAST file. | **RESOLVED for ATTACH/REPLACE/MOVE (M9 W2, 2026-06-03); FREEZE → B4** | **RESOLVED (M9 W2, 2026-06-03).** The `moveDirectory` detached-staging→active branch (`republishDetachedStagingIntoActive`, 6e076f1feb8) closed the data-loss gap, and the gate was lifted (95f2c7e75ef) for `ATTACH PARTITION` / `ATTACH PART` / `REPLACE PARTITION[ FROM]` / `MOVE PARTITION TO TABLE` / `DETACH`. Final verification under the CA-default stateless config (33-test partition-clone batch): **Passed: 31, Skipped: 1, Failed: 3** — the only failures are the 3 FREEZE tests (`Code: 344 SUPPORT_IS_DISABLED`), no corruption, all clones read back identical data and survive restart. 30 tests un-tagged (1344ad1056e). **FREEZE/UNFREEZE stay gated** (the shadow namespace is unhandled on CA — it is a silent no-op; tracked under B4, the frozen-ref-set work) and **FETCH** stays gated (replication, B1). Cross-disk `MOVE … TO DISK/VOLUME` (byte-copy `clonePart`) + `BACKUP` (B34) remain separate and unverified. Historical root-cause analysis preserved below. ——— **GATED (original finding). M9 W2 (2026-06-03): the whole-part transaction in `DataPartStorageOnDiskBase::freeze` is NECESSARY BUT NOT SUFFICIENT.** Empirically un-gating the clone-class and running the partition-clone stateless tests under CA-default gave **13/13 real-bug failures** (no gate hits), in two clusters: **(1) clone publishes an INCOMPLETE manifest** — `ATTACH`/`REPLACE PARTITION` + `ATTACH PART` produce a part whose manifest is missing refs to specific files (reads throw `FILE_DOESNT_EXIST: no ref for …/primary.idx` / `…/data.cmrk4` in `loadMarks`/`loadIndex`), and `01021_only_tuple_columns` (DETACH→ATTACH `tuple()` round-trip) **silently returned 0 rows instead of 1,000,000** — so the `freeze`→`Backup` clone path does NOT route ALL the source part's files through the CA carry-forward (index/mark files appear dropped). **(2) detached-part discovery is broken**: `ATTACH PART` after `DETACH PART` throws `BAD_DATA_PART_NAME: Detached part "…" not found` in `MergeTreeData::getDiskForDetachedPart` — the CA metadata storage doesn't surface the detached part dir for re-attach. The gtest `WholePartClonePublishesOneRefToSourcePartId` (12667865f05) PASSES because it drives metadata-level `createHardLink`-all-files+commit directly — it does NOT exercise the real `freeze`/`Backup` enumeration, which is where the gap is. **PRECISE ROOT CAUSE (reproduction-debugged 2026-06-03, supersedes the "freeze drops files" theory):** built a local server with an inline CA disk over `local`, ran `INSERT`+`DETACH`+`ATTACH`, and decoded the raw manifests. Findings: **DETACH is CORRECT** — the `detached` ref's manifest is COMPLETE (all 13 files incl `primary.cidx` + every `*.cmrk2`, re-keyed `all_1_1_0/<file>`). The `listDirectory(detached/<part>)` fix (da1b99eb06f) was needed (to load the detached source) but is NOT the bug. The REAL bug is the **ATTACH staging→active rename**: `tryLoadPartsToAttach` renames the detached part to `detached/attaching_<part>` (handled by `rekeyDetachedPartDir`), loads it, then `renameTempPartAndAdd` → `IDataPartStorage::renameTo` → on CA `ContentAddressedTransaction::moveDirectory(from=detached/attaching_<part>, to=<uuid>/<active_part>)`. **`moveDirectory` has a branch for every transition EXCEPT this one** (committed→detached; detached→detached; table-rename; tmp_insert→active) — `from` parses to `part_name==detached`/`file=attaching_<part>` and `to` is active, matching none → it **falls through and publishes NO active ref**. So the attached part lives only in memory (count() works transiently) with no on-disk `refs/<active_part>` → reads throw `no ref for …/<active_part>/<file>` and the data vanishes on restart (the `01021` 0-row signature). Confirmed on disk: after ATTACH there is NO `refs/all_2_2_0`; the clone sits in `refs/detached` as a manifest keyed `attaching_all_1_1_0/<file>`. **REAL FIX (W2):** add a `moveDirectory` branch for **detached-staging → active** (`from=detached/<staging>` with `file` a single component, `to=<uuid>/<part>` with empty `file`): publish a new active ref `<part>` whose manifest carries the detached part's blobs re-keyed from `<staging>/<file>` to BARE `<file>` (+ the mutable sidecar), and remove `<staging>` from the `detached` ref — the detached→active inverse of `republishCommittedPartIntoDetached`. This covers `ATTACH PARTITION`/`ATTACH PART`. Then (b) re-run the partition-clone tests as the oracle (read back IDENTICAL data + survive restart); (c) separately re-check cross-table `REPLACE PARTITION FROM` (`00626`) and `ATTACH PART`-after-`DETACH PART` discovery (`getDiskForDetachedPart`) — they may need their own branch/fix; (d) only then lift the gate. Cross-disk `MOVE … TO DISK/VOLUME` (byte-copy `clonePart`) + `BACKUP` (B34) remain separate. |
| B23 | **DONE (sidecar). Split shared identity from mutable per-ref state (P1, review-sharpened).** `computePartId` excluded `uuid.txt`/`txn_version.txt`/`metadata_version.txt` from `part_id`, but `commit` used to write ONE shared manifest containing all recorded files — so two *logically different* parts (identical content, different mutable files) mapped to the **same manifest**, and the second part read the first's mutable files. **Fixed in M5.3:** one canonical constant `kMutablePerPartFiles` in `PartManifest.h` drives both `computePartId`'s exclusion and the split (`isMutablePerPartFile`). `commit` partitions recorded files: content files → the shared manifest (dedup preserved), mutable files → a PRIVATE per-ref sidecar — a bundle `RefSidecar` index at `store/<server>/<uuid>/refs/<part>.meta` plus one tiny per-file object `refs/<part>.<file>.meta` (the bundle is the list/carry-forward index; the per-file objects back the byte reads, since the disk read path resolves `getStorageObjects`→`readObject` and needs exactly that file's bytes). The read path (`existsFile`/`getFileSize`/`getStorageObjects`/`listDirectory`) overlays the sidecar; a missing entry fails closed. Mutable files are written through `ContentAddressedInlineWriteBuffer` (in-memory, no blob upload → no orphan blob). **Sidecar-orphan decision (option b):** sidecars live UNDER the same `refs/` prefix as the ref, so `removeRecursive`'s ref-scoped deletion (single-part: ref + `<part>`/`<part>.*` sidecars; table: everything under `refsPrefix`) reclaims them synchronously, and a crashed write that never publishes the ref leaves only ref-scoped objects the next DROP reclaims — never a content-addressed object the reachability sweep (blobs/+parts/ only) could miss. Ref-enumerators (table-dir listing, `existsDirectory`, GC `listLivePartIds`) skip `.meta` via `isRefMetaKey`. Tests: collision regression (identical content, different mutable files → same manifest, distinct sidecars), read overlay, sidecar removal/non-CA, and stateless oracle `04282` (`assign_part_uuids=1`, CA-vs-normal, two distinct uuids). | **DONE** (M5.3); unit `ContentAddressed*` 41→47, stateless 04278–04282 `[ OK ]` | (done) `PartManifest.{h,cpp}`, `PoolPaths.{h,cpp}`, `Identifiers.h`, `ContentAddressedTransaction.{h,cpp}`, `ContentAddressedMetadataStorage.{h,cpp}`, `ContentAddressedGC.cpp`. Residual → loose-ends parking lot (metadata-only `ALTER` sidecar rewrite when B30 mutations land; the two sidecar representations bundle+per-file). |
| B28 | **DONE (M5.4). Unify ref-payload parsing (read path vs GC) — load-bearing for GC correctness.** Previously the ref payload was a **bare `part_id`** and the GC parsed it as the "first hex run", a data-loss trap once the ref format gained a version byte / header. **Fixed in M5.4:** the ref payload is now a versioned struct `MAGIC(4 = CARF)+version(1)+part_id` written by ONE `serializeRefPayload` and parsed by ONE `partIdFromRefPayload`, called by BOTH the read path (`ContentAddressedMetadataStorage::readRefPartId`) AND the GC (`ContentAddressedGC::listLivePartIds`) — they cannot diverge by construction. The parser is EXACT (fail-closed on bad magic / unknown version); the "first hex run" parse is gone. Reserves room for B1's `ReplicatedMergeTreePartHeader` + the per-ref mutable fields as future additive versions. | **DONE** (M5.4); golden-byte + unknown-version + bad-magic tests; the single parser is wired at both call sites | (done) `ContentAddressedGC.{h,cpp}` (`serializeRefPayload`/`partIdFromRefPayload`), `ContentAddressedTransaction.cpp` (commit writes the versioned payload), `ContentAddressedMetadataStorage.cpp` (read path). |
| B33 | **Hard-gate `ReplicatedMergeTree` (and any clone-on-commit engine) on a `content_addressed` disk — required before M6.** Replication-internal clones (fetch-fallback / REPLACE/MOVE via the queue → `cloneAndLoadDataPart`→`freeze`→`Backup`→ per-file `createHardLink` autocommit) are background/queue-driven, NOT routed through `checkAlterPartitionIsPossible`, so they would corrupt a part (B21 mode) if a Replicated table ran on CA. M1 sets `supportZeroCopyReplication=false` and is single-server, but there is **no hard rejection of `ReplicatedMergeTree` on a CA disk**. | Review IMPORTANT; the stateless suite (M6) contains Replicated tables → unguarded corruption path | Reject `Replicated*MergeTree` at `CREATE`/`ATTACH` on a `content_addressed` disk (extend `checkContentAddressedDiskRestrictions`) with a clear "not supported yet (B1)". Do in the M6 capability-gate prep. |
| B34 | **DONE (2026-06-05).** Clean BACKUP gate (+ verify pointer-holding BACKUP). `BACKUP` of a table in an **Ordinary** (non-UUID) DB reaches `disk->createHardLink` with a non-part-shaped temp path → CA throws a raw `LOGICAL_ERROR` (fail-closed, NOT corruption). Atomic/UUID DBs (normal) set `make_temporary_hard_links=false` → BACKUP uses pointer-holding (`getStorageObjects`), no `createHardLink`. | Review IMPORTANT (fail-closed, ugly error); ties to B16 | **DONE (2026-06-05).** Pointer-holding BACKUP/RESTORE round-trips on CA are verified end-to-end (Atomic/UUID DBs, the suite default, never reach `createHardLink`). RESTORE materializes parts through one whole-part `ContentAddressedTransaction` (`restorePartFromBackup`, `d384298602b`); all 13 tests pass on CA un-gated (`868c4f869ef`); oracle `05005_content_addressed_backup_restore`. See the "B16/B34 … DONE" note below. |
| B36 | **DONE (M6 Task C).** `DETACH PARTITION` on CA mis-listed in `system.detached_parts` (real bug, category-2). Root cause: `DETACH PARTITION` clones the part into `detached/` via `IMergeTreeDataPart::makeCloneInDetached` → `DataPartStorageOnDiskBase::freeze` → `Backup` → per-file `DiskObjectStorage::createHardLink` with NO enclosing transaction (the B21 mode), so each file autocommitted a one-file `detached` ref and the last-written file (`metadata_version.txt`) won — the `detached` manifest ended up empty + a sidecar holding only `metadata_version.txt`, so `system.detached_parts` showed `metadata_version.txt` instead of `all_1_2_1`. **Fix:** `freeze` now runs the clone through ONE self-created disk transaction when no external transaction is supplied and the disk `isContentAddressed`, so all `createHardLink` calls land in a single `ContentAddressedTransaction` and produce one manifest whose keys are `<detached_part>/<file>` (e.g. `all_1_2_1/data.bin`). `ContentAddressedMetadataStorage::listDirectory` got a `detached`-namespace branch that collapses those keys to the detached part directory names (skipping dir-stripped sidecar files), so `getDetachedParts`'s `iterateDirectory` yields `all_1_2_1`. The rename-based detach path (`renameToDetached` on broken parts → `moveDirectory`) is also handled: CA `moveDirectory` now re-publishes a committed part dir as a `detached` ref via `republishCommittedPartIntoDetached` (re-keys manifest+sidecar under the detached dir, unlinks the source ref). | **DONE** (M6 Task C). | `DataPartStorageOnDiskBase::freeze` (whole-part clone transaction), `ContentAddressedMetadataStorage::listDirectory` (detached branch), `ContentAddressedTransaction::moveDirectory`/`republishCommittedPartIntoDetached`, `PoolPaths.h` (`kDetachedDirName`). Tests: stateless `04287_content_addressed_detach_partition_listing` + example `00502_custom_partitioning_local` `[ OK ]`; unit `MoveCommittedPartIntoDetachedRekeysRef`, `DetachedDirListsPartDirNamesNotInnerFiles`. |
| B37 | **DONE — now SUPPORTED (2026-06-03), superseding the M6 gate.** `non_replicated_deduplication_window` now WORKS on a content_addressed disk (the same non-append rewrite-per-record path a plain s3 disk uses). The M6 `SUPPORT_IS_DISABLED` CREATE gate was removed. Two pieces: (a) **table-level files may live in a subdirectory** — the `deduplication_logs` directory name is RESERVED (`kDeduplicationLogsDirName`, like `kDetachedDirName`) so `<uuid>/deduplication_logs/<file>` resolves as a table-level verbatim file under the `files/` namespace instead of being content-addressed as a part; `parseTableFilePath` keeps the full sub-path tail, and `existsDirectory`/`listDirectory` gained a table-level-subdirectory branch. (b) **`MergeTreeDeduplicationLog::load()` now creates the writer on CA** — a content_addressed (like a Plain) object storage does not materialize empty directories, so the old guard returned before creating `current_writer` for a fresh table and the first INSERT failed closed; CA is now treated like Plain there (fall through → enumerate existing logs → rotate to create the Rewrite writer). Verified: 12/13 dedup stateless tests pass under CA-default (the 13th, `02434_cancel_insert_when_client_dies`, fails for an orthogonal reason — see B57). Tests: unit `ParseTableFilePathNested`, `TableLevelSubdirectoryRoundTripsLikeDedupLog`; stateless `04285_content_addressed_dedup_window_inline_disk` (inline CA disk, dedup behaviour + DETACH/ATTACH reload). The defensive null-writer `LOGICAL_ERROR` in `addPart`/`dropPart` is kept (it now never fires on CA, but still guards other misuse). ——— ORIGINAL (M6 Task C — CRITICAL crash fixed): Non-replicated deduplication log SIGSEGV on a CA disk (real bug, category-2, server crash). A plain `MergeTree` with `non_replicated_deduplication_window > 0` builds a `MergeTreeDeduplicationLog` that writes `deduplication_logs/` files at the table root via `disk->writeFile(…, WriteMode::Append)`. On CA `disk_supports_writing_with_append` is false and the rotate path left `current_writer == nullptr`; `addPart` derefed it (the `chassert` at line 271 is a release no-op) → SIGSEGV inside `INSERT … VALUES` → the server died and aborted the whole run. **Fix (both halves done):** (1) **targeted gate** — `MergeTreeData::checkContentAddressedDiskRestrictions` now rejects `non_replicated_deduplication_window != 0` at `CREATE`/`ATTACH` on a `content_addressed` disk with a clear `SUPPORT_IS_DISABLED` message; (2) **defensive no-crash** — `MergeTreeDeduplicationLog::addPart`/`dropPart` now throw a clear `LOGICAL_ERROR` if `current_writer` is null instead of dereferencing it (general, helps non-CA too). | **Most severe M6 Task B finding — fixed.** Verified: a `CREATE … SETTINGS non_replicated_deduplication_window=…, disk=<CA>` throws cleanly; `03008_deduplication_cases_from_docs` under CA-default now fails with the clean gate error (no crash — the whole 13-test run completed, server survived, only `Received signal 15` SIGTERM at shutdown). | `MergeTreeData::checkContentAddressedDiskRestrictions` (gate), `MergeTreeDeduplicationLog::addPart`/`dropPart` (null-writer throw). Tests: stateless `04285_content_addressed_dedup_window_rejected`; example `03008_deduplication_cases_from_docs` (clean `SUPPORT_IS_DISABLED`, no SIGSEGV). |
| B38 | **DONE (M6 Task C).** `system.remote_data_paths` traversal threw on a CA disk (real bug, category-2). `03000_traverse_shadow_system_data_paths`: the traversal probes `disk->existsFile` on pool sub-dirs (e.g. `…/store`); CA's `existsFile` resolves a non-part path to a verbatim object key and called `LocalObjectStorage::tryGetObjectMetadata` → `std::filesystem::file_size` on a **directory** → `Is a directory` (Code 1001) escaped. **Fix:** `LocalObjectStorage::tryGetObjectMetadata` now treats a directory path as a missing object (returns `nullopt`) instead of calling `file_size` on it — general (helps any caller probing whether a path is a readable object), and exactly what CA's `existsFile`/`getFileSize`/`existsFileOrDirectory` need (a directory is not-a-file → false / `FILE_DOESNT_EXIST`, never a raw FS error). | Surfaced by M6 Task B; fixed. Verified: `03000_traverse_shadow_system_data_paths` under CA-default now `[ OK ]`; `system.remote_data_paths` is queryable on a CA table (no throw). | `LocalObjectStorage::tryGetObjectMetadata` (directory-safe). Tests: unit `ExistsFileOnDirectoryShapedPoolPathReturnsFalse`; stateless `04286_content_addressed_remote_data_paths`; example `03000_traverse_shadow_system_data_paths` `[ OK ]`. |
| B39 | **DONE (2026-06-05, via B67).** Transactions are now FULLY supported on a content-addressed disk: Tier-1 (`INSERT`/`COMMIT`/`ROLLBACK`) was DONE in the CAS txn milestone (2026-06-04), and Tier-2 (transactional merges/mutations/isolation) is DONE via the multi-part `ContentAddressedTransaction` (B67). Transactional `INSERT`/`COMMIT`/`ROLLBACK` now works on a content-addressed disk. The MVCC engine itself (`MergeTreeTransaction`, CSN/TID bookkeeping) is storage-agnostic and UNCHANGED; the work was teaching the CA disk to carry the per-part transaction sidecar. **Mechanism (committed):** (1) the transaction gate is DECOUPLED from append — `StorageMergeTree`'s `support_transaction` no longer derives from `supportWritingWithAppend(disk)`; a new `supportsTransactionalMutableFiles` capability is true for CA, so `supportsTransactions()` returns true and the planner admits the transaction. (2) The per-part mutable `txn_version.txt` (and its `<mutable>.tmp` staging — `isMutablePerPartFile` now also matches `.tmp`) is persisted to the per-ref sidecar via `ContentAddressedTransaction::replaceFile` + `moveFile` mutable handling. (3) A mutable-only commit branch (`recorded.empty()` → sidecar-in-place on an already-committed part) keeps the existing manifest/`part_id`/ref while updating only the mutable sidecar. (4) A relaxed autocommit-write guard in the CA branch of `DiskObjectStorageTransaction::writeFileImpl` allows mutable per-part autocommit writes (commit-on-finalize). **Un-gated + passing on CA (Tier-1):** `01172_transaction_counters`, `01173_transaction_control_queries`, `02345_implicit_transaction`, `01133_begin_commit_race`, `01170_alter_partition_isolation`, `03657_merge_tree_disk_support_transaction`; new inline-CA oracle `05004_content_addressed_transactions`. gtests: 119 `ContentAddressed*` pass. **Re-validated 2026-06-04:** plain (non-CA) txn regression `01172`/`01173`/`01168`/`01174` all pass (6/6, gate-decoupling did not regress normal disks); CA non-txn smoke `04278`/`04279`/`04280`/`04292`/`04287`/`04285` all pass (6/6, the `.tmp`/commit-branch/writeFileImpl-guard changes did not regress ordinary CA paths). **B67 DONE (both layers, 2026-06-04):** transactional merges/mutations now work on CA via a multi-part CA disk transaction (see B67). ——— ORIGINAL (M6, superseded): transactions were a clean capability gate via `supportWritingWithAppend(disk)`=false; ~10 txn isolation tests tagged `no-content-addressed-storage`. | **DONE (2026-06-05, via B67)** — Tier-1 (INSERT/COMMIT/ROLLBACK) DONE; Tier-2 merges/mutations/isolation DONE via B67 (multi-part CA transaction). | `StorageMergeTree::supportTransaction` now via `supportsTransactionalMutableFiles`; `ContentAddressedTransaction::replaceFile`/`moveFile` + mutable-only commit branch; `DiskObjectStorageTransaction::writeFileImpl` CA-branch guard; `isMutablePerPartFile` `.tmp` match. |
| B40 | **DONE (M6 — data-integrity fix).** CA read-miss `ContentAddressed: no object / no ref for <part-file>` on a SUPPORTED read after a SUPPORTED write. Two distinct root causes, both in the table/part path split: **(1) non-Atomic DBs** (Ordinary/Memory/Lazy, tables at `data/<db>/<table>/` not the uuid-anchored `store/` layout) — the parser anchored ONLY on the `<uuid[:3]>/<uuid>` pair, absent there, so every part file fell into verbatim disk-file passthrough: the write went to a verbatim key under `tmp_insert_<part>/`, the tmp->final `moveDirectory` rename moved nothing, the read of the final part dir found no object. **(2) RENAME TABLE / cross-engine table move** — `disk->moveDirectory(old_table_path, new_table_path)` (a TABLE-dir rename) was ignored by CA `moveDirectory`, so refs stayed under the old table identifier while the read used the new one (e.g. Ordinary `data/db/mt` -> Atomic `<uuid>`; this is the `01114_database_atomic` `0_1_1_0` no-ref). **Fix:** `parsePartFilePath`/`isPartFilePath`/`parseTableUuid`/`parseTableFilePath` fall back to the MergeTree part-dir grammar (the universal `_<min>_<max>_<level>` block-range suffix) when the uuid anchor is absent, with the full preceding path as the table id (Atomic + the unit tests unchanged); `moveDirectory` re-keys every ref+sidecar+table-file from the source table id to the destination via `republishTableRefs`. Examples now `[ OK ]`: `00542_access_to_temporary_table_in_readonly_mode`, `01069_database_memory`, `01114_database_atomic`, `01516_create_table_primary_key`. | **DONE** (M6); the most important new finding. | `PoolPaths` (part-dir-grammar fallback), `ContentAddressedTransaction::moveDirectory`/`republishTableRefs`. Tests: unit `ParseNonAtomicPartFilePath`, `WriteNonAtomicPartViaTmpRenameThenReadBack`, `RenameTableDirRekeysRefs`. |
| B41 | **DONE (M6 — concurrency fix).** CA concurrent-blob `CORRUPTED_DATA: blob … already exists with size 0 but new content has size N` under parallel inserts. `LocalObjectStorage::writeObject` writes IN PLACE (no temp+rename), so while one writer streamed a blob to its final content-hash key, a concurrent writer racing the same key observed a partial (size-0/short) object and the put-if-absent size-guard fired on a valid insert. **Fix:** `ContentAddressedWriteBuffer::uploadBlobAtomically` — for the local backend, upload to a unique temp object key in the SAME directory then atomically `std::filesystem::rename` onto the final key (the final key only ever appears fully written); for an atomic-PUT backend (S3/Azure) keep the one-shot upload. The size-guard is retained (once writes are atomic, a different-size object at the content key is a genuine collision). Examples now `[ OK ]`: `00276_sample`, `01825_json_type_parallel_insert`. | **DONE** (M6). | `ContentAddressedWriteBuffer::uploadBlobAtomically`. Test: unit `ConcurrentIdenticalBlobWritesAreAtomic` (16 racing writers, same payload). |
| B42 | **DONE (M6 — downstream of B40).** CA part-lifecycle `Directory <part> (part to remove) doesn't exist` warning + result mismatch on `01625_constraints_index_append` (a `Memory`-engine DB with a MergeTree table). It was a downstream symptom of B40: the part's ref resolved under the wrong (verbatim) non-Atomic path, so the engine's outdated-part cleanup could not find it. With B40 fixed the example passes (`01625_constraints_index_append` `[ OK ]`). Added a regression test pinning that the CA part-removal path is itself idempotent (a double-remove, or a remove after the ref is already unlinked, is a no-op — never throws). | **DONE** (M6). | `ContentAddressedTransaction::removeRecursive` (already idempotent). Test: unit `PartRemovalIsIdempotent`. |
| B43 | **DONE (M6 — gated-feature result-diffs, tagged).** Both were result-diffs caused purely by a gated CA feature firing, not CA correctness bugs: `00427_alter_primary_key` diverges because `ALTER … MODIFY COLUMN` (a data ALTER) is gated off on CA (so downstream rows differ); `03008_deduplication_random_settings` diverges only on the `deduplicate_dst_table` cases because the random settings enable a non-replicated deduplication window, which is gated off on CA (B37 family). Both are genuinely gated features, so tagged `no-content-addressed-storage` (verified `[ SKIPPED ]`). | **DONE** (M6); gated-feature, not a CA bug. | Tags on `00427_alter_primary_key.sh`, `03008_deduplication_random_settings.sh`. |
| B44 | **DONE (M6 — Task C3, 02* range).** Reading `system.detached_parts.modification_time` for a part detached on a CA disk threw `ContentAddressed: file <detached_part> not in manifest of store/.../detached/<detached_part>` (`FILE_DOESNT_EXIST`), failing `02881_system_detached_parts_modification_time` deterministically in the praktika harness (the Atomic-DB `store/<uuid[:3]>/<uuid>/detached/<detached_part>` 4-component layout). Root cause: a detached part is stored under a ref named `detached` whose manifest keys are `<detached_part>/<file>` (B36); `StorageSystemDetachedParts` reads mtime via `IDisk::getLastModified` on the detached part DIRECTORY, which `parsePartFilePath` parses to `part_name="detached"` + a NON-empty `file` = `<detached_part>`, so `getLastModified` skipped the part-dir branch (empty-`file` only) and fell through to a manifest lookup of a key named `<detached_part>` (the keys are `<detached_part>/<file>`), throwing. **Fix:** `getLastModified` recognizes the detached-part-directory case (`part_name==kDetachedDirName`, single-component non-empty `file`) and reports the `detached` ref manifest object's mtime, mirroring the active part-dir branch. | **DONE** (M6). | `ContentAddressedMetadataStorage::getLastModified`. Test: stateless `04288_content_addressed_detached_part_modification_time`. |
| B45 | **FIXED (M6 — Task C3).** Real root cause (NOT the GC-grace hypothesis): MergeTree removes a COMPLETE part via the FAST path `DataPartStorageOnDiskBase::clearDirectory` — and that path first renames the committed part to `delete_tmp_<part>` (`DataPartStorageOnDiskBase::remove`, `disk->moveDirectory(<part>, delete_tmp_<part>)`, `DataPartStorageOnDiskBase.cpp:806`), then unlinks files one by one and finally calls `disk->removeDirectory(delete_tmp_<part>)` — it NEVER calls `removeRecursive` on the part dir. On CA: (1) `moveDirectory` with nothing staged in the transaction only ADOPTED the destination as the staged target (the INSERT `tmp_insert`→final case) and did NOT re-key the committed `<part>` ref to `delete_tmp_<part>`, so the original `store/<srv>/<uuid>/refs/<part>` ref survived; (2) the per-file `unlinkFile` calls are no-ops on a committed ref and the final `removeDirectory` was a no-op. Net: the dropped part's live ref persisted, so the re-attach part-load scan (`MergeTreeData::loadDataPartsFromDisk` → `iterateDirectory` → CA `listDirectory`, which enumerates strictly from refs) rediscovered it. The empty drop-range cover part (`all_3_3_1`, 0 rows, empty checksums) took the SLOW path (`removeSharedRecursive`→`removeRecursive`, which already unlinked its ref), which is why only the 1-row part reappeared. `03276_index_empty_part` (TRUNCATE) is the same defect on the same fast path. **Fix (3 parts, all CA-local):** (a) `ContentAddressedTransaction::moveDirectory` — when nothing is staged and the source has a committed ref, re-key the ref + per-ref sidecar objects to the destination (`renameCommittedPartRef`); (b) `ContentAddressedTransaction::removeDirectory` — for a part-directory path, unlink the ref + sidecars (`unlinkPartDirRefs`, shared with `removeRecursive`) instead of being a no-op; (c) `ContentAddressedMetadataStorage::isDirectoryEmpty` — report a part directory as empty (its files are virtual) so `DiskObjectStorage::removeDirectory` proceeds to the ref-unlink instead of throwing `CANNOT_RMDIR` and forcing a noisy `<Error>` recursive fallback. **Regression coverage:** gtests `RemoveDirectoryUnlinksPartRef` and `RenameCommittedPartThenRemoveUnlinksRef`; stateless `02521_merge_over_gap` and `03276_index_empty_part` both `[ OK ]` under the CA-default config, NOT tag-hidden. | **DONE** (M6); fixed in `ContentAddressedTransaction` + `ContentAddressedMetadataStorage`. | `ContentAddressedTransaction::moveDirectory`/`removeDirectory` + `ContentAddressedMetadataStorage::isDirectoryEmpty`. Examples `02521_merge_over_gap`, `03276_index_empty_part`. |
| B46 | **DONE (M6 — Task C3, 03* range).** Multi-partition `DETACH PARTITION` on a CA disk listed only ONE detached part instead of all of them (`03203_drop_detached_partition_all` showed 1 detached part for 3 detached partitions). Each `DETACH PARTITION` clones one part into `detached/<detached_part>/` via a fresh CA commit; `ContentAddressedTransaction::commit` published the shared "detached" ref with `WriteMode::Rewrite` from only the in-flight files, so detaching the 2nd/3rd partition OVERWROTE the ref the 1st had written. **Fix:** when the commit target is the `detached` namespace, `commit` MERGES the new keys into the existing detached ref's manifest + sidecar bundle (keeping the new bytes on a key collision), so several detached parts coexist — mirroring the moveDirectory-based `republishCommittedPartIntoDetached` path. | **DONE** (M6). | `ContentAddressedTransaction::commit`. Test: stateless `04289_content_addressed_multi_detach_drop`. |
| B47 | **DONE (M6 — Task C3, 03* range).** `DROP DETACHED PARTITION` removed nothing on a CA disk (`03203_drop_detached_partition_all` left the detached count at 3 instead of 0). `MergeTreeData::dropDetached` renames each detached part to `deleting_<part>` (`PartsTemporaryRename`) before removing it — `disk->moveDirectory(detached/<part>, detached/deleting_<part>)` — but CA `moveDirectory` had no detached->detached branch, so the rename was a silent no-op and the subsequent `removeRecursive(detached/deleting_<part>)` matched no part. **Fix:** `moveDirectory` re-keys a detached part dir within the shared detached ref (`rekeyDetachedPartDir`: re-key the `<old>/` prefix to `<new>/` in the manifest + sidecar, move the per-file sidecar objects, republish the ref), and `removeRecursive` now handles a detached part DIRECTORY by removing only that part's keys from the shared ref (unlinking the ref+bundle when the last detached part goes). Shared content blobs are kept (deferred GC). | **DONE** (M6); the ATTACH-side `attaching_<part>` rename uses the same path. | `ContentAddressedTransaction::moveDirectory`/`rekeyDetachedPartDir`/`removeRecursive`. Test: stateless `04289_content_addressed_multi_detach_drop`. |
| B49 | **DONE (CAS M6, in-process blob PIN — the real fix; fail-close demoted to a never-expected safety net). CRITICAL (GC-on): dedup/carry-forward old-blob race — now PREVENTED, not just detected.** The first fix only fail-closed: `commit` re-HEADs every referenced blob under the per-pool GC lock and throws `CORRUPTED_DATA` ("concurrently reclaimed; retry") if any is gone. A 035-049 suite run showed that fired for ~20 ordinary tests (tiny deduped files like `count.txt`/`primary.cidx`/empty `.bin` go transiently unreferenced then get reused, so the dedup-skip commit fail-closed) — a real availability bug. **The pin closes the race so the inserts SUCCEED while GC still reclaims true orphans.** Design (all under the existing `gcLock` mutex so pin/sweep are mutually exclusive): (1) `ContentAddressedMetadataStorage` owns a per-pool `std::shared_ptr<std::set<std::string>> in_flight_pinned_blobs` (`inFlightPinnedBlobs`), with `pinBlobLocked`/`unpinBlob`. (2) `ContentAddressedWriteBuffer::finalizeImpl` wraps {existence-check → upload-or-skip → pin} in ONE `lock_guard(gcLock)` (`ContentAddressedTransaction.cpp:1125-1141`): if absent it uploads then is pinned, if present it pins then skips — either way the blob is present AND pinned before the lock is released. (3) `ContentAddressedGC::runSweepOnce` (holds the same lock for its whole mark+delete) treats every pinned key as reachable (`ContentAddressedGC.cpp:248-249`). (4) The transaction tracks `pinned_blob_keys` and unpins on `commit` SUCCESS — directly, while still holding `gcLock`, AFTER the ref is published (`ContentAddressedTransaction.cpp:727-729`) — and on abort/rollback/destructor via `releasePinnedBlobs` (`ContentAddressedTransaction.cpp:39-63`); unpin happens exactly once even on throwing paths. The commit re-validate (the old fail-close) STAYS as a belt-and-suspenders assertion — with the pin it must never fire; if it ever does, that is a bug. Regression tests: `DedupCommitFailsClosedWhenBlobConcurrentlyReclaimed` UPDATED to pin semantics (the dedup `writeFile` pins B, the `runSweepOnce(grace=0)` does NOT delete B, and `T2.commit` SUCCEEDS — asserts B survives + the ref is published + reads back); `DedupCommitBeforeSweepKeepsReusedBlobAlive` kept; `OrphanBlobReclaimedAfterTransactionUnpins` added (a pinned blob survives a sweep during the txn, then after the txn unpins on abort a later sweep DOES reclaim it). The previously-fail-closing stateless tests now run with **zero** "concurrently reclaimed by GC" errors (verified `00432`/`03640`/`03928`/`03991`/`04035`/`04044`: the GC race is gone; the two that still FAIL do so only on the immutable-disk mutation/ALTER gate, not the race). **Cross-process case remains B32** (pin+lease+fence); the in-process pin is single-server only. Original report below.<br/>**CRITICAL (GC-on): dedup/carry-forward old-blob race — unmitigated.** With background GC enabled, an in-flight identical-content INSERT/merge that *dedup-skips* re-uploading an existing blob B (`finalizeImpl`) can have B deleted by the sweep before the new ref is published: P1 (sole referencer of B) is dropped→ref unlinked at T0; sweep marks B unreferenced; at ≈T0+grace a dedup INSERT records B (skip-upload); the same-cycle sweep lists live refs (new ref not yet committed)→B unreachable, grace elapsed→deletes B; INSERT then publishes a ref to the now-deleted B → dangling → CORRUPTED_DATA/FILE_DOESNT_EXIST on read. The design (§5.4) requires the writer to re-validate referenced blobs under the GC lock and fail-close; **not implemented**. Untested (`SweepClearsTimerWhenReachableAgain` re-adds the ref *before* the grace sweep, never exercising skip-then-publish). | Final review CRITICAL; only fires GC-on; the M6 test config runs GC on | **FIX (M1 in-process subset of B32):** a per-pool in-process GC lock — the sweep holds it for mark+delete; `commit` holds it to re-HEAD every referenced blob and publish the ref atomically, failing closed (throw, no dangling ref; insert is retryable) if any referenced blob is gone. Fully closes it for single-server; cross-process is the full B32 (pin+lease+fence). |
| B52 | **DONE (CAS M6). CRITICAL (GC-on): the B49 fail-close fired on ORDINARY inserts under the shared-pool GC-on default config — an availability bug.** B49 closed the data-loss path (a dedup-skipping insert reusing a blob the sweep reclaimed in the finalize→commit window must not publish a dangling ref) by re-validating referenced blobs under the GC lock in `commit` and throwing a *retryable* `CORRUPTED_DATA` if any was gone. But MergeTree's INSERT path does NOT retry that throw, so under the M6 default (one shared pool across all parallel tests, `content_addressed_gc_enabled=1`, short grace) the throw surfaced as a **hard test failure** on completely ordinary single inserts: a common small part file (`columns.txt`, `count.txt`, an empty-column `.cmrk2`, `primary.cidx`) deduped against an unreferenced-and-past-grace blob from another test's dropped table, then the sweep reclaimed it before the ref was published. Six-plus tests across `035`–`049` failed with `blob … was concurrently reclaimed by GC before the ref could be published; retry the insert` on a plain `INSERT`. **FIX:** an in-flight blob PIN set. `ContentAddressedWriteBuffer::finalizeImpl` now takes the per-pool GC lock, PINS the blob key in a shared `in_flight_pinned_blobs` set (owned by `ContentAddressedMetadataStorage`, guarded by `gc_lock`), and makes its dedup existence-check decision under that lock; the sweep (`runSweepOnce`, already under the same lock) treats every pinned key as reachable, so a reused-but-not-yet-referenced blob cannot be reclaimed. The owning transaction releases its pins under the lock right after publishing the ref in `commit` (the ref now keeps the blob reachable), and an uncommitted/aborted transaction releases them in its destructor (so a cancelled insert never pins forever). The B49 `commit` re-validate is kept as a belt-and-suspenders for carried-forward (mutation/hardlink) blobs that are reachable only through a live source ref. Two gtests added (`InFlightPinKeepsDedupReusedBlobAliveAcrossSweep`, `InFlightPinReleasedWhenUncommittedTransactionDestroyed`). After the fix a clean `035`–`039` CA-default re-run had ZERO `concurrently reclaimed` failures (cat-2 went from 9→6, all 6 remaining being non-CA: 2 cross-branch leftovers + 2 memory-budget + 2 gated). Cross-process case remains B32. | Surfaced as widespread cat-2 in the `035`–`049` CA-default run; the B49 in-process subset closed the data-loss but not the availability hole | **FIX:** `finalizeImpl` pin-under-lock + sweep honors pins + transaction unpin on commit/destruction. Fully closes single-server; cross-process is the full B32 (pin+lease+fence). |
| B57 | **DONE (2026-06-03).** Fixed: the pin write now retries the LocalObjectStorage dir-prune race and the GC read skips a vanished session. **Root cause (not abort-cleanup as first guessed):** `LocalObjectStorage::removeObject` unlinks the object AND prunes empty parent dirs (`while … rmdir`), while `persistSession` writes the pin via `writeObject` (`create_directories(parent)` then open). Under concurrent inserts on one mounter (shared flat `sessions/` prefix), a sibling's `releaseSession`→`removeObject` rmdir's the now-empty `sessions/` dir in the window between this `create_directories` and the open → `CANNOT_OPEN_FILE`/ENOENT surfaced on the INSERT (S3 has no dirs → local-backed only). **Fix:** (1) `persistSession` bounded-retries on `CANNOT_OPEN_FILE`/`FILE_DOESNT_EXIST` (each attempt re-runs `create_directories`; fail-closed after `max_attempts` so the cross-mounter pin is never silently dropped); (2) `sessionPinnedBlobs` skips a session removed between LIST and READ (its blobs are reachable via the published ref on commit / never referenced on abort), swallowing only the missing-object errors — a malformed session still fails closed via `deserialize`. **Verified:** `02434_cancel_insert_when_client_dies` un-gated, now `[ OK ]` under CA-default with zero `sessions/`/ENOENT errors; gtest `SessionPersistSurvivesSessionsDirPrune` (94 CA gtests pass). The intrinsic `create_directories`→open window in LocalObjectStorage is not deterministically unit-testable; `02434` is the end-to-end regression. `ContentAddressedTransaction::persistSession`, `ContentAddressedGC::sessionPinnedBlobs`. ——— ORIGINAL: **(dedup un-tag, ORTHOGONAL to dedup — robustness) A client killed mid-INSERT leaks `FILE_DOESNT_EXIST` on a `…/sessions/<id>` pin to stdout on a CA disk.** Surfaced un-tagging `02434_cancel_insert_when_client_dies` (which SIGKILLs the client mid-`INSERT` in a loop, table on the CA default disk with `non_replicated_deduplication_window=1000`). The final row count is CORRECT (`count() = 500000`, no data loss) and the server does NOT crash, but in-flight inserts that are killed leave a read of the M8 WriteSession pin object `content_addressed_pool/sessions/<uuid>-<random>` failing with `Code: 107 … FILE_DOESNT_EXIST`, which reaches the client stdout and trips the test's "exception in stdout" check. This is the M8 session-pin path under abort, NOT the dedup log — it would affect ANY killed CA insert. Test re-tagged `no-content-addressed-storage`. | Found un-tagging the dedup family (2026-06-03); data-correct, no crash — robustness/cleanliness only | **FIX:** make the WriteSession pin read/cleanup abort-safe — treat a missing `sessions/<id>` object as "session already gone" (no error) on the abort/rollback path, or remove the pin under the same guard that tears down the in-flight insert, so a killed insert never surfaces a raw `FILE_DOESNT_EXIST` to the client. Localized to `WriteSession`/`ContentAddressedTransaction` session handling. |
| B58 | **DONE (2026-06-04).** Fixed: merge and mutate now route the projection SUB-part through the PARENT part's whole-part transaction on a content-addressed disk, so the `<proj>.proj/<inner>` blobs land in the parent's `recorded` map and are published in the parent manifest (durable across reloads) — exactly like INSERT. **CA-CONDITIONAL** (the non-CA path is byte-for-byte unchanged): the gate is `IDataPartStorage::isContentAddressed()` (new virtual, backed by `disk->isContentAddressed()`). Changes: (1) `IMergeTreeDataPart::getProjectionPartBuilder` — `use_parent_transaction = !is_temp_projection \|\| isContentAddressed()` (a `.tmp_proj` sub-part shares the parent txn on CA). (2) `MergeTreeDataWriter::writeProjectionPartImpl` — skip the sub-part `beginTransaction()` when CA. (3) `MergeTask::PrepareStage` (~561) — projection sub-part `getProjection(..., use_parent_transaction = parent_part is CA)`, skip its `beginTransaction()` (new `global_ctx->projection_uses_parent_transaction`). (4) Skip the sub-part `commitTransaction()` on CA at the four rebuild commit sites (`MergeTask` ~1247/1272, `MergeProjectionPartsTask` ~132, `MutateTask::writeTempProjectionPart` ~1673) — rely on the parent's single commit; the `.tmp_proj`→`.proj` rename via `moveDirectory`→`rekeyStagedProjectionDir` then re-keys the staged blobs into the committed parent manifest. (5) Mutation CARRY-FORWARD of an EXISTING projection: the hardlink already rides the parent txn (the dst `getProjection(name)` defaults `use_parent_transaction=true`), so the manifest is correct, BUT `loadProjections` (run during finalize, pre-commit) probes `existsDirectory`, which reads COMMITTED metadata and cannot see the still-uncommitted staged projection dir on CA → the projection reappeared only after the next reload. Added CA-only `MutationHelpers::registerCarriedForwardProjectionForCA` (called from both `MutateAllPartColumnsTask` and `MutateSomePartColumnsTask` carry-forward loops) that registers the carried projection in-memory directly from the source part's already-loaded projection part (columns/serialization/checksums copied; part type from the source), mirroring what `loadProjections` would have done on a normal disk. **Verified:** 100 `ContentAddressed*` gtests pass; `04299` (extended to DETACH/ATTACH after the merge, after MATERIALIZE, and after a no-op DELETE mutation, asserting the projection is still `active` and selected under `force_optimize_projection=1`) PASSES on BOTH the CA-default job AND the normal job against the SAME (non-CA oracle) reference — proving the projection is durable on CA exactly as on a plain disk; 3 real gated projection tests (`02968_projection_merge`, `02516_projections_with_rollup`, `04035_commit_order_projection_mutation`) PASS when temporarily un-tagged under the CA-default job (then re-tagged for Phase 4); the normal `projection` family shows IDENTICAL failures with and without the change (verified by a baseline revert+rebuild+rerun — 12 pre-existing failures, e.g. `03460_*` projection-index and the stale `04281_content_addressed_projection_rejected`, are unrelated to B58 and unchanged), i.e. zero non-CA regressions. Files: `src/Storages/MergeTree/IDataPartStorage.h`, `DataPartStorageOnDiskBase.{h,cpp}`, `IMergeTreeDataPart.cpp`, `MergeTreeDataWriter.cpp`, `MergeTask.{h,cpp}`, `MergeProjectionPartsTask.cpp`, `MutateTask.cpp`; test `tests/queries/0_stateless/04299_content_addressed_projection_inline_disk.{sql,reference}`. ——— ORIGINAL ROOT CAUSE (confirmed): **Root cause is NOT the mutation carry-forward — it is that ANY merge/mutate LOSES projections from the rebuilt part's CA manifest. The carry-forward "failure" is a downstream symptom.** **Empirical root cause (debug build + manifest decode under the CA-default job, GC disabled, no DROP):** projection files are stored as nested `<proj>.proj/<inner>` keys in the PARENT part's single `PartManifest`. The ONLY write path that records them is **INSERT**: `MergeTreeDataWriter` writes the part AND its projection via `getProjectionPartBuilder(is_temp=false)` → `getProjection(<proj>.proj, use_parent_transaction=true)`, so the projection's blobs land in the SAME parent transaction's `recorded` map (`precommitTransaction`, never a separate commit). Verified: `commit part …/all_1_1_0: recorded=17 incl p_by_b.proj/*`. **Merge/mutate do NOT do this.** A merge's projection sub-part is written by a nested `MergeTask` (with `parent_part` set): `MergeTask::PrepareStage` line ~561 calls `parent_part->getDataPartStorage().getProjection(local_tmp_part_basename, /*use_parent_transaction=*/false)` → the projection sub-part gets its OWN `ContentAddressedTransaction`, which is never folded into the parent. The parent merge part then commits from ITS `recorded` map, which holds ONLY the base-column blobs. Verified: single-part `OPTIMIZE FINAL` `all_1_1_0`→`all_1_1_1` and 2-part `all_1_2_1` BOTH `commit … recorded=9, proj keys: []`. The same holds for the mutate temp-projection path (`writeTempProjectionPart` → `getProjectionPartBuilder(is_temp=true)` → own transaction, `commitTransaction()` at MutateTask ~1673 / MergeTask ~1247,1272). So after the FIRST merge (the `OPTIMIZE` in `04299`) the on-disk manifest already has NO projection — `system.projection_parts … active` still shows it because the projection part is held IN MEMORY (loaded via `loadProjections`), which is why same-session `uses_projection`/`MATERIALIZE` SELECTs pass and the gap was missed in Phase 2/3. The mutation then reads the source part's on-disk manifest (no `<proj>.proj`), so `MutateAllPartColumnsTask`'s carry-forward loop (which iterates `source_part->getDataPartStorage().iterate()`) never even sees `p_by_b.proj` as a directory entry — confirmed: the iterate loop logged only the 11 top-level files, `entries_to_hardlink=[p_by_b.proj]` but the dir is absent from the listing, so the projection-hardlink branch never runs. NOTE: on CA `isFullPartStorage` is TRUE (`DataPartStorageOnDiskFull::getType()==Full` regardless of the object-storage disk underneath), so the mutate path is `MutateAllPartColumnsTask` with `p_by_b` going to the HARDLINK-carry set (not recalculated); the original B58 hypothesis (recalc-vs-hardlink) and the "`!isFullPartStorage`" assumption were both wrong. Also observed: after a merge the in-memory-only projection can be reclaimed/absent at query time, making `04299`'s `force_optimize_projection` line intermittently throw `PROJECTION_NOT_USED` (one transient failure seen) — further evidence the projection is not durable on CA. The Phase-3 test `04299` still PASSES with its `KNOWN CA GAP` reference (left unchanged) and stays gated. | Root-caused 2026-06-04 via debug build + CA manifest decode; data-correct (base table serves queries) but projections are not durable through merge/mutate on CA | **FIX (sketch, deferred — touches the shared merge path for ALL storage types; not safe to land without broad regression):** make the merge/mutate projection SUB-part write into the PARENT part's transaction so its `<proj>.proj/<inner>` blobs land in the parent's `recorded` and are published in the parent manifest, exactly like INSERT. Concretely: (1) `MergeTask::PrepareStage` ~561 `getProjection(..., use_parent_transaction = true)` when `parent_part` is set — but then SKIP `data_part_storage->beginTransaction()` at ~581 for the `parent_part` case (sharing the parent's txn; an unconditional `beginTransaction` throws `Uncommitted shared transaction already exists` — observed when I tried the one-line flip), and do NOT `commitTransaction()` the projection sub-part (a shared txn can't be committed; rely on the parent's single commit, mirroring INSERT's `precommitTransaction`). (2) The mutate temp-projection path (`writeTempProjectionPart`, `MutateTask`/`MergeTask` ~1247/1272/1673) similarly must not commit a separate projection transaction; the `.tmp_proj`→`.proj` rename via `moveDirectory`→`rekeyStagedProjectionDir` ALREADY re-keys the parent txn's `recorded` in place and works ONLY if the staged `.tmp_proj` blobs are in the PARENT `recorded` (they currently are not). (3) Once merge persists projections, the mutate carry-forward iterate loop will see `<proj>.proj` and hardlink it forward correctly (the `createHardLinkFrom` on the dst projection storage already uses `use_parent_transaction=true` and records the nested keys — verified by code, just never reached today). Add a `ContentAddressed*` gtest that seeds a parent part, writes a projection sub-part through the parent transaction, commits once, and asserts the manifest carries the `<proj>.proj/*` keys; plus a stateless oracle that DETACH/ATTACHes after a merge and asserts the projection is still usable. Correct the `04299` reference once durable (oracle on a default disk: `projections_after_add` = both `p_by_b 1` and `p_sum 1`; `projections_after_drop` = 1). Files: `src/Storages/MergeTree/MergeTask.cpp`, `src/Storages/MergeTree/MergeTreeDataWriter.cpp` (`writeProjectionPartImpl`/`writeTempProjectionPart`), `src/Storages/MergeTree/IMergeTreeDataPart.cpp` (`getProjectionPartBuilder`), `src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/ContentAddressedTransaction.cpp` (`rekeyStagedProjectionDir`/`moveDirectory`). |
| B59 | **DONE (2026-06-04) — read-your-writes overlay landed + verified; un-gating then surfaced a SEPARATE bug → B63.** Fixed via Approach A (spec `2026-06-04-cas-mergetree-projection-readback-design.md`): `ContentAddressedTransaction` now resolves files it staged-but-not-committed (`tryGetInFlightStorageObjects`/`tryReadFileInFlight`/`tryGetInFlightFileSize` over `recorded`/`recorded_mutable`); `DataPartStorageOnDiskFull` consults its held transaction on read (`prepareRead`/`readFileIfExists`/`existsFile`/`getFileSize`/`getRemotePaths`) before the committed path, gated on `transaction != nullptr` (committed reads unchanged). Also fixed a DROP-wedge it exposed (`getLastModified` lacked a projection-dir branch, `0d1cad92659`). Verified: gtests `InFlightReadYourWritesBeforeCommit` + `…MutableFileBeforeCommit` (112 CA gtests); new multi-block oracle `04300_content_addressed_projection_multiblock` (1.3M distinct keys → ≥2 temp projection parts → exercises the read-back for an OPTIMIZE merge AND a MATERIALIZE mutation; passes); `01710_projection_vertical_merges` un-gated + passing; no non-CA regression. **Of the 7 originally-gated tests only `01710` un-gated cleanly; the other 6 fail for a DISTINCT root cause (B63), not the staged-temp-block read-back this fixed.** Commits `5839dbb5664`/`d52f6487aa5`/`ec4b75eeb35`/`80d4e0272eb`/`828040aa662`/`0d1cad92659`/`453e7b310d3`/`184906f17d9`. ——— ORIGINAL: **CA mutation / `MATERIALIZE PROJECTION` path cannot read back its own staged temp projection sub-parts.** A mutation (and `ALTER … MATERIALIZE PROJECTION`, `ALTER MODIFY COLUMN` that rebuilds a projection) builds the projection in *multiple* temp blocks `PartMergerWriter::writeTempProjectionPart` → `<part>/<proj>_<n>.tmp_proj`, then `MergeProjectionPartsTask` merges them into the final `<proj>.proj`. On CA the temp sub-parts are written into the parent's *in-flight* whole-part transaction and NOT committed (`MutateTask.cpp:1724` skips `commitTransaction` for CA, by design). `MergeProjectionPartsTask` then reads `<part>/<proj>_2.tmp_proj/<file>` back — but the CA read path (`ContentAddressedMetadataStorage::resolveBlobEntry`) resolves only *committed* manifests via `readRefPartId`, so the in-flight `recorded` blobs are invisible → `ContentAddressed: no ref for …/tmp_mut_…/<proj>_2.tmp_proj/<file>` (`FILE_DOESNT_EXIST`). Result: mutation fails, or (when there is a single block / no read-back) the post-mutation projection is silently wrong (missing rows / wrong agg). | Same class as the B58 merge-durability fix but for the *mutation* projection flow; the projection half of B5 only proved CREATE/INSERT/SELECT/merge + ALTER ADD/DROP. | Two viable fixes: (a) let the CA read path see the *current* transaction's staged `recorded` blobs when the part being read is the part this transaction is assembling (an in-flight read overlay), so `MergeProjectionPartsTask` reads its own temp projection sub-parts; or (b) publish each temp projection sub-part as an early standalone sub-commit ref (mirroring the early-sub-commit the merge path already describes at `ContentAddressedTransaction.cpp:851`), then `rekeyStagedProjectionDir` folds them into the parent at commit and `unlinkPartDirRefs` removes the temp refs. Plug-in points: `MutateTask::PartMergerWriter::writeTempProjectionPart`/`finalizeTempProjectionsAndIndexes`, `ContentAddressedMetadataStorage::resolveBlobEntry`, `ContentAddressedTransaction`. Re-gated tests: `02371_select_projection_normal_agg`, `01710_projection_vertical_merges`, `02920_alter_column_of_projections`, `02941_projections_external_aggregation`, `03401_normal_projection_with_part_offset`, `03401_normal_projection_with_part_offset_no_sorting`, `03464_projections_with_subcolumns`. |
| B60 | **DONE.** **CA part-directory removal logged `CANNOT_RMDIR` for a part's `<proj>.proj` projection subdir.** After a merge/optimize, removing a part directory that has a projection logged `Cannot quickly remove directory …/<part>/<proj>.proj by removing files; fallback to recursive removal. Reason: … the directory is not empty. (CANNOT_RMDIR)` (an `<Error>`). The removal still SUCCEEDED (recursive fallback + the whole-part ref-unlink reclaims everything), but the stderr noise failed clean-log asserts in `03560_parallel_replicas_projection` and `03823_optimize_dry_run_projections`. **Root cause:** `DiskObjectStorage::removeDirectory` checks `isDirectoryEmpty` BEFORE `rmdir`. The B45 fix already reports a PART directory (`parsePartFilePath` → empty `file`) as empty so `removeDirectory` proceeds straight to the authoritative ref-unlink. But a PROJECTION subdir `<uuid>/<part>/<proj>.proj` parses to a NON-empty single-component `file` ending in `.proj`, missing the `file.empty()` branch and falling through to the `iterateDirectory` listing — which lists the projection's manifest-nested inner files → non-empty → `removeDirectory` attempts `rmdir` on the non-empty subdir → `CANNOT_RMDIR`. **Fix:** `ContentAddressedMetadataStorage::isDirectoryEmpty` now also reports a projection subdir empty — a `parsePartFilePath` result whose `file` is non-empty, has no `/`, and ends with `.proj`/`.tmp_proj` (the same gate the projection branches in `existsDirectory`/`listDirectory` use) returns `true`. Removing a projection subdir on CA is a no-op (its blobs live in the parent part's manifest, reclaimed when the part's ref is unlinked), so reporting it empty lets `removeDirectory` skip the failing `rmdir` exactly as for the part dir itself (B45). Does NOT match the detached namespace or a real table dir. `src/Disks/.../ContentAddressed/ContentAddressedMetadataStorage.cpp` `isDirectoryEmpty`. **Tests:** both `03560_parallel_replicas_projection` and `03823_optimize_dry_run_projections` un-gated and now `[ OK ]` under the CA-default job. Regression gtest `ContentAddressedMetaTest.ProjectionSubdirReportsEmptyForRemoval` (asserts the `.proj` subdir + part dir report empty, table dir stays non-empty). | — | — |
| B61 | **(CAS M6 replicated triage, 2026-06-04) Replicated-CA queue-clone partition ops audit (Phase 3.2) + one reattach-readonly bug candidate. BOTH SUB-ITEMS NOW DONE.** **(a) DONE (superseded by Phase 3.2 + B62).** The 23 queue-clone partition ops are NO LONGER fail-closed — Phase 3.2 (`8fcea70ae3d`, see `B61-RESOLVED(a)` below) audited the replication-queue clone stack, found it routes through the SAME audited whole-part `freeze` CA transaction the non-replicated ALTER PARTITION stack uses (NOT the per-file-autocommit B21 mode the original note feared), and REMOVED the blanket `Code: 344` gate; B62 (`4b…`/see B62 row) fixed the one residual CA-only ATTACH-PARTITION-after-chained-RENAME bug. Verified un-gated and passing: `01034_move_partition_from_table_zookeeper`, `02271_replace_partition_many_tables`, `02445_replicated_db_alter_partition`, `01901_test_attach_partition_from`, `02731_replace_partition_from_temporary_table`, `02916_another_move_partition_inactive_replica`, `03905_chained_rename_column_mutation`. **(b) DONE (2026-06-04) — `02486_truncate_and_unexpected_parts` reattach-readonly FIXED, localized + CA-gated.** ROOT CAUSE (reproduction-debugged against the CA-default server log): after `alter … drop partition` / `truncate` on a Replicated table, `removePartsInRangeFromWorkingSetAndGetPartsToRemoveFromZooKeeper` (`src/Storages/MergeTree/MergeTreeData.cpp:5707-5743`) creates an EMPTY covering part whose SOLE purpose is to sit on disk as an Outdated part so a restart/reattach does not treat the dropped data parts as *uncovered unexpected parts*. It does `renameTempPartAndAdd(rename_in_transaction=false)` then deliberately `transaction.rollback(&lock)` to leave the empty part Outdated (not Active) — and `MergeTreeData::Transaction::rollback` (`MergeTreeData.cpp:8643`) NEVER calls `commitTransaction` on the disk storage. On a PLAIN disk the directory rename in `renameTo`→`DataPartStorageOnDiskBase::rename`→`moveDirectory` is immediately durable, so the empty cover survives on disk. On a CONTENT-ADDRESSED disk the part's ref is published only at `commitTransaction` (the deferred-transaction seam: `DataPartStorageOnDiskBase::executeWriteOperation`, `DataPartStorageOnDiskBase.h:131`, routes `moveDirectory` into the held transaction), so the never-committed empty cover left NO on-disk ref and VANISHED on detach/reattach. On reattach `MergeTreeData::loadDataParts` enumerated only the original data parts `0_0_0_0`/`1_0_0_0` (server log: "Found 2 unexpected data parts", "Expected parts: ." empty, "Uncovered unexpected parts: 0_0_0_0, 1_0_0_0") → with `max_suspicious_broken_parts=0` the part-set sanity check threw `TOO_MANY_UNEXPECTED_DATA_PARTS`, the replica stayed readonly, and the next ordinary `INSERT` threw `Code: 242 TABLE_IS_READ_ONLY` at `ReplicatedMergeTreeSink::commitPart`. (NOT a per-server ref-namespace collision: the two same-server replicas `rmt`/`rmt1` are DISTINCT tables with DISTINCT `table_uuid`s, so their `store/<server>/<table_uuid>/refs/` namespaces do not collide; ServerUUID is shared but table_uuid keys them apart. NOT a relinked-part checksum reconcile issue either.) FIX (CA-gated; plain unchanged): in `removePartsInRangeFromWorkingSetAndGetPartsToRemoveFromZooKeeper`, after `renameTempPartAndAdd` and before `transaction.rollback`, if `new_data_part->getDataPartStorage().isContentAddressed() && hasActiveTransaction()` then `commitTransaction()` — publishing the empty cover's durable ref while the in-memory rollback still leaves it Outdated, exactly mirroring plain-disk behavior. VERIFIED: `02486_truncate_and_unexpected_parts` un-gated → `[ OK ]` under `Stateless tests (arm_binary, content_addressed storage, parallel)` (the whole test passes, including the later post-Phase-3.2 REPLACE/MOVE PARTITION + DROP PART steps); 112/112 `ContentAddressed*` gtests pass; replicated DROP/TRUNCATE non-regression `00516_deduplication_after_drop_partition_zookeeper` + `00623_replicated_truncate_table_zookeeper_long` pass. (`01166_truncate_multiple_partitions` times out under the CA-default job, but it is an ATTACH-PARTITION-heavy NON-replicated `MergeTree` test — `create_empty_part=false` for non-replicated, so this fix is a strict no-op for it; its slowness is an orthogonal B21-class ATTACH-PARTITION-on-CA characteristic, not a regression of this change.) ——— ORIGINAL FINDING: **(a) Queue-clone partition ops (23 tests) stay FAIL-CLOSED on a Replicated CA table** — `REPLACE PARTITION`, `MOVE PARTITION`, `ATTACH PARTITION [FROM]`, `ATTACH PART` all throw `Code: 344 SUPPORT_IS_DISABLED` "the replication-queue clone path is not audited on CA (B33/B21)". These flow through `cloneAndLoadDataPart`→`freeze`→`Backup`→ per-file `createHardLink` autocommit (the B21 corruption mode), driven by the replication QUEUE (background), so they are NOT routed through `checkAlterPartitionIsPossible`. Representative tests: `00753_alter_attach`, `01034_move_partition_from_table_zookeeper`, `01149_zookeeper_mutation_stuck_after_replace_partition`, `01166_truncate_multiple_partitions`, `01451_replicated_detach_drop_part_long`, `01901_test_attach_partition_from`, `02012_zookeeper_changed_enum_type`, `02271_replace_partition_many_tables`, `02445_replicated_db_alter_partition`, `02456_alter-nullable-column-bag-2`, `02461_alter_update_respect_part_column_type_bug`, `02470_mutation_sync_race`, `02731_replace_partition_from_temporary_table`, `02916_another_move_partition_inactive_replica`, `03100_lwu_22_detach_attach_patches`, `03100_lwu_39_after_replace_partition`, `03231_create_with_clone_as`, `03283_optimize_on_insert_level`, `03460_basic_projection_index`, `03822_attach_with_unknown_projection`, `03905_chained_rename_column_mutation`, `03919_modify_nullable_statistics`. (The plain partition-clone ops FREEZE/FETCH/FORGET — `01417`, `01650`, `02995`, `03350`, `03612` — are the same B21/B4 class, gated at `checkAlterPartitionIsPossible`.) **(b) REAL CA-replication-bug candidate: `02486_truncate_and_unexpected_parts`.** Two same-server replicas of one table (`'1'`,`'2'`) with `max_suspicious_broken_parts=0`; after a sequence of `truncate`/`alter drop partition`/`detach … sync`/`attach`, the SECOND replica goes **readonly** and the next ordinary `INSERT` throws `Code: 242 TABLE_IS_READ_ONLY` at `ReplicatedMergeTreeSink::commitPart` (replica_path `…/replicas/1`). The committed reference EXPECTS success — so this is a genuine failure of the part-set RECOVERY path on a CA reattach (not a deliberately-unsupported op). The test ALSO uses REPLACE/MOVE PARTITION later (queue-clone), so even with (b) fixed it needs (a). | Found by the CAS M6 un-gate-and-run of the replicated stateless suite; (a) is the deliberate B33/B21 deferral, (b) is a new candidate | **(a) FIX:** route the replication-queue clone (`cloneAndLoadDataPart` and the REPLACE/MOVE/ATTACH log-entry executors) through the whole-part write contract (B30) instead of per-file `createHardLink` autocommit — i.e. make the queue clone publish a single content-addressed ref like a normal commit, then lift the `Code: 344` gate. **(b) FIX:** investigate why `attach` on a CA replica with `max_suspicious_broken_parts=0` mis-reconciles its part set against Keeper and trips readonly — guess: the suspicious/unexpected-parts check enumerates on-disk parts via a path/listing that differs on CA, or the in-Keeper checksum reconcile rejects a relinked part. Add a CA-default stateless oracle (two same-server replicas, detach sync/attach, then INSERT) once root-caused. |
| B62 | **DONE (2026-06-04).** ATTACH PARTITION of the table's OWN detached part after a chained `RENAME COLUMN` failed on CA only. `03905_chained_rename_column_mutation`: `INSERT` (col `c0`) → `DETACH PARTITION` → `RENAME COLUMN c0 TO c3 TO c4` → `ATTACH PARTITION` → `SELECT *` threw `Code: 16 NO_SUCH_COLUMN_IN_TABLE` "There is no column c0 in table" on CA, passed on plain. **ROOT CAUSE (two halves of one prefix-vs-basename inconsistency, NOT in `loadPartAndFixMetadataImpl` — which is shared/correct).** A part's `metadata_version.txt` is a MUTABLE per-part file: on CA it is NOT content-addressed, it lives in the per-ref sidecar (`refs/<part>.<file>.meta`), keyed by the part-relative file name (B23/B36). For an ACTIVE part that key is the bare `metadata_version.txt`; for a DETACHED part it must be `<detached_part>/metadata_version.txt` — the SAME `<part>/` prefix the detached MANIFEST blobs carry — because the ATTACH publish (`republishDetachedStagingIntoActive`) re-keys the staging `<part>/` prefix OFF the sidecar entries to build the active part. **(1) Write side** (`ContentAddressedTransaction::createHardLink`, the DETACH clone): stored the carried mutable bytes under `fs::path(dst->file).filename()` (the BARE basename), dropping the `<detached_part>/` prefix — so the manifest blobs were prefixed but the sidecar key was bare. The ATTACH staging-prefix filter then never matched, and the active part lost `metadata_version.txt` entirely. **(2) Read side** (`isMutablePerPartFile`, exact-string match): a detached/staging mutable path (`detached/attaching_<part>/metadata_version.txt`, parsed `file=attaching_<part>/metadata_version.txt`) did NOT match the bare names, so `existsFile`/`getStorageObjects` routed it to the manifest (where mutable files never live) instead of the sidecar → `readFileIfExists(metadata_version.txt)` returned nothing → `IMergeTreeDataPart::loadColumnsChecksumsIndexes` fell back to the table's CURRENT metadata version (`MergeTreeData.cpp` fallback, `IMergeTreeDataPart.cpp:2108-2113`). With `part_metadata_version == params.metadata_version` the on-fly RENAME conversion (`ReplicatedMergeTreeQueue::MutationsSnapshot::getOnFlyMutationCommandsForPart`) was skipped (`seen_all_metadata_mutations`), so the read kept the stale `c0`. Confirmed in the replicated CA praktika server log: the attach logged "Loaded metadata version 2" (current) instead of 0. Distinct from B61 (queue-clone) — REPLACE/MOVE PARTITION FROM are un-gated and pass. | Found re-running the un-gated Phase-3.2 set after the B61(a) lift — 1 of 22 still failed, only on CA | **FIX (CA-gated; plain unchanged):** (1) `createHardLink` now keys `recorded_mutable[dst->file]` (the FULL part-relative dest path) exactly as `recordBlob` keys `recorded[dst->file]`, so the detached part's mutable sidecar carries the `<detached_part>/` prefix and the ATTACH rekey/republish chain finds and re-keys it to the active part. (2) `isMutablePerPartFile` now matches on the LAST path component (basename), so a prefixed detached/staging mutable path is recognized and routed to the per-ref sidecar on both read and write. Together the attached active part ends up with `metadata_version.txt = 0`, the on-fly RENAME applies, and `SELECT *` returns the renamed column. The `dst->file` keying mirrors the existing `moveFile` (which already used `dst->file`) and `recordBlob` convention. **VERIFIED:** `03905_chained_rename_column_mutation` un-gated → PASSES on `Stateless tests (arm_binary, content_addressed storage, parallel)`; 110/110 `ContentAddressed*` gtests pass; `01015_attach_part` + `01213_alter_rename_compact_part` + `03905` all pass on BOTH the plain and the CA-default jobs (no non-CA regression). The test (which covers 2/3/5 chained renames + multi-column independent chains) is the regression oracle; gate removed. |
| B63 | **DONE (2026-06-04) — localized CA fix; root cause was NOT heterogeneous-part read selection (that works) but the CA carry-forward of an ALREADY-EXISTING projection during `MATERIALIZE PROJECTION`.** Root cause (`src/Storages/MergeTree/MutateTask.cpp`, `registerCarriedForwardProjectionForCA` ~line 1218): when a mutation rebuilds projections, a part that ALREADY has the projection does NOT recalc it — it CARRIES IT FORWARD (the projection's files are hardlinked into the new part). On a plain disk the new part's hardlinked `<proj>.proj` dir is visible immediately, so `loadProjections` (called in `finalize` before commit, `if_not_loaded=true`) reloads the projection part from disk with its full read-time state (rows_count, index granularity, primary index). On CA those hardlinked files are NOT yet committed when `finalize` runs (B58), so `loadProjections` can't see them; `registerCarriedForwardProjectionForCA` was added to register the projection in-memory instead — but it ONLY copied columns + checksums, leaving `rows_count == 0` and an empty `index_granularity`. A projection-served `SELECT` then read back ZERO rows from that part and silently dropped its rows from the aggregate. The earlier "exactly 1/4" symptom in `02941` was this: of two 50k-row parts the first (no prior projection) REBUILDS correctly, the second (built WITH the projection via INSERT-after-ADD-PROJECTION) carries forward → empty → the projection-served sum was just the first part's contribution (~1/4 of the total, since the second part's values are larger). Heterogeneous-part read selection, projection discovery (`existsDirectory`/`loadProjections`/`hasProjection`), and the staged-temp-block read-back (B59) were all confirmed CORRECT. Fix: `registerCarriedForwardProjectionForCA` now also copies the SOURCE projection part's already-loaded `rows_count`, `index_granularity`, primary index, minmax index, and on-disk byte sizes (the carried-forward part is byte-identical — same hardlinked content). CA-gated (function returns early on non-CA); zero non-CA behavior change. Verified empirically (minimal repro: pre-fix CA `projection_parts` showed `all_2_2_0_3 aaaa 0` + 1/4 sum; post-fix `aaaa 4` + exact sum). New regression oracle `05000_content_addressed_projection_carry_forward` (heterogeneous coverage + MATERIALIZE; compares projection-served vs non-projection aggregate in-run; passes on BOTH plain and CA — would fail on CA without the fix). All 6 re-gated tests now PASS un-gated on the CA-default job: `02371_select_projection_normal_agg`, `02920_alter_column_of_projections`, `02941_projections_external_aggregation`, `03401_normal_projection_with_part_offset`, `03401_normal_projection_with_part_offset_no_sorting`, `03464_projections_with_subcolumns`. No non-CA regression (3 of them re-run on the plain job: pass). 112 `ContentAddressed*` gtests pass. **Projections on CA are now complete (B5/B59/B63 all closed).** | Found after the B59 read-your-writes fix; the LAST known projection-on-CA correctness gap — silently-wrong, so high severity | (resolved) |
| B64 | **DONE (2026-06-04) — localized CA metadata-storage fix; root cause was NARROWER than the investigation guess (NOT a post-commit in-memory state copy like B63, but a projection-directory PATH-RECOGNITION gap during the attach-time load).** Symptom (confirmed by minimal control): a CA table with a projection `p`, `DETACH PARTITION` then `ATTACH PARTITION` (NO projection drop) → `system.projection_parts` shows `p` with `rows=0` (plain: `rows=7`), the projection is unusable (`PROJECTION_NOT_USED` under `force_optimize_projection=1`), and `CHECK TABLE` throws `Code: 713 BROKEN_PROJECTION` (in-memory `projection->getColumns()` EMPTY / 0 columns vs on-disk 2 columns, `checkDataPart.cpp:179`); base data intact. **ROOT CAUSE** (`src/Disks/.../ContentAddressed/ContentAddressedMetadataStorage.cpp`): on CA the ATTACH part is loaded from its detached STAGING directory `detached/attaching_<part>/` (`MergeTreeData::tryLoadPartsToAttach` → `loadPartAndFixMetadataImpl` → `loadColumnsChecksumsIndexes` → `loadProjections`, all BEFORE the attach commit). The projection sub-dir is therefore read as the NESTED path `detached/attaching_<part>/<proj>.proj` → `parsePartFilePath` yields `part_name=detached`, `file=attaching_<part>/<proj>.proj` (the `file` CONTAINS a '/'). But the projection branches of `existsDirectory`/`listDirectory`/`isDirectoryEmpty`/`getLastModified` were gated on a SINGLE-component `.proj`/`.tmp_proj` file (`file.find('/') == npos`), recognizing a projection dir only as a DIRECT child of a part (`<part>/<proj>.proj`). The nested staging shape was MISSED → `existsDirectory("<proj>.proj")` returned false during the attach-time load → `IMergeTreeDataPart::loadProjections` fell to its `checksums.has(path)`/broken branch, building the projection part WITHOUT loading columns/rows (empty, `rows_count==0`). The detached manifest already carries `attaching_<part>/<proj>.proj/*` correctly (rekeyed by `rekeyDetachedPartDir`), so the on-disk projection was always intact — only the in-memory discovery failed. Same projection-on-CA family as B58/B63 (load reads incomplete state at attach time), but the fix is at the projection-dir RECOGNIZER, not an in-memory state copy. **FIX (CA-internal; zero non-CA behavior change):** added a `projectionDirManifestPrefix` helper that recognizes a projection dir by its LAST path component (`.proj`/`.tmp_proj`) and returns the WHOLE `file` + '/' as the manifest key prefix (so the `attaching_<part>/` staging prefix is preserved). Replaced the four single-component projection gates in `existsDirectory`/`listDirectory`/`getLastModified`/`isDirectoryEmpty` with this recognizer, so the nested staging projection is discovered and `loadProjections` loads it with full read-time state (`rows_count`, columns, granularity) directly from the committed detached manifest — no post-commit reload needed. The direct-child shape still matches (the LAST component is the whole `file` when there is no '/'), so detached-namespace dirs and real table dirs are never matched. **VERIFIED:** minimal control on CA now gives `p rows=7`, projection serves under `force_optimize_projection=1`, `CHECK TABLE` passes; `03822_attach_with_unknown_projection` un-gated → PASSES on the CA-default job (the orphan-`pp.proj` diagnostic now surfaces because the surviving `p` no longer throws `BROKEN_PROJECTION` first); new oracle `05001_content_addressed_attach_partition_projection` (DETACH+ATTACH PARTITION with a projection → `rows>0`, projection usable, CHECK passes; identical output plain vs CA) PASSES on BOTH the CA-default and plain jobs; `01710_projection_detach_part` + `01710_projection_part_check` + `03822` + `05001` all pass on the plain job (no non-CA regression); 113 `ContentAddressed*` gtests pass incl. new `NestedStagingProjectionDirIsRecognized` (seeds a detached staging projection + asserts `existsDirectory`/`listDirectory`/`getStorageObjects` recognize the nested shape). Gate removed. | Found by the CAS M6 replicated/projection re-triage; root-caused under B64 with a controlled inline-CA-vs-plain repro (single-node CA server, `ca_policy`) | (resolved) |
| B65 | **DONE (2026-06-05).** `ContentAddressedWriteBuffer` now honours `buf_size` + `use_adaptive_write_buffer` / `adaptive_write_buffer_initial_size` (was hard-coded `DBMS_DEFAULT_BUFFER_SIZE`, so a wide part pre-allocated ~1 MiB per column and blew the INSERT memory budget), threaded through exactly as the S3/Azure write buffers do; commit `c0287d2219c`. Un-gated + passing on CA: `03770_min_columns_to_activate_adaptive_write_buffer`, `03829_insert_deduplication_info_memory`. ——— ORIGINAL: **(CAS M6 empirical re-triage, 2026-06-04) REAL CA BUG candidate: the content-addressed INSERT write path has higher peak memory than plain, tripping a tight `max_memory_usage`.** `03829_insert_deduplication_info_memory` sets `max_memory_usage = '150M'` and inserts 10000 rows of `(UInt32, FixedString(10000))` (~100 MB of data) with `max_insert_threads=1`, asserting the INSERT stays in budget (the test verifies `DeduplicationInfo` does not retain `original_block` when dedup is off, which would roughly double peak memory). On the PLAIN job it PASSES in 0.15 s, comfortably in budget. On the CA-default job it throws `Code: 241 MEMORY_LIMIT_EXCEEDED` — "would use 143.46 MiB … maximum: 143.05 MiB" — i.e. the CA write path pushes peak INSERT memory ~0.4 MiB over the effective cap. A SUPPORTED op (plain INSERT) regresses on CA via extra peak memory. Re-gated `no-content-addressed-storage` (reason recorded in the test) pending a controller-side fix. | Found in the M6 empirical re-triage (batch 5): passes on plain, fails on CA with a memory-limit excess on an ordinary INSERT | **GUESS / where to look:** the `content_addressed` write path (`ContentAddressedWriteBuffer` / the per-part transaction's `recorded` blob map and in-flight pin set) likely buffers or hashes part-file/blob content in memory during the whole-part commit, adding peak-memory overhead per INSERT proportional to part-file size. Profile a CA INSERT under a memory tracker vs plain; reduce the CA write buffer's resident footprint (stream/hash incrementally rather than holding the whole blob), or account it so a tight `max_memory_usage` INSERT does not regress relative to plain. Files: `src/Disks/.../ContentAddressed/ContentAddressedWriteBuffer.*`, `ContentAddressedTransaction.*`. |
| B67 | **DONE (both layers, 2026-06-04). Transactional merges/mutations now work on a content-addressed disk via a MULTI-PART CA transaction.** Tier-1 transactional `INSERT`/`COMMIT`/`ROLLBACK` was DONE in B39; B67 closed the remaining gap — transactional merges/mutations. **B67 had TWO layers, both now DONE.** **(Layer 1, DONE — commit `ad136563cfe`)** a transactional `COMMIT` appends the CSN to the table-level `mutation_<n>.txt` via `WriteMode::Append`, which CA rejected (`NOT_IMPLEMENTED`) INSIDE the `noexcept MergeTreeTransaction::afterCommit` → `std::terminate`/abort. Fixed by servicing `WriteMode::Append` on a CA non-part VERBATIM file via read-modify-rewrite (relaxed the CA branch of the `writeFileImpl` append guard; read existing bytes, rewrite existing+new; append on a CA PART file stays rejected). gtest `AppendOnTableLevelVerbatimFileRewrites`. This layer had been MASKING layer 2 (the append crashed first, so an early clean-binary probe mis-concluded "B67 is only the append"). **(Layer 2, DONE — commit `e055a13243e`)** with the append fixed, a transactional merge reached the single-part `ContentAddressedTransaction` limit. A transactional `OPTIMIZE`/mutation/`DELETE` runs a background merge whose ONE `DiskObjectStorageTransaction` touches MULTIPLE parts at once — the merge-output part PLUS removal-TID-lock `txn_version.txt` rewrites on the covered SOURCE parts (so a rollback can un-cover them). The old `ContentAddressedTransaction` was SINGLE-part: one `(table_uuid, part_name)` keyed to one manifest/sidecar, so recording a second part aborted in `rememberTarget` with a `LOGICAL_ERROR` and the server aborted on rollback. **FIX (landed):** generalized the CA transaction to multiple parts — per-part `recorded`/`recorded_mutable`/sidecar staging maps keyed by `(table_uuid, part_name)`, a multi-part `commit`/`commitOnePart` under a single `gc_lock` (each NEW whole part published normally, each EXISTING part updated via the B39 mutable-only branch `recorded.empty()` → sidecar-in-place, atomically over all parts), and `moveDirectory` re-keys staging across parts including the deferred `tmp_merge→final` merge. **Plus two ancillary fixes that landed with this milestone:** (a) a `LocalObjectStorage::listObjects`/`tryGetObjectMetadata` TOCTOU fix that tolerates concurrently-deleted objects (shared with non-CA local-object-storage; plain regression confirmed clean); (b) a `cancelImpl` fix for `ContentAddressedWriteBuffer` so an insert-cancel no longer leaks the inner buffers. **Un-gated + passing on CA:** `01168_mutations_isolation`(+`_2`,+`_3`), `01170_alter_partition_isolation`, `01174_select_insert_isolation`, `01167_isolation_hermitage`, `01169_alter_partition_isolation_stress`(+`_old`), `01171_mv_select_insert_isolation_long`, `02421_truncate_isolation_no_merges`(+`_with_mutations`), `02435_rollback_cancelled_queries`, `03803_transaction_mutation_race`, `03657_merge_tree_disk_support_transaction`, `03752_attach_as_replicated_transaction_metadata`, `03916_attach_as_replicated_implicit_transaction`. **Still gated → moved to BACKUP/RESTORE-on-CA blocked set (B16/B34), NOT B67:** `04036_backup_partition_transaction_visibility` — RESTORE materializes content part files via an autocommit one-shot which CA rejects (`NOT_IMPLEMENTED Autocommit writes are not supported for content part files`); needs RESTORE to drive a `ContentAddressedTransaction`. | Found + fixed finalizing the CAS txn milestone (2026-06-04): a transactional merge's single `DiskObjectStorageTransaction` spans the merge-output part plus source-part `txn_version.txt` rewrites, exceeding CA's old single-part transaction | **DONE:** multi-part `ContentAddressedTransaction` — per-part `recorded`/`recorded_mutable`/sidecar maps + a multi-part atomic `commit`/`commitOnePart` under one `gc_lock` (new parts published normally, existing parts updated via the B39 mutable-only branch); `moveDirectory` re-keys staging across parts incl. deferred `tmp_merge→final`. Files: `src/Disks/DiskObjectStorage/ObjectStorages/ContentAddressed/ContentAddressedTransaction.{h,cpp}` (`rememberTarget`/`recorded`/`commit`/`commitOnePart`/`moveDirectory`/`rollback`); `LocalObjectStorage::{listObjects,tryGetObjectMetadata}` TOCTOU; `ContentAddressedWriteBuffer::cancelImpl`. Commits `ad136563cfe` (layer 1) + `e055a13243e` (layer 2). |
| B70 | **DONE (2026-06-05) — GC S4 review remediation (the B69 resolution path).** Tier 1 (correctness) + Tier 2 (G1/G3) per spec `../specs/2026-06-05-ca-gc-s4-review-remediation-design.md`. This is the work that discharges the CODE portion of **B69** (the S4 attended-review gate) — cross-ref B69. **Tier 1 (correctness):** #1 generation-drop in `splitDeltaByShard`; #6 drop-path settled-generation carried via the `.meta` bundle sidecar; #2 fail-closed sticky session + bounded re-log on a `+`-flush failure; #5 the unlocked `in_flight_pinned_blobs` data race; #7 stale `*Locked` contracts / names. **Tier 2 (acceleration):** #3 lock-free `GcLogWriter` I/O (fold-ins: shutdown → `flushAll`; remove the double `persistSession`); #4 a `gc/sealed/<shard>` index replacing the per-round full **candidate-discovery** scan (Scan A). The new lockless-path oracles are the gate. **Two-scans correction (review author, verified):** there are TWO distinct full scans — **Scan A** = candidate discovery (`collectSealedTombstoneCandidates`), what #4 replaces; **Scan B** = the delete gate (`markReachableBlobs` / `identity_reachable_in`), a generation-BLIND over-protective reachability check that #4 does NOT touch and that SURVIVES this remediation. So #1/#2/#6 are LEAK / log-drift today **and after Tier 2** — they become data-loss only when a FUTURE follow-up (**B78**) replaces Scan B with the §6.2 sessions+compaction authoritative gate. **Ordering: Tier 1 first** is sound practice (the #4 `gc/sealed` bookkeeping needs #1's generations), NOT "#4 removes the net"; the safety-load-bearing coupling is Tier 1 + oracles → the future B78 Scan-B replacement. **What landed:** Tier 1 (#1/#6/#2/#5/#7) + Tier 2 (#3 + fold-ins, #4) all green. Oracles 1–6 pass. 156 `ContentAddressed*` gtests + CA stateless smoke (5/5) + non-CA regression all pass. **Scan B (`markReachableBlobs` delete gate) is UNTOUCHED** — its replacement is the deferred, data-loss-critical follow-up **B78** (which carries its own attended-review gate). **B69 human sign-off remains OUTSTANDING** — this row's completion is the CODE remediation only; the production-merge gate is B69 (sign-off) + B78 (Scan-B replacement). | Code review of the GC S1–S4 work split its findings into an in-scope remediation spec (this row) + deferred items (B71–B77 below); this is the correctness + load-bearing-acceleration subset | Spec `../specs/2026-06-05-ca-gc-s4-review-remediation-design.md`. Tier 1 plugs into `splitDeltaByShard`, the drop-path `.meta` sidecar, the `+`-flush session state, `in_flight_pinned_blobs` locking, and the `*Locked` contract names; Tier 2 plugs into `GcLogWriter` (lock-free I/O) + the new `gc/sealed/<shard>` index. New lockless-path oracles are the proof. |
| B91 | **DONE 2026-06-12 — TLA+ model refreshed onto the AMENDED protocol, and it caught TWO real C++ bugs in the process.** New flag-gated stages: `EnableRegistry` (namespace registry + manifest creation: `WRegister`, `GFenceRegistry` ending the round's retiring, FENCE-TIME shard universe, absent-manifest minting, registration-time floors, `INV_MAN_EXISTS`) and `EnableEvStale` (evidence with recorded view rounds, `WEvObserve` re-observation, `EvOK` gate, dep-set `TreeDepsOK`); unconditional `ViewableRound` (view rounds carry the implementation's `gc/state.round` coverage property). Full suite re-run green (9 positive configs, queues exhausted; `stage2_live` reproduces its documented bound-artifact lasso); 12 sabotage controls fire, including 3 new ones: `sab_noregistry`, `sab_foldtimeuniverse`, `sab_noevreobserve`. **C++ fixes machine-derived:** (1) `Gc::fence` fenced the FOLD-TIME registry universe — a namespace registered between the fold's registry read and the registry-fence CAS fell between both horns ⇒ dangle; fixed to the committed registry-fence universe (`724eb5363ff`, red-verified). (2) `Build::adoptTree` recorded BLIND tokenless evidence — a reclaimed detached tree could be re-attached as a dangling ref past every gate; fixed to cold-reuse via `observeAndAdmit` (`a247e29c125`, red-verified); spec `W-EVIDENCE` SCOPE amended (tokenless evidence only for witnessed-live-source children). Model commit `6802d3e1801`; results ledger in `CaIncarnationCore_RESULTS.md` §B91. | — | Residuals spun off to B104. |

## Done or resolved by the core rewrite + M-W cutover (2026-06-12) {#done-mw}

| # | Item | Status / why deferred | Notes |
|---|---|---|---|
| B12 | **DETACHED namespace** `store/<serverid>/<uuid>/detached/<name>` → `part_id` (GC root, not active). DETACH/ATTACH = ref move (no byte movement). **NOT yet implemented** — the CA transaction's `moveFile`/`moveDirectory` inherit `throwNotImplemented`, so DETACH/ATTACH on a CA table currently throws. Foreign-part ATTACH-FROM-external (ingest bytes) is further deferred. | Needs a task: override `moveDirectory`/`moveFile` to move the ref between `refs/` and `detached/` | Detached is one more GC-root ref namespace; ingestion reuses the content-addressed write path. **ARCHIVED 2026-06-12 (M-W groom):** the M-W layout makes detached a first-class namespace with per-part refs (route() re-splits the parser's B36 shape); DETACH/ATTACH are pure ref ops and the full battery passes. |
| B27 | **Non-part objects & directory ops — server-readiness gap (surfaced by the first `clickhouse local` run).** The CA read/write path only ever handled *part files* (`<uuid[:3]>/<uuid>/<part>/<file>` → ref→part→footer→blob). A real MergeTree table also needs (a) **directory operations** — `CREATE TABLE` calls `MergeTreeData::initializeDirectoriesAndFormatVersion` → `DiskObjectStorage::createDirectories` → the CA transaction's `createDirectory`, which currently `throwNotImplemented`; and (b) **table-level / non-part files** — `format_version.txt` at the table root (no `<part>` component, so it doesn't fit ref→part resolution at all), and later `mutation_*.txt`, etc. The seeded unit tests never exercised either. | First server run (`clickhouse local`, inline `content_addressed` local disk) fails at `CREATE TABLE` before INSERT; not catchable by the part-file-only unit tests | **Decision:** directories = **no-op** (object-storage convention; `existsDirectory` already derives from the refs/objects prefix). Non-part files (path not matching `<part>/<file>`) = **plain passthrough**: written verbatim to a direct key under the table path (`store/<serverid>/<uuid>/<relpath>`), read directly via `getStorageObjects`/`readFileIfExists`, and registered as **GC roots** (Phase-4 reachability must mark them — they're not in any footer/ref). Plugs into `ContentAddressedTransaction` (`createDirectory*` + a non-part branch in `writeFile`) and `ContentAddressedMetadataStorage` (`getStorageObjects`/`existsFile`/`readFileIfExists` non-part branch). No change to the part/blob layout. **ARCHIVED 2026-06-12 (M-W groom):** verbatim namespace files + the generic-disk namespace cover all non-part objects; directory ops are dispatch rows in the M-W transaction; SQL lanes green. |
| B29 | **Typed identities, not raw `std::string`.** The GC data-loss bug was literally a string confusing a *bare blob hash* with a *blob object key*; the ref-parse ambiguity (B28) is the same class. Introduce small wrappers: `BlobHash`, `BlobObjectKey`, `PartId`, `PartObjectKey`, `RefObjectKey`. Manifests store **hashes**; object keys are derived only at the object-storage boundary. | Review P2 — cheapest insurance against a whole bug class; do FIRST of the structural work | `PartManifest::BlobEntry`, `PoolPaths` (key builders take/return typed keys), `ContentAddressedGC`/`Reachability` (reachable set is `BlobObjectKey`), the read path. Makes the B-class bugs compile errors. **ARCHIVED 2026-06-12 (M-W groom):** the core has strong-typed ids (Cas::BlobId/TreeId/PackId, CasIds.h) and typed keys via Cas::Layout since M-C1. |
| B30 | **Whole-part commit contract (the root fragility).** `IMetadataStorage` is *file*-oriented (`writeFile`/`getStorageObjects`/`createHardLink`/`moveFile` per file), but a content-addressed part is an *atomic unit* (N files → one manifest → one ref). Every scar (`use_fake_transaction`, `moveDirectory` re-pin, the per-file `createHardLink` clone corruption B21) is that mismatch. | Review P1 (architectural); the user's "file-by-file feels fragile" — confirmed root cause | A first-class begin-part/write-files/commit-part contract; route every part-producing path (INSERT, merge, mutate, clone, ATTACH) through exactly one such transaction, and **fail closed** anywhere one transaction per whole part can't be supplied (interim safety before the full contract). **M7: the mutate / data-ALTER / lightweight-DELETE part-producing paths are DELIVERED.** `MutateTask` already builds the mutated part through ONE `DataPartStorage` transaction (`beginTransaction` → `createHardLinkFrom` carry-forward of unchanged columns + `writeFile` changed columns → one `commitTransaction`), exactly like merge — so enabling mutations needed only un-gating `supportsHardLinks` plus two CA fixes: `moveFile` of the table-level mutation entry (`mutation_N.txt`), and `listDirectory` enumerating table-level files so `system.mutations` is populated. Verified by 286 un-tagged mutation/lwd stateless tests + the M7 oracles. INSERT / merge / mutate / lightweight-DELETE now go through one whole-part commit; the **clone/ATTACH/partition-move/FREEZE/BACKUP** paths remain the per-file-autocommit hazard (B21) and stay gated. **ARCHIVED 2026-06-12 (M-W groom):** Cas::Build IS the whole-part commit contract - one Build per part, one publish CAS (M-C2/M-W D-W2); the fragility this item described cannot be expressed through the new API. |
| B32 | **Safe GC protocol (supersedes grace) — pin + lease + fence; Keeper OR bucket-native.** Time may protect only *failure detection*, never *live work* (so `grace`-for-success is out). In-flight writes must be VISIBLE to the GC. **Protocol:** a shared catalog holds active refs, live **write-sessions** (lease + a *pending list* of object keys), and a single **GC-leader lock**. Write (build-local gives all hashes before upload): open session → **pin** = write pending list `{hashes + part_id}` *before* uploading → upload blobs (put-if-absent; a deduped blob is already pinned) → write manifest, publish ref → drop pending list. GC (leader, under lock): roots = refs **∪ live sessions' pending lists** → mark → candidates = listed − reachable → **re-validate under the lock immediately before delete** (closes the scan/enumeration-order race the current sweep has: it reads refs *then* lists objects with no re-check) → delete; a dead-lease session's pending list is itself reclaimable. **No Keeper required:** the catalog primitives are a shared compare-and-set + lease, provided by object-store conditional writes (S3 `If-None-Match` GA; local/NFS `O_EXCL`). The paused-writer hazard (lease wrongly expires, writer resumes) is closed with a **fencing token** on commit. Keeper (B11) is just a faster catalog; this is the same protocol over a different backend. | Replaces B3's grace approach + unifies B3 (pins) + B11 (coordination); the user's "time-based is not safe enough" + enumeration-order points | The reachability sweep stays; the change is the *roots* (add live pins), the *snapshot+re-validate-under-lock* discipline, the write-side pin/fence, and the catalog backend (in-process for single-owner M1 → conditional-write objects or Keeper for shared). Re-enable background sweep only after this + ownership (B11 `_pool_meta`). **ARCHIVED 2026-06-12 (M-W groom):** the incarnation protocol (M-C3) is this item's protocol, strengthened: retire/fence/recheck + exact-token deletes + heartbeats, TLA+-modeled with sabotage controls. Pins/lease/fence all exist in their final form. |
| B35 | **RESTORE onto a content_addressed disk fails closed (`NOT_IMPLEMENTED` autocommit-part-write).** A backup restores by writing each part file through `DiskObjectStorage::writeFile` autocommit, which CA rejects (only the whole-part transaction may write part files). So `RESTORE TABLE … (onto CA)` cannot work in M1 even though `BACKUP` (pointer-holding) does. Clean error, not corruption. | Surfaced by the B34 smoke (`04284`); ties to B30/B16 | Route restore's per-part file writes through the B30 whole-part write contract (begin-part → write files → commit-part), the same seam INSERT/merge will use; until then the fail-closed error is the M1 contract. **ARCHIVED 2026-06-12 (M-W groom):** RESTORE works through the whole-part restore transaction (MergeTreeData CA branch); 05005_content_addressed_backup_restore green on both lanes. |
| B50 | **IMPORTANT: table-level/generic non-part files are never GC-reclaimed.** `unlinkFile` for a `store/<srv>/<uuid>/files/<f>` object is a no-op and the sweep scans only `blobs/`+`parts/`. Space leak (not data loss — files are rewritten in place). DROP TABLE's `removeRecursive` does clear the table's `files/` prefix, so it only leaks for mid-life `unlinkFile`. | Final review IMPORTANT; affects the literal "pool empty after a full suite" claim | Either sweep `files/` against the live ref set too, or reclaim on `unlinkFile`. Localized to the sweep scope + `unlinkFile`. **ARCHIVED 2026-06-12 (M-W groom):** M-W unlinkFile reclaims verbatim objects immediately (T5/T6), dropNamespace removes a table's files wholesale; orphans from crashes are the full-GC walk's job (B94). |
| B66 | **(CAS FETCH, 2026-06-04) FETCH PARTITION/PART now SUPPORTED on CA (byte-fetch into `detached/`); the concurrent fan-out path is the residual deferral.** `ALTER TABLE … FETCH PARTITION/PART … FROM '<zk_path>'` is a `ReplicatedMergeTree` op (now supported on CA, B1). The `checkAlterPartitionIsPossible` `Code: 344` gate was lifted — `FETCH_PARTITION` added to the CA `supported_commands` (FETCH PART parses to the same command type with `part=true`, so both are covered). Approach: force the BYTE-FETCH path for `to_detached` (the relink path `relinkPartToDisk` stages at the ACTIVE path and ignores `to_detached`), by tightening the relink-capability advertise guard in `Fetcher::fetchSelectedPart` from `if (try_zero_copy)` to `if (try_zero_copy && !to_detached)` — a detached fetch then streams bytes which `downloadPartToDisk` writes into `detached/tmp-fetch_<part>/`, and `fetchPartition` renames it to `detached/<part>`. **Detached-landing fix (committed):** the byte fetch writes each part into the SHARED `detached` ref via a whole-part CA transaction; `ContentAddressedTransaction::commit`'s read-modify-write of that shared ref (read existing detached manifest, merge the part's keys, recompute part id, publish; plus the existing-sidecar merge) was OUTSIDE the per-pool `gc_lock`, so two concurrent commits lost-update each other (only one part's blobs survived → `FILE_DOESNT_EXIST` on the rest). Moved that RMW UNDER `gc_lock` so every part accumulates. **VERIFIED:** `01650_fetch_patition_with_macro_in_zk_path_long` un-gated → `[ OK ]` on the CA-default job; new inline-CA oracle `05002_content_addressed_fetch_partition` (Replicated CA table FETCHes a single partition from its own zk path into `detached/`, asserts `system.detached_parts`, ATTACHes, reads back exact data) PASSES on BOTH the CA-default and plain jobs; 113 `ContentAddressed*` gtests pass; no non-CA regression (`01650`/`03350`/`05002` pass on the plain job). **RESIDUAL DEFERRAL (B66a):** `03350_alter_table_fetch_partition_thread_pool` (fetches a 100-part partition CONCURRENTLY via the FETCH thread pool) stays `no-content-addressed-storage`. The parallel fetches all REWRITE the one shared `detached` ref / sidecar-bundle pointer object; the READ side of that hot mutable object is not serialized against the truncating in-place rewrite of `LocalObjectStorage::writeObject` (open-with-O_TRUNC, no atomicity), so a concurrent reader (e.g. the per-part `loadColumnsChecksumsIndexes` right after its own commit, or `system.detached_parts`) can observe a TORN ref/manifest → `CANNOT_READ_ALL_DATA` (`partIdFromRefPayload` reading 0/4 header bytes) or `NO_FILE_IN_DATA_PART`. **FIX (B66a):** make the small mutable pointer-object publish ATOMIC so a concurrent reader sees old-or-new, never torn — write the ref + sidecar-bundle to a unique temp key then atomic-rename into place (needs a `moveObject`/rename primitive on the object storage; `LocalObjectStorage` would back it with `std::filesystem::rename`), OR serialize the SHARED-`detached`-ref reads under the same per-pool `gc_lock` the commit RMW now holds (a non-recursive locked/unlocked split of `readRefPartId`/`loadPartManifestOrThrow`/`readRefSidecarIfExists` so the commit path, which already holds the lock, uses the unlocked internals). Files: `src/Disks/.../ContentAddressed/ContentAddressedMetadataStorage.cpp` (reads), `ContentAddressedTransaction.cpp` (commit/republish), and/or `src/Disks/DiskObjectStorage/ObjectStorages/Local/LocalObjectStorage.cpp` (atomic write). | Found running the un-gated FETCH tests under the CA-default job (CAS FETCH probe, 2026-06-04): `01650` (single-part) passes, `03350` (100-part concurrent fan-out) tears the shared detached ref | (B66 resolved — FETCH supported + detached-landing RMW serialized; B66a deferred — atomic pointer-object publish for safe concurrent fan-out) **ARCHIVED 2026-06-12 (M-W groom):** FETCH PARTITION works (05002 green); the concurrent fan-out test 03350 was un-gated in the T13 groom and runs in the lanes. |
| B87 | **DEFERRED (real write-path bug — surfaced by the final CA-local re-run) — CA `moveFile` throws `LOGICAL_ERROR` on an attach-rollback DIRECTORY rename, aborting the server under `abort_on_logical_error`.** `00753_alter_attach`'s `ATTACH PARTITION` found the attaching part broken (it referenced a missing blob generation `blobs/e80a…/1` — the B85 read-resolution family, on the attach-LOAD path), so `MergeTreeData::PartsTemporaryRename::rollBackAll` (`MergeTreeData.cpp:5194`) undid the rename via `disk->moveFile(new_dir → old_dir)`. CA `ContentAddressedTransaction::moveFile` (`ContentAddressedTransaction.cpp:1164`) only understands part-*file* renames (it parses `parsePartFilePath` + works on file entries); the detached-part-*directory* rename is not a staged file, so it hits the final guard `throw Exception(LOGICAL_ERROR, "ContentAddressed: moveFile source not recorded: {}")` → `abort_on_logical_error` → SIGABRT → the whole server died → every concurrent test failed + praktika's hung-check fired (the "hang" is the post-abort symptom, NOT a deadlock; the read-path FIX 1 is exonerated — zero frames in the stacktrace). **Fix:** CA `moveFile`, for a part-DIRECTORY source it didn't stage (the attach/detached rollback case), must route to the directory re-key logic (`moveDirectory` already re-keys detached/attaching part dirs) or treat it as a recoverable move — it must NOT raise `LOGICAL_ERROR` on a legitimate rollback. **Root trigger** = the same read-path generation-resolution gap as **B85** (the attach-load saw a swept generation as a missing/broken blob); completing B85's 404→repair fallback would prevent the broken-part-on-attach in the first place, so B85 is the higher-leverage fix and B87 is the latent escalation it exposes. In a RELEASE build the `LOGICAL_ERROR` is a recoverable exception (rollback fails) rather than a crash; with `abort_on_logical_error` (CI) it aborts. | Surfaced by the final CA-local full re-run (2026-06-05): server SIGABRT on `00753_alter_attach` rollback; investigation traced it to `moveFile` LOGICAL_ERROR + a B85-family attach-load missing-generation trigger | `ContentAddressedTransaction::moveFile` (the `LOGICAL_ERROR "source not recorded"` guard) — handle/no-op the part-directory rollback rename (delegate to the `moveDirectory` re-key); + B85 to prevent the broken-part-on-attach trigger. **[DONE 2026-06-06, commit `6e89957d7d4`: `moveFile` `is_part_dir` predicate delegates BOTH active + detached part-dir renames to `moveDirectory`; the concurrent-attach race `01164` also exposed+fixed two more fatals — see B88. B85 DONE `a6588c9d7ac`.]** **ARCHIVED 2026-06-12 (M-W groom):** M-W moveFile/replaceFile delegate part-DIRECTORY shapes to moveDirectory (the B87 guard, with a comment at the site); attach-rollback covered by the DetachAttach wiring tests. |
| B90 | **DEFERRED (CA-S3 SIGSEGV — root cause UNCONFIRMED; the mitigation MASKS a suspected CA-side UAF, it does NOT fix it — do NOT call CA-S3 "fixed").** A full CA-over-S3 run SIGSEGV'd in `WriteBufferFromS3::makeSinglepartUpload` → `ProfileEvents::Counters::increment` writing to garbage `0x1413` on a pooled async-upload worker, during a CA `sessions/<id>` PUT. **CORRECTION (a second, skeptical re-investigation REFUTED the first diagnosis):** the original "generic `ThreadGroupSwitcher` no-ops on a null group → dangling `current_performance_counters`" story is **FALSE** — `current_performance_counters` is initialized to the thread's own `&performance_counters` and is mutated ONLY by `ProfileEventsScope` (which the IO/S3 path never uses); attach/detach-group don't touch it; a null group is SAFE (else every group-less `BackgroundSchedulePool` S3 write in stock ClickHouse would crash — they don't). AND the S3 write path is synchronous-join (`finalizeImpl`→`waitAll`, dtor→`safeWaitAll`), so the submitter's group is pinned alive — the genuine upstream prefetch-UAF class (`ee02e2b24b7`) doesn't cleanly map either. **Most probable ACTUAL cause: a memory-lifetime UAF / heap corruption in the CA write path itself** (a buffer/object the upload or the worker `ThreadStatus` touches, freed/raced — the async pool merely widens the window), NOT a generic ProfileEvents bug. `0x1413` (a tiny value) ≈ a `current_performance_counters`/`current_thread` overwritten by a wild write or freed-and-reused `ThreadStatus`. **Mitigation in place** (commit `0ac4423acf4`: all CA-internal writes → `s3_allow_parallel_part_upload=false` → inline `syncRunner`) removes the pooled worker and SUPPRESSES the crash — but it likely MASKS the UAF (a UAF can corrupt data, not just crash), so CA-S3 is NOT proven sound. **REQUIRED next step: reproduce under ASan** (async path active, i.e. WITHOUT the mitigation — build at pre-mitigation `5014bff7900` or temp-revert the flag) to capture the alloc/free stacks for the `0x1413` object → identify the real freed object (CA buffer? `WriteBufferFromS3` lifetime? CA-clobbered `ThreadStatus`?), then fix THAT. Secondary (correctness, NOT the crash): the CA GC thread runs group-less (BackgroundSchedulePool attaches no group) where merges attach `ThreadGroup::createForMergeMutate` — attach one for throttling/accounting parity. After the real fix: re-enable parallel CA blob upload (the mitigation made large-blob uploads synchronous — a throughput regression). | 2026-06-06 full CA-S3 run SIGSEGV; FIRST root-cause (generic ProfileEvents UAF) REFUTED by a second investigation — real cause is a suspected CA-side memory-lifetime UAF, unconfirmed pending ASan | Reproduce under ASan to attribute the freed `0x1413` object; fix the CA write-path UAF; then attach a ThreadGroup to the CA GC thread (parity) + re-enable parallel CA blob upload. Mitigation `caControlWriteSettings` (parallel-upload-off) is a band-aid, not the fix. **UPDATE 2026-06-06 (2nd hypothesis ALSO refuted):** the suspected CA-side dangling-`[this]` UAF was fixed (commit `2a3a8cedb753`: `DiskObjectStorageTransaction::writeFile` blob branch pins the transaction until the buffer finalizes — a real latent-UAF hardening, KEPT) and **validated by a full async-active CA-S3 run (mitigation reverted): the SIGSEGV RECURRED** (identical `0x1413` / `makeSinglepartUpload`→`ProfileEvents`), so that was NOT the cause either. Notably that full run had **ZERO CA-logic failures** among its non-crash failures (all 55 were non-CA: async-insert/S3-latency wall-clock timeouts, qemu/IPv6/MySQL env, the gitignored `test_optimize_using_constraints`) — so CA itself is sound; the ONLY CA-S3 blocker is this generic crash. **Most precise mechanism now:** `WriteBufferFromS3::makeSinglepartUpload` schedules the upload via `threadPoolCallbackRunnerUnsafe`, which captures the submitter's `ThreadGroup` by RAW pointer; `ThreadGroup::performance_counters.parent` points into that group; if the originating query's `ThreadGroup` is destroyed before the detached upload task runs, `Counters::increment` walks a freed `->parent` → write to garbage `0x1413`. **Generic upstream ClickHouse S3-async-upload UAF**, exposed (not caused) by CA's S3-write-heavy workload; the synchronous mitigation avoids it because the upload runs inline in the still-alive caller (no detached worker, no dangling parent). **The mitigation is, pragmatically, the WORKING fix for CA-S3** (a 1h+ mitigated run was crash-free; tiny CA control objects cost nothing synchronous). **The generic fix** — make `WriteBufferFromS3` keep the captured `ThreadGroup` alive for the upload's duration (shared-ptr / the non-`Unsafe` runner), or sever the dangling parent — touches HOT generic IO shared by every S3 user (high blast radius), and needs ASan to confirm the freed object before any change. THREE code-reasoning hypotheses have now missed; do NOT attempt a 4th blind fix — **the next step is an ASan repro to definitively attribute the freed object**, then a careful (likely upstream) fix. **UPDATE 2026-06-06 — ASan REPRO DONE, ROOT CAUSE CONFIRMED (3rd-hypothesis mechanism, definitive).** The branch was merged onto current `upstream/master` (merge `e0bc6794215`; 3 conflicts resolved; submodules synced; rust-src installed for the merge-bumped `nightly-2026-03-22` toolchain) and built with `-DSANITIZE=address`; the CA-S3 stateless suite was run with the mitigation reverted (async parallel-upload LIVE). **ASan fired repeatedly (5 sanitizer triggers); the merge did NOT fix it.** Definitive ASan report (saved: `tmp/asan_report_b90.txt`, full err.log `tmp/asan_errlog_b90_full.txt`): `heap-buffer-overflow READ of size 8` of a `MemoryTracker*` in `MemoryTracker::setParent` (`MemoryTracker.cpp:786`, the parent-chain `next->parent.load()` walk) ← `ThreadStatus::attachToGroupImpl` (`ThreadStatusExt.cpp:358` — `memory_tracker.setParent(&thread_group->memory_tracker)`) ← `ThreadGroupSwitcher` (`ThreadGroupSwitcher.cpp:51`) ← the **`threadPoolCallbackRunnerUnsafe` detached task** (`threadPoolCallbackRunner.h:37`) on a `ThreadPool` worker (T1451). The attached `ThreadGroup`'s MemoryTracker **parent chain references a freed query-lifetime tracker** — the nearest freed chunk is a `QueryStatusInfo` allocated by `ProcessList::getQueryInfo` and released in `QueryMetricLog::collectMetric` on the `BackgroundSchedulePool` (T32), i.e. the per-query `ProcessListElement`/`MemoryTracker` torn down at query end. ClickHouse's own signal-handler stacks in err.log corroborate the detached task is the S3 upload: **21× `WriteBufferFromS3::finalizeImpl() WriteBufferFromS3.cpp:234`**. This is the SAME bug as the original `0x1413` crash, hit via the adjacent `performance_counters.setParent(&thread_group->performance_counters)` at `ThreadStatusExt.cpp:357` instead of `memory_tracker.setParent` at :358 — one dangling query-lifetime `ThreadGroup` parent, two fields. **CONFIRMED: generic upstream ClickHouse lifetime bug.** The `threadPoolCallbackRunnerUnsafe` is *Unsafe by contract* — its own header warns "you MUST ensure that all async tasks are finished before any objects they may use are destroyed." An S3 upload task captures the submitter's `ThreadGroup` and, on some path, outlives the query whose `MemoryTracker` is its parent (the `finalizeImpl`→`waitAll` join is evidently bypassed/raced on this path). CA merely exposes it via S3-write-heavy load. **Fix direction (high blast radius — touches hot generic IO; needs care / likely upstream):** either (a) make the captured group's parent chain stay alive for the task's duration, (b) the genuine fix to the responsible caller so the async upload completes before the query/`ProcessListElement` teardown, or (c) keep the CA mitigation as the pragmatic working fix. Mitigation `caControlWriteSettings` (parallel-upload-off) RESTORED in the committed tree. **UPDATE 2026-06-06 — no-CA repro ATTEMPTED + mechanism fully pinned (still only CA reproduces; here is why).** Built a standalone plain-`s3`-disk harness (dedicated minio + ASan server on the merged tree, zero CA: `tmp/b90_nocastand/`) and confirmed multipart uploads exercise the exact path (`S3CreateMultipartUpload`/`S3UploadPart`). A 15-min high-concurrency hammer (12 foreground multipart inserts + 6 async inserts `wait_for_async_insert=0` + 4 mid-flight cancellations + merges + query-churn; **2201 multipart uploads, 21222 part uploads, 5675 queries, 221M rows**) did **NOT** trip ASan — server survived clean. Root-caused WHY: every **synchronous** S3 write path is structurally safe because `WriteBufferFromS3::finalizeImpl`→`task_tracker->waitAll()` (and the dtor `safeWaitAll`) holds the originating `ThreadGroup` alive until ALL detached part-upload tasks finish — so the upload provably cannot outlive its group. Merges are doubly safe: `ThreadGroup::createForMergeMutate` parents `memory_tracker` to the process-global `background_memory_tracker` (never freed). The captured-group ctors DO use raw parent pointers — `ThreadGroup::ThreadGroup(ContextPtr, ThreadGroupPtr parent)` (the `createForFlushAsyncInsertQueue` ctor, `ThreadStatusExt.cpp:132`) and `ThreadGroup::ThreadGroup(ThreadGroupPtr parent)` (the `createForMaterializedView` ctor, :118) initialize `performance_counters(VariableContext::Process, &parent->performance_counters)` and `memory_tracker(&parent->memory_tracker, …)` and **do NOT retain a `shared_ptr` to `parent`** — exactly the dangling shape in the ASan stack — but the only thing that keeps the parent group alive is the enclosing job scope, which also runs the `waitAll`. **So the bug needs a DEFERRED S3-buffer finalize that escapes the group-holding scope** — which CA's transactional/background commit does (a CA write buffer is finalized outside the query/group scope that anchors its parent tracker) and which no stock synchronous path does. Net: the crashing CODE is generic (`threadPoolCallbackRunnerUnsafe`/`ThreadGroupSwitcher`/the raw-parent group ctors), proven by the ASan stack; the LIFECYCLE that exposes it is the deferred-finalize pattern, currently only reached via CA (or a constructed deferred-finalize-to-S3 generic case — e.g. S3Queue / a buffer held past its query — not yet built). **Clean fix candidate (generic, modest blast radius):** make a borrowed/captured `ThreadGroup` retain a `shared_ptr<ThreadGroup>` to its parent (or have the S3 upload runner keep the captured group's full ancestry alive) so the parent `memory_tracker`/`performance_counters` cannot be freed while any thread is attached to the child group. Harness left running for follow-up (minio `b90_minio` :9000, ASan server pid in `tmp/b90_nocastand/server.pid`). **UPDATE 2026-06-06 — ROOT CAUSE PINPOINTED (exact freed object + free site) + FIX implemented.** Re-ran the praktika CA-S3 suite on a rebuilt ASan binary (mitigation off + 2GB ASan quarantine baked into `__asan_default_options` so the real freed object survives recycling) and streamed the server stderr out of the running container with `tail -F` (capturing in the 60s sanitizer-wait window, beating the restart-overwrite that lost the first report). Captured a clean `heap-use-after-free` (`tmp/asan_pinpoint_b90_full.txt`, 402 lines). **Freed object = a query `ThreadGroup`** (`ThreadGroup::createForQuery` ← `QueryScope::create` ← `TCPHandler::runImpl:572`). **Freed by:** `MergeTreeDeduplicationLog::shutdown()` (`MergeTreeDeduplicationLog.cpp:402` `current_writer.reset()`) ← `IStorage::flushAndShutdown` ← `InterpreterDropQuery::executeToTableImpl` (DROP TABLE), via `WriteBufferWithFinalizeCallback::~`→`WriteBufferFromS3::~`→`TaskTracker::~`→ the `threadPoolCallbackRunnerUnsafe` scheduler lambda dtor (`threadPoolCallbackRunner.h:33`) dropping the captured group's last ref. **Access (UAF):** a detached S3 upload task (`ThreadPool` worker) `ThreadGroupSwitcher`→`attachToGroupImpl:358`→`MemoryTracker::setParent:786` walks the parent chain of a **borrowed child group** whose `memory_tracker.parent` is a RAW pointer into that now-freed query group. **MECHANISM:** `MergeTreeDeduplicationLog` keeps a LONG-LIVED `WriteBufferFromS3` (`current_writer`) on the S3 disk whose parallel-upload scheduler captured — and thus PINNED — the `ThreadGroup` of the query that created the writer; `DROP TABLE` destroys that buffer and frees the pinned group while a concurrent borrowed-child upload still references it. This is GENERIC upstream code (`MergeTreeDeduplicationLog` + any S3 disk + `non_replicated_deduplication_window`), exposed by CA (every table on the CA disk + dedup-window enabled per B37 + heavy DROP churn). My earlier plain-`s3` hammer missed it only because it lacked `non_replicated_deduplication_window` — a plain-s3 + dedup-window + DROP-churn repro should also trip it (→ candidate upstream issue with a real-life repro). **FIX (root cause; works with parallel upload ON):** the borrowed-child `ThreadGroup` ctors (`ThreadStatusExt.cpp:118`/`:132`, `createForMaterializedView`/`createForFlushAsyncInsertQueue`) now retain a `shared_ptr<ThreadGroup> parent_thread_group` (new member in `ThreadStatus.h`) so the parent's `memory_tracker`/`performance_counters` cannot be freed while any thread is still attached to the child group. Rebuilding + re-running the CA-S3 ASan repro to confirm crash-gone with parallel upload live; if clean, the `caControlWriteSettings` mitigation can be dropped and parallel CA blob upload restored. | 2026-06-06: 1st (generic ThreadGroup-noop) and 2nd (CA dangling-`[this]`) hypotheses both refuted by reproduction; **3rd hypothesis (detached `threadPoolCallbackRunnerUnsafe` task walking a freed query-lifetime `ThreadGroup` parent MemoryTracker/Counters) CONFIRMED by ASan on the merged tree** — generic upstream S3-async-upload UAF exposed by CA; plain-S3 sync paths are waitAll-protected so only the deferred-finalize (CA) pattern trips it | Decide the generic fix (keep captured group alive / fix the caller's join) vs retaining the CA mitigation; re-enable parallel CA blob upload after the real fix. Reports: `tmp/asan_report_b90.txt`, `tmp/asan_errlog_b90_full.txt`. Lifetime fix `2a3a8cedb753` KEPT as defensive hardening (not the crash fix). **ARCHIVED 2026-06-12 (M-W groom):** the root-cause fix (borrowed-child ThreadGroup retains parent_thread_group, ThreadStatusExt.cpp) is in-tree; the PoC-side mitigation (caControlWriteSettings) was deleted WITH the PoC; the new Native write path ran the CA-S3/RustFS lanes with no SIGSEGV recurrence. The generic upstream issue (threadPoolCallbackRunnerUnsafe lifetime contract) remains upstream's. |
| B100 | **M-W: production GC pacing + wiring scheduler.** The lease steal window is observation-based (no clocks in `Cas::Gc`); production semantics ("incumbent failed to renew for one scheduler period") come from the wiring's timer thread. Also: `HeartbeatKeeper` background-thread restart-after-failure intentionally throws "already running" until `stopBackground` (gate observes the frozen `lastRenewTime`) — the wiring's build lifecycle must handle it. | Pacing/thread ownership is a wiring concern (design §4: startup/shutdown drive the scheduler) | `ContentAddressedMetadataStorage::startup` owns the thread; `Gc` needs zero changes. **ARCHIVED 2026-06-12 (M-W groom):** CasGcScheduler (M-W T10): one Gc per thread lifetime (stable lease observer), interval knob, swallow-and-retry, back-off observability; HeartbeatKeeper lifecycle owned by Build via startBuild/abandon. |

## Obsolete — superseded by the incarnation design / PoC deletion {#obsolete}

The incarnation-token core (spec 2026-06-10) replaced the PoC's grace-sweep GC, generations,
tombstones, write-sessions, pool-ownership claim, and refcount machinery wholesale; M-W deleted
the code. Items below targeted that machinery or are otherwise superseded.

| # | Item | Status / why deferred | Notes |
|---|---|---|---|
| B3 | **Reachability GC hardening**: grace-from-loss-of-reachability, ephemeral reader pins (stateless compute), self-healing GC coordinator, MARK-epoch revalidation | Distributed concerns; M1 is single-process | M1's removal hook is the same `grabOldParts`→`IDataPartStorage::remove` path; the hardened GC is a drop-in replacement of the removal decision, not a new lifecycle. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the grace-based sweep this hardened was deleted with the PoC; the incarnation GC (M-C3) is the hardening. |
| B5 | **Projections as nested manifests; patch parts (lightweight delete) as a separate ref class** | Extra manifest structure + a second active-set namespace | The **versioned** footer reserves a `projections` section (nested `part_id`s); patch parts are a separate reachability root in the GC. **GATE NOW PARTIALLY ENFORCED (M5.2 / B31):** projections are rejected at `CREATE`/`ATTACH` (`MergeTreeData::checkContentAddressedDiskRestrictions`) AND via `ADD PROJECTION` (the disk no longer advertises `supportsHardLinks`, so `checkAlterIsPossible` rejects it). Patch parts / lightweight delete are gated *at the operation* — `DiskObjectStorage::supportsHardLinks` now returns false for `content_addressed`, so `checkMutationIsPossible` (and lightweight delete, which calls it) fail closed with an "immutable disk" error. The *nested-manifest* support that would actually allow projections/patch parts is still Phase 5+ unbuilt. **M7: PATCH PARTS NOW SUPPORTED (lightweight-delete half of B5 closed).** A patch part is just a normal content-addressed part — NO separate ref class / second namespace was needed: it is published as a ref under the existing `store/<server>/<uuid>/refs/` namespace and the reachability sweep enumerates all refs, so it is protected and reclaimed like any part. Verified by gtest `PatchPartIsReachableThenReclaimedLikeAnyPart`, stateless `04294` (forced `lightweight_update_force` produces a real `patch…` part on CA, results match plain), and no-leftovers `04295`. **M-proj: PROJECTIONS NOW SUPPORTED for CREATE/INSERT/SELECT/merge + ALTER ADD/DROP/MATERIALIZE (projection half of B5 closed) — Approach A.** A projection's files live nested in the PARENT part's manifest under the key prefix `<proj>.proj/<inner>` (no separate part/ref; NOT the originally-reserved footer `projections` section). `existsDirectory`/`listDirectory`/`iterate` in `ContentAddressedMetadataStorage` discover and enumerate a projection by the `.proj`/`.tmp_proj` suffix (mirroring the detached-part-dir branches); the build path stages projection files in the parent's shared transaction and `rekeyStagedProjectionDir` re-keys `<proj>.tmp_proj/<inner>` → `<proj>.proj/<inner>` at the parent's single commit. Merge/mutate durability fixed in B58 (`24f89d7ce78`). Gate lifted: projections no longer rejected at `CREATE`/`ATTACH`/`ADD PROJECTION`. **Phase 4: 129 projection stateless tests un-gated and passing under the CA-default job** (`Stateless tests (arm_binary, content_addressed storage, parallel)`). 22 re-gated for orthogonal reasons (replicated/B1, ATTACH·MOVE PARTITION/B21, BACKUP·RESTORE/B16·B34·B35, distributed/remote). **Two residual projection-mechanism gaps remain (B59, B60)** — see those rows; the affected tests are re-gated with an inline reason comment. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** projections live as nested tree entries (Approach A) and patch parts pass on CA; the design question is settled by the tree codec. |
| B7 | **Hardening**: `(cityHash128, size)` collision guard; conditional-write capability probe for non-AWS S3; long-key/path handling | Production-robustness, not needed to prove the model | Local to the object-store write path and key generator. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the capability probe shipped in M-C1 (Cas::Probe, fail-closed); the (hash,size) collision-guard question is recorded as B114; long-key handling is bounded by the fixed key shapes of Cas::Layout. |
| B9 | **Persisted refcount + prefix-sharded reconciliation** (RocksDB-backed `BlobRefIndex`; sharded full-reachability rebuild for crash/drift/DR) | M1 ships an in-memory `BlobRefIndex` impl; persistence is a scale concern | M1 puts the refcount **behind the `BlobRefIndex` interface** and GC consumes the **deref-delta** (never a full scan) from day one → swapping in-memory→RocksDB and adding sharded reconciliation is an impl change, no rework. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the incarnation design has no refcount - reachability is the in-degree snap folded from manifest journals; reconciliation is the full-GC walk (B94). |
| B11 | **`coordination = keeper` — multi-server POOL coordination** (a *disk-level* property, orthogonal to table replication): single-leader Keeper-elected GC + GC lock + mark-over-union-of-refs + re-validate-at-delete; refs published via the Keeper `RefCatalog` whose watches are the leader's **delta feed** (so even multi-mounted *non-replicated* pools stay delta-driven, no `/log` needed). **Replication (B1) is one consumer of this**, not the only one. | Distributed concern; M1 ships `coordination = none` + the self-check | Layout UNCHANGED (pool shared by mounting the same root). M1 reserves the seams: **`RefCatalog`** (S3→Keeper), **GC coordination/lock** (in-process→Keeper leader+lock). **`_pool_meta` self-check ships in M1** (validates config + detects concurrent mounters → fail-closed), so multi-mount is *safe* before `keeper` coordination is *supported*. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** superseded by B101 (Keeper as optional accelerator over the S3-only baseline). |
| B18 | **GC fail-close on a missing manifest** — `markReachableBlobs`'s `FooterResolver` must throw when a live ref's footer is missing (NOT return an empty `Footer`, which would silently widen the sweep set → data loss). | Surfaced by the Phase-1 final review; the Phase-1 pure library left it to the caller | Document the throw-on-missing contract on `FooterResolver` (or change it to `std::optional<Footer>` and have `markReachableBlobs` throw on `nullopt`); the GC reconcile path (Phase 4) enforces it. Matches the PoC (`cas.cpp:741`) and CLAUDE.md fail-close. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the old GC was deleted; the new recheck fails closed by construction (the four INV-NO-LOSS gates). |
| B20 | **Phase-2 read-side polish** (from the Phase-2 final review): add tests for empty-table-dir, nested-file-path resolution, and `getLastModified` success; reconcile `getLastModified` on a *directory* (currently throws `FILE_DOESNT_EXIST` vs plain's placeholder `Poco::Timestamp{}`); cheapen `existsDirectory` table-dir to `existsOrHasAnyChild`/`max_keys=1`; unify the transaction "unsupported options" error code (`commit` uses `throwNotImplemented`, `tryCommit` uses `LOGICAL_ERROR`). | Minor asymmetries / coverage; not correctness-blocking for read-only Phase 2 | Local to `ContentAddressedMetadataStorage`/`ContentAddressedTransaction` + the gtest; resolve when the Phase-3 write/commit path lands. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the read side was rewritten in M-W T2; the surviving perf intent (cheap existsDirectory / fewer GETs) is B113. |
| B22 | **Phase-3 write-path follow-ups** (from the Phase-3 final review): (a) the CA write buffer's spill `temp_dir` is derived from `object_storage->getCommonKeyPrefix()`, correct for `LocalObjectStorage` but **wrong for S3** (that's a key prefix, not a local path) — must point at a real local scratch dir before the S3 write path is enabled; (b) the gated CA write branch **skips multi-location blob replication** (`recordBlobsReplication`/`ForkWriteBuffer`) — fine for single-writer M1, but document it and reconcile with B1/B11; (c) the **ref payload is a bare `part_id`** with no version byte — add a version prefix before it's persisted in production (forward-compat, B1 header); (d) coverage: empty-file write, a part composed entirely of carried-forward files, commit-with-no-writes. | Surfaced by the Phase-3 review; M1-local works, these gate S3 / multi-writer / production | (a) write-buffer ctor temp dir; (b) the gated branch in `DiskObjectStorageTransaction::writeFileImpl`; (c) `ContentAddressedTransaction::commit` ref write + the read-side parse; (d) gtests. None change the on-object layout. By-design (not bugs): `undo` leaves CA blobs (immutable/shared → Phase-4 GC sweeps orphans), and `BlobEntry.key==checksum` today (a comment should note the intended distinction). **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** (a) was fixed long ago (cas_scratch_path); the remaining items referenced PoC code deleted in M-W T2. |
| B24 | **`autocommit` CA writes + `generateObjectKeyForPath` placeholder.** The gated CA write branch rejects `autocommit` writes with `NOT_IMPLEMENTED` (no MergeTree caller exercises it on a CA disk yet — confirm and support if one does). `ContentAddressedTransaction::generateObjectKeyForPath` is a Phase-2 placeholder (`createAsAbsolute(path)`), unused by the CA write path but a landmine if some code path calls it. | Phase-3 followups; not hit by current callers | (autocommit) the gated branch in `DiskObjectStorageTransaction::writeFileImpl`; (key) make `generateObjectKeyForPath` throw a clear `LOGICAL_ERROR` ("content-addressed keys are assigned at finalize") rather than return a wrong key. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** autocommit was redesigned in M-W (verbatim files durable on finalize; mutable one-shots via updateRefPayload); the generateObjectKeyForPath placeholder is now a deliberate fail-loud stub tracked in B108(e). |
| B51 | **IMPORTANT: `_pool_meta` claim fails open on a truly-concurrent first-mount** (absent→claim has no compare-and-set). Fine for single-owner M1; if two servers start against a fresh shared pool simultaneously, both claim + both may GC. | Final review IMPORTANT; out of single-server scope | A conditional-create (`If-None-Match`/`O_EXCL`) claim — part of B32/B11; for M1, assert one storage per root per process. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** PoolCoordination and the ownership claim were deleted; the incarnation pool is multi-writer by design (lease = work dedup only). |
| B53 | **Pre-existing, NOT CA-specific (surfaced by CAS M7). A table created with an inline `disk = disk(...)` MergeTree setting cannot run any `ALTER` that goes through `checkAlterIsPossible` (e.g. `MODIFY COLUMN`, `MODIFY SETTING`): it throws `Code: 170 BAD_GET: has CustomType, requested String`.** The inline disk definition is stored as a `CustomType` (AST) `Field` in the table's `settings_changes` AST. The canonical materialisation (`CustomType` AST → disk-name string) happens in `MergeTreeSettingsImpl::loadFromQuery` but is NOT written back into the stored AST, so multiple engine-agnostic `ALTER` sub-checks that re-process the raw `settings_changes` read the `disk` value as a `String` and throw: confirmed at `MergeTreeData::checkColumnFilenamesForCollision` (`getDefaultSettings()->applyChanges(changes)`) and `MergeTreeData::checkAlterIsPossible` → `Context::checkMergeTreeSettingsConstraints` → `SettingsConstraints::getNewValueToCheck` → `MergeTreeSettings::castValueUtil` (likely more sites downstream in the ALTER execution). Heavy mutations (`ALTER UPDATE/DELETE`) are unaffected (they go through `checkMutationIsPossible`, not `checkAlterIsPossible`) and work on CA. `MODIFY COLUMN` on a content-addressed disk works through the **storage-policy** path (no `CustomType` in `settings_changes`), so CAS M7 verifies data-`ALTER`-on-CA via the CA-default suite (Task 7) rather than the inline-disk oracle. | Found in CAS M7 Task 3: the inline-disk mutation oracle could not `MODIFY COLUMN`; established as orthogonal to content-addressing and deferred | **FIX (holistic, separate from CA):** normalise the resolved disk name into the metadata's stored `settings_changes` once (so every downstream consumer sees a `String`), or have all ALTER-path settings processing skip/convert the `disk`/`storage_policy` `CustomType`. Should be done as a general inline-disk hardening task and verified on a non-CA inline-disk table. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** pre-existing upstream issue, not CA work; kept here only as a pointer. |
| B55 | **(M8 review, hardening — NOT a data-loss hole given re-validate) Fenced GC-lock steal is last-writer-wins, not a CAS.** `tryAcquireGcLock`'s steal path `rewriteObject(gc.lock)` unconditionally, so two mounters that both see an expired lock can transiently both believe they lead (each allocates a DISTINCT, atomic fence token, but the lock-object rewrite races). The per-delete fence guard (`on-disk fence != held fence ⇒ abort`) + the re-validate-under-lock (re-reads refs ∪ live sessions before each delete) mean a transient second leader can only ever delete a GENUINELY-unreachable blob (idempotent, safe) — so this is a liveness/efficiency + cleanliness issue, not data loss. | Found in CAS M8 final review (CRITICAL severity for cleanliness; defused for safety by re-validate) | **FIX:** make the steal a true CAS — e.g. `delete`-then-`condCreateIfAbsent` the lock (only one stealer wins the create), or a fence-keyed lock object — so at most one holder ever has `on-disk-fence == held-fence`. Also change the guard to `on-disk fence > held ⇒ lost`. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the old gc.lock was deleted; the new lease IS a CAS on gc/state and is modeled (stage 5 split-brain). |
| B56 | **(M8 review, MINOR) `pool/fence/<n>` objects accumulate unbounded + cold-acquire is O(total-ever-allocated).** The fence allocator `condCreateIfAbsent(pool/fence/<n>)` never deletes old tokens, and the absent-lock acquire path starts the scan at 1, re-probing every historical fence on each cold acquire. Bounded in the warm case by the `existing.fence_token + 1` hint. | Found in CAS M8 final review | **FIX:** seed the cold-acquire hint from `max(listed pool/fence/*) + 1`, and/or GC fence objects below the current holder. Also: `sessionPinnedBlobs` casts the unsigned `lease_deadline_unix` to signed `int64_t` for the `< now` compare — compare in the unsigned domain (errs toward "pinned") to be safe. Plus a documented RESIDUAL of the B-fix (15da0f90c32): a writer that stalls >lease_seconds entirely within the few-ms commit critical section (between the commit renewal and the ref publish) reopens the tiny window; the commit re-HEAD-after-renew retry mitigates, and the lease duration is tunable — a fully-airtight close needs a stronger primitive (conditional delete / 2-phase publish) or Keeper (B11). **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the old fence/<n> allocator was deleted; the new fence is a monotone field in the manifests + registry. |
| B68 | **OPEN (tracked 2026-06-05; moved from the night log).** GC S4 Phase 4 — OPTIONAL Keeper acceleration (spec §12.2). A pure **bounded cache** (ephemeral session pins, a per-shard epoch mirror, a valid-negative tombstone cache with the *seal-to-Keeper-before-recheck* coherence rule, and a log-tail mirror) reaching the **1-PUT normal-mode floor**. The lockless GC model is **COMPLETE + correct WITHOUT it** (the S4 tests pass without Keeper), so this is purely the remaining acceleration — north-star: tests green WITH and WITHOUT Keeper. | Optional acceleration only; the lockless §7 handshake (S4) is complete and correct without Keeper | Plan: `../plans/2026-06-05-ca-gc-s4-lockless-handshake.md` (Phase 4). Backend swap only (bounded cache over the same protocol), no layout change; mirrors the B11 "Keeper is a faster catalog" seam. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** old-GC Keeper acceleration; superseded by B101 over the incarnation protocol. |
| B69 | **OPEN — DO-NOT-MERGE-UNTIL-PROVEN attended-review gate (tracked 2026-06-05; moved from the night log).** GC S4 drops the `gc_lock` (a data-loss-critical change); the §7/§5.1 race oracles want **human review before a production merge**. S4 is implemented + oracle-green on the PoC branch — the sign-off is the user's. **Residual review points:** (1) the in-process-pin-set narrow-guard shape; (2) the scoped `detached`-ref serializer; (3) op-budget measured as distinct-control-object depth (not raw PUTs). **RESOLUTION PATH → B70** (the GC S4 review-remediation tracker carries the Tier 1 correctness + Tier 2 G1/G3 fixes that discharge this gate). **Code remediation (B70) DONE — human sign-off still pending.** | S4 changes locking + deletion semantics where a bug = data loss; the attended-review sign-off is outstanding | The §7 interleaving oracle + the lock-free append-as-epoch-folds + the reaper-race oracles are the proof (148 `ContentAddressed*` gtests green); the gate is the human sign-off, not more code. See the "CA GC S4 DONE" subsection + plan `../plans/2026-06-05-ca-gc-s4-lockless-handshake.md`. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the S4 review gate guarded the OLD GC's gc_lock removal; that code was deleted wholesale - the incarnation GC shipped with its own adversarial review + TLA+ controls. |
| B71 | **DEFERRED — GC observability counters.** Missing spec-mandated ProfileEvents: `cas_s3_sequential_control_depth_per_commit`, `cas_writer_tombstone_s3_fallbacks`, `cas_log_batch_size`, `cas_s3_session_fallbacks`. The S1 drift validator should be `LOG_WARNING` + a counter (under-count drift is the dangerous direction). Sweep stats + reaper failures log at `LOG_TEST` (invisible in production) → promote. | Review (GC S1–S4); observability gap, not correctness-blocking | The ProfileEvents register at the relevant GC/commit/session sites; the S1 drift validator's log level + a new counter; promote the sweep-stats / reaper-failure log levels off `LOG_TEST`. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** old-GC counters; the intent lives in B99 (RoundReport-based observability). |
| B72 | **DEFERRED — `GcCompaction` spill-or-doc.** The header claims spill-to-scratch but `base` / folds / `all_keys` are fully in-memory. Either implement the spill, or correct the doc AND use a merge-iterator over the two sorted maps instead of `all_keys`. | Review (GC S1–S4); doc-vs-impl mismatch + an in-memory structure where the header promised spill | `GcCompaction` (the `base`/folds/`all_keys` structures); either a real spill-to-scratch or a merge-iterator over the two sorted maps + a doc correction. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** GcCompaction was deleted with the PoC. |
| B73 | **DEFERRED — `allSettledEpochsFolded` per-commit LIST.** It constructs a fresh `GcCompaction` and LISTs `gc/snap/` per commit for an eager-reap optimization — uncounted S3 LISTs on the commit path. Cache the folded watermark, or drop the eager reap. | Review (GC S1–S4); uncounted S3 LISTs on the hot commit path for an optional eager-reap optimization | `allSettledEpochsFolded` (the per-commit `GcCompaction` construction + `gc/snap/` LIST); cache the folded watermark or remove the eager reap. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** allSettledEpochsFolded was deleted with the PoC (the epoch/fold handshake does not exist in the incarnation design). |
| B74 | **DEFERRED (cleanup) — GC dead / redundant code.** `flushDueWindows` has no caller; `enqueue`'s return value is always discarded; `CompactionResult::folded_counts` is read only by tests; the S1 `InMemoryBlobRefIndex` drift validator runs every sweep and is EXPECTED to under-count on multi-node pools (log noise) → gate it behind a flag / demote per the spec's "transitional instrumentation." | Review (GC S1–S4); dead code + transitional instrumentation that produces expected log noise on multi-node pools | Remove / gate `flushDueWindows`, the discarded `enqueue` return, the test-only `CompactionResult::folded_counts`; gate the S1 `InMemoryBlobRefIndex` drift validator behind a flag (spec's "transitional instrumentation"). **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the dead code this listed was deleted with the PoC. |
| B75 | **DEFERRED — GC robustness nits.** No overflow guard in the decimal generation parsers (`PoolPaths.cpp` / `readActiveGenHint`); no `chassert` against `uint64_t` epoch wrap; `reconciliation_cadence_rounds` is a non-atomic `int64_t` written by the config thread + read by the GC thread → make it `std::atomic`. | Review (GC S1–S4); robustness hardening (parse overflow, epoch wrap, a benign cross-thread non-atomic read) | `PoolPaths.cpp` / `readActiveGenHint` (overflow guard); a `chassert` on `uint64_t` epoch wrap; `reconciliation_cadence_rounds` → `std::atomic`. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** old-GC robustness nits; the machinery no longer exists. |
| B76 | **DEFERRED — GC diagnostics.** Corrupt `gc/log` / snapshot exceptions lack the object key. The resurrection-cap exception says "retry the operation" (misleading). The v1→v2 `WriteSession` deserialize is fail-closed but on a shared / mixed-binary pool throws `NOT_IMPLEMENTED` every round and can abort the whole sweep → add a version-conditional parse like `PoolMeta` already has. | Review (GC S1–S4); diagnosability + a fail-closed version mismatch that can abort the sweep on a mixed-binary pool | Add the object key to corrupt-`gc/log`/snapshot exceptions; fix the resurrection-cap message; give `WriteSession` deserialize a version-conditional parse (mirroring `PoolMeta`). **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** old-GC diagnostics; the intent lives in B99. |
| B77 | **DEFERRED (needs-verify, 2 items).** (a) **session-lease-vs-large-uploads:** a >300 s upload can expire the session (the §7 flag) before the writer's tomb re-check — confirm the renewal cadence covers worst-case part sizes (couples to B70 #2 session-state work). (b) **`resolveAndResurrectGeneration`** can return a generation whose object was never created if the sealed predecessor's bytes are unavailable — add a presence check (low-probability, masked by the read-path repair). | Review (GC S1–S4); two needs-verify items — a lease-vs-upload-duration window and a low-probability resurrect-without-object case | (a) verify the session-renewal cadence vs worst-case part-upload duration (couples to B70 #2); (b) add a presence check to `resolveAndResurrectGeneration`. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the needs-verify items referenced old-GC behavior deleted with the PoC. |
| B78 | **DEFERRED (the G3/authority completion — review author's required follow-up).** Replace **Scan B** — the sweep's generation-BLIND full re-validate delete gate (`sweepCandidates` → `identity_reachable_in` → `markReachableBlobs` over `listLivePartIds`, checking the bare `blobKey(H)` at g=0) — with the **§6.2 sessions+compaction authoritative check** (the compaction count over folded epochs ∪ live sessions, generation-aware). Only then does the lockless layer become the **sole** delete authority and is G3 ("no bucket-wide scan on the normal path") truly met. **PRECONDITION (load-bearing):** B70 Tier 1 (generation accounting #1/#6 + fail-closed session coverage #2) **and** the new lockless-path oracles MUST be green first — Scan B is the current over-protective net that makes #1/#2/#6 leak-only; removing it without Tier 1 = the I7c/I1/I6/I8 data-loss path. **DO-NOT-MERGE-UNTIL-PROVEN: this is the data-loss transition** (it changes the delete authority); attended review + the §6.2 race oracles are mandatory. | Review author's two-scans correction: B70 #4 only replaces Scan A (discovery); Scan B (the gate) survives, so the authority/G3 goal is not actually reached until Scan B is replaced — tracked here as the explicit follow-up | Spec `../specs/2026-06-05-ca-gc-s4-review-remediation-design.md` §7 (headline follow-up). Plugs into `ContentAddressedGC::sweepCandidates` / `identity_reachable_in` / `markReachableBlobs` (the delete gate) — swap the full-scan reachability for the §6.2 sessions ∪ folded-snapshot compaction count. Gated on B70 Tier 1 + oracles. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the G3/authority completion belonged to the old GC's checkpoint design; the incarnation full-GC walk (B94) has its own exact-cut authority design (spec section 8). |
| B81 | **DEFERRED (flaky, pre-existing) — `04295_content_addressed_mutation_no_leftovers` GC-reaper race.** No-projection GC reclamation oracle; leftover count varies run-to-run (140/126), all `.tombstone` markers in `blobs/` the background reaper left within the 60s poll (grace=2s/interval=1s). Independent of the projection read-back work (test+ref byte-unchanged; zero GC code changed; eager-vs-queued hardlink publishes identical durable state). Stabilize the oracle (poll until the reaper converges, or assert on live blobs/parts not `.tombstone` gravestones) or fix reaper completeness vs the poll window. | Surfaced during Phase A smoke (2026-06-05); confirmed pre-existing by static evidence | `04295` test polling vs the background GC tombstone reaper (grace/interval vs 60s poll). **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the GC-reaper race belonged to the old sweep; 04295 is green on both lanes under the incarnation GC. |
| B83 | **DEFERRED (the principled cure for the resurrection-cap exhaustion) — pin the content `part_id` manifest in-flight to close the seal-vs-resurrect race.** A full CA-local run (2026-06-05) failed 33 tests with `resolveAndResurrectGeneration exceeded N iterations`: the GC writes a PERMANENT `.tombstone` gravestone per round (kept forever even after SWEEP), so a hot content identity accrues sealed generations ~1/`gc_interval_sec`; a commit must resurrect through them all, and the bounded loop throws when it exceeds the cap. Tonight's fix raised the cap 8→256 (safe headroom — the loop is correct+terminating at any cap), which clears the test suite, but does NOT cure the root cause. **Root race:** the write-path `WriteSession` pins the part NAME (`session.part_id = part_name`), NOT the content-derived `part_id` from `computePartId(manifest.blobs)` (only known at commit), so `sessionPinnedPartKeys` does not protect the manifest object during the window between `condCreateIfAbsent` and ref-publish (ref published LAST). The GC can therefore seal a manifest generation a committer is actively resurrecting. **Cure:** make the manifest object in-flight-pinned during commit (mirror the B49/B52 blob in-flight pin for the content `part_id`), OR have the GC skip sealing a generation with a live in-flight manifest pin — so a generation under active resurrection is never sealed. Then the cap can return to a small value. **Design-sized** (touches session/pin lifecycle + the §7 handshake proof) → not a tonight-fix. | A full CA-local suite run surfaced it (2026-06-05); analysis ranked the in-flight manifest pin as the principled cure vs. the cap-bump band-aid | `ContentAddressedTransaction::resolveAndResurrectGeneration` (the loop) + `commitOnePart` (the manifest `condCreate`→ref-publish window) + `ContentAddressedGC::sweepCandidates` seal guard (currently only `successorGenerationExists`/reachability) + the `WriteSession` part-id-vs-name pin. Pin the content `part_id` in `in_flight_pinned_blobs`-style state and have the seal honor it. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** generations/seals were deleted wholesale; resurrection is an in-place fresh-tag overwrite with no cap to exhaust. |
| B84 | **DEFERRED (deepest fix; reopens a closed hole) — bound the permanent-gravestone count.** SWEEP keeps the `.tombstone` gravestone forever (commit `db1e1dcc262`) to close an ABA hole, so the sealed-generation count of a hot identity grows without bound over time — the ultimate driver of B83's cap exhaustion. A bounded scheme (delete the gravestone once the identity is fully unreachable AND no successor chain is live) would cap the count, but it reopens the exact ABA window the permanent gravestone was introduced to close → needs the §7 proof revisited. Lower priority than B83 (B83's in-flight pin removes the test-relevant pathology without reopening ABA). | Analysis of the 2026-06-05 resurrection-cap exhaustion (deepest root: unbounded gravestone accrual) | `ContentAddressedGC` SWEEP branch (keeps `.tombstone` forever) — a bounded-gravestone scheme; must preserve the ABA safety the permanent gravestone provides. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** permanent gravestones do not exist in the incarnation design (exact-token deletes need no tombstones). |
| B85 | **DEFERRED (robustness completion of the read-path generation resolution) — wire the 404→`repairBlobGenOn404` fallback into the blob data GET.** 2026-06-05 FIX 1 made `resolveBlobGenKeyForRead`/`resolvePartGenKeyForRead` read the `active` hint (default 0) on a cache miss, which resolves the common resurrected-content read (the writer best-effort-advances `active`→g+1). But `active` is BEST-EFFORT (a failed PUT leaves it stale), so a reader can still resolve to a swept generation and get `S3_ERROR 499`/`FILE_DOESNT_EXIST`. The plan's (`2026-06-05-ca-gc-s3-generations-tombstones.md` Task 2 Step 2) safety net — on a blob/manifest GET 404, call the EXISTING `repairBlobGenOn404`/`repairPartGenOn404` (LIST `blobs/<H>/`, pick the highest present generation, repair `active`, cache) and retry once — is NOT wired to the column-data GET (only to `loadPartManifestOrThrow`). Wire it. The surface is awkward: `getStorageObjects` hands a bare `StoredObject` to a generic `ReadBufferFromS3`, so this needs a CA-aware read wrapper that catches the miss (`S3_ERROR`/`NoSuchKey` on S3, `CANNOT_OPEN_FILE`/`FILE_DOESNT_EXIST` on local) and re-resolves, OR carrying the `BlobHash` on the `StoredObject` for a disk-level callback. Not data loss (bytes present at the live generation); this closes the stale-`active` edge that FIX 1 doesn't. | 2026-06-05 CA-S3 full run found the read-path resolved bare H→g0 (swept); FIX 1 (active-hint read) handles the common case, this is the completing safety net (the dropped plan Task 2 Step 2) | `ContentAddressedMetadataStorage::getStorageObjects` read boundary + `repairBlobGenOn404`/`repairPartGenOn404` (exist, wired only for manifests) — catch the data-GET 404 and re-resolve+retry. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the 404->repair generation fallback machinery was deleted; one key per hash means there is nothing to repair toward. |
| B86 | **DEFERRED (test-harness / infra, not a CA bug) — the full stateless suite needs S3-aware timeouts to run clean on CA-over-S3/minio.** The 2026-06-05 full CA-S3 run had ~139 failures that are NOT CA logic bugs: ~84 `Timeout exceeded (180s) while flushing system log ... to S3` (the query/part/text logs write to the S3-backed CA disk and the flush is slower than the 180s cap under load), ~46 process-level wall-clock `Timeout! Killing process group` (S3 read/write latency vs local disk), + a few timing-flaky result diffs (`02456_async_inserts_logs`, `02676_optimize_old_parts_replicated`, `00652_mergetree_mutations`). All pass on CA-local and on plain disks. To get a clean full-suite signal on CA-over-S3, either point the *system logs* at a non-CA/local disk in the CA-S3 test config, or raise the system-log-flush + per-test wall-clock timeouts for the S3-storage job, or accept these as known-infra and filter them in triage. Not a product defect. | 2026-06-05 CA-S3 full run: ~139 of 147 failures were S3-latency timeouts (system-log-flush + process), structurally absent on CA-local | The CA-S3 test config (`content_addressed_s3_storage_policy_for_merge_tree_by_default.xml`) + the s3-storage job's timeout settings; route system logs off the CA-S3 disk or tune timeouts. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** the CA pool moved off MinIO to RustFS (T13/B93); CA-S3 lane timeout tuning is part of normal lane triage now. |
| B88 | **DEFERRED (non-blocking follow-ups from the B87-completion review).** (a) **`LocalObjectStorage::listObjects` directory-symlink parity:** the race-tolerant recursive-walk rewrite (commit `6e89957d7d4`, explicit `directory_iterator` stack + `error_code`) now FOLLOWS directory symlinks (a cycle could loop), whereas the old `recursive_directory_iterator` SKIPPED them. Harmless today (CA object-store/local trees have no dir symlinks) but it's a GENERIC class — add an `is_symlink` skip for exact parity. (b) **`republishDetachedStagingIntoActive` diagnosability:** the `LOGICAL_ERROR`→`FILE_DOESNT_EXIST` downgrades (matching the plain-disk concurrent-ATTACH race) make a non-race ref inconsistency surface quietly; add a `LOG_DEBUG`/`text_log` note on the benign concurrent-consume. | B87-completion review (2026-06-06): both non-blocking | (a) `LocalObjectStorage::listObjects` skip dir symlinks; (b) `ContentAddressedTransaction::republishDetachedStagingIntoActive` debug-log. **ARCHIVED 2026-06-12 (M-W groom, OBSOLETE):** tied to the B87-completion context (done); the one generic residue - LocalObjectStorage::listObjects following directory symlinks - is an upstream nit independent of CA. |

## Historical narrative — PoC era (2026-06-02..05), verbatim {#poc-history}

> Session-log sections moved out of the live backlog 2026-06-12 (M-W groom). Everything here is
> PoC-era history: the mechanisms described (per-part footers, sidecars, `gc_lock`, generation
> tombstones, reachability sweep) were DELETED at M-W; the incarnation core replaced them.
> Rows that remained live (B48, B54, B66a/b, B79, B80, B82, B89, B31) were lifted into the live
> file; the surrounding context is preserved here unedited.

| ID | Item | Why deferred | Where it plugs in (dead-end check) |
|----|------|-------------|-------------------------------------|
| B2 | ~~Cross-table dedup / global pool~~ **PULLED INTO M1** (decision): `parts/`+`blobs/` are a **global per-disk pool** from day one; GC reachability is global (marks all tables' refs); cross-table ATTACH is **ref-only, no copy**. Deferred-but-pool-compatible: the *distributed/multi-coordinator* global reachability and GC-coordinator election (→ B3), and cross-cluster pool sharing. | Pool scope is architecturally load-bearing — retrofitting global onto per-table reaches into GC mark-roots, removal, and ATTACH, so it is built global up front. | INVARIANT: one disk root = one pool = one coordinator/GC owner; no cross-cluster sharing of a disk root without a shared catalog+GC (else cross-catalog reachability is blind → data loss). |

**Concurrency invariant baked into M1 (not deferred):** one pool (disk root) = exactly one GC coordinator, and **delta-driven GC requires that coordinator to observe the *complete* delta feed of ref changes**. M1 (single process) is complete by construction. Coordinated multi-writer (B11) gets its global delta feed from the existing Keeper replication `/log` + per-replica `/parts` watches consumed by a single-leader GC — still no full `LIST`. Uncoordinated multi-writer has no global delta feed and is **fail-closed** by the **pool-ownership lease/marker** M1 ships (a process refuses to GC/mount if another live, non-coordinated owner holds the pool) — so two independent servers sharing a bucket are *safe by default* even before B11. Layout note: refs are namespaced **`store/<serverid>/<table_uuid>/`** (per server/replica; GC marks the union across serverids); `frozen/` lives outside the table path (independent lifetime).

**Scale invariants baked into M1 (not deferred):** GC is **delta-driven, never a per-pass full `LIST`**; the refcount is a **derived cache** (S3 is ground truth, rebuildable by sharded reachability); the metadata storage is **lazy** (LRU footer cache, refs listed once at load) so RAM ≈ existing `data_parts`, with the only O(M-blobs) structure persisted off-heap.


| — | (Already flagged as in-code `TODO(phase3)`): honor `object_storage->getCommonKeyPrefix()` at the three resolution sites; add a footer cache behind `loadFooterOrThrow` (single chokepoint). | Phase-3 work | No interface change — localized. |

| — | **Phase 4 — GC: built, then DOWNGRADED to opt-in after review.** A per-disk reachability sweep (`ContentAddressedGC::runSweepOnce`: live refs → `part_id`s → `markReachableBlobs`; sweep `blobs/`+`parts/` unreferenced past `grace`) + a background thread (`ContentAddressedGCThread`). The 2026-06-02 review found a **P1 data-loss bug** (`markReachableBlobs` returned BARE hashes while the sweep listed FULL fan-out object keys → reachable set never matched → the sweep would delete LIVE blobs; the tests missed it because they hand-seeded a self-consistent key convention and the integration test had no *live* table during a sweep). **Fixed** (reachable set now uses full object keys; a regression test builds parts through the real transaction and asserts a live part survives a sweep). It also confirmed the **grace window is unsafe** (time must not protect *live* work — see B32) and that **un-coordinated background deletion is unsafe** (no pool-ownership enforcement). So: the background sweep is now **OFF by default** (`content_addressed_gc_enabled`, default false); only `triggerAndWait` (manual/test) runs. **Re-enable only after B32 (safe protocol) + the `_pool_meta` ownership self-check.** `BlobRefIndex` stays an UN-WIRED seam (quarantined; reachability is the explicit M1 GC) until B9. | Built; gated off pending B32 | Background sweep stays disabled until the pin+lease+fence protocol (B32) + ownership (B11 `_pool_meta`) land. |

| B25 | **Refs keyed by `ServerUUID` — non-replicated robustness.** Non-replicated refs live under `store/<ServerUUID>/<table_uuid>/refs/…`. If the server's UUID changes (lost/regenerated `server_uuid` file, a cloned/restored server), the refs are orphaned — blobs/footers are intact (global pool) but the table's parts "disappear" until a DR rebuild (list `store/*/` + covering). | Subtle operational footgun; surfaced by the audit | Decide: tolerate (DR rebuild from `store/*/` is already the B3/B9 reachability primitive) vs. a **stable per-disk/per-table ref root** for the non-replicated case (server UUID is the right key only for the multi-replica B1 case). |
| B26 | **Storage-backend orthogonality (local / NFS / shared-fs), explicitly supported & tested.** The content-addressed metadata storage is generic over `IObjectStorage` (the Phase 1–3 unit tests already run it over `LocalObjectStorage`), so `metadata_type = content_addressed` composes with **any** `object_storage_type` (`s3`, `local`, `azure`, …) — incl. a plain local directory or an NFS/shared-fs mount, giving content-addressed dedup/no-rename benefits without S3. Make this a **first-class, documented, tested** configuration, not just an incidental property. | Validated only at the unit level over `LocalObjectStorage`; not yet exercised in a running server or documented as supported | Caveat to harden: **put-if-absent atomicity** is read-after-write on S3 but a local existence-check+write on `LocalObjectStorage` (racy multi-writer; NFS create atomicity is weaker) → safe single-writer (M1), multi-writer needs B11/B7. Verify `LocalObjectStorage::writeObject` is crash-safe (write-temp-then-rename) for blob durability. No layout change — same objects, different backend. |


**P3.5 server bring-up — DONE (B27 resolved + root cause `use_fake_transaction`).** A `content_addressed` disk now runs end-to-end in a real server (`clickhouse local`): CREATE / INSERT / SELECT / persist-across-restart / blob-dedup / `OPTIMIZE FINAL` merge, verified independently. Required: (1) `use_fake_transaction=false` for `content_addressed` (`RegisterDiskObjectStorage.cpp` — content-addressing needs Keeper-style real deferred transactions so the footer/ref publish at `commit` has a commit point; the per-file autocommit default bypassed it); (2) directory ops + derived per-file metadata ops (`setLastModified`/`chmod`/`setReadOnly`/`truncateFile`) as no-ops, `moveDirectory`/`moveFile` as fail-closed re-pin/re-key, `getLastModified` part-dir branch; (3) non-part passthrough (`store/<server>/<uuid>/files/`). server_id is the nil UUID under `clickhouse local` (expected; B25).

**B22(a) FIXED (both halves):** (1) the CA key builders prepend the metadata storage's `storage_path_prefix` (= the object-storage common key prefix) on read and write, so objects land under the configured disk root, not the process CWD; (2) the write-buffer **spill** goes to a real **server-local scratch dir** (`<server path>/disks/<name>/cas_scratch/`, mirroring the `metadata_path` convention; configurable via `cas_scratch_path`) instead of `getCommonKeyPrefix()` (which is an S3 key, not a local path). B22(a) is fully resolved.

**P3.6 — stateless test GREEN in the real praktika harness (DONE).** `tests/queries/0_stateless/04278_content_addressed_disk.sql` (a natural black-box oracle: CA-disk table vs a normal MergeTree table on identical data) passes `Stateless tests (arm_binary, parallel)`: `04278_content_addressed_disk [ OK ]`, `Failed: 0` (independently re-run). It exercises CREATE/INSERT/SELECT/dedup/`OPTIMIZE FINAL`/`DROP`. Two further server-only gaps the harness caught and we fixed: (a) the disk-startup **access-check probe** (`clickhouse_access_check_<uuid>` at the disk root) → a generic verbatim disk-file passthrough (`diskFileKey`, B27); (b) **`DROP TABLE` hung** because `removeRecursive` was unimplemented → it now **unlinks refs / table-files / generic files** scoped by `table_uuid` and **never deletes `blobs/` or `parts/`** (the `should_remove_objects` predicate is ignored — for CA we always keep shared blobs and always remove refs). Orphaned blobs/footers leak until the Phase-4 GC — the accepted M1 state (a unit test `RemoveUnlinksRefsKeepsBlobs` pins that blobs survive a drop). 26 `ContentAddressed*` + 66 regression unit tests pass.

**P3.6b — MinIO/S3 integration test GREEN (DONE).** `tests/integration/test_content_addressed_s3/` runs a `content_addressed` disk over **real S3 (MinIO)** and passes `Integration tests (arm_binary, distributed plan, 1/4)` (`1 passed`, `0 failed`, independently re-run): CREATE/INSERT/SELECT/dedup/`OPTIMIZE FINAL`/**node restart → persists**/`DROP`. No S3-specific CA-code changes were needed beyond the B22(a) spill fix — `putIfAbsent`/`tryGetObjectMetadata`/`listObjects`/access-check all worked over MinIO unchanged. This **proves B26** (the layout is backend-agnostic: the same code passes over `local` AND `s3`). **Still open:** Phase 4 GC (DROP/merge leak blobs — the biggest remaining gap for a usable system), Phase 5 feature gate (projections/patch parts not yet rejected), B23 mutable per-part state, B1 replication.

**M5.2 — honest capabilities + reject-unsupported + `_pool_meta` self-check (B31 + B11 self-check) — DONE.** Part A: `DiskObjectStorage::supportZeroCopyReplication` and `supportsHardLinks` now return false for a `content_addressed` disk (new `IDisk::isContentAddressed` predicate, overridden via `IMetadataStorage::isContentAddressed`). Zero-copy is consumed across MergeTree to decide replicated-blob sharing (out of scope, B1); `supportsHardLinks` is consumed at exactly two MergeTree sites — `checkAlterIsPossible` (data/`ADD PROJECTION` ALTERs) and `checkMutationIsPossible` (mutations, and lightweight delete which calls it) — so false fails all of those closed with a clear "immutable disk" message while INSERT/SELECT/merge/DROP/DETACH/settings-and-comment-ALTER are untouched (they do not consult the flag). Part B: `MergeTreeData::checkContentAddressedDiskRestrictions` (called from the `MergeTreeData` ctor for both CREATE and ATTACH) rejects a table that **has any projection** on a content_addressed disk with `SUPPORT_IS_DISABLED`. Part C: a `_pool_meta` object at the pool root (`PoolMeta` struct: format `version` + `owner_server_id` (`ServerUUID`) + `claimed_at_unix`) is read/written on metadata-storage `startup` via `claimPoolOwnership` — absent → claim; same server → OK; unknown version → fail closed; different server → fail closed unless the disk config sets `content_addressed_allow_shared_pool` (default false). This is the guard that gates re-enabling background GC (B32). Single-process-correct only (no CAS/lease — that is B32). Stateless: `04281_content_addressed_projection_rejected`. **No new unsupported-feature gap discovered** beyond what B5 already tracks — projections and patch parts are the known gaps and are now gated.

## Post-review hardening (2026-06-02 design review) {#post-review}

A close review surfaced one live data-loss bug, one corruption bug, and a set of structural fixes. Immediate fixes (data-loss GC key space + regression test; background sweep off-by-default; partition-clone ALTERs gated off — B21) are in progress. The rest is the **structural hardening sequence** below.

| ID | Item | Why / status | Where it plugs in |
|----|------|-------------|-------------------|

**Recommended sequence (post-review):** (1) B29 typed identities → (2) B31 capability gate + reject-unsupported → (3) B23 split mutable state out of the manifest ✅ (M5.3) → (4) B19+B28 versioned little-endian formats + exact parse rules ("POCs become production data") ✅ (M5.4) → (5) B30 whole-part commit contract → (6) B32 safe GC protocol (then re-enable background GC). After (2) the disk is at least *honestly* scoped. See `plans/2026-06-02-cas-mergetree-m5-hardening.md`.

## North star (overnight autonomous goal) {#north-star}

**Goal:** a praktika **stateless test configuration** that swaps the *default* MergeTree disk for a `content_addressed` disk (mirroring the existing "s3 storage" stateless variant), under which **(almost) all existing, unpatched stateless tests pass** and **the S3 bucket is empty after the run** (no leftovers). This is the drop-in acceptance bar — it forces the CA disk to be a near-complete storage backend and the GC to actually reclaim on DROP.

**Acceptance criteria:**
- A `config.d` (+ praktika job flavor) makes the server's default storage a `content_addressed` disk (local-backed for the fast variant; S3/MinIO for the leftover check), with `content_addressed_gc_enabled=1` + a short grace so GC runs during the suite.
- The bulk of the stateless suite passes; features the CA disk legitimately cannot support yet are explicitly **skipped/gated** (tracked), not silently broken — "almost all".
- After the suite (all tables dropped), the pool's `blobs/` + `parts/` are empty (GC reclaimed everything) — a post-run bucket-empty assertion.

**Path:** finish the **M5 hardening** sequence (B29→B32) so the disk is correct + honest + safely GC'd, then **M6 = the drop-in config + run-triage-fix loop** (the full suite is the oracle, as the smoke test was): run → classify failures (unsupported-feature → gate/skip + backlog; real bug → fix) → repeat until "almost all" pass + no leftovers. Adjust the design as needed per the trends here (typed IDs, whole-part contract, pin+lease GC). M6 gets its own plan when M5 lands.

## Loose ends / tech-debt parking lot {#loose-ends}
- **Background GC must be re-enabled** for the M6 test config (it's off by default now) — but only via the opt-in flag, and the safe protocol (B32) should land before any *shared/S3* multi-mounter use; for a single-owner test pool the reachability sweep + flag is acceptable.
- **praktika `result_job.json` false `failure`** artifact: the stateless run shows `Failed: 0, Passed: N` + `Ok [...]` but `result_job.json` can say `status: failure` (a post-step/ownership-fix harness quirk). The per-test `test_result.txt` + the `Failed: 0` banner are authoritative. (Also: the integration job name `amd_binary, 1/5` is stale; use `Integration tests (arm_binary, distributed plan, 1/4)`.)
- A pre-existing uncommitted edit to `plans/2026-06-02-cas-mergetree-m1.md` (a prior-session build-wiring note) is in the working tree, left out of commits; fold in or drop.
- **`generateObjectKeyForPath` placeholder** (B24) still returns a bogus key on the CA transaction — make it throw.
- **Detached/frozen namespaces** (B4/B12) unimplemented; DETACH currently allowed only as `DROP_PARTITION`-class (kept), not the ref-move detach — revisit with the whole-part contract.
- **B23 sidecar residuals (M5.3):** (1) the per-ref sidecar uses TWO representations — a bundle `RefSidecar` index (`refs/<part>.meta`, for `listDirectory`/carry-forward) and per-file objects (`refs/<part>.<file>.meta`, for the byte reads, since `getStorageObjects`→`readObject` needs exactly one file's bytes). Both are tiny and ref-scoped; collapse to one when a metadata-storage inline-read path exists, or once `readInlineDataToString` is wired into the disk read path. (2) A metadata-only `ALTER` that rewrites `metadata_version.txt` is currently gated off (`supportsHardLinks=false`); when mutations land (B30) it becomes a sidecar rewrite (rewrite the per-file object + bundle), not a manifest rewrite — wire it there. (3) `createHardLink` decides mutable-vs-content on the SOURCE basename so clone-into-`detached/` paths still carry mutable bytes by value; revisit when the whole-part clone contract (B30) makes detached parts first-class (the autocommitted "detached" part shape is the same pre-existing quirk B23 did not change).

## M5 Steps 1–3 milestone review (2026-06-02, fresh subagent + codex) {#m5-review}

**Verdict: no BLOCKER / no CRITICAL.** The B21 per-file-autocommit corruption mode (`DiskObjectStorage::createHardLink` building a one-file manifest+ref) was traced on every production caller for a `content_addressed` part; each is gated (`supportsHardLinks=false` for mutations/lightweight-delete/data-ALTER; `checkAlterPartitionIsPossible` for MOVE/REPLACE/ATTACH/FREEZE partition) or fails closed (`writeFileImpl` rejects `autocommit && isPartFilePath`; cross-disk copy → `createMetadataFile` NOT_IMPLEMENTED). Typed-IDs, `_pool_meta` fail-closed, and the `.meta` sidecars (consistent `isRefMetaKey`, `.`-vs-`_` part-name boundary, ref-scoped cleanup) all verified clean. Two follow-ups (must close before M6 exposes the full suite):

| ID | Item | Why / status | Where it plugs in |
|----|------|-------------|-------------------|

Minors (acceptable for M1): `partIdFromRefPayload` throws on an empty ref payload (fail-closed; one corrupt ref aborts the sweep + fails that part's reads); `createHardLink` mutable-vs-content by source basename is safe (the 3 mutable names are reserved part-internal files).

## M6 Task A — B33/B34 gates + drop-in config + first smoke (2026-06-03) {#m6-task-a}

**B33 — SUPERSEDED / lifted (CAS replication).** The blanket `CREATE`/`ATTACH` rejection of
`ReplicatedMergeTree` on a `content_addressed` disk has been LIFTED. **Single-replica
`ReplicatedMergeTree` on a CA disk now supports the full core lifecycle** — CREATE/INSERT/SELECT/
merge/mutation/DROP plus fetch-by-relink (one replica, no peer fetch). Only the **queue-clone
partition ops** (`REPLACE PARTITION`, `MOVE PARTITION`, `ATTACH PARTITION [FROM]`, `ATTACH PART`)
remain FAIL-CLOSED on a Replicated CA table (`Code: 344 SUPPORT_IS_DISABLED`, "the replication-queue
clone path is not audited on CA (B33/B21)"; audit deferred to Phase 3.2 — see B61 below).
Original test `04283_content_addressed_replicated_rejected.sql` (asserted blanket rejection) is now
obsolete.

**Replicated-CA stateless triage (CAS M6, 2026-06-04).** Un-gated all 360 `no-content-addressed-storage`
tests that use `ReplicatedMergeTree`/`Replicated(`, ran the 350 runnable stems (10 stay gated via
`no-object-storage`/`.gen`) under `Stateless tests (arm_binary, content_addressed storage, parallel)`
in 7 batches. **310 pass un-gated** (confirms single-replica Replicated-CA INSERT/SELECT/merge/mutation
broadly works). **40 re-gated**, by bucket: **23 queue-clone** (REPLACE/MOVE/ATTACH PARTITION/PART,
Code 344 — incl. `02916` whose 344 is swallowed by `distributed_ddl never_throw` into a data diff);
**5 partition-clone** (FREEZE/FETCH/FORGET, B21/B4 — `01417`, `01650`, `02995`, `03350`, `03612`);
**6 orthogonal** (tests `rm -rf` `system.parts.path`, which on CA is a relative object-storage key, not
a local FS path — `02253`, `02254`, `02255`, `02369`, `02370`, `02444`); **4 orthogonal** (need an
auxiliary ZooKeeper ensemble — `02442`, `02447`, `02735_system_zookeeper_auxiliary`,
`02735_system_zookeeper_connection`); **1 orthogonal** (`02980` needs an s3_plain disk + hits the
BACKUP gate); **1 real-CA-replication-bug candidate** (`02486`, B61 below).

**B34 DONE (gate + honest behavior verified).** Two parts: (1) the temporary-hard-link BACKUP path
(`DataPartStorageOnDiskBase::backup`, the Ordinary-DB path) now fails closed with a clear
`SUPPORT_IS_DISABLED` instead of a raw `LOGICAL_ERROR` when `make_temporary_hard_links &&
disk->isContentAddressed()`. (2) Empirically: in an Atomic (UUID) DB — the suite default — `BACKUP`
uses the pointer-holding path (`make_temporary_hard_links=false`) and **succeeds** on CA, but
`RESTORE` onto a CA disk **fails closed** with `NOT_IMPLEMENTED` "Autocommit writes are not supported
for part files on a content-addressed disk" (restore writes part files via the disk autocommit path,
which the B30 whole-part write contract is required to support — a separate known M1 gap, NOT
corruption). Test: `04284_content_addressed_backup_pointer_holding.sh` (asserts BACKUP succeeds +
RESTORE fails closed). **New deferred item B35** (restore-onto-CA) below.

**Drop-in config + run mode.** `tests/config/config.d/content_addressed_storage_policy_for_merge_tree_by_default.xml`
(content_addressed disk over `object_storage_type=local`, set as the default MergeTree policy,
`content_addressed_gc_enabled=1`/`grace=60`/`interval=5`). Installed by a new
`--content-addressed-storage` flag in `tests/config/install.sh` (mutually exclusive branch — installs
ONLY this config, no s3/azure defaults), wired to a `content_addressed storage` praktika option in
`ci/jobs/functional_tests.py`, exposed as the job parametrization
`Stateless tests (arm_binary, content_addressed storage, parallel)` (`ci/defs/job_configs.py`).

**First smoke taxonomy (seeds Task B).** ~19 tests run under CA-default (substring expansion of the
chosen stems). Pass: `00001_select_1`, `00098_primary_key_memory_allocated`, `00203_full_join`,
`00386_has_column_in_table`, `00564_temporary_table_management` (basic SELECT/INSERT/JOIN/temp).
Distinct failure shapes:
- **(1) unsupported-feature, gated (expected):** `Mutations are not supported for immutable disk` (`SUPPORT_IS_DISABLED` 344) — mutations + lightweight-delete (`02352_lightweight_delete*`, `00652_mergetree_mutations`); `Table projections are not supported on a content_addressed disk yet` (344) — projection tests at CREATE (`01710_projections*`); `ALTER TABLE commands are not supported on immutable disk … except setting/comment` (344) — `01278_alter_rename_combination`; `ReplicatedMergeTree is not supported on a content_addressed disk yet (B1)` (344) where a test created a Replicated table. These are the gates firing correctly → the tests legitimately can't run on CA (skip-candidates).
- **(1-derived) cascade:** `No projection is used …` (`PROJECTION_NOT_USED` 584) and `Unknown table expression identifier` (`UNKNOWN_TABLE` 60) are downstream of an earlier gated CREATE in the same projection test (the table never got created), not independent bugs.
- **(2) REAL BUG candidate:** `00502_custom_partitioning_local` — `DETACH PARTITION` on CA leaves a wrong `system.detached_parts` row (`\N  metadata_version.txt  \N …` instead of the part name `all_1_2_1`). `DETACH PARTITION` is NOT in the gated clone-class set (only MOVE/REPLACE/ATTACH/FREEZE are), so a supported-ish op misbehaves — the detached-part materialization on CA produces a sidecar/mutable file (`metadata_version.txt`) where the part dir is expected. First category-2 finding; needs root-cause in Task C.

> M6 acceptance ("almost all pass") will be reached by **skipping** the category-(1) feature tests
> (mutations/lwd/projections/data-ALTER/replicated) via the suite's tag mechanism, UNLESS Task B's
> larger run shows a gated feature dominates (then pull in B30). Category-(2) (`DETACH PARTITION`)
> is a real bug to fix.

### M6 Task B — baseline suite taxonomy + path-to-almost-all decision (2026-06-03) {#m6-task-b-taxonomy}

**What was run.** A curated slice of **~584 distinct stateless tests** spread across the whole numbering
range (substring stems `0000 0001 0002 0003 0050 … 02900 03000 … 03800`), under the drop-in mode
`python3 -m ci.praktika run "Stateless tests (arm_binary, content_addressed storage, parallel)"`
(CA disk over `object_storage_type=local` as the default MergeTree policy, GC enabled, `grace=60`,
`interval=5`). Two passes were needed because of a server crash:

- **Run 1** aborted at **129/658 selected tests** when the server took **SIGSEGV** during
  `03008_deduplication_cases_from_docs` (the dedup-log bug → **B37** below). The hung-check killed the
  run, so everything after the crash was truncated.
- **Run 2** re-ran the **453 not-yet-run tests with the `*dedup*` family excluded** (13 tests held out
  as the known B37 crash cluster, plus the crasher itself) and completed cleanly with **no crash**.

Combined distinct coverage: **583 tests with a result (491 passed, 92 failed)** plus the **14 held-out
dedup-family tests** (B37 cluster) ≈ **~597 tests covered**.

**Counts table (combined, distinct).**

| Bucket | Signature (first distinct error / diff) | Count | Example test |
|---|---|---|---|
| **Total run** | — | **583** (+14 dedup held out) | — |
| **Passed** | `[ OK ]` | **491** | `00001_select_1`, `00203_full_join` |
| **Failed** | `[ FAIL ]` | **92** | — |
| (1) Replicated reject | `ReplicatedMergeTree is not supported on a content_addressed disk yet (B1)` (344) | 45 | `00509_extended_storage_definition_syntax_zookeeper`, `03100_lwu_02_basics` |
| (1) ALTER-data gate | `ALTER TABLE commands are not supported on immutable disk … except setting and comment` (344) | 21 | `00030_alter_table`, `00804_test_alter_compression_codecs` |
| (1) Mutations gate | `Mutations are not supported for immutable disk` (344) | 15 | `00806_alter_update`, `01200_mutations_memory_consumption` |
| (1) Partition-clone gate | `Partition operation ALTER TABLE … is not supported on a content_addressed disk` (344) | 3 | `04005_empty_part_name`, `02009_array_join_partition` |
| (1) Projections gate | `Table projections are not supported on a content_addressed disk yet` (344) | 3 | `03002_analyzer_prewhere`, `04004_part_log_projections_duration_ms` |
| (1) RESTORE-onto-CA (**B35**) | `Autocommit writes are not supported for part files on a content-addressed disk` (48) | 2 | `03001_backup_matview_after_modify_query`, `03001_restore_from_old_backup_with_matview_inner_table_metadata` |
| **(2) DETACH mis-list (B36)** | `system.detached_parts` shows `metadata_version.txt` not `all_1_2_1` (result-diff) | 1 | `00502_custom_partitioning_local` |
| **(2) remote_data_paths (B38)** | `filesystem error: in file_size: Is a directory ["content_addressed_pool/store"]` (1001) | 1 | `03000_traverse_shadow_system_data_paths` |
| **(2) dedup-log SIGSEGV (B37)** | SIGSEGV (signal 11, read 0x8) in `MergeTreeDeduplicationLog::addPart` during `INSERT … VALUES` | 1 (+13 held-out family) | `03008_deduplication_cases_from_docs` |
| (3) infra/flaky | `Timeout!` — `stateful, long` test mis-selected into the stateless slice (needs `test.hits`, uses `parallel_replicas`) | 1 | `03800_autopr_reuse_index_analysis` |

So of **92 failures: 89 are category-(1) expected gate firings**, **3 distinct category-(2) real-bug
clusters** (B36, B37, B38 — one test each in this slice, but B37's whole `*dedup*` family ≈ 14 tests),
and **1 category-(3)** infra mis-selection. **No leftover/flaky-passed-with-objects (the third "leftover"
sub-bucket) was observed in this slice** — that is Task D's no-leftovers assertion to verify directly.

**Top category-(2) real-bug clusters = Task C's queue:**
1. **B37 — non-replicated dedup-log SIGSEGV (crash).** The dominant bug: `non_replicated_deduplication_window`
   on a plain `MergeTree` (a SUPPORTED op) writes an append-mode dedup log the immutable CA disk cannot
   host; `current_writer` stays null and `addPart` derefs it → server crash → aborts the whole run.
   Fix or fail-closed before any unattended full run.
2. **B38 — `system.remote_data_paths` traversal throws** `Is a directory` on the CA pool root (a SUPPORTED
   introspection read path).
3. **B36 — `DETACH PARTITION` mis-lists** a sidecar `metadata_version.txt` as the detached part in
   `system.detached_parts` (carried over from Task A; reconfirmed identical here).

**Decision — skip+fix is enough; do NOT pull in B30 yet.** Of the 89 category-(1) failures, the dominant
gates are **Replicated (45)** and the **mutation/ALTER family (21 ALTER-data + 15 Mutations + 3 partition-clone
= 39)**. Crucially, the Replicated bucket is heavily inflated by **one feature family in this numeric slice**
— the `03100_lwu_*` lightweight-update tests, which create `ReplicatedMergeTree` and/or run mutations
(≈30 of the 92 failures are `03100_lwu_*`). These are **legitimately un-runnable on CA** (replication = B1,
mutations = B30, both deferred) and are exactly the kind of tests that get **skipped**, not fixed.

Counting the path to "almost all":
- **583 run − 89 category-(1) skips = 494 candidate-runnable**; of those, **491 already pass** and only
  **3 distinct real bugs** (B36/B37/B38) fail. So after **skipping category-(1)** and **fixing the 3
  category-(2) clusters**, the runnable suite is essentially **green (~494/494 ≈ 100% of runnable; ~85%
  of all-selected stay, ~15% skipped as known-unsupported)**.
- No single gated feature is so pervasive among *otherwise-passing* tests that we'd have to *support* it
  to reach "almost all": the gated tests are concentrated in feature-specific files (lwu, mutations,
  replicated, projections) that are themselves the unsupported feature, not incidental users of it.
  Mutations/ALTER do NOT dominate the *passing-but-for-one-mutation* population — almost every
  cat-(1) failure is a test whose *entire point* is the gated feature.

**Conclusion:** "almost all pass" is reachable by **skip (category-1) + fix (B36/B37/B38)** alone. **B30
(whole-part commit contract) is NOT required for the M6 north star** and stays deferred; it remains the
right home for *supporting* mutations/clone/RESTORE later (B35), but is not on the critical path to
"almost all".

**Skip-tag gap (Task C prerequisite).** There is currently **no `no-content-addressed-storage` skip tag**
in `tests/clickhouse-test` and the CA run passes no CA-aware flag, so category-(1) tests cannot yet be
skipped by a tag — Task C must add the skip mechanism (mirror `no-s3-storage` / `no-object-storage`:
an `args.content_addressed_storage` flag set by the praktika option + a `no-content-addressed-storage`
tag honored in the test-selection guard). For THIS taxonomy run the only skip used was holding out the
`*dedup*` family to stop the B37 crash from truncating the run (record-only, per the plan).

> **Status:** taxonomy complete. Task C queue = **B37 (crash, top priority) → B38 → B36**, plus add the
> `no-content-addressed-storage` skip tag for the 89 category-(1) tests.

| ID | Item | Why / status | Where it plugs in |
|----|------|-------------|-------------------|

### M6 Task C2 — `no-content-addressed-storage` skip tag + suite tagging + new backlog (2026-06-03) {#m6-task-c2}

**Tag mechanism added.** Mirroring `no-s3-storage`: `tests/clickhouse-test` got a `--content-addressed-storage`
arg, a `CONTENT_ADDRESSED_STORAGE` `FailureReason`, and a `no-content-addressed-storage` tag check that
skips when the arg is set; `ci/jobs/functional_tests.py` passes `--content-addressed-storage` to the test
runner for the `content_addressed storage` praktika option. Verified end-to-end: a tagged test (`00030_alter_table`,
`00509_extended_storage_definition_syntax_zookeeper`) is `[ SKIPPED ]` under the CA-default mode and `[ OK ]`
under the normal mode.

**`no-object-storage` now also fires for CA.** The `content_addressed` disk is object-storage-backed
(`object_storage_type=local`), so the existing `no-object-storage` / `no-object-storage-with-slow-build`
guards in `tests/clickhouse-test` were widened to also trigger on `args.content_addressed_storage`. This
cleanly skips the class of tests that assert local-disk-specific behaviour on CA (local mmap counters,
hard-coded `disk = 'default'` in `system.tables`, opened-files counts) — e.g. `01343_min_bytes_to_use_mmap_io`,
`01344_min_bytes_to_use_mmap_io_index`, `00753_system_columns_and_system_tables_long`, `01475_read_subcolumns`,
`01533_multiple_nested`, `00731_long_merge_tree_select_opened_files`, `04201_trivial_count_with_additional_filter`.
These are NOT CA bugs — they are object-storage-incompatible tests (exactly the S3 variant's behaviour).

**Tagged 429 capability-gated tests** in the `00 01` substring slice (~4013 distinct tests run under
`Stateless tests (arm_binary, content_addressed storage, parallel)`). Each tagged test's failure is a
recognized CA capability-gate (signature counts): ALTER-data 163, Replicated 155, projections 41,
mutations 41, partition-clone 27, RESTORE-onto-CA/B35 2, plus the projection-merge failpoint hang
`04000_system_merges_projection_progress` (which *hangs* on a `SYSTEM WAIT FAILPOINT … PAUSE` because the
gated projection merge never reaches the failpoint — gated, tagged). Counts for the covered slice:
**~4013 run, 3551 passed, 3 pre-tagged-skipped, 459 FAIL lines → 429 distinct cat-1 (now tagged), ~31
distinct cat-2 real-bug/infra**. Of the runnable (non-gated) set, the suite is green except the cat-2
clusters below. NB: the run was force-killed by its 1h `timeout` during the serial failed-test *rerun*
phase (a property of the huge 4k-test slice, not a server crash) — the per-test `[ FAIL ]/[ OK ]` results
used for classification were already complete; **no server crash / fatal signal anywhere in the slice**
(only benign SIGTSTP=20 job-control stops from the harness timeout-killer).

**New real-bug backlog rows (category-2, NOT tag-hidden):**

| ID | Item | Why / status | Where it plugs in |
|----|------|-------------|-------------------|

**Infra/flaky (NOT CA, NOT tagged):** `00091_prewhere_two_conditions` (Timeout/hang — investigate separately),
`01103_check_cpu_instructions_at_startup` (Timeout; needs `lldb` python module), `01880_remote_ipv6`
(`All connection tries failed`), `00163_shard_join_with_empty_table` (`TOO_MANY_ROWS` random-settings flake),
`02479_mysql_connect_to_self` / `02784_connection_string` (`Can't connect`/`Connection refused` to the
MySQL/native `::1` ports — IPv6 unreachable in the local harness; both fail identically on the plain config),
`02874_parquet_multiple_batches_array_inconsistent_offsets` (`MEMORY_LIMIT_EXCEEDED` parquet parse — fails on the plain config too).

> **Status (Task C2):** tag mechanism DONE + verified; `no-object-storage`→CA widening DONE; 429 cat-1
> tests tagged + committed; cat-2 real bugs filed as B39–B43 (NOT tag-hidden). Covered ~4013 tests of the
> `00 01` slice. Remaining for a final full run: the `02*`/`03*`/`04*` numeric ranges not in this slice,
> and re-running the tagged batch to confirm green (tagged→skip, rest→pass).


### M6 Task D — no-leftovers proof (create → write → DROP → GC → empty) (2026-06-03) {#m6-task-d}

The north-star "no S3 leftovers" criterion is now proven by **two dedicated create→write→drop→wait→check-empty
oracles**, both asserting that BOTH the pool's `blobs/` AND `parts/` (content blobs + part footers) are
reclaimed by the single-owner background reachability GC after a `DROP TABLE … SYNC`:

- **Stateless (local pool):** `tests/queries/0_stateless/04290_content_addressed_no_leftovers.sh`. An inline
  `content_addressed` disk over `object_storage_type=local` with the pool rooted at an **absolute** `path`
  under `CLICKHOUSE_USER_FILES_UNIQUE` (shared between server and the test shell on a local run, so the `.sh`
  can `find` the pool directly), `content_addressed_gc_enabled=1`, `content_addressed_gc_grace_sec=2`,
  `content_addressed_gc_interval_sec=1`. It records baseline `blobs/`+`parts/` file count, CREATEs a MergeTree
  on the CA disk, INSERTs 6 distinct 100k-row batches (count rises above baseline), `DROP TABLE … SYNC`, then
  **bounded-polls** (hard 60s cap, re-checking each second — not a fixed sleep) until `blobs/`+`parts/` return
  to empty, and asserts `_pool_meta` (the durable single-owner marker) survives. The local-pool inspection
  WORKED — no fallback was needed.
- **Integration (S3/MinIO):** `tests/integration/test_content_addressed_gc_s3/test.py` was **strengthened** to
  count BOTH `cas_gc_data/blobs/` and `cas_gc_data/parts/` (previously only `blobs/`) and assert both drain
  back to ~baseline after DROP — the authoritative no-leftovers proof over real object storage.

**Observed counts** (stateless local run): baseline 0 → after-insert >0 (`grew_above_baseline 1`) → after
drop+GC 0 (`no_leftovers 1`), `_pool_meta` present. Both tests `[ OK ]` / `1 passed`.

**Informational (step 3, settle check).** A small batch (`00001 00080 00502`) under the CA-**default** config
(`Stateless tests (arm_binary, content_addressed storage, parallel)`) passed (7 passed, 1 skipped), but the
server's default pool at `ci/tmp/run_r0/content_addressed_pool` still held ~4432 blobs + ~465 parts at
teardown. This is **expected, not a leak**: the M6 default config uses `grace=60s`/`interval=5s` (deliberately
larger than any single test op so the write-vs-GC race cannot bite), and praktika shuts the server down
immediately after the short batch — well before the 60s grace window elapses — so the sweep never reclaims
the dropped test tables' objects, and the harness does not wipe the on-disk pool on shutdown. The takeaway:
the authoritative no-leftovers proof is the dedicated short-grace create→drop→wait→check-empty test (04290 +
the strengthened integration test), NOT post-suite inspection of a shared long-grace pool the harness tears
down before grace elapses. A future enhancement could add an explicit post-suite settle (`SYSTEM` flush +
wait > grace, or a teardown-time pool sweep) if a suite-wide empty-pool assertion is wanted.

> **Status:** Task D DONE. Local-pool inspection worked (no fallback). Both `blobs/`+`parts/` proven reclaimed
> on DROP, on local and S3. CA unit (69) + CA stateless (04278–04290) green; no leftover processes.

## Final external review (2026-06-03, fresh subagent) {#final-review}

**Verdict:** with background GC **OFF (the default)** — no blockers/critical for the single-server non-replicated scope. With background GC **ON** (the M6 test config) — ONE CRITICAL data-loss path (B49). Importants B50/B51; minors are existing B24 + the path-grammar heuristic.

| ID | Item | Why / status | Where it plugs in |
|----|------|-------------|-------------------|

## M6 Task E — cover 035–049 under the CA-default config (2026-06-03) {#m6-task-e}

Extended the CA-default stateless coverage to prefixes `035`–`049` (the prior sweeps reached `00`–`034`).
One real CA bug surfaced and was fixed (B52); 162 capability-gated tests were tagged
`no-content-addressed-storage`.

## M6 — empirical re-triage of ALL directly-tagged `no-content-addressed-storage` tests (2026-06-04) {#m6-empirical-retriage}

Premise: many gates were stale or carried a wrong reason. All **77** directly-tagged stateless tests
(not `.gen.sql`) were un-gated, run on the CA-default job (`Stateless tests (arm_binary, content_addressed
storage, parallel)`), and every FAILURE re-run on the plain job (`Stateless tests (arm_binary, parallel)`)
to classify it. Tally:

- **77 un-gated** for re-triage (commit 1).
- **19 now-passing → stayed un-gated** (stale gates cleared): `00427_alter_primary_key`,
  `01903_correct_block_size_prediction_with_default`, `03084_analyzer_join_column_alias`,
  `03168_attach_as_replicated_materialized_view`, `03641_analyzer_issue_85834`,
  `03672_columns_same_as_subcolumns`, `03707_statistics_cache`,
  `01701_clear_projection_and_part_remove`, `01710_aggregate_projection_with_normalized_states`,
  `01710_minmax_count_projection`, `01710_projection_detach_part`,
  `02888_attach_partition_from_different_tables`, `02998_projection_after_attach_partition`,
  `04010_create_or_replace_table_clone_as`, `04011_detach_rename_attach_column`,
  `03008_deduplication_random_settings`,
  `02861_alter_replace_partition_do_not_wait_mutations_on_unrelated_partitions`,
  `03920_replace_partition_mutation_race`, `03593_backup_with_broken_projection`. **The entire
  projection family passes on CA** (B5/B58/B59/B63/B64 closed — projections are durable on CA).
- **4 wrong-reason gates removed** (fail on CA **and** plain → not CA-specific): the auxiliary-ZooKeeper
  group `02442_auxiliary_zookeeper_endpoint_id`, `02447_drop_database_replica_auxiliary_zookeeper`,
  `02735_system_zookeeper_auxiliary`, `02735_system_zookeeper_connection`. They throw `Code: 999
  KEEPER_EXCEPTION ZooKeeper root doesn't exist … auxiliary_zookeeper2` on BOTH jobs — the local test
  config lacks the `auxiliary_zookeeper2` ensemble. A topology gap, not content-addressing. They keep
  their own appropriate tags (`no-shared-merge-tree` etc.) and stay un-gated.
- **53 confirmed-feature gates re-gated** with accurate, evidence-based reasons:
  - **part-file path-shape (6)** — `02253`, `02254`, `02255`, `02369`, `02370`, `02444`: each test body
    asserts `throwIf(substring(system.parts.path, 1, 1) != '/')`, i.e. that the part path is an ABSOLUTE
    local FS path. On CA the path is a relative object-storage key `content_addressed_pool/store/…`, so
    the test's own assertion throws `Code: 395`. Orthogonal — the storage works; the test's
    absolute-path assumption is what is CA-incompatible.
  - **FREEZE / FORGET partition-clone (8, B21)** — `00952`, `01325`, `01414`, `01417_freeze_partition_verbose`,
    `01417_freeze_partition_verbose_zookeeper`, `02995_forget_partition`, `03611`, `03612`: FREEZE is
    NOT a silent no-op — it fails closed with `Code: 344 SUPPORT_IS_DISABLED` "…not supported on a
    content_addressed disk yet (it clones parts file-by-file with no transaction, which would corrupt
    the clone)".
  - **FETCH partition-clone (2, B21)** — `01650`, `03350`: `Code: 344 SUPPORT_IS_DISABLED` FETCH
    PARTITION.
  - **transactions (21, B39, object-storage-wide)** — `01133`, `01167`, `01168_mutations_isolation`,
    `01168_mutations_isolation_2`, `01169_alter_partition_isolation_stress`,
    `01169_old_alter_partition_isolation_stress`, `01170`, `01171`, `01172`, `01173`, `01174`,
    `02345_implicit_transaction`, `02421_truncate_isolation_no_merges`,
    `02421_truncate_isolation_with_mutations`, `02435_rollback_cancelled_queries`,
    `02497_source_part_is_intact_when_mutation`, `03373_named_session_try_recreate_before_timeout`,
    `03657_merge_tree_disk_support_transaction`, `03752_attach_as_replicated_transaction_metadata`,
    `03803_transaction_mutation_race`, `03916_attach_as_replicated_implicit_transaction`: all throw
    `Code: 48 NOT_IMPLEMENTED` "MergeTree … does not support transactions" on CA. Proven CA-specific:
    `03373` and the batch-5 transaction tests PASS on the plain job (which supports experimental
    transactions) and FAIL only on CA (the append-based transaction log is incompatible with the
    immutable CA disk).
  - **BACKUP/RESTORE (15, B16/B34)** — `02843_backup_use_same_password_for_base_backup`,
    `02843_backup_use_same_s3_credentials_for_base_backup`, `02864_restore_table_with_broken_part`,
    `02974_backup_query_format_null`, `02980_s3_plain_DROP_TABLE_MergeTree`,
    `02980_s3_plain_DROP_TABLE_ReplicatedMergeTree`, `03001_backup_matview_after_modify_query`,
    `03001_restore_from_old_backup_with_matview_inner_table_metadata`, `03032_async_backup_restore`,
    `03145_non_loaded_projection_backup`, `03214_backup_and_clear_old_temporary_directories`,
    `03286_backup_to_memory`, `03315_query_log_privileges_backup_restore`, `03760_backup_tar_archive`,
    `03831_backup_archive_to_plain_rewritable_disk`, `04036_backup_partition_transaction_visibility`:
    RESTORE writes part files via autocommit (`Code: 48` "Autocommit writes are not supported for part
    files on a content-addressed disk") OR the deprecated BACKUP-via-temporary-hard-links path
    (`Code: 344` "use an Atomic database which backs up via pointer-holding"). All PASS on plain.
- **1 REAL CA BUG re-gated → B65** (`03829_insert_deduplication_info_memory`, see B65 below).

The three user hypotheses, resolved with evidence:
1. **part-file / `system.parts.path`** (`02253`/`02254`/`02255`/`02369`/`02370`/`02444`): they FAIL on
   CA, but the failure is the test's own `throwIf(path[0] != '/')` assertion — the IDisk paths work, the
   path is just a relative object-storage key, not an absolute local path. Orthogonal part-file/path-shape.
2. **auxiliary-ZK** (`02442`/`02447`/`02735_*`): they FAIL on PLAIN too (`Code: 999` missing
   `auxiliary_zookeeper2` root) → wrong-reason gate → un-gated.
3. **FREEZE** (the `*freeze*`/`forget`/`frozen` set): works as fail-close, NOT a silent no-op — every
   FREEZE/FORGET op throws `Code: 344 SUPPORT_IS_DISABLED` on CA. Confirmed-feature gate (B21).

| ID | Item | Why / status | Where it plugs in |
|----|------|-------------|-------------------|






| B61-RESOLVED(a) | **(CAS replication Phase 3.2, 2026-06-04) Replicated-CA queue-clone partition ops AUDITED + UN-GATED — the B33 gate was over-conservative.** The Phase 3.2 audit traced the replication-QUEUE clone stack on a CA disk: `executeReplaceRange` (`StorageReplicatedMergeTree.cpp` ~L3327), `replacePartitionFrom` (~L9146), and `movePartitionToTable` (~L9442) all reach `MergeTreeData::cloneAndLoadDataPart` (`MergeTreeData.cpp` L9367) with `must_on_same_disk=true` (REPLACE/MOVE) or a same-pool source (ATTACH), **no `external_transaction`**, and **`copy_instead_of_hardlink=false`** on CA (CA disks return `DiskObjectStorage::supportZeroCopyReplication()==false`, `DiskObjectStorage.h` L54, so `isStoredOnRemoteDiskWithZeroCopySupport` is false). That routes the same-disk clone through `DataPartStorageOnDiskBase::freeze` (`DataPartStorageOnDiskBase.cpp` L503), which — no caller transaction + CA disk — **self-creates ONE whole-part `ContentAddressedTransaction`** (L519-522) so all part files land in a single content-addressed commit. This is the SAME audited+verified whole-part clone the non-replicated ALTER PARTITION stack uses (M9 W2, B21 resolved) — NOT the per-file-autocommit B21 corruption path the original B33 note feared. The blanket `Code: 344` reject in `StorageReplicatedMergeTree::checkAlterPartitionIsPossible` was therefore **removed** (it now delegates to the base check, which already permits `REPLACE`/`MOVE`/`ATTACH PARTITION` on CA and keeps `FREEZE`/`FETCH PARTITION` gated). **Bug fixed during the audit:** the replicated clone params set `metadata_version_to_write`; `cloneAndLoadDataPart` wrote `metadata_version.txt` via a per-file **autocommit** `writeFile` AFTER the freeze transaction committed → tripped the B21 guard (`Code: 48` "Autocommit writes are not supported for part files on a content-addressed disk", `DiskObjectStorageTransaction.cpp` L292). Fix: `freeze` now writes `metadata_version.txt` INSIDE its self-created CA transaction from `params.metadata_version_to_write`, and `cloneAndLoadDataPart` skips its post-freeze autocommit write on the CA same-disk path. **Empirical result:** of the 22 re-gated Phase-3.2 tests un-tagged and run under the CA-default job, **21 pass with correct data** (`Failed: 1, Passed: 21`) — including `02916_another_move_partition_inactive_replica` (the MOVE PARTITION local-vs-distributed oracle), `01034_move_partition_from_table_zookeeper`, `02271_replace_partition_many_tables`, `02731_replace_partition_from_temporary_table`, `02445_replicated_db_alter_partition`, `01451_replicated_detach_drop_part_long`. The residual caveat (identical for non-replicated) is cross-disk `MOVE ... TO DISK/VOLUME`, which byte-copies via `clonePart`/`freezeRemote` (safe re-content-address, not the corrupting path); the base check cannot distinguish destination and lets it through. 110/110 `ContentAddressed*` gtests + `test_cas_replicated_relink` integration test pass (no regression). | Phase 3.2 audit of the B33 queue-clone deferral | DONE — gate lifted; `metadata_version.txt` written inside the freeze CA transaction. |




**CLOSED / answered (note, not an open item).** The earlier "data-loss-reachability under reconciliation" question is **ANSWERED**: the reconciliation reachability check is identity-level (generation-blind) and therefore over-protective, so there is **no data loss either way**. No open row tracks it; recorded here as closed (see the §9 reconciliation / DRAIN identity-level notes in the "CA GC S4 DONE" subsection).

**By-definition-gated tests (won't-fix as CA features; informational + a cleanup note).** Broken-part / lost-part tests `02253`, `02254`, `02255`, `02369`, `02370`, `02444` `rm`/`truncate` a raw `system.parts.path` (a relative object-store key on CA) — object-storage-incompatible BY NATURE (would also fail on plain s3); a cleaner long-term fix is to tag them `no-object-storage` rather than CA-specific (low-priority cleanup). `02980_s3_plain_DROP_TABLE_*`×2 test the `s3_plain` disk type specifically (correctly gated). `03350_alter_table_fetch_partition_thread_pool` is **B66a** (concurrent-FETCH fan-out torn read; local-`LocalObjectStorage`-only, S3 PUTs atomic).

## Goal A test-stabilization (2026-06-04 night) — END STATE {#goal-a-stabilization}

The "un-gate all CA-incompatible stateless tests, run on the CA-default job, fix-small / document-big /
re-gate-only-disk-incompatible-by-definition" milestone reached its end state this session. Net: the
directly-tagged `no-content-addressed-storage` set dropped from **43 → ~22**, and the ~21 un-gated tests
are green on `Stateless tests (arm_binary, content_addressed storage, parallel)`. Companion running log:
`docs/superpowers/NIGHT_LOG_2026-06-04.md`.

- **B65 DONE** — `ContentAddressedWriteBuffer` now honors `buf_size` + `use_adaptive_write_buffer` /
  `adaptive_write_buffer_initial_size` (it had hard-coded `DBMS_DEFAULT_BUFFER_SIZE`, so a wide part
  pre-allocated ~1 MiB per column and blew the INSERT memory budget → `MEMORY_LIMIT_EXCEEDED`). The
  flags are now threaded through exactly as the S3/Azure write buffers do. **Supersedes the B65 row
  above** (which recorded it as a re-gated "REAL CA BUG candidate" — that row is now resolved). Un-gated
  + passing: `03770_min_columns_to_activate_adaptive_write_buffer`, `03829_insert_deduplication_info_memory`.
  Commit `c0287d2219c`.
- **B67 DONE** — multi-part `ContentAddressedTransaction` (transactional merges/mutations); recorded in
  full in the B67 row above. Cross-reference only.
- **Un-gated this session (now green on CA):**
  - **transaction / isolation cluster (B67)** — `01168_mutations_isolation`(+`_2`,+`_3`),
    `01170_alter_partition_isolation`, `01174_select_insert_isolation`, `01167_isolation_hermitage`,
    `01169_alter_partition_isolation_stress`(+`_old`), `01171_mv_select_insert_isolation_long`,
    `02421_truncate_isolation_no_merges`(+`_with_mutations`), `02435_rollback_cancelled_queries`,
    `03803_transaction_mutation_race`, `03657_merge_tree_disk_support_transaction`,
    `03752_attach_as_replicated_transaction_metadata`, `03916_attach_as_replicated_implicit_transaction`.
  - **JSON / dynamic-ALTER** — `01825_json_type_3`, `01825_json_type_add_column`,
    `03210_json_type_alter_add_column`, `03246_alter_from_string_to_json`, `03272_json_to_json_alter`.
  - **parallel-replicas / distributed** — `02967_parallel_replicas_joins_and_analyzer`,
    `02980_dist_insert_readonly_replica`, `03279_pr_3_way_joins_{full,inner,left,right}_first`.
  - **read / merge** — `03001_block_offset_column`, `02461_prewhere_row_level_policy_lightweight_delete`,
    `02994_merge_tree_mutations_cleanup`, `02260_alter_compact_part_drop_nested_column`.
  - **stale transaction gate** — `03373_named_session_try_recreate_before_timeout`,
    `02497_source_part_is_intact_when_mutation` (their B39 "append-based / object-storage-wide" reason
    is stale now that B67 landed).
  - **FORGET PARTITION** — `02995_forget_partition` (only manipulates ZK metadata, no part write — a
    simple gate-lift). Commit `604cc66aec9`.
  - Plus the two ancillary CA-path fixes recorded under B67: the `LocalObjectStorage::listObjects` /
    `tryGetObjectMetadata` TOCTOU fix (shared with non-CA local-object-storage) and the
    `ContentAddressedWriteBuffer::cancelImpl` fix.
- **B16/B34 BACKUP/RESTORE on CA — DONE (2026-06-05).** RESTORE now materializes each restored part
  through ONE whole-part `ContentAddressedTransaction` (`restorePartFromBackup`, commit `d384298602b`) —
  the same INSERT/merge write seam — instead of the per-file autocommit one-shot that CA rejected
  (`NOT_IMPLEMENTED: Autocommit writes are not supported for content part files`). BACKUP-read already
  worked (it reads through the metadata storage like any read), so only the restore-write path needed
  the wrap. The wrap is `isContentAddressed`-gated: when `restore_tx == nullptr` the path is
  byte-identical to the original, so plain disks are unchanged. **All 13 BACKUP/RESTORE tests un-gated
  and passing on CA with ZERO CA-source changes beyond the restore wrap and ZERO re-gates** (commit
  `868c4f869ef`): projections, broken-part → detached, materialized-view inner tables, and the
  `tmp_restore` → active rename all compose with existing CA mechanisms (nested-manifest projections,
  detached-staging republish, `moveDirectory` branches). Un-gated tests:
  `02843_backup_use_same_password_for_base_backup`,
  `02843_backup_use_same_s3_credentials_for_base_backup`, `02864_restore_table_with_broken_part`,
  `02974_backup_query_format_null`, `03001_backup_matview_after_modify_query`,
  `03001_restore_from_old_backup_with_matview_inner_table_metadata`, `03032_async_backup_restore`,
  `03145_non_loaded_projection_backup`, `03214_backup_and_clear_old_temporary_directories`,
  `03286_backup_to_memory`, `03315_query_log_privileges_backup_restore`, `03760_backup_tar_archive`,
  `03831_backup_archive_to_plain_rewritable_disk`, `04036_backup_partition_transaction_visibility`.
  **Inline-CA round-trip oracle:** `05005_content_addressed_backup_restore` — `CREATE` a CA table with
  a `PROJECTION`, two-insert/two-part deterministic data, `BACKUP TABLE … TO Disk('backups', …)`,
  `RESTORE TABLE … AS …_restored`, then assert byte-for-byte data equality (`count`/`sum`/`arraySort
  groupArray`) and a `force_optimize_projection = 1` projection-served query on the restored table.
  Passes on BOTH the CA-default job and the plain job; non-CA regression (`02974`/`03286`/`03032`)
  stays green. See the spec (`2c762c70d33`) + plan (`2effa99a21e`).
- **Legitimately gated by definition (not bugs):** broken-part / lost-part path-shape tests —
  `02253_empty_part_checksums`, `02254_projection_broken_part`, `02255_broken_parts_chain_on_start`,
  `02369_lost_part_intersecting_merges`, `02370_lost_part_intersecting_merges`,
  `02444_async_broken_outdated_part_loading` (they `rm`/`truncate` a raw `system.parts.path`, which on
  CA is a relative object-store key — would also fail on plain s3); `02980_s3_plain_DROP_TABLE_MergeTree`,
  `02980_s3_plain_DROP_TABLE_ReplicatedMergeTree` (test the `s3_plain` disk type specifically);
  `03350_alter_table_fetch_partition_thread_pool` (B66a concurrent-FETCH fan-out torn read, local-only,
  known — see B66a).

## CA GC convergence — S1 DONE (2026-06-05) {#ca-gc-convergence-s1}

S1 of the GC convergence spec (`../specs/2026-06-04-ca-gc-convergence-design.md`, §10 stage table,
§5 end-state, §11 S1 test) is **DONE** (commits `9fb5b1cb7a3`, `3864c7be507`, `891538596e9`,
`0476946f35d`). S1 is **instrumentation only** — it wires the dormant `InMemoryBlobRefIndex` (the B9
seam) as an incremental reverse index and *validates* it against the authoritative full-scan, but
changes **no deletion behaviour**: the `gc_lock` + full `parts/`+`blobs/` scan stay authoritative, and
the fence lease is unchanged.

- **`InMemoryBlobRefIndex` wired.** Owned per pool by `ContentAddressedMetadataStorage` (alongside
  `gc_lock`), thread-safe (mutex-guarded `addPart`/`removePart`/`refcount`/`unreferenced`, with the
  `applied_parts` idempotency guard kept). `commit` (the content-publish branch) calls `addPart`; ref
  unlink/`drop` calls `removePart`. Index updates are **best-effort and exception-swallowed** — a
  failure to update the index logs and continues, never blocking the data path (instrumentation must
  never break commit/drop).
- **Sweep logs drift, deletes nothing differently.** `runSweepOnce` computes the index's reachable view
  after the authoritative `markReachableBlobs` and logs the disagreement as `cas_gc_index_drift`
  (`missing_in_index` / `extra_in_index`) — `to_remove` and every delete decision are unchanged. In a
  multi-node pool, or a process that started after parts existed, the index is expected to under-count
  (it only sees this process's commits since startup), so the drift line is **informational**.
- **gtest proves index ≡ authoritative scan** on a single-node-from-empty pool —
  `ReverseIndexEqualsAuthoritativeScan` — covering shared-blob dedup (two parts pin the same `H` →
  refcount 2 → drop one → still reachable → drop both → unreferenced) and idempotent re-add. This is the
  spec's S1 gate ("the validator must never disagree"). 123 `ContentAddressed*` gtests pass; CA-default
  smoke green incl. `04279_content_addressed_gc` (the sweep deletes exactly as before).
- **Transitional.** Per spec §10, the wired in-memory index is transitional instrumentation. Once S2's
  streaming snapshot+log compaction (`gc/snap` + `gc/log`) becomes the **authoritative candidate
  source**, the in-memory index downgrades to an optional per-epoch pre-filter hint (or is removed) —
  consistent with §5's "never materialized" end-state, where the durable authoritative reverse index is
  always the sorted snapshot + log.

### CA GC S2 DONE — log-structured streaming compaction is the authoritative candidate source

S2 (`../plans/2026-06-05-ca-gc-s2-log-structured.md`, spec §5/§5.1/§9/§11/§12.3) is implemented and tested:

- **Candidate source = the per-shard streaming compaction.** `runSweepOnce` no longer calls
  `listLivePartIds` / `markReachableBlobs` / `LIST blobs/` on the normal path (**G3 retired**). Per shard it
  closes the epoch (a plain fenced PUT `gc/current_epoch/<shard>=E+1`, no CAS — §5.1 rule 1) **before**
  folding, then streaming merge-joins `gc/snap/<E>.<shard>` + `gc/log/<E>.<shard>/` with **dedup-on-fold by
  `event_id`**, streams the new `gc/snap/<E+1>`, and emits every key whose running count reaches 0 (blob
  `(H)` pins **and** the `(part_id)` manifest edge) as a candidate in one pass.
- **The refs→manifests re-validate stays as the authoritative safety net** (`sweepCandidatesLocked`): it
  lists `refs/` (NOT `blobs/`/`parts/`), recomputes reachability, and drops any candidate still reachable,
  then applies the unchanged `grace` + fence + `removeObjectsIfExist` delete tail. S4 swaps this re-validate
  for the sessions + compaction handshake (§6.2).
- **Full scan survives only as `runReconciliationScan`** (the §9 orphan-drift / abandoned-upload / rebuild
  fallback), scheduled by the now-configurable `reconciliation_cadence_rounds` (disk knob
  `content_addressed_gc_reconciliation_cadence_rounds`, default 0 = off). `rebuildFromSnapshotAndLog`
  recomputes counts from snapshot + un-folded log with **no blob `LIST`**; total log+snap loss falls back to
  the scan.
- **Epoch-close-before-fold + writer epoch re-append + dedup are wired under the still-held `gc_lock`** so S4
  (which drops the lock) can rely on log-completeness; the in-memory `InMemoryBlobRefIndex` is downgraded to
  an optional pre-filter / drift cross-check (`cas_gc_index_drift`).
- **Tests (132 `ContentAddressed*` gtests green; CA-default smoke green incl. `04279_content_addressed_gc`):**
  9 new S2 oracles in `gtest_content_addressed_gc_s2.cpp` over an op-counting in-memory `IObjectStorage`
  fake — streaming-merge correctness + exactly-the-count-0 candidates (incl. the part edge), rebuild with
  **0 `blobs/`+`parts/` LISTs**, epoch advance/reclaim + idempotent empty re-run, shard isolation,
  append-as-epoch-folds (no under-count), duplicate-`+` dedup-on-fold, and live-part-is-never-a-candidate;
  plus op-budget asserts (0 `blobs/`+`parts/` LISTs on the normal compaction path; a burst of N commits
  coalesces into one log object, `cas_log_batch_size == N`).

- **OPEN (CLOSED BY S3 — see the S3 DONE note below):** on the compaction-driven normal path a key is emitted as
  a count-0 candidate **only in the fold where it crosses to 0** (`writeSnapshot` drops `<=0` keys, so a
  later empty-epoch fold never re-emits it). With `grace > 0`, `selectForSweep` records the timer on that one
  round, the key is absent next round, the timer is cleared, and the candidate is **never deleted on the
  normal path** — only the (default-off) reconciliation scan re-derives it. So S2 normal-path reclamation is
  effective only at `grace == 0`; `grace > 0` currently relies on reconciliation. `04279` stays green because
  it is a **safety** oracle (CA == reference table; never asserts reclamation by a deadline). S3/S4 must add a
  **candidate-timer carry-forward** (persist count-0 keys with their first-unreachable timer, or age
  candidates across folds) — a deletion-timer semantic change deliberately NOT made in S2 (candidate-source
  only; lock + grace + fence + delete tail unchanged). The white-box scan-based gtests drive the
  reconciliation cadence to exercise reclamation in the meantime.

### CA GC S3 DONE — generations + GC-owned tombstones make the lockless delete ABA-safe

S3 (`../plans/2026-06-05-ca-gc-s3-generations-tombstones.md`, spec §6/§6.1/§6.2/§9/§11/§13) is implemented
and tested. It replaces the bare-key direct `removeObjectsIfExist` (G2) with the generation + tombstone
state machine, the **carrier the S4 lockless handshake needs** — while the `gc_lock` is **still held** (S3
adds the tomb barrier; S4 drops the lock).

- **Generations on BOTH `blobs/<H>/<g>` AND `parts/<part_id>/<mg>` (symmetric, §9).** Every content/manifest
  key carries an explicit generation (`PoolMeta` v3, no back-compat); `blobKey(H)` == `blobGenKey(H, 0)` is
  the common-case wrapper. The manifest body still pins **bare `H`**, so `part_id` stays a pure content
  function (dedup/idempotency unchanged). A best-effort `active` hint (a plain PUT, never a CAS — G4/I7d)
  names the preferred generation; it is absent in the common g=0 case (readers default to 0).
- **GC-owned `.tombstone` seal + permanent gravestone.** A count-0 candidate is **sealed**
  (`condCreateIfAbsent(<g>.tombstone)`, single-owner, fence-gated) — a DURABLE condemnation that survives
  the process and is **re-presented every round** via `collectSealedTombstoneCandidatesLocked` (this closes
  the S2 `grace>0` liveness gap: a candidate condemned in the crossing fold is re-discovered until swept or
  recovered). SWEEP **keeps the gravestone forever** (only RECOVER deletes a tombstone).
- **Resurrection to `g+1`/`mg+1`.** A writer that finds its resolved generation **sealed** abandons it and
  `condCreateIfAbsent`s the next generation (a different physical key → ABA-proof, I4); the `+` delta carries
  the resolved `(H,g)`/`(part_id,mg)` (the S2 `event_id` generation discriminator is now used). **The
  tombstone — not the object's presence — is the only condemnation signal:** an absent-but-UNSEALED
  generation is a fresh object (the g=0 blob the write buffer already uploaded, or the manifest the commit is
  about to `condCreate`), so resurrection fires **iff sealed**.
- **mark / recover / drain / sweep** (`sweepCandidatesLocked`, generation-aware, the SAME tail for the
  compaction normal path and the reconciliation fallback): seal → grace (liveness only) → **fresh
  authoritative re-check** (refs→manifests reachability + live sessions — S3 keeps this pass; S4 swaps it for
  the sessions+compaction-only §6.2 form) → branch: **SWEEP** (identity unreachable → delete `<g>`,
  best-effort reset `active`, keep gravestone) / **RECOVER** (identity reachable, no successor → delete the
  tombstone, re-open `g`) / **DRAIN** (identity reachable, a successor `g+1…` exists → keep BOTH the tombstone
  and `<g>`, do NOT re-open — the I7a two-generations-pinned reality).
- **Reader: `active`+404→LIST fallback; a tombstone NEVER blocks a read** (§6.1). The steady g=0 read is one
  GET; a 404 LISTs the generation prefix and reads the highest present generation (byte-identical, I7c),
  opportunistically repairing `active`.
- **Reconciliation folds generation dirs** (`collectReconciliationCandidatesLocked`): a `.tombstone`/`active`
  is never an orphan (`parseGenObjectKey` skips them); a present `<g>` is an orphan only if its **identity**
  is unreachable (the manifest pins bare `H`, so the safety net is identity-level).
- **Observability (§13):** `ContentAddressedGenerationResurrectionsTotal`, `…DuplicateGenerationBytes`,
  `…TombstonesTotal`, `…GenerationsObserved`/`…HashesObserved` (the generations-per-hash p99 proxy), and
  `…OrphanBytesEstimate` are exported as ProfileEvents and asserted by a gtest.

- **Two real bugs the oracles + the (never-rebuilt) S1/S2 gtest suite exposed, fixed at the source:**
  (1) `resolveAndResurrectGeneration` resurrected on `!present`, so a freshly-created manifest (absent at
  `mg=0` before `condCreate`) spun to the 8-iteration cap and threw on **every insert** — fixed to resurrect
  iff **sealed** (a swept generation keeps its gravestone, so it is still `sealed` and routes to `g+1`).
  (2) the GC manifest resolver read `parts/<id>/0` only, so a live part whose manifest was **resurrected to
  `mg>0`** was mis-reported as a missing manifest (`CORRUPTED_DATA`, sweep aborts) — added
  `resolveManifestAtAnyGeneration` (mirrors the reader's `repairPartGenOn404`; B18 fail-close preserved).
- **Tests:** 9 new S3 oracles in `gtest_content_addressed_gc_s3.cpp` over a real `LocalObjectStorage`
  (real `O_EXCL` `condCreateIfAbsent`, real `runReconciliationScan` seal/recover/drain/sweep tail) —
  seal/resurrect/gravestone-lineage, active-reset+reader-fallback, tombstone-doesn't-block-reads,
  SWEEP/RECOVER/**DRAIN** branches, two-generations-pinned (byte-correct from either generation, the
  single-live-generation lemma NOT assumed), manifest symmetry (relink→`mg+1`), and the §13 counter asserts.
  **141 `ContentAddressed*` gtests green; CA-default smoke green incl. `04279_content_addressed_gc`.**
- **Known S3 caveat (S4 tightens):** the DRAIN re-check is **identity-level**, so a predecessor `g` is
  reclaimed only once the WHOLE identity becomes unreachable (a conservative leak while a successor still
  lives — **never data loss**). S4's generation-precise re-check (sessions + compaction, §6.2) tightens it
  AND drops the `gc_lock`; the durable tomb barrier built here is the safety carrier S4 relies on. **S3 is
  attended-review-gated** (it changes deletion semantics — a bug is data loss); the §11 race oracles above
  are that proof.

- **CA GC S4 DONE (lockless handshake, G1):** the §7 commit-vs-sweep handshake replaces the
  `gc_lock`. **Writer order:** session → upload → recheck-tomb → `+` (the `active`/generation publish)
  → live-ref — the **session is the §7 flag** (a writer is in-flight iff its session is live).
  **Session-held-until-folded** (§5.1 rule 3): a writer's session stays live until its `+` is folded
  into a durable epoch, so a GC that seals between a writer's recheck-tomb and its `+` still observes
  the session and backs off (the §7.3 reaper handles a crashed writer's stale session). **GC order:**
  seal → fresh §6.2 re-check (live-refs + manifests + **live sessions**) → recover / drain / sweep.
  **`gc_lock` DROPPED between commit and sweep (G1)** — demoted to a narrow **in-process-pin-set
  guard** (the B49 in-process blob PIN) plus a scoped **shared-`detached`-ref commit-vs-commit guard**;
  the §7 handshake now carries commit-vs-sweep safety and the fence lease carries GC-vs-GC. Safety
  proven by the §7 interleaving oracle + the lock-free **append-as-epoch-folds** + the **reaper-race**
  oracles (**148 `ContentAddressed*` gtests green**; CA-default smoke green incl.
  `04279_content_addressed_gc`; broad CA regression of 27 non-gated merge/mutation/optimize/insert/alter
  + txn-isolation tests all green, no lock-removal regression). **Op-budget:** ≤2 sequential control
  writes (the S3-only floor), measured as distinct-control-object depth (not raw PUTs).
  **FLAG — data-loss-risk stage: a DO-NOT-MERGE-UNTIL-PROVEN attended-review gate is OUTSTANDING**
  (the §7/§5.1 oracles want human review before a production merge — S4 changes locking + deletion
  semantics). **Residual review concerns:** (1) the in-process-pin-set narrow-guard shape; (2) the
  scoped `detached`-ref serializer; (3) op-budget measured as distinct-control-object depth, not raw
  PUTs. **REMAINING (optional): Phase 4 Keeper acceleration (§12.2)** — a pure **bounded cache**
  (ephemeral sessions, per-shard epoch mirror, a valid-negative tombstone cache with
  seal-to-Keeper-before-recheck, a log-tail mirror) reaching the 1-PUT normal-mode floor; the lockless
  model is **complete + correct WITHOUT it** (the "tests pass without Keeper" half of the north star
  holds), and "with Keeper" is purely the remaining acceleration.
  **Plan:** `../plans/2026-06-05-ca-gc-s4-lockless-handshake.md`; spec
  `../specs/content_addressed_shared_mergetree_design.md` (v3, §5.1/§6.2/§7/§12.2/§13).

**Plans:** S1 `../plans/2026-06-05-ca-gc-s1-reverse-index.md`;
S2 `../plans/2026-06-05-ca-gc-s2-log-structured.md`;
S3 `../plans/2026-06-05-ca-gc-s3-generations-tombstones.md`;
S4 `../plans/2026-06-05-ca-gc-s4-lockless-handshake.md`.
S3/S4 change deletion/locking semantics and carry a **data-loss RISK / attended-review** flag in their
headers — they must not ship until the §5.1 + §6 race oracles are proven.

> Add new deferred items here as they arise during planning/implementation, always filling the
> "where it plugs in" column so the architecture stays dead-end-free.

---


## Groomed 2026-06-18 — B160/B167 landed + soak-era resolved/obsolete {#groomed-2026-06-18}

> Moved from the live backlog. B160/B167 were the GC lease-heartbeat + per-server build watermark work; both landed and were exercised/validated (the live rows still read "IN PROGRESS" — stale). **B160 ARCHIVED-NOTE:** C++ impl landed `5a56962a7cc` (GC lease advisory heartbeat); productive (non-livelocked) GC has run in every soak since, including the B167 validation run it enabled. The rest are FIXED/RESOLVED/EXPLAINED/SUPERSEDED. Rows verbatim, in the live 4-column format.

| ID | Item | Why deferred | Where it plugs in |
|----|------|-------------|-------------------------------------|
| B105 | **FIXED by B116 (`d8fd11659df`, 2026-06-13).** CA blob reads now route through the STANDARD object-storage pipeline (gather + FileCache/page-cache + async prefetch) bounded by a `ReadBufferFromFileView` stage, so cache stages DO see CA blob reads (the in-manifest tiny-bytes path stays correctly memory-served, uncached). Full CA-S3 lane green (no wrong-results from caching). Original below. `ContentAddressedMetadataStorage::prepareReadPipeline` sets a custom buffer source (in-manifest bytes from memory; blob payloads through a `ReadBufferFromFileView` skipping the 256B CHCA header), so FileCache / page-cache stages never see CA reads. Parity with the PoC (the CA-S3 disk ran cacheless, B86), but now structural: the proper fix is the upstream `StoredObject` range plumbing (spec §13 integration delta) — once a StoredObject can carry (offset, length), CA returns plain ranged objects and the standard pipeline (incl. caches) applies. | StoredObject has no range field upstream; touching the generic read stack was out of M-W scope | Revisit when CA-S3 perf wants caching; one focused upstream PR (StoredObject + gather + cache key shape). |
| B106 | **AUDITED — not a bug (2026-06-13).** The in-manifest placeholder (`StoredObject{remote_path="", size}`) is correct for size consumers and fail-LOUD for byte readers, and the real consumers never read bytes from it: BACKUP reads via `BackupEntryFromImmutableFile::getReadBuffer` -> `disk->readFile` -> CA `prepareRead` (in-manifest bytes served from memory, blob bytes via `getBlobViewPlan`), NOT via the placeholder; `system.remote_data_paths` only reports the (empty) path. EVIDENCE: the CA BACKUP tests `04284_content_addressed_backup_pointer_holding` and `05005_content_addressed_backup_restore` PASS in the full CA-S3 lane. The only path that would touch the placeholder is checksum-from-remote (`allow_checksums_from_remote_paths`), which the immutable-file entry computes from `readFile` when no remote checksum is available. No fix needed. Original note below. Mutable per-part files / inline entries / verbatim namespace files return `StoredObject{remote_path="", size}` — correct for size consumers, fail-loud for any byte reader that bypasses `prepareReadPipeline`. Consumers to audit at/after T13: BACKUP of a CA disk, `system.remote_data_paths`, checksum collectors. | No second read mechanism exists for manifest-embedded bytes; loud beats silently wrong | If a legitimate consumer surfaces, either teach it `tryGetInManifestBytes` or land B105 and inline-as-objects. |
| B113 | **FIXED (`d1590609597` shard cache + `47ec3241d53` tree cache, 2026-06-13).** Per-read shard-manifest + tree decode (~2x on read-heavy). FIX: token-validated decode cache for shard manifests (`Store::readShardDecoded`, head-validated, immutable shared_ptr; resolveRef/listRefs use it, writers stay uncached) + content-addressed tree decode cache (immutable, bounded). MEASURED CA-local read-heavy: 2.66x -> 1.51x vs plain (read time -44%). New gtest `ResolveDecodeCacheInvalidatesOnWrite`; CA battery 224/224. Residual 1.51x is inherent per-read overhead (head probe, per-file blob GETs), not decode. Original below. Every wiring read resolves ref->tree via `resolveRef` (GET + strict-JSON decode of the WHOLE shard manifest - which holds many parts' refs) + `readTree` (GET + decode). `00139_like`: 228s plain vs 456s CA-local. The PoC decoded one compact per-part binary manifest. Compounds with B105 (no cache stages). | Correctness-first wiring; no caching layer yet | A small wiring-side resolve cache keyed (ns, shard) -> decoded RootShard, invalidated by shard_version (one HEAD or carried from the last CAS); plus tree-decode memoization per TreeId (immutable - safe forever). Natural M-F optimization. |
| B115 | **FIXED (`440871098a9`, 2026-06-12). WRONG RESULTS (T13 CA-S3 lane): `optimize_read_in_order` + experimental reverse key on a CA-S3 disk returned wrong rows.** ROOT CAUSE: `ReadBufferFromFileView` tracked its buffer-end offset incrementally and broke when the inner buffer DISCARDS its working buffer in `setReadUntilPosition` (which `ReadBufferFromS3` legitimately does on a range change, rebasing to the consumer position) — the view's stale buffer-end got clamped to the new until-bound, teleporting the reported position forward by the discarded bytes; `CompressedReadBufferFromFile`'s "already at position"/"within working buffer" shortcuts then re-served a stale block (duplicated + missing granules, checksum-clean). The in-order reader triggers it by adjusting the right mark per granule with pending buffered data. FIX: rebase `file_offset_of_buffer_end` from the impl's own post-op accounting (`getPosition() + available()` inside the swap window) after EVERY impl op — also repairs the truncate-then-extend drift in `nextImpl`. Also affected upstream `PackedFilesReader` (packed parts on remote disks) — the fix + new gtest battery (`gtest_read_buffer_from_file_view.cpp`, first-ever coverage for the view; 14/36 red pre-fix) are upstream material. VERIFIED: minimal repro green, exact test file 5/5 MATCH, view battery 36/36, CA battery 241/241, `03257` triple `Failed: 0, Passed: 3` on the CA-S3 praktika job. Original analysis preserved below. Deterministic minimal repro (single process, `clickhouse local`, fresh pool, even `max_threads = 1`): table `(i, j)` `ORDER BY (i, j DESC)` with `allow_experimental_reverse_key = 1`, `index_granularity = 2`, 100 rows; `SELECT * FROM x2 ORDER BY i, j DESC LIMIT 5` returns `1090,1080,1050,1040,1030` (expected `1090..1050`), the ASC variant returns rows UNSORTED — duplicated + missing granules, block-aligned (off-by-granules). Data is intact (`count`+checksum OK); `optimize_read_in_order = 0` is correct. ISOLATION MATRIX: CA-local correct, plain-s3 correct, CA-S3 wrong — only the combination fails. Plans identical (`EXPLAIN PIPELINE` and `EXPLAIN indexes = 1` byte-equal CA-local vs CA-S3), so the bug is in read EXECUTION: blob payloads are served via `ReadBufferFromFileView` (256 B envelope-skipping view) over the backend's `readObject` buffer — plain `ReadBufferFromFile` on local (correct) vs `ReadBufferFromS3` on S3 (wrong), so the view's seek/swap/resize state machine over a LAZY-seek inner buffer is the prime suspect (`ReadBufferFromFileView` has ZERO test coverage). Surfaced as `03257_reverse_sorting_key_simple` failing in the T13 diff-retest (the full-lane `03257_reverse_sorting_key` diff was the same bug). Repro script: `tmp/03257_min4.sql` (needs RustFS on 127.0.0.1:11122, bucket `test`). | The ONE confirmed real CA bug from the T13 full-lane triage; everything else was env (B93) or pre-existing | NEXT: (1) a semantics gtest battery for `ReadBufferFromFileView` over an inner buffer with lazy/buffer-resetting seek (mimic `ReadBufferFromS3`) — random seek/read/setReadUntilPosition sequences vs golden bytes; (2) debug-build run of the minimal repro to log the view's seek/next offsets; (3) fix + un-gate/re-run the 03257 pair; suspect sites: `ReadBufferFromFileView::seek` (`file_offset_of_buffer_end = result + available()` after the swap dance), `resizeWorkingBuffer` interaction with the inner buffer's in-buffer-seek bookkeeping. |
| B116 | **FIXED (read path `d8fd11659df` + write path `5b69a99fd5b`, 2026-06-13). The CA-S3 lane timeout driver.** Two independent roots, both fixed: (READ) CA blob reads bypassed the standard pipeline and issued whole-blob GETs that were torn mid-response on right-mark changes → broken-pipe churn; fixed by routing blob reads through the standard gather/cache/async pipeline bounded by a `ReadBufferFromFileView` stage (right-mark bounds now reach `ReadBufferFromS3`, GETs drain and pool). (WRITE) the conditional-PUT retry storm — see [[B118]]. VERIFIED: the 4 read-in-order tests that timed out at 600s now pass in <4s in the CA-S3 lane. Original analysis below. CA-S3 read connection-churn + broken-pipe retry storm (T13, 2026-06-12). The lane's ~50 test timeouts are this, not scanner/heal (B93) or concurrency.** End-to-end chain, all evidence-backed: (1) **Root — connection churn.** The CA read path serves blob bytes via `ReadBufferFromFileView` over `ReadBufferFromS3`; read-heavy queries (read-in-order, `LIMIT`, merges) open a GET per granule range and ABORT it before draining the response (the reader took what it needed and moved on / `LIMIT` stopped early). An aborted-before-drain GET cannot be returned to the HTTP keep-alive pool, so it is hard-closed. `ss` during a live lane: **ClickHouse-side `TIME-WAIT` ~100 every sample, RustFS-side `TIME-WAIT` = 0, `ESTAB` ~2** — i.e. ClickHouse does all the active closes; near-zero reuse; ~100-connection/s churn. (2) RustFS is the victim of the mid-response close: it logs `rustfs_ecstore: get_object_with_fileinfo .../blobs/<h> err Io(Kind(BrokenPipe))` (broken pipe writing the half-sent GET body). (3) Under that churn a fraction of ops surface to ClickHouse as `Poco::IOException: Broken pipe` (18,365 in one lane, growing 327->502/min). (4) ClickHouse RETRIES broken-pipe (correctly classified retryable) but with NO cancellation bound in the outer `ReadBufferFromS3` loop (see B-retry-cancel below), so a query whose client was killed ZOMBIES — observed retrying one blob 521x over 42 min (client killed at +10 min). (5) Churn latency + retries push read-heavy queries past the 600 s test timeout. REPRODUCES at `--workers 3` (NOT load-dependent) and the broken-pipe count is identical with scanner/heal disabled (NOT B93). AWS S3 / MinIO tolerate mid-response aborts silently (RST, no retry); RustFS logs them and the churn destabilises it. | The actual blocker for "green CA-S3 lane without timeouts"; everything else (B93, B115) was secondary | FIX DIRECTIONS, in order of leverage: **(a) B105 proper ranged-read plumbing** — issue exact `Range` GETs that the buffer fully drains, so connections return to the keep-alive pool and the abort-churn disappears (the real root fix; upstream `StoredObject` range field). **(b) drain-or-bounded-abort policy** in the CA read path / `ReadBufferFromS3` so an abandoned GET with little remaining is drained (poolable) rather than hard-closed. **(c) retry-cancellation fix** (below) to kill the 42-min zombie amplifier. **(d) RustFS-side**: investigate whether RustFS can be made to not error/destabilise on client aborts. Pin tooling: `tmp/capture_who_closes.sh` (ss-based; no host CAP_NET_RAW so tcpdump only works inside the --privileged praktika container, which lacks the binary —  install or use ss). **UPDATE 2026-06-12: RustFS-tuning REJECTED, ClickHouse-side root CONFIRMED.** (1) Re-ran the 4 read-in-order tests with `RUSTFS_RUNTIME_WORKER_THREADS=24 RUSTFS_RUNTIME_MAX_BLOCKING_THREADS=2048 RUSTFS_OBJECT_MAX_CONCURRENT_DISK_READS=4096` -> all 4 still timed out at 600 s. NOT RustFS runtime/Tokio starvation; fix-direction (d) is out. (2) Mechanism = ClickHouse reads FEWER bytes than the GET response holds and never drains it: each blob read builds a `ReadBufferFromFileView` sized to the WHOLE blob payload (`readBlobPayload`: `[location.offset, location.offset+location.length)`), but the consumer (a granule / `LIMIT` / a column slice in a merge) reads only part and the buffer is destroyed, OR `ReadBufferFromS3::seek` hits a backward / >`remote_read_min_bytes_for_seek`(=4 MB default) forward seek - both call `impl.reset()` / close the GET mid-response (`ReadBufferSeekCancelConnection`) instead of draining. RustFS logs the half-sent response as `BrokenPipe`; a fraction surface to ClickHouse and retry (B117). ACTUAL fix = (a)/(b): make CA blob reads request+drain only the consumed bytes (B105 ranged-read plumbing - bound the GET to the granule range so it drains and pools) or drain-on-close in the CA read path. NB `readBigAt` already drains (`istr.ignore(INT64_MAX)`); the streaming `nextImpl`/`seek` path does not. |
| B117 | **FIXED (`dd408fef7ba`, 2026-06-13).** Outer `ReadBufferFromS3` retry loop ignored query cancellation. FIX: `processException` now returns false (stop retrying) when `CurrentThread::get().isQueryCanceled()`, mirroring the SDK `RetryStrategy` check, so a killed query's reads no longer zombie-retry a transient error. Largely upstream-general. Original below. `Client::RetryStrategy::ShouldRetry` (SDK layer, `src/IO/S3/Client.cpp:107`) bails on `isQueryCanceled()`, but the OUTER ClickHouse loop in `ReadBufferFromS3::nextImpl` (`src/IO/ReadBufferFromS3.cpp:160-203`) and `processException` (~L320-352) have NO cancellation check — they retry broken-pipe with exponential backoff up to `s3_max_single_read_retries`, compounding with the SDK retries (observed "attempt 107/50"). So when a test's client is killed at the 600 s deadline the server query does NOT abort: it keeps retrying broken-pipe reads for tens of minutes (observed 42 min), piling up zombie queries that add load and worsen the churn (B116). Broken-pipe itself is correctly retryable (transient network drop). | Turns a transient drop into a 600 s+ zombie; makes the lane tail (and resource use) far worse; largely upstream-general, not CA-specific | Add an `isQueryCanceled()` / connection-alive check to the outer `ReadBufferFromS3` retry loop (and the write path), mirroring the SDK strategy at `Client.cpp:107`; optionally bound the broken-pipe retry budget. Upstream-relevant. |
| B118 | **FIXED (`5b69a99fd5b`, 2026-06-13; regression test `05006_content_addressed_dedup_blob_insert`). CA-S3 INSERT hang.** ROOT CAUSE: a conditional blob PUT (`If-None-Match: *`) that loses its precondition (e.g. two byte-identical columns dedup to one blob) shipped its full multi-MB body before reading the response; RustFS rejects the large doomed body with a mid-upload close / `500 UploadStreamError` (RETRYABLE), so the AWS SDK retried the upload up to `s3_retry_attempts`(=500)× with backoff while `WriteBufferFromS3::finalizeImpl → TaskTracker::waitAll` blocked the INSERT (~40 min). FIX: `Expect: 100-continue` on conditional PUTs (`PocoHTTPClient`: `setExpectContinue` + `peekResponse`) — the server evaluates the precondition before the body, returns a clean 412, the body is never sent, no retry storm. Verified on the deterministic repro (count 1000000, exit 0, zero broken-pipe/500/retry) and in the CA-S3 lane (Failed: 0, Passed: 5 incl. the 4 read-in-order tests that previously 600s-timed-out). Same root family as the lane write-storm — now resolved. Original analysis below. (distinct from the lane broken-pipe issue; relates to B48).** A single-process `clickhouse local` with a `content_addressed` disk over standalone RustFS: `CREATE TABLE` succeeds, logs `Start to refresh statistics`, then the process BLOCKS during the first large INSERT (`numbers(1e6)`) — killed at 300 s, never committed a part; **RustFS logs ZERO broken pipes** (so this is a pure ClickHouse-side block, NOT a RustFS drop). A 1000-row insert finishes in <10 s, so it is volume-triggered. `ptrace_scope` blocks `gdb` attach without root, so no stack captured yet. Likely the same family as B48 (`clickhouse local` + CA lifecycle hang). Repro: `tmp/ca_hang_stack.sh` (full 2M-row workload). | Found while trying to reproduce B116 in `clickhouse-local`; it reproduces a DIFFERENT failure, so `clickhouse-local` is not a faithful vehicle for B116 (which needs server mode) | Capture a stack (enable `ptrace_scope=0` or run under root/`strace`, or use the server's `system.stack_trace`); determine if it is the CA write path (manifest CAS loop / GC heartbeat) or statistics (`PackedFilesReader`) blocking. Server-mode INSERTs DO work (the lane inserts fine), so it may be local-mode-specific. |
| B122 | **FIXED 2026-06-13 (`08add5c0d4f`) — `commit` partial-commit rollback.** `ContentAddressedTransaction::commit` published N refs SEQUENTIALLY and flipped `committed=true` only at the end, with NO rollback of already-published refs if a later `publish` threw → a multi-part txn (`parts` keyed by `{ns,ref}`, size>1 reachable) that failed mid-loop left earlier refs LIVE+visible while the disk layer saw the txn as failed. Five umbrella reviewers flagged this as the top blocker. FIX: track the refs THIS commit creates and, on any exception in the loop, best-effort `dropRef` them before rethrowing (all-or-nothing). **Fail-closed:** only refs ABSENT before publish are rolled back (a pre-existing ref is never destroyed on the error path — publishing over a live ref does not occur in the MergeTree write path, unique part names); `updateRefPayload` mutations (autocommit one-shots) are individually durable by design and deliberately NOT rolled back. **GUARANTEES/TLA+ (user Q 2026-06-13):** a partial commit is NOT a CAS/incarnation protocol violation — each `publish` and the rollback's `dropRef` are individually gate-checked + journalled, leftover uploads are GC-reclaimable debris; spec §9 invariants (INV-NO-LOSS/NO-RETURN/JOURNAL-COVERAGE) all hold. It is a wiring-layer transaction-atomicity gap at the IDisk boundary, ABOVE the model's abstraction line (the model's atomic action is a single publish/dropRef; a sequence where one fails is already covered as independent events). The fix adds NO new model obligation → correct coverage tier is a wiring-layer fault-injection gtest, NOT TLA+. TDD: `CaWiringWrite.PartialCommitRollsBackPublishedParts` (`FaultyLocalObjectStorage` fails the 2nd shard-manifest publish) — verified failing pre-fix, passing post-fix; full Cas*/Ca* battery green (225). | Top write-path review finding | DONE. REMAINING sub-item: RENAME TABLE `move_namespace` (`:567`) still has the cross-ref/verbatim multi-namespace gap (+ may leave a table split across two namespaces on a mid-loop throw) — a longer, separate multi-namespace move not covered by this commit-loop rollback; tracked as [[B126]]. |
| B132 | **RESOLVED 2026-06-14 (soak-test Phase-1 GREEN exercised the populated-pool fsck path).** The soak harness runs `Cas::runFsck` (via the mounted, fixed binary) at every quiescent checkpoint against the live, POPULATED shared pool, asserting `dangling=0` and `{dryrun} ⊆ {fsck unreachable}` — exactly the sub-project-D smoke this item asked for. Phase-1 GREEN (seed=20260613, 1500 ops, 6 workers, 5 checkpoints) confirmed it end-to-end: every checkpoint had counts matching both replicas, `dangling=0` (HEAD-confirmed, see [[B141]]), `dryrun_subset=ok`, residual `unreachable` logged as B140 M-F debris. Note from the original — fsck/read-only mode needs a POPULATED pool; live CLI smoke deferred to the soak harness:** The read-only CA disk mode + `clickhouse-disks fsck`/`ca-gc-dryrun` (DONE — see [[project_ca_fsck_readonly_subproject_a]] / commits T1-T7 on `cas-mergetree-poc`) are gtest-covered at the storage level (`Cas::runFsck`, `Gc::previewDeletes`, read-only fail-closed — 232 Cas*/Ca* green), and the CLI commands' build/register/dispatch/guards + the `--query` nonzero-exit-on-failure are verified. NOT yet exercised: the happy-path CLI run of `fsck`/`ca-gc-dryrun`/`list`/`read` against a POPULATED read-only pool. Reason: opening a FRESH/empty CA pool read-only fails at mount (correct — `PoolMeta::createOrValidate` cannot write the absent pool-meta on a read-only backend), and populating a pool needs a running writable server. So the live smoke belongs in soak-test **sub-project D** (running replicas populate the shared pool; then fsck/ls/cat run at quiescent checkpoints). | Sub-project A wrap-up | In sub-project D: at a quiescent checkpoint, run `clickhouse-disks fsck --detail` + `ca-gc-dryrun` + `list --recursive`/`read` against the live shared pool opened read-only; assert dangling=0, exit codes, and `{preview} ⊆ {fsck unreachable}`. |
| B138 | **RESOLVED 2026-06-14 — CONCLUSION (1): NOT a CA data-loss bug; it was the harness's unsafe ASYNC-insert retry.** Decisive A/B (seed=20260613, same 1500-op workload, fresh cluster each): with **SYNC inserts** (`async_insert=0`) every count checkpoint matched the model EXACTLY (op 300/600/900/1200 = 13463/7951/21716/32640, dangling=0) — including the ops that lost rows under async; with **ASYNC inserts** the op-627 resurrect-`ABORTED` retry was followed by a 20-row deficit at checkpoint 900 (both nodes 21696 vs model 21716). So the CA core does NOT lose data; retrying an async insert is non-idempotent (the async dedup token commits before the part publish fails, so the retry is deduped-away and the rows never land). FIX: soak runs SYNC inserts by default (retry then truly re-inserts). The async-insert+dedup-token-vs-part atomicity is split to [[B139]] (lower priority). The sync run's remaining stop was a DIFFERENT, non-loss issue (`fsck unreachable=61 at fixpoint`, dangling=0) — a GC-fixpoint-vs-grace timing matter in the checker, fixed harness-side (drive GC to unreachable==0 with a grace-aware bound). | Soak Phase-1 A/B | DONE (conclusion 1). |
| B141 | **RESOLVED 2026-06-14 (found by soak Phase-1, fixed + verified GREEN) — fsck HEAD-confirms `dangling`; LIST-lag false-positive eliminated.** `Cas::runFsck` built the REACHABLE key set from the authoritative ref-walk (listNamespaces→listRefs→readTree→locate) and the PRESENT set from a raw `backend.list` of `blobs/`/`trees/`/`packs/`, then classified any reachable key absent from the LIST set as `dangling` (an INV-NO-LOSS violation → nonzero fsck exit). But an S3/object-store LIST can LAG for recently-written objects (eventual consistency / mid-churn), so a reachable, ACTUALLY-PRESENT object missing from a lagging LIST was FALSELY flagged dangling. Confirmed false-positive in the soak: counts matched both replicas, re-fsck showed `dangling=0`, the object was a superseded short-lived intermediate, no server `FILE_DOESNT_EXIST`. FIX: a reachable key not found in the LIST(present) set is now `backend.head`-confirmed — `head` is AUTHORITATIVE for presence, LIST is advisory. If HEAD finds it → reclassify `Reachable` (and add its HEAD size to physical accounting, since the LIST scan missed it); only a HEAD-ALSO-absent reachable object is truly `Dangling`. We HEAD only the suspected-dangling set (reachable∖LIST, ~0 in the healthy case), so it stays cheap. `unreachable` stays LIST-based (M-F debris metric, not safety-critical — not HEAD-confirmed). Regression gtests: `CasFsck.ListLagDoesNotFalseFlagPresentObjectAsDangling` (present-on-HEAD + absent-from-LIST → reachable, not dangling) and `CasFsck.HeadAbsentReachableIsStillDangling` (HEAD-absent reachable still dangling — fix does not mask real loss), via a `ListLaggingBackend` wrapper that omits a key from `list` while `head`/`get` still see it. Verified by soak Phase-1 GREEN (seed=20260613, 1500 ops): 5/5 checkpoints `dangling=0`. | Soak Phase-1 finding (the harness working as intended) | DONE — `src/Disks/DiskObjectStorage/MetadataStorages/ContentAddressed/Core/CasFsck.cpp` (HEAD-confirm branch) + `src/Disks/tests/gtest_cas_fsck.cpp`. |
| B142 | **RESOLVED 2026-06-14 (HARNESS robustness gap — NOT a CA bug; found by soak Phase-2 T11 chaos integration). A graceful `docker restart`/shutdown cancels an in-flight query server-side (`QUERY_WAS_CANCELLED`, code 394) instead of dropping the socket; the workload's node-down retry only caught connection-level errors, so the cancelled INSERT killed the run.** First Phase-2 run (seed=20260613, chaos_seed=20260613, mean_interval=90s): two checkpoints passed CLEAN (op-400 count=16439 / op-800 count=18315, both replicas == model, `dangling=0`), then fault #1 (`both restart dur=56s`) fired and the in-flight ops correctly rerouted/retried on `ConnectionResetError`/`Broken pipe`, BUT INSERT op-898 on node2 came back as HTTP 500 `Code: 394 QUERY_WAS_CANCELLED` — a SERVER-SIDE exception body, not a dropped connection. The harness classified that as a `QueryError` (a real workload error), not a transport failure, so `retry_on_transport` did not catch it and the driver exited `WORKLOAD FAILURE`. **DIAGNOSIS: harness-side, not CA.** No checkpoint failed, no divergence, `dangling=0` throughout — the CA core was fine. A graceful shutdown is the node-down twin of a dropped connection: the in-flight query is cancelled (394) or hits a mid-query network error (`NETWORK_ERROR` 210 / `SOCKET_TIMEOUT` 209) instead of the TCP connection simply going away. **FIX (harness):** added `QueryError.is_node_down` + `is_node_down(exc)` (`soak/cluster.py`) recognising `NODE_DOWN_CODES={394,209,210,735}` and the connection-level `is_transport_error`; `retry_on_transport` now retries/reroutes on EITHER. Kept the B137 ABORTED (236) on its own `retry_on_aborted` path (excluded from node-down so it is not double-handled) and logic errors (e.g. `UNKNOWN_TABLE`) propagate immediately. Idempotency holds: the rerouted retry of a sync INSERT is deduped by RMT block-dedup if the original committed (same B138 sync-insert guarantee), else it re-inserts. Unit tests: `tests/test_transport_retry.py` (394/210 classified node-down; ABORTED + logic errors excluded; cancelled-then-recovers retry). | Soak Phase-2 T11 finding (`utils/ca-soak/logs/phase2_*_server.log`, `failure.json` op 898; reproducer: `utils/ca-soak/scripts/run_phase2.sh` seed=20260613 chaos_seed=20260613) | DONE (harness) — `utils/ca-soak/soak/cluster.py` (`is_node_down`/`NODE_DOWN_CODES`/`QueryError.is_node_down`), `utils/ca-soak/soak/run.py` (resilient insert/optimize/barrier reroute), `utils/ca-soak/tests/test_transport_retry.py`. |
| B143 | **RESOLVED 2026-06-14 (HARNESS-CONFIG gap surfaced by soak Phase-2 T11 restart chaos — NOT CA data loss; the pool was clean, `dangling=0`). On a server RESTART, the table fails to ATTACH with `UNKNOWN_DISK` (code 479) because the harness defines a second, read-only CA disk (`ca_ro`) aliasing the SAME pool, and the orphaned-parts scan treats the policy parts under `ca_ro`'s same-pool path as orphans.** Second Phase-2 run (after the B142 fix; seed=20260613, chaos_seed=20260613): checkpoints op-400 (count=16439) and op-800 (count=18315) passed CLEAN (both replicas == model, `dangling=0`); fault #1 (`both restart dur=56s`) fired and the B142 node-down reroute correctly carried the workload through (45 node-down retries incl. 394 `QUERY_WAS_CANCELLED`); but when ch1 came back up its async table-load failed: `Code: 722 ASYNC_LOAD_WAIT_FAILED` → `695/696 load table` → `Code: 479 UNKNOWN_DISK: Part '20260614_724_724_0' (store/.../...) was found on disk 'ca_ro' which is not defined in the storage policy 'ca' or broken (defined disks: [ca], skipped disks: [])`. The driver surfaced this as `WORKLOAD FAILURE` at op-893; `failure.json` shows `fsck dangling=0 reachable=10376 unreachable=52 exit=0` — **the CA pool is intact; this is a table-ATTACH config issue, not loss**. **ROOT CAUSE (code-confirmed):** `MergeTreeData::loadDataParts` runs an orphaned-parts safety scan on ATTACH/CREATE (`MergeTreeData.cpp:2302-2355`) that iterates EVERY disk in `getContext()->getDisksMap()` — not just the storage policy's — and for any non-policy disk eligible under `search_orphaned_parts_disks` (default `ANY`, `MergeTreeSettings.cpp:2177`; `isDiskEligibleForOrphanedPartsSearch` at `:2269`) that `existsDirectory(relative_data_path)` and holds a parseable part name, it throws `UNKNOWN_DISK` (`:2347-2352`). The harness's `ca_ro` (read-only S3 view over the SAME RustFS endpoint as `ca`, present in the server config because the offline `clickhouse disks fsck` applet reads that same config — `soak/fsck.py` `run_fsck(disk='ca_ro')`) is remote, not-broken, not-custom → eligible under `ANY` → its same-pool path holds the very parts policy disk `ca` owns → throws. **Phase-1 never restarted a server (table loaded once at fresh CREATE, no part-discovery scan), so it never surfaced.** **FIX (harness, minimal + semantically correct):** the table DDL now sets `search_orphaned_parts_disks='local'` (`soak/run.py` `DDL_TEMPLATE`), so the orphan scan is limited to LOCAL disks and skips the remote `ca_ro` alias (a false positive — `ca_ro` is the same pool as the policy disk `ca`, not orphaned storage), while still catching genuinely-orphaned local parts. A cleaner-but-larger alternative (deferred) is to stop defining `ca_ro` as a SERVER disk altogether and give the fsck applet its OWN config file (config/compose restructuring). **CA-relevance note (worth a look, not blocking):** a read-only CA disk that is an alias of a writable policy disk's SAME pool is indistinguishable from an orphan-bearing foreign disk to this generic MergeTree scan; if CA read-only aliases become a supported production pattern, the scan (or the CA disk) may want to recognise a same-pool read-only alias and skip it rather than rely on the table setting. | Soak Phase-2 T11 finding (`utils/ca-soak/failure.json` op 893, last_fault=both/restart/56s; reproducer: `utils/ca-soak/scripts/run_phase2.sh` seed=20260613 chaos_seed=20260613 — fails on ch1 restart after fault #1 WITHOUT the DDL setting) | DONE (harness) — `utils/ca-soak/soak/run.py` `DDL_TEMPLATE` (`search_orphaned_parts_disks='local'`). Follow-up (optional): split `ca_ro` into a fsck-applet-only config; consider CA-side same-pool-alias recognition in the orphaned-parts scan. |
| B150 | **EXPLAINED (source-verified 2026-06-14) — "why so many read failures?" The 14.9M `S3ReadRequestsErrors` (66% read-error rate) are overwhelmingly EXPECTED `404`/`NoSuchKey` responses to existence-checking HEADs, NOT genuine I/O failures.** ClickHouse's S3 metric code counts ANY non-2xx, non-429/503 response — including `404` — as an error: `PocoHTTPClient.cpp:787-789` does `else if (status_code >= 300) addMetric(request, S3MetricType::Errors)`. The `Expect404ResponseScope` (`getObjectInfo.cpp:143/168/198`, used by `objectExists`/`getObjectInfo`) only suppresses the **error log line** (`PocoHTTPClient.cpp:720`, `AWSLogger.cpp:79`, `Client.cpp:857`) — it does NOT gate the metric increment, so "expected" 404s still bump `S3ReadRequestsErrors`. Numeric confirmation from the soak (ch2): `S3ReadRequestsErrors`=14.9M vs `S3HeadObject`=20.3M ≈ **73% of HEADs returned 404** — exactly the CA existence-check miss pattern (`resolveRef`→`readShardDecoded` HEADing not-yet/​no-longer-present shards, `Gc::retire` HEADing condemned candidates, head-after-put). Proof these are benign: they are NOT surfaced as exceptions — `system.errors S3_ERROR`=1.21M which matches `WriteBufferFromS3RequestsErrors`=1.21M **exactly** (the read 404s are absorbed by the existence-check callers as "not exists"; the real exception set is write-side). **Genuine** failures are a much smaller, separate bucket: `S3WriteRequestsThrottling`=19,284 (real 429/503 overload from single-disk RustFS), the 1.21M write-buffer errors (conditional-PUT `412` + `500`s under load), `NETWORK_ERROR`=370, `RECEIVED_ERROR_FROM_REMOTE_IO_SERVER`=111. CONCLUSION: the "read failure" count is an accounting artifact of the HEAD-heavy existence-checking pattern, not a correctness or transport problem — and it reinforces [[B148]]: killing the per-access HEAD (Pillar A resident-snap retire + Pillar B decode-cache no-HEAD-on-hit) removes the bulk of BOTH the 20.3M HEADs and the 14.9M "errors" in one move. | Soak read-failure diagnosis | Covered by [[B149]] op-count reduction; no separate fix. Optional: a CA-level ProfileEvent distinguishing expected-404 existence-misses from real read errors would make future soak triage unambiguous. |
| B153 | **REGRESSION FOUND + FIXED (2026-06-14) — Pillar B bounded-TTL decode cache broke read-your-writes; fix = invalidate the shard decode cache on write.** The Pillar B opt-in TTL decode cache (`Store::readShardDecoded(..., allow_stale=true)`, default 200ms) was opted into by existence/read callers (`existsDirectory`/`existsFile`/`getStorageObjects`/`getLastModified` via `resolveRouted`), but `Store::mutateShard` (the single chokepoint for ALL shard-manifest writes — `dropRef`/`updateRefPayload`/`dropNamespace`/`Build::publish` at `CasBuild.cpp:650` all route through it) did NOT invalidate the `shard_decode_cache` entry on a committed `casPut`. So a same-`Store` write followed by an `allow_stale` read within the TTL served the STALE pre-write decode → a just-dropped ref still appeared present, or a just-written ref appeared absent. **FIX (commit `cf71b1f059c`):** in `mutateShard`, after the committed `casPut`, `shard_decode_cache.erase(key)` (cache mutex scoped, not held across the network op). A LOCAL write is now reflected immediately; cross-node staleness stays bounded by the TTL (acceptable per the spec's caller audit). **Impact correction:** this regressed **7 unit tests** — 5 `CaWiring*` (`CommittedPartRenameMovesTheRef`, `DetachAttachRoundTrip`, `RemovalsDropRefsAndNamespaces`, `DroppedPartIsReclaimedByRounds`, `AdoptPartPublishesOwnRef`) PLUS 2 I had MISLABELED as pre-existing baseline failures (`CasProtocol.DropReattachThroughDetachedNamespace`, `CasTruncateReclaim.PerRefDropOfSharedBlobsReclaimsToZero`). All 7 now pass. **The ONLY genuine pre-existing red test is `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak`** (the B140 M-F-deferred guard). **Process lesson:** the op-count tasks' gate used `--gtest_filter=CaWiring.*` (matches only the `CaWiring` suite, NOT `CaWiringOps`/`CaWiringGc`/`CaWiringExchange`), so the regression was invisible during Pillar B execution; future CA test gates must use `CaWiring*` (glob) + the full `Cas*` suite. | Surfaced during B151-T1 (full `CaWiring*` run) | DONE — `mutateShard` invalidates the decode cache on write (`cf71b1f059c`). Re-validate under soak (B151-T4). | 
| B151-VALIDATED | **B151 lock-scope fix VALIDATED by soak (2026-06-15).** Two 12h soak runs on the B151-fixed binary (seed 20260614, 6 workers, 25 GB). Across EVERY pulse and EVERY reached stage — warmup, steady, mutations, ttl_pressure (delete-heavy), gc_checkpoint, and early-chaos (with REAL docker kill/restart/pause faults causing genuine ZK-session loss + recovery) — `system.parts` stayed responsive (**0% slow polls, max 0.01–0.08s**) and `system.trace_log` showed **0 `WriteBufferFromS3::finalizeImpl` under `MergeTreeData::Transaction::commit(DataPartsLock&)` on both nodes**. This is decisive vs the B151/T9 BASELINE (same config, pre-fix): ~12% slow polls, **max 220s** `system.parts`, 427% CPU saturation, with the under-lock manifest publish as the dominant exclusive-lock holder. The fix DECOUPLES responsiveness from the op-count/HEAD pressure: even under a 28M-HEAD GC storm at 90%+ CPU (run #1 pulse #2), readers stayed at 0.02s. No correctness regression (dangling=0 in every recovery checkpoint; no loss). NOTE: neither run reached the full 12h — run #1 wedged on [[B154]] (fsck), run #2 PHASE3-FAILED on [[B155]] (SYNC-REPLICA readonly) — but BOTH are soak-HARNESS chaos-robustness gaps, NOT B151/server issues; B151's responsiveness goal is met across the hard stages it did reach. | B151 soak validation (runs #1+#2) | B151 RESOLVED/validated. Optional: a clean full-12h run after the B154+B155 harness fixes (in progress). | 
| B167 | **IN PROGRESS (2026-06-16): spec + TLA+ DONE (writer-side fix; C rejected); C++ impl pending. Ships WITH B160.** Spec `2026-06-16-ca-resurrect-reupload-design.md`: `resurrect`/gate-revalidation always RE-UPLOADS a fresh incarnation from the caller's recreatable bytes (never GET-from-existing) → fresh incarnation is fold-invisible until referenced → GC-safe by construction; thread the recreatable source through `deps`; NO GC change (C/back-link rejected per user). TLA+ `CaResurrectLiveness.tla` (commit `b7319bb2c05`) TLC-green: the LIVENESS dimension the safety-only model omitted — `<>published` HOLDS with the recreatable fix, VIOLATED under SabotageBodyless (lasso counterexample = the B167 livelock matching soak #11's recreate/condemn/delete churn). REMAINING: C++ (DepEntry carries recreatable source; resurrect/gate re-upload; remove GET-from-existing for recreatable deps) + unit tests + soak (B160+B167 → 0 broken parts, fsck dangling=0). ORIGINAL (corrected) writeup below. — CA LIVENESS/robustness gap (NOT a safety/data-loss bug) found by the B160 soak validation (soak #11): the bodyless publish-gate `Build::resurrect`-vs-GC-delete race makes merges fail to CONVERGE under productive GC → broken detached parts. B156-family; UNMASKED by B160, NOT caused by it.** WHY IT'S NOT A SAFETY BUG (and why TLA+ didn't "catch" it): `CaIncarnationCore` proves SAFETY (INV-NO-LOSS/NO-DANGLE/NO-RETURN) and that HELD — soak #11 had NO data loss (active table intact: 7 parts / 809,779 rows; the broken part is a FAILED merge whose source data stays in the active parts; the 2 "missing" blobs are referenced ONLY by the quarantined detached-broken part). The model's writer retries the resurrect-vanish UNBOUNDED-until-converged (the "bytes in hand → re-PUT" arm) and proves THAT safe; but the real PUBLISH-GATE dep-revalidation is BODYLESS (no bytes to re-PUT) → propagates ABORTED, and the real retry is BOUNDED → under productive GC + RustFS-412 churn the GC keeps winning the race so the merge never converges. A safety model doesn't check "writer eventually publishes under an adversarial GC" — that's LIVENESS. The incremental GC (M-C) does NOT honor the build heartbeat (per `CasHeartbeat.h`, heartbeats gate only the UNIMPLEMENTED full GC M-F; the publish GATE is the safety mechanism); a never-referenced blob is safe (not everEdged), but a dedup-reuse of an ALREADY-CONDEMNED blob (referenced→dropped→condemned→re-reused) is the gap. Also a SECONDARY fsck false-positive: fsck counts `detached/broken_*` refs as reachable-must-exist → inflates a quarantined failed-merge into an INV-NO-LOSS alarm. Mechanism (ch2 server log): a merge dedup-hits an existing blob (conditional PUT → 412 PreconditionFailed = "exists"), but that blob is a CONDEMNED GC candidate; `Build::resurrect` tries to re-reference it (HEAD→GET), and the now-productive GC **deletes it between the HEAD and the GET** → `Code 236 ABORTED: "Build::resurrect: condemned incarnation of <blob> deleted by GC between HEAD and GET; retry"`. The retry re-races (GC keeps deleting, amplified by the RustFS-412 dedup churn [[B158]]/[[B166]]) → the merge ultimately produces a BROKEN part referencing GC-deleted blobs → detached as `broken_<part>`. fsck then counts the detached part's refs as reachable → `dangling=2 … INV-NO-LOSS` (stable on the quiescent pool). Evidence: soak #11 ch2 part `20260616_2881_3917_13`, blobs `2b/2bfab…`,`d1/d144…`; **29 broken detached parts**; 7 `resurrect…deleted by GC` aborts (ch1:1, ch2:6). **Why B160 unmasked it:** pre-B160 the GC livelocked (~0 productive rounds, [[B160]]), so it barely deleted → the resurrect-vs-delete race almost never fired; with B160 the single leader runs 225 productive rounds and actively deletes condemned blobs → the race fires hard. **NOT live-data loss:** the active table was intact (7 parts / 809,779 rows); the broken part was a FAILED merge (never active — its source data stays in the active parts); the INV-NO-LOSS is on quarantined detached-broken refs. **TWO things to fix:** (1) the resurrect path must WIN against GC for a body-recreatable blob — e.g. on the ABORTED race, fall back to writing the blob FRESH (the merge has the bytes) instead of re-resurrecting the condemned incarnation; OR GC must defer deleting a blob a writer is actively resurrecting (a writer-pin / resurrect grace, like the B156 fix for `reuseBlob`); needs design + TLA+ (B137/B156 precedent). (2) fsck should NOT count `detached/broken_*` parts' refs as reachable-must-exist (they are quarantined, not live) — otherwise every broken part inflates into an INV-NO-LOSS alarm. **SEQUENCING:** B160 is correct in isolation (TLA+ + unit tests + 0 contention), but enabling productive GC without B167 creates broken parts under dedup+GC load — so B160 should ship WITH B167 (or B167 first). | Soak #11 B160 validation 2026-06-16 | Design + implement the resurrect-wins-or-GC-grace fix (B156-family) + fsck detached-broken exclusion; model-check. Blocks B160 merge. |
| B166-CORRECTED | **CORRECTION (2026-06-16, soak #9 live data): the 412 is NOT retried and is NOT the stall cause — I was wrong in the first B166 writeup below.** DECISIVE evidence (soak #9 ch1, ~5 min, chaos-off): **186,649× 412 `PreconditionFailed` but 0 retried**, vs **680 connection failures (`HTTP response code: -1`) and EXACTLY 680 SDK retries** (`before attempting again`) — retries are 1:1 with connection failures, never with 412s. Verified in the SDK: `PreconditionFailed` is NOT in the AWS C++ error-name map (only a doc comment in `aws-cpp-sdk-s3/.../Error.h`), so `AWSErrorMarshaller::Marshall` hits line 247 → `UNKNOWN`, **`isRetryable=false`**, name preserved; and `IsRetryableHttpResponseCode(412)`=false. So a 412 is NEVER retried — the CA code (`finalizeConditionalWrite`/`WriteBufferFromS3`) matches it by NAME and handles it. **The 412 is benign: an SDK name-mapping GAP (the user's "old aws client" guess) → log noise only, NOT a RustFS bug.** The REAL `SYNC REPLICA`-stall cause = **connection-level failures (-1 / NETWORK_CONNECTION) from single-disk RustFS overwhelmed by the CA op volume**, which ARE correctly retried (backoff to 5000 ms) → upload stall → queue stall → timeout. The 412 storm is a SYMPTOM of that volume (heavy CAS/dedup conditional PUTs), not the mechanism. **The committed fix `b566f00ef4f` (412→non-retryable in `ShouldRetry`) is therefore INERT defensive hardening (correct, but 412 was already non-retryable) — kept, not the stall fix.** REAL mitigations: cut S3 op/conditional-PUT volume ([[B164]] manifest binarization, [[B148]] op-count, [[B160]] GC contention) + the single-disk RustFS test-rig limit; OPTIONAL cleanup: suppress the 186k expected-412 WARN log lines (an "Expect412" scope mirroring `Expect404ResponseScope`) to stop log bloat. **OPEN QUESTION (do we have an old SDK?): the vendored AWS C++ SDK is `1.11.771` (fork `ClickHouse/aws-sdk-cpp`, pinned 2026-03-24); `S3Errors.h/.cpp` has NO `PreconditionFailed` mapping (only a doc comment in `model/Error.h`). Investigate whether a newer SDK maps the `PreconditionFailed` S3 error name to a typed value — if so, an SDK bump removes the "Unable to parse ExceptionName" log noise without a ClickHouse-side Expect412 scope. CAVEAT: even upstream S3Errors may leave conditional-write errors UNKNOWN-mapped (S3's typed error enum is narrow), so the bump may not help and the name-match handling stays the authoritative path regardless. Low priority (benign noise).** | Soak #9 corrected diagnosis 2026-06-16 | 412 is benign (SDK-gap log noise); stall = connection-failure retries under RustFS saturation → load reduction (B164/B148/B160). Optional: Expect412 log-suppression scope; investigate AWS SDK 1.11.771 vs a newer version for PreconditionFailed name mapping. |
| B166-original-WRONG | **(SUPERSEDED by B166-CORRECTED above — left for the record; this writeup incorrectly blamed 412 retries.)** the AWS SDK RETRIES RustFS's 412 `PreconditionFailed` (it can't parse the error body), amplifying a normal CAS/dedup precondition into a part-upload stall that hangs `SYSTEM SYNC REPLICA`.** Sharpens [[B158]] with the mechanism. Evidence (host-mounted ch1 log, B165): in 08:25–08:29 alone, **46,910× `Message: Unable to parse ExceptionName: PreconditionFailed`** (HTTP 412) + some `HTTP response code: -1` (connection drops), each followed by `AWSClient: Request failed, now waiting {25..5000} ms before attempting again` on the SAME part (escalating backoff). Merge/mutate parts (`20260616_0_1306_9_*`, `…_3452_4381_8`) upload content via conditional PUT (`If-None-Match` dedup, `If-Match` CAS); RustFS returns 412 with a body the AWS SDK can't parse as a terminal `PreconditionFailed`, so the SDK **generic-retries** it instead of returning it to the CA caller (which would treat 412 as dedup-hit/CAS-conflict and handle it WITHOUT retry, as on real S3). The trapped uploads stall the 9 in-flight replication-queue entries → `SYSTEM SYNC REPLICA ca_stress` (issued 08:26:05) waited the full **673 s** → `Code 159 TIMEOUT_EXCEEDED` at 08:37:19 → soak `WORKLOAD FAILURE`. NOT chaos (ch2 never paused — no log gap), NOT readonly (ch1 kept committing to Keeper throughout), NOT a B164 regression (codec is on a different path). **FIX:** classify 412 as TERMINAL/non-retryable on the CA conditional-PUT path — an "expected-412" scope mirroring the existing `Expect404ResponseScope` (`getObjectInfo.cpp`) — so `casPut`/dedup gets the 412 immediately and the SDK never retries it. Secondary: RustFS's 412 error body is non-S3-conformant (the SDK can't extract `ExceptionName`); a conformant body would also stop the misclassification. Validate via a re-run after the fix (SYNC REPLICA should drain promptly). Related: [[B158]], [[B150]] (expected-404 metric artifact), [[B148]]. | Soak #8 SYNC-REPLICA-timeout diagnosis | Classify 412 terminal on the CA conditional-PUT path (Expect412 scope); re-run to confirm SYNC REPLICA drains. |
| B162 | **FIXED (2026-06-16, `fac220cf3c9`) — soak-driver OOM (run #6 ended ~3h, `rc=137`); harness bug, NOT a CA fault.** Kernel oom-killer (conclusive): `Out of memory: Killed process 935198 (python3) total-vm:13618192kB anon-rss:12864068kB ... task=python3` @ 03:25:06. The python `soak.run` driver grew to **~12.86 GiB** in 3h (swap 7G/7G also exhausted; Zoom co-resident; disk NOT full, 375G free). The CA cluster was healthy throughout (0 no-ref/0 broken, both nodes 0.01s responsive, replLag ≤6s). **Root cause:** `Model.rows` (the authoritative per-rid oracle, `soak/model.py`) gained ~100 entries per INSERT op and **never evicted TTL-expired rows**; the table sheds them via `TTL … DELETE` (active parts fell 311→172→80) but the dict only grew. With `ts = base_time + (op_id % TS_WINDOW)` anchored to a fixed ~2h window at start, after the TTL horizon ~every inserted row is permanently expired yet was retained → monotonic growth (~12.8 GiB at 3h, ~43 GiB at the full stream). **Fix:** `Model.prune_expired(now)` evicts rows `_expired`/`live_rows` already exclude (safe: `now` monotonic, `ts` fixed → once-expired-always-expired → can't change any aggregate at this/any later `now`); called from `checkpoint` after the aggregate assert (workload drained, `now` quiesced — no worker race). Also stopped storing the unused 256 B `payload` in the model row (only the INSERT SQL emitter needs it, recomputed independently from `row_for_rid`) — it dominated the per-row footprint. Tests: `test_prune_expired_removes_only_expired`, `test_prune_expired_does_not_change_live_view`, `test_model_row_carries_no_payload`; full driver suite 142 pass. | Run #6 OOM diagnosis 2026-06-16 | DONE (`fac220cf3c9`). Relaunch the 12h soak on the fixed driver. |
| B160 | **IN PROGRESS (2026-06-16): spec + TLA+ DONE; C++ impl pending. Fix = clock-free lease + advisory heartbeat (Approach A).** Spec `2026-06-16-ca-gc-lease-heartbeat-design.md`. TLA+ `CaGcLeaseCore.tla` (commit `b27f183e89e`) TLC-green: NoEpochCollision (safety) holds WITH and WITHOUT the heartbeat (atomic single-CAS steal + fence_seq make every steal safe regardless of timing — the heartbeat does not weaken safety); NoFalseSteal holds WITH the heartbeat and is VIOLATED under SabotageNoHeartbeat (7-state counterexample reproduces the false-steal). C++ IMPL DONE (commit `5a56962a7cc`): `gc/hb` key + `GcHeartbeat` codec, `Gc::pulseHeartbeat` (static), heartbeat-gated steal in `acquireOrRenewLease` (`last_seen_hb_*`), heartbeat thread in `CasGcScheduler` (H=interval/4, `i_am_leader` flag). Tests: `GcHeartbeatRoundTrip`, `CasGcLease.HeartbeatBlocksFalseStealOfAliveLeader` + `FailoverStealOnceHeartbeatStops`; existing lease/steal tests green; CA suite 253/254 (1 red = pre-existing B140). REMAINING: soak validation (rebuild `clickhouse`, two-replica soak, confirm contention ~70-80% → ~0 and `gc/state.round` advances steadily). ORIGINAL FINDING below. — GC multi-mounter retire-contention LIVELOCK (real CA bug; the cause of the run #4 80%-404 HEAD storm).** Both replicas run the `CasGcScheduler` against the SHARED pool and race at `retire`: ~70–80% of GC rounds FAIL with `Code 236 ABORTED: "CAS gc retire: gc/state moved during retire (another leader advanced it); retry next round"` (`CasGc.cpp:709`). Measured run #5: ch1 300 failed / 84 ok / 250 retire-contention; ch2 294 / 141 / 245. The scheduler loop checks `report.acquired_lease` and backs off if not acquired (`CasGcScheduler.cpp:69-84`), but at the aggressive `gc_interval_sec=2` BOTH replicas acquire+fold+recheck+`retire` and one loses the `gc/state` CAS every round. **Consequence (instrumented + model-consistent):** the loser burns a full round (lease acquire, `loadSnap`, `fold`, `recheck` — each HEAD-ing candidates) then throws it away; `gc/state` never advances; the candidate set never shrinks; the GC re-scans the SAME + growing stale set every 2 s, HEAD-ing objects the OTHER replica already deleted → **the 80%-404 HEAD storm** (confirmed: run #5 fresh-pool 404-HEAD stacks are 100% `Gc::retire`; the storm grows with pool age as the un-retired backlog grows). Reclamation barely progresses (deleted≈1046 over the run). **NOTE:** this is the *regular incremental* GC (M-C); the lease is a liveness optimization, NOT the correctness mechanism (the publish gate is — per `CaHeartbeat.h`), so the livelock is wasteful + load-amplifying, not a loss/dangle. **FIX DIRECTIONS (needs design + TLA+ check, like B136/B137):** (a) a real single-leader gate so a non-leader backs off BEFORE the expensive fold/recheck (cheap lease-peek first), not after a failed `retire`; and/or (b) lengthen/jitter the lease + `gc_interval` so rounds don't overlap (the implementation's lease observation-window vs the 2 s cadence is mistuned for 2 mounters); and/or (c) only ONE replica runs the GC scheduler for a shared pool (config: `content_addressed_gc_enabled` on one node), with lease failover. Evidence: `docs/superpowers/reports/2026-06-15-ca-soak-opcount-and-rustfs-findings.md` §A2. Related: [[B158]] (the storm's surface symptom), [[B148]]. | Run #5 GC-round-outcome measurement | DEFERRED (backlog only per user 2026-06-15). Fix = single-leader gate / lease-cadence retune / one-scheduler-per-pool; design + model-check before implementing. |
| B157 | **CONFIRMED CORRECTNESS BUG in my own Pillar B work + FIXED (2026-06-15, soak run #4 ~1h): the `allow_stale` TTL decode-cache has a read-your-writes COHERENCE RACE that makes a freshly-adopted part transiently unresolvable → marked broken → replication GET_PART livelock. (Initial 2026-06-15 attribution to RustFS saturation was WRONG — corrected below. RustFS saturation is REAL but CONCURRENT/separate, not the cause of these specific broken parts.)** **Smoking gun (user zgrep of `20260615_17792_17792_0`):** `relink`-adopt CLEAN → merge read of `op_id.bin` → `getStorageObjects` line 645 throws `FILE_DOESNT_EXIST "ContentAddressed: no ref for …/op_id.bin"` → "Part looks broken" → detach (`broken_20260615_17792_*`, `broken_21212_*`). The `no ref for {path}` there fires when `resolveRouted` → `resolveRef(ns, ref, allow_stale=true)` returns nullopt — i.e. the **part ref itself** is missing from the decode (NOT a file-within-tree miss; `op_id.bin` is just the file being read at that instant). **Mechanism (CONFIRMED by code reading + a deterministic gtest repro, no soak needed):** `Store::loadShardDecoded` does HEAD→GET→decode→populate. An in-flight reader can HEAD/GET a shard whose bytes predate a concurrent `Build::publish`; the writer's `mutateShard` `casPut` commits the new ref and erases the decode-cache entry (B153), but the in-flight reader then POPULATES the TTL cache with its STALE (pre-publish) decode AFTER that erase. A subsequent `allow_stale` read (the merge's `getStorageObjects` → `resolveRouted`, `ContentAddressedMetadataStorage.cpp:314`) hits the poisoned entry within the TTL window and sees no ref for the just-published part → broken. The adopt's publish happens-before the merge read on the SAME thread, so the merge's own GET would be fresh — the poisoning comes from a CONCURRENT reader, which is why it only surfaced under heavy soak churn. **FIX (commit pending):** per-key monotonic `shard_write_seq` bumped in `mutateShard`'s invalidation; `loadShardDecoded` captures it before its GET and only populates the decode cache if it is unchanged at populate-time (a write that landed during the GET means the decode may be superseded and its invalidation already ran → skip caching, never poison). Keeps the Pillar B op-count win (steady-state: no concurrent write → seq stable → caches normally) AND closes the race. Test `CasStoreDecodeTtl.ConcurrentWriteDuringGetDoesNotPoisonStaleEntry` (GatedGetBackend: snapshot-at-entry + gated return; root_shards=1) reproduces RED→GREEN. **SUB-FINDING (real, still worth a fix, separate):** once a part IS broken, the REFETCH livelocks — `GET_PART 21212` postponed **353,984×** (`postpone_reason: "…we still have broken part with that name. Waiting for the broken part to be removed first"`) → replication stuck (~60min lag, NO_REPLICA storm ch1 171k / ch2 624k). A healthier recovery path should bound/break that spin. **The 412/503/broken-pipe "storm" that I first (wrongly) lumped in here as "RustFS saturation" is a SEPARATE env-side issue — MOVED OUT to [[B158]]** (RustFS per-object 64-permit cap × root-shard fanout; NOT this bug, NOT disk load, NOT a correctness risk). NOT confirmed data loss (dangling=0; the other replica holds a good copy). | B151 soak run #4 live diagnosis (user-driven `system.replication_queue` + `system.detached_parts` + `system.text_log` + zgrep) | DONE (correctness): coherent decode cache (per-key write-seq) — see B157-impl. REMAINING (separate from the fixed coherence race): (a) CA recovery-robustness — bound the broken-part-refetch livelock (the `GET_PART` 353,984× postpone spin); (b) the env-side 412/503/broken-pipe storm is its own item → [[B158]]. |
| B157-impl | **IMPLEMENTED + TDD (2026-06-15) — B157 coherence-race fix shipped. Commit `e1f69b696e6`.** Per-key monotonic `shard_write_seq` in `Store` (guarded by `shard_decode_cache_mutex`): bumped in `mutateShard`'s invalidation block (alongside the existing B153 `shard_decode_cache.erase(key)`); `loadShardDecoded` captures it just before `pool_backend->get(key)` and only inserts into `shard_decode_cache` if the counter is unchanged at populate time (else the just-decoded bytes may be superseded by a write whose invalidation erase already ran → skip caching, never poison; the reader still returns its own point-in-time decode). Steady-state behaviour is unchanged (no concurrent write → seq stable → caches as before), so the Pillar B op-count win is preserved — verified by the still-green `CasStoreDecodeTtl.WarmHitWithinTtlSkipsHead`. Counter kept monotonic across the wholesale cache clear (never reset) so a captured value can't spuriously match. Test `CasStoreDecodeTtl.ConcurrentWriteDuringGetDoesNotPoisonStaleEntry` + `GatedGetBackend` (snapshot-at-entry + gated return; `root_shards=1` so `part_1`/`part_2` collide on shard 0) was RED before the fix (`resolveRef(part_2, allow_stale)` → false) and GREEN after. Full `Cas*`/`CaWiring*` (246 tests) green except the pre-existing baseline `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` ([[B140]] guard). **NOT yet re-validated under soak** (run #4 still up demonstrating the bug; a fresh run should show no `no ref`-driven broken parts). The B157 recovery-robustness sub-finding (broken-part-refetch livelock) and the RustFS-saturation env issue remain open + separate. | B157 fix (TDD) | DONE (code+unit); soak re-validation pending. |
| B156-FIXED | **B156 FIXED + adversarially-reviewed (Approved) (2026-06-15).** `Build::reuseBlob` now takes `body_recreatable` and converts `observeAndAdmit`'s HEAD-absent `FILE_DOESNT_EXIST` → retryable `ABORTED` ONLY when the reused blob is body-recreatable (it was `putBlob`'d by a build in this txn → tokened dep → an INSERT retry re-writes the content → converges). For ADOPTED-from-committed (tokenless W-EVIDENCE) reuses, it stays **fail-loud `FILE_DOESNT_EXIST`** — a HEAD-absent referenced blob there is a genuine INV-NO-LOSS signal that MUST surface, not be masked. Discriminator = new `Build::depIsTokened(hash)` (`putBlob`→tokened, `adoptFromTree`→tokenless `nullopt`, no-dep→false=fail-loud safe default). Per-site `body_recreatable`: createHardLink (`src_st->build->depIsTokened`), moveDirectory-merge (`dst||src build`), moveFile (`src_st.build`). Commits: `cf454abd826` (initial blanket fix — adversarial review caught it MASKED the adopted-blob loss + didn't converge) → `94ff4f7c9cb` (the discriminator fix). Tests: `VanishedRecreatableBlobIsRetryableAborted` (putBlob'd→ABORTED), `VanishedAdoptedBlobStaysFailLoud` (adopt→FILE_DOESNT_EXIST, proves no masking), `DepIsTokenedDiscriminatesPutBlobVsAdopt`; `observeAndAdmit`/`putBlob`/`resurrect`/`adoptTree` byte-unchanged; CA suite green except baseline `CasGcLeak`. **Minors (non-blocking):** `hasDep` helper is test-only (no prod caller); **residual follow-up** — the deeper root is GC condemning an in-flight build's blob after a heartbeat LAPSE under load (the B156-fixed retry recovers it, but the heartbeat-renewal-cadence-vs-GC-condemnation-grace under heavy load is worth a separate look). | B156 fix + 2-round adversarial review | DONE; re-run the 12h soak with the fix (run #4). | 
| B156 | **REAL CA dedup-vs-GC robustness bug (2026-06-15, soak run #3 ~1.25h, steady stage) — `reuseBlob`/`observeAndAdmit` HEAD-absent branch is NOT retry-safe against the dedup-reuse-vs-GC vanish race; surfaces as a fatal `FILE_DOESNT_EXIST` INSERT failure. NOT data loss, NOT introduced by this session, NOT B151.** Error: `Code 107 DB::Exception: Build: object soak_pool/blobs/ab/ab065611... absent — cannot reuse (caller must upload it). (FILE_DOESNT_EXIST)` on an INSERT (op_id=31655) → `WORKLOAD FAILURE` → `PHASE3 FAILED`. Mechanism: `Build::observeAndAdmit` (`CasBuild.cpp:193-196`, shared by `reuseBlob`) HEADs the dep blob; **HEAD-absent ⇒ hard `FILE_DOESNT_EXIST`**. The code comment assumes HEAD-absent = "genuinely-never-existed" and is explicitly **"untouched"** by the B136/B137 fixes — but the soak shows a blob the writer DEDUP-REUSED (existed at decision time) can be GC-deleted (exact-token `deleteExact`) BEFORE the reuse-HEAD, a benign race. The sibling condemned-token→`resurrect`→GET-vanish branch correctly surfaces ABORTED (retryable; `putBlob`'s body-holding retry re-uploads), but the HEAD-absent branch does NOT — and `reuseBlob` callers (`ContentAddressedTransaction.cpp:595/737/822` — projection hardlink, merge re-key, adopt) hold no body, so they can't re-upload → the INSERT fails hard. **Not loss** (the insert never committed; no dangling ref created). **Pre-existing latent** (observeAndAdmit/reuseBlob unchanged by B151/B153; surfaced only once the soak sustained heavy insert+dedup+GC churn, which runs #1/#2 didn't reach before their harness wedges). Family: [[B136]]/[[B137]] (resurrect race, fixed) + B139 (async+dedup). **FIX DIRECTION (needs B136/B137-level care + adversarial review):** make the dedup-reuse HEAD-absent case retry-safe — either (a) surface it as retryable `ABORTED` ("dep deleted by GC between dedup-decision and reuse; retry") so the writer retries and, on retry, the now-absent blob is re-uploaded via `putBlob` from source instead of reused; or (b) have the reuse callers retain/re-derive the body and fall back to `putBlob` on vanish. Must NOT mask a genuinely-never-existed dep (a real corruption/loss signal) as retryable — distinguish "was dedup-reused (we expected it)" from "never existed". | Soak run #3 diagnosis | Fix the reuseBlob/observeAndAdmit HEAD-absent dedup-vs-GC race to be retry-safe (re-upload fallback), with adversarial review (INV-NO-LOSS: don't mask a real never-existed dep); then re-run the soak. Decision pending (user). |
| B151-impl | **IMPLEMENTED + adversarially-reviewed (2026-06-14) — B151 lock-scope fix shipped (Mechanism B, zero `src/Storages`).** Per spec `2026-06-14-ca-manifest-commit-lock-scope-design.md` + plan `2026-06-14-ca-manifest-commit-lock-scope.md`. Commits: `864d10c11ab` (extract `publishStaging` + `PartStaging.published`), `cf71b1f059c` (B153 read-your-writes cache invalidation), `6f5e38667104` (publish final ref at the lock-free `moveDirectory`/rename; `commit()` skips published), `a9dba473b36` (rollback safety: drop rename-published refs on abandon). The CA manifest publish now happens at the lock-free tmp→final rename for all replicated INSERT/merge/mutation paths; under-lock `commit` does no S3 write. Adversarial review (2 rounds) found + got fixed a **Critical** rollback gap (a part published at the rename before the ZK multi would orphan its ref on ZK-failure rollback → resurrection/unexpected-part risk; fixed by the path-agnostic drop-on-abandon in `~ContentAddressedTransaction` when `!committed`). Final review verdict: **Approved**. Tests: new `CaTransactionLockScope.*` (4 tests: publish-at-rename, no-spurious-publish-on-committed-move, drop-on-abandon, survive-on-commit) + all 27 `CaWiring*` green; only `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` red (genuine B140 guard). **Remaining (minor, non-blocking):** optional `chassert(!dst_st.published)` before the staged-source merge in `moveDirectory` (defense-in-depth for a theoretical merge-into-already-published-dst; no reaching sequence found). **Pending:** soak re-validation (B151-T4) — confirm `system.parts` responsive + no `WriteBufferFromS3` under `DataPartsLock` in trace_log. | B151 implementation | DONE (code); soak re-validation pending. |
| B151 | **ROOT CAUSE of CA `system.parts` unresponsiveness — CONFIRMED by trace + code (2026-06-14 T9 re-validation soak). The unresponsiveness is a LOCK-SCOPE bug, NOT S3 op-count: the CA manifest S3 write happens while holding the exclusive MergeTree `data_parts` lock.** After Pillar B + A1-min + zstd landed (op-count down, GC-leader CPU healthy ~<250%, no 427% saturation), `system.parts` STILL stalls intermittently up to the 60s+ client timeout (~35% of 60s-spaced polls slow; observed server-side `query_duration_ms` up to **220s**). `system.query_log` for those slow `SELECT count() FROM system.parts WHERE active` queries shows **`S3HeadObject=0, S3GetObject=0, OSIOWaitMicroseconds=0`, and only 6–12 ms of CPU** for a 60–220s wall-clock query → the query did NO work and NO I/O; it was BLOCKED ON A LOCK. `system.trace_log` (Real, 220/220 samples) parks it entirely in `DB::SharedMutex::lock_shared() <- DB::DataPartsSharedLock <- DB::MergeTreeData::getDataPartsVectorForInternalUsage <- DB::StorageSystemParts::processNextStorage` — i.e. waiting for the SHARED `data_parts` lock, blocked by an exclusive holder. Server-wide Real traces show the dominant wall-time is CA manifest writes blocked in `WriteBufferFromS3::finalizeImpl <- TaskTracker::waitAll <- future::get` (the multipart PUT to the overloaded single-disk RustFS) across `mutateShard`→`casPut` from publish/commit/dropRef/republishRef/heartbeat. **The smoking gun:** the exclusive-lock holders blocked on that S3 write are `DB::MergeTreeData::Transaction::commit(DataPartsLock&) <- ReplicatedMergeTreeSink::commitPart` (INSERT commit) and `<- StorageReplicatedMergeTree::checkPartChecksumsAndCommit <- ReplicatedMergeMutateTaskBase` (merge/mutation commit). **Code confirms it:** `MergeTreeData::Transaction::commit()` (`MergeTreeData.cpp:8790`) does `auto lock = data.lockParts();` then `commit(lock)`, which at `MergeTreeData.cpp:8805-8807` calls `part->getDataPartStorage().commitTransaction()` — for the CA disk this is `ContentAddressedTransaction::commit` → the manifest CAS S3 PUT — **all under the held exclusive `data_parts` lock**. Under a slow store each commit holds the lock for seconds; a continuous stream of part commits (INSERT/merge/mutation at load) starves `system.parts` and every other `data_parts` reader for 60–220s. **This is an architectural mismatch:** the MergeTree commit path assumes metadata-commit is a fast LOCAL op (true for the standard S3 disk — only DATA on S3, metadata is a local file), but the CA disk puts the MANIFEST (metadata) on S3, turning the in-lock metadata commit into a network round-trip. **Op-count work (B147/B148/B149: Pillar B+A1+zstd) is still valuable** (it cut CPU + HEAD storm + RustFS load, changing the failure from sustained 28s degradation to intermittent stalls) **but does NOT address this root** — neither A1b (snap checkpoint) nor A2 (retire-HEAD) touch the commit lock-scope. **FIX DIRECTION (do NOT implement without scope approval; architectural):** get the CA manifest network write OUT from under the `data_parts` lock — e.g. perform the durable manifest CAS BEFORE acquiring `lockParts` (the manifest publish is the CA commit point; the in-memory part-state swap under the lock should be a fast pointer flip), or split `IDataPartStorage::commitTransaction` into a slow durable phase (S3, lock-free) + a fast in-memory phase (under lock). Must preserve commit atomicity (manifest durability vs visible part state) and crash-consistency. No correctness issue observed (no dangling/loss; counts bounded). Evidence: T9 soak (seed 20260614, 6 workers, 25 GB) `query_log`+`trace_log` with `allow_introspection_functions=1`; poller TSV `utils/ca-soak/logs/revalidate_poll_*.tsv`. | T9 re-validation trace analysis (user-directed: query_log ProfileEvents + trace_log Real) | Move the CA manifest CAS write out of the exclusive `data_parts` lock (durable-publish-before-lock, or split commitTransaction into durable+in-memory phases); re-validate `system.parts` stays responsive under commit load. This is the top responsiveness lever, above A1b/A2. |
| B167-DONE | **VALIDATED & DONE (2026-06-17) — per-server build watermark.** Implemented (13-task plan), TLA+-verified (`CaBuildWatermark` guard+sabotages, `CaBuildWatermarkNum` numeric multi-server+monotone-build_seq finding, + safety invariants), unit-green 265/266 (only pre-existing B140 red). Soak: short soak fully clean (`dangling=0`/`broken=0`/`noref=0`, oracle+both replicas exact 604,895); 12h attempts ran clean through warmup→steady→mutations→ttl→**gc_checkpoint** (the drain that killed the workers=6 run — load-specific, B167g) at workers=2, incl. a ~4.9h chaos run exercising node restarts (the epoch/restart path). All soak failures were infra/op-count (B167f/g) or a harness oracle race (B167h), never the watermark. Replaces the reverted per-build heartbeat existence guard. Commits `15c77af`..`02e5023a` + spec/TLA+/plan. | DONE | n/a |

## Groomed 2026-06-20 — soak-harness cluster A + B182/B183/B123-126 + detached/audit-log landed {#groomed-2026-06-20}

Moved from the live backlog (fully DONE / resolved / not-a-bug). See git history + the worklog
`docs/superpowers/UNATTENDED-WORKLOG-2026-06-19-ca-vfs.md` for details.

| ID | Item | Why deferred | Where it plugs in (dead-end check) |
|----|------|-------------|-------------------------------------|
| B54 | **Potential CA mutation bug (data-correct, pruning-efficiency only): secondary-index granule/part pruning after a mutation differs on CA.** Surfaced by `03702_alter_column_update_and_delete_with_secondary_index_rebuild` under the CA-default config: after an `ALTER … UPDATE`/`DELETE` that rebuilds a `set` skip index, `EXPLAIN indexes = 1` shows the index pruning `Parts: 1/2, Granules: 1/2` on CA vs the expected `0/2, 0/2` (one part is not pruned). The query result is CORRECT (`count() = 0` either way) — only the skip-index pruning differs, so a query reads one extra part. Likely cause: the rebuilt/ carried-forward skip-index file (`skp_idx_*.idx` + its marks) for a mutated part on CA is stale or not consulted for pruning on one of the two parts. Re-tagged `no-content-addressed-storage` pending investigation. **INVESTIGATED — NOT a CA bug (closed).** A controlled CA-vs-plain reproduction (same schema, same `alter_column_secondary_index_mode='rebuild'` mutation, same explicit settings, an inline `content_addressed` disk vs the default disk) produced **byte-identical** `EXPLAIN indexes = 1` output (`Skip idx_v → Parts: 1/2, Granules: 1/2`) and identical results — so a content-addressed part resolves and prunes a rebuilt secondary index exactly like a plain part. The `03702` failure under the CA-default config is **environment/part-layout sensitivity**: that config applies *random* merge-tree settings, which produce a different merge/part state than the committed `.reference` assumes, changing which parts the skip index prunes. Not content-addressing. The test stays `no-content-addressed-storage` (environment-sensitive under the random-settings CA-default config), like other layout-sensitive tests. | Found in CAS M7 Task 7; investigated post-M7 — controlled repro shows CA == plain, so reclassified from "potential CA bug" to "not a CA bug / config-sensitivity" | **RESOLVED:** no code change — CA index resolution matches plain. Tag retained as environment-sensitive. |
| B123 | **DONE 2026-06-20 (= user's "B86").** Two parts. (1) Verbatim RMW (`moveFile` `tmp_mutation_N.txt`->`mutation_N.txt`, mountpoint move): `casPutObject` is last-writer-wins, so under the CA SINGLE-WRITER contract the blind `put(dst)` is safe (no concurrent writer) — documented that contract, and made the move IDEMPOTENT on re-drive (source gone + destination present → no-op instead of `FILE_DOESNT_EXIST`; only both-absent throws). (2) `unlinkFile` no-op for a committed CONTENT file is a **LOAD-BEARING invariant**, NOT to be "fixed" with a blanket fail-closed assert: the MergeTree fast-removal path unlinks every part file one-by-one then `removeDirectory` (= the whole-part ref-drop, the real removal unit), so a blanket assert would fire on every normal removal. Documented exhaustively in the code (the three sub-cases, why the no-op is required, the narrow fail-open caveat + the MergeTree whole-part-removal invariant that makes it safe, and the non-CAS contrast). Tests: `CaWiringOps.VerbatimMoveIsIdempotentOnRedrive` (+ existing `VerbatimMoveAndUnlink`). | DONE. | `ContentAddressedTransaction.cpp` (`moveFile` verbatim/mountpoint idempotency + single-writer doc; `unlinkFile` invariant doc). |
| B124 | **DONE 2026-06-20 (= user's "B87").** Aligned both move paths to **SOURCE-wins** (a move/rename carries the source's content to the destination — the POSIX `rename` semantic the rest of MergeTree assumes, and what the atomic `.tmp`->final rename requires). `moveFile` already did source-wins (documented as canonical); `moveDirectory` staged-merge changed from `emplace` (dest-wins) to source-wins. Added a **fail-loud guard**: a genuine collision with DIFFERING bytes (the "assumed op-order wrong" case, absent on the happy path where the dest staging is empty) throws `LOGICAL_ERROR` instead of silently dropping a just-written mutable file; identical bytes are a benign idempotent re-key. Test: `CaWiringOps.MoveDirectoryMutableCollisionPolicy` (differing→throws, identical→ok). | DONE. | `ContentAddressedTransaction.cpp` (`moveDirectory` staged-merge source-wins + guard; `moveFile` policy doc). |
| B126 | **DONE 2026-06-20 (= user's "B88").** `move_namespace` (RENAME TABLE) is RE-DRIVABLE/IDEMPOTENT and now documents it: `republishRef` no-ops on an already-moved (gone) source ref, `putNamespaceFile` is last-writer-wins (re-put of identical bytes is a no-op), `dropNamespace` of an empty/absent ns is a no-op — so a mid-loop throw leaves the table SPLIT across the two namespaces but re-driving the same RENAME completes it. Wrapped in try/catch with a loud `LOG_ERROR` (names both namespaces, says the move is re-drivable) so the split state is diagnosable. True cross-namespace atomicity (a durable move-journal) is deliberately NOT done — out of scope (would touch the tested GC/journal layer). Test: `CaWiringOps.TableRenameIsIdempotentOnRedrive` (+ existing `TableRenameMovesRefsFilesAndDetached`). | DONE. | `ContentAddressedTransaction.cpp` (`move_namespace` try/log + idempotency doc). |
| B140-dangle | **FIXED 2026-06-18 (cursor-in-snap + fail-closed coherence guard; soak re-validation pending — task #124). Was HIGH — DATA LOSS; the severe DANGLING variant of [[B140]], found 2026-06-17 in the P9 instrumented soak. NOT covered by B140's M-F-backstop verdict.** Same root cause as B140 (the GC fold fails to record a tree's `tree→blob` edges into the snap, so `zeroInDegreeKnown` surfaces the blob at in-degree 0 and the single content-delete site deletes it), but here the under-recorded tree is **LIVE** (a current manifest ref still resolves to it), so GC deletes a **referenced** blob → `fsck dangling>0` = **INV-NO-LOSS violation (data loss)**, not a harmless leak. **The M-F full-sweep backstop (B140's verdict) does NOT fix this**: the blob is already physically deleted by the time a sweep runs — a sweep reclaims debris, it cannot restore a deleted live blob. So this needs a PREVENTIVE fix (the wrong delete must never happen), distinct from B140-leak. **Soak evidence (2-node, RustFS, 64 root_shards, seed 20260617):** reproduced `dangling=25` (and earlier `=55`) at the `gc_checkpoint`, PRE-chaos (`last_fault=null`). Instrumented (`CAGCDEL`/`CAREUSE` audit logs): all 25 blobs ch2 `CAREUSE adopt` round 334 → ch1 `CAGCDEL Deleted` round 340, EXACT token match; `reachable_from` = a merged-then-detached broken part (ch2 merge 19:27:03 reused the 25 blobs → GC deleted them round 340 → next merge read 404 → part broken → detached). So GC deleted a live freshly-merged part's blobs. **NOT P9** (P9 prunes `everEdged`, not fold edge-recording; verified separately). **Mechanism (subagent-verified — built+ran 9 natural single-process interleavings, none dangle):** the fold is NOT buggy in single-process sequential operation (the `displaced_later` skip is fail-closed for any live ref — rethrows `FILE_DOESNT_EXIST` when no later same-ref record; the cascade advances the cursor in the SAME CAS that clears the marker, with a fold-through-fence recheck). The broken invariant — **"a `markExpanded` tree that is still LIVE (in-degree≥1) has ALL its `tree→blob` child edges in the durable snap"** — arises only across rounds/leaders (cross-node): mechanism (b), the cascade-vs-recreate marker race the spec comment at `CasGc.cpp:402-407` warns about. **Likely PRODUCER on the leader: the cross-node replication relink** (`adoptFromTree` tokenless-evidence, `CasBuild.cpp:607-630`) — audit with the fold invariant. **Failing regression gtest:** `CasGcDangle.MarkedExpandedWithoutEdgesDeletesLivePinnedBlob` (`src/Disks/tests/gtest_cas_b140_dangle.cpp`) — injects the corrupt durable snap via the public `GcSnap` API, asserts `runFsck().dangling==0`; RED today (`dangling=1`). Companion `CasReuseGcRace.ReuseOfBlobDeletedBeforePublish` PASSES (the LOCAL reuse-vs-GC publish gate is correctly fail-closed — the bug is the fold edge-recording invariant, not the reuse gate). **Fix-safety:** preserve INV-NO-LOSS/INV-NO-RETURN/INV-OVER-COUNT-ONLY; candidate fixes = re-expand a live tree on a folded Add when a child edge is missing / never advance `folded_cursor` past an Add whose tree is not fully edge-recorded / refuse to retire a candidate whose live parent is `markExpanded` / coherent re-verify at the delete site; model the producer + fix in `CaIncarnationCore.tla`. **2026-06-18 CORRECTION (ground truth — supersedes the "marker-without-edges" mechanism above):** decoding the REAL saved soak snaps (`/tmp/snapdump/snap_67{1,2,3}/0`) with the production codec found `markers_with_ZERO_child_edges = 0` in EVERY generation ⇒ the live-markExpanded-without-edges state DOES NOT occur in reality (the hypothesis, the fix spec's fold-watermark fix, and the `CasGcDangle` injection test's injected state are all REFUTED). The 25 dangling blobs are `known=Y, inDeg=0` while a LIVE tree references them ⇒ REAL bug = **shared/deduplicated-blob in-degree under-count** (a blob shared by a deleted/stripped tree and a still-live tree, whose live-tree edge was never present in the folded snap). A FAITHFUL TLA+ model (`CaB140DangleFaithful.tla`) exhausts 9.1M states with NO dangle (non-vacuity verified) ⇒ producer is UPSTREAM in the real `foldShardRecords` edge-construction (`displaced_later` skip applicability for a still-live ref / shared child, or cursor/shard-version windowing), below the abstract model — STILL UNPINNED. Next: natural shared-blob fold gtest + cross-node/cursor repro; correct the injection test; rewrite the fix spec once pinned. **2026-06-18 PRODUCER PINNED (ground-truth decode of the actual delete-gen gc/snaps 1280/1281): VERDICT B = shared/deduplicated-blob in-degree UNDER-COUNT.** Trace (B=ff647af8): healthy gens have the OLD part's tree T_live live+expanded with T_live→B (B inDeg=1); the old part merges away → T_live root edge gone → T_live tree retired+deleted → cascade stripTree(T_live) drops T_live→B → B inDeg=0 → GC deletes B; but B is content-SHARED and a DIFFERENT LIVE part's tree references it via dedup, and THAT edge was never in the GC in-degree snapshot (243/243 VERDICT B, 0 VERDICT A — every healthy gen records T→B correctly, so NOT fold edge-construction). The local publish-gate handles the same-process reuse (CasReuseGcRace PASSES); the CROSS-NODE dedup-vs-GC version is the producer. OPEN for the fix: was the live part's dedup-publish BEFORE B's delete (publish-gate/retire-view cross-node freshness should catch the condemnation) or AFTER (dedup-HEAD of an about-to-be-deleted blob)? — picks between (i) GC reachability-completeness for dedup-shared blobs vs (ii) cross-node dedup/publish-gate freshness. The injection test `CasGcDangle` reproduces the WRONG mechanism (A) and must be rewritten to the real shared-blob/dedup shape. Artifacts: `utils/ca-soak/tmp/snapdump_live/`, `/tmp/snapdump3/`. **2026-06-18 FIXED — PINNED to the CLASS (exact cross-leader interleaving stays unpinned, hence a producer-agnostic fix):** the snap codec OMITTED the fold cursor, so `gc/state.folded_cursor` and the snap edges were two separately-durable values that could diverge (cursor ahead of edges); the already-present gated journal trim then over-trimmed and lost the live shared-blob edge. **Fix = ONE cursor, in the snap (single source of truth):** the per-shard fold cursor is now a field of `GcSnap`, serialized in the codec (reset to v1, no migration); `gc/state.folded_cursor` is DELETED; `fold` and `cascadeAndPersist` write their own coherent cursor into the persisted snap (never ahead of the edges); byte-equal generation adoption is thereby cursor-aware. Plus a fail-closed round-start snap↔journal coherence guard (`Gc::assertSnapJournalCoherent`): every latest-for-ref `Add` at/below the cursor must have its tree in the snap, else `CORRUPTED_DATA` and no delete (the no-loss net). **TLA+ proof:** `CaB140DangleMerge.tla` reproduces the dangle and proves both halves independently necessary, jointly sufficient (merged = clean, 5.33M states; `CaB140DangleMerge_RESULTS.md`). **gtest:** `CasGcDangle.SharedBlobUnderCountDeletesLivePinnedBlob` (faithful shared-blob cursor-skip) RED→GREEN; `CasGcSnap.CursorIsPartOfSnapIdentity` (structural). Full `Cas*:CaWiring*` green except the intentional `CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` red. Spec: `docs/superpowers/specs/2026-06-18-ca-b140-dangle-fix-v2-design.md`; plan: `docs/superpowers/plans/2026-06-18-ca-b140-dangle-fix-v2.md`; commits `ee86bf7f4a0`,`cad349cac40`,`858860ec266`,`13988c8195d`,`d4e79c148a3`. | P9 instrumented soak 2026-06-17 (`utils/ca-soak/logs/p9_instr_FAILURE_dangling25.json`, `p9_instr_correlation.txt`, `p9_instr_fsck_detail.txt`; round-340-era `gc/snap` gens 671-673 + outcomes/340 + state preserved at `/tmp/snapdump`). | **Preventive fix needed (data loss) via the proper flow (brainstorm→spec→TLA+→plan→implement).** Audit `CaIncarnationCore.tla` `GFold`/expansion + the relink producer (`adoptFromTree`) for the same invariant class; durable fix proven in TLA+ + TDD gtest (`CasGcDangle.*` green); add sibling-scenario tests; re-soak (quick → 12h chaos) confirming `dangling=0`. Related: [[B140]] (leak variant), [[B160]] (lease/leadership churn = the cross-leader trigger), [[B164]] (manifest). |
| B155 | **DONE (confirmed in code 2026-06-20):** `checker.py` `sync_replica_with_readonly_retry` retries `SYSTEM SYNC REPLICA` on `TABLE_IS_READ_ONLY` (code 242) with bounded backoff until read-write or a budget; `cluster.py` `is_readonly` + `with_node_down_retry` also treat 242 as a transient; the OPTIMIZE worker swallows it too — `test_readonly_retry.py` green. Original finding: **SOAK HARNESS chaos gap (2026-06-15, run #2 ~4.9h) — recovery-checkpoint `SYSTEM SYNC REPLICA` is intolerant of the transient `TABLE_IS_READ_ONLY` a replica shows while recovering its ZK session after a chaos fault → spurious `PHASE3 FAILED`. NOT a server/CA bug, NOT data loss.** Run #2 progressed past gc_checkpoint into the chaos stage (B154 fsck-timeout degrade worked); a chaos fault (ch2 restart / rustfs pause) dropped node2's Keeper session — Keeper log: `Found dead session 2` → ~30s later `Received session ID 3` (the replica RECOVERED). During that readonly window the harness's recovery convergence issued `SYSTEM SYNC REPLICA ca_stress` against node2 → `Code: 242 TABLE_IS_READ_ONLY` → the harness treated it as a hard WORKLOAD FAILURE → `PHASE3 FAILED (rc=1)`. This is the same class as [[B154]]: the harness isn't robust to an EXPECTED transient (readonly during ZK-session recovery), exactly as it already retries transport-failures/`ABORTED`. **FIX (harness):** retry `SYSTEM SYNC REPLICA` (and any post-fault convergence op) on `TABLE_IS_READ_ONLY` (Code 242) with bounded backoff until the replica is read-write or a timeout, then proceed — do not count a transient readonly as a workload failure. | B151 soak run #2 diagnosis | Make the recovery-checkpoint SYNC REPLICA tolerate/retry transient readonly; re-run the 12h soak. |
| B154 | **DONE (confirmed in code 2026-06-20):** `run_fsck`/`run_dryrun` (`fsck.py`) take `timeout_s` and raise `FsckTimeout`; every checkpoint call site bounds it and degrades gracefully (logs `WARNING [B146/B154] … SKIPPING …`, soak continues) — `test_fsck_timeout.py` green. Original finding: **SOAK HARNESS WEDGE (2026-06-15, found during B151 12h soak ~pulse #3) — a checkpoint `clickhouse-disks fsck --detail` hangs the driver; `run_fsck` has NO subprocess timeout. NOT a B151 or server issue.** [B154 fsck-timeout fix VALIDATED in run #2: the gc_checkpoint summary fsck timed out at 180s and DEGRADED GRACEFULLY (`WARNING [B146/B154] ... SKIPPING ... soak continues`), no wedge — the driver progressed into the chaos stage.] The 12h B151 soak driver wedged at ~00:51 (early-steady stage, ~1.5h in): `soak.run` main thread blocked in `do_wait` on `docker exec ca-soak-ch1-1 clickhouse disks --disk ca_ro --query fsck --detail` (pid running **42+ min, not completing**); all worker + ticker threads gone (Threads:1); no log output / metrics tick since #47 (00:51). Root: `soak/fsck.py:98` `run_fsck` does `subprocess.run(cmd, capture_output=True, text=True)` with **no `timeout=`**, and the checkpoint (`run.py:455-457`) runs the `--detail` fsck whenever `unreachable>0` (ALWAYS true — M-F debris). `--detail` is an O(pool) full LIST + per-object HEAD reachability scan; over the **342k-object / 3.6 GB** pool on the single-disk RustFS that was ALSO serving the live GC HEAD storm (28M HEADs), it crawls to effectively-hung, and the un-timed `subprocess.run` wedges the whole driver. This is the [[B146]] offline-fsck O(pool) cost surfacing as a harness hang. **The SERVER stayed healthy + responsive (system.parts 0.02s) throughout** — only the harness's Python driver wedged. **FIX (harness):** add a `timeout_s` to `run_fsck`/`run_dryrun`'s `subprocess.run`; on timeout, raise a typed exception the checkpoint catches → log a loud B146/B154 slow-fsck finding + degrade gracefully (skip the `--detail`-dependent dryrun-subset assert for that checkpoint; keep the cheap summary `dangling==0` loss gate) + CONTINUE, instead of wedging. (Deeper: [[B146]] make offline fsck not O(pool) — incremental/sharded.) | B151 12h soak pulse #3 diagnosis | Add the fsck subprocess timeout + graceful checkpoint degradation; then re-run the 12h soak at full scale to exercise the later delete/TTL/truncate/chaos stages. |
| B152 | **DONE 2026-06-20.** Fixed `wait_for_pool_consistent` (`run.py`) to distinguish FLAPPING-CLEAN (reached `dangling==0` at least once but didn't hold it `stable` consecutive — a settling artifact; the aggregate oracle `compare_aggregates` is the authoritative no-loss gate and is asserted separately → warn loudly + return the clean reading, do NOT fail) from PERSISTENT NEVER-CLEAN (`dangling>0` on every read → raise with an accurate message; no longer cries INV-NO-LOSS on a `dangling==0` timeout). Tests `test_pool_consistent_flapping_clean_does_not_raise` + `test_pool_consistent_persistent_never_clean_raises` (152 harness tests green). Original finding: **T9 soak verdict (2026-06-14): NOT data loss, but `PHASE3 FAILED` on a post-fault consistency-SETTLING gate — plus a checker MESSAGE BUG that mislabels it as INV-NO-LOSS.** The 2h re-validation soak (seed 20260614, 6w/25GB, on the Pillar B+A1+zstd binary) failed at a chaos-recovery checkpoint: `wait_for_pool_consistent` (`run.py:554-567`) requires `stable`(3) CONSECUTIVE `dangling==0 && exit_code==0` fsck reads within 180s after a fault window; it timed out. **The final read was `dangling=0, exit_code=0` (CLEAN), reachable=129341, unreachable=17430.** So there is **NO loss / NO dangling** — `dangling=0`. The failure is that the pool kept **FLAPPING** in/out of the documented transient post-restart fsck-incoherence window (B141/B144/B145: HEAD-absent reachable trees while the object store is mid-recovery) for >180s under the aggressive load, so the gate never saw 3 consecutive stable-clean reads. The 17430 `unreachable` are M-F-deferred debris (over-count, INV-OVER-COUNT-ONLY-permitted, GC-backlog), explicitly NOT loss per `checker.py:88`. **Two findings:** (1) **Checker message bug** — `run.py:561-566` interpolates `PERSISTENT dangling={last.get('dangling')}` (=0 here) and calls it "a REAL crash-recovery / object-store durability finding (INV-NO-LOSS)"; a `dangling==0` timeout is a SETTLING/flap failure, NOT an INV-NO-LOSS finding. The message must distinguish persistent-`dangling>0` (real loss escalation) from never-stabilized-but-clean (recovery still settling). (2) **Recovery-settling-time signal** — that the post-fault incoherence window stays open >180s under load is plausibly LINKED to [[B151]] + the HEAD storm: slow under-lock manifest writes + 20M-HEAD RustFS overload delay post-fault object-store coherence (manifest visibility lag), keeping fsck flapping. Fixing B151 (publish out of lock) + op-count should let recovery settle faster. Responsiveness over the run: `system.parts` slow(>1s) in 27/220 polls (~12%), recovered to 0.00s at the end. | T9 re-validation soak | (a) Fix the checker message to not cry INV-NO-LOSS on `dangling==0`; gate on `unreachable` stabilization for the settling case. (b) Re-validate post-fault settling after [[B151]] + op-count land. NOT a data-loss bug (dangling=0). |
| B170 | **Operability: optional always-on `system.content_addressed_events` audit log — every CAS decision & event as structured rows.** This session's B140-dangle diagnosis needed piecemeal INFO audit lines (`CAGCDEL`/`CAREUSE`/`CASTRIP`/`CAROOTREM`, added ad-hoc across B90/B140/B167/B168), grepped from `syslog`-owned container logs (a permission pitfall that produced a WRONG conclusion until re-run via `docker exec`), plus a throwaway snap decoder. A first-class, optionally-enabled `SystemLog` sink would make all of this a SQL query. Capture one structured row per event with columns like `(event_time, node, event_type, namespace, ref_name, object_kind, object_hash, token, round, gen, at_version, outcome, reason, detail)` for: **writer/build** — putBlob, reuseBlob (adopt/resurrect), putTree, publish, dropRef, gate revalidate/abort, watermark/heartbeat; **GC** — fold root add / `removeRootEdge` / re-point, tree expansion, retire (observe HEAD+token), recheck outcome (Deleted/Replaced/Spared/Absent), cascade `stripTree`, the single content-delete, P9 `forget`, fence, lease acquire/steal/heartbeat; **snap** — generation persist + folded_cursor advance. Consolidate the existing ad-hoc audit loggers (`CasGcAudit`/`CasBuildAudit`) into this one sink. Make it **optional and cheap** (config-gated; high volume — ~87k events / 20 min in the soak — so a bounded ring-buffer / sampling / per-namespace filter mode), suitable to leave ON in soak/CI and toggle in prod for incident forensics. Enables token-join attribution (reuse-adopt <-> GC-delete), root-edge-removal -> strip -> blob-delete causal chains, and snap-vs-truth divergence queries — exactly the B140-dangle / B144 / B167 forensics — without log-grep, permission pitfalls, or throwaway decoders. | Deferred: not needed for first milestone; the ad-hoc INFO lines sufficed (barely) for the soak investigations but proved fragile (permission/grep errors) and unattributable at scale. | A `SystemLog`-backed sink (model on the existing `system.content_addressed_garbage_collection_log` + its Context accessor) fed by the `CasGcAudit`/`CasBuildAudit` seam in `Core/`; the GC scheduler already injects a logging sink. Complements [[B169]] (decode/introspect CLI), [[B99]]/[[B15]] (GC observability / system.* views). **DONE 2026-06-18** — implemented as `system.content_addressed_log` (off by default, config-gated); `CasEvent` POD + `Store::emitEvent`/`hasEventSink` + bridge in `ContentAddressedMetadataStorage`; ~42 emit hooks; validated live in the soak (pinned B140-dangle). Commits d80d94a113b..76a0705cab4. |
| B173 | **SOAK HARNESS robustness (found in the B171 12h soak, 2026-06-19) — workload ops must tolerate transient `TABLE_IS_READ_ONLY` (code 242) during chaos recovery.** The chaos `both pause 40s` fault drops BOTH replicas' Keeper sessions, so on recovery a `ReplicatedMergeTree` replica is transiently read-only until it reconnects. The harness's concurrent workload `OPTIMIZE TABLE` hit code 242 and the driver classified it as a fatal `WORKLOAD FAILURE` → `RUNPY_EXIT=1` at ~6.5h, even though `fsck dangling=0`/`fsck_status=settled` and all CA anomalies were 0 (NOT a product/B140 bug). `checker.py` already had `sync_replica_with_readonly_retry` for SYNC REPLICA, but the workload INSERT/OPTIMIZE/BARRIER paths lacked the same tolerance. **FIXED 2026-06-19**: `cluster.is_readonly` helper + `retry_on_transport` now retries readonly (covers INSERT + all BARRIER ops via `_with_transport_retry`); `_optimize_with_retry` swallows readonly (OPTIMIZE has no model effect). Same class as B167h (harness too strict under chaos). | n/a (harness, not product) | `utils/ca-soak/soak/cluster.py` (`is_readonly`, `retry_on_transport`), `utils/ca-soak/soak/run.py` (`_optimize_with_retry`) |
| B181 | **Fold detached parts INTO the table's own `@cas@` archive — eliminate the parallel detached namespace.** Today detached lives in a separate sibling namespace `<server>/detached/store/<u3>/<uuid>@cas@` (post-path-mapping form). That parallel namespace is a workaround from when the layout had no hierarchy: a detached part can share a name with a live part (`all_1_1_0` may exist both live and detached), and a flat ref-keyspace forced a second namespace to dodge the collision. **Direction (operator, 2026-06-19):** detached should live *inside* the table's main archive — make detached parts **refs within the table's OWN archive namespace** keyed by a `detached/`-prefixed ref name (`detached/<part>` vs live `<part>`). One namespace per table, no parallel `detached/...` namespace, no `route` re-split; the name collision is impossible because the ref names differ. **GC-safe by construction:** GC discovers + folds via the registry + the static `[0, root_shards)` fan-out (NOT by prefix-listing), so detached-as-refs is just more refs in one manifest — there is **no nested-namespace concern** (the earlier path-mapping spec §5.7 worry assumed a child *namespace*; refs-with-prefix sidesteps it cleanly). **Work:** rework the wiring `route`/`parsePartFilePath` so `<table>/detached/<part>/...` resolves to `(table-ns, ref="detached/<part>", file)` instead of `(detached-ns, ref="<part>")`; delete `detachedNamespace`; make the `@cas@`-archive list/exists path surface `detached/`-prefixed refs under the `detached/` subdir; no migration (POC). This is the **detached half of spec D3** (projections, already a `<proj>.proj/` tree-entry prefix, are the other half — leave as-is). Validation: full FREEZE/detach/attach/`system.detached_parts` lane + a soak. **DONE 2026-06-20 (branch `cas-vfs-path-mapping`):** `route` re-split now yields `(liveNamespace(table), ref="detached/"+part, file)`; `detachedNamespace` deleted (only a historical comment remains; `grep -rn detachedNamespace src/` finds no code); `<table>/detached` container lists the table's refs filtered to the `detached/` prefix (stripped), table-dir listing collapses `detached/<part>` refs to the single `detached` subdir; transaction DROP TABLE drops ONE namespace, DROP DETACHED drops only `detached/`-prefixed refs, RENAME TABLE moves all refs in one namespace move, DETACH/ATTACH unchanged. New constant `kDetachedRefPrefix`. **Validation:** CA gtests 347 PASS / 2 pre-existing-red (`CasGcLeak.DisplacedUnexpandedTreeBlobsLeak` known; `CaWiringOps.FreezeViaHardLinksIntoShadow` fails identically on the pre-B181 baseline — unrelated); new `CaWiringRoute.DetachedFoldsIntoTableNamespaceWithPrefixedRef` + reworked detached read test (same-base-name live part proves no collision). Detach/attach stateless oracle 04287/04288/04289/04278 stdout matches reference EXACTLY (verified vs a local server); their praktika FAIL is the spec-acknowledged benign LOCAL-pool emulated-conditional-ops WARNING-to-stderr artifact, not a diff. 04036 FREEZE/backup OK (shadow untouched). **Chaos soak DEFERRED — B185 (rustfs loses ~38% of writes); lane+gtests are the validation.** | Deferred 2026-06-19: the path-mapping refactor kept detached a sibling namespace to stay GC-inert; folding is a wiring route/parse rework + full detach/attach lane validation, out of that refactor's scope. | `MetadataStorages/ContentAddressed/ContentAddressedMetadataStorage.cpp` (`route`, drop `detachedNamespace`, the classify/list/exists detached branches), `PartPathParser` (detached re-split → in-namespace `detached/`-prefixed ref naming). Relates to spec `2026-06-19-ca-vfs-path-mapping-design.md` §5.7 / §6 D3. |
| B182 | **FIXED 2026-06-20 — was a B151 REGRESSION, not a pre-existing gap (the user's instinct was right).** Symptom: merge/mutation inside an explicit `BEGIN/COMMIT` → `Code: 107 ContentAddressed: moveFile source mutable file missing: store/<uuid>/tmp_merge_*/txn_version.txt.tmp`. Root cause (confirmed by trace): `DiskObjectStorageTransaction::moveDirectory` dispatches the CA part-dir rename EAGERLY (B151, 6f5e3866710) — publishing `tmp_*` → final ref BEFORE the QUEUED `replaceFile` (`txn_version.txt.tmp → txn_version.txt`) replays at commit. The eager publish captures the un-renamed `.tmp` and erases the staging; the queued replaceFile then finds an empty staging → committed-mutable branch → `resolveRef(tmp ref)` fails (ref published under the FINAL name). Plain INSERT/merge work because non-transactional `txn_version` writes are deferred. Fix: `DiskObjectStorageTransaction::moveFile`/`replaceFile` dispatch a CA **mutable per-part file** atomic-write rename EAGERLY (like writeFile/createHardLink/moveDirectory) via helper `isContentAddressedMutablePartFileRename`, so the `.tmp → final` re-key completes in staging before the eager publish. Verified: repro correct; oracle CA==default; 296/298 CA gtests (2 unrelated pre-existing). | DONE. | `DiskObjectStorageTransaction.cpp` (moveFile/replaceFile CA eager dispatch). |
| B183 | **FIXED 2026-06-20 — was a REGRESSION (text-index temp-storage × CA single-ref model), not a carry-forward gap.** Symptom: vertical-merge OR mutation `MATERIALIZE INDEX` on a text index → `Code: 107 ContentAddressed: file skp_idx_*.cmrk2 / statistics.packed not in tree of .../all_*`. Root cause (confirmed by content/publish trace): the text-index builder (`createTemporaryTextIndexStorage`, MergeTask:2847 / MutateTask:1764) writes scratch under `<part>/text_index_tmp/` through a SEPARATE `DataPartStorage`+transaction whose own `commitTransaction()` DURABLY PUBLISHES a committed `<part>` ref holding ONLY the scratch files. The main merge stages the real files (+ merged skip-index/statistics) and publishes the correct manifest at the eager rename — but `moveDirectory`'s unconditional `republishRef(tmp → final)` then ADOPTS the spurious scratch ref OVER the real manifest, clobbering it. (`removeRecursive(text_index_tmp)` is a no-op on CA.) Plain INSERT + HORIZONTAL merge work (no temp storage). Fix: in `moveDirectory`, when the move had a STAGED source (fresh tmp→final), DROP any committed source ref of the same name (`dropRefIfPresent`) instead of `republishRef`-ing it — the staged manifest is authoritative; the scratch ref's blobs become unreachable → GC reclaims. Committed-source rename path (DETACH/ATTACH/delete_tmp) unchanged. Verified: repro correct; oracle CA==default (vertical-merge text index, MATERIALIZE INDEX, LWD+merge, projection+mutation); 296/298 CA gtests. | DONE. | `ContentAddressedTransaction.cpp` (`moveDirectory` staged-source drop-vs-republish). |
| B187 | **FIXED 2026-06-20 (per-disk Expect:100-continue threshold; commit on `cas-vfs-path-mapping`).** Fix: new per-disk S3 setting `expect_continue_min_bytes` (default `DEFAULT_EXPECT_CONTINUE_MIN_BYTES`=1 MiB — unchanged for every other store, no [[B120]] regression); the CA-over-rustfs soak disk sets it to 64 KiB so ALL non-tiny conditional blob PUTs negotiate `Expect: 100-continue` (extends the B118 fix that was gated at >=1 MiB and missed the 967 KiB dedup blob). A dedup-hit conditional PUT now gets a clean early 412 BEFORE the body -> handled as a dedup hit by finalizeConditionalWrite -> no mid-body close -> no Broken pipe -> no 500x5s retry storm -> no merge wedge. Retry budget left unchanged (operator request). **VALIDATED** on a fresh 20-min no-chaos soak: Broken-pipe=0 and Retrying-S3=0 on both nodes (was 360x on one blob), merges elapsed=0s/progress=1 (no wedge). Files: `src/IO/S3Defines.h`, `src/IO/S3AuthSettings.cpp`, `src/IO/S3/PocoHTTPClient.{h,cpp}`, `src/Disks/DiskObjectStorage/ObjectStorages/S3/diskSettings.cpp`, `utils/ca-soak/configs/storage_conf.xml`. --- ORIGINAL FINDING --- OPEN (HIGH — merge-wedge under rustfs broken-pipe; found 2026-06-20 in the P1/P2 baseline soak).** A 20-min no-chaos soak stalled at the final checkpoint: `quiesce` waited on "1 active merge" for >25 min. The merge was NOT slow-compute (B184) — `system.merges` showed `progress=1`, 10.9 MiB / 363k rows, elapsed 1466s. The merge thread is wedged in `MergeTask::finalizeProjectionsAndWholeMerge → MergedBlockOutputStream::finalizePart → ... → Cas::Build::putBlob → NativeStreamingSink::finalize → finalizeConditionalWrite → WriteBufferFromS3::finalizeImpl → TaskTracker::waitAll → future::get` (cond_wait). The blocking S3 PUT (`blobs/58/5801c6…`, retried **360×**) keeps getting **Broken pipe** (Poco I/O error, `e.code()=32`) from rustfs; ClickHouse's S3 `RetryStrategy::ShouldRetry` correctly classifies broken-pipe as a retryable NETWORK error → retries up to `s3_retry_attempts` (`DEFAULT_RETRY_ATTEMPTS=500`) with a **5000 ms** backoff = up to **~42 min** retrying ONE PUT, on the merge's finalize critical path → the merge wedges → the quiesce/checkpoint never drains → soak stalls (doom-loops: exhaust 500 → merge aborts → re-run → broken-pipe again). **CONFIRMED MECHANISM (not random rustfs flakiness):** the blocking PUT is a **conditional `If-None-Match:*` write of a blob that ALREADY EXISTS** — verified: `blobs/58/5801c6…` is on rustfs disk (990177 B, written earlier in the run) → a **dedup hit**. It is DETERMINISTIC, not transient, which is exactly why **0 of the 360 retries succeed** (the object exists on every attempt; there is no transient blip to win). The 412 should surface cleanly via `detail::finalizeConditionalWrite` (which catches `S3Exception("PreconditionFailed")` from `buf.finalize()`), but instead rustfs rejects the conditional PUT **early and closes the connection before draining the ~967 KB request body**; ClickHouse, still streaming the body, gets a transport **Broken pipe** from the socket write — NOT a parsed 412 response. So it evades BOTH the CA 412-catch (it is a transport `Poco::Exception`, not an `S3Exception("PreconditionFailed")`) AND the [[B166]] SDK fast-path (`ShouldRetry` only short-circuits a *parsed* `PRECONDITION_FAILED` response code) → falls into the generic network-retry budget (500 × 5 s). Likely tied to **`Expect: 100-continue`** ([[B118]]/[[B120]]): had the client waited for `100 Continue`, the early 412 would arrive BEFORE the body was sent → clean 412, no broken pipe. Many dedup-hit blobs hit this (5801c6 ×360, 9601d5 ×71, …). **P1/P2 ([[B168]]) PREVENTS the wedge, not just the cost:** a HEAD-first dedup hit never issues the conditional body-PUT → no early-412-close → no broken pipe → no 500×5s storm. system.errors at the time: S3_ERROR 213k (mostly by-design 412), ABORTED 7 (`publish dependency lost — retry`), NO_REPLICA_HAS_PART 3. | Found while capturing the P1/P2 op-count baseline; distinct from B184 (slow large merge) — this is broken-pipe × oversized retry budget. | **Fix directions:** (1) cap the S3 WRITE retry budget for the CA disk (`retry_attempts` ~10-20 + shorter backoff) so a persistently-broken-pipe PUT fails fast → the merge ABORTs and re-runs (CA writes are abort-retry-safe) instead of wedging ~42 min and stalling quiescence; (2) investigate WHY rustfs persistently broken-pipes blob PUTs under load ([[B119]]: keepalive/concurrency/size or a rustfs-beta bug); (3) P1/P2 ([[B168]]) cuts blob-PUT volume (HEAD-first on dedup) → less exposure, but new-content PUTs stay vulnerable. `src/IO/S3/Client.cpp` ShouldRetry; `src/IO/S3Defines.h` DEFAULT_RETRY_ATTEMPTS; disk-config `retry_attempts`/backoff; rustfs compose. Relates to B119/B166/B184/B185. |
