\* Defect: after an ambiguous absent staged copy is condemned, retry copies the same complete
\* bytes and reproduces the queued ETag; that old exact delete can remove the replacement.
\* EXPECTED: `ExactDeleteCannotRemoveFreshIncarnation` violated.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    WriterOne = w1
    WriterTwo = w2
    TokenFamilies = {"ETag"}
    MaxAttempts = 2
    MaxWrites = 2
    MaxEnvelope = 6
    MaxToken = 2
    MaxMetaVersion = 4
    MaxFence = 2
    Defects = {"recopy_after_condemned"}
INVARIANTS
    TypeOK
    ExactDeleteCannotRemoveFreshIncarnation
CHECK_DEADLOCK FALSE
