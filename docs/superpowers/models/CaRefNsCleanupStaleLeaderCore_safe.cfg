\* Honest stale-leader gate (spec 2026-07-11-cas-ref-table-snapshot-log-design.md §gc-step-clean-ref-objects
\* / §namespace-birth): the Pending pass re-reads the round, aborts on the `_cleanup` marker, and
\* epoch-filters manifests, so a straggler never deletes recreated data. GREEN.
SPECIFICATION Spec
CONSTANTS
    MaxRound = 3
    SabotageNoStragglerGuard = FALSE
INVARIANT TypeOK
INVARIANT NoRecreatedDataDeleted
