\* Honest rebirth (spec 2026-07-27-cas-ref-chain-complete-cut-design.md §2 INV-3, §3 Lifecycles):
\* recreation always mints a fresh incarnation, so the stale leader's unconditional, unguarded
\* physical-delete pass -- scoped only to the incarnation it captured before deposition, never
\* re-checked at delete time -- structurally cannot reach the reborn life's data. GREEN.
SPECIFICATION Spec
CONSTANTS
    MaxInc = 3
    SabotageNoIncarnation = FALSE
INVARIANT TypeOK
INVARIANT NoLiveDataDeleted
