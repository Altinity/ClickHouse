\* Stage 2: + the session-expiry-vs-client-awareness gap (the make-or-break hinge).
\* Server may expire a writer's session independently of client awareness.
SPECIFICATION Spec

CONSTANTS
    Writers = {w1, w2}
    Leaders = {L1}
    MaxEpoch = 2
    Retention = 1
    EnableExpiry = TRUE
    EnableSplit = FALSE
    EnableWipe = FALSE

CONSTRAINT StateConstraint

INVARIANT TypeOK
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_DANGLE
INVARIANT INV_NO_ABA
