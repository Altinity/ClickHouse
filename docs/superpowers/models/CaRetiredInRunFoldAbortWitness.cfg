\* EXPECTED-RED WITNESS (not a gate): reproduces the deposed-leader stray-Clean clearSparedMeta hole
\* (INV_NO_LOSS). See docs/superpowers/reports/2026-07-11-cas-deposed-leader-stray-clean-meta.md. Honest
\* CaRetiredInRun.cfg (add-only advisory meta) is GREEN; this witness (faithful clean-on-spare) is RED.
SPECIFICATION Spec
CONSTANTS
    Blobs = {b1, b2}
    MaxRound = 4
    MaxToken = 3
    MaxJournal = 5
    Sabotage = "none"
INVARIANT TypeOK
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_RETURN
INVARIANT INV_COVERAGE
INVARIANT INV_ONE_PASS
