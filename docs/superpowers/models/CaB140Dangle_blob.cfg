\* B140-dangle PRODUCER, hunting the SHARPENED target: a blob child of a LIVE markExpanded tree
\* gets condemned/deleted. Pins the exact spec mechanism (live marked T, missing T->B edge).
SPECIFICATION Spec
CONSTANTS
    Leaders = {L1, L2}
    Trees = {t1}
    Blobs = {b1}
    MaxGen = 3
    MaxLog = 4
    EnableSplitExpand = TRUE
    EnableInBuildStrip = TRUE
    EnableLeaseSteal = TRUE
    EnableAdopt = TRUE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_BLOB_NOT_CONDEMNED
