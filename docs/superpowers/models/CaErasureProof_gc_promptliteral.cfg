\* GC exactly as the Task-8 prose argues it: hb only inside rounds, no new
\* round after terminal intent, grace ON. Checks whether
\* round_in_flight-at-sample + has_observation + LIST-reset close the GC
\* window mechanically. EXPECTED: VIOLATES TruthEmpty (a fresh scheduler's
\* round creates gc/state after the final LIST and completes before the
\* round_in_flight read).
SPECIFICATION Spec
CONSTANTS
  EnableGc = TRUE
  GraceOn = TRUE
  GcStopsOnIntent = TRUE
  EnableHbOutsideRound = FALSE
  GcLiveGate = FALSE
  MaxGen = 4
INVARIANT TypeOK
INVARIANT TruthEmpty
CHECK_DEADLOCK FALSE
