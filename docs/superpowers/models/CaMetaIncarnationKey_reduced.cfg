\* Option B (reduced): per-incarnation keys + fresh-incarnation-on-resurrect hold both invariants (no wait, no tombstone).
SPECIFICATION Spec
CONSTANTS
    MaxInc = 8
    Writers = {w1, w2}
    SabResurrectReuseIncarnation = FALSE
INVARIANT TypeOK
INVARIANT INV_NO_DANGLE
INVARIANT INV_META_BODY
