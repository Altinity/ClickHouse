\* B140-dangle PRODUCER: flags ON. Expect a counterexample to INV_MARKER_EDGES / INV_NO_LOSS.
SPECIFICATION Spec
CONSTANTS
    Leaders = {L1, L2}
    Trees = {t1}
    Blobs = {b1}
    MaxGen = 3
    MaxLog = 4
    EnableSplitExpand = TRUE
    EnableInBuildStrip = TRUE
    EnableLeaseSteal = TRUE
    EnableAdopt = TRUE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_MARKER_EDGES
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_DANGLE
