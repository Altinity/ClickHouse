\* Sabotage: commit a `-1` body token at EDGE granularity (the pre-fix bug) instead of on full log
\* fold. The post-CAS delete then reclaims A's body while its log is clamped, and every re-fold clamps
\* forever. Must violate NoDeleteBehindClamp (safety) and EventuallyFolded (permanent freeze). RED.
SPECIFICATION Spec
CONSTANTS
    SabotageEdgeGranularity = TRUE
    SabotageDeleteBeforeAdoption = FALSE
    SabotageNominationContaminates = FALSE
INVARIANT TypeOK
INVARIANT NoDeleteBehindClamp
PROPERTY EventuallyFolded
