\* Witness: the trip#2 race is real -- an in-flight attempt's reclaim can
\* complete inside the join window, leaving Live/mayMutate at the Trip2
\* step (violation = good).
SPECIFICATION Spec
CONSTANTS
  EnableTrip2 = TRUE
  DrainGate = TRUE
  EnableGcSelfExit = TRUE
INVARIANT WNoJoinWindowReclaim
CHECK_DEADLOCK FALSE
