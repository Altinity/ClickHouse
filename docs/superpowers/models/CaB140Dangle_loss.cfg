\* B140-dangle PRODUCER, hunting actual DATA LOSS only. Expect INV_NO_LOSS counterexample —
\* a content-delete of a blob a live tree still references.
SPECIFICATION Spec
CONSTANTS
    Leaders = {L1, L2}
    Trees = {t1}
    Blobs = {b1}
    MaxGen = 3
    MaxLog = 4
    EnableSplitExpand = TRUE
    EnableInBuildStrip = TRUE
    EnableLeaseSteal = TRUE
    EnableAdopt = TRUE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_NO_LOSS
