\* REGRESSION CANARY (reframed 2026-07-14, rev.6 round 6 -- was a SABOTAGE cfg through round 4):
\* drops the epoch-match conjunct (`epochOK`) from `Write`, on the theory that this should let a
\* superseded holder mutate. In the PRODUCT, `epochOK`'s load-bearing role is fencing a LATE-LANDING
\* in-flight write: an old incarnation's already-in-flight S3 request that lands AFTER a takeover
\* bumped the epoch -- the conditional write is exactly what fences that out. This model's `Write` is
\* INSTANTANEOUS (no in-flight/late-arrival phase), so that failure source is inexpressible here --
\* the SAME abstraction-limit class as Task 1's `_rev6_latedelivery` adjudication (a stronger-than-
\* designed guarantee from the model's own idealization, not a real absence of the underlying risk).
\* Within the model, epoch divergence while unfenced is now PROVABLY UNREACHABLE (rev.6 round 5's
\* exhaustive vacuity argument: `ClaimMount`/`AdoptWrite` always write `mount.epoch`/`localEpoch[a]`
\* together; the corrected `AllocEpoch` guard only fires when the mount is absent, foreign -- impossible
\* for the sticky owner -- or FENCED, and the fenced case is independently blocked by `~mount.fenced` in
\* `Write` regardless of `epochOK`; `ObservedReclaim`/`WallClockReclaim` touch only the durable `epoch`
\* counter, never `mount.epoch`/`localEpoch[a]`), so `SabSupersededWrites` is now INERT -- this cfg is
\* expected GREEN, with the IDENTICAL reachable-state count as `_stage1` (both differ only in this one
\* now-inert flag). CANARY: if this cfg EVER goes RED, an epoch-divergence path has re-opened in the
\* model (almost certainly an ordering guard regressed, e.g. `AllocEpoch`'s fence-only guard) --
\* investigate before trusting the rest of the matrix. Do not delete this cfg and do not manufacture a
\* new adversary mechanism to force it red again.
SPECIFICATION Spec
CONSTANTS
    Actors = {A, B}
    None = NoneVal
    MaxClock = 4
    MaxEpoch = 3
    MaxToken = 6
    TTL = 2
    SabForeignTakeover = FALSE
    SabEpochReset = FALSE
    SabSupersededWrites = TRUE
    SabAdoptWedgeOnTouch = FALSE
    SabAdoptIgnoresFence = FALSE
    Drift = 0
    SabWallClockReclaim = FALSE
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT NoTwoServerUuidsOwnSameServerRoot
INVARIANT ForeignUuidNeverAutoTakesOver
INVARIANT WriterEpochMonotoneUnique
INVARIANT SupersededWriterMakesNoMutation
