\* FINDING, violation = evidence. NO sabotage is enabled: this is the fully honest catalog, checked
\* against the reconciliation invariant the task brief proposed,
\*
\*     (entry.state = "creating") => (ckptOf # {} \/ creatorAlive \/ entry.inc = 0)
\*
\* which is RED on a legitimate transient state — a creator whose fence goes terminal before its
\* `_ckpt` create. That state is exactly the one reconciliation EXISTS to clean up, so the formula
\* cannot be the safety property; it is a liveness wish about reconciliation eventually running.
\* The property is stated over the damage instead (`INV_RECONCILE_SAFE`), and this config is why.
SPECIFICATION Spec
CONSTANTS
    MaxInc = 3
    SabotageJanitorEatsNewborn = FALSE
    SabotageZombieGoLive = FALSE
    SabotageReconcileLiveCreator = FALSE
    SabotageReconcileStaleToken = FALSE
    SabotageSameIncarnationRebirth = FALSE
    SabotageFloorRetainsDeadName = FALSE
    SabotageDeleteWithoutEvidence = FALSE
    SabotageDeleteWithForeignEvidence = FALSE
    SabotageDeleteUnderHold = FALSE
    SabotageDeleteWithoutExactObservation = FALSE
    SabotageDeleteWithoutCleanupAttempt = FALSE
INVARIANT TypeOK
INVARIANT INV_RECONCILE_SAFE_BRIEF
