\* 2026-07-28 v9 WITNESS (negated reachability -- TLC reporting `W_SealRejectedRetry` VIOLATED is
\* the evidence): an OLD generation's lane really does meet a SUCCESSOR's `EpochSeal` at the
\* frontier key and is conclusively rejected by it.
\*
\* This is plan task 1's SECOND hand-off, discharged. `CaRefTableSnapshotLogCore` has a single
\* `rPhase`, so at most one recovery is ever in flight there and a seal planted by a DIFFERENT
\* recoverer than the one reading it is unrepresentable -- INV-2's "`Occupied` with an `EpochSeal`
\* terminates the walk" branch was therefore never exercised (see that module's `_RESULTS.md`
\* Scoping, "Concurrent recoverers", and the task-1 fix-round I-2 note handing it here). This model
\* has per-actor recovery state, so the SUCCESSOR incarnation's `SealSlot` plants the seal and the
\* OLD generation's wedged lane meets it.
\*
\* Trace, depth 12: `ClaimOwnerEmpty -> ClaimMount -> Tick -> Tick -> WedgeAdmit` (a wedged lane at
\* generation 0) `-> ClearExpiredMount -> AllocEpoch` (generation 1) `-> ClaimMount -> RecoveryStart`
\* (`recGen[A] = 1`) `-> SealSlot`, which finds the key `Created` and occupies it with the SUCCESSOR
\* generation's `EpochSeal` (INV-2: "the store's conditional create is the fence") `->
\* WedgeRetryOccupied`, where the generation-0 lane finds `Occupied(EpochSeal)` and resolves
\* DEFINITIVELY FAILED with no ack.
\*
\* Note that the witness flag requires the seal's generation to be STRICTLY LATER than the lane's.
\* Without that, TLC satisfied this witness at depth 8 with ONE generation sealing the key and its
\* OWN lane then meeting it -- the ordinary self-walk, which the sibling module already covers and
\* which proves nothing about two recoverers racing. See `WedgeRetryOccupied`'s first branch and
\* `CaCasMountCore_RESULTS.md` `{#findings}`.
\*
\* This branch is deliberately GENERATION-INDEPENDENT in the model and is touched by no sabotage in
\* this round: v9 has two defenses here (the generation recheck and the seal) and neither may be
\* allowed to hide the other. The recheck's own reds are `_sab_staleinstall` and
\* `_sab_wedgeretryoldgen`; this config is the seal's positive evidence.
\*
\* All sabotage flags OFF.
\*
\* BOUNDS: MaxClock = 2, MaxEpoch = 2, MaxToken = 4, TTL = 2, Drift = 0, MaxAdmissions = 3 -- the
\* SAME bounds as the green gate `_v9_recoverygen.cfg`, which is where they are explained (that
\* config is exhaustive only at these). The reds and the green must share bounds or the pair carries
\* no evidence. This witness was FIRST found at `_stage1`'s wider bounds and still fires here.
SPECIFICATION Spec
CONSTANTS
    Actors = {A, B}
    None = NoneVal
    MaxClock = 2
    MaxEpoch = 2
    MaxToken = 4
    TTL = 2
    SabForeignTakeover = FALSE
    SabEpochReset = FALSE
    SabAdoptWedgeOnTouch = FALSE
    SabAdoptIgnoresFence = FALSE
    Drift = 0
    SabWallClockReclaim = FALSE
    SabEpochGuardOff = FALSE
    SabDecomBlindBypass = FALSE
    RecoveryGenOn = TRUE
    SabStaleInstall = FALSE
    SabWedgeRetryOldGen = FALSE
    SabSlotNoByteCompare = FALSE
    MaxAdmissions = 3
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT W_SealRejectedRetry
