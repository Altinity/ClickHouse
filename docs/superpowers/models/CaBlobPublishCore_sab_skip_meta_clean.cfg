\* Defect: a landed publication is reported ready without reconciling metadata to `Clean`.
\* EXPECTED: `ReadyRequiresCleanMeta` violated.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    WriterOne = w1
    WriterTwo = w2
    TokenFamilies = {"ETag"}
    MaxAttempts = 2
    MaxWrites = 2
    MaxEnvelope = 6
    MaxToken = 2
    MaxMetaVersion = 4
    MaxFence = 2
    Defects = {"skip_meta_clean"}
INVARIANTS
    TypeOK
    ReadyRequiresCleanMeta
CHECK_DEADLOCK FALSE
