\* LIVENESS WITNESS (round 7, consult point 5 -- the anti-wedge/anti-dead-end check): assert
\* W_RecoveryAfterObservedReclaim as an INVARIANT so TLC reports it VIOLATED -> some actor
\* whose mount was reclaimed by ObservedReclaim later completes the FULL legitimate recovery
\* loop (GcFence on the now-stale successor body -> AllocEpoch, now allowed since fenced ->
\* ClaimMount's fencedReclaim branch, strictOK satisfied since localEpoch[a] now equals the
\* fresh epoch -> Write, not superseded) is REACHABLE. Proves round 8's body-faithful reclaim
\* + strict-order guard block the SUPERSEDED incarnation's re-arm WITHOUT wedging the SAME
\* uuid's eventual, legitimate recovery -- honest-path safety (CaCasMountCore_rev6_observe.cfg)
\* is not "safe by permanent dead-end". Generous bounds: this scenario needs more steps than a
\* bare reclaim (reclaim -> wait for the successor body itself to lapse -> fence -> realloc ->
\* reclaim -> write). MUST remain reachable (its RED still fires) -- re-confirmed round 8.
SPECIFICATION Spec
CONSTANTS
    Actors = {A, B}
    None = NoneVal
    MaxClock = 12
    MaxEpoch = 4
    MaxToken = 12
    TTL = 3
    SabForeignTakeover = FALSE
    SabEpochReset = FALSE
    SabAdoptWedgeOnTouch = FALSE
    SabAdoptIgnoresFence = FALSE
    Drift = 2
    SabWallClockReclaim = FALSE
    SabEpochGuardOff = FALSE
    SabDecomBlindBypass = FALSE
    RecoveryGenOn = FALSE          \* 2026-07-28 v9: the recovery-generation layer is INERT here --
                                   \* this cfg predates it and its state space is therefore
                                   \* unchanged (see CaCasMountCore_RESULTS.md's before/after table)
    SabStaleInstall = FALSE
    SabWedgeRetryOldGen = FALSE
    SabSlotNoByteCompare = FALSE
    MaxAdmissions = 0          \* the layer is off here; the bound is only
                            \* declared because TLC requires every constant
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT W_RecoveryAfterObservedReclaim
