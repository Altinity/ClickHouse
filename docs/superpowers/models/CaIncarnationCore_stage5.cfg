\* Stage 5: + split-brain leaders (two concurrent GC round machines) + unconditional overwrite. Fuller bounds (like stage 4 full). Run only if the small config passes with huge headroom.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1}
    Leaders = {L1, L2}
    Shards = {s1, s2}
    Hashes = {t1, t2, h1, h2}
    TreeHashes = {t1, t2}
    MaxToken = 2
    MaxRound = 2
    MaxLog = 6
    EnableResurrect = TRUE
    EnableTrees = TRUE
    EnableDebris = TRUE
    EnableSplit = TRUE
    EnableOverwrite = TRUE
    SabotageNoFence = FALSE
    SabotageNoRecheckFold = FALSE
    SabotageNoRetireView = FALSE
    SabotageUncondDelete = FALSE
    SabotageReusedTag = FALSE
    SabotageCascadeRace = FALSE
    SabotageCutOverclaim = FALSE
    EnableReval = FALSE
    SabotageNoReobserve = FALSE
    EnableRegistry = FALSE
    EnableEvStale = FALSE
    SabotageNoRegistry = FALSE
    SabotageFoldTimeUniverse = FALSE
    SabotageNoEvReobserve = FALSE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_NO_DANGLE
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_RETURN
INVARIANT INV_JOURNAL_COVERAGE
PROPERTY MonotoneGC
