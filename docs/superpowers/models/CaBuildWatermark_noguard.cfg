\* SABOTAGE (a): today's GC ignores the watermark -> re-condemns the build's own fresh incarnation
\* every upload->publish window -> the B167 starvation lasso. Liveness VIOLATED.
SPECIFICATION Spec
CONSTANTS
  Builds = {b1, b2}
  NoOwner = NoOwner
  WatermarkGuard = FALSE
  SoundDetection = TRUE
  ActiveSetCorrect = TRUE
  CanCrash = FALSE
INVARIANT TypeOK
PROPERTY Liveness
CHECK_DEADLOCK FALSE
