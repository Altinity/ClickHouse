\* Defect: publication begins while the writer precommit is still absent.
\* EXPECTED: `PublicationRequiresDurablePrecommit` violated.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    WriterOne = w1
    WriterTwo = w2
    TokenFamilies = {"ETag"}
    MaxAttempts = 2
    MaxWrites = 2
    MaxEnvelope = 6
    MaxToken = 2
    MaxMetaVersion = 4
    MaxFence = 2
    Defects = {"publish_before_precommit"}
INVARIANTS
    TypeOK
    PublicationRequiresDurablePrecommit
CHECK_DEADLOCK FALSE
