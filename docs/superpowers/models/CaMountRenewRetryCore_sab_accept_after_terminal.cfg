\* Sabotage: a late exact body is accepted after local terminalization. EXPECTED: `TerminalNeverRearmsAuthority` violated.
SPECIFICATION Spec
CONSTANTS
    AttemptIds = {attempt1, attempt2}
    CurrentAttempt = attempt1
    OtherAttempt = attempt2
    Holders = {self, foreign}
    SelfHolder = self
    ForeignHolder = foreign
    PredecessorToken = predecessor
    RenewalToken = renewal
    TwinToken = twin
    SuccessorToken = successor
    ForeignToken = foreignToken
    InitialConfirmedDeadline = 3
    MaxTime = 4
    LeaseDuration = 4
    RequestTimeout = 1
    CadencePeriod = 2
    MaxRetries = 1
    MaxOutstanding = 2
    Defects = {"accept_after_terminal"}
INVARIANTS TypeOK TerminalNeverRearmsAuthority
CHECK_DEADLOCK FALSE
