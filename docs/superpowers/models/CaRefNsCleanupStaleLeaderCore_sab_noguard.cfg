\* Sabotage: drop the round/marker/epoch straggler guards (the pre-fix live-LIST + exact-token delete).
\* A leader that stalls past a successor's Completed+marker and the writer's recreation then reclaims the
\* recreated manifest and verbatim file. Must violate NoRecreatedDataDeleted. RED.
SPECIFICATION Spec
CONSTANTS
    MaxRound = 3
    SabotageNoStragglerGuard = TRUE
INVARIANT TypeOK
INVARIANT NoRecreatedDataDeleted
