\* Defect: initial `Condemned` selects an ambiguous retagged PUT, retry `HEAD` observes a miss,
\* and the original staged envelope is then copied despite an earlier publication attempt.
\* EXPECTED: `VerbatimCopyOnlyFirstAbsent` violated.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    WriterOne = w1
    WriterTwo = w2
    TokenFamilies = {"ETag"}
    MaxAttempts = 2
    MaxWrites = 2
    MaxEnvelope = 6
    MaxToken = 2
    MaxMetaVersion = 4
    MaxFence = 2
    Defects = {"first_condemned_then_copy"}
INVARIANTS
    TypeOK
    VerbatimCopyOnlyFirstAbsent
CHECK_DEADLOCK FALSE
