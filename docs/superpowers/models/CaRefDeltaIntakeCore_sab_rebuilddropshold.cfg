\* Sabotage: THE r8 BLOCKER. A forced REBUILD rebuilds cursors from catalog + `_ckpt` + arithmetic
\* tails and forgets the holds. Spec §7 requires it to carry every hold verbatim.
\*
\* Trace (verified, 21 states): round 1's hint returns the witness and the namespace is HELD; the
\* rebuild then drops the hold (leaving a debt) and, being its own operation, discards that round's
\* destructive authorization; round 2 starts clean, its hint no longer mentions the namespace, the
\* same 404 reads as an honest frontier, and destruction proceeds ONE ROUND LATER over a gap nothing
\* records any more. Must be RED on HoldSuppresses. Control: `_ctl_holdsuppresses`.
SPECIFICATION Spec
CONSTANTS
    T1 = t1
    T2 = t2
    MaxSeq = 3
    EdgeOf <- EdgeRoles
    HintSilent = FALSE
    HintComplete = FALSE
    CkptWitness = FALSE
    EnableHiddenHole = TRUE
    SabotageSkipQuietProbe = FALSE
    SabotageRebuildDropsHold = TRUE
    SabotageClearHoldOnAbsent = FALSE
    SabotageDestroyUnderHold = FALSE
    SabotageDeleteIgnoresIndeg = FALSE
    SabotageAdoptBeforeCommit = FALSE
    SabotageCleanupIgnoresCursor = FALSE
    PlanOnly = FALSE
    SabotageAdapterMints = FALSE
INVARIANT TypeOK
INVARIANT HoldSuppresses
