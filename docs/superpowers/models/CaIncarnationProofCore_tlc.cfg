\* TLC cross-validation of the trimmed proof core against the main model's reval_stage2 bounds.
\* TLC model values for the identity sets (Apalache uses CInit's literal form instead).
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    Shards = {s1}
    Hashes = {h1}
    MaxToken = 3
    MaxRound = 2
    MaxLog = 4
CONSTRAINT StateConstraint
\* IndInv v1 conjuncts — each on its own line so a failure names the culprit (Task 4 pre-filter).
INVARIANT TypeBounds
INVARIANT NoDangle
INVARIANT NoReturn
INVARIANT InflightAllocated
INVARIANT PhaseRoundActive
INVARIANT AbsentObsolete
INVARIANT RootEdgesTyped
INVARIANT InflightHeld
INVARIANT RetiredCurrentOrDead
INVARIANT InflightVsRefs
INVARIANT SendImpliesFenced
INVARIANT RefsFromLog
INVARIANT SnapFromPrefix
