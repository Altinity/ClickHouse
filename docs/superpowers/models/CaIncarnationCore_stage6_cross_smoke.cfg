\* Stage 6c (B91 review follow-up): registry x evidence-staleness CROSS SMOKE. Not a proof of the
\* combined space (the full combination is a recorded residual, B104) - a small-bounds interaction
\* check: reg + wEv UNCHANGED discipline, PubFloor + EvOK composing in one gate, manifest minting
\* under a tree publish whose children ride evidence deps.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1}
    Leaders = {L1}
    Shards = {s1}
    Hashes = {t1, h1}
    TreeHashes = {t1}
    MaxToken = 2
    MaxRound = 2
    MaxLog = 4
    EnableResurrect = TRUE
    EnableTrees = TRUE
    EnableDebris = FALSE
    EnableSplit = FALSE
    EnableOverwrite = FALSE
    SabotageNoFence = FALSE
    SabotageNoRecheckFold = FALSE
    SabotageNoRetireView = FALSE
    SabotageUncondDelete = FALSE
    SabotageReusedTag = FALSE
    SabotageCascadeRace = FALSE
    SabotageCutOverclaim = FALSE
    EnableReval = TRUE
    SabotageNoReobserve = FALSE
    EnableRegistry = TRUE
    EnableEvStale = TRUE
    SabotageNoRegistry = FALSE
    SabotageFoldTimeUniverse = FALSE
    SabotageNoEvReobserve = FALSE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_NO_DANGLE
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_RETURN
INVARIANT INV_JOURNAL_COVERAGE
INVARIANT INV_MAN_EXISTS
PROPERTY MonotoneGC
PROPERTY MonotoneRegistry
