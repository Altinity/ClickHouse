\* Non-vacuity witness, not a sabotage verdict: proves the hidden-hole shape itself is reachable —
\* an enumeration that ran to the END of the key space still omitted a log that is PRESENT, so the
\* hint-folding reader installs a state missing a record nothing ever deleted. This is the observed
\* 0x1430c/0x1430d defect (reports/2026-07-26-list-incompleteness-investigation.md).
\* EXPECTED TO VIOLATE W_NO_HINT_HOLE; the violation IS the evidence. Needed because
\* _sab_scanistruth's shortest counterexample is the weaker "the hint reader has no vanish
\* detection" shape, which would leave the hole shape unproven.
SPECIFICATION Spec
CONSTANTS
    MaxSeq = 4
    MaxRestarts = 2
    LatePred = FALSE
    SabotageReuseAfterAmbiguous = FALSE
    SabotageGapOnFail = FALSE
    SabotageNoSeal = FALSE
    SabotageBlindPut = FALSE
    SabotageScanIsTruth = TRUE
    SabotageCleanupAboveCkpt = FALSE
    SabotageStaleCkptCorruption = FALSE
    SabotageSealClobbersBase = FALSE
INVARIANTS TypeOK W_NO_HINT_HOLE
