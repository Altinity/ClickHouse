\* FAITHFUL B140-dangle hunt with a SELECTIVELY-SHARED blob (Job A's real producer shape):
\* Trees={t1,t2}, Blobs={b1,b2}; one blob (b1) is shared by BOTH trees, the other (b2) is private
\* to the FullTree. Faithful strip (clears marker), byte-equal probe-upward adoption, the
\* displaced_later FILE_DOESNT_EXIST expansion-skip, relink with bottom-up child presence.
\* Question: is INV_NO_LOSS / INV_NO_GC_LOSS / INV_MARKER_EDGES violated by a FAITHFUL trace?
SPECIFICATION Spec
CONSTANTS
    Leaders = {L1, L2}
    Trees = {t1, t2}
    Blobs = {b1, b2}
    MaxGen = 4
    MaxLog = 5
    EnableTreeGone = TRUE
    EnableRelink = TRUE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_GC_LOSS
INVARIANT INV_MARKER_EDGES
