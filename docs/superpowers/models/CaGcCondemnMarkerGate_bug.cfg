\* Pre-fix model: graduation is unconditional (the condemn marker is fire-and-forget).
\* EXPECTED: NoDangle VIOLATED — the §3.4 interleaving (swallowed marker, writer adopts
\* the same token in the [cut, redelete] window, exact-token delete fires under a live
\* committed edge).
SPECIFICATION Spec
CONSTANTS
  GateOnMarker = FALSE
  MaxGen = 3
  MaxRounds = 6
INVARIANTS
  TypeOK
  NoDangle
