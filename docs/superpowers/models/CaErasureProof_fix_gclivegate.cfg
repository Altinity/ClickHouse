\* Candidate fix, mechanically validated: scheduler rounds AND heartbeat
\* pulses refuse unless the pool lifecycle is Live (the refusal the manual
\* GC entry points already make via checkOpAdmitted(Admin)). MUST HOLD.
SPECIFICATION Spec
CONSTANTS
  EnableGc = TRUE
  GraceOn = TRUE
  GcStopsOnIntent = TRUE
  EnableHbOutsideRound = TRUE
  GcLiveGate = TRUE
  MaxGen = 4
INVARIANT TypeOK
INVARIANT TruthEmpty
CHECK_DEADLOCK FALSE
