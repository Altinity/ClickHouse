\* Defect: one source publishes logical content that does not match the content-addressed key.
\* EXPECTED: `KeyNamesPayload` violated.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    WriterOne = w1
    WriterTwo = w2
    TokenFamilies = {"ETag"}
    MaxAttempts = 2
    MaxWrites = 2
    MaxEnvelope = 6
    MaxToken = 2
    MaxMetaVersion = 4
    MaxFence = 2
    Defects = {"wrong_payload"}
INVARIANTS
    TypeOK
    KeyNamesPayload
CHECK_DEADLOCK FALSE
