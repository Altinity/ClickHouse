\* Phase-1: BUGGY ungated trim. Expect INV_NO_LOSS / INV_NO_GC_LOSS VIOLATED (the dangle reproduces).
SPECIFICATION Spec
CONSTANTS
    Leaders = {L1, L2}
    Trees = {t1, t2}
    Blobs = {b1}
    MaxGen = 4
    MaxLog = 4
    TrimGated = FALSE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_GC_LOSS
