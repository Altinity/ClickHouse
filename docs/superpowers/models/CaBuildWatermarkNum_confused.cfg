\* SABOTAGE: guard checks the WRONG server's watermark -> a blob protected by its OWN server's
\* watermark gets condemned -> Inv_ProtectedNeverCondemned VIOLATED. Proves per-server scoping.
SPECIFICATION Spec
CONSTANTS
  Servers = {s1, s2}
  NoneServer = NoneServer
  MaxSeq = 2
  MaxEpoch = 1
  ConfusedGuard = TRUE
  MonotonicSeq = TRUE
INVARIANT TypeOK
INVARIANT Inv_ProtectedNeverCondemned
INVARIANT Inv_NoDangle
CHECK_DEADLOCK FALSE
