\* Defect: response loss is treated as materialization without recovery `HEAD`.
\* EXPECTED: `ReadyRequiresObservedMaterialization` violated.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    WriterOne = w1
    WriterTwo = w2
    TokenFamilies = {"ETag"}
    MaxAttempts = 2
    MaxWrites = 2
    MaxEnvelope = 6
    MaxToken = 2
    MaxMetaVersion = 4
    MaxFence = 2
    Defects = {"ready_without_reobserve"}
INVARIANTS
    TypeOK
    ReadyRequiresObservedMaterialization
CHECK_DEADLOCK FALSE
