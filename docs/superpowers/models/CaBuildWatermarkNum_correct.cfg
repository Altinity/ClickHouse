\* Correct per-server guard: a genuinely protected blob is never condemned; no dangle. SAFETY HOLDS.
SPECIFICATION Spec
CONSTANTS
  Servers = {s1, s2}
  NoneServer = NoneServer
  MaxSeq = 2
  MaxEpoch = 1
  ConfusedGuard = FALSE
  MonotonicSeq = TRUE
INVARIANT TypeOK
INVARIANT Inv_ProtectedNeverCondemned
INVARIANT Inv_NoDangle
CHECK_DEADLOCK FALSE
