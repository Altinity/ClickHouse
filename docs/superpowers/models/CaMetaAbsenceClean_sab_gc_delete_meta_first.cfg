\* §5 gate (sabotage): GC delete flips to meta-BEFORE-body. Absence lands while the body
\* (still at the condemn-time token, still queued for exact-token delete) is dying.
\* MUST violate INV_ABSENCE_NO_QUEUED_DELETE.
SPECIFICATION Spec
CONSTANTS
    MaxGen = 8
    SabResurrectMetaFirst = FALSE
    SabGcDeleteMetaFirst = TRUE
    SabAdoptOverTombstone = FALSE
    SabCreateReadsMeta = FALSE
INVARIANT TypeOK
INVARIANT INV_ABSENCE_NO_QUEUED_DELETE
INVARIANT INV_NO_DANGLE
INVARIANT INV_NO_LOSS
INVARIANT INV_META_BODY
