\* As-built GC: heartbeat pulses outside rounds; the scheduler does NOT
\* exit on the terminal transition (no lifecycle check in loop()).
\* EXPECTED: VIOLATES TruthEmpty (hb pulse in the LIST..qual window;
\* post-promotion round/hb writes recreate control keys).
SPECIFICATION Spec
CONSTANTS
  EnableGc = TRUE
  GraceOn = TRUE
  GcStopsOnIntent = FALSE
  EnableHbOutsideRound = TRUE
  GcLiveGate = FALSE
  MaxGen = 4
INVARIANT TypeOK
INVARIANT TruthEmpty
CHECK_DEADLOCK FALSE
