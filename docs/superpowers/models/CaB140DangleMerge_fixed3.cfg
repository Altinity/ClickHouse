\* Phase-2: FIXED trim-gate (trim only up to committed gcState.cursor). Expect CLEAN.
SPECIFICATION Spec
CONSTANTS
    Leaders = {L1, L2}
    Trees = {t1, t2}
    Blobs = {b1}
    MaxGen = 3
    MaxLog = 3
    TrimGated = TRUE
CONSTRAINT StateConstraint
INVARIANT TypeOK
INVARIANT INV_NO_LOSS
INVARIANT INV_NO_GC_LOSS
