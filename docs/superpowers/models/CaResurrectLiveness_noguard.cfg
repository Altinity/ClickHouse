\* Guard OFF (today's incremental GC): GC re-condemns + deletes even the build's OWN fresh incarnation
\* in every upload->publish window -> Liveness VIOLATED. Proves writer-side re-upload ALONE is starvable
\* and the heartbeat guard is load-bearing (body-in-hand, NOT the bodyless GET path).
SPECIFICATION Spec
CONSTANTS HeartbeatGuard = FALSE
INVARIANT TypeOK
PROPERTY Liveness
CHECK_DEADLOCK FALSE
