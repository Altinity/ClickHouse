\* Defect: an old-token delete lands but remains queued, retry observes absence, and the same
\* staged ETag is copied again for a later exact-delete retry to remove.
\* EXPECTED: `ExactDeleteCannotRemoveFreshIncarnation` violated.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    WriterOne = w1
    WriterTwo = w2
    TokenFamilies = {"ETag"}
    MaxAttempts = 2
    MaxWrites = 2
    MaxEnvelope = 6
    MaxToken = 2
    MaxMetaVersion = 4
    MaxFence = 2
    Defects = {"recopy_after_absent"}
INVARIANTS
    TypeOK
    ExactDeleteCannotRemoveFreshIncarnation
CHECK_DEADLOCK FALSE
