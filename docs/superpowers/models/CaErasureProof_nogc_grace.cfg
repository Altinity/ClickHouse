\* Writers only, grace ON: the writer-side proof machinery
\* (op-gate Live admission + guard counter + LIST-reset + grace) MUST HOLD.
SPECIFICATION Spec
CONSTANTS
  EnableGc = FALSE
  GraceOn = TRUE
  GcStopsOnIntent = TRUE
  EnableHbOutsideRound = FALSE
  GcLiveGate = FALSE
  MaxGen = 4
INVARIANT TypeOK
INVARIANT TruthEmpty
CHECK_DEADLOCK FALSE
