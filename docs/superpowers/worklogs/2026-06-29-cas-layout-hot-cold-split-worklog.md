# Work log — CA layout hot/cold split (server_root_id identity, mount safety, relocation, sweep, ordinal)

Spec: `docs/superpowers/specs/2026-06-28-cas-layout-hot-cold-split-design.md` (rev5, approved).
Branch: `cas-layout-hot-cold-split` (off `cas-gc-part-manifest-impl` @ `a0c21fe`).
Mode: **fully unattended**, subagent-driven TDD, **codex CLI (`codex exec --sandbox read-only`, gpt-5.5) as reviewer** (fall back to self/subagent review if codex is unusable). Don't stop, don't ask.

## Ultimate goal (user directive 2026-06-29)
1. Deliver the FULL spec via the ritual: per-phase plan → review → subagent-driven TDD impl → final review. Phases: 0 (mount safety — plan done) → 1 (relocation) → 2 (cursor sweep) → 3 (writer_epoch rename + manifest_ordinal).
2. Backlog grooming: read `docs/superpowers/deferred_backlog/`, mark done/resolved/obsolete by last-changed, move to archive; partial → keep only the remainder, archive the rest. No noise.
3. Run `utils/ca-soak/scenarios/`, collect observations; obvious problems → fix, non-obvious → backlog.
4. Start the 4h soak.

## Environment
- codex CLI `codex-cli 0.142.2`, `codex exec --sandbox read-only "<prompt>"` confirmed headless (rc=0).
- deferred_backlog: `cas-gc-redesign-backlog.md` (41K, 2026-06-27), `cas-mergetree-integration.md` (207K, 2026-06-26), `cas-mergetree-integration-archive.md` (352K, 2026-06-26).
- Phase-0 plan: `docs/superpowers/plans/2026-06-29-cas-layout-phase0-mount-safety.md` (8 tasks; Task 1 = TLA+ CaCasMountCore gate).

## Timeline

### 2026-06-29 — kickoff
- Created branch `cas-layout-hot-cold-split`. Verified codex headless. Worklog started.
- Starting **Phase 0** subagent-driven: Task 1 (TLA+ gate) → Tasks 2-8 (TDD, codex review between).
- **Phase 0 Task 1 (TLA+ gate CaCasMountCore) — DONE + verified + committed `e843bc1`.** stage1 green (6085 distinct, 5 invariants); 3 sabotages counterexample their named invariant; reclaim witness reachable. Controller re-ran stage1 (6085) + foreigntakeover (violates) to verify. `WriterEpochMonotoneUnique` strengthened to a monotone-ceiling (single sticky owner makes the distinct-actor clause trivial — the ceiling is what epoch-reset must break). Codex reserved for the C++ tasks (TLC is the oracle here).
- Phase-0 code unblocked. Dispatching Task 2 (required server_root_id config + validateServerRootId + PoolConfig field + thread-through + ServerUUID demotion).
- **Phase 0 Task 2 — DONE (`1e15753`) but codex review found a BLOCKER + a leak:**
  - `validateServerRootId` + PoolConfig field + required-config thread-through: codex confirms SOUND (rule complete, ServerUUID unchanged/only-added, no Store::open scope creep, ctor order fine). New gtest `CasServerRootId.*` passes. Pre-existing `CaWiringOps.FreezeViaHardLinksIntoShadow` failure re-confirmed unrelated (stash+rebuild baseline still fails).
  - **BLOCKER (codex):** making `server_root_id` required (no default) breaks every TRACKED `content_addressed` disk config lacking it — 5 integration `storage_conf.xml`, 2 `tests/config/config.d/content_addressed*.xml`, ~15 inline-disk `.sql` stateless tests → fail on server start / CREATE TABLE. The gtest-only run missed it. → **resumed the Task-2 implementer to add `server_root_id` to ALL tracked content_addressed disk defs** (XML child + inline SQL arg, clean ids, distinct per replica if a pool is shared).
  - **Config Blocker FIXED `84d93c2`** (37 files, config-only): all tracked content_addressed disk defs carry `server_root_id` (stateless policies, single-server integration, 24 inline-SQL defs; 3 shared-pool integration tests via per-instance `server_root_id_node{1,2}.xml` overrides since CH does not macro-expand disk-config values). Controller-verified: no def lacks the key; inline-SQL counts match.
  - **BACKLOG (non-obvious, for grooming):** the 3 shared-pool integration tests (`test_cas_replicated_relink`, `test_cas_gc_sharded`, `test_content_addressed_shared_pool`) modeled the OLD `content_addressed_allow_shared_pool` SAME-tree mode; Phase-0 mount-safety forbids two servers owning one `server_root_id` subtree (spec's "shared blobs, per-server trees"). Distinct node1/node2 ids keep them booting but change what they verify (e.g. relink's fetch-by-relink premise assumes one shared tree). → reconcile/rewrite these tests for the per-server-tree model (or formally retire `allow_shared_pool`). Add to backlog.
- **Phase 0 Task 5 (MountLeaseKeeper + claimMount) — DONE + self-reviewed/APPROVED `4d35c82`.** 8/8 tests. claimMount five branches verified (foreign→no-write; same-uuid+same-epoch→adopt; diff-epoch live→LiveDoubleStart-no-write / expired→reclaim; race→fail-closed via token-checked putOverwrite). claim hook adopts same-(uuid,epoch), throws on foreign/diff-epoch (no lost-update). Minor non-issues: seq resets to 1 on adopt + a redundant 2nd startup PUT — safety uses uuid+epoch+expiry not seq, epoch unique per root, so benign. `terminate()` wired by Task 7. enum unscoped (matches plan's test code).
- **Phase 0 Task 8 (read-only + error text) — DONE + APPROVED `a14870`.** Verification + 2 locking tests (read-only foreign-owned pool opens without mutation, byte-equal control objects; LiveDoubleStart error text has all substrings + 3 remediation lines). No src change needed (Task 7 covered both). Full `Cas*:Ca*` **408/409** (only pre-existing CaWiringOps).
- **★ PHASE 0 COMPLETE ★** — 11 commits `e843bc1`(TLA+ gate)→`1e15753`→`84d93c2`→`09b0e65`→`517ccd5`→`596bfc3`→`4d35c82`→`508d2dab`→`f4ed456`→`b8ae6eac`→`a14870`. Store::open startup order verified exactly per spec (validate→claimOwner→allocateWriterEpoch+bridge→claimMount→keeper.start[setFenceCallbacks-first]+armMountFence→gated startBackground→Watermark(writer_epoch)). 5 review fix-forwards caught (config blocker, epoch-reserve-0, startBackground gate; + 2 backlog/delivery items: gtest_cas_build leak, shared-pool-tests semantic conflict). TLA+ gate green; suite 408/409.
- **Phase 0 Task 7 — fix applied + APPROVED `b8ae6eac`** (startBackground gated behind background_watermark; 11 mount + 119 GC/Build green). Dispatching Task 8 (read-only + error text) to finish Phase 0.

## Phase 1 — re-rooted relocation (cas/refs + cas/manifests)
Ritual: writing-plans (Phase-1 plan) → self-review → subagent-driven TDD → final review. Builds on Phase 0 (server_root_id-rooted namespaces; `casRefsServerPrefix`/`casManifestsServerPrefix`/`serverRootDataPrefix` helpers already added in Task 4). Gathering Phase-1 code surface (key derivations, discovery LIST, fsck, namespace derivation) then writing the plan.
- **Phase 1 Task 1 — DONE + committed `f5f96dce01a`.** `rootShardKey` moved to `cas/refs/<ns>/<shard>`, `manifestKey` moved to `cas/manifests/<ns>/...`, discovery LIST/strip retargeted to `casRefsPrefix()`, sweep/fsck manifest enumeration retargeted to `manifestNamespacePrefix(ns)`. Identity untouched. Verification: Task-1 targeted suite green except known pre-existing `CaWiringOps.FreezeViaHardLinksIntoShadow`.
- **Phase 1 Task 2 — DONE + committed `a4b8185f0fa`.** `liveNamespace` / live-tree browse prefix switched from UUID-derived `serverPrefix` to configured `server_root_id`; self-review expanded the task to loose mountpoint objects too (`writeFile`, `moveFile`, `unlinkFile`, `existsFile`, `getFileSize`, `getStorageObjects`) so all ordinary mirrored/verbatim files live under `roots/<server_root_id>/...`. Added wiring assertion that `liveNamespace("uuid-1")` starts with `test/`. Verification: build `unit_tests_dbms` green; targeted `CaWiring*:CaTransaction*:CasLayout.*:-CaWiringOps.FreezeViaHardLinksIntoShadow` 58/58; broad `Cas*:Ca*` 409/410 with only known `CaWiringOps.FreezeViaHardLinksIntoShadow`.
- **Phase 1 Task 3 — DONE + committed `b15f1ef9d28`.** `ObjectStorageBackend::list` now uses lazy `IObjectStorage::iterate` in native mode instead of `listObjects(..., max_keys=0)` full materialization. Backend pagination cursor contract was made explicit and uniform: `next_cursor` is the last returned key and resume is exclusive; `InMemoryBackend` and backend contract tests were updated to match. Emulated-single-process keeps the old materialize/sort path for stable local test ordering, but uses the same exclusive cursor contract. Verification: build `unit_tests_dbms` green; targeted `CasBackendContract*:CasBackend*:CasObjectStorageBackend*:CasBackendListTokens*:CasGc*:CasLayout.*:CasOrphan*:CasFsck*:CasRetireView.*` 126/126; broad `Cas*:Ca*` 409/410 with only known `CaWiringOps.FreezeViaHardLinksIntoShadow`.
- **Phase 1 COMPLETE.** Commits: `f5f96dce01a` (relocate refs/manifests), `a4b8185f0fa` (live namespace + mirrored files use `server_root_id`), `b15f1ef9d28` (backend list pagination). Remaining known unrelated state: user/uncommitted `CasBuild.cpp/.h`; pre-existing `CaWiringOps.FreezeViaHardLinksIntoShadow` failure.

## Phase 2 — cursor-paced bounded orphan-manifest sweep
- **Phase 2 plan — DONE + committed `7cf47362ae9`.** Plan file: `docs/superpowers/plans/2026-06-29-cas-layout-phase2-cursor-sweep.md`.
- **Phase 2 Task 1 — DONE + committed `885520bf295`.** Added non-load-bearing `GcState.manifest_sweep_cursor`; added `PoolConfig.manifest_sweep_list_budget_keys` / `manifest_sweep_delete_budget_keys`; threaded optional disk config through `MetadataStorageFactory` → `ContentAddressedMetadataStorage` → `Store`. Verification: build `unit_tests_dbms` green; targeted `CasGcFormats*:CasStore*:CaWiring*:-CaWiringOps.FreezeViaHardLinksIntoShadow` 86/86; broad `Cas*:Ca*` 411/412 with only known `CaWiringOps.FreezeViaHardLinksIntoShadow`.
- **Phase 2 Task 2 — DONE + committed `737c1d9b43e`.** Added `sweepManifestCursorPage` over pool-wide `cas/manifests/`; added `Layout::casManifestsPrefix`; made sweep/fsck eligibility resolve watermark by namespace → `server_root_id` → `serverRootWatermarkKey`, with writer-epoch comparison. The cursor page does one bounded LIST, skips malformed keys, caches eligibility per `(namespace, writer, build)` and active owner sets per namespace, and deletes only by listed token or `HEAD` + `deleteExact`. Verification: build `unit_tests_dbms` green; targeted `CasOrphanManifestSweep*:CasFsck*:CasBackendContract*:CasBackend*:CasObjectStorageBackend*:CasBackendListTokens*` 17/17.
- **Phase 2 Task 3 — DONE + committed `49a62a0fb1f`.** Replaced the production `pickOneSweepTarget` / `sweepNamespace` tail in `Gc::runRegularRound` with `runManifestSweepCursorPass`, using `PoolConfig.manifest_sweep_*` budgets and a best-effort post-round `gc/state` CAS that updates only `manifest_sweep_cursor`. A lost CAS discards cursor progress and does not fail the completed reachability round. Added a round-level test proving bounded deletion + cursor persist/wrap over two rounds. Verification: build `unit_tests_dbms` green; targeted `CasGc*:CasOrphanManifestSweep*:CasRetireView*:CasFsck*` 115/115; broad `Cas*:Ca*` 415/416 with only known `CaWiringOps.FreezeViaHardLinksIntoShadow`.
- **Phase 2 Task 4 — DONE + committed `d0e52549d06`.** Removed the obsolete production picker API (`pickOneSweepTarget` / `SweepTarget`) and its registry-per-namespace scan. Kept `sweepNamespace` as a narrow liveness-predicate test seam. Replaced the old picker pagination regression with a cursor-page advance/wrap test. Verification: build `unit_tests_dbms` green; targeted `CasOrphanManifestSweep*:CasGc*:CasLayout.*` green.
- **Phase 2 COMPLETE.** Commits: `7cf47362ae9` (plan), `885520bf295` (format/config), `737c1d9b43e` (cursor page API), `49a62a0fb1f` (GC tail integration), `d0e52549d06` (remove legacy picker).

## Phase 3 — writer_epoch + manifest_ordinal
- **Phase 3 plan — DONE + committed `edf6e663890`.** Plan file: `docs/superpowers/plans/2026-06-29-cas-layout-phase3-manifest-identity.md`.
- **Phase 3 implementation — BLOCKED on dirty user-owned `CasBuild.cpp/.h`.** The identity reshape necessarily changes `Build::stageManifest` and `Build` fields, but those files already have unrelated uncommitted changes (102-line diff in `.cpp`, 9-line diff in `.h`) that this run must not touch/stage. Resume Phase 3 after that work is committed or explicitly handed over.

- **Phase 0 Task 7 (Store::open mount-safety startup) — DONE `f4ed456`, 1 fix → resumed.** Strict order wired (validate→claimOwner→allocateWriterEpoch+`process_epoch=writer_epoch` bridge→claimMount→keeper.start adopts+armMountFence→WatermarkKeeper(writer_epoch)). Keeper↔fence: added `onRenewSucceeded`/`onRenewFailed` no-op virtuals to `SingleWriterSlot` (WatermarkKeeper unaffected), MountLeaseKeeper overrides → `setMountDeadline`/`tripMountLost` callbacks (fence stays local). Store dtor `mount_keeper->stop()` retires lease (try/catch). Config `mount_lease_ttl_ms=30000`/`mount_renew_period=10000`. Full `Cas*:Ca*` **406/407** (only pre-existing CaWiringOps). Both startup tests pass (SecondServerSameRootFailsClosed; WriterEpochStrictlyIncreasesAcrossReopen via dtor-retired lease + higher durable epoch). Companion test edits: gtest_cas_store/gtest_cas_gc_token_diff distinct server_root_id for concurrent opens. **FIX (consistency/test-hygiene): `mount_keeper->startBackground` was unconditional → every test Store spawns a renewal thread; gate it behind `config.background_watermark` like WatermarkKeeper (prod renews, tests don't).** Resumed implementer.
- **Phase 0 Task 6 (local write fence) — DONE + self-reviewed/APPROVED `508d2dab`.** `MountFence{uuid,epoch,atomic<steady_clock::tp> deadline, atomic<bool> lost}` in Store; `mayMutate` (acquire loads, no S3 read), `tripMountLost` (latching), `setMountDeadline`/`armMountFence`; gate at `mutateShard` top (line 634, ABORTED) — the single ref-shard mutation chokepoint; bootstrap writes (owner/epoch/mount/watermark via their own CAS helpers) exempt. Permissive default (`deadline=max(), lost=false`) keeps everything green: GC/Build 120/120, mount/server-root 9/9. `CaWiringOps` failure re-confirmed pre-existing. Dispatching Task 7 (startup wiring).
- **CODEX UNUSABLE as reviewer in this env:** `codex exec` (gpt-5.5) timed out at 420s (xhigh) AND 360s (medium) on focused diff reviews — too slow (large-repo exploration). Per the directive ("review yourself if unresolvable"), **switching to controller self-review for all remaining tasks** (read the diff + verify the correctness-critical points directly). Documented; not retrying codex per-task.
- **Phase 0 Task 4 — fix applied + APPROVED `596bfc3`** (first allocated writer_epoch=1, reserves 0; `EXPECT_GE(e1,1)`; 5/5). Dispatching Task 5 (MountLeaseKeeper).
- **Phase 0 Task 4 (owner claim + durable-monotone writer_epoch allocator) — DONE `517ccd5`, self-review found 1 fix → resumed.** Functions correct: `claimOwnerOrThrow` (present-equal-ok / present-differ-CORRUPTED / absent-requires-empty-subtree-else-CORRUPTED / putIfAbsent race re-get+compare); `allocateWriterEpoch` (CAS-bump, retry-on-Conflict, missing-epoch-over-data → CORRUPTED); `serverRootSubtreeEmpty` (lists cas/refs+cas/manifests+roots per srid); + layout helpers `serverRootDataPrefix`/`casRefsServerPrefix`/`casManifestsServerPrefix`. 5/5 tests. **FIX (real): the allocator handed out the first writer_epoch as 0, but epoch 0 is a RESERVED sentinel (CasStore.cpp mints process_epoch nonzero "to avoid the 0/UINT64_MAX sentinels"; Task 7 feeds writer_epoch into the watermark epoch). Must reserve 0 → first allocated = 1 (also matches the TLA+ gate's first AllocEpoch=1).** Resumed implementer for the one-line fix + `EXPECT_GE(e1,1)` assertion. (Minor noted, not fixed: 100-retry terminal uses CORRUPTED_DATA vs ABORTED for contention — startup-only, fail-closed, acceptable.)
- **Phase 0 Task 3 (layout keys + Owner/ServerEpoch/MountLease protos + codecs) — DONE + reviewed/approved `09b0e65`.** 7 files; codecs mirror CasWatermark (own FormatId magic per codec, `checkCompatibility` before field reads, UInt128 BE); FormatId 16/17/18 = CAOW/CAEP/CAML (distinct; both switches); keys `gc/server-roots/<srid>/{owner,epoch,mount,watermark}`; glob picked up the new `.cpp`. 2/2 round-trip tests pass. codex review timed out (tooling) → controller self-verified the risk points (compat-ordering, per-codec magic, both switches, magic distinctness incl. ruling out the `0x4C424143` Blob-comment false-dup, key separators). Dispatching Task 4.

  - **LEAK (deferred, documented):** the FIRST commit `1e15753` swept in the user's uncommitted `gtest_cas_build.cpp` putBlob test (`PutBlobStreamsSourceOnceNoFullMaterialization`) via the mechanical `server_root_id="test"` edit; `CasBuild.cpp/.h` stay uncommitted, so `1e15753` is broken-in-isolation (the test uses the uncommitted streaming `BlobSource.write_payload`) yet green in-tree. The block is cleanly separable (lines +174..+203). **Decision: do NOT surgically un-leak mid-run** (would touch the user's uncommitted CasBuild work + risk losing the test). Reconcile at final delivery: drop the leaked test from the branch tip (and re-add to the user's uncommitted set) when the CasBuild work is committed. Future tasks must NOT touch `gtest_cas_build.cpp`.
