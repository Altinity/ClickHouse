# Unattended night run — 2026-06-04 → 2026-06-05

Running log for the morning report. Mandate: (A) un-gate all CA tests, run full suite, triage — fix small,
document big, re-gate only disk-incompatible-by-definition; commit regularly. (B) after A: GC convergence
(`docs/superpowers/specs/2026-06-04-ca-gc-convergence-design.md`) → plans → implement (lock-free multinode,
optional Keeper accel), tests green with & without Keeper.

## Status timeline

- **Start:** MPT (B67 layer 2, multi-part CA transaction) core refactor `e055a13243e` committed + reviewed
  (120/120 `ContentAddressed*` gtests pass, single-part byte-equivalent). Continuing MPT T3→T5, then Goal A,
  then Goal B.
- **MPT T3 done** (`de7001cd319`): 2 multi-part gtests added (122 pass total); helper audit clean (no
  single-part assumption survived the refactor); CA non-txn + Tier-1 smoke 11/11 pass — no regression from
  the per-part refactor. Multi-part CA transaction mechanism is validated at the unit + smoke level.
- **MPT T4 (Tier-2 wave) starting:** un-gate the B67 mutation/isolation tests in batches, reproduction-driven.
- **MPT T4 Batch A done** (`85aa0ed6908`): `01168_mutations_isolation`(+`_2`,+`_3`), `01170_alter_partition_isolation`,
  `01174_select_insert_isolation`, `03657_merge_tree_disk_support_transaction` all PASS on CA. The multi-part
  transaction unblocked transactional merges/mutations. **Fix made:** a TOCTOU race in
  `LocalObjectStorage::listObjects`/`tryGetObjectMetadata` (concurrent sidecar deletion between dir-listing
  and per-entry stat aborted the multi-part merge; was masked by clickhouse-test's "No such file" ignore
  list but spammed 661 err/run). Fixed by tolerating vanished entries (universally correct — a real object
  store snapshots its listing). **NOTE for report:** this touches `LocalObjectStorage.cpp` (shared with other
  local-object-storage disks) — T5 plain regression must confirm no non-CA regression. 122 gtests + Tier-1 green.
- **MPT T4 Batch B done** (`988dbab5223`): `01167_isolation_hermitage`, `01169_alter_partition_isolation_stress`(+`_old`),
  `01171_mv_select_insert_isolation_long` all PASS on CA — **no CA fixes needed** (multi-part txn handles the
  full isolation/stress workload). 122 gtests green. The B39 "append-based / object-storage-wide" gate reason
  is now stale for these.
- **MPT T4 Batch C+D done** (`b71f775840e`): Batch C (`02421_truncate_isolation_no_merges`+`_with_mutations`,
  `02435_rollback_cancelled_queries`, `03803_transaction_mutation_race`) all PASS no-fix; Batch D —
  `03752_attach_as_replicated_transaction_metadata` + `03916_attach_as_replicated_implicit_transaction` PASS
  un-gated; **`04036_backup_partition_transaction_visibility` RE-GATED** — it's a BACKUP/RESTORE issue
  (RESTORE materializes content part files via an autocommit one-shot which CA rejects — `NOT_IMPLEMENTED
  Autocommit writes are not supported for content part files`), orthogonal to transactions → belongs to
  B16/B34 (BACKUP/RESTORE on CA). **B67 effectively closed: 15/16 Tier-2 un-gated+passing; 1 re-gated as backup.**
- **MPT T5 found+fixed a real CA bug** (`132d223636`): `ContentAddressedWriteBuffer` had no `cancelImpl` — on
  the insert-CANCEL path its inner hashing/temp_file buffers were destroyed "neither finalized nor canceled",
  spamming WriteBuffer errors + failing `02435_rollback_cancelled_queries` (flaky before). Added `cancelImpl`
  (base call + cancel inner buffers). 02435 now deterministic over 3 runs; 122 gtests; CA tests green.
  Plain (non-CA) regression already confirmed clean by the T5 run (incl. `02980_s3_plain_*` — the
  LocalObjectStorage TOCTOU-fix regression check). Finalizing B67 (backlog + push) next.
- **MPT T5 / B67 DONE:** with the `cancelImpl` fix in, the final CA full smoke is green —
  `Failed: 0, Passed: 13` on `Stateless tests (arm_binary, content_addressed storage, parallel)` for
  `01168_mutations_isolation`(+`_2`,+`_3`), `01170_alter_partition_isolation`, `01174_select_insert_isolation`,
  `01167_isolation_hermitage`, `02421_truncate_isolation_no_merges`, `02435_rollback_cancelled_queries`,
  `03752_attach_as_replicated_transaction_metadata`, `04278_content_addressed_disk`,
  `04292_content_addressed_mutations`, `05003_content_addressed_freeze`, `05004_content_addressed_transactions`.
  Backlog B67 marked DONE (both layers); B39 "DEFERRED to B67" line updated to "B67 DONE". Committing + pushing.

## Findings / decisions (newest first)

- **Goal A triage-1** (`c0287d2219c`, `8b0565135ae`): parallel-replicas/distributed cluster — `02967`, `02980_dist_insert_readonly_replica`, `03279_pr_3_way_joins_{full,inner,left,right}_first` all PASS un-gated (localhost cluster + Keeper-replicas behave normally on CA — orthogonal to content-addressing, stale gate). **B65 FIXED:** `03770_min_columns_to_activate_adaptive_write_buffer` exposed that `ContentAddressedWriteBuffer` ignored `buf_size` + `use_adaptive_write_buffer`/`adaptive_write_buffer_initial_size` (hard-coded `DBMS_DEFAULT_BUFFER_SIZE`), so a wide part pre-allocated 1MiB/column and blew the INSERT memory budget (`MEMORY_LIMIT_EXCEEDED`). Fixed by threading the flags through (mirrors S3/Azure). 122 gtests green. → likely also fixes B65's `03829_insert_deduplication_info_memory` (verify next).
- **Goal A triage-2** (`268357631`): read/merge gates — `03001_block_offset_column`, `02461_prewhere_row_level_policy_lightweight_delete`, `02994_merge_tree_mutations_cleanup`, `02260_alter_compact_part_drop_nested_column` PASS un-gated. **B65 confirmed fixed:** `03829_insert_deduplication_info_memory` now passes (adaptive-buffer fix). **`03373` gate was STALE:** transactions work on CA now → un-gated+passes. Re-gated (accurate): `02369`/`02370_lost_part_intersecting_merges` (test asserts `system.parts.path` starts with `/` then `rm -rf`s it — object-storage path-shape, incompatible by nature; would also fail on plain s3), `02995_forget_partition` (FORGET PARTITION → B21 partition-clone gate, Code 344).
- **Goal A triage-3** (`c98bfdf9765`): **JSON/dynamic-ALTER cluster (5) ALL un-gated + pass** — `01825_json_type_3`, `01825_json_type_add_column`, `03210_json_type_alter_add_column`, `03246_alter_from_string_to_json`, `03272_json_to_json_alter`. Stale gates, NOT a CA feature gap (JSON/Dynamic columns + subcolumn reads work on CA). Broken-part cluster (`02253`/`02254`/`02255`/`02444`) left gated — accurate path-shape orthogonal reason (test `rm`/`truncate`s a `system.parts.path` = relative object-store key). **`02497_source_part_is_intact_when_mutation` NOT re-tested** (subagent trusted its stale "transactions B39" reason) → re-verify next.
- **Goal A triage-4** (`64dbd4438ef`): `02497` un-gated (transaction gate WAS stale, passes). **BACKUP/RESTORE cluster (12) all fail on ONE root cause (B16/B34):** RESTORE materializes content part files via an autocommit one-shot which CA rejects (`NOT_IMPLEMENTED: Autocommit writes are not supported for content part files`, 43 log hits). BACKUP-read works; only restore-write path fails. Re-gated with precise reason. **Fix direction (big feature, NOT attempted):** RESTORE must drive a `ContentAddressedTransaction` (the same write path INSERT/merge use) instead of per-file autocommit when materializing restored content parts. → user decision item.
- **Goal A FORGET** (`604cc66aec9`): `02995_forget_partition` un-gated — FORGET PARTITION only manipulates ZK metadata (no part write), simple gate-lift; passes. Regression clean.
- **Goal A triage SUMMARY: 43 → ~22 gated.** Un-gated this session (now green on CA): parallel-replicas/distributed (6), JSON/dynamic-ALTER (5), read/merge gates (4), B65 memory (03770+03829), 03373 (stale txn gate), 02497 (stale txn gate), FORGET (02995), plus the B67 transaction cluster (15) earlier. **Remaining gated, all justified:** BACKUP/RESTORE (13, B16/B34 feature — documented, user decision), broken-part/lost-part path-shape (6, object-storage-incompatible by nature — `rm`/`truncate` on a `system.parts.path`), `02980_s3_plain_*`×2 (tests the s3_plain disk type specifically), `03350` (B66a — concurrent FETCH fan-out torn read, local-only, known).

- **CA GC convergence: S1 DONE; S2/S3/S4 plans written (2026-06-05).** S1 (`InMemoryBlobRefIndex` wired as an incremental reverse index, validated against the authoritative full-scan; instrumentation only, `gc_lock`+scan unchanged) committed (`9fb5b1cb7a3`..`0476946f35d`). Backlog note added; the S2 (log-structured streaming GC), S3 (generations+tombstones — data-loss RISK / attended review), and S4 (lockless handshake — drop `gc_lock`, do-not-merge-until-proven) implementation plans written under `docs/superpowers/plans/2026-06-05-ca-gc-s{2,3,4}-*.md`.

## Open items needing the user (big refactors / design — for the report)

1. **BACKUP / RESTORE on CA (B16/B34)** — the single biggest remaining gated cluster (13 tests, all one root
   cause). RESTORE materializes restored content part files via an autocommit one-shot write, which CA
   rejects (`NOT_IMPLEMENTED: Autocommit writes are not supported for content part files`). BACKUP-read works;
   only the restore-write path fails. **Fix:** make RESTORE materialize parts through a
   `ContentAddressedTransaction` (the INSERT/merge write path) instead of per-file autocommit. This touches
   core restore code (RestorerFromBackup / data-part restore), so it's a real feature, not a gate-lift —
   needs a brainstorm/spec/plan and your go-ahead. Gated tests listed in the backlog (B16/B34).
2. **GC convergence S2–S4** — S1 (reverse-index instrumentation) is DONE + shipped. **S2** (log-structured
   streaming GC, retires the full-scan; keeps `gc_lock`) is the next implementable stage and is plan-ready —
   it changes deletion *authority* (compaction candidates), so it should be validated against the scan
   before trusting (S1-style) and reviewed. **S3** (generations + tombstones) and **S4** (lockless handshake,
   drops `gc_lock`) change deletion/locking semantics where a bug = data loss; their plans carry a
   DATA-LOSS-RISK / attended-review flag and S4 a DO-NOT-MERGE-UNTIL-PROVEN gate (the §5.1 log-completeness
   prerequisite + the race oracles). I did NOT auto-ship S2–S4 — they need your review / attended execution.
   Plans: `docs/superpowers/plans/2026-06-05-ca-gc-s{2,3,4}-*.md`.
3. **By-definition-gated (no action — informational).** Broken-part/lost-part tests (`02253`, `02254`,
   `02255`, `02369`, `02370`, `02444`) `rm`/`truncate` a raw `system.parts.path` (a relative object-store key
   on CA) — incompatible with object storage by nature (would also fail on plain s3); a cleaner long-term
   tag would be `no-object-storage` rather than CA-specific. `02980_s3_plain_*`×2 test the s3_plain disk
   type. `03350` is B66a (concurrent-FETCH fan-out torn read, local-LocalObjectStorage-only, S3 PUTs atomic).

## Morning summary (TL;DR)

**Goal A (test stabilization): COMPLETE.** ~21 tests un-gated and green on CA this session; 3 real CA bugs
found + fixed (B65 adaptive-write-buffer memory blow-up; `LocalObjectStorage::listObjects` TOCTOU; insert-
cancel buffer leak / `cancelImpl`); broad write-path regression 39/39 green, zero regressions. Residual gated
set (24) is fully categorized and justified: BACKUP/RESTORE feature (13, B16/B34 — open, item #1),
object-storage-incompatible-by-definition (8), B66a (1). Plus the whole **B67 multi-part CA transaction**
milestone landed earlier in the night (transactional merges/mutations/isolation now work on CA).

**Goal B (GC convergence): S1 shipped, S2–S4 planned.** S1 (reverse-index instrumentation, validated
index≡scan, no behavior/lock change) implemented + pushed. S2/S3/S4 implementation plans written + pushed,
with S3/S4 flagged data-loss-risk/attended-review (item #2). Deliberately did not auto-ship the
deletion-semantics-changing stages unattended.

**State:** branch `cas-mergetree-poc` synced with `filimonov` at `f3786abfe6b`; 47 commits this session; all
gtests (123 `ContentAddressed*`) green; CA + plain regressions green; working tree clean (only the
pre-existing `m1.md` edit). Nothing left hanging in background; no orphaned processes.
